/**
 * registry_shims.ts — GENERATED, DO NOT HAND-EDIT.
 * ===================================================
 * Generated by `platform-mcp/scripts/generate_registry_shims.ts` from the registry CapabilityDescriptor modules
 * named in `registry_manifest.ts` (STRANGLER pilot set — design §19 + brief §6.2). Each
 * schema below is derived from that instrument's `input_schema` + `required_inputs`,
 * parsed statically (TypeScript AST, no import/execution — see the generator's docstring
 * for why). Run `npm run codegen:registry-shims` to regenerate; run with --check in CI.
 *
 * LANDED ALONGSIDE, NOT WIRED IN (brief §6.2 strangler discipline): these schemas are NOT
 * imported by `register_p1_ganita.ts`'s live server.tool() registrations. The existing
 * handwritten schemas (ganitaStrengthGetInputSchema / ganitaSadeSatiGetInputSchema /
 * ganitaTajakaGetInputSchema, exported from that file) remain the live contract. The
 * parity-gate test (`src/__tests__/r5_codegen_parity.test.ts`) is what proves these
 * generated schemas already agree with the live ones on the intersecting field set —
 * promotion to "live" is a separate, later, single-instrument decision.
 */
import { z } from 'zod'

/** Generated from ../../platform/src/lib/retrieval/registry/layers/L1_ganita/get_strength.ts (export getStrengthCapability), uri=marsys://tool/L1/get_strength. */
export const ganitaStrengthGetGeneratedInputSchema = {
  chart_id: z.string().uuid().describe("Chart UUID"),
  ayanamsha_id: z.string().describe("Filter by ayanamsha_id. Omit for all.").optional(),
  categories: z.array(z.string()).describe("Subset of strength categories (default: all 21).").optional(),
  graha_key: z.string().describe("Filter to one graha (e.g. \"SU\" for Sun). Omit for all.").optional(),
  offset: z.number().optional().default(0),
  limit: z.number().optional().default(500),
} as const

/** Generated from ../../platform/src/lib/retrieval/registry/layers/L1_ganita/get_sade_sati.ts (export getSadeSatiCapability), uri=marsys://tool/L1/get_sade_sati. */
export const ganitaSadeSatiGetGeneratedInputSchema = {
  chart_id: z.string().uuid().describe("Chart UUID"),
  ayanamsha_id: z.string().describe("Filter by ayanamsha. Omit for all.").optional(),
  categories: z.array(z.string()).describe("Subset of Sade Sati categories.").optional(),
  offset: z.number().optional().default(0),
  limit: z.number().optional().default(500),
} as const

/** Generated from ../../platform/src/lib/retrieval/registry/layers/L1_ganita/get_tajik.ts (export getTajikCapability), uri=marsys://tool/L1/get_tajik. */
export const ganitaTajakaGetGeneratedInputSchema = {
  chart_id: z.string().uuid().describe("Chart UUID"),
  ayanamsha_id: z.string().describe("Filter by ayanamsha. Omit for all.").optional(),
  include_varsha: z.boolean().describe("Include l1_tajik_varsha_year_lords rows (default true)").optional().default(true),
  year_min: z.number().describe("Filter varsha lords to year >= this.").optional(),
  year_max: z.number().describe("Filter varsha lords to year <= this.").optional(),
  offset: z.number().optional().default(0),
  limit: z.number().optional().default(200),
} as const

/** mcpToolName → capability URI, for reference / future promotion wiring. */
export const REGISTRY_SHIM_NAME_MAP: Record<string, string> = {
  "ganita_strength_get": "marsys://tool/L1/get_strength",
  "ganita_sade_sati_get": "marsys://tool/L1/get_sade_sati",
  "ganita_tajaka_get": "marsys://tool/L1/get_tajik",
}
