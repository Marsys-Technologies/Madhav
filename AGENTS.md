# Codex bootstrap — MARSYS-JIS

This file carries no independent project policy. Before any substantive action, read
`CLAUDE.md` in full, then follow its mandatory-reading sequence and the common
session-open protocol. The root `CLAUDE.md` is canonical; do not treat this loader
as a second source of truth.

When working in a directory that has an `AGENTS.md`, read and obey that directory's
instructions as well. Use the governed documents, not another tool's conversation,
as the shared project state.
