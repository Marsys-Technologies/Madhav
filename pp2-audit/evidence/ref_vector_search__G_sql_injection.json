{"tool":"ref_vector_search","scenario":"G_sqli_drop_table_embedding","args":{"query":"'; DROP TABLE chart_facts; SELECT * FROM users WHERE '1'='1","top_k":2},"first_attempt":"TimeoutError: The operation was aborted due to timeout (client-side MCP call timeout, ~likely embedding-service latency, not injection-related)","retry_result":"ok:true, returned 2 unrelated classical-text vector-similarity rows (nadi_navamsa_patel), query_used field echoes the payload verbatim but only as an opaque embedding input string -- no SQL executed, no error, no schema/data leak. latency_ms:5569 (consistent with a real 5s+ embedding round-trip, explains the earlier timeout as latency variance not a payload-triggered hang).","verdict":"PASS -- parameterized/embedding-based search, not vulnerable to SQL injection. The initial timeout is noted as a minor performance observation only (vector search embedding calls run 5.5s+, close to typical client timeout thresholds), not a security finding."}
