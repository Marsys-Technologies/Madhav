{
  "description": "Re-verification of decade-seam (~3652.5-day mark) contiguity for kala_field, native chart 482012f1-710e-4a25-994a-93821f5871aa, field_snapshot_id kfs_e23ba1abdf1c6fd3a1cc5c08c7538aeb",
  "reproduce_cmd_1_full_horizon_gap_scan": "WITH ordered AS (SELECT event_class, segment_index, t_start, t_end, LAG(t_end) OVER (PARTITION BY event_class ORDER BY segment_index) AS prev_t_end FROM kala_field WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa') SELECT event_class, segment_index, prev_t_end, t_start, t_end, (t_start - prev_t_end) AS gap FROM ordered WHERE prev_t_end IS NOT NULL AND t_start <> prev_t_end ORDER BY event_class, segment_index LIMIT 200;",
  "result_1": "0 rows returned -- zero segment-to-segment gaps found across all 25 event classes over the full [0, 36525] horizon",
  "reproduce_cmd_2_per_class_span": "SELECT event_class, count(*) as n_segments, min(t_start) as min_start, max(t_end) as max_end FROM kala_field WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa' GROUP BY event_class ORDER BY event_class;",
  "result_2": "All 25 event classes: n_segments=343991, min_start=0, max_end=36525 (identical for every class; 25 x 343991 = 8599775 = total kala_field row count for this chart)",
  "reproduce_cmd_3_decade_mark_straddle_check": "WITH decades AS (SELECT generate_series(0,9) AS d), marks AS (SELECT d, d * 3652.5 AS mark FROM decades) SELECT m.d, m.mark, kf.event_class, kf.segment_index, kf.t_start, kf.t_end FROM marks m JOIN kala_field kf ON kf.chart_id='482012f1-710e-4a25-994a-93821f5871aa' AND kf.t_start <= m.mark AND kf.t_end >= m.mark WHERE m.mark NOT IN (0, 36525) ORDER BY m.d, kf.event_class LIMIT 250;",
  "result_3": "For each of the 9 interior decade marks (3652.5, 7305, 10957.5, 14610, 18262.5, 21915, 25567.5, 29220, 32872.5) and each of the 25 event classes, exactly two segments were found whose combined span covers the mark with no gap: one segment ending exactly AT the mark (t_end == mark) and the next segment starting exactly AT the mark (t_start == mark). No missing coverage at any decade seam.",
  "verdict": "PASS -- the previously-fixed decade-seam defect does not reproduce for the native chart's current ka_kshetra build (field_snapshot_id kfs_e23ba1abdf1c6fd3a1cc5c08c7538aeb, built_at 2026-08-15T06:41:14.952Z)."
}
