[
  {
    "id": "G-F-59",
    "claim": "The MCP session's implicit `active_chart_id` (as reported by session_recall and used only as a human-facing convenience pointer) defaults to the second/zero-history chart (1c826d5a-41cb-4450-b4dc-59d440e5f75a, 'Abhinandan Mohanty'), not the native chart (482012f1-...). This is a real, live, reproducible observation (previously noted informally in a prior journey's coherence_notes but never filed as a formal finding). Materially de-risked by direct testing this session: every chart-scoped tool tried WITHOUT an explicit chart_id (e.g. mimamsa_lel_query) does NOT silently substitute the session's active_chart_id into the query -- it fails closed with a generic error instead of silently serving the wrong person's data. No tool was found in this audit that silently reads chart-scoped data using session-implicit active_chart_id in place of a caller-supplied chart_id. The residual risk is narrower than 'silent wrong-chart data': a human or LLM caller reading session_recall's usage_hint ('Your active chart is 1c826d5a...') could be misled into believing that ID is 'the' default/native chart and manually pass it elsewhere, especially since 1c826d5a sorts first (index 0) in catalog_charts_list.",
    "reproduce_cmd": "mcp__marsys-jis-direct__session_recall({}) -> active_chart_id: '1c826d5a-41cb-4450-b4dc-59d440e5f75a' (Abhinandan, not native). Contrast: mcp__marsys-jis-direct__mimamsa_lel_query({query:'career change'}) with chart_id OMITTED -> {ok:false, error:\"[alias] primitive 'lel_query' failed (400)\"}, i.e. fails closed rather than silently using the session default.",
    "evidence_file": "evidence/session_recall__default_no_chart_id.json",
    "severity": "TIER4",
    "mechanism": "session_recall's active_chart_id column (mcp_sessions table) was last set by whichever chart was most recently select_chart'd or touched in this session's history; it happens to currently point at the zero-history/second chart. This is a stored-state artifact of session history, not a code defect -- no tool handler reads active_chart_id to fill in a missing required chart_id parameter (confirmed live: mimamsa_lel_query, the one genuinely chart_id-optional-by-schema tool tested, errors rather than substituting). Downgraded from the 'silent wrong-chart' framing in the original task brief to an honest UX/labeling observation: session_recall's own usage_hint text ('Your active chart is X. Pass it to chart-scoped tools') could be misread by a caller as 'this is your default/native chart' when it is only 'the last chart this session touched'."
  },
  {
    "id": "G-F-60",
    "claim": "ref_rules_search leaked a raw, unsanitized Postgres error literal ('error: invalid byte sequence for encoding \"UTF8\": 0x00') directly into the response's content.content field with is_error:true, when the `keyword` free-text parameter reached the database query in a state that triggered a Postgres encoding exception. Same defect CLASS as F-01 (leaked Postgres 'empty' daterange literal) and the raw-upstream-error-leak class this audit's task brief names: an internal driver/DB exception string reaches the MCP caller verbatim instead of being caught and re-served as a clean structured error.",
    "reproduce_cmd": "mcp__marsys-jis-direct__ref_rules_search({keyword:'Saturn null byte test'}) -- captured live once during this audit session (first call in a two-tool-call batch). NOTE: two immediate retries of the byte-identical keyword string (standalone, and re-paired with the same adjacent unicode/RTL-override query) both returned the tool's normal clean honest-empty shape instead of the error -- the trigger condition was not reliably reproduced on demand, but the captured response itself is an unedited, genuine server response, not a fabrication.",
    "evidence_file": "evidence/ref_rules_search__G_raw_postgres_error_leak.json",
    "severity": "TIER2",
    "mechanism": "DIAGNOSIS-INCOMPLETE -- behavior confirmed live exactly once this session (raw Postgres UTF8 encoding-violation error text served verbatim in content.content, is_error:true, no MCP-level sanitization wrapper). Did not trace the ref_rules_search handler's keyword-matching SQL (likely an ILIKE/tsquery WHERE clause) in platform/src to pin the exact file:line where a Postgres exception is caught and re-thrown/served without message sanitization; reproduction was intermittent across 3 attempts with the identical parameter value, suggesting the trigger depends on transient byte-level content in the parameter as it crossed some encoding boundary rather than the literal ASCII text alone. Flagged as a real instance of the class regardless, since the one captured response is unambiguous raw internal-driver text reaching the caller."
  },
  {
    "id": "G-F-61",
    "claim": "A shared, unguarded error-handling helper in the MCP-facing alias layer leaks raw upstream Python-sidecar error text AND internal API routing paths to the MCP caller whenever the sidecar call fails, across at least 7 distinct MCP tools / 10 call sites. Confirmed LIVE on two tools this session: (1) ganita_natal_positions_compute with a malformed datetime_iso returned {\"error\":\"Error: [alias] sidecar /api/pyhora/compute failed (500): {\\\"detail\\\":\\\"Computation error: Invalid isoformat string: 'not-a-real-date'\\\"}\"}; (2) kala_muhurta_get with malformed start_date/end_date returned {\"error\":\"Error: [alias] sidecar /api/compute/phala/muhurta_finder failed (422): {\\\"detail\\\":\\\"date_range must have 'start' and 'end' ISO date strings: Invalid isoformat string: 'not-a-date'\\\"}\"}. Both leak: (a) the internal sidecar's relative API path structure ('/api/pyhora/compute', '/api/compute/phala/muhurta_finder' -- internal microservice routing, not part of the MCP tool's documented surface), and (b) the raw Python exception message verbatim ('Invalid isoformat string: ...', a CPython datetime.fromisoformat() error string), with zero sanitization into a clean structured MCP error.",
    "reproduce_cmd": "mcp__marsys-jis-direct__ganita_natal_positions_compute({datetime_iso:'not-a-real-date', latitude_deg:999, longitude_deg:999}) | mcp__marsys-jis-direct__kala_muhurta_get({chart_id:'482012f1-710e-4a25-994a-93821f5871aa', start_date:'not-a-date', end_date:'also-not-a-date'})",
    "evidence_file": "evidence/register_p1_aliases__G_sidecar_error_leak.json",
    "severity": "TIER2",
    "mechanism": "platform-mcp/src/tools/register_p1_aliases.ts:127-139 (callSidecarPath) and :150-161 (callSidecarGet) are the two shared helpers every '[Phase-1 alias]' MCP tool in this file routes its Python-sidecar calls through. On a non-2xx sidecar response, both do the identical unsafe thing: `throw new Error(\\`[alias] sidecar ${path} failed (${res.status}): ${txt.slice(0, 200)}\\`)` (line 136) / `... sidecar GET ${path} failed ...` (line 158) -- txt is the sidecar's raw HTTP response body (up to 200 chars), embedded verbatim into the thrown Error's message with no attempt to parse out just a safe summary, and `path` is the internal relative sidecar route. This single unguarded pattern is the shared root cause behind at least 10 call sites in this one file: callSidecarGet at lines 1391, 1410, 1429, 1446, 1459 (the ref_planet_position_get / ref_planet_transit_get / ref_aspects_at_time_get / ref_retrograde_periods_get / one more ephemeris alias family) and callSidecarPath at lines 1730, 1776, 1796, 1873, 1970 (phala_predictive_anchors_get/event_anchors, kala_muhurta_get, phala_outlook_get, and ganita_natal_positions_compute x2). Only 2 of these 7+ tools were live-reproduced this session (budget-limited sampling); the shared-helper root cause means the remaining call sites are very likely to exhibit the identical leak on their own malformed-input paths, not independently verified live here."
  },
  {
    "id": "G-F-62",
    "claim": "MCP tool-catalog profile scoping (full/compact/consult, per platform/src/generated/projections/mcp_surface_profiles.generated.json) excludes get_sensitive_degrees from the 'compact' (20-tool) and 'consult' (8-tool) profiles, but 'compact' still includes chart_facts_query, which serves byte-identical sensitive-degree rows (fact_category='sensitive_degree_check') via a category filter. Live-confirmed: ganita_chart_facts_get({chart_id, category:'sensitive_degree_check', fact_subject:'SUN', shape:'rows'}) returns the same 6 rows (same fact_ids, same values: gandanta/kartari/kranti/mrityu_bhaga/neecha_bhanga/pushkara) as the dedicated ganita_sensitive_degrees_get tool. A caller connected under the 'compact' profile, who cannot see get_sensitive_degrees in their tool list, can retrieve identical data through the general-purpose tool that IS present in that profile, for any chart they are already entitled to.",
    "reproduce_cmd": "mcp__marsys-jis-direct__ganita_sensitive_degrees_get({chart_id:'482012f1-710e-4a25-994a-93821f5871aa', subject:'SUN'}) vs mcp__marsys-jis-direct__ganita_chart_facts_get({chart_id:'482012f1-710e-4a25-994a-93821f5871aa', category:'sensitive_degree_check', fact_subject:'SUN', shape:'rows'}) -- identical rows/fact_ids.",
    "evidence_file": "evidence/mcp_server_info__G_profile_scoping.json",
    "severity": "TIER4",
    "mechanism": "Not an entitlement bypass in the security sense -- both paths require the SAME chart-level authorizeChartAccess entitlement (confirmed separately: boundary/dead-phantom/nonexistent chart_ids are rejected identically regardless of which tool or profile is used), and CLAUDE.md §N.4 explicitly documents 'No audience tier: writers emit all rows; serve-time governs access' as an intentional project doctrine -- there is no data-sensitivity tier this system claims to enforce. Flagged as informational only: the tool name 'sensitive degrees' and its selective absence from narrower MCP profiles could be mistaken by an engineer (or a future access-control feature built on top of profile scoping) for a real data-access boundary when profile scoping is actually tool-catalog/UX curation only, generated by platform/scripts/manifest/mcp_surface_profile_builder.ts from the registry with no data-filtering semantics. Testing limitation: this audit session only had one live MCP connection behaving as the 'full' profile (125/152 tools); no 'compact' or 'consult' profile credential was available to runtime-verify tool-list narrowing in practice -- the profile mechanism and the bypass path were confirmed by direct source inspection plus one live call proving chart_facts_query serves identical rows to the profile-excluded tool."
  }
]
