.DS_Store
.venv/
venv/
.tools/
.claude/
!.claude/agents/
!.claude/agents/**
!.claude/skills/
!.claude/skills/**
.claire/
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.env
.ipynb_checkpoints/
/*.png

# Env files with secrets
.env.*
!.env.example

# IDE / tooling artifacts
.continue/
.kilo/
.playwright-mcp/
.superpowers/
.memory/

# Governance auto-generated reports (not source artifacts — regenerated on demand)
00_ARCHITECTURE/drift_reports/
00_ARCHITECTURE/mirror_reports/
00_ARCHITECTURE/schema_reports/

# Failed git rewrite leftovers (crashed session artifacts)
.git.failed-rewrite-*/

# NVIDIA NIM diagnostic report output (not source)
NVIDIA_NIM_LLM_Report.docx
# platform/scripts/nim_*.mjs — removed 2026-05-03: scripts are tracked diagnostic tools, not scratch

# Git worktree sibling directories (created by parallel session setup)
# Pattern: ../Madhav-{session-id}/ — these are worktrees, not subdirectories,
# but listing them here prevents accidental tracking if someone nests them.
# Actual worktree exclusion is handled by git automatically.

# BHISMA Wave 2 — NIM debugging scripts (local probes, not committed)
platform/scripts/nim_catalog_check.mjs
platform/scripts/nim_model_test.mjs
platform/scripts/run_nim_catalog_check.command
platform/scripts/run_nim_test.command

# Eval scratch logs and result dumps (committed results live in platform/tests/eval/)
platform/scripts/eval/results_*.json
platform/scripts/eval/eval_results_*.json
platform/scripts/eval/*_stderr.log
evals/mcp-routing/results_*.json
evals/k2/disagreement_ledger/*.json
evals/k2/battery_runs/*.json

# Claude Code local-only settings (personal permission overrides)
.claude/settings.local.json

# Personal sadhana / spiritual practice notes (not for git)
/99_PERSONAL_SADHANA/

# MCP key setup script — contains live API key, must never be committed
scripts/setup_mcp_env.sh

# Post-merge smoke test scratch space
/post_merge_smoke/

# MCPT v3.2: track eval-results/ JSON, suppress legacy log noise
eval-results/*.log
tools/program-tracker/

# Terraform local state/cache (per-module — not committed; recreated by `terraform init`)
**/.terraform/
**/.terraform.lock.hcl

# Conductor daemon runtime files (not to be tracked)
00_ARCHITECTURE/CONDUCTOR/**/*.pid
00_ARCHITECTURE/CONDUCTOR/**/*.log
00_ARCHITECTURE/CONDUCTOR/**/daemon_heartbeat.json
00_ARCHITECTURE/CONDUCTOR/**/data_build_status.json
00_ARCHITECTURE/CONDUCTOR/build_orchestrator/tracker/state.json
00_ARCHITECTURE/CONDUCTOR/build_orchestrator/tracker/serve.log

# JH parity scratch — [[no-jh-parity-anywhere]]
platform/python-sidecar/tests/test_pyjhora_adapter/_scratch/

# Antigravity IDE briefing pastes — ephemeral working documents, not canonical artifacts
ANTIGRAVITY_PASTE_*.md

# Test result output directories (regenerated on demand — not source artifacts)
test-results/

# Git worktrees
.worktrees/

# Local dev scripts with hardcoded DB credentials — never commit
console-full.txt
console-warnings.txt
platform/python-sidecar/run_l1_fast4_rebuild.py
platform/python-sidecar/run_l1_fix_rebuild.py
platform/python-sidecar/verify_l1_fixes.py
python-sidecar/venv/

# Orphaned root-level lock file — no root package.json exists; per-package locks tracked in their own dirs
/package-lock.json

# Accuracy-harness output dumps (generated per-chart result JSON; do not commit new ones — CLASS-C ruling 2026-07-12)
/accuracy/*.json

# Personal devotional documents (not MARSYS artifacts)
/Yajna_*
/Yajna_Vidhi_Design_Philosophy_*
