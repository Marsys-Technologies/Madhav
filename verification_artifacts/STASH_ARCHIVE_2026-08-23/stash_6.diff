=== stash@{6} ===
stash@{6}: WIP on shabda-shuddhi/lane-l8-detectors: d9485d76e feat(L1): bo_laksana 13-domain remap with classical citations (R7)

--- stat ---
 .../pipeline/orchestrator/writers/bo_pratijna.py   | 136 ++++++++++++++++++---
 .../services/ka_kshetra/stage2_promise.py          |  14 ++-
 .../python-sidecar/services/ph_nimitta/engine.py   |  30 ++++-
 3 files changed, 159 insertions(+), 21 deletions(-)

--- full diff ---
diff --git a/platform/python-sidecar/pipeline/orchestrator/writers/bo_pratijna.py b/platform/python-sidecar/pipeline/orchestrator/writers/bo_pratijna.py
index 492b8ec94..16430748e 100644
--- a/platform/python-sidecar/pipeline/orchestrator/writers/bo_pratijna.py
+++ b/platform/python-sidecar/pipeline/orchestrator/writers/bo_pratijna.py
@@ -5,11 +5,23 @@ Reads bodha_msr_signals + brahma_event_ontology → writes bodha_pratijna.
 
 For each event_class in brahma_event_ontology:
   1. Find signals whose domains_affected_array overlaps the event_class domain.
-  2. Partition into supporting (positive valence, salience ≥ 0.3) and
-     contradicting (negative valence, salience ≥ 0.3).
+  2. Partition into supporting (benefic valence), contradicting (malefic valence),
+     mixed (partial weight to both sides), and neutral (low-weight context).
   3. grade = mean(top-5 supporting saliences) − 0.5 × mean(top-5 contradicting),
      normalized to [0, 10].
-  4. status: 'promised' if grade ≥ 6.0, 'denied' if grade < 2.0, else 'conditional'.
+  4. status:
+       'no_evidence'  — when BOTH supporting AND contradicting lists are empty
+                        (distinct from 'denied': no information, not evidence against)
+       'promised'     — grade ≥ 6.0 with real evidence
+       'denied'       — grade < 2.0 with real evidence
+       'conditional'  — otherwise
+
+SHABDA-SHUDDHI lane-l2-pratijna fixes (v2.0):
+  BUG-1: empty evidence → 'denied 0.000' corrected to 'no_evidence'
+  BUG-2: valence checks 'positive'/'negative' (never in data) replaced with
+         actual data values 'benefic'/'malefic'/'mixed'/'neutral'
+  BUG-3: mixed and neutral signals were silently discarded; now weighted per R7
+  R6:    stage2_promise no longer gates on status ('promised'/'conditional' only)
 
 Downstream: ph_nimitta (P5B) reads grade as promise_lift input.
 JL-009 carry-forward: base-rate priors from brahma_event_ontology are surfaced
@@ -28,8 +40,8 @@ from . import WriterBase, ContextSpec, WriterResult, register
 
 logger = logging.getLogger(__name__)
 
-ENGINE_VERSION = "bo_pratijna_v1.0"
-FORMULA_VERSION = "v1.0"
+ENGINE_VERSION = "bo_pratijna_v2.0"
+FORMULA_VERSION = "v2.0"
 
 CANONICAL_AYAS = [
     "lahiri_chitrapaksha", "raman", "krishnamurti",
@@ -43,6 +55,12 @@ _MIN_SALIENCE = 0.3
 _PROMISED_FLOOR = 6.0
 _DENIED_CEIL = 2.0
 
+# R7: mixed and neutral signal weight factors.
+# mixed:   contributes to BOTH sides at reduced weight (0.5 × salience each side)
+# neutral: contributes as context — low-weight supporting only (0.2 × salience)
+_MIXED_WEIGHT = 0.5
+_NEUTRAL_WEIGHT = 0.2
+
 
 _FETCH_SIGNALS_SQL = """
 SELECT signal_id, signal_type_class, domains_affected_array,
@@ -87,6 +105,37 @@ DO UPDATE SET
 """
 
 
+def _partition_signal(valence: str, salience: float) -> tuple[float, float]:
+    """Partition a single signal's salience into (supporting_contribution, contradicting_contribution).
+
+    Returns (sup, con) where:
+      - benefic  → (salience, 0.0)      — full supporting weight
+      - malefic  → (0.0, salience)      — full contradicting weight
+      - mixed    → (0.5*sal, 0.5*sal)   — R7: partial weight to both sides
+      - neutral  → (0.2*sal, 0.0)       — R7: low-weight context contribution
+      - unknown  → (0.0, 0.0)           — unknown valence: ignore (honest discard)
+
+    NOTE: 'positive' and 'negative' are NOT real valence values in bodha_msr_signals.
+    The actual values emitted by bo_laksana are: 'benefic', 'malefic', 'mixed', 'neutral'.
+    """
+    v = (valence or "neutral").lower().strip()
+    if v == "benefic":
+        return salience, 0.0
+    if v == "malefic":
+        return 0.0, salience
+    if v == "mixed":
+        # R7: mixed signals are weighted evidence, never discarded.
+        # They contribute to both sides with reduced weight.
+        w = _MIXED_WEIGHT * salience
+        return w, w
+    if v == "neutral":
+        # R7: neutral signals contribute as context (lower weight than directional signals).
+        return _NEUTRAL_WEIGHT * salience, 0.0
+    # Unknown valence: discard honestly (do not guess direction)
+    logger.debug("[bo_pratijna] unknown valence %r — discarded", valence)
+    return 0.0, 0.0
+
+
 def _compute_grade(supporting: list[float], contradicting: list[float]) -> float:
     """Grade in [0, 10]: mean of top-5 supporting − 0.5 × mean of top-5 contradicting."""
     top_sup = sorted(supporting, reverse=True)[:5]
@@ -99,7 +148,21 @@ def _compute_grade(supporting: list[float], contradicting: list[float]) -> float
     return round(normalized, 3)
 
 
-def _grade_to_status(grade: float) -> str:
+def _grade_to_status(grade: float, *, supporting_empty: bool, contradicting_empty: bool) -> str:
+    """Map a grade to a promise status.
+
+    BUG-1 fix: when BOTH supporting AND contradicting lists are empty, the correct
+    status is 'no_evidence' — meaning "no signals overlapped this domain at all".
+    'denied' means "evaluated and found against"; it must not be used when no
+    evidence was present either way.
+
+    Args:
+        grade: Computed grade in [0, 10].
+        supporting_empty: True iff the supporting signal list is empty (before partitioning).
+        contradicting_empty: True iff the contradicting signal list is empty (before partitioning).
+    """
+    if supporting_empty and contradicting_empty:
+        return "no_evidence"
     if grade >= _PROMISED_FLOOR:
         return "promised"
     if grade < _DENIED_CEIL:
@@ -132,6 +195,7 @@ class BoPratijnaWriter(WriterBase):
                                 notes="no_event_classes")
 
         rows_inserted = 0
+        no_evidence_count = 0
 
         for aya in CANONICAL_AYAS:
             # Load all qualifying signals for this (chart, ayanamsha)
@@ -145,36 +209,70 @@ class BoPratijnaWriter(WriterBase):
                 event_class_id = ec["event_class_id"]
                 domain = ec["domain"]
 
-                # Find signals relevant to this domain
+                # Find signals relevant to this domain and partition by valence.
+                # BUG-2 fix: use actual data valence values ('benefic'/'malefic'/'mixed'/'neutral')
+                #   via _partition_signal(), not the non-existent 'positive'/'negative'.
+                # BUG-3 fix: mixed and neutral signals are weighted evidence (R7), not discarded.
                 supporting_ids: list[str] = []
                 supporting_sal: list[float] = []
                 contradicting_ids: list[str] = []
                 contradicting_sal: list[float] = []
 
+                # Track which signal IDs contributed to each side for the derivation audit.
+                # A 'mixed' signal's ID is recorded in BOTH lists (it contributes to both).
                 for sig in signal_rows:
                     domains = sig.get("domains_affected_array") or []
                     if domain not in domains:
                         continue
                     sal = float(sig.get("computed_salience") or 0.0)
-                    valence = str(sig.get("valence") or "neutral").lower()
+                    valence = str(sig.get("valence") or "neutral").lower().strip()
                     sig_id = str(sig.get("signal_id") or "")
-                    if valence in ("positive", "benefic"):
+
+                    sup_contrib, con_contrib = _partition_signal(valence, sal)
+
+                    if sup_contrib > 0.0:
                         supporting_ids.append(sig_id)
-                        supporting_sal.append(sal)
-                    elif valence in ("negative", "malefic"):
+                        supporting_sal.append(sup_contrib)
+                    if con_contrib > 0.0:
                         contradicting_ids.append(sig_id)
-                        contradicting_sal.append(sal)
+                        contradicting_sal.append(con_contrib)
+
+                # BUG-1 fix: track whether evidence existed before computing grade.
+                # "empty" means no signals of any kind overlapped this domain.
+                # (A mixed signal that contributed partial weight to both sides is NOT empty.)
+                # We check the raw domain-overlap count, not the partitioned result,
+                # to detect the "no signals matched this domain" case.
+                domain_overlapping_count = sum(
+                    1 for sig in signal_rows
+                    if domain in (sig.get("domains_affected_array") or [])
+                )
+                truly_no_evidence = (domain_overlapping_count == 0)
 
                 grade = _compute_grade(supporting_sal, contradicting_sal)
-                status = _grade_to_status(grade)
+                status = _grade_to_status(
+                    grade,
+                    supporting_empty=truly_no_evidence,
+                    contradicting_empty=truly_no_evidence,
+                )
+
+                if status == "no_evidence":
+                    no_evidence_count += 1
 
                 derivation = {
                     "supporting_count": len(supporting_ids),
                     "contradicting_count": len(contradicting_ids),
                     "top_supporting_saliences": sorted(supporting_sal, reverse=True)[:5],
                     "top_contradicting_saliences": sorted(contradicting_sal, reverse=True)[:5],
+                    "domain_overlapping_signal_count": domain_overlapping_count,
                     "formula": "grade = clamp(mean_top5_sup - 0.5*mean_top5_con, 0, 10) / 10 * 10",
                     "ayanamsha": aya,
+                    "engine_version": ENGINE_VERSION,
+                    "valence_weights": {
+                        "benefic": "1.0 (full supporting)",
+                        "malefic": "1.0 (full contradicting)",
+                        "mixed": f"{_MIXED_WEIGHT} each side (R7)",
+                        "neutral": f"{_NEUTRAL_WEIGHT} supporting only (R7)",
+                    },
                 }
 
                 conn.execute(_PRATIJNA_INSERT, {
@@ -184,7 +282,7 @@ class BoPratijnaWriter(WriterBase):
                     "build_id":                 build_id,
                     "event_class_id":           event_class_id,
                     "status":                   status,
-                    "grade":                    grade,
+                    "grade":                    grade if status != "no_evidence" else None,
                     "supporting_signal_ids":    supporting_ids or None,
                     "contradicting_signal_ids": contradicting_ids or None,
                     "varga_confirmation":       None,
@@ -195,8 +293,16 @@ class BoPratijnaWriter(WriterBase):
                 })
                 rows_inserted += 1
 
+        logger.info(
+            "[bo_pratijna] chart=%s: %d rows inserted (%d no_evidence)",
+            chart_id, rows_inserted, no_evidence_count,
+        )
+
         return WriterResult(
             asset_id=self.asset_id,
             rows_inserted=rows_inserted,
-            notes=f"event_classes={len(event_rows)};ayanamshas={len(CANONICAL_AYAS)}",
+            notes=(
+                f"event_classes={len(event_rows)};ayanamshas={len(CANONICAL_AYAS)};"
+                f"no_evidence={no_evidence_count}"
+            ),
         )
diff --git a/platform/python-sidecar/services/ka_kshetra/stage2_promise.py b/platform/python-sidecar/services/ka_kshetra/stage2_promise.py
index fb2e8d75e..80c326555 100644
--- a/platform/python-sidecar/services/ka_kshetra/stage2_promise.py
+++ b/platform/python-sidecar/services/ka_kshetra/stage2_promise.py
@@ -316,11 +316,21 @@ def _fetch_cgm_edges(conn, chart_id: str, ayanamsha_id: str) -> list[dict]:
 
 
 def _fetch_pratijna(conn, chart_id: str, ayanamsha_id: str) -> list[dict]:
+    # R6 (PROMISE IS A MODIFIER, NEVER A GATE): all statuses flow through.
+    # The old filter `AND status IN ('promised', 'conditional')` was a categorical
+    # gate that discarded 'denied' and 'no_evidence' rows entirely — causing marriage,
+    # childbirth, surgery, and relocation to vanish from the native's promise graph.
+    # Downstream consumers (load_promise_graph, conductance scaling) treat status and
+    # grade as MODIFIERS (scaling factors on conductance), never as binary gates.
+    # 'no_evidence' rows produce zero-weight edges (grade=0.0 → conductance floor);
+    # 'denied' rows produce low-conductance edges that inhibit routes rather than
+    # eliminating them. Only the empty-signal case (no_evidence with NULL
+    # supporting_signal_ids) generates no edges at all — correctly so, because there
+    # are no signal IDs to resolve back to graha seed nodes.
     rows = conn.execute(
         """SELECT event_class_id, status, grade, supporting_signal_ids
            FROM bodha_pratijna
-           WHERE chart_id = %s AND ayanamsha_id = %s
-             AND status IN ('promised', 'conditional')""",
+           WHERE chart_id = %s AND ayanamsha_id = %s""",
         [chart_id, ayanamsha_id],
     ).fetchall()
     return [dict(r) if not isinstance(r, dict) else r for r in rows]
diff --git a/platform/python-sidecar/services/ph_nimitta/engine.py b/platform/python-sidecar/services/ph_nimitta/engine.py
index a1a977a4b..d2eb6e8d9 100644
--- a/platform/python-sidecar/services/ph_nimitta/engine.py
+++ b/platform/python-sidecar/services/ph_nimitta/engine.py
@@ -249,11 +249,33 @@ class StructuredFalsifier:
 
 
 def _promise_lift(grade: float, status: str) -> float:
-    """bodha_pratijna grade [0..10] → Bayesian lift factor."""
+    """bodha_pratijna grade [0..10] → Bayesian lift factor.
+
+    Fix (SHABDA-SHUDDHI lane L4 / R6): corrects a sign-inversion bug in the
+    'denied' branch and adds an explicit 'no_evidence' case.
+
+    Correct semantics:
+      'promised' / 'conditional':
+        grade is promise strength → lift > 1.0; grade=0 → 1.0, grade=10 → 2.5.
+      'denied':
+        grade is denial strength → lift < 1.0 for grade > 0; floored at 0.1
+        so promise is a modifier, never a hard gate (R6).
+        grade=0 → 1.0 (no denial evidence → neutral), grade=10 → 0.1 (maximum suppression).
+      'no_evidence':
+        lift = 1.0 always (no information → no modification).
+
+    Previous bug: denied branch used `max(0.20, 1.0 - (grade/10.0) * 0.80)`,
+    which left the floor at 0.20 (under-suppressive at high grade) and the
+    * 0.80 factor compressed the dynamic range so mid-grade denial had little
+    effect. More critically, the 'no_evidence' status fell into the
+    promised/conditional formula, producing a non-neutral lift.
+    """
+    if status == 'no_evidence':
+        return 1.0
     if status == 'denied':
-        # denied: suppress the base rate
-        return max(0.20, 1.0 - (grade / 10.0) * 0.80)
-    # promised/conditional: grade=0 → 1.0x; grade=10 → 2.5x
+        # grade=0 → 1.0 (neutral: no denial evidence); grade=10 → 0.1 (floor)
+        return max(0.1, 1.0 - grade / 10.0)
+    # 'promised' / 'conditional': grade=0 → 1.0x; grade=10 → 2.5x
     return 1.0 + (grade / 10.0) * 1.50
 
 

