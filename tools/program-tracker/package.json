{
  "name": "program-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Ephemeral live dashboard for the Platform Modernization program. Deleted at program close.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "collect": "node -e \"import('./collect.mjs').then(m=>m.collect().then(s=>console.log(JSON.stringify(s,null,2))))\"",
    "smoke": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
