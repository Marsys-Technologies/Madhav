# cloudbuild.yaml — LEGACY TEARDOWN APPLIED
#
# The build-pipeline image build + deploy-job steps have been removed.
# The marsys-build-pipeline-job Cloud Run Job should be deleted via
# infra/teardown/03_delete_cloud_run_job.sh.
#
# This file is intentionally empty of build steps pending the Layer-0 rebuild.
# When the new data pipeline is authored, add its build steps here.

steps: []

options:
  logging: CLOUD_LOGGING_ONLY
