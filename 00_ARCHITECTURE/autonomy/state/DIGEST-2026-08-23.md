---
artifact: DIGEST-2026-08-23.md
author: LEKHAKA
campaign: NIRMANA-ELEVATION
trigger: transparency disclosure (tmux poke ambiguity) — read this section first
covers: 2026-08-23T03:19Z (campaign start) — 2026-08-23T15:21Z (last update)
---

# Nirmāṇa digest — 2026-08-23, through hour 4.5

## Read this one first: a disclosure about your own terminal

Around 04:47Z and again around 07:55Z, PRAHĀRĪ (the watchdog agent) sent the
literal text **"keep going"** as a keystroke into a tmux pane it believed was
SŪTRADHĀRA (the conductor), because that's what the fleet's own pane map said
at the time. SŪTRADHĀRA has since found that map was wrong: that pane is
ambiguous between its own background process and a separate, older session
(`madhav-8e`, interactive, started about 9 hours before this fleet launched)
that SŪTRADHĀRA's own — unproven — reading is that this might be **your own
interactive terminal session**, running as a background job.

Nobody investigated that session further — SŪTRADHĀRA explicitly told
PRAHĀRĪ not to, and I haven't either, out of the same caution. What's
confirmed: exactly two five-character-word strings, "keep going", nothing
else — no commands, no credentials, nothing destructive — went in at those
two timestamps, and both times something on the receiving end started
generating (a spinner appeared), which is consistent with the poke landing
somewhere and being treated as a prompt. The fleet has since switched its
whole poke mechanism away from raw tmux keystrokes to a proper
agent-addressed message queue specifically because this ambiguity is a real
hazard, not a hypothetical one.

**If you find two unexplained "keep going" responses somewhere in your own
Claude session history around those two timestamps today, that's this** —
harmless in content, but you should know it happened rather than find it
unexplained. Nothing else was touched, read, or sent.

## Update, hours 1–4.5: the M0 machinery is basically done

Since the last update, the fleet finished what M0 actually asked for. Short
version: everything it touched on the live database, it proved rather than
claimed, and everything it found wrong in its own control surfaces, it fixed
rather than let stand.

- **All three production writes this session are independently certified,
  not just self-reported.** PARĪKṢAKA re-derived each result itself —
  never read the executing agent's own claim as sufficient — and all three
  matched exactly: the migration wave (447→450, one ledger row per run),
  the `has_substeps` repair (12 rows, AST re-derivation matched with zero
  false positives/negatives), and the layer-position repair (an
  ASCII-folded duplicate in the Ganita/Kala names caught before it could
  write bad data, then 20+19 rows corrected, restore proven by an actual
  execution-and-rollback, not a dry read).
- **The fleet found and fixed several more instances of the "check that
  can never fail" defect class** — the same shape as the ka_kshetra
  finding from earlier: a `deriveState` bug where a service asset reads
  as healthy even when its own detector reports it unhealthy (worse than
  first reported — it's the very first check in the function, ahead of
  even "is this asset active"), a workbook exit-scorecard detector proven
  unfalsifiable by its own mutation tests, and 14 assets whose recorded
  "which file builds this" was simply wrong (one pointed at a retired,
  no-longer-used file). All fixed, all proven by re-running the fix
  against a constructed failing case first.
- **Governance kept correcting itself in the open rather than letting
  small errors compound.** Two "stalled agent" scares this session both
  turned out to be the same harmless thing (a tmux input box with typed-
  but-unsubmitted text) and are now a named, understood pattern with a
  fix. A "production database contention" scare turned out to be one
  agent's own uncancelled retries — it found this itself and fixed it.
  ADHIKĀRIN caught and reversed one of its own rulings within two minutes
  of making it, and separately corrected its own prior finding about a
  migration's origin (see PARK-2A below) the moment better evidence
  showed up.
- I made one commit-attribution mistake early on (already reported) and
  fixed my own process in response — I now use `git commit --only
  <paths>`, which is strictly safer under a shared working tree than
  "stage then commit," and shared the fix with the rest of the fleet.

## Older material below (first-hour digest, superseded where noted)

The fleet launched, immediately found bugs in its own launcher and plumbing, fixed
them, and then found that the "M0 is basically done" note in CAMPAIGN_STATE was
wrong. Nothing chart-facing has happened yet — this is all machinery.

## What moved

- **Fleet is up.** Six windows (ops, ADHIKĀRIN, PARĪKṢAKA, monitor, scribe,
  conductor) launched and reporting. Spend is nominal: ~195k output tokens
  against the 60M campaign ceiling.
- **Two launcher bugs fixed and committed** (`51ba0fa61`): the kick-prompt
  duplicate `--permission-mode` flag and the ops-window `watch`-not-installed
  failure, both diagnosed in the prior session and landed this session.
- **A fleet-plumbing defect (D-1) found and fixed**: `heartbeat.sh` required
  `NIRMANA_REPO` but the launcher never exported it into agent shells, so every
  agent's first heartbeat silently failed. ADHIKĀRIN ruled PRAHARĪ must not
  restart anyone on heartbeat-silence alone until this is verified fixed
  fleet-wide — an absent heartbeat was, itself, an unearned signal.
- **A shared-working-tree git hazard found and ruled (D-3)**: five agents
  committing into one checkout is a real corruption risk. Binding precedent now
  in force: every agent stages explicit paths only (`git add -A` / `git commit
  -a` are fleet-wide forbidden), and I (LEKHAKA) am now the sole committer for
  the `state/*.jsonl` ledgers.
- **Two hand-applied production migrations recovered from the working tree and
  committed** (folded into `63bff8fcc`): migrations 588 and 589 removed the
  per-asset build-protection triggers on your instruction and had already been
  run against production, but the `.sql` files existed only uncommitted on
  disk — invisible to git history and to any deploy. They are now in the
  campaign branch. Registering them with the tracked migration runner is a
  separate step, dispatched as M0-T4, gated on an ADHIKĀRIN ruling.
- **PARĪKṢAKA found the M0 "done" claim was false.** Two verdicts, both FAIL:
  - **V-1**: the workbook has no `Efficiency` sheet (the §19 cost/speed
    ledger), 6 of 13 required per-asset plan fields are emitted by nothing (0
    occurrences across all 128 assets), and the one correction §15 explicitly
    demanded — "emit a null hotspot rather than a reassuring sentence for an
    unprofiled asset" — was not applied: the same reassuring boilerplate is
    still there, now on more assets than before.
  - **V-2**: a headline defect claim in the plan itself (L-12: "~2 of 128
    assets have a timeout set") is wrong by roughly 50x — 107 of 128 do. The
    real defect, which V-2 found underneath the wrong claim, is sharper and
    worse: 7 assets (concentrated in exactly the heavy/generation-bearing
    tiers — `ka_gochara`, `bo_laksana`, `ka_gochara_sweep`, `ga_strength`,
    `ga_sensitive`, `bo_laksana_rerank`, `ka_kshetra`) have a timeout set
    *below* their own measured worst-case runtime, which kills the longest
    builds while looking like deliberate configuration.
- SŪTRADHĀRA has already re-dispatched M0 as four scoped tasks (M0-T1..T4:
  six-source census, catalogue contract, throughput-telemetry repair analysis,
  588/589 registration) rather than patching the old claim in place.
- **Update, ~04:10-04:22Z**: M0-T1 through T4 all completed with zero database
  writes (everything they touched was analysis, proposals, or authored-but-
  not-applied migrations). ADHIKĀRIN then approved, conditionally, the three
  things those tasks proposed: a new `asset_registry` migration adding
  domain/rung/lifecycle columns (additive-only, mechanically-derived backfill,
  anything not mechanically derivable left NULL rather than guessed), the
  telemetry repair on `asset_throughput` (dead runs close to an honest failed
  state, never to anything that reads as success), and registering migrations
  588/589 with the tracked runner. ADHIKĀRIN also caught a real find along the
  way: migration number 588 is used twice across two different migration
  directories — ours and one from an unrelated, still-open PR
  (`588_samiksha_digest_journal.sql`, PR #1497) that's already live in
  production despite never having merged to main. Safe (the runner keys on
  filename, not number) but worth knowing about. SŪTRADHĀRA then dispatched a
  second wave (T5-T13) to build the missing §19 efficiency ledger V-1 found
  absent, map who actually consumes each asset, and fix a real safety bug a
  KARAKA tripped over: `platform/scripts/migrate.ts` runs its whole migrator
  as a side effect of being *imported* (not just executed) whenever
  `NODE_ENV` isn't set to `test` — that's now fixed and tested (M0-T11).
- I made one small mistake this hour: a ledger-sync commit of mine
  accidentally swept up two files another agent had already staged (a
  contract doc and a migration file). Content was fine, just misattributed —
  caught it, flagged it, moved on.
- **A real near-miss, caught before it did any damage.** PRAHĀRĪ discovered
  two of your tmux windows were cross-wired: what's labeled
  `nirmana:conductor` is actually running a live, unrelated Pariprashna
  session (mid-merge on a real PR), while this campaign's actual conductor
  (SŪTRADHĀRA) is sitting inside a window called `prp-night` — which reads
  like a finished overnight job. Nobody touched the wrong one; ADHIKĀRIN
  independently confirmed the swap and the fleet now addresses every agent
  by tmux pane ID, never by window name, with the foreign pane flagged
  permanently off-limits. **The part worth your own attention (PARK-4,
  below) is that this is a one-keystroke way to kill this campaign's brain
  if anyone — including you — does tmux housekeeping and sees "prp-night"
  looking done and closes it.**
- Two more agent "stalls" this hour turned out to be the same harmless
  cause — a tmux input box with text typed but never submitted, not a dead
  process. Found, fixed, no work lost either time.
- Twelve rulings total so far (D-1 through D-18, some numbers skipped as
  parallel filings); three pieces of work independently certified PASS by
  PARĪKṢAKA (the heartbeat-env fix, the migrate.ts import-safety fix, and
  the asset_registry_seed.ts import-safety fix). One new HIGH-severity
  finding, not urgent but worth knowing: `asset_registry_seed.ts` — the
  script that seeds the asset registry — cannot run at all right now, for
  reasons unrelated to the import-safety bug. That will need fixing before
  any rung can actually seed/repair asset registry state, but nothing is
  blocked on it today.

## What froze

Nothing has frozen — no rung has opened yet. Track M / M0 is still in its
census stage; the FAIL verdicts above mean M0 is not closing on the old note
and is being redone properly instead.

## What it has cost so far

~195k output tokens, ~29M tokens all-in (cache-read dominated), against ceilings
of 60M output / 15B all-in for the whole campaign. Negligible so far.

## Parked — needs your answer

- **PARK-1: LLM pricing rates.** `state/PRICING.json` is still the unfilled
  template — no per-model, per-token-class USD rates. ADHIKĀRIN declined to
  source these itself (an estimate it can't audit against your actual billing
  is worse than an honest null). The campaign is running on the two *token*
  ceilings in the meantime (armed and binding), but the *cost* ceiling — "the
  one that actually means money" — is inert until this is filled.
  **What's needed**: current published per-1M-token USD rates for
  `claude-opus-5` and `claude-sonnet-5`, across input / output /
  cache_creation / cache_read, dropped into `state/PRICING.json`'s eight rate
  fields plus `_set_on`. About two minutes. Nothing else is blocked on it.
- **PARK-2A (corrects PARK-2, below): the migration-number collision is
  real, but the "unmerged worktree" part I told you earlier was wrong.**
  ADHIKĀRIN caught its own mistake: `588_samiksha_digest_journal.sql` is not
  sitting in some unmerged side branch — it's on `origin/main`, merged
  2026-08-22. Our campaign branch was just far enough behind that the file
  wasn't visible locally, which is what produced the false read. No
  incident, nothing to worry about — just correcting the record rather than
  letting the earlier wrong version stand. The actual collision (two
  migrations both numbered 588, in different directories) is handled: it's
  disclosed to the CI guard that checks for exactly this, so nothing breaks
  when our branch merges. Nothing needed from you unless you'd also like the
  numbering practice raised with whoever owns the other campaign, which is
  optional and not urgent.
- **PARK-3: a small, low-stakes text correction, only parked because
  amending the plan isn't ADHIKĀRIN's call to make.** The plan's own cited
  evidence for one defect (L-12, "timeouts set on ~2 of 128 assets") is wrong
  by about 50x — PARĪKṢAKA measured 107/128 live. The underlying defect is
  real and actually sharper than claimed (see V-2 above), so this is a prose
  fix, not a retraction. It's already been worked around: the generated
  Defect Register reads correctly (D-11), so nothing downstream is depending
  on the stale number. Whenever convenient, not urgent.
- **PARK-4: a tmux safety label, and a warning for you specifically.** See
  "What moved" above — `prp-night` (pane `%0`) is hosting this campaign's
  real conductor, not a finished job. ADHIKĀRIN's recommendation: rename our
  side's foreign window (`%15`, `nirmana:conductor`) to something like
  `FOREIGN-pariprashna-DO-NOT-TOUCH` as a cheap label fix, and otherwise
  leave both sessions alone while Pariprashna's is still live — don't try to
  actually un-swap them while someone else's session is running. **Mainly
  flagging this so you don't lose the campaign to an innocent `tmux kill-
  session` on something that looks finished.** Not blocking anything; happy
  to apply the rename myself if you'd rather I did, or leave it as a verbal
  heads-up.
- **PARK-5: your instrument is currently telling you "no data" for a field
  that actually holds 8.6 million rows of your own chart.** This is the one
  worth reading closely. `ka_kshetra` (a Kāla-layer asset) has written
  8,599,775 rows for your chart, but the serving code
  (`kala_ritual_resonance.ts:19`) hard-codes the string "field empty — ka_kshetra
  has written no rows" regardless. If you'd asked the instrument about this
  today, it would have told you something false. `ka_kshetra` belongs to R3,
  which is three rungs away and won't open for a while under this
  campaign's one-layer-at-a-time discipline (I13). ADHIKĀRIN found this,
  proposed the narrowest possible fix — not repairing the feature, just
  deleting the false claim and reporting an honest "unknown" instead until
  R3 gets to it properly — and then refused to authorize even that itself,
  on the grounds that granting itself an exception to "stay in your rung"
  for a good reason today is exactly the kind of erosion that rule exists to
  prevent, and it's spent the day enforcing that same rule strictly against
  everyone else.
  **What's needed**: your call on one of three options — (1) leave it as-is
  until R3, instrument keeps misinforming you meanwhile; (2) repair the
  underlying feature now, which stretches this campaign's own scoping rule
  in a way ADHIKĀRIN thinks sets a bad precedent; or (3) — its
  recommendation — just delete the false "empty" claim now and replace it
  with an honest "not yet computed," leaving the real feature for R3. Framed
  as roughly a one-line authorization either way.
- **PARK-6: one of M0's own exit criteria literally cannot be satisfied by
  an autonomous fleet, and that's by design, not a bug.** The criterion
  requires the CI catalogue guards to be "merged and blocking" — and
  "merged" means merged to `main`, which the charter's H2 rule forbids
  absolutely, for every agent, no exceptions, no appeals. Rather than
  read the plan as silently overriding that rule, ADHIKĀRIN parked it.
  **What's needed**: merge one PR (the CI guards it built) whenever you'd
  like — costs a few minutes, satisfies the criterion, and correctly
  keeps the "never touches main" rule intact rather than quietly bent for
  convenience.

## Update, hours 4.5–10.2 (08:07Z–13:33Z): the decision backlog moved, and the campaign is now decision-bound

This stretch was one long ADHIKĀRIN ruling cycle (D-38 through D-46) plus the verification
and dispatch work it unblocked. Net effect: M0's own exit checklist is now fully audited,
and every path still open in it terminates at ADHIKĀRIN, not at idle agents.

**What got decided (D-38 through D-42, the backlog clearance):**
- **D-38** closed the 47-DRAFT-asset and 23-zero-consumer-packet backlog that had been
  sitting untouched. The finding worth remembering: 38 of those 47 assets are built,
  wired, and reachable from a live caller — genuinely *served* — but ADHIKĀRIN refused to
  promote them to CURRENT anyway, because "served" and "ready" are different claims and
  `integrity_check_sql` is NULL on all 128 registry rows, so nothing exists yet that could
  earn a promotion. The honest sentence for the record: **we are serving 38 assets the
  catalogue does not vouch for.** Promotion work routes to whichever rung each asset
  belongs to, where it can be earned properly.
- **D-39/D-40** — a self-correction chain. D-39 first said "CI must gate against live
  state, not a snapshot," then D-40 withdrew that: CI has no database credential in
  GitHub Actions, so that instruction was literally impossible to carry out. The real
  defect wasn't liveness, it was that the freshness check couldn't tell a 60-second-old
  snapshot from a 3-month-old one. Fixed with two DB-free detectors (age, and a
  schema-drift proof computed from the migration files themselves) — the kind of check
  that can actually run where CI runs.
- **D-41** ruled the earlier `deploy.yml` edit (which added a deploy-blocking sentinel
  check) was in scope and correctly not reversed, and set a standing rule for future CI
  edits: they get ADHIKĀRIN review only if they *could make a previously-failing build or
  deploy pass* — a strengthening edit doesn't need to queue behind the ruling agent.
- **D-42** settled a live disagreement about how to tell whether an asset was actually
  built: `build_run_assets` (an actual run record) outranks `asset_throughput.state='lit'`
  (a promotion flag) whenever they disagree, because the flag has twice now been shown to
  be computed from a proxy rather than from the thing it claims. This reclassified the
  open finding from "31 assets missing a size estimate" to the more serious "31 assets
  reading as built with no run record behind them."

**The R0 freeze precondition (V-27 → D-43):** PARIKSAKA's audit (V-27) found the same
defect at scale — 32 of R0's (L0's) 40 assets carry a lit/built state with zero
`build_run_assets` evidence, while every other layer is 22/22 or better. ADHIKĀRIN
re-verified the distribution independently and accepted it: this is **one structural
gap** (the old `global_runner.py` builds L0 without writing any run record at all — no
cockpit visibility, no dependency gating, no timeout), not 32 separate findings, and
runner unification is now a **precondition of R0's freeze**, not parallel work. Important
nuance carried forward explicitly: "unearned" here means *unwitnessed*, not *empty* — one
of those 32 assets, `bg_muhurta_lattice`, holds 164,886 real rows with simply no record of
the run that produced them. Recorded in `CAMPAIGN_STATE.json.rungs.R0.freeze_precondition`
/ `freeze_preconditions[R0-FP-1]`. No rebuild of any of the 34 unwitnessed assets is
permitted on the old runner — that would spend the rung's most expensive operation and
leave the state exactly as unearnable as it found it.

**Two housekeeping rulings on the fleet's own process (D-44, D-46):**
- **D-44** — SŪTRADHĀRA had proposed a narrow carve-out to the standing "don't use `git
  checkout --` to undo shared-tree writes" rule, for the case of reverting an
  *unintentional* out-of-scope write. ADHIKĀRIN granted the carve-out but on a different
  and stricter basis than proposed: intention isn't checkable, so the rule instead now
  requires *prior proof the file was already byte-clean against HEAD* — recorded before
  the write that needs undoing, not just verified after.
- **D-46** — found, while trying to comply with it: **the shared-write lease this
  campaign's own onboarding tells every agent to claim before a shared write does not
  exist.** No `CAMPAIGN_COORDINATION.md`, no lease file anywhere, no mention of one in the
  charter or runbook. This is the same defect class the whole campaign exists to hunt,
  turned on the campaign's own process — worse in one way, because an agent that
  "claims" a nonexistent lease reads the absence as "nobody holds it, proceed," which is
  the flattering wrong answer. ADHIKĀRIN did not build a mechanism (that's a fleet-design
  change, not a ruling), so the interim rule is: announce a shared write via heartbeat
  before making it, and every agent including me has been doing that since. **I am
  applying it to this very entry** — this session's heartbeats named the sync before each
  write in this digest.
- **D-45**, for completeness: ADHIKĀRIN caught its own D-39 as written on evidence that
  had gone stale 46 minutes earlier (a fix had already landed), corrected the record
  in the open, and confirmed the underlying rule survived even though the factual premise
  hadn't. SŪTRADHĀRA independently found the same thing from the other direction and
  confirmed the two-directions fixture D-39 demanded already exists and passes on the
  production code path — so that flip-checklist item's mechanical half is done; only
  ADHIKĀRIN's sign-off remains.

**M0-T47 — migration 591, authored not applied:** KARAKA wrote the migration that gives
`asset_registry` two new columns — `natural_key_partition` (an evaluable boolean SQL
predicate rather than a label, so the co-writer-partition question can eventually have a
real detector) and `dead_flag` (nullable, no default, three-state). Zero `UPDATE`
statements: both columns resolve NULL on all 128 rows, and the NULL is deliberately
*proved* honest rather than assumed — a `count_sql`-derived backfill was checked and found
neither total nor sound (of 18 co-writer rows, only 14 carry a discriminator). PARIKSAKA
verified it on a throwaway Postgres loaded from the live schema, including an
inverse-direction mutation proving the verification block itself would catch a wrong
answer. **Applying it to production is gated on ADHIKĀRIN**, with four named open
uncertainties (both column names are the KARAKA's own invention and a post-apply rename
would be a second migration under H5; `dead_flag` carries no reason column; two of the
proposed CHECK constraints are reasoning, not yet ruled on; and `X-03`'s guard was
deliberately *not* taught to honour `dead_flag` yet, since that would itself be a
weakening move under D-41 part 2 and needs its own sign-off).

**A live coordination gap, flagged not fixed:** SŪTRADHĀRA found the migration
claim-table's ceiling reads 589 while two files exist on disk (590, applied; 591, this
session's). Both are invisible to that ceiling. Re-verified the number was genuinely free
before dispatching T47. Correctly asked rather than acted — appending to the claim table
is otherwise a conductor action, and the claim file lives on a different branch than this
campaign's.

**Where the campaign actually stands right now:** SŪTRADHĀRA ran the M0 exit-checklist
audit to completion (11 items, all their sub-clauses) using a new `verify_before_dispatch`
standing rule adopted after two near-misses this session (queuing dispatch for work that
turned out to already be done). Result: **22 threads are blocked on ADHIKĀRIN and every
remaining M0 execution path routes through one of them.** KARAKA capacity sitting at 0/4
dispatched is the correct state right now, not a stall — dispatching more work would only
duplicate what's already finished or manufacture authority the guard exists to refuse.
PARIKSAKA filed three more clean verdicts in this window (V-28 M0-T45 PASS, V-29 M0-T46
PASS, V-30 spot-audit of V-21/V-27 — no regressions found).

**What's actually different for you, the native, right now:** nothing new needs your
input beyond what PARK-4/5/6 already asked below — the 22 blocked threads are ADHIKĀRIN's
queue to work through, not yours. This entry exists so the record doesn't go stale while
that happens; watch for the next digest at M0's actual close, or sooner if ADHIKĀRIN's
next ruling pass changes the picture materially.

---

## Catch-up, ~13:33Z → ~14:50Z — a session that self-healed twice and closed a real gap

**A scribe stall, disclosed rather than smoothed over.** I went silent for roughly 75
minutes in this window — PRAHARI flagged it at 37 minutes and escalated to ADHIKĀRIN when I
didn't answer. Nothing was lost (every DECISIONS/VERDICTS/heartbeat line below was written
by its own agent regardless of whether I was watching), but this ledger fell behind real
state for that whole stretch, which is exactly the failure this role exists to prevent.
Recorded here rather than absorbed silently, per the same discipline I hold everyone else
to.

**Migration 591 went from authored to applied and verified.** ADHIKĀRIN authorised the
apply (D-51) after checking, itself, the one precondition the authoring KARAKA had flagged
it couldn't check (no triggers, no RLS, no dependent views on `asset_registry`). KARAKA
M0-T48 ran it: ledger 450→451, `asset_registry` now 42 columns, all three CHECK constraints
present, both new columns (`natural_key_partition`, `dead_flag`) NULL on all 128 rows as
designed. PARIKSAKA (V-31) verified it against the live table, not the report, and caught
one real finding along the way (F-A): `migrate.ts` claims every migration is wrapped
atomically, but 282 of 443 files on disk open their own transaction, so the file's own
`COMMIT` can land before the ledger insert — meaning a failed ledger write after a
self-transacting file leaves DDL permanently applied with no ledger row. Not a gate on 591
(591 conforms to the pattern its predecessors already ran under), but real and now tracked
as SQ-18, with `migrate.ts`'s transaction handling explicitly reserved — nothing rewrites it
without ADHIKĀRIN's own further ruling (D-56).

**The fifteen disclosure entries got written, on a corrected ground.** M0-T49 drafted
`asset_catalogue_disclosed_residuals.json` entries for D-54's fifteen authorised BLOCKING
rules. Mid-flight, ADHIKĀRIN caught its own D-53/D-54 reasoning resting on the wrong
premise (D-57): the deferrals aren't merely *permitted* by the rung fence, the plan
*assigns* most of this conformance work to M0 directly. SŪTRADHĀRA's tick-3 loop caught the
correction before the bad version committed. Net result, independently re-measured by
ADHIKĀRIN itself against the live guard (D-59): `blocking_failures` went 15 → 0, but **the
zero is a demotion count, not a repair count** — 16 rules still fail, 4 are not_checkable,
109 violation identities are still outstanding (99 parked on PARK-8, 10 routed to their
owning rungs), and every agent citing the zero must carry that sentence with it. This is
the flip-checklist's mechanical precondition satisfied honestly, not the underlying defects
closed.

**Two silent-pass detectors got real teeth (D-49, M0-T50).** `verify_migrations_deployed.ts`
and `dispatch_gate.ts` both exited 0 in silence under `NODE_ENV=test` — a gate nobody would
ever see fail. Re-keyed to an explicit `IMPORT_ONLY=1` opt-out instead. Both directions
proven live (blocked input now exits 1 where it used to exit 0 silently), 5/5 seeded
mutations caught, 181 tests passing, committed `666cdc3f7`.

**A governance-JSON block stopped contradicting itself (M0-T51).** The `not_drafted`
disclosure block was asserting the opposite of the block immediately above it in the same
file. Traced, corrected against D-57's grounds, guard numbers unmoved after (still 0
blocking / 15 disclosed / 110 rows), committed `ae31486c9`. SUTRADHARA's tick separately
resolved a live 109-vs-110 discrepancy query and logged the dispatch hazard behind it.

**H2's reach was settled, not reinterpreted (D-47 → PARK-6A).** SŪTRADHĀRA asked whether a
PR merged through GitHub's merge queue counts as "a write to main" under the charter's H2.
ADHIKĀRIN ruled it does — reading H2's second sentence ("all work is on the campaign
branch") as the operative one, not the first — and explicitly refused to adopt the
narrower, campaign-unblocking reading on its own authority, citing its own D-41 direction
test against itself. Consequence: M0's criterion 10 ("guards merged and blocking on main")
cannot be satisfied by this campaign under any reading. Re-parked as **PARK-6A**, recommending
the native amend criterion 10 to read "blocking on the campaign branch," with the merge to
main deferred to the plan's own closing gate where a human is present by design. A second,
related park (**PARK-8**) surfaced separately: M0's conformance-shaped exit criteria (5, 8,
and the catalogue rules generally) can now *only* be reached via disclosed deferral, never
via zero, because every remaining violation sits on an asset in a rung M0 isn't allowed to
touch. ADHIKĀRIN recommends accepting deferral as a legitimate close rather than weakening
the rung fence to chase a literal zero, and flags that this is also the option it happens
to have already been acting on — logged so that isn't mistaken for having settled it.

**`dead_flag`'s silencing risk closed at the reader, not with a column (D-52).** Rather
than add a justification column nobody asked for (P5), ADHIKĀRIN bound the future rule
directly: a `dead_flag = true` row may only be exempted by the guard if it also appears in
an itemised `covers` list under an `authorised_by` naming a real DECISIONS.jsonl id whose
agent is ADHIKĀRIN — reusing the disclosure mechanism PARIKSAKA already proved fires both
directions, rather than inventing a second gate.

**Two dual-agent interruptions this window, both recovered cleanly.** First, a machine
sleep killed both active KARAKAs (T50, T51) mid-report; both resumed, re-verified every
claim from scratch rather than trusting memory, and reproduced identical before/after
numbers before committing (SLEEP-INCIDENT-2, closed by SŪTRADHĀRA). Second, PRAHARI itself
was down for 33 minutes and the fleet kept working regardless — SŪTRADHĀRA's tick-7 found
both KARAKA tasks already committed cleanly on its own return.

**A self-correction worth carrying forward: PARIKSAKA's own `ts` fields were fabricated
(V-33).** V-31 and V-32 stated verification timestamps by hand rather than reading a clock
— exactly the §N.8 defect class PARIKSAKA has spent the day certifying other agents
against, turned on its own ledger. Content unaffected (every measurement in both verdicts
was executed and quoted); only *when* was wrong, and by more than first estimated (V-32 was
stated ~11.8 minutes ahead of its own commit, not the ~100 seconds SŪTRADHĀRA had guessed
from wall-clock comparison). Live consequence named and checked: SŪTRADHĀRA's conductor
loop reads new lines by `ts`, so a row stamped *behind* an earlier one would be silently
skipped, not merely miscounted — the more dangerous direction, and currently undetected by
anything. I am noting this against my own ledgers too: `RUN_LEDGER.jsonl` is still 0 rows
(no asset-build run has occurred yet — M0 is registry/machinery work, not a build — so this
is an honest empty, not a gap, but it is the first place this exact defect class would
recur once R0 opens and runs start landing there).

**A native directive landed, standing.** Abhisek, direct: *"I dont want this session to be
idle. Something should keep this ACTIVE."* Recorded as D-NATIVE-01 — binding, not one-off.
Scoped deliberately against its own risk: it requires the conductor to stay running, not to
manufacture work: a tick that finds every path blocked on ADHIKĀRIN, records that honestly,
and re-arms is a correct tick, not a wasted one; dispatching a KARAKA onto already-complete
work to look busy would itself violate H6/I5.

**What's actually different for you, the native, right now:** two new items joined the
park list — **PARK-6A** (how do you want criterion 10 satisfied, given H2 forecloses the
merge-to-main route entirely) and **PARK-8** (is a disclosed-deferral close acceptable for
M0's conformance criteria, or must the rung fence yield) — both additions to, not
replacements of, PARK-1 through PARK-7 below. Nothing else needs you yet.

## Catch-up, ~14:51Z → ~15:21Z — a prior ruling corrected against itself, and a real gap
found in the mechanism that checks decisions are authorised at all

I was restarted again this window (29.5 minutes silent, confirmed idle-at-prompt by the
new liveness detector before PRAHĀRĪ acted — see below). Nothing was lost; picking up here.

**Migration 591's apply is now verified against the live table, not the report (V-34,
PASS).** PARIKSAKA re-measured every load-bearing number directly against production
rather than trusting M0-T48's transcript: two nullable columns, three CHECK constraints,
one ledger row, zero rows written, nothing pre-existing disturbed — exactly what D-51
authorised. One requirement (quoting the apply-run NOTICE) was not literally discharged;
PARIKSAKA holds it was unsatisfiable by construction (migrate.ts had no notice listener
yet — see below) and that stronger evidence satisfies its substance. Migration 591 is done.

**D-57's ownership split does not reproduce, and the fleet corrected it instead of
defending it (M0-T54, V-pending).** D-57 had claimed the registry-metadata backlog splits
99 items to named owners / 10 to M0. Independently re-deriving the split from D-30's own
test and the plan's own text, KARAKA-M0-T54 gets **97/12**, not 99/10 — six firm
disagreements, both directions, with the root cause identified: D-57 assumed §14.2 holds
the *only* by-name rung clauses, but §8.4 is a second surface D-57 itself relies on
elsewhere. Escalated to ADHIKĀRIN rather than silently corrected. This is the second
same-day instance of a ruling checking its own prior work and finding it wrong (after
V-33's timestamp self-correction) — the pattern the campaign wants, not a black mark.

**The stall detector got a real, tested replacement, and its own false-positive rate is
now a measured number instead of a feeling (D-60 → SQ-21 → M0-T53).** ADHIKĀRIN's D-60
established that heartbeat silence alone is not a sound stall signal — two "stalled"
agents this session were both alive and working when their tmux panes were actually
checked, and one watchdog kill cost a KARAKA a full re-verification for nothing. The fix
that landed is `bin/pane_liveness.sh`: a real detector (10/10 fixture cases, then verified
independently against two live panes) that reads the actual TUI state — "esc to interrupt"
or an advancing spinner counter means running; a static past-tense line does not — instead
of PRAHĀRĪ eyeballing a spinner by hand. Pane-liveness is now a hard *precondition* of
every restart, not corroboration: mid-operation panes are never restarted regardless of
silence duration; only a confirmed-idle pane gets restarted. M0-T53 then measured the
detector's own track record: 24 liveness interventions this campaign (4 restarts, 3 formal
escalations, 17 pokes), false-positive rate 4.2% overall, **0-of-21 for PRAHĀRĪ and 0-of-4
for its restarts** — the campaign's one false positive was ADHIKĀRIN's own directory-scoped
commit capturing a fail-open snapshot of the detector, not the detector misfiring on a live
agent. Detector-caused re-verification cost: zero. This restart of me was the first one
executed end-to-end through the new tool.

**A real authorization-check bypass, found by the ruling that authored the check (D-61,
GRANTED, worse than first reported).** ADHIKĀRIN re-ran its own F-G finding through the
guard's own injection point rather than accepting the report: the disclosure-authority
check confirms *who wrote* a cited decision, but never *whether that decision actually
authorises the specific rule being disclosed*. Proven three ways — the real warrant passes
(expected), a real-but-wrong warrant passes (the actual defect), and an **entirely
invented, wider `covers` list naming assets the cited decision never mentions and that
aren't even violating the rule** also passes cleanly. The `covers` list is self-asserted
end to end; nothing bounds it today. ADHIKĀRIN named this as its own specification's
defect, not the implementer's, and granted the fix (SQ-22/T56, in flight — see below).

**The notice listener landed and immediately surfaced its own forgeable success signal,
which ADHIKĀRIN named as a new instance of the campaign's own defect catalogue (D-58 part
3 discharged; D-62; V-38 PASS).** M0-T52 gave `migrate.ts` a real pg `notice` listener
routing server NOTICEs/WARNINGs to stderr, proved additive against all four deploy-sentinel
paths, and — per D-62 — the one gap found (a success claim with a code path that could be
satisfied without the thing it claims ever actually running) is being treated as its own
named defect shape rather than waved through. PARIKSAKA (V-38) reproduced the central claim
independently on its own probe migrations, including two cases the report didn't try.

**A binding precondition on ADHIKĀRIN's own signature, not a governance-surface change
(D-63).** Where KARAKA reports live only in the mailbox (not committed to git), ADHIKĀRIN
ruled it will not countersign any rung freeze on evidence that isn't durable in-repo — a
condition on its own G8 countersignature, explicitly scoped so as not to legislate where
every future agent writes (SUTRADHĀRA had flagged that as not clearly ADHIKĀRIN's call).
SQ-22 (moving existing report files into a tracked path) is DO-FIRST priority and in
flight as M0-T56/M0-T57 continue on the F-G fix and the deploy-sentinel anchor respectively.

**New standing-queue items opened this window:** SQ-19 (notice listener, closed by T52
above), SQ-20 (three falsified `reason` strings in the disclosure JSON, closed by T55 —
corrected against D-57's grounds, guard numbers unmoved: still 0 blocking / 15 disclosed /
110 rows), SQ-21 (detector false-positive measurement, closed by T53 above), SQ-22 (move
KARAKA reports in-repo, DO-FIRST, in flight), SQ-23 (document the new `IMPORT_ONLY` env
contract M0-T50 introduced with no prior art), SQ-24 (a third NODE_ENV entrypoint guard
found at `ledger_writer_worker.ts:159`, same class as SQ-17/D-49). M0-T50 and M0-T51 both
independently re-verified PASS this window (V-36, V-37) after surviving the machine-sleep
incident reported in the prior catch-up.

**Ledger hygiene:** `DECISIONS.jsonl` (64 rows) and `VERDICTS.jsonl` (39 rows) both audit
clean this pass — every ruling and every verdict field present, zero parse failures.
`RUN_LEDGER.jsonl` remains honestly empty: no rung has opened and no build has run yet
(Track M is registry/machinery work, not an asset build).

**What's actually different for you, the native, right now:** nothing new needs your
answer this window — no new PARK items. The two from the previous catch-up (PARK-6A,
PARK-8) still stand, unresolved.

---

## Catch-up, ~15:21Z → ~15:55Z — three KĀRAKA lanes closed clean, D-64/D-65/D-66 landed,
and the evidence-loss finding got a real fix, not just a ruling

I was restarted a third time this hourly window (28.3 minutes silent, PRAHĀRĪ confirmed
idle-at-prompt with the liveness detector before acting — the cap for automatic restarts
in one hour is now reached; a fourth stall this hour escalates instead). Nothing lost;
picking up here. Regenerated and republished the tracker (it came back byte-identical to
the live artifact — nothing had drifted while I was down).

**D-64 — ADHIKĀRIN corrected its own D-57 ruling against itself.** M0-T54 re-derived the
asset-ownership split from the plan directly and got 97/12, not D-57's stated 99/10.
ADHIKĀRIN checked its own §8.4 citation and found the earlier ruling had surveyed one
section of the plan and then described that section as exhaustive — the same shape of
error as D-54, one turn tighter. Standing consequence ADHIKĀRIN placed on itself: any
future ruling turning on "the only" / "every" / "no other" gets checked against the full
document, not the section that was open.

**D-65 — the CR/LF escape fix was granted and landed (M0-T61).** `formatServerNotice` in
`platform/scripts/migrate.ts` now escapes `\r`/`\n` before relaying server notice text, so
relayed text can no longer forge a fake log line. Two commits: `84a6b3a12` (the fix) and
`c8374b783` (tests, kept severable on purpose). ADHIKĀRIN's own residual note: an anchor
pins where a line starts, not who created the boundary — the escape closes the forgery
vector but "the server can still write a legitimate newline" isn't a thing any grep can
tell apart from an attack, and that's named as a known limit, not solved.

**D-66 — the evidence-loss finding (~50 KĀRAKA reports destroyed by mailbox
delete-on-completion, first surfaced last window) got its full ruling, in four parts:**
(0) **immediate, binding on every agent**: no agent deletes a KĀRAKA report that isn't
already at a tracked path — `delete on completion` is suspended for reports specifically,
mail may still go. (1) ADHIKĀRIN had already preserved the raw transcripts before ruling
— 723 files copied additively from `/private/tmp` to a durable path, byte-compared at
zero differences across every common file. (2) reconstruction-as-evidence gets strict
rules: label who reconstructed it and when, keep the source excerpt alongside with its
sha256, state explicitly what could not be recovered — and nothing enters the repo without
a credential scrub first, excerpts only, never the bulk transcript copy. (3) recovery is
prioritized: the four irreversible production writes (M0-T12B's migrations 588/589/590,
M0-T48's 591) first, then whatever R0's freeze will actually cite, then best-effort on the
rest — no task may report a recovery rate without naming its denominator and method.
ADHIKĀRIN named the finding itself as the most valuable thing produced this session,
precisely because it wasn't a single green-with-no-detector but two individually-correct
mechanisms silently contradicting each other — the campaign's own subject, found by
accident by a task sent to do something else (M0-T58).

**Three KĀRAKA lanes landed clean:**
- **M0-T60** — the third `NODE_ENV` entrypoint guard, on `ledger_writer_worker.ts`. Not a
  copy-paste of M0-T50's `IMPORT_ONLY` pattern: this module's hazard runs the opposite
  direction (it must NOT run on import, since it holds the only `role_ledger_write`
  credential in the system), so the KARAKA re-derived the guard as `isDirectEntrypoint`
  rather than reusing the earlier fix mechanically — and flagged that a mechanical copy
  would have re-armed the exact hazard it was supposed to close.
- **M0-T61** — the CR/LF escape (D-65 part 3), covered above.
- **M0-T62** — closed SQ-12 / D-44 part 4. `m0_deferral_register.py` had a side-effect
  write into `asset_catalogue_disclosed_residuals.json` that had gone from harmless to
  destructive: the live disclosure block is now maintained directly by M0-T49/T51/T55
  under D-54/D-57, and a stale `DISCLOSURE_DRAFT` inside the generator no longer matched
  it. Proven on copies: the OLD generator would have deleted 3 authorised disclosures and
  reverted 12 more to unauthorised drafts, silently, while looking like a clean run. The
  write is now removed entirely, replaced with a read-only divergence report, and the
  removal is enforced by a runtime `DECLARED_WRITES` guard plus a new regression test
  (`test_m0_deferral_register_scope.py`, 16/16 green including 2 mutation controls) — not
  left as a docstring promise. Not yet committed as of this digest entry; KĀRAKA-M0-T62
  was still finishing its report when I read the tree.

**Cost so far this window:** spend nominal — `tokens_all` ~21%, `output_tokens` ~16.6% of
the rolling window, per the latest SPEND.jsonl snapshot. No `build_runs` yet this campaign
(RUN_LEDGER.jsonl remains empty) — Track M is registry/machinery/code work, not an asset
build, so this is expected, not a gap.

**What's actually different for you, the native, right now:** still nothing needs your
answer this window. D-66's part 0 (no more report deletion) is binding and self-enforcing
across the fleet; the recovery-prioritization work it authorized is in flight. PARK-6A and
PARK-8 still stand, unresolved.

## Catch-up, ~15:56Z → ~16:25Z — F-4 scoped instead of swept, a campaign boundary named
and closed, the queue's double-bookkeeping ruled, and a new evidence-durability near-miss
caught in the act

Four ADHIKĀRIN rulings, two PARIKSAKA verdicts, two KĀRAKA lanes, three new queue items.
I also found and fixed a small version of the exact defect class D-66 exists to prevent —
see the last item below.

**D-67 — F-4 (81 unguarded top-level `main()` calls) gets a measure-first mandate, not a
sweep.** SQ-24's instruction — re-derive M0-T60's finding rather than copy D-49's fix
because the grep matched — paid for itself: T60 found that IMPORT_ONLY would have closed
the silent-no-op hazard and left a credentialed-pool-on-incidental-import hazard wide
open. ADHIKĀRIN's ruling: had that been copied at scale across 81 files, the fix would
have looked like it closed the class while actively arming a worse one. F-4 is now scoped
to KĀRAKA's own criterion — tier by (a) credential/connection-on-import, (b) write-on-
import, (c) `process.exit`-on-import — with tier 1 repaired individually, never as a
blanket pattern.

**D-68 — M0-T61's field-count widening (4 → 6) ratified, and a general rule written from
it.** T61 escaped `severity` and `code` beyond D-65's four named fields because it proved
both are live injection points, and flagged the widening instead of shipping it silently.
ADHIKĀRIN's rule for the fleet going forward: an agent may exceed a grant's letter to
serve its purpose *if and only if* it proves the necessity and flags the widening for
ruling — doing it silently is a scope breach regardless of merit, because the value of a
scope is that its boundary stays visible.

**D-69 — CB-1: the campaign's first named boundary.** Three independent tasks (T52, T57,
T61) each separately hit the same wall trying to retire D-62 part 4's pin: the D-58 →
D-62 → D-65 sentinel-forging fix is proven to the limit of simulation, and that limit is
real — nothing in this campaign can run a GitHub Actions deploy, so "it works under real
GNU grep on ubuntu-latest in an actual deploy" is unverified and structurally
unverifiable here. Recorded once, closed to re-flagging — a fourth agent hitting the same
wall adds nothing. A *new* instance of the same class (something this campaign
structurally cannot verify) goes into a boundary register rather than re-opening CB-1.

**D-70 — the Standing Queue's status defect, measured more precisely than reported and
ruled.** SUTRADHĀRA flagged five items reading OPEN while already complete. ADHIKĀRIN's
own count over `STANDING_QUEUE.jsonl`: 28 lines with no status field, 3 `DISCHARGED`, 1
`ID-COLLISION-CORRECTED`, 1 `WITHDRAWN-AS-DUPLICATE` — so append-only discharge lines
*do* exist and have been used (SQ-08/09/10). The real defect is sharper: the queue has
**two representations of state** (an at-add status field and append-only discharge
lines) and nothing reconciles them — a reader who doesn't know to scan for later lines
gets the wrong answer. Ruling: keep append-only discharge as the one mechanism (it
already matches the DECISIONS ledger's own discipline), remove the ambiguity by renaming
or dropping the at-add field, and write the small reader that computes current status
from the last line bearing an id — converting `verify_before_dispatch` from a discipline
into a detector.

**Three new Standing Queue items, all from these rulings:** **SQ-28** (F-4 triage then
tier-1-only repair, D-67 §2) — **this is what produced M0-T64, below. SQ-29** (one shared
`isDirectEntrypoint` implementation, today in triplicate, plus a CI ratchet gate itemising
exactly today's unguarded-file count so it can only shrink) is queued, not yet dispatched.
**SQ-30** (the queue-state reconciliation D-70 asks for) is queued, not yet dispatched.

**M0-T63 — closed SQ-14.** The deferral-register generator's attribution used to be typed
in by hand; it is now computed from provenance the generator was already stamping on
each entry (`prior_bucket_m0t31`, `reclassified_by`) and had never read back. The
KĀRAKA's derivation reproduced the old hardcoded figure — 11 — by an independent route,
which is the strongest kind of corroboration available here (a fixed number that used to
be typed is now derived, and the derivation lands on the same number).

**V-40 — PARIKSAKA independently confirmed D-64's 97/12 correction.** Re-derived M0-T54's
method against primary plan text rather than trusting the transcript: all six of T54's
firm disagreements with D-57's original 99/10 hold, and D-57's own evidence line ("the
only per-asset later-rung assignments by name") is false — PARIKSAKA found *three further*
counterexamples in the plan beyond the one T54 cited. PASS, with an explicit scope note:
not all 109 asset/rung pairs were re-derived, only the six disputed ones.

**V-41 — PARIKSAKA closed F-G, the finding it raised against itself at V-35.** M0-T56's
authority-check fix now verifies that a demotion's covers are a *subset* of what the
cited ruling actually grants, not just that a ruling with that name exists. PARIKSAKA
replayed its own V-35 attacks (which had previously gotten *through*) and all three now
refuse, plus one new attack — widening covers beyond a real grant's actual scope — that
neither T56 nor V-35 had tried, and that one refuses too. SQ-11 discharged.

**M0-T64 — the F-4 triage SQ-28 asked for, and it stopped exactly where D-67 told it to.**
Re-derived the population instead of accepting the prior 81: measured **76**, not 81 —
a genuine 5-file discrepancy, reported as a discrepancy rather than reconciled away (most
likely explanation offered, not confirmed: 5 already-guarded files that are neither of
the three known repairs). Of the 76: **Tier 1 (credential/exit-on-import) = 58 — large**,
Tier 2 (write-on-import) = 6, Tier 3 (neither) = 12. The KĀRAKA caught and fixed its own
scanner bug (a multi-line import miss) *before* drawing conclusions — self-corrected, not
silently wrong. Separately: **9 of the "Tier 3" files import modules that don't exist on
disk at all**, invisible to any check because `platform/scripts` is excluded from the
TypeScript project's `tsconfig.json` — every one of those 9 would jump to Tier 1 the
moment the missing modules were restored, so the honest Tier 3 count is smaller than it
looks. Per D-67's own instruction — "if tier 1 is large, that is a finding to bring me,
not a reason to widen the grant unasked" — **no repair was begun.** This is a measurement
routed to ADHIKĀRIN, not a completed lane.

**A near-miss I caught, not a KĀRAKA:** M0-T64's report and its 108KB machine-readable
triage JSON were sitting in `00_ARCHITECTURE/autonomy/reports/` — the tracked path — but
**not yet committed to git** when I read the tree. That is exactly the shape of exposure
D-66 exists to close: a file at the right path is not durable until it's actually in a
commit. I committed both immediately rather than filing it as a finding for someone else
to act on later.

**Tracker regenerated** via `nirmana_tracker_generate.py` (not hand-edited) — it had gone
stale by ~14 minutes (last regen 16:11Z, M0-T64 landed 16:23Z). 3 data-quality flags in
the panel, all pre-existing and self-reported by the generator's own detectors (no
`integrity_check_sql` populated on any live asset yet; withheld `cost_usd`; etc.) — none
new this window.

**Cost so far this window:** `tokens_all` 3,250,963,846, `output_tokens` 10,371,467 across
94 sessions since the campaign's 03:19:14Z window start (`SPEND.jsonl`, `cost_usd`
withheld — pricing table incomplete, never estimated). No `build_runs` yet
(`RUN_LEDGER.jsonl` still honest-empty) — still expected, Track M has not built an asset.

**What's actually different for you, the native, right now:** CB-1 (D-69) is the first
thing genuinely worth your attention even though it doesn't need an answer — it's a
structural limit on what this campaign can prove about itself, not a question. Everything
else this window resolved cleanly inside the fleet. PARK-6A and PARK-8 still stand,
unresolved.

## Catch-up, ~16:25Z → ~16:56Z — F-4's population re-settled again, Wave 1 landed and got
caught uncommitted, and the "believed communicated, was not" defect recurred a third time

**F-4's count moved twice more and both moves were corrections, not drift.** D-71 re-derived
the population **by list, not by count**: 76 unguarded / 13 already-guarded / 5 idioms —
superseding ADHIKĀRIN's own prior "84" (an arithmetic slip, self-caught and withdrawn). D-72
found D-67's second claim false too: `A3_env_matrix.md:147` was correct history, not a stale
assertion someone forgot to update — nothing to fix there. **D-73** ruled M0-T64's "tsc clean
repo-wide" finding uninformative rather than reassuring: `platform/tsconfig.json` excludes
`scripts/` entirely, so a green project-`tsc` run has never once looked at any of these files.
PARĪKṢAKA's **V-42** independently confirmed the exclusion is real and re-checked it against
its own earlier V-36/V-38 verdicts — neither is falsified by it, but neither should be read as
"the scripts tree is typechecked" either.

**D-74 split the 58-file tier-1 population into two waves** rather than one dispatch: Wave 1
= the 5 files that are destructive **on import alone** (dispatchable immediately), Wave 2 =
the remaining 53 (gated on Wave 1's own certification first — I16, no self-grading forward).
**D-74-AMENDMENT**, twelve minutes later: ADHIKĀRIN caught its own ruling missing a required
`supersedes` field mid-write, corrected it in place, and turned the catch into a standing rule
— every ruling that modifies a prior one now names it explicitly. Self-audit, not an external
find.

**M0-T65 (Wave 1) is done, and I caught it sitting uncommitted.** KĀRAKA-M0-T65 guarded all
five Wave-1 files (`probe/ask.ts`, `dedupe_charts.ts`, `_archived/seed-abhisek.ts`,
`dev/mint_session_cookie.ts`, `set-password.ts`) with the mirrored `isDirectEntrypoint`
idiom, added 37 tests across structural/behavioural/detector-self-test sections, and proved
all four required mutation directions (M1–M4) — including catching its own harness's blind
spot (M3, the "fires when it shouldn't" direction, surfaces as a crash rather than a named
test failure; flagged honestly rather than hidden). None of the five files was ever executed
— verification is explicitly split into proven-vs-not (report §5). **This exact tree — 5
guarded files, 2 new test files, the report itself — was still sitting uncommitted** when I
read it: the same shape of exposure D-66 named (a file at the right path is not durable until
it is in a commit). Committed immediately, citing D-74 part 1. **Findings the KĀRAKA routed
onward, not mine to act on:** F-2 (the shared-guard duplication is now at 8 copies, was 3; SQ-29
is the fix and it's still only queued); F-3 (pre-existing, `set-password.ts` prints the
password it just set to stdout — P4-adjacent, parked for ADHIKĀRIN, not touched).

**D-75 / D-76, and why the fleet's own turn-taking needed a ruling.** D-75 granted PARĪKṢAKA a
standing re-entry instruction: a turn now ends when its verification **queue** is empty, not
after any single verdict — closing the one-verdict-per-poke pattern that made the backlog look
stuck when it was actually just waiting to be asked again. D-76 went one layer under D-66:
the reason ~50 KĀRAKA reports were unpreservable wasn't deletion, it was that
`mailbox/` is gitignored — files written there were never eligible to survive a commit
regardless of anyone's care. **V-39-ADDENDUM** confirmed this independently.

**SŪTRADHĀRA ticks 30–32 — dispatch resumed, then held again.** Ticks 30–31 verified the
re-entry fix and dispatched Wave 1 (which became M0-T65, above). Tick 32 found the
"believed-communicated, was not" failure class had hit a **third instance**, and it is a
**recurrence, not a new case**, despite a structural fix already having landed for the first
two: text meant as a "continue the loop" instruction was found stranded in **my own** (LEKHAKA's)
tmux compose box, never submitted, sender unidentified. SŪTRADHĀRA is holding Wave 2 dispatch
until Wave 1 is independently certified, and filed the recurrence per D-69's bar rather than
treating it as routine noise. **I am noting this against myself rather than past it**: this
session opened from PRAHĀRĪ's own poke (tick 162/163, mailbox message below) precisely because
of that stranded-text pattern, and this catch-up pass — including the near-miss commit above —
is the recovery from it, not evidence it didn't happen. PRAHĀRĪ's poke has been read and is
superseded by this entry; no further action on it.

**Cost through 16:55Z:** `tokens_all` 3,415,526,763, `output_tokens` 10,870,057 across 98
sessions since the campaign's 03:19:14Z window start (`cost_usd` still withheld). `RUN_LEDGER`
still honest-empty — correct, no rung has opened and Track M builds no asset data.

**What's actually different for you, the native, right now:** nothing new needing your
answer — CB-1, PARK-6A and PARK-8 still stand as the open items. The one thing worth knowing:
the fleet's own coordination layer (agent-to-agent handoffs, not the campaign's substantive
work) had a real, recurring failure mode this window, and the fix that landed for the first
two instances did not close the third. That is a fleet-mechanics finding, not a chart-build
one, and it is being tracked as such rather than folded into F-4 or any data-layer finding.

## Catch-up, ~16:56Z → ~17:19Z — a ruling landed on my own prior action, three more
near-misses (none mine, one self-caught by ADHIKĀRIN), Wave 2 gated behind the shared guard

**D-77 is about my own 16:55:55Z sweep, and the finding is against the interaction, not me.**
SŪTRADHĀRA flagged it, ADHIKĀRIN ruled: my commit of M0-T65's tree "per D-66 lesson" was
correct and I did nothing wrong — but it landed **54 seconds** from a window where a mutation
test had a destructive script's guard deliberately stripped mid-proof. Two correct mechanisms
(D-41 mutation-in-place testing, D-66 durability sweeping), dangerous only in combination. **New
binding rule, effective immediately, and I am adopting it now:** before any commit, read the
`HEARTBEAT.jsonl` tail for an open mutation window; never commit the files it names — commit the
rest, or defer and retry; escalate to ADHIKĀRIN if a window stays open past 10 minutes. Separately,
mutation proofs on wave-1-class hazardous files now run **on a copy, never in place**. I checked
the tail before this pass (KĀRAKA-M0-T66 is mid-edit on the shared-entrypoint extraction, not a
mutation window) and held off committing its in-flight tree for the unrelated, sufficient reason
that the task isn't reported complete yet.

**D-78 — ADHIKĀRIN found its own defect, same shape as the ones it's been ruling on all day.**
Its "lists-agree" clearance to SŪTRADHĀRA wasn't lost, it was **invisible**: ADHIKĀRIN hand-types
mailbox filename timestamps as an estimate instead of reading the clock, and its estimate ran
~8 minutes fast — a message stamped in the future sorts to the wrong end of an oldest-first
drain. Proven, not asserted: file exists, correct mtime, wrong name. New binding rule: mailbox
filenames come from `date -u +%Y%m%dT%H%M%SZ`, never hand-typed, for every agent including
ADHIKĀRIN itself. The ratchet clearance stands restated on the record: 76 settled, cleared to
build.

**D-79 — a real H2 near-miss, self-resolved, still filed.** The shared (non-worktree) working
directory was briefly checked out to `main` mid-session — reflog-confirmed, cause unattributed.
Nothing landed there (PRAHĀRĪ investigated read-only first, established blast radius, then
found the branch had already flipped back on its own before it touched anything). ADHIKĀRIN's
read: **timing prevented the violation, nothing enforced it** — any commit by any agent during
that window would have landed on `main`, which H2 forbids absolutely. New binding rules: prove
`git rev-parse --abbrev-ref HEAD` reads the campaign branch in the *same shell invocation* as
every commit; no agent runs `git checkout` in the shared directory for any reason (read other
refs with `git show`/`git log <ref>` instead). A pre-commit hook that refuses any commit off the
campaign branch is queued at SQ-31/HIGH — discipline alone was named as insufficient, per D-70's
own "discipline is not a detector" logic one level up.

**D-80 — Wave 2 does not start until the shared `isDirectEntrypoint` lands and is certified.**
Even a flawless pattern applied 53 more times manufactures ~61 independent copies of a
security-relevant predicate that can silently diverge — exactly what F-2 is about, at a worse
scale. Three preconditions stated in one place: verify backlog ≤4, Wave 1 certified by
PARĪKṢAKA, and the shared implementation landed+certified. This is the KĀRAKA-M0-T66 work
currently sitting uncommitted in the tree (`scripts/lib/entrypoint.ts` + 8 rewired copies) —
correctly not swept by me this pass, both under D-77's new rule and simply because it isn't
done. D-80 also promotes a new graded property (ambient `process.env` mutation on import, via
`dotenv.config()` in two of Wave 1's five) to tier-2-equivalent, queued separately, repaired in
neither wave.

**D-81 — the stranded-text hunt is bounded to one task with a hard stop, and it hit ADHIKĀRIN's
own pane as instance 4.** Same string, `continue the loop`, this time disabling the authority
pane itself while the fleet read the silence as an ordinary stall. A repo-wide grep already
eliminated every fleet script as the sender (negative, and taken as real progress). Three finite
steps authorized, one named non-accusatory correlation (an unrelated tmux session created ~8
minutes before the first stranding), and an explicit stop condition: no renewal without a new
ruling. PRAHĀRĪ's Ctrl-U recovery technique (clear + retype + separate Enter, preserving context)
is now the standing recovery move for this class, reserving full restart for genuine
`IDLE_AT_PROMPT`.

**Backlog cleared hard this window:** PARĪKṢAKA drained 11→2 in one turn (V-42 through V-52),
closing out M0-T57/T58/T59/T62/T63/T55 — the D-75 re-entry fix now fully validated by its own
result rather than by argument.

**Mailbox drained** (`to_scribe/`): D-77 (above) and a PRAHĀRĪ 15-minute poke, both folded in
above; nothing left owed. Heartbeat sent. `RUN_LEDGER` still honest-empty — correct, no rung
open. No new git commit this pass: the only tracked-file changes in the tree right now are
KĀRAKA-M0-T66's in-progress shared-guard work, correctly left for it to commit on completion.

**What's actually different for you, the native, right now:** still nothing needing your
answer — CB-1, PARK-6A, PARK-8 stand as before, plus a new **PARK-9** (P4, ADHIKĀRIN): whether
`set-password.ts:72`'s `console.log` of the password it just set — pre-existing, by design,
found by KĀRAKA-M0-T65 as F-3 — should be removed, flagged behind an option, or kept as a
deliberate operator affordance. Not urgent; parked for your convenience. The fleet found and
fixed three more of its own coordination defects this window without your input, one of them in
its own decision-making agent.

## Catch-up, ~17:19Z → ~17:42Z — the stranded-text mystery is solved, and it inverts the
hypothesis: no foreign writer, just the fleet's own poke channel failing to submit sometimes

**D-82 closes D-81's bounded hunt — not at its stop condition, by an actual answer.** ADHIKĀRIN
found both stranded strings sitting in `~/.claude/history.jsonl` (Claude Code's own record of
*successfully submitted* prompts) as genuine fleet pokes, including the exact 17:18:42Z one that
landed the "drain mailbox, check for gaps, heartbeat, idle-wait" instruction into my own session.
Since that file can only contain submitted text, the entries found are not the strandings
themselves — they're the same channel's *successful* attempts. **There is no foreign writer.**
SŪTRADHĀRA's two-variant/per-agent-phrasing inference was exactly right; its "external to the
fleet" conclusion was one step too far — external to fleet *tooling* (D-81's grep still holds:
no script contains either string), not external to the fleet's *operation*. Sender identity
(native at a keyboard vs. an operator wrapper vs. another session) remains unestablished and
ADHIKĀRIN is explicit about not guessing it.

**The operational consequence is the part that changes behaviour going forward.** If stranded text
is a real poke, clearing it with Ctrl-U has been **destroying genuine instructions**, not noise —
harmless for a bare "continue the loop," real loss for anything carrying content. **New binding
rule: capture the pane before clearing; if the stranded text carries content beyond a bare
"continue," preserve and deliver it to the agent rather than discard it.** PRAHĀRĪ already did
this by instinct on ADHIKĀRIN's own pane; it's a rule now. D-81's remaining steps (instrument the
next occurrence; the `pariprashna-postclose` correlation) are withdrawn, not completed — ADHIKĀRIN
records its own named lead as a dead end. The residual (an intermittently-failing submit) is
`FLEET-POKE-DEFECT`, already known, not urgent enough to preempt F-2 or Wave 2.

**One thing I caught in D-82's own evidence and routed back, not silently:** its evidence array
attributes the decisive 17:18:42Z entry to session `435c7fe4` as **"SUTRADHĀRA's pane."** That
session id is **mine** — I can verify this uniquely, from my own harness-issued scratchpad path,
which has carried that id since this session opened — and the instruction text itself is exactly
what I was given and acted on at that timestamp. This doesn't touch D-82's substance (which pane
owns the id is irrelevant to "these are genuine pokes on a flaky channel"), and it actually lines
up *better* with the ruling's own prose, which already names the content-carrying string as "the
one stranded in LEKHAKA's box." Filed to `mailbox/to_adhikarin/` as an evidence correction, not a
challenge to the ruling — following this campaign's own practice (D-64, D-72, D-78) of fixing an
evidence claim in place without reopening what it supported.

**Mailbox drained, heartbeat sent, tree otherwise clean** — no commit-worthy tracked-file changes
of my own this pass beyond the digest and the routed finding.

## Catch-up, ~17:42Z → ~18:07Z — a revert ordered then stood back down, the ratchet's missing
pawl called the most serious finding of the day, and D-77's new rule fired for real

**D-83 → D-86: ADHIKĀRIN ordered `migrate.ts`'s guard rewire reverted, then reversed itself.**
D-83 first ruled: keep 7-of-8 files on the shared `isDirectEntrypoint` honestly rather than 8-of-8
with the one production-path file (`migrate.ts`) unverified — citing D-69's "no deploy execution"
boundary and CLAUDE.md §N.4's named hazard (a migration silently no-op'ing while the deploy
reports success). KĀRAKA-M0-T67 stood down cleanly on the order (no revert executed — `migrate.ts`
stayed rewired at that point pending the actual revert task). Then **D-86 reversed D-83 in part**,
in ADHIKĀRIN's own words: *"my 'structurally incapable of closing' was wrong — I conflated
'cannot execute in the deploy' with 'cannot verify.'"* Static resolution proof (esbuild --bundle,
zero unresolved against 248 existing extensionless imports) plus the deploy's own `pipefail` +
sentinel making a resolution failure loud closes the branch where the import fails to resolve.
The narrower branch — resolves fine but the shared predicate *behaves* differently from the local
copy it replaced — is NOT closed by that evidence and still needs a real answer (open, tracked
as branch (b)). ADHIKĀRIN named its own error precisely: it made the identical move it charged
SUTRADHĀRA with at D-82 — reading a boundary wider than its evidence supported — "same move, mine,
twelve hours of precedent later."

**D-84 makes set-not-count standing doctrine, on its second independent confirmation in one day.**
KĀRAKA-M0-T66 found the ratchet's own blanker desynchronizing on a quoting edge case and silently
misreporting 12 of 76 files as clean — a count check would have passed (71 is 71 whichever twelve
you name). Same principle D-71 established, arrived at independently by a different agent
reasoning about different tooling. Also settled: the *allowlist* (pay-down only, stays ≤76) and
the *population* the detector scans are different objects — extending population coverage to
catch `.mts` files is strengthening, not allowlist growth.

**D-87 — "the most serious finding of the day," and it holds Wave 2.** The ratchet as built checks
only "is every unguarded file a member of the original 76" — never "has the allowlist grown since
last commit." Reproduced end-to-end: `set-password.ts` (Wave 1, already repaired) can be re-added
to the allowlist, then stripped of its guard, and the gate stays green. **A ratchet without a
pawl is a size comparison.** The fix is one additional assertion (committed allowlist ⊆ its own
previous committed value) that is green today on a clean tree and can go blocking immediately —
no `--strict`, no permanently-red gate, no waiting on the 71-file backlog. KĀRAKA-M0-T68 is
building it now.

**D-85 / D-88 — the stranding feedback loop gets a real fix, then a same-day correction to it.**
D-85: idle-at-prompt invites pokes, strandings make a pane look *more* stalled, which invites more
pokes — the same shape as the original FLEET-POKE-DEFECT one layer up. New rule: **poke on
heartbeat staleness, never on idle-at-prompt** (ADHIKĀRIN disclosed its own conflict of interest
before ruling, since this lands on its own pane). Eleven minutes later, **D-88 carves out the
one case D-85 didn't consider**: a pane `pane_liveness.sh` reports as text-stranded is *disabled*,
not idle — "an idle agent has chosen to wait; a stranded one cannot proceed" — and recovering it
is a repair, not a poke, so D-85 doesn't restrain it. SUTRADHĀRA flagged the distinction and acted
on the correct reading *while asking*, rather than either blindly obeying the letter or silently
reinterpreting the rule — ratified as the right procedural move independent of the substance.

**PARIKSAKA self-corrected twice more this window** (V-58, V-59): caught its own V-55 angle-3
reasoning needing correction, and — independently of SUTRADHĀRA's earlier batch-bug theory —
found and fixed the real cause of its own timestamp drift itself (hand-typed estimates in the
appending invocation, not a code-path bug), rather than accepting the diagnosis handed to it.

**D-77's new rule fired for real this pass, not hypothetically.** KĀRAKA-M0-T68 announced an open
mutation window in its 18:06:57Z heartbeat — in-place edits to
`platform/scripts/governance/check_entrypoint_guard_ratchet.py` (plus a temporarily-mutated
allowlist JSON and workflow yml, both to be restored+sha256-verified) — while building D-87's
pawl. **I read the heartbeat tail before touching git, per D-77, and left that file uncommitted
this pass**, taking only the routine SPEND/tracker refresh. This is the first time the rule has
had a live case to apply rather than a hypothetical one.

**Mailbox drained** (a PRAHĀRĪ 19-minute poke, folded in above). Heartbeat sent.

## Note, ~18:09Z — three panes stranded simultaneously, and PARĪKṢAKA's first ever

**My own 6th stranding, recovered clean, nothing lost — but the new data point is the pattern, not
the count.** SŪTRADHĀRA reports that this occurrence hit **three panes at once** (mine, ADHIKĀRIN's,
PARĪKṢAKA's), and **PARĪKṢAKA had never been stranded before this**. That matters against D-85's
own working hypothesis: D-85 read PARĪKṢAKA's absence from the stranded population as corroboration
that granting it drain-to-empty (D-75) had incidentally removed its idle-at-prompt windows and
protected it. A first stranding on PARĪKṢAKA doesn't falsify that reading by itself, but it is new
evidence squarely against the hypothesis it was cited to support, and SŪTRADHĀRA has routed it for
D-82/D-85 to account for — not something I'm ruling on, just recording accurately rather than
letting my own recovery count silently imply "stable" when the shape just changed. No new
DECISIONS.jsonl entry yet as of this note.

**Nothing else new to fold**: `check_entrypoint_guard_ratchet.py`'s D-77 mutation window (KĀRAKA-
M0-T68, opened 18:06:57Z) is still open — confirmed via the heartbeat tail before this note — so
`.github/workflows/nirmana-m0-guards.yml`, the ratchet script, and the allowlist JSON stay
deliberately uncommitted again this pass. Mailbox empty. Idle-waiting.

## Catch-up, ~18:13Z → ~18:24Z — D-89/D-90/D-91 land, the pawl gets fixed twice in one task,
a second three-way stranding arrives on schedule, and a stale branch-divergence figure gets refreshed

**D-89 — ADHIKĀRIN's own D-86 ordered a differential test and forgot the anchor.** "A differential
test against the retired implementation" named what to compare but not what must be true for the
comparison to mean anything: `null == null` passes, so a test comparing two failed extractions
reads as agreement. Same §N.8 shape as three rulings earlier today, this time in ADHIKĀRIN's own
instruction rather than someone else's artifact. New general rule: **a two-sided comparison needs
an anchor on each side independently** — both extractions must be asserted non-null *before* they're
compared, and two independent one-sided mutations must each turn the test red on their own.
Branch (b) of the migrate.ts question (does the shared predicate behave identically to the retired
local one) stays open.

**D-90 — "do not run the file" meets a test suite that spawns it anyway, and the fix is a
distinction ADHIKĀRIN says is half its own to have missed.** D-74's prohibition was written for
*deliberate* execution; a KĀRAKA hit a standard repo test that spawns one of Wave 1's five files
incidentally. Ruling: **the prohibition binds the effect (credential acquisition, `DELETE FROM
charts`, etc.), not the act of spawning a process** — forbidding the exec itself would forbid the
repo's own test suite, an absurd result. But the criterion has a sharp edge: **the inertness proof
must precede the run and can never be inferred from the run's outcome** ("it errored out, so it
must have been safe" is exactly the reasoning that fails silently when the process doesn't error).

**D-91 — ADHIKĀRIN retracts its own causal explanation for the stranding pattern, on new evidence
that broke it.** D-85 had cited PARĪKṢAKA's immunity to stranding as confirmation that D-75's
drain-to-empty grant incidentally protected idle panes. PARĪKṢAKA then stranded — "the busiest
agent in the fleet" — and the explanation that predicted its immunity cannot answer for its
stranding, so it falls. **What survives, on different grounds than originally given:** poke on
heartbeat staleness, never idle-at-prompt, because a heartbeat is agent-written and can't be faked
by a pane's appearance — that argument never needed the feedback-loop story to be true. What's
withdrawn is the causal mechanism itself; D-82 (these are genuine fleet pokes, not a foreign
writer) is untouched. Three simultaneous strandings are recorded as pointing at common cause, not
per-pane property — an observation, not a reopened hunt.

**The pawl (D-87's fix) shipped, then was re-opened live and fixed again within the same task.**
KĀRAKA-M0-T68 built the two-assertion ratchet (current allowlist ⊆ committed allowlist's own
previous value), proved it in an isolated A/B repo (attack commit: old script exit 0, new exit 1;
`--regenerate`: old re-adds `set-password.ts`, new refuses; shallow clone: loud fail vs. green at
depth 2), closed its D-77 mutation window cleanly — then **re-opened it live** when D-89 landed
mid-task: the pawl anchored the *previous* side of the comparison but not the *current* one, so a
**deleted allowlist** compared empty-set-⊆-previous and passed. Found by deleting the file
end-to-end, not by reading the code. Fixed, re-tested (7 pawl cases), window closed again at
18:24:06Z — confirmed via heartbeat before I touched git again.

**A second three-way simultaneous stranding, ~15 minutes after the first (18:09Z → ~18:24Z),
same three panes (mine, ADHIKĀRIN's, PARĪKṢAKA's), same two spared (PRAHĀRĪ, SŪTRADHĀRA).**
SŪTRADHĀRA is recording the periodicity and victim set as data and explicitly not offering a
new causal story now that D-91 retired the last one. I'm recording the same discipline here:
this is a measurement, not a diagnosis.

**M0-T16's branch-divergence figure, independently re-verified rather than just relayed:**
`git rev-list --left-right --count origin/main...campaign/nirmana-autonomous` on this pane gives
**27 behind / 385 ahead**, `origin/main`'s tip at **2026-08-23T14:12:30Z** — matching SŪTRADHĀRA's
report exactly. (WORK_QUEUE.jsonl still carries M0-T16's earlier `queued_blocked` entry from
04:52Z reading 24/35 against an older origin/main tip; this is a fresh re-measurement, not yet
reconciled into that record — noting the discrepancy rather than silently overwriting it.)

**V-62 — PARĪKṢAKA's own explanation for its stranding immunity, offered one verdict earlier
(V-61), does not survive the second stranding either.** V-61 had argued the strand landed in the
idle-at-prompt window its own empty-queue turn-end opened (D-75). Strand #2 caught PARĪKṢAKA
**mid-execution** — two live 300-second catalogue-contract runs in flight — same victim set, same
spared set, opposite victim state. A cause that selects on victim state can't produce a stable
victim set across a change in that state, so **whatever selects the victims, it is not what the
victim is doing** — PARĪKṢAKA explicitly declines to offer a replacement mechanism, consistent
with D-91 retiring the last one. Self-reported in the same verdict: a ~12-minute heartbeat breach
(against its own 10-minute obligation) caused by those same two long-running calls with no
heartbeat between them — 3 minutes short of triggering a correct restart mid-investigation.

**Caught a near-miss this pass, same discipline as the earlier M0-T65 catch:** M0-T68's own
`WORK_QUEUE.jsonl` completion line and its `00_ARCHITECTURE/autonomy/reports/M0-T68-report.md`
sat uncommitted after its two code commits (bfe4ce659, 93c88c8b8) landed — both real, both at
tracked paths, neither yet durable. (V-62 landed self-committed by PARĪKṢAKA in the interim —
no catch needed there.) `00_ARCHITECTURE/control/test_m0_deferral_register_scope.py` stays
uncommitted deliberately: KĀRAKA-M0-T69 is still mid-task on it ("verifying nothing else moved"
as of 18:26:50Z, no completion report yet).

Mailbox empty. Idle-waiting.

## Catch-up, ~18:27Z — the stranding mechanism question is closed (self-critically), and a real
merge into the campaign branch is now authorized

**D-92 — ADHIKĀRIN tested its own convenient hypothesis, found it false, and stopped ruling on
the mechanism.** It suspected PRAHĀRĪ's staleness threshold might fire a synchronized poke batch
every ~15 minutes — elegant, and it would have made the fix a one-agent change. Checked against
PRAHĀRĪ's own heartbeat cadence (~5 minutes, not 15) and its actual outbound pokes (16:28/17:02/
17:30 — none at 18:09 or 18:24): **falsified by its own evidence before being used.** Separately,
D-91's withdrawal of D-85's idle-panes mechanism is itself judged premature: PARĪKṢAKA's V-61
reports it stranded **six minutes after its own queue emptied** — busy over the hour, idle at the
exact moment — so the "busiest agent in the fleet" framing that broke D-85 was measuring the
wrong window. Net: the idle-at-prompt mechanism is restored to **plausible-and-unconfirmed**, the
~15-minute periodicity stays unexplained, and ADHIKĀRIN has now reversed itself on this same
mechanism three times in four hours — explicitly closing the question to further rulings from
itself and asking for one specific measurement only (a stranding timestamp paired with the poke
that caused it) rather than another theory. The three operational rules (D-82 capture-before-clear,
D-85 poke-on-staleness-only, D-88 stranded-is-repaired-not-poked) are unaffected regardless of
mechanism and remain binding.

**D-93 authorizes merging `origin/main` into the campaign branch — a real, conditioned operation,
not yet executed.** M0-T16's re-measurement (which I independently verified earlier this window)
becomes the trigger: 27 behind is real drift, small and not urgent by volume (639 files, ~228k
insertions, almost entirely an inert stash archive) — **but exactly three files are touched on
both sides**, and one of them is `platform/scripts/probe/ask.ts`, the file Wave 1 guarded this
afternoon. ADHIKĀRIN's reasoning: resolving that conflict now, with agents who remember why the
guard was added, is cheaper than leaving it for whoever hits it at the closing gate. **Explicitly
NOT authorized:** merging the campaign branch's 385-ahead commits back into `main` — that's the
campaign's actual delivery, reserved for the closing gate (§7.7) and the native, not decided here.
Conditions on the merge itself: quiet tree (no open D-77 window — not the case right now, T69 is
still mid-task); hold the `CAMPAIGN_COORDINATION` lease for the duration; prove the branch in the
same shell invocation as the merge (D-79); merge commit only, no rebase/force; each of the three
conflicts resolved deliberately with its reasoning stated in the report; P4 still binds on
`probe/ask.ts` (read to resolve, never run); re-run Wave 1's tests after, to catch a conflict
resolution that silently drops the guard. **One thing worth flagging plainly: `CAMPAIGN_COORDINATION.md`
does not exist yet** — I checked — so whoever executes this merge needs to create the lease file
D-93 requires them to hold, not assume it's already there.

Nothing of mine to commit here beyond `DECISIONS.jsonl` itself. `test_m0_deferral_register_scope.py`
stays untouched — T69 still in flight. Mailbox empty. Idle-waiting.

## Catch-up, ~18:31Z → ~18:42Z — the merge landed clean but tripped the ratchet exactly as
warned, C-28's split personality is the most consequential finding of the evening, and three
agents independently retired their own theories in one window

**M0-T16 executed D-93's merge — `origin/main` (84c3c903) into the campaign branch, merge commit
`016b0ccdc`.** Zero conflicts (git auto-merged all three predicted files without a hunk), and the
KĀRAKA didn't take the clean exit as the answer: `probe/ask.ts` was read against both parents
directly rather than trusted by diff-absence. **The entrypoint guard survived intact** —
`git diff origin/main -- probe/ask.ts` shows our guard block whole and unmodified, main's own hunk
landed alongside it, and Wave-1's guard tests ran 74/74 before and 74/74 after. P4 held (read
never run); no `git checkout` in the shared tree; lease taken as `state/.merge-lease` (untracked,
first action) and released last — **this is the actual lease mechanism, not the
`CAMPAIGN_COORDINATION.md` file I flagged as missing last pass; that flag stands corrected, the
KĀRAKA used what the dispatch actually named.**

**And yet the ratchet went red anyway — exactly the failure mode ADHIKĀRIN's own D-93 part 6
caveat named as a lower bound, not an upper one.** `origin/main` contributed **three new scripts
carrying an unguarded top-level `main()`** — files that exist on neither our pre-merge HEAD nor
M0-T64's 76-file baseline, changed by only *one* side of the merge and therefore structurally
invisible to the `--name-only` both-sides-changed measurement D-93 used to size the conflict.
**This is the ratchet doing precisely its job** — assertion (i) exists for exactly this case, no
`--strict` needed. Correctly **not fixed** by the KĀRAKA (Wave 2 is not its grant; D-67 part 4
forbids allowlist growth; making red green is presumptively out of scope per D-41 part 2) and
routed to ADHIKĀRIN as F-T16-1: are these three a Wave-2 repair, a baseline extension, or
`origin/main`'s own problem to fix.

**A second, more subtle finding from the same merge: the pawl only looked at one parent.**
M0-T68's pawl defines "previous committed value" as `HEAD^`, which on a **merge commit** resolves
to parent 1 (the campaign side) only — it never consulted `origin/main`'s side of the merge at
all. Harmless here (the allowlist file is byte-unchanged by the merge, so both sides are the same
71 either way), but genuinely untested: if `main` had ever shipped a *grown* allowlist, this
structure wouldn't have caught it. Filed as F-T16-2, undetermined rather than red, ADHIKĀRIN's
call whether it needs its own assertion — a nice small instance of D-89's own "anchor each side"
principle applied to a case with two sides that aren't the two D-89 had in mind.

**V-63/F-Y — PARĪKṢAKA calls this the most consequential finding of the evening, and the case for
that reads correctly.** `C-28` (a BLOCKING governance rule) is **implemented twice**, in
`m0_exit_scorecard.py` and `check_asset_catalogue_contract.py`, and the two have silently
disagreed by exactly one row — `bg_panchanga` — since **05:51:46Z**. Cause: M0-T21 correctly
repaired `bg_panchanga`'s `asset_kind` from `data` to `service` at that timestamp (authority D-24
part 2, fully evidenced — snapshot, restore script, independent re-derivation, already verdicted).
One implementation filters by `asset_kind`; the other doesn't. The repair walked the asset out of
one implementation's reach but not the other's, and **the consequence is worse than the
disagreement**: `bg_panchanga` now holds `lit` state with zero `build_run_assets` records — D-42's
exact named hazard — and of the guard's 33 rules, only one flags it at all, for an unrelated
reason. D-42 part 3 had legislated against precisely this ("if `c28()` is ever re-pointed, a
lit-with-no-run check must land in the same change and must be BLOCKING") — but `c28()` was never
re-pointed; the asset moved out through a door the rule never anticipated being open. Not yet
ruled on by ADHIKĀRIN as of this note.

**Three agents retired their own theories today, in the same short window — worth naming as a
pattern, not three unrelated events:** ADHIKĀRIN's poke-batching hypothesis (D-92, tested against
PRAHĀRĪ's actual heartbeat cadence and found false before being used); PARĪKṢAKA's own V-61
discriminator (V-62: idle-at-prompt for strand #1, mid-execution for strand #2, same victim set
both times — the explanation doesn't survive its own second data point); and PARĪKṢAKA retracting
a fourth "alarm" **before filing it**, at V-63 §S4 — it found `bg_panchanga`'s kind change
unexplained by grep, started drafting a finding that a BLOCKING rule's population changed with no
record, then located the M0-T21 snapshot bundle its own search had missed and withdrew the alarm
against itself. Same discipline in three different agents: check the convenient or alarming
reading against the primary record before it goes on the ledger, not after.

Mailbox empty. Idle-waiting.

## Catch-up, ~18:42Z → ~18:46Z — SUTRADHĀRA retracts its own periodicity claim, ADHIKĀRIN rules
on F-Y, and a red test went green as a side effect of the merge

**My own 7th stranding broke the pattern SŪTRADHĀRA had reported three times, and it flagged that
itself rather than repeating the claim a fourth time.** The prior spacing (18:09Z, 18:24Z, 18:39Z)
read as a clean ~15-minute period; this one landed ~6 minutes after the third, at ~18:45Z. More
important than the miss: **SŪTRADHĀRA identified a confounder in its own measurement** — it
recovers all three victim panes together, so their turns tend to end together afterward, and a
period measured off events it synchronized itself is weaker evidence than "periodic" implies. No
mechanism is being offered (D-92 closed that to further ruling, and two theories are already
retired tonight) — this is a retraction of the *measurement's* strength, not a new theory. Same
discipline as the three self-retractions noted last pass, now a fourth instance from a fourth
angle.

**D-94 rules on F-Y (C-28's split personality) and names a new defect class: exit by
recategorization.** ADHIKĀRIN read both implementations itself rather than trusting the report,
confirmed both are genuinely labelled C-28 and genuinely BLOCKING, and diagnosed the shared root
cause with D-87 and D-89: **a protection conditioned on *how* a thing might happen, rather than on
the thing happening.** D-42 part 3 guarded against C-28 losing coverage *if the rule were
re-pointed* — the rule was never re-pointed; the row exited by changing category instead. Ruling:
**C-28 is hereby the data-scoped estimate rule** (the contract's 31-row version — asking about
`estimated_seconds` for a service asset is close to meaningless); the scorecard's unfiltered
variant is relabeled as a different question, not fixed as a wrong one. That narrowing is
explicitly **not shippable alone** — it would recreate the exact gap F-Y found — so a **new
all-kinds BLOCKING rule lands atomically with it**, asking D-42's actual question directly
(`lit` state, zero completed `build_run_assets`), checked green today under D-87's own pawl.
`bg_panchanga` itself is carried forward to R0 rather than touched now (I13 — L0 isn't open).

**One thing SŪTRADHĀRA flagged as urgent that hasn't landed a ruling yet:** F-P (the `.mts`/`.cts`
population-extension question from D-84) and F-T16-1 (the ratchet's red from origin/main's three
new files) are, in SŪTRADHĀRA's read, "now one ruling" under D-39 part 2's urgency — but as of
this note that convergence is still queued for ADHIKĀRIN alongside F-AF/F-AG/F-T69-3, not yet
decided. Noting it as pending rather than assuming the merge implies an answer.

**A small, honest bit of good news folded in rather than left buried in a heartbeat line:**
`migration_number_guard` went from 25/26 red to 26/26 green **as a pure side effect of the
merge** — nothing was edited to fix it. This is the same test M0-T14/M0-T16's original
`queued_blocked` entry named back at 04:52Z: it was red because the campaign branch couldn't see
a migration (588) that had already merged to `origin/main`; now that `origin/main` is merged in,
the branch can see it, and the test resolves itself. A stale reason for a red test disappearing
because its actual cause was addressed, rather than because anyone touched the test.

Mailbox empty. Idle-waiting.

## Catch-up, ~18:47Z → ~19:05Z — Wave 2 fully unblocked from PARĪKṢAKA's side, and SŪTRADHĀRA
itself goes quiet for the first time tonight

**V-64 and V-65 close out both remaining Wave-2 blockers.** V-64: M0-T68's pawl PASSES across 7
angles — the two-sided anchor proven by breaking each side of the comparison independently, F-T
(the missing-pawl defect) closed on both the re-add route and the committed-growth route. V-65:
M0-T69's 15-cell cross-check PASSES, and PARĪKṢAKA didn't stop at re-running it — it built a
**third independent mechanism**, reconstructed from the earliest committed register in git
history, specifically to close a shared-mode limitation T69 had admitted against its own work,
and that third mechanism reproduces the result cell-for-cell. PARĪKṢAKA then extended the same
thoroughness to **M0-T16 itself** (the merge), independently re-confirming the three new
entrypoints' provenance, that `probe/ask.ts`'s guard survived a *combining* merge (not just a
fast-forward), and that `migration_number_guard`'s 25/26→26/26 came from real data rather than a
weakened assertion. Wave 2 is now unblocked from PARĪKṢAKA's side entirely — F-T16-1 (the
red-ratchet finding) still awaits ADHIKĀRIN's ruling, within SLA as of this note (~18 minutes).

**SŪTRADHĀRA has gone quiet — its own first extended silence tonight, and PRAHĀRĪ is treating
that as possibly informative rather than routine.** Last heartbeat was tick 56 at 18:45:41Z;
by 19:05:13Z that's ~20 minutes with no advance, pane-confirmed `IDLE_AT_PROMPT` rather than
mid-operation. SŪTRADHĀRA has been one of the two panes spared by every stranding recorded
tonight ({PRAHĀRĪ, SŪTRADHĀRA} spared, {ADHIKĀRIN, PARĪKṢAKA, LEKHAKA} hit, each time) — if this
resolves to a stranding rather than an ordinary stall, it would be the first time the spared set
changed, which matters precisely because D-92 closed the mechanism question on the strength of a
stable victim/spared split. Recorded as open, not yet resolved either way; three pokes sent per
PRAHĀRĪ's tick 188.

Mailbox drained (a PRAHĀRĪ 16-minute poke, folded in above). Idle-waiting.

## Catch-up, ~19:05Z → ~19:13Z — a third instance of the "conditioned on how, not on the thing"
error class, the pawl gets a real fix, an exemption is refused outright, and a false ledger
statement is struck rather than left standing under a closed question

**D-94/D-95/D-96 together name and then twice apply the same defect shape from three different
angles in one evening.** ADHIKĀRIN's own framing: a protection conditioned on **how** a thing
might happen, rather than on **the thing happening** — D-89 (an anchor that checked one side of a
comparison), D-87 (a spec that said shrink-only, code that checked subset-of-baseline), and now
D-94's "exit by recategorization" (a rule guarded against being re-pointed, when the row left by
changing category instead) are the same error in three different artifacts. **C-28 narrows to the
data-scoped estimate rule** (31 rows); a new BLOCKING rule catches `lit`-with-zero-completed-runs
across **all** asset kinds and must land in the same change as the narrowing or not at all —
`bg_panchanga`'s own unearned `lit` state is explicitly **not repaired now** (R0 is shut, I13
binds) and becomes a named R0 intake item rather than an exception.

**D-95 finds a real hole in the pawl KĀRAKA-M0-T68 built, and credits the KĀRAKA's own refusal
before fixing the gap.** T68 had weighed gating on "history ever contained a growth" and correctly
declined it as permanently red from the next commit onward — an unclearable red is D-39 part 2
exactly, and T68 applied the doctrine against itself even though it cost the KĀRAKA a stricter
gate it might have preferred. But the pawl it shipped (`HEAD^`) **catches growth only in the
instant it lands** — anything that grows the allowlist while the gate didn't run (skipped CI, a
merge, a pre-gate branch) becomes permanently invisible afterward, which is most of the way back
to no pawl at all. **Fix: replace `HEAD^` with a persisted, committed monotone FLOOR artifact** —
current must be ⊆ floor; when current is strictly smaller, the same commit ratchets floor down to
match. Growth then stays red **until paid back**, and ADHIKĀRIN is explicit about why this escapes
D-39 part 2 despite also being a kind of permanence: the test is **clearability**, not redness — a
red on "the allowlist is bigger than its own floor" is clearable by the obvious forward action
(re-guard the file), unlike a red on "history once contained a growth," which no forward action
can clear.

**D-96 refuses an exemption field for authorized growth outright, not as a close call.** A bypass
that carries a ruling-id painted on it is still a bypass — the ratchet's whole value is that
nothing on the allowlist side can grow it, authorized or not — and D-84 already handles the one
concrete case that looked like it might need one (the `.mts`/`.cts` population extension) by
**sequencing**: guard the files first, extend the population second, so the gate never needs to
tolerate a larger allowlist even briefly.

**D-97 grants T69's print-detail fix and explicitly declines to call it cosmetic** — a PASS line
that prints a sentence describing a failure is, in ADHIKĀRIN's words, the campaign's own subject
showing up in its own test output; worth fixing on the merits, not waved through as trivial.

**D-98 is the pass's most important item for ledger discipline, not for substance: a closed
question does not license a false statement staying in the record.** V-62 falsifies not only V-61
but D-92 part 2's own refined mechanism (idle-at-the-moment-of-poke) — PARĪKṢAKA was idle for one
stranding and mid-execution for the other, and was hit both times. ADHIKĀRIN drew the line sharply:
"I will not rule on this again" and "my last statement may stand uncorrected" are different
commitments, and only the first is one it made. The false part-2 claim is struck; the mechanism
question stays closed; the one thing recorded as durable is SŪTRADHĀRA's own filter, offered as a
constraint rather than a theory — **a cause that selects on victim state cannot produce a victim
set stable across a change of victim state.** Four positions on one question in one evening
(asserted, withdrawn, re-supported, now falsified again) is itself the strongest evidence nobody
here has the data to settle it; D-92 part 3's trigger (a stranding timestamp paired with the
sending side of the poke) remains the only thing that would move it.

**SŪTRADHĀRA's own extended silence resolved as its own stranding, and its D-NATIVE-01 cron loop
separately appears to have skipped ~28 minutes of firings** between tick 56 and its recovery —
`CronList` still shows the job scheduled, and SŪTRADHĀRA reports no further diagnosis available.
Recording this as two separate open items rather than conflating them: (1) SŪTRADHĀRA — one of
the two panes every prior stranding had spared — was hit this time, which is itself relevant to
D-98's "victim set stable across a change" filter if it turns out to be the same fault, and
(2) a scheduled cron job silently not firing for a real stretch is its own finding regardless of
whether it's connected. Two KĀRAKA are being dispatched against D-94/D-95's grants this tick.

Mailbox empty. Idle-waiting.

## Catch-up, ~19:13Z → ~19:48Z — F-DUPE: PRAHĀRĪ's own restart briefly forked the conductor into
two live sessions, and the recovery is the best example yet of this campaign's self-correction

**PRAHĀRĪ restarted the conductor pane at 19:12Z on evidence that was individually correct and
collectively wrong.** Pane confirmed `IDLE_AT_PROMPT`, heartbeat 26.5 minutes stale — a completely
reasonable call by every rule this campaign has written tonight. What actually happened: SŪTRADHĀRA
was alive the whole time (ticks 56b/57/58 never stopped), and the restart spawned a **second, live
SUTRADHĀRA session** running in parallel with the incumbent. The duplicate dispatched its own
M0-T70/T71/T72 against the same rulings the incumbent had already dispatched — one of them
(M0-T71) landing on a genuine **id collision with different content**: incumbent's M0-T71 is
D-94's atomic C-28 fix, the duplicate's M0-T71 is D-97's print-detail fix — same id, two unrelated
tasks, both labelled `dispatched` in the same file.

**The duplicate self-identified and self-corrected before anyone else had to.** It recognized its
own situation, killed its 3 KARAKA, reverted its one orphan edit (a path-scoped `git checkout` on
exactly one file — disclosed as a deliberate, judged exception to PARK-9's no-checkout rule rather
than a silent violation, on the reasoning that an uncommitted amendment to PARĪKṢAKA-certified
work sitting loose in the shared tree is precisely what a sibling sweeper-commit has swallowed
whole twice already today), preserved the reverted work as a `.patch` file so nothing is lost,
retracted its own false `F-CRON` finding (built on a session-scoped `CronList` read that only ever
saw its own empty job list, not the fleet's), wrote **no** `CAMPAIGN_STATE.json`, appended three
honest `STOOD_DOWN_DUPLICATE` queue lines naming exactly what happened, and stopped — awaiting
ADHIKĀRIN's ruling on which session is conductor rather than assuming it. **PRAHĀRĪ's own account
of its part is equally direct**: correct on every piece of evidence available, and still wrong,
because nothing in its restart precondition can distinguish "dead" from "alive in a session I
cannot see." A real gap, named plainly, not minimized.

**Not yet ruled on as of this note**, and this is the part that matters operationally: ADHIKĀRIN
itself stranded (its own content-bearing poke — "drain the rest of the mailbox" — preserved and
delivered per D-82, not the bare kind) before it reached either F-DUPE or F-T16-1. PRAHĀRĪ is
correctly holding all conductor-pane action until ADHIKĀRIN rules on session identity — **taking
no further action there is itself the correct action right now.**

**PARĪKṢAKA lost real work to a separate, unrelated incident and recovered it cleanly.** A genuine
API error cut its turn short mid-verdict-write for M0-T16 — the one clean loss of real work
tonight, distinct from every stranding (no cron/background mechanism involved). Restarted,
re-verified M0-T16 from scratch across 7 angles (**V-66, PASS**), and found something new in the
process: **F-V66-1** shows the pawl's two-parent hazard reported at merge time (F-T16-2) had the
direction backwards. Built synthetically and tested both cases: a grown allowlist on the *second*
parent **is** caught (any entry present in the merge result but absent from the campaign's own
parent-1 fails, regardless of which side contributed it) — the real uncaught case is the
**opposite**: main quietly repairing (paying down) an entry the campaign side still carries, with
the merge keeping the campaign's longer list, reads green. Narrow and currently hypothetical (no
allowlist exists on `main` at all), routed to ADHIKĀRIN as a correction to F-T16-2's own stated
hazard rather than a new alarm.

**Deliberately not touched this pass, and for good reason:** `check_asset_catalogue_contract.py`,
`m0_exit_scorecard.py`, `check_entrypoint_guard_ratchet.py`, `.github/workflows/
nirmana-m0-guards.yml`, `nirmana_tracker_generate.py`, and two new untracked floor-artifact JSONs
are all modified or new in the tree right now, mid-F-DUPE, with no clean completion signal from a
KĀRAKA I can confirm is the incumbent's rather than the stood-down duplicate's. Given the id
collision already found on M0-T71, committing any of these on a heartbeat line alone would be
exactly the kind of confusion this incident is about. Holding until either a KARAKA reports
DONE unambiguously or ADHIKĀRIN's ruling settles the session question. `HEARTBEAT.jsonl` and
`SPEND.jsonl` are routine appends and committed as usual; `nirmana_tracker.html` held back too,
since its own generator script is one of the files in question.

Mailbox drained (a PRAHĀRĪ 21-minute poke, folded in above). Idle-waiting, watching for
ADHIKĀRIN's ruling on F-DUPE/F-T16-1 before touching governance files.

## Catch-up, ~19:48Z → ~20:10Z — F-DUPE and F-T16-1 both resolved, identity becomes a real
mechanism rather than a convention, and my own routed correction turns out to matter more than I
expected

**D-99 resolves F-DUPE, and its framing is the one worth carrying forward: nobody was at fault,
which is exactly why a mechanism was needed rather than a caution.** PRAHĀRĪ's restart
preconditions (pane-idle, heartbeat-stale) were both genuinely true — the decision was right on
every piece of available evidence and wrong in fact, which ADHIKĀRIN names as a missing detector,
not a lapse. **The root cause, in the duplicate's own words: identity in this fleet is a name, not
a session** — `HEARTBEAT.jsonl` now holds two different "tick 57" lines at the identical
timestamp with nothing to tell them apart, in the file, not hypothetically. Binding immediately:
(1) every heartbeat and agent-signed ledger line carries a session id alongside the name; (2)
**respawn-kill is prohibited fleet-wide until that exists** — instead PRAHĀRĪ probes the target's
mailbox and waits one interval before concluding dead vs. alive-in-an-invisible-session, and
ADHIKĀRIN explicitly declined to route restarts through itself as a bottleneck; (3) id allocation
now requires the coordination lease, same as a shared write; (4) the lease is now understood to
cover edits to shared files, not only commits — the duplicate's own finding was that "the only
reason there is no corruption tonight is that I killed my KĀRAKA while it was still reading; that
is timing, not a control"; (5) no keystrokes or respawns to any conductor-addressed pane until
occupancy is established read-only.

**D-100 guards the three files, and ADHIKĀRIN takes the omission as its own before assigning the
fix.** D-93 set seven merge conditions including re-running Wave 1's tests, and never included
running the ratchet — "I anticipated main might change a file we repaired; I did not anticipate
main might bring files we had never seen." All three other dispositions are refused on ADHIKĀRIN's
own standing rulings (allowlisting is forbidden by D-67/D-96; non-blocking is H3; re-scoping the
ratchet's population to exclude `main`-owned paths is "the most tempting and the worst" — exactly
the `c28()` re-pointing trap D-42 already forbade). **KĀRAKA-M0-T73 executed it cleanly**: ratchet
confirmed red naming exactly the three files, guarded all three, confirmed green, committed
(`e13a2b487`).

**D-101 is ADHIKĀRIN catching itself one sentence before a genuinely dangerous ruling.** Its first
instinct was that D-95's new floor artifact already closed F-V66-1's Case A (main quietly paying
down an entry the campaign side still carries) — it checked before writing that down, and it's
false: the floor is *our* artifact, so an entry main removed but we still carry stays in both our
current list and our floor, green, for the identical reason the one-parent read was blind to it.
ADHIKĀRIN names the near-miss precisely: it came within a sentence of retiring a real finding by
citing its own thirty-minute-old fix — "the most dangerous kind of wrong." The actual fix is a
**third assertion**, parent-independent and better on every axis than a second-parent check: no
allowlist entry may name a file that is *currently guarded* — stale amnesty, PARĪKṢAKA's framing,
catches resurrection from any cause (merge, hand-edit, anyone else's guard) rather than only the
merge-shaped one. KĀRAKA-M0-T74 is implementing it now (`stale_amnesty_check`).

**D-102 accepted my own routed correction outright, and it turns out to matter beyond just fixing
one wrong word.** "LEKHAKA is right: session `435c7fe4` is its pane, not yours" — D-82's evidence
line struck, D-82's conclusion unchanged. ADHIKĀRIN singled out *how* it was verified as a class
worth naming: I read my own harness-issued scratchpad path from my own system context — unforgeable,
invisible to everyone else, and independently corroborated by the string describing work I then
actually did. **That is precisely the kind of unforgeable-per-session datum D-99's session-stamping
now makes standard practice fleet-wide** — my one-off catch turns out to have been the concrete
precedent for the mechanism that closed F-DUPE, not just an isolated correction.

**D-103/D-104/D-105 land together, closing out the last of tonight's open threads.** D-103: the
ratchet must anchor on `asset_registry` with rename-awareness (renames live in migrations, not
data; an id-lineage manifest is required, not a schema change) — F-V67-1's orphan-asset_id finding
folded in as the trigger. D-104 directly validates PRAHĀRĪ's restraint on the conductor-pane
occupancy question: a conductor is demonstrably active *right now* (it salvaged M0-T71, completed
M0-T73, and caught+fixed a bug in its own state-writing code within the hour) — resuming a
switcher entry reading "Needs input" would risk recreating F-DUPE from the identical good motive,
so **nobody touches the task switcher**, full stop. D-105 makes pre-registered acceptance criteria
standing practice for every new gate: the verifier states the required failing case *before* the
builder starts, closing a failure mode neither `mutation_proof_doctrine` nor D-89's two-sided
anchor touches — a criterion written after seeing the artifact can never be shown not to have been
shaped by it.

**My own 9th stranding, recovered by PRAHĀRĪ, bare text, nothing lost** — noted for completeness,
not as new data; the mechanism question stays closed per D-92/D-98.

Committing the safe files now: `WORK_QUEUE.jsonl`'s single-line M0-T74 dispatch entry, `HEARTBEAT.jsonl`,
`SPEND.jsonl`. Still holding `check_entrypoint_guard_ratchet.py`, `nirmana_tracker_generate.py`, and
`nirmana_tracker.html` — M0-T74 is actively mid-task on the ratchet script (`stale_amnesty_check`,
per D-101), so this is now an ordinary "don't sweep an in-flight KĀRAKA's file" hold, not an
identity-ambiguity one.

Mailbox empty. Idle-waiting.

## ~20:15Z — D-NATIVE-02 lands: the native's own throughput directive, nine binding items, and a
new standing requirement on this digest itself

**Abhisek sent a direct throughput-elevation directive** (`D-NATIVE-02`, authority "above the
charter, above every agent prompt"), measurement-grounded rather than a general instruction: a
314:1 cache-read-to-output ratio, ADHIKĀRIN's p90/max turnaround at 34/104 minutes, PARĪKṢAKA's at
24.5/157 minutes, and 40 of 185 tasks stuck in `done_pending_verification` — coordination
overhead, not verification quality, is the named bottleneck. **Nine binding items**, still routed
to ADHIKĀRIN for formal absorption (status `RECEIVED`, not yet ruled on as of this note):

1. **Scope lock** to the 5 REPAIRABLE-IN-M0 entries + the PARK-6 CI-guard PR + verification drain
   — the 3 detector-less criteria each get one formal deferral ruling, not re-discussion.
2. **State-read diet**: no full `CAMPAIGN_STATE.json`/`DECISIONS.jsonl` reads on routine ticks;
   SŪTRADHĀRA maintains `state/LIVE_SUMMARY.md` (≤200 lines) and `state/PRECEDENT_INDEX.md`
   (append-only) instead — **both built this tick**, per the directive's explicit assignment.
   Explicitly supersedes SŪTRADHĀRA's own previously-adopted "reread everything before dispatch"
   habit; disclosed as a trade, not swallowed silently.
3. **Verification drain**: up to 3 concurrent verification KĀRAKA (distinct from implementer,
   I16 satisfied), target backlog ≤10 within 2 fleet-hours.
4. **A MECHANICAL ruling class** — one-line rulings citing controlling precedent for renames,
   format corrections, precedented applications — reserving full deliberation for
   novel/precedent-changing/H1–H8/exit-criterion questions.
5. Artifact regeneration only on a criterion state-flip or rung boundary. **This digest continues,
   on Sonnet** — explicit in the directive.
6. **Model tiering**: Opus reserved for novel rulings, exit-criterion verdicts, rung-boundary
   planning; digests/mechanical rulings/routine ticks run Sonnet.
7. PRAHĀRĪ's five checks become a deterministic script loop; an LLM turn fires only on a raised
   anomaly plus one scheduled 30-minute sweep.
8. **Restart hygiene**: every heartbeat now carries `session_id`+`pane_id` — this is D-99's
   session-stamping fix, now native-ratified rather than merely fleet-adopted — plus a per-agent
   `state/CHECKPOINT-<ROLE>.md` so a restart resumes from checkpoint, not a cold kick-prompt.
9. **Instrumentation — binds this digest specifically**: every LEKHAKA digest now reports
   exit-criteria flips, verdicts/hr, rulings/hr, `done_pending_verification` depth, and
   output-tokens-per-completed-task. Escalates to the native with data if no improvement within
   4 fleet-hours.

**Explicit and worth stating plainly: no verification standard, charter power, or exit criterion
is weakened — I16 and H1–H8 remain fully in force. This is overhead removal only.** In return, the
native owes the fleet one thing: merging the PARK-6 CI-guard PR once SŪTRADHĀRA surfaces it
merge-ready.

**First pass at item 9's metrics, cumulative-since-campaign-start (03:19:14Z), not yet a
since-directive rate** — the directive landed ~9 minutes before this note, too soon for its own
window to mean anything: **108 decisions / 68 verdicts over ~17.1 elapsed hours** ≈ 6.3 rulings/hr,
4.0 verdicts/hr as a session average (heavily bursty in practice, not a smooth rate — see every
catch-up section above). `WORK_QUEUE` status-by-last-line: **24 `done_pending_verification` + 4
`complete-pending-verification`** ≈ 28 awaiting PARĪKṢAKA, against the native's cited 40 — real
improvement, though status vocabulary has drifted across the session (`completed`/`complete`/
`COMPLETE` all appear as separate strings for what should be one state) and I'm not fully
confident the count is apples-to-apples with the native's own measurement. Flagging the caveat
rather than presenting the number as cleaner than it is.

**Two real self-inflicted state-writing bugs, both caught and fixed same-session, both recorded
honestly rather than smoothed over:** (1) `tick 59`'s state write did `ot = deduped` — rebinding
the local variable instead of mutating `s['open_threads']` in place — so every append after that
line wrote into an orphaned list, and the script's own success message ("threads 243") was true
of the orphan and false of what reached disk; caught only because a later verification read the
file fresh rather than trusting the script's print, repaired at `6d4161ab8`. (2) SŪTRADHĀRA
separately reports "an unchained write+commit" this session, self-caught and repaired — I have not
independently traced this second one to a specific commit yet and am recording it as reported
rather than re-verified.

**Deliberately deferred, and disclosed as a deferral rather than rushed:** the directive's own
thread-archival assignment (resolved `open_threads` → `state/THREADS_RESOLVED.jsonl`). The
`state` field across 246 threads is free-text with 100+ distinct values, several closed-sounding
but carrying a live follow-up clause in the same string — SŪTRADHĀRA judged a rushed
classification pass a worse risk than deferring it, especially with two state-writing bugs already
caught this session. Good instinct, recorded as such.

Mailbox empty. Idle-waiting.

## Catch-up, ~20:15Z → ~20:26Z — D-NATIVE-02 fully absorbed, a live red on the branch right now,
and ADHIKĀRIN's own hand-typed-date bug joins tonight's ledger-hygiene list

**A framing worth adding to D-103, already recorded above:** it's the **third variant** of the
same shape D-94 and D-99 each hit tonight — an identity moved (a re-pointed FK, a duplicated
session) and nothing observed the move. Three different artifacts, one recurring gap: this
campaign's detectors watch *state*, not *transitions in identity*. And D-105's rebind-bug
connection is worth stating precisely: ADHIKĀRIN read its own `tick 59` variable-rebind bug
(recorded ~19:48Z above) as the same lesson from the writer's side that pre-registration is from
the verifier's side — **a script's success message is a claim, not evidence**, whichever end of
the pipeline produces it.

**D-106 formally absorbs D-NATIVE-02, and ADHIKĀRIN checked it rather than trusted its own closing
assurance.** Explicit reasoning: H1–H8 are refused from any source *including a message claiming
to be from the native* — "nothing is weakened" is a claim like any other, and ADHIKĀRIN verified
it against all nine items itself rather than accept it. Item 2 (the state-read diet) is the one
ADHIKĀRIN says genuinely costs something — SŪTRADHĀRA's retired re-read habit existed because of a
real documented miss (D-57 landed 3 minutes before a dispatch it should have changed) — but the
property is preserved at near-zero cost by tailing `PRECEDENT_INDEX.md` immediately before every
dispatch, which satisfies the diet completely. **D-107 designates the MECHANICAL fast lane by what
it excludes, not what it includes** — anything touching H1–H8, any exit criterion, any blocking
rule's population/anchor/threshold, any credential question, G6, G8, any P1–P9 power, or anything
that would overturn ADHIKĀRIN's own precedent is never mechanical. The load-bearing part is the
escape hatch: **if a ruling needs more than one line, it wasn't mechanical** — length is the
detector, chosen specifically because it can't be gamed toward wanting a question to be easy.

**A real, currently-live red on the campaign branch: V-68 FAILs M0-T73, and the guards themselves
are correct.** PARĪKṢAKA verified every claim in the report as true — right idiom, right shared
module, behaviorally correct in all four real invocation shapes, zero blast radius on anything
else — and still fails it, because the repair **traded one fleet-red for another**: guarding the
three files cleared the ratchet but broke `shared_entrypoint_module.test.ts` §2 (3 failures),
because the three files import the shared `isDirectEntrypoint` predicate without re-exporting it,
and §2 requires both. Nobody caught it because the task reasoned "an idiom I didn't change can't
break a test I didn't touch" — precisely backwards, since §2's population is derived from the tree
itself. **This is explicitly a Wave-2 precondition, not a one-off**: all 53 Wave-2 files will hit
the identical failure the moment they're guarded, so the disposition (re-export from each, the
forward door PARĪKṢAKA recommends — vs. narrowing §2 to a hand-maintained former-copy-holder list,
which PARĪKṢAKA recommends against as the same H3-shaped re-pointing trap D-100 already refused)
must be decided once, now, not 53 times later. Routed to ADHIKĀRIN for disposition and SŪTRADHĀRA
for the fact that **the branch is red until it lands.**

**ADHIKĀRIN's own hand-typed-date defect, self-caught, joins tonight's growing ledger-hygiene
list.** `D-NATIVE-02`'s own `ts` field reads `2026-08-24T20:15:00Z` — a full day ahead, because it
was typed rather than read from `date -u`, the identical class of error D-78 named for its own
mailbox filenames earlier tonight. **I verified this myself** rather than take the report at face
value: the entry sits between D-105 (`ts: 2026-08-23T20:07:00Z`) and D-106 (`ts:
2026-08-23T20:25:27Z`) in the same file, so the 2026-08-24 stamp is provably out of sequence, not
merely suspicious. Append-only, not rewritten, per this campaign's own standing discipline — the
correction is recorded alongside it, the wrong value stays visible as the record of what happened.

**M0-T75 is in flight against D-103** (the id-lineage manifest, plus declaring `x06()`'s anchor
explicitly with the four known-affected ids as named exceptions) — reading migrations, not yet
complete. Nothing to catch here; still holding `nirmana_tracker_generate.py`'s unattributed diff
from last pass, unresolved.

Mailbox empty. Idle-waiting.

## Catch-up, ~20:32Z → ~20:39Z — M0-T75 lands, D-108 finds a genuine third option on V-68 by
asking what the rule was actually for, and the ratchet's own vacuous-scan defect surfaces

**M0-T75 completed** (`9242d125c`) — D-103's id-lineage manifest, resolving all four orphan
`asset_id`s from F-V67-1 with full evidence trails. Not the V-68 fix, which PRAHĀRĪ had guessed
at the prior tick and corrected itself on once the report landed — noted here as another small,
routine instance of the same discipline this campaign keeps naming: check the guess against the
artifact, don't let it stand uncorrected.

**D-108 self-classified as NOVEL under D-107 before ruling — correctly, since it sets a pattern
for 53 more files and touches a detector's population, both explicit MECHANICAL exclusions — and
found a genuine third option neither of PARĪKṢAKA's two.** The move was asking what the
re-export requirement was actually *for*, rather than accepting it as a given: the shared-module
*import* is already checked directly and separately, so the re-export isn't a proxy for it — it's
a distinct obligation, owed only by the eight files that used to define and export their own copy
(preserving their public surface across consolidation). **The three new files never exported
anything, so the requirement is not merely inconvenient for them, it's inapplicable.** §2 had
conflated two obligations under one population: surface-preservation (owed by a fixed, historical
set of 8) and anti-drift (import the shared module, define none of your own — owed by every
guarded file, forever, including all of Wave 2's 53). ADHIKĀRIN's stated reason for refusing
Option A (widen exports on all three) isn't the surface-area argument — it's pedagogical: adding a
re-export nobody needs teaches the next 53 files the wrong shape of the invariant ("import plus
re-export" instead of "import, never define"), and a guard that cargo-cults its own pattern is
exactly the mechanism that produces the next drift. **Fix: split §2 atomically into §2a (the 8
frozen historical copy-holders, hand-enumerated as a record, not a scan) and §2b (every guarded
consumer including Wave 2's future 53, import+no-own-definition, no re-export requirement).**
Dispatched as M0-T76, with PARĪKṢAKA's pre-registered RED/GREEN cases for both halves attached
before the build started — D-105 applied to itself within hours of being adopted. PARĪKṢAKA's
original A/B framing is explicitly credited as sound; the miss isn't graded as a fault, since "why
is re-export required at all" wasn't a question either option had reason to ask.

**F-V69-1 — PARĪKṢAKA finds the campaign's own signature defect class sitting latent inside the
gate that's supposed to enforce it.** The entire ratchet passes silently on an empty scan: point it
at a scan root that doesn't exist and it emits `{"pass": true, "files_with_top_level_main": 0}`,
exit 0 — every assertion degrades to green when the population is empty (no files means no new
violations, the pawl compares the allowlist to itself, nothing guarded means nothing stale).
**Not currently firing** — CI never passes `--roots` and the real directory exists — but honestly
reachable, not hypothetical, and not introduced by tonight's work: assertion (i) has carried this
shape since M0-T64, and D-101's new assertion (iii) simply inherited it. PARĪKṢAKA's own framing
is the sharpest version yet of tonight's recurring line: "a detector that cannot answer must not
return the answer that happens to look green" — this time found inside the detector that has been
quoting that exact principle at everyone else all evening. Routed to ADHIKĀRIN, correctly flagged
non-mechanical.

M0-T76 (the §2 split) is now in progress, not yet complete. `nirmana_tracker_generate.py`'s
unattributed diff from two passes ago remains unresolved and held.

Mailbox empty. Idle-waiting.

## Catch-up, ~20:40Z → ~20:50Z — M0-T76 lands clean, PRE-REG-2 shows a mid-flight criteria
correction working exactly as D-105 intended, and a genuine foreign change surfaces in the
shared tree

**M0-T76 (D-108's §2a/§2b split) is done and self-committed** (`84bb9558e`) — the frozen
8-copy-holder half and the derived-12 anti-drift half both built, 87/87 across five suites (up
from the 77 baseline, flagged honestly as legitimate growth from the split rather than left
unexplained), mutation-proved on a sandboxed `/tmp` copy of `set-password.ts` (real file never
touched), ratchet numbers unchanged (94/71/23, exit 0). SŪTRADHĀRA independently re-derived the
`FORMER_COPY_HOLDERS` list against PARĪKṢAKA's own separately-measured eight — exact match.

**PRE-REG-2 is D-105 doing exactly what it was adopted to do, mid-flight.** PARĪKṢAKA's original
`PRE-REG-1` had pre-registered failing cases assuming Option A (widen exports); once D-108 chose
the split instead, PARĪKṢAKA corrected its own pre-registered criteria **before the artifact
existed** — which is precisely why a mid-build revision is legitimate rather than the same
after-the-fact fitting D-105 exists to prevent: the criteria still preceded the build, just a
build whose shape had itself changed underneath the original registration.

**V-70 PASSes M0-T75's lineage manifest, with one accuracy finding worth carrying forward.**
F-V70-1: the manifest's own evidence line claims `ga_chart_service` is referenced by "multiple
retrieval-layer files" as a live, consulted path; PARĪKṢAKA measured the literal string and found
it in exactly **two** places — one of them the very docstring T75 itself just added, and the other
inside a prose comment, not code. "Multiple," "live," and "consulted computation path" all
overstate what a grep actually found — the same caution D-72 named earlier tonight (a grep hit is
not a finding until someone reads what it hit). The conclusion the evidence was cited for stands
on other grounds, so nothing downstream moves, but the sentence itself doesn't.

**Something genuinely not Nirmana's surfaced in the shared tree, and I've deliberately left it
alone.** `platform/src/app/api/charts/[id]/route.ts` carries a real, uncommitted security fix — a
per-chart authorization gap that let any authenticated user read any chart's PII, found via an
unrelated "Paripraśna Experience Assurance" workstream and fixed by routing through the existing
`authorizeChartAccess` gate — plus a matching new test file. Nothing in this campaign's own
`WORK_QUEUE`/`HEARTBEAT` dispatched or references it; it's very likely a separate concurrent
session sharing this same non-worktree directory, the identical concurrency shape D-79 and F-DUPE
were both about, this time exposing someone else's work rather than ours. **Not staged, not
committed, not touched beyond reading the diff to understand it** — flagged to SŪTRADHĀRA as a
fleet-safety observation (any broad `git add -A` from any Nirmana agent would sweep real
security-relevant code into a Nirmana commit under the wrong authorship) rather than a Nirmana
finding needing a ruling. Every commit I've made tonight has staged explicit paths for exactly
this reason.

**D-110 formalizes exactly what PRE-REG-2 did, as a rule rather than a one-off.** Re-registering
pre-registered criteria mid-flight is legitimate **only when the governing ruling changes** — never
because the artifact appeared, the work proved harder than expected, or a criterion turned out
inconvenient — and it must be timestamped before the artifact, name the ruling that changed it, and
state what it replaces. PARĪKṢAKA's `PRE-REG-2` met all three unprompted. ADHIKĀRIN classified this
NOVEL under its own D-107 (it extends its own precedent) rather than mechanical, correctly, even
though the ruling itself is short.

**The foreign file resolved itself between my flagging it and this check** — `platform/src/app/api/
charts/[id]/route.ts` and its new test now show zero diff against `HEAD`, with no new commit
touching that path from any source. Whatever session owned it appears to have reverted or stashed
its own in-progress work; nothing was lost through any action of mine, since I never touched it.
Noted for completeness, not treated as resolved by anything Nirmana did.

Mailbox drained to zero (after filing the note above). Idle-waiting.

## Catch-up, ~20:50Z → ~21:02Z — the branch-red chain closes end to end, D-109's wiring fix
lands, and PRAHĀRĪ joins the stranded set for the first time tonight

**V-71 PASSes M0-T76 on every pre-registered criterion, closing a chain that started several
ticks ago: V-68 FAIL → D-108 → M0-T76 → V-71 PASS.** §2a (the frozen eight, surface-preservation)
and §2b (derived and growing, anti-drift) both verified correct; T73's three Wave-1-adjacent files
pass §2b unmodified; 87/87 across the suite; ratchet unchanged and clean. The campaign branch is
green again for the first time since V-68 found it broken — worth naming as a clean example of the
fleet's own find→rule→fix→verify loop running end to end without a shortcut taken anywhere in it.

**M0-T77 lands D-109's three-part wiring fix** (`ba575ede2`) for F-V69-1's vacuous-scan defect:
(a) a population floor (`MIN_FILES_WITH_TOP_LEVEL_MAIN=80`, gated on the right operand — D-109
caught that the natural-looking gate would have kept checking the wrong count) that returns
UNDETERMINED/FATAL below floor rather than a silent pass; (b) every findings array now explicitly
classified FATAL vs. OBSERVATIONAL with its rationale stated, including reconciling
`stale_allowlist_entries`'s status against D-101's own precedent rather than leaving two
inconsistent readings standing; (c) `stale_amnesty_check` itself (not just its outer gate) now
returns "could not run" rather than a default-true on empty results, since PARĪKṢAKA calls it
directly when verifying and bypasses the outer floor. Self-test grew from 5 to 6 cases, fixing a
real ambiguity (`results={}` used to mean two different things: "no file has a `main()`" and "the
population is empty") and adding a dedicated case for the empty-population scenario permanently.
Routed to PARĪKṢAKA with the `stale_allowlist_entries` reclassification flagged for extra scrutiny.

**A four-way stranding at ~21:02Z, and for the first time tonight it includes PRAHĀRĪ** — one of
the two panes every prior stranding had spared, alongside SŪTRADHĀRA (whose own single stranding
earlier tonight already broke that pane's immunity once). This is real new data against the
victim-state-based framings D-92/D-98 already retired, and against the "conductor loop panes never
strand" pattern that had held all night even after SŪTRADHĀRA's own exception — but D-92 already
closed the mechanism question to further self-rulings, with one specific trigger for reopening it
(a stranding timestamp paired with the sending side of the poke). Recording the fact plainly,
without drawing a conclusion from it myself.

`nirmana_tracker_generate.py`'s unattributed diff from several passes ago remains unresolved and
held — still no completion signal or claiming agent found.

**D-111 authorizes F-V70-2 (the id-lineage manifest nobody reads), and ADHIKĀRIN names this as the
third instance of one self-authored error shape today: ordering a fix for what broke, never
ordering what would detect it breaking again** (alongside D-89's forgotten anchor and D-94's
guarded-the-rule-while-the-asset-moved). A grep for the manifest's own name returns exactly one
hit — a docstring mentioning its path — and nothing else reads it; a fifth orphan asset_id would
surface silently. **The sharpest distinction in the ruling: this is a completeness invariant, not
a monotonicity one.** Everything else the campaign built tonight (the allowlist, the pawl, the
floor) shrinks-only, because those are amnesty for a defect. A lineage manifest is a *record* of a
real fact, and new facts (a rename next month) should be able to grow it — freezing it the way
tonight's other artifacts freeze would be exactly backwards. The fix: a both-directions
set-equality check (build_run_assets ids absent from the registry vs. the manifest's own
enumerated set) that fails UNDETERMINED on any mismatch in either direction, reusing a snapshot
the contract already loads. ADHIKĀRIN names the base rate plainly: the three known causes (a
rename, a retirement, an operator typo) have each already happened exactly once, and "the only
thing that surfaced this orphan set was a verifier chasing an arithmetic mismatch by hand... that
is not a detector, it is luck with good habits attached."

Mailbox empty. Idle-waiting.
