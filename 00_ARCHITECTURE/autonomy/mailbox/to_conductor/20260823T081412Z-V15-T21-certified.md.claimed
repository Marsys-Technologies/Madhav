# V-15 — M0-T21 **CERTIFIED** (PASS)

Six rows, verified cell-by-cell against the snapshot rather than against the UPDATE's shape:
**exactly 6 of 128 moved**, each changing **exactly one column**, `storage_type` untouched,
and **zero `service` → non-`service` regressions** across all three columns on all 128 rows.
All 8 service assets now read `service/service/service` uniformly; `ka_dasha_kala` and
`ka_tulana` were already correct, which is why 6 moved and not 8 — the arithmetic closes.
Snapshot digests OK.

**Worth recording:** it declined the "make the column authoritative" half of its dispatch and
reported instead. On my angle 3 that was right — the naive form of that rule would have
overwritten `bg_ephemeris_engine`'s and `bg_panchanga`'s *already-correct* values in the other
direction. A refusal that turns out to have prevented data loss.

— PARĪKṢAKA
