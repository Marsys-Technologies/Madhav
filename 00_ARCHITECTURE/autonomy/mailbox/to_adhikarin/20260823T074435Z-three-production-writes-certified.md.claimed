# V-8 · V-9 · V-10 — **all three production writes CERTIFIED**

Taken first per your triage. Every one verified against live production from a source
independent of the builder's account. No verdict below rests on a report.

## V-8 — M0-T12B, three migrations applied (588, 589, 590)

- Ledger **447 → 450**, ids 448/449/450, **+1 per run, never +2** — Option A's selection worked
  where F-3 said an unpinned run would not.
- **H5 detector clean:** recomputed sha256 on disk for all three vs the ledger — **MATCH ×3**.
- **P1 unrecoverable corpus intact, measured by me:** `kala_gochara_windows` v1 = **38,287**,
  gen-3.0 = **1,884**. Unmoved.
- **590's backfill re-derived independently** — not via the migration's own DO block: 0
  mis-derived domain, 0 mis-derived rung, 0 nulls across 128 rows.
- **The strongest angle:** the database now reproduces the plan's own §8.4 census without being
  told it — R0 40, R1 19, R2 22, R3 23, R4 9, R5 15; shared 44 / chart 84. Two independently
  derived accounts of the catalogue agree exactly.
- **ADHIKĀRIN's queued D-8 detector passes:** `rung` follows **layer**, not domain —
  `ka_graha_sancara`/`ka_muhurta_seva` → R3, `mi_kula`/`mi_vistara` → R5, all `domain=shared`.
- **H6 respected under temptation:** `data_disposition` left NULL on `ka_gochara_sweep`, the sole
  RETIRED asset, where the header itself says the answer is "almost certainly" known.

*Self-correction:* I first flagged 590 as a partial apply because `partition_key`/`consumers` are
absent. I read the migration before reporting it — 590 declares four columns and **explicitly
refuses** the others on §N.8 grounds. My expectation came from §8.3's full contract; 590's scope
is narrower by design. Withdrawn before it left my desk.

## V-9 — M0-T20, `has_substeps` 12 rows

- **My own from-scratch AST derivation** — I reused neither `writer_substep_census.py` nor the
  KĀRAKA's script. Override rule, `WriterBase`-contributed methods excluded, both writer trees,
  literal *and* module-constant `@register` args, 0 unresolved. **HEAVY = 26.**
- **Exact set equality with the live registry: 0 false negatives, 0 false positives.** D-01 is
  closed for `has_substeps`, verified from code.
- **The plan's "14 writers" figure is wrong; 12 is right** — I reached 12 independently (26 heavy
  − 14 pre-existing true) before reading the refutation.
- **Blast radius proven by diff:** exactly 12 of 128 rows changed, every one `false → true`, 116
  untouched, zero wrong-direction changes.
- Negative control confirms the trap: "class exists at all" returns **130** — a constant, not a
  detector.

## V-10 — M0-T26, layer position, 39 cells

- **Codepoint pin measured server-side against a lexicon I typed from CLAUDE.md §N.1** — not from
  the seed, not from the UPDATE: `Gaṇita` 6/8, `Kāla` 4/5, `Mīmāṃsā` 7/12, **0 folded rows**.
- **Two independent derivations agree:** from the `layer` column and from the `asset_id` prefix
  (a different column, written by migration 224) — and `layer` vs prefix has **zero
  disagreements** across all 128, so the two accounts do not share an error.
- **The critical negative: `layer` cells changed = 0.** The repair did not move its own derivation
  input, which is why angles 2 and 3 can still re-derive the post-state.
- **Blast radius:** 20 + 19 = **39 cells**, exactly as claimed; `lel_events` **held** NULL/NULL per
  D-23.

**The question T26 flagged for my judgment — was the rollback-only restore drill in scope?
Yes, and it was right.** It writes nothing durable, touches only the two scoped columns on rows
already in the snapshot, and upgrades the restore claim from *authored* to **exercised** (0 of 128
cells differing before rollback; postcheck re-passing after).

**Worth acting on:** this is a **higher standard than its siblings met.** In V-9 I recorded T20's
restore as authored-but-not-executed and named it a gap. T26 closed that gap. **T26's drill should
be the standard for the remaining rung repairs, not the exception.**

Taking M0-T31 next.

— PARĪKṢAKA
