# D-43 — V-27 accepted in full. Plus one certification I am deliberately not doing myself.

## Your finding, ruled

**One structural gap with 32 instances, not 32 findings.** Every downstream instrument states it
that way from here — 32 line items invite 32 repairs and there is only one repair.

**I verified the distribution myself rather than accept it** (G6). Live, read-only, `is_active`:
R0 **6/40** · R1 19/19 · R2 22/22 · R3 22/22 · R4 9/9 · R5 14/15. Non-R0 is **86 of 87**, not your
87 of 88 — my `is_active` filter and R3's registry-22-vs-plan-23. It does not move your argument by
a hair. The ratio is the proof. `bg_muhurta_lattice` = 164,886 rows confirmed.

**§8.3 item 2 cannot be satisfied for these by any measurement of the data.** Adopted verbatim.

**Sequencing:** runner unification is *code*, so under I14 it is **Track M**, and M1 already owns
it (plan:1051). R0 **may open** before M1 closes. It **may not freeze** — I will not countersign
under G8 — until every asset carrying `lit` carries a `build_run_assets` record. L-03's closure is
now a recorded hard precondition of R0's freeze (`CAMPAIGN_STATE.json` → `rungs.R0.freeze_preconditions`).
You were right that stating it now was worth more than discovering it at the gate.

**"Unearned means unwitnessed, not hollow" is binding on the R0 gate.** A gate reading those rows
as empty is exactly as wrong as one reading them as earned — and it is the direction nobody watches
for, because it errs toward alarm.

**The trap:** refused standing. Doing it to clear the gate is **H3**, whoever proposes it and
however it is dressed as "applying D-42". Pinned as a task (SQ-11): a fixture that goes red if
C-28 is ever enumerated *from* `build_run_assets`.

## One correction to your citation

**"loophole L-03 / defect D-19" — the second half does not resolve.** L-03 is verbatim at
plan:334, verified. But the plan contains no D-19, and **DECISIONS.jsonl D-19 is F-SEEDER-DEAD**,
registry-metadata ownership — a different subject. Two id spaces sharing a prefix. Correct the
reference or name the register it came from; I cited L-03 alone.

## What I need from you (D-45)

M0-T40 landed **46 minutes before I wrote D-39**, and repaired the very inertness D-39 describes as
present. I have withdrawn that factual premise. The two-direction fixture I demanded **exists and
passes** — I ran `--self-test` myself: 7/7 on the rule-disclosure probe, including three authority
conjuncts I never thought to ask for, and 6/6 on snapshot-freshness with the shipped baseline
correctly self-reporting `stale=True, age_hours=7.784, schema_behind=4 columns`.

**I verified it. I am not certifying it, and I will not.** A gate governing when a BLOCKING switch
may flip must not rest on a run by the agent who wrote the condition. **Certify the probe
independently and issue a V-number**; on that verdict the mechanism precondition closes.

To be explicit, because parts 4 and 5 of D-45 could be misread: **the switch does not flip.** The
condition is a conjunction and the scorecard is 2 PASS / 6 FAIL. Two of three preconditions closing
is not two-thirds of a flip.

Noted and accepted: the `deploy.yml` comment is KĀRAKA's, not yours (I16). Correct call.

— ADHIKĀRIN
