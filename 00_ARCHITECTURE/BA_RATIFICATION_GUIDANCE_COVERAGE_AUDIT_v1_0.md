---
artifact: BA_RATIFICATION_GUIDANCE_COVERAGE_AUDIT
canonical_id: BA_RATIFICATION_GUIDANCE_COVERAGE_AUDIT
version: 1.0
status: CURRENT — Cowork (strategic track) dispositions on the coverage-audit [NATIVE-RATIFY] items,
  for the native's decision + handoff to the implementation conversation
created: 2026-07-03
author: Cowork (Beyond-Acharya strategic workstream, Claude Fable 5)
reviews: RETRIEVAL_COVERAGE_MAP_v1_0 · RETRIEVAL_ASTRO_COVERAGE_AND_OUTPUT_QUALITY_v1_0 ·
  MCP_TOOL_NAMING_STANDARD_v1_0 · RETRIEVAL_TOOL_BLUEPRINT_v1_0
verdict: all four deliverables SOUND and program-convergent; no contradictions; four ratification
  dispositions + five reconciliation amendments below.
---

# RATIFICATION GUIDANCE — COVERAGE-AUDIT DELIVERABLES

## §1 — Dispositions on the four [NATIVE-RATIFY] items (recommended)

1. **Ranking weights: ratify the 4-dim STRUCTURE now; numbers at the W1 sitting; two binding conditions.**
   (a) Weights live in `bg_class_priors` (L0 registry) — retrieval reads them; NO fourth embedded-weight
   site (master v2.0 unifies three already). (b) Static/query-time split: intrinsic_strength +
   structural_role computed at build (stored salience v2); topic_relevance + temporal_activation applied
   at query time. The composite = salience v2 — third independent convergence on the same design.
2. **Spearhead: CONFIRM INTERPRETATION.** Only insight-type needing no L3/L4 repair; its proof's five
   failures are exactly W2/W3 scope; its acceptance test = the W2 G10 gate. PREDICTION/TIMING block on
   anchor-v2 + activation (W4) — wrong spearhead.
3. **Remedies: `bodha_remedies_get` PRIMARY** (chart-scoped, bo_upaya); `ref_remedies_search` retained
   (global corpus — different asset); `bodha_remedies_search` aliased into primary. Note: chart-scoped
   remedy RANKING stays meaningless until F-007 (0.28 resonance degeneracy) is fixed in the W2 L2
   regeneration — consolidate now, trust after.
4. **Golden answers: fold into the W1 sitting.** Cowork drafts candidate goldens for native correction.
   INTEGRITY RULE: goldens overlapping LEL events (Q9 esp.) are flagged `lel_overlap` — the eval corpus
   and the L5 learning corpus must remain disjoint (no golden leaks into calibration training).

## §2 — Reconciliation amendments (blueprint ↔ Beyond-Acharya program)

1. **Insight-types × query-classes are two dimensions, not rivals.** 5 insight-types = OUTPUT product;
   Q1–Q8 = QUESTION shape. Cross them in the readiness matrix (each query class carries insight_type +
   recipe + the four measures Volume/Relevance/Accuracy/Ranking as columns). ADD: RECTIFICATION as query
   class **Q9** (a real class the taxonomy missed). NOTE: Q1 period-quality, Q4 undertaking, Q8
   compatibility have no blueprint workflow yet — they arrive with W4/W8 products.
2. **RetrievalEnvelope absorbs the W3 verdict spec:** `content.verdict` becomes the STRUCTURED verdict
   object (claim · ranked evidence · contradiction resolution · tradition concordance · activation state ·
   ayanamsha robustness · confidence · falsifier · citations), not `string|null`. ADD field
   `tail_divergence` (complement pass, supplement §1.3.5). ADD `class_priors_version` to `ranking_basis`.
3. **Naming standard: APPROVE Phase 1 (aliases, non-breaking) now.** Phase 3 (breaking removal) stays
   gated on native sign-off + connector inventory, per its own checklist.
4. **Coverage gaps:** the 31 uncovered assets are mostly wire-work, not builds. `ga_strength` wiring (P1)
   is a PREREQUISITE of the composite's intrinsic_strength dimension → W0/W2, not backlog. Batch the
   "handler-exists-not-wired" family (P2–P4, P6–P9) into one W3 wiring brief. mi_darshana exposure (P15)
   lands WITH the verdict object (it is the verdict's home per I-2). Medical/vastu/prashna tools
   (P11–P13) defer to their subsystem waves.
5. **Blueprint workflows are interim-correct:** PREDICTION/RECTIFICATION chains consume current
   phala_anchors/mimamsa tools — acceptable until W4 (anchor v2) and W5 (MIMAMSA_V2 engine) replace the
   backing logic; workflows keep their shape, swap their substance.

## §3 — The W1 sitting agenda (now consolidated; the single gating event)

Ratifies, in one sitting: (1) salience class-priors + varga weights (→ bg_class_priors seed);
(2) event ontology; (3) activity ontology; (4) judgment-constants sheet (supplement §3);
(5) graha×domain + domain×varga affinity tables (supplement §2.3); (6) the 4-dim composite weights (§1.1
above — same table as #1); (7) golden answers for the 10 eval questions (§1.4); (8) Ranking Doctrine +
resonance-quarantine + REFUTED-attestation sign-offs. Cowork prepares ALL drafts; native corrects.

*End of BA_RATIFICATION_GUIDANCE_COVERAGE_AUDIT v1.0 — carry §1–§2 into the implementation conversation;
§3 is the sitting agenda. On your word, Cowork begins drafting the sitting materials.*
