---
canonical_id: BA_STRATEGIC_TRACK_HANDOFF
version: 1.0
status: CURRENT — paste into a fresh conversation to continue the Beyond-Acharya STRATEGIC track
created: 2026-07-04
author: Cowork (Claude Fable 5, strategic workstream) — handoff to the successor strategic conversation
purpose: >
  Zero-context-loss handoff. The successor conversation (any capable model) assumes the STRATEGIC-TRACK
  role this conversation held: gate-checking phase close reports from the autonomous conductor, authoring
  corrective/fix briefs, dispositioning findings, protecting the program's doctrine and traps, and
  advising the native. IMMEDIATE next activity: the two cockpit L1 chart rebuilds (§6) and their
  post-verification, then the M1/M2 fix wave, then P3B (the one-shot L2 regeneration).
how_to_use: paste this file (or its path) as the opening message; read §2's document stack on demand,
  not upfront — this handoff carries the operative summary of each.
---

# BEYOND-ACHARYA STRATEGIC TRACK — HANDOFF v1.0

## §1 — WHAT THIS PROGRAM IS (three paragraphs of context)

MARSYS-JIS is an LLM-operated Vedic-astrology instrument for native **Abhisek Mohanty** (canonical chart
`482012f1-710e-4a25-994a-93821f5871aa`; born 1984-02-05 10:43 IST Bhubaneswar; FORENSIC anchors: Sun
Capricorn · Moon Purva Bhadrapada · Lagna Aries ×5 ayanamshas · Shukla Tritiya · Ravivara · Shiva ·
Garaja). Six data layers (L0 Brahmagyan global reference — ONE copy, no chart_id; L1 Gaṇita → L5 Mīmāṃsā
per-chart), ~85 assets, a FROZEN orchestrator ("click Build" drives any chart's assets in dependency
order via the WriterBase contract — never modified), a sealed retrieval registry serving an external MCP
channel (~70 namespaced tools + aliases) and the internal portal chat. Test chart: Abhinandan
`1c826d5a-…` (always built FIRST). Other entitled charts: Arunima `acdf0d66-…`, Kiran `cb73cd3d-…`.
`362f9f17-…` is a dead phantom — never write it.

**The BEYOND-ACHARYA program** (this track's charge): elevate the instrument from "richly built,
correctly computed, poorly judged" to output exceeding any individual acharya — grounded, cited,
falsifiable, calibrated. Diagnosis (triple-converged): the data was nearly complete but not ORGANIZED AS
JUDGMENT — salience v1 was degenerate (no signal-type/varga terms; a D2700 bindu tally outranked a
raja-yoga), the graph projected one edge type, contradictions were counted not weighed, L4 predicted
timing-first without promises or base rates, L5 waited for future outcomes while 57 LEL events sat
unbacktested. The program fixes judgment in one continuous autonomous run: P-1 sync-freeze → PG grounding
→ P0 serving truth → P1 tool wiring → P2 query-time 4-dim ranking (the "zeroing bench" that tuned the
class priors cheaply) → **P3A L0 seeds + L1 extensions → P3B the ONE-SHOT L2 regeneration (stored
salience v2)** → P4 verdict object + Mahā-Brief + eval → P5 prophecy pipeline (kala activation + anchor
v2) → P6 learning engine (retrodiction) → P7 classical completions + portal loops. Execution: a Claude
Code CONDUCTOR runs nine pre-authored frozen root briefs (fills only ⟦SLOT⟧ fields), an
**Ācārya-Pratinidhi** agent makes all domain-judgment calls under full native delegation (constitution:
seed package > Ranking Doctrine > classical citation > mainstream-with-contested-flag; everything logged
to BA_JUDGMENT_LEDGER for retrospective native veto), and a 7-role verifier swarm enforces three rings
(lane-merge / promotion-deploy-with-golden-eval-regression / terminal-with-red-team).

**Doctrine (native-ratified, binding):** RANK EVERYTHING, DROP NOTHING, HUNT THE TAIL — salience is a
column never a filter; discovery is guaranteed by five organs (specificity, structural elevation,
contradiction surface, empirical promotion, the complement/tail pass), not by flatness. Deterministic
completeness: LLMs never compute chart values; the serving layer ranks/aggregates/structures by CODE and
the LLM only narrates on top. All judgment constants live in L0 registries (brahma_class_priors,
brahma_formula_constants, ontologies) — never Python literals. All synthesis LLMs are 1M-context class:
bounding = de-noising, not shrinking; flagship product = the Mahā-Brief (whole-chart one-shot briefing,
P4). Model policy: Gemini/DeepSeek in product paths, Anthropic banned there; L5 scoring paths 100%
LLM-free. Backwards design: the golden-question eval (G10-QT rubric, /15, pass ≥13) is a standing
regression gate at every phase close — "the LLM's astrological answers got measurably better" is the
definition of done.

## §2 — THE DOCUMENT STACK (precedence order; all committed in 00_ARCHITECTURE unless noted)

1. `BEYOND_ACHARYA_UNIFIED_EXECUTION_PLAN_v1_0.md` — sequencing + gates + §2.1 commitments (perf gates,
   seeds GLOBAL judgment / per-chart evidence, eval-as-definition-of-done) + §2.2 invariants (layer-scope
   law, seamless DAG fold, UI/UX consistency) + §V grounded corrections. WINS on sequencing.
2. `BEYOND_ACHARYA_MASTER_IMPLEMENTATION_PLAN_v2_0.md` (in-file v2.1) — full asset delta, Nirmāṇa
   embedding mechanics, traps #1–19. WINS on data-layer matters.
3. `RETRIEVAL_MODERNIZATION_MASTER_PLAN_v1_0.md` — serving substance: envelope, tool census, Mahā-Brief,
   38-topic completeness gate. WINS on serving matters.
4. `MIMAMSA_V2_LEARNING_LAYER_DESIGN_v1_0.md` — L5 redesign (adjudication, null-anchored scoring,
   retrodiction, shrinkage, two-key snapshots, resonance quarantine).
5. `BEYOND_ACHARYA_W1_JUDGMENT_SEED_PACKAGE_v1_0.md` — the judgment constitution (priors v1.0 P2-tuned,
   ontologies, constants, affinities, goldens).
6. `BA_AUTONOMOUS_RUN_CHARTER_v1_0.md` — conductor/swarm roles, rings, promotion points, P3 special
   regime (snapshot-before-rebuild, Abhinandan-first, rollback rehearsed), 6 HALT conditions.
7. Nine root briefs `CLAUDECODE_BRIEF_BA_P1…P7B` (frozen substance; conductor fills ⟦SLOT⟧s). NOTE:
   P3B amended to v1.1-in-place: L0 CLASSICAL BRIDGE step added (populate classical_sources_array,
   ≥60% coverage gate on yoga/dosha signals).
8. Live ledgers: `BA_RUN_LEDGER_v1_0.md` (phase/gate/SHA truth) · `BA_JUDGMENT_LEDGER_v1_0.md` (every
   Pratinidhi ruling) · `BA_GROUNDING_REPORT_v1_0.md` (facts) · `BA_PRE_REBUILD_AUDIT_REPORT_v1_0.md`
   (latest gate) · CURRENT_STATE_v1_0.md (governance you-are-here).
Background (read only if needed): BEYOND_ACHARYA_GAP_ANALYSIS…v1.3 (the strategic WHY),
BEYOND_ACHARYA_SUPPLEMENT_RANKING_SALIENCE_CONSTANTS (doctrine + constants rationale),
BA_BRIEF_PACK_P2_P7 (SUPERSEDED consolidated record + implementation annex: code anchors, DDLs,
formula specs, sanity values, anti-goals, smoke one-liners).

## §3 — EXACT CURRENT POSITION (as of this handoff)

- **P-1, PG, P0, P1, P2 = COMPLETE** (prod-verified). P2 froze `prior_version=1.0` after the tuning
  loop; query-time ranking is LIVE (career/wealth/health G10-QT pass at query time); tools are
  namespaced (`ganita_*`, `bodha_*`, `apex_*` …) with non-breaking aliases.
- **P3A = COMPLETE**: migrations 385–389 deployed (charts.chart_type · 12-domain normalization ·
  brahma_class_priors 164 rows [165-vs-164 reconciled in the audit report §C5] · brahma_event_ontology 22
  + brahma_activity_ontology 12 · brahma_formula_constants 11); writers bg_class_priors/bg_ghatana/
  bg_formula_constants live; L1 EXTs merged (bhava arudhas w/ 2-exception rule + AL/UL, Rao Chara dasha,
  per-varga sthana bala, sayanadi/lajjitadi/yuddha). Ring-1 done (PRs #398/#401/#402/#405, final SHA
  e692604f); Ring-2 4.5/5 gates evidenced. **The 0.5: `bhava_arudha` facts are DEFERRED pending the
  cockpit L1 rebuilds — the immediate next activity (§6).**
- **PRE-REBUILD AUDIT (BA_PRE_REBUILD_AUDIT_REPORT_v1_0.md) = COMPLETE**: **GO for both rebuilds, 0
  BLOCKERs** (A4 verified every writer DELETE predicate verbatim — no accrete/over-delete risk; cascade
  opt-in default-off; A9 blast-radius statement written for the native). **CONDITIONAL GO for P3B**
  pending two MAJORs: **M1** ga_condition count_sql misses 3 fact_categories (display-only; migration
  fix) · **M2** `bodha_discoveries_get` queries non-existent `bodha_bimba` → `FROM bodha_discoveries`
  at platform-mcp/src/tools/register_p1_synthesis.ts:150 + redeploy. B6 LLM end-to-end proof scored
  **13/15**. MINORs logged (incl. classical_sources NULL → now a P3B step; 9 service-asset coverage
  deferrals; envelope completeness on raw ganita tools → P4).
- **A fix-wave prompt for M1/M2 was already handed to the conductor** — verify its state in the RUN
  LEDGER before re-issuing.

## §4 — THE SUCCESSOR'S ROLE (how this track operates)

You are the STRATEGIC GATE-CHECKER, not the implementer. The split: Cowork/strategic authors briefs +
dispositions findings + protects doctrine; the Claude Code conductor + swarm implements; the native
clicks only what requires his Firebase auth (cockpit builds) and holds retrospective veto via the
ledgers. When a phase close report arrives: (1) check DELIVERABLES vs the brief's EXIT GATES — gates are
DATA-level prod evidence, never "code written / py_compile clean" (the V1.3 scar: worktree-complete ≠
complete; this failure recurred at P3A hand-off and was corrected); (2) verify Ring-1 (merge+CI) and
Ring-2 (deploy truth: Cloud Run revision SHA == merged HEAD, CDN wait, prod probes) before accepting
COMPLETE; (3) enforce the golden-eval non-regression at every close; (4) frozen brief substance is
unalterable by the conductor — substance changes are checkpoint-and-report events that YOU disposition
(you may amend as author, versioned); (5) every Pratinidhi ruling must appear in the Judgment Ledger with
basis + reversibility. Traps most worth repeating: two-dir migration numbering (scan BOTH dirs);
count_sql is cockpit truth + Clear must null rows_written; degenerate-distribution gate on every scoring
column (the 2.326672/0.28 scars); chart-agnostic contamination check after every rebuild (identical
outputs across charts = alarm); Abhinandan before native, always; snapshot before any data rebuild.

## §5 — WHAT REMAINS (the run's forward path)

**P3B** (one-shot L2 regeneration; brief at root, v1.1): salience v2 stored (class priors × varga weight
× specificity × rescaled verification [lift the 0.778 cap] × condition terms × bala_gate ×
functional_context; percentiles-in-class; NULL-propagation; robustness; aggregation composites;
signature_tier recut; constituent re-resolve ≥99%; **classical_sources bridge ≥60%**), typed graph edges
+ valence + pagerank backfill, contradiction domain-attribution + reconciliation, bo_pratijna (Promise
Register), bo_sangati triangulation, bo_upaya resonance de-degeneracy. ONE regeneration, Abhinandan
first, snapshot + rehearsed rollback, then the retrieval intrinsic-input swap (zero interface change).
**P4**: verdict object (mi_darshana home) + query-time judgment terms + attention budget 70/20/10 +
complement pass + **Mahā-Brief** + full Q1–Q9 golden eval + PD-5 UI refactor. **P5A/P5B**: kala
activation (7 systems, period dossiers ka_avadhi, waveform ka_taranga, conflation fixes) + phala v2
(posterior = base_rate × promise × activation × trigger lifts; structured falsifiers; prashna path).
**P6**: MIMAMSA_V2 engine (LEL intake FROM MARKDOWN `01_FACTS_LAYER/LIFE_EVENT_LOG_v1_2.md` — the DB
table is empty/chart-less; blind retrodiction + control windows + ablation; shrinkage; two-key
snapshots; 3-site weight unification; sensitivity harness). **P7A∥P7B**: Nadi/AV-transit/avastha
completions + portal ask-cards/attestation/prashna-follow-ups/co-sign. Then Ring 3 terminal (full golden
set both charts, 38-topic four-measure matrix, north-star tests, red-team) + worktree cleanup +
BA_RUN_REPORT for the native's retrospective review — which this track then independently audits.

## §6 — THE IMMEDIATE NEXT ACTIVITY: THE TWO CHART REBUILDS (native-executed; you verify)

Pre-conditions ALL MET (audit GO, 0 blockers). The native reads the audit report **§4 blast-radius
statement** first. Then, in the cockpit (Firebase auth, owner account):

1. **Abhinandan first:** select chart `1c826d5a` → Nirmāṇa/Build → **Rebuild L1** → watch to green.
   (Cascade is opt-in and OFF — L2–L5 untouched. Expected: chart_facts fully re-derived + new
   categories: bhava_arudha 12×5 ayanamshas, chara-dasha rows, per-varga sthana bala,
   sayanadi/lajjitadi/yuddha.)
2. **Native chart:** select `482012f1` → Rebuild L1 → green.
3. **Post-rebuild verification (you drive; conductor or psql):**
   - `SELECT chart_id, ayanamsha_id, count(*) FROM chart_facts WHERE fact_category='bhava_arudha'
     GROUP BY 1,2;` → 12 × 5 × both charts.
   - FORENSIC 7/7 re-check on 482012f1 (full rebuild re-derives everything; anchors must survive).
   - Contamination: Abhinandan's Sun=Aquarius/Moon=Gemini ≠ native's Sun=Capricorn/Moon=Aquarius.
   - Cockpit live counts match count_sql (M1's understatement on ga_condition display is KNOWN until
     its fix lands — do not mistake it for data loss).
   - Spot MCP: `ganita_positions_get` + `bodha_signals_get(career)` still serve correctly post-rebuild;
     golden-eval spot question non-regressed.
4. **Then:** confirm M1+M2 fix wave merged + prod-verified (may already be done — check RUN_LEDGER) →
   flip P3A brief COMPLETE (if not already) → conductor fills P3B's ⟦P3A_CLOSE_SHA⟧ = e692604f (or the
   fix-wave HEAD if later) → **P3B begins with its Step 0 snapshot.**

If a rebuild fails or FORENSIC breaks: STOP, do not retry-hammer; capture the run log + failing step;
the fix is a brief, not an improvisation; Abhinandan-first exists precisely so the native chart never
sees an unproven writer.

## §7 — STANDING NATIVE PREFERENCES (how Abhisek works, for the successor)

Concise, direct, no flattery-padding. Every output = a committed .md artifact or a pasteable prompt —
never chat-only bullets he must transcribe. He delegates domain judgment fully to the agents (they are
"closer to the data") but expects a JUDGMENT LEDGER he can veto retrospectively. He values: honest
verdicts over agreeable ones (he ratified "the null finding published with equal prominence"), gates
over promises (prod evidence, not worktree claims), one-shot discipline with the rifle zeroed first,
audits before irreversible actions, and the eventual-LLM-response quality as the only true acceptance.
When he shares a completion report, GATE-CHECK it before celebrating. When he asks "should we X," give
the honest trade-off and a recommendation, not a menu.

*End of BA_STRATEGIC_TRACK_HANDOFF v1.0. First actions for the successor: (1) read
BA_PRE_REBUILD_AUDIT_REPORT §4 with the native; (2) supervise §6; (3) verify the M1/M2 fix-wave state;
(4) green-light P3B. The program is mid-flight and healthy — keep the gates honest and the doctrine
intact. Śubham astu.*
