# Cockpit Consolidated Phase Log

Branch: `feature/cockpit-v2-three-views`
Brief: `ANTIGRAVITY_PASTE_COCKPIT_CONSOLIDATED_PHASES.md`

| Phase | Status | Commit SHA | Notes |
|-------|--------|------------|-------|
| Phase 1: render-layer | COMPLETE | — | Dev server ready at 220ms. Native confirmed Incognito render: hero shows, layer panels populated. |
| Phase 2: date format + top bar | COMPLETE | abdc562e | formatDate() utility; birthDate → dd-MMM-yyyy in CockpitHeader; breadcrumb + ChartSwitcher hidden on /build via usePathname(). |
| Phase 3: asset-state correctness | COMPLETE | 70325af7 | Root cause: Cause 1+3 — 5 assets have is_active=false; UI mapped to "INACTIVE". Fixed to "NOT MIGRATED" neutral label. |
| Phase 4: schema migration 171 | COMPLETE | 2bc93126 | build_runs + build_run_assets + asset_throughput (chart_id, state, built_against_*, last_built_at, last_error) applied to prod. |
| Phase 5: plan resolver + staleness | COMPLETE | 357d7f75 | plan.ts + staleness.ts; 24 vitest tests pass. |
| Phase 6: API endpoints | COMPLETE | ed2ae9f9 | POST plan, POST runs, POST pause/resume/stop, GET active — all super_admin gated. |
| Phase 7: build UI components | COMPLETE | d0128385 | BuildActionButton (auto-label: Build/Update/Rebuild) + PlanModal + CascadeBanner + PauseStopGroup wired at global/layer/asset scopes. useActiveRun polls every 5s. | POST /api/cockpit/plan, POST /runs, POST /runs/[id]/{pause,resume,stop}, GET /runs/active — all super_admin gated. | plan.ts + staleness.ts pure functions; 24 vitest tests pass (empty DAG, linear chain, branching DAG, cycle detection, all 4 actions, scopes, null estimates). | Root cause: Cause 1+3 — 5 assets (panchanga_almanac, balatva, suksmabindu, sade_sati, tajaka) have is_active=false; UI mapped that to "INACTIVE" chip. Fixed to "NOT MIGRATED" neutral label. asset_throughput has no chart_id/state yet (Phase 4 adds those). |
| Phase 8: orchestrator wiring | DEFERRED | — | Orchestrator source outside this repo. Cloud Run Job: `brahma-build-pipeline-job` (asia-south1), image: `asia-south1-docker.pkg.dev/madhav-astrology/amjis/brahma-pipeline:latest`. No Dockerfile or pipeline source found in platform/. Cowork to scope a separate brief for the orchestrator repo covering: (1) accept --run-id arg, (2) poll pause/stop signals, (3) mark downstream stale post-write. Watchdog also missing (separate brief). |
| Phase 9: E2E verification | COMPLETE (partial) | a5f8344a | AC1 PASS — build_run INSERT succeeds in prod (chart_id/scope/action/state correct; build_run_assets 3 rows queued). AC2 PASS at schema level — pause_requested_at sets, cleared on resume, state transitions correct; full orchestrator-driven round-trip blocked by Phase 8 DEFERRED. AC3 PASS — stop_requested_at sets, state→stopped confirmed in prod. AC4 BLOCKED — no stale assets (orchestrator not yet wired to mark downstream stale; Phase 8 DEFERRED). AC5 BLOCKED — CascadeBanner component present; no stale assets to trigger it (blocked by AC4). Test run cleaned up: state=stopped, ended_at set. |
| Phase 10: push + close-out | COMPLETE | a5f8344a | Branch pushed to origin/feature/cockpit-v2-three-views. 9 net-new commits on this branch above main (8 cockpit phases + 1 phase 8 syntax fix). Close-out report in session message. |

## Consolidated Brief (ANTIGRAVITY_PASTE_COCKPIT_FULL_v1_0.md) — Phases 6–14

| Phase | Status | Commit SHA | Notes |
|-------|--------|------------|-------|
| Phase 6: clear + SSE APIs | COMPLETE | 650fa6e4 | POST /api/cockpit/clear (preview: row counts, hash, downstream closure), POST /api/cockpit/clear/execute (hash verify + typed confirmation + DELETE + throughput reset + downstream stale mark), GET /api/cockpit/sse (SSE stream, 30s heartbeat, exponential reconnect). computeDownstreamClosure + computeUpstreamClosure exported from plan.ts. |
| Phase 7: bilingual headers + DAG layout | COMPLETE | 3e43d288 | LAYER_NAMES map (sa/en). Sanskrit 22px var(--font-serif) var(--gold-high) above English 14px var(--ui-stack). Collapsed default (defaultExpanded=false). 60/40 flex layout with DAGPlaceholder stub. Column headers grid 40/15/15/20/10 (Asset/Rows/State/Last built/Actions). stats endpoint enriched with state + last_built_at from asset_throughput. AssetRow rewritten to match grid. ClearIconButton added at asset+layer scope. |
| Phase 9: ClearConfirmModal | COMPLETE | 6f2502df | ClearConfirmModal (3 scope variants: asset/layer/global). Shows top-5 tables, total_rows, affected_assets count, downstream_stale list. Global scope: typed input disabled until typed === subject_name. POST /clear/execute with preview_hash + typed_confirmation. ClearIconButton: POST /clear → set preview → open modal. AssetStats interface extended (state, last_built_at). |
| Phase 10: LiveDependencyGraph | COMPLETE | cc9f125c | Full constellation SVG. 6 orbital rings (brahmagyan–mimamsa at fixed ratios). Cream-pearl nodeBead / rootBead radialGradients. 4-tier edge taxonomy: rootSpoke/direct/crossWeave/skip(dashed). Deterministic djb2-like hash for angular jitter. Hover: upstream+downstream sets highlighted; unrelated nodes dim to opacity 0.15. Hover tooltip (english_name + asset_id). Click: onNodeClick callback → DataAssetsView scrolls LayerPanel into view + sets focusedAssetId + clears after 1.5s. ResizeObserver: H = W×1.25. 30 deterministic particle dust dots. ROOT node centered. DataAssetsView wired: 60/40 flex, DAGPlaceholder replaced, layerRefs map, handleNodeClick. LayerPanel forceExpand + focusedAssetId props. AssetRow highlighted prop (gold glow + box-shadow). |
| Phase 11: SSE consumer hook | COMPLETE | 3d1e41e3 | useCockpitSSE(chartId, onEvent). EventSource with withCredentials. Exponential backoff 1s→30s cap, reset on hello. 60s heartbeat watchdog (no event → reconnect). Cleanup: aborted flag + clearTimeout + es.close(). DataAssetsView: sseOverlay Map patches asset state from asset.state_change + asset.progress events; run.state_change calls refreshRun(). assetsWithState merges assets + stats + sseOverlay. isLayerExpanded auto-expands layers with active run. |
| Phase 12: wire-up audit + error toasts | COMPLETE | a3facf7d | Audit: 5 action paths verified (Build→plan+runs, Pause/Resume/Stop→runs/[id]/action, Clear→clear+execute, Cascade→runs, DAG node click→local only). sonner toast.error() wired to all silent catch paths: PauseStopGroup (HTTP error detection + toast), ClearIconButton (preview fetch error), CascadeBanner (cascade POST error). All three now throw on !r.ok and display toast. |
| Phase 13: E2E verification | COMPLETE (partial) | — | AC1 PASS — build_runs row exists, state=stopped, scope=global. AC3 PASS — stop_requested_at=2026-06-06T08:24:26Z set on stopped run. AC6 PASS (DB) — count_sql populated for all per_chart assets; ganita_dashas=819, bodha_signals=569, ganita_positions=40 for native chart; preview endpoint logic verified. AC7 PASS (logic) — execute route: hash verify → DELETE per target_table → throughput reset to dormant → downstream marked stale; SQL verified against live schema. AC8 PASS — subject_name='Abhisek Mohanty'; exact-match enforcement confirmed (case-sensitive). AC2/AC4/AC5/AC9 BLOCKED-pending-orchestrator. AC10/AC11 BLOCKED-browser-only (require authenticated Incognito session). |
| Phase 14: push + close-out | COMPLETE | a3facf7d | See below. |

## Round 4 (ANTIGRAVITY_PASTE_COCKPIT_R4_v1_0.md) — 2026-06-06

| Phase | Status | Commit SHA | Notes |
|-------|--------|------------|-------|
| Phase 1: asset row typography bump | COMPLETE | 206954a4 | Sanskrit 15→16px, English 12→13px, white/80→85 on secondary line. |
| Phase 2: AssetProgressBar + grid 44/30/12/14 | COMPLETE | 0bad604a | New AssetProgressBar component: animated fill (Framer Motion), state-aware tint, building shimmer overlay, state pill embedded inside bar. Grid rebalanced 44/30/12/14 (Asset/Progress/Last built/Actions). Volume+State columns retired. formatRelative() added to date.ts. |
| Phase 3: MiniDAG in PlanModal + retire CascadeBanner | COMPLETE | 5a1c66ac | MiniDAG.tsx: BFS layer layout, bead gradients matching constellation, Framer Motion edge trace + node spring-in. Wired into PlanModal (optional assets prop; renders when plan.length>1 + action≠build + scopeTarget provided). allAssets threaded DataAssetsView→LayerPanel→AssetRow→PlanModal. CascadeBanner.tsx deleted (was only defined, not imported anywhere). |
| Phase 4: Workflow + Agents tabs built + API endpoint | COMPLETE | 8a02256b | WorkflowView: live plan timeline (per-asset state dot + elapsed) + event log tail ring-buffer (50 entries, auto-scroll). AgentsView: active agent cards, completed rows, error rows — all from useActiveRun(chartId). Pro-mode toggle (button in CockpitHeader replaces static pill); TabBar gates Workflow/Agents behind proMode=true. /api/cockpit/runs/[id]/assets GET endpoint added (super_admin gated). |
| Phase 5: status pills wired | COMPLETE | a4875a83 | /api/cockpit/status GET endpoint (WRITERS/QUEUE/BUILD from DB). useCockpitStatus hook (30s poll). CockpitHeader wired: 4 pills only (QPS + SPEND dropped). BUILD pill blue when running; SIDECAR pill red when DOWN. |
| Phase 6: Clear flow verification + fix | COMPLETE | 3803ac88 | Audit: asset/layer scopes — ClearIconButton → /api/cockpit/clear → ClearConfirmModal → /api/cockpit/clear/execute ✓. Global scope — CockpitShell was calling /api/cockpit/clear/preview (non-existent) + reading body.data. Fixed to /api/cockpit/clear + body.preview. Cancel writes nothing. SUBJECT_NAME_MISMATCH confirmed in execute route. |
| Phase 7: push + close-out | COMPLETE | — | Branch pushed to origin/feature/cockpit-v2-three-views (HEAD 3803ac88, ahead of main by 6 Round-4 commits + prior orchestrator commits). |
