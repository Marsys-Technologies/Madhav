---
artifact: STATE_D-1.5b
type: WAVE STATE LEDGER (CONDUCTOR_PROTOCOL §6.1)
---

```yaml
wave: D-1.5b
lifecycle_step: 8   # CLOSE. All 7 lanes deployed; full L1->L5 rebuild done; 3 real defects found+fixed during rebuild (B-4 amplifier x2, bo_laksana cross-writer delete); Gate B = 17/17 GREEN on deployed connector after 4 serving-gap fixes. REPORT_D-1.5b.md written. Wave CLOSED.
brief_bound: true
binder_annotations:
  - "B-1: all file:line citations confirmed within +/-1 line (brief's line numbers off by one vs actual). PyJHora is pip-installed (requirements.txt pinned 4.8.6), NOT repo-vendored as the brief states -- wording inaccuracy, harmless."
  - "B-2: PyJHora's bhava_bala (jhora.horoscope.chart.strength:1094) is a 3-source composition (adhipathi+dig+drik bala), NOT six-source as the brief states. Amended: use the library per no-hand-roll rule, but facts must record verification_pass_status='documented_approximation' and cite the 3-component composition."
  - "B-3/B-4 draft DR-n salience proposal (Binder/Fable, conductor to formalize): sudarshana_agreement=1.15, frame_divergence=1.00, bhavat_bhavam_amplifier=0.85, subsystem=structural for all three."
  - "MATERIAL BLOCKER: ka_vighnakara ForeignKeyViolation (carried forward from D-1.5a REPORT) blocks 23 error + 19 stale assets in the L3->L5 cascade. D-1.5b's brief mandates a FULL L1->L5 rebuild for Gate B -- this cannot go green with the defect unfixed. Must be fixed BEFORE the wave's rebuild+gate step, even though outside the original 7 lanes' declared scope."
  - "Also found: 1 orphaned build_run (5580b8a5, state=planned) needs cleanup before dispatching, same class of issue D-1.5a hit with _MAX_CONCURRENT_RUNS."
  - "Scope recommendation: stage lane spawn in 2 cycles given wave size (7 lanes + only full-rebuild wave + live library integration). Cycle 1: B-5 -> B-2 -> B-1 (fact writers + astronomical core). Cycle 2: B-3 -> B-4 -> B-6 -> B-7 (consumers/serving/governance). Single rebuild+gate after cycle 2, per brief's own merge order."
rollback_pin:
  images:
    amjis-web: "d64bc575f72335cbd8b756a5a1768a2aa4d93f26"
    amjis-mcp: "53b4b44cf3381bba3eb1cb59e8edeeac529e03ab"
    brahma-pipeline: "2271c32e647d52f7be77447343038468d0ef74bd"
  build_ids:
    "482012f1-710e-4a25-994a-93821f5871aa": "5bdd933f-86b2-4609-ba7d-177f30ea1675"
prerequisite_check: {d1_5a_gate: green, note: "13/15 + 2 documented PARKs per REPORT_D-1.5a.md; brief's hard-block (gate GREEN) satisfied"}
lanes:
  # CYCLE 1 (Binder-recommended staging): fact writers + astronomical core. Merge order B-5 -> B-2 -> B-1.
  - {lane: B-5, branch: wave/D-1.5b/B-5, status: receipted, agent_id: a0e2585e962da68fa, commit: d0a176ca, receipt_ref: "verifier aeb81d309df523957 ACCEPT: diff 013ff082...d0a176ca, all 4 items independently reproduced, karakamsa 'already-exists' claim confirmed genuine (not skipped work), MIGRATION CLAIM VERIFIED (no migration needed -> facts cockpit-visible via count_sql), 10/10 new tests + full suite 3363/0, scope_warden pass", claim: "CR-17 karakamsa regression-lock; CR-18 shadbala ratio; CR-58 varga_hora_class(D2, no migration); CR-46 ph_nimitta dedup. Follow-up: optional ga_vargas target_floor re-baseline."}
  - {lane: B-2, branch: wave/D-1.5b/B-2, status: receipted, agent_id: a01f9747c2c67140c, commit: 803368ff, base: 68bb0246, receipt_ref: "verifier afe548aac2e1480be ACCEPT (resumed after transient API error): read strength.py to confirm bhava_bala IS 3-source (bb=sum(adhipathi,dig,drik)); hand-rolled version genuinely removed; documented_approximation + 3-source cite present; AV fully additive, bindu VALUES untouched (§L.3), house-keyed retained, SAV=337; schema edit justified (gates.py:274-289 fails on undeclared categories); key names unchanged so MV/count_sql resolve; suite 3378/0; scope clean (4 files). Minor: 17 tests not claimed-18 (immaterial).", claim: "3-source bhava bala + additive AV; CHART_FACTS_SCHEMA.json edit REQUIRED for drift gate."}
  # !!! INTEGRATION FLAG: B-5 and B-2 BOTH modified ga_strength_writer.py AND CHART_FACTS_SCHEMA.json (shadbala + bhava bala both live in the strength writer). Merge order B-5 -> B-2; expect conflicts in both files -> resolve + MANDATORY full-suite re-test on the integration branch (this is exactly the cross-lane interference the INTEGRATE step catches).
  - {lane: B-1, branch: wave/D-1.5b/B-1, status: receipted, agent_id: ad8c83defdb551e7b, commit: 44ec371a, base: 68bb0246, verifier_compute: "aff6be16efd8855e9 ACCEPT -- INDEPENDENTLY recomputed cusps via own drik script: lagna Aries 12.4212 (FORENSIC ok), Moon 327.055=Purva Bhadrapada (FORENSIC ok; brief's 29d46' WRONG, impl correct), Moon in Sripati H12 / whole-sign H11 REPRODUCES independently; local-time JD ok; whole-sign untouched; 14/14 + suite 3375/0", verifier_quarantine: "a1f5158bb061b786b ACCEPT -- both fake-cusp sites eliminated (grep-confirmed), sub-lord IS a real 4-level Vimshottari engine (compute_kp_lords, not old stub), honest [EXTERNAL_COMPUTATION_REQUIRED] fallback (not silent revert), additive categories orb 3deg, scope clean. 14/14; its 36 suite failures all pre-existing on base 68bb0246 in untouched modules (harness/DB-config, not a regression)", claim: "chalit + real cusps; fake KP cusps -> real Placidus + real sub-lord; whole-sign untouched; NO migration. HEADLINE lane fully RECEIPTED (2/2 verifiers ACCEPT)."}
  # CYCLE 2 (spawn after cycle 1 integrates): B-3 (Sudarshana), B-4 (Bhavat Bhavam), B-6 (serving hygiene), B-7 (governance+derived view). B-3/B-4 need the salience DR (Binder draft: sudarshana_agreement=1.15, frame_divergence=1.00, bhavat_bhavam_amplifier=0.85) formalized by conductor at cycle-2 open.
  - {lane: B-3, branch: wave/D-1.5b/B-3, status: receipted, agent_id: a7decbb7da46b7f3a, commit: 746b15cd, base: 417dadab, receipt_ref: "verifier a1dc2e1e773c2ee72 ACCEPT: pure read of chart_facts graha_position (no new compute), house_from formula verified for all 3 frames, class_prior=1.15/subsystem=structural hard-coded per DR-3 (grepped), type specimen Sun+Mercury 10th-from-Lagna/12th-from-Moon independently reproduced via live DB query + hand arithmetic, WriterBase contract conformant (@register, no commit/close, no asset_throughput writes), migration 438 idempotent + depends_on=[ga_positions] correct, suite 3427/0 (own run), scope clean (6 files, l0_class_priors.py pure append), no B-4 overlap", claim: "Tri-frame Sudarshana Chakra writer + sudarshana_agreement MSR emitter; brief's exact type specimen holds on 482012f1."}
  - {lane: B-4, branch: wave/D-1.5b/B-4, status: receipted, agent_id: a089e0b9ee8f74875, commit: 746800a9, base: 417dadab, receipt_ref: "verifier ab3ef46cb07cd474c ACCEPT: 12-cell odd-house map verified exact match both Python+TS; never-a-generator guard traced (only fires on pre-existing fired-yoga or major/chart_defining MSR tier); never-outranks-primary clamp hand-tested with adversarial counterexample (primary=0.05 vs occupant=10.0 -> 0.0425<0.05, double-guarded by raw formula too); no-chaining stripped at both caller+module level; both type specimens (Dhana Yoga H9->H11, Sasa H7->H10) confirmed live on 482012f1; bo_laksana.py edit is pure append using existing SAVEPOINT pattern; own test run 3406/0 (tests/ tree) + 29/29 direct (bodha_writers/__tests__ not collected by standard CI target -- PRE-EXISTING repo gap shared with test_formulas.py, flagged as non-blocking hygiene follow-up, not a B-4 defect) + TS 5547/0; scope clean (10 files)", claim: "Gated Bhavat-Bhavam amplifier with hard-coded restraint guards (never-generates, never-outranks-primary, no-chaining); both brief specimens verified live.", follow_up: "hygiene: bodha_writers/__tests__/*.py not collected by CI's pytest target -- recommend moving under tests/ or extending the pytest target (applies to test_formulas.py too, not new to this lane)."}
  - {lane: B-6, branch: wave/D-1.5b/B-6, status: receipted, agent_id: a52bd9dffe948b462, commit: 7076a334, base: 417dadab, receipt_ref: "verifier a793bc3c835c52429 ACCEPT: all 6 items verified in real code (positions ordering, 3 response-budget fixes incl. confirmed ganita_tajaka_get's limit??25000 override bug pre/post, ref_remedies_search real substring search not pass-through, structural_get best-effort N.6 fields flagged pending B-7, B9 dosha gate applied to BOTH count+page via shared whereClause -- not the lying-total bug class); own test runs: platform tsc clean, lint identical to main baseline, npm test 5532/0; platform-mcp tsc clean, 75/435 vs confirmed 75/426 on main baseline (delta = exactly new tests, claim verified exactly not approximately); scope clean (10 files, TS-only, zero Python touched, zero lane overlap)", claim: "6-item serving hygiene pass: CR-50 position ordering, response budgets (domain_reading/ephemeris_year/tajaka), CR-42 remedies-search honor-or-reject, structural_get N.6 best-effort, B9 dosha gate, dedicated kala_sarpa facet."}
  - {lane: B-7, branch: wave/D-1.5b/B-7, status: receipted, agent_id: a58b11dc99acb9ca5, commit: 3c098fa5, base: daffc284, receipt_ref: "verifier aa528c120b7e53154 ACCEPT: version-drift claim confirmed real (main frontmatter said 6.0, footer said 6.2) and genuinely corrected to 6.3 w/ changelog; N.6 text independently cross-checked against response_budget.ts hardFloor + get_yoga_dosha.ts catalog split + register_d9_judgment.ts bearing_yogas layering -- derived not invented; density harness run live by verifier, 4/4 green, DENSITY-2 calls real applyResponseBudget not a mock; CI job purely additive, all 10 existing jobs untouched; dasha_lord_capability confirmed request-time aggregation (no migration), columns cross-checked against ga_dashas/ga_strength/ga_vichara writers + live DB query on 482012f1 incl. Mercury dusthana_lord/temporal_malefic specimen; warning_tier confirmed deterministic no-LLM w/ cited constants; own test runs clean (platform 495/0, platform-mcp 75 pre-existing failures confirmed identical to main baseline); scope clean (9 files)", claim: "CLAUDE.md SS N.6 Serving Density Principle (v6.0->6.3, corrects a real version-drift bug) + 4-assertion CI density harness + dasha_lord_capability derived view (no migration required)."}
cycle: 2
precascade_rebuild:
  ka_vighnakara_fk: RESOLVED   # was NOT a code bug -- purely the orphaned-concurrent-build_runs race from D-1.5a (cleaned up). ka_sangam/ka_vighnakara/full Kala cascade rebuilt lit once the pileup was cleared.
  local_proxy_diagnosis: >
    Repeated local-rebuild failures ROOT-CAUSED (not a product bug): the laptop
    cloud-sql-proxy path is unreliable -- proxy intermittently drops (fresh
    connect() fails in logs) AND bo_samskara's ~2-min/ayanamsha Vertex AI embed
    loop holds an idle DB conn long enough that a drop mid-embed kills the whole
    run, restarting embedding from scratch (Sisyphus loop). Product code already
    fully resilient (keepalives, idle-txn timeout=0 x2, batch-level embed
    tolerance) and completes fine in prod. Fix: stop using the laptop proxy for
    the build -- dispatch the Cloud Run job (protocol §8.2 canonical mechanism),
    which runs inside GCP with a direct Cloud SQL connection.
  method: "gcloud run jobs execute brahma-build-pipeline-job --args=--run-id,<id> (build_runs row created via scripts/dispatch_d1_5b_precascade_job.py; execution brahma-build-pipeline-job-h5c7l; run_id 585d4a9c-8341-4b40-a116-0f063c3855c0; 23 remaining assets, bo_samskara first)"
  status: COMPLETE   # job completed clean: 64/64 per-chart assets lit, 0 error/stale/dormant. Health verified: bodha_signal_embeddings(47848)==bodha_msr_signals(47848) -> bo_samskara fully embedded, NO partial loss (the exact failure the local proxy kept causing); kala_obstruction=679 -> ka_vighnakara ran clean; chart_facts=136029 (~27554 x5 aya); 0 orphaned runs after cleanup.
  learning: >
    D-1.5b's OWN mandated FULL L1->L5 gate rebuild (brief §G) MUST use this same
    Cloud Run job path, NOT the laptop cloud-sql-proxy -- the proxy path cannot
    reliably complete bo_samskara's embedding loop. dispatch_d1_5b_precascade_job.py
    generalizes trivially (it plans the full non-lit closure); for the gate rebuild,
    reset the wave's touched layers to dormant, create the run row, dispatch the job.
integration_cycle1:
  branch: wave/D-1.5b/cycle1-integration
  merges: "B-5(d0a176ca) -> B-1(44ec371a) -> B-2(803368ff), no textual conflicts (B-5/B-2 ga_strength_writer.py + CHART_FACTS_SCHEMA.json auto-merged in disjoint regions)"
  full_suite: "3404 passed / 0 failed on the integrated tree -- CONFIRMS B-5 shadbala-ratio + B-2 bhava-bala-rewrite co-exist SEMANTICALLY (same file, both green together), and B-1 chalit co-exists clean. The cross-lane interference the INTEGRATE step exists to catch = NONE at the code level."
  cross_lane_gap_found: "B-1 emits 3 new chart_facts categories (bhava_cusps/house_chalit/sandhi_flag) but did NOT declare them in CHART_FACTS_SCHEMA.json. The drift_detector gate (gates.py:274-289) fails on undeclared categories -- separate governance gate from the DB CHECK, surfaced by B-2's lane. NOT caught by the pytest suite. Re-opened B-1 (targeted, not a rejection)."
  gap_resolved: "B-1 commit 9f59a193: declared all 3 categories (traced from actual emission -- subjects BHAVA_01..12 padded, 9 grahas for house_chalit/sandhi_flag, verification_min=single matching _chalit_row) + a TestSchemaDeclaration regression test (7 asserts) that guards emission==declaration going forward. Re-integrated fresh (B-5 d0a176ca -> B-1 9f59a193 -> B-2 803368ff, no conflicts). Merged schema valid, 188 categories, all lanes' new categories declared. Full suite on re-integrated tree: 3408 passed / 0 failed. Cycle-1 INTEGRATE COMPLETE + clean."
  status: MERGED_AND_DEPLOYED
  pr: "570 MERGED to main @ 417dadab (all 14 CI checks green). CI-on-main green + deploy.yml run 29429801340 completed SUCCESS. amjis-web + amjis-sidecar confirmed live at 417dadab. Cycle-1 fact writers deployed. Full rebuild + Gate B deferred to after cycle-2 per brief staging."
integration_cycle2:
  branch: wave/D-1.5b/cycle2-integration
  merges: "B-3(746b15cd) -> B-4(746800a9) -> B-6(7076a334) -> B-7(3c098fa5), base f299f852 (main). ONE expected conflict: l0_class_priors.py (both B-3+B-4 appended at same anchor per the brief's coordination note) -- resolved by keeping BOTH appends in commit order (sudarshana_agreement then bhavat_bhavam_amplifier), 4/4 new rows confirmed present post-merge. B-6/B-7 merged with zero conflicts (pure TS/docs lanes, no file overlap)."
  full_suite: "Python 3429/0 (own re-run on integrated tree); platform TS tsc clean + 5561/0 (npm test); platform-mcp tsc clean + 75 failed/435 passed -- EXACTLY matching the pre-existing main-baseline count independently confirmed by both B-6's and B-7's verifiers (75/426 on main; delta=new tests) -- NOT a cross-lane regression."
  cross_lane_gap_found: "none — all 4 lanes were pure-append or disjoint-file lanes by design this cycle; the one conflict was anticipated and mechanical."
  status: MERGED_AND_DEPLOYED
  ci_finding: "TAP-6 (M-22 method-audit grep) caught B-3's sudarshana_emitter.py hardcoding verification_pass_status=\"two_pass_verified\" as a literal at 3 emit sites (param + 2 dict keys) -- real violation, not a false positive: verification status must be earned by a verifier pass, never asserted at the emit site. Fixed directly on the integration branch (commit bda11540) by changing all 3 to \"documented_approximation\", matching the established L2 convention already used by bo_laksana.py and B-4's own bhavat_bhavam_amplifier.py for computed (even fully-deterministic) signals. B-3's 21/21 tests unaffected by the fix. Also hit one unrelated CI-infra flake (Build Check docker buildx 'no space left on device', transient runner disk exhaustion, unrelated to any code change -- confirmed by the SAME workflow succeeding on the pre-fix commit) -- resolved via gh run rerun --failed, passed clean on retry."
  pr: "571 MERGED to main @ 92a7df4c (all 20 CI checks green after 1 code fix + 1 infra-flake retry). CI-on-main green (TAP CI + Ganga Quality Gate) -> deploy.yml run 29436536037 completed SUCCESS. amjis-web (rev amjis-web-00973-m99) + amjis-sidecar (rev amjis-sidecar-00860-8xb) confirmed live at 92a7df4c. Cycle-2 (B-3/B-4/B-6/B-7) fully deployed. All 7 D-1.5b lanes now merged+deployed."
gate_b_rebuild_attempt_1:
  dispatch: "dispatch_d1_5b_gate_rebuild_job.py (generalized full-rebuild variant of the precascade script -- resets EVERY chart-scoped asset to dormant, not just non-lit) -> run_id 45733f22, Cloud Run job brahma-build-pipeline-job-qktjm."
  finding: "bo_laksana looped 20+ min, NOT hung -- a REAL defect in B-4's bhavat_bhavam_amplifier (merged cycle-2, PR #571) invisible to its original small-scale Phase-1 verification: (1) to_msr_row() hardcoded constituent_facts_array=None violating bodha_msr_signals' NOT NULL constraint -> 100% insert failure; (2) compute_bhavat_bhavam_amplifiers() did an unbounded N-by-M candidate cross-join (real pools run 50-60+ salient configs/house) -> 20,000+ near-duplicate rows observed for a single H9->H5 pair before cancellation. Job cancelled via gcloud; build_run 45733f22 marked failed; bo_laksana asset_throughput reset dormant/rows_written=0 (confirmed 0 bhavat_bhavam_amplifier rows landed -- clean state, no partial data)."
  fix: "commit 89fe8824: bounded to exactly ONE AmplifierSignal per (primary_house, derived_house) pair (single highest-salience candidate per house, deterministic tie-break) -- matches the doctrine's relationship-not-enumeration framing, caps emission at 12 rows/ayanamsha-build. constituent_facts_array/constituent_signals_array now resolve through to real L1/L2 lineage (ga_yoga_firings.constituent_fact_ids / source MSR row's own array), never None. Independently verified ACCEPT by adversarial fresh-context verifier (traced logic, ran local repros incl. the H1 self-corroboration edge case, re-queried live DB for both type specimens, ran tests directly) -- verifier flagged one residual same-class bug (_load_vichara_divergence_signals's `[] or None` falsy-collapse, different CR-57/CR-91 signal class, same file) as a non-blocking finding; fixed proactively anyway (commit 0e302504) before redispatch rather than risk a second stall. Both live type specimens (Dhana Yoga H9->H11, Sasa H7->H10) + all 3 restraint guarantees re-verified unchanged. Full suite 3429/0 both commits."
  pr: "573 (bundles the 2 hotfix commits + the new Gate B harness, commit ee7a3851). MERGED_AND_DEPLOYED @ 0682e023."
gate_b_rebuild_attempt_2:
  dispatch: "User correctly noted L1 was already rebuilt cleanly in attempt 1 (22 ga_* assets 'lit', confirmed) -- no need to redo it. Used the ORIGINAL precascade dispatch script (topo-sorts only non-lit) instead of the full-reset variant: resumed exactly the 43 remaining L2-L5 assets (bo_laksana onward) -> run_id 352bf72c, job brahma-build-pipeline-job-rpdn7. Completed cleanly: 43/43 complete, 0 errors, all 65 chart-scoped assets now 'lit'. bhavat_bhavam_amplifier: 60 rows landed (12/ayanamsha x 5 -- exactly matching the B-4 fix's bounded-emission design). Chalit facts (bhava_cusps/house_chalit/sandhi_flag): 675 rows present."
  finding_2: "sudarshana_agreement showed 0 rows post-rebuild despite bo_sudarshana's asset_throughput claiming state=lit/rows_written=45 (stale/lying success state) from ITS EARLIER successful run in attempt 1. ROOT CAUSE (different bug, NOT a B-3 defect): bo_laksana's shared idempotency helper replace_prior_msr_for_chart() did an UNCONDITIONAL DELETE FROM bodha_msr_signals WHERE chart_id=X AND ayanamsha_id=Y (+ child tables) -- correct ONLY while bo_laksana was the sole writer to that table. B-3's bo_sudarshana (new in cycle-2) is an independent top-level asset that ALSO writes directly into bodha_msr_signals via its own correctly-scoped call -- but every bo_laksana rebuild (which ran AFTER bo_sudarshana in the DAG) silently wiped bo_sudarshana's rows too, since the blanket delete didn't distinguish ownership. Confirmed via live query: bo_laksana's own 49,315 rows across its 13 known signal_type_classes were intact/uncontaminated -- only bo_sudarshana's rows were destroyed."
  fix_2: "commit 75e492e3: replace_prior_msr_for_chart() now requires a non-empty owned_signal_type_classes allowlist and scopes every DELETE (embeddings, contradictions, signals) to signal_type_class = ANY(allowlist) -- raises ValueError rather than falling back to a blanket delete. bo_laksana.py declares BO_LAKSANA_OWNED_SIGNAL_TYPE_CLASSES, the complete enumerated closed set (13 values from _signal_type_class()'s mapping+fallback + varga_ratification_divergence + bhavat_bhavam_amplifier). Independently verified ACCEPT (adversarial verifier hand-counted every SQL placeholder, confirmed the allowlist is genuinely complete via full-file grep, confirmed the regression test's fake-conn models real row-level filtering not a tautology, independently re-ran live DB counts matching exactly, confirmed bo_sudarshana was the only OTHER writer and was never at risk itself). Full suite 3432/0. Committed direct to main working tree, pending PR."
  repair_needed: "bo_sudarshana needs a targeted re-run (its 45 sudarshana_agreement rows are gone; its asset_throughput state is stale/lying) -- bo_laksana does NOT need re-running (its own data is complete and correct, and the fix makes it a no-op for bo_sudarshana's rows going forward regardless of order)."
  repair_done: "bo_sudarshana re-run via Cloud Run job (run 4a8427ba, job brahma-build-pipeline-job-qqg7s) after the delete-scope fix deployed live @ 53d03d73 -- 45 sudarshana_agreement rows restored, state=lit, bo_laksana's 49,315 rows confirmed untouched. Full rebuild genuinely complete: 65/65 assets lit, all new categories present."
gate_b_run:
  harness: "platform/scripts/audit/doctrine_harness (Lane A-0 harness extended with 17 D-1.5b B-* assertions -- committed 0566e1d6). Run against deployed connector post-rebuild."
  finding: "First full run 21g/11r; triage found the deployed connector RATE-LIMITS under sustained 32-assertion load (429s cascade -> false reds on ~9 previously-green assertions incl. all D-1.5a ones; the client only retries 5xx, so 2xx-with-error-body/empty payloads passed through as false empties). Confirmed via isolated re-run: assertions 2/3/6a/6b/7/8/10/11 + B_dosha_gate_kalasarpa all GREEN in isolation -- rate-limit artifacts, NOT regressions. Also fixed 5 harness bugs (wrong category/tool names + a density-floor pagination-truncation bug) surfaced by the live run."
  real_reds_found: "4 genuine serving-layer gaps (data built+correct in every case, serving surface didn't expose it): B2_sudarshana (class-scoped rows unreachable -- bodha_signals_get never declared signal_type_class param), B7_budgets (bodha_domain_reading_get 909KB -- each question-lens dumped its full ~1637-row family; B-6's lens_limit bounded lens COUNT not rows-per-lens), B_shadbala_ratio (required_rupa is ayanamsha_id='INVARIANT' but chart_facts_query's WHERE ayanamsha_id=$2 hid ALL invariant facts), B_d2_hora_class (varga_hora_class lives in chart_divisionals EAV rows, no exposed MCP tool surfaced them -- the S-12 '21,635 MCP-invisible rows' layer). Native decision (2026-07-16): fix all 4, then close green."
  fixes: "4 lanes in isolated worktrees, each independently diagnosed+tested: B_shadbala(11dfd51e, WHERE ayanamsha_id IN ($2,'INVARIANT') -- also un-hides naisargika+panchanga anchors), B2_sudarshana(42ee85bb, expose signal_type_class on bodha_signals_get alias; filter is pre-LIMIT so class-scoped query bypasses the per-ayanamsha window; salience UNTOUCHED), B7(57a82ef3, cap ranked_signals to 25/lens=RANKED_HYDRATE_PER_LENS w/ N.6 disclosure -> ~120KB capability -> <=40KB served), B_d2_hora(d4175a6f, chart_facts_query serves chart_divisionals rows in a budget-capped source-tagged divisional_facts section when divisional_chart set -- no new tool/migration). Integrated on fix/d1-5b-gateb-serving-integration (base 0566e1d6): 2 shared files auto-merged clean in disjoint regions (register_d7_channel.ts x2, register_p1_aliases.ts x3), all changes verified present. Full re-test: platform tsc clean + 5573/0; platform-mcp tsc clean + 75/435 (pre-existing baseline, 0 new)."
next_phase:
  cycle_2: "B-3 (Sudarshana), B-4 (Bhavat Bhavam), B-6 (serving hygiene), B-7 (governance+derived view). B-3/B-4 need the salience DR formalized at cycle-2 open (Binder draft: sudarshana_agreement=1.15, frame_divergence=1.00, bhavat_bhavam_amplifier=0.85, subsystem=structural). B-6 is single-owner of the retrieval registry (serving). B-7 lands the N.6 density text + CI harness + dasha_lord_capability derived view."
  then: "single FULL L1->L5 rebuild via the Cloud Run job path (validated this session -- NOT the laptop proxy) + Gate B battery on the deployed connector. Gate B assertions must be built as harness scripts extended from Lane A-0's harness (no named lane owns this -- conductor/a small pre-gate task builds it)."
gate_b_final:
  run: true
  result: "D-1.5b Gate B = 17/17 GREEN on the deployed connector (amjis-mcp @ 470f2290 proxying amjis-web's capability API). All B-* assertions pass: B1_chalit, B2_sudarshana, B3_bhavat_bhavam, B4_bhava_bala, B5_sav_bav_sign_keyed, B6_positions_lead, B7_budgets, B8_n6_anchor, B9_ci_density_job, B_karakamsha, B_shadbala_ratio, B_d2_hora_class, B_anchor_dedup, B_remedies_search_honest, B_structural_envelope, B_dasha_lord_capability, B_dosha_gate_kalasarpa."
  path_to_green: "First live run surfaced 4 real serving gaps + 5 harness bugs + the rate-limit cascade. Fixed all 4 serving gaps (PR #575: shadbala INVARIANT, sudarshana class-scope, B7 lens-cap, d2-hora divisional_facts) + the harness bugs (0566e1d6) + B7 2nd-pass (PR #576: template_element_ids_jsonb.signal_ids was the residual 155KB->41KB) + wired the B2/B_d2_hora assertions to the live serving fixes (PR #576). B7 confirmed 41KB (was 909KB). Each fix independently diagnosed+tested; PR #575's 4-fix bundle adversarially verified (incl. DB-level INVARIANT-pivot-collision analysis -> no corruption)."
  d1_5a_carryforward: "The 2 non-B reds in the full 32-battery (assertion 4 = CR-90 keyword_heuristic_v1 valence; A7 = parashari-aspects serving) are the IDENTICAL 2 items D-1.5a documented as PARKED (REPORT_D-1.5a.md lines 75-76) -- pre-existing, non-regressions, out of D-1.5b scope, and provably unaffected by D-1.5b's TS-serving-only fixes (valence_source is a build-time field). Also confirmed the deployed connector rate-limits under the full 32-assertion sustained load (429 cascade); the 17 B-* run in one batch stays under that threshold and is clean."
updated_at: "2026-07-15 (D-1.5b pre-lane blocker RESOLVED; estate 64/64 lit & healthy via Cloud Run job; ready to spawn cycle-1 lanes B-5/B-2/B-1)"
```
