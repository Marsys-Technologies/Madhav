---
artifact: BRIEF_D4A
type: WAVE BRIEF (two-part: FROZEN + BIND-AT-OPEN)
wave: D-4a — Measurement Foundry
version: 1.0
status: FROZEN — cut from TEMPORAL_ENGINE_ARC_PLAN_v1_0.md §3 (native-ratified 2026-07-19);
  §B slots bind at wave open
governing: CONDUCTOR_PROTOCOL.md + TEMPORAL_ENGINE_ARC_PLAN_v1_0.md (the ARC PLAN is this
  wave's design authority; BRIEF_D4.md v2.0 is SUPERSEDED-BY-ARC and serves as lane-text source)
prerequisite: D-3 CLOSED_BLOCKED_RED (sealed); pre-D-4 staging complete (PRE_D4_WRAPUP_REPORT);
  native questionnaire RETURNED (NATIVE_DATE_TIGHTENING_RESPONSES_v1_0.md — ingestion is A-1's
  job, item-#3-quarantine RESOLVED, all corrections append-only)
gate: §G below — harness-capability gate, NOT a calibration gate; DR-12 adjudication is
  explicitly DEFERRED to D-4b (any reading of A-5's dry-run as a model ruling is a violation)
---

# D-4a — Measurement Foundry

Purpose: after this wave, ANY timing model can be scored honestly against the LEL under DR-13
semantics with mirrored controls — and the prospective outcome clock is running. Full lane
rationale + arc context: TEMPORAL_ENGINE_ARC_PLAN §§0–3. Execution discipline, operational
constants, promise-ledger and DONE definition: inherit BRIEF_D4 v2.0 §F3 verbatim (unchanged).

## §F1 — Lanes (6)

**A-0 — Serving-substrate repair.** BRIEF_D4 v2.0 Lane C-0 verbatim, horizon amended to
chart-relative birth→birth+100y (ARC PLAN §10 Q3): CR-109 cardinality fix (FIX-COV worktree is
the starting point; surgical migration §N.4; serve every period the dasha table holds);
CR-110 double-spine (VERIFY-FIRST per CR-96, then ayanamsha filter/disclosure); CR-111
convergence build-vs-serve join (VERIFY-FIRST); D-3 per-event artifact rescue into
`artifacts/D-3/` (re-derive + mark if scratchpad expired); canonical_faces +3 tools.
Zero kernel/weight/threshold/orb/valence changes — Opus verifier checks the diff for exactly that.

**A-1 — Shape-aware matcher + LEL v2 + native data ingestion.** CR-47 matcher root fix, speced
against DR-13 (point/interval/chain; overlap scoring; confidence-scaled tolerance: exact ±45d /
month ±75d / year → labeled secondary interval battery). LEL schema v2 additive migration
(APPROVED proposal). Ingest NATIVE_DATE_TIGHTENING_RESPONSES_v1_0.md per its own ingestion
notes: 4 corrections (#2 congenital stammering, #3 dialogues-2001 initiation, #10 Mahadev
2021-04/05, #13 relationship-end 2022-07-14) as append-only v2 correction entries with
provenance; windfall interval [2010-07→2011-03]; item #1/#5 arc linkage; #7 onset day-locked to
the arthroscopy event; #16/Mahindra-crash confirmations. leverage_index `subject=venus`
false-empty fixed here or in a named D-4b row. Verifier: both named specimens match with
nonzero fit (windfall scored as INTERVAL), synthetic chain event scores per-milestone, all
corrections resolve via provenance not overwrite.

**A-2 — Canonical event ontology.** ARC PLAN §3 A-2 verbatim — stable event-class IDs shared by
LEL/evidence/λ_e; event-class-specific evidence (gain ≠ loss); non-discriminating self-report;
kill-switch criteria as data; **canonical temporal SHAPE per class** (point / interval+duration-
prior / chain+milestone-template, incl. irreversibility milestone) as ontology data consumed by
A-1, A-4, and D-5. Published as a first-class artifact other waves import.

**A-3 — Controls + proper-scoring harness.** Shuffled-birth + antiphase controls REAL (stubs
die); CRPS/log-score primary with skill = 1 − CRPS_model/CRPS_control, hit-rate legacy
secondary; control-mirroring ENFORCED IN THE HARNESS (a scoring rule not applied to the control
refuses to run); model-agnostic interface: anything exposing `curve(chart, event_class,
[t1,t2])` is scoreable. Sealed test-split discipline unchanged (ESCALATION §4).

**A-4 — Prospective ledger, LIVE.** ARC PLAN §3 A-4 + §11 data-governance verbatim: prediction
store with claim_shape validated against A-2 ontology (point-claim for interval-class = schema
violation), generator_class enum [anchor_engine | reading_synthesis | engine |
native_intuition], configuration_signature (nullable until D-5), falsifier mandatory;
LEL-append → outcome-matching hook via A-1; native-readable served surface; first entries =
the wealth-baseline arc predictions (Sat–Jup pratyantar 2027-04-09→08-18; Ketu-MD
consolidation shape 2027–2034; Venus-MD 2034 activation) each with falsifier + generator_class
reading_synthesis. **§11 governance is binding: predictions exist by explicit filing only;
chat is never mined.**

**A-5 — Harness dry-run (gate lane).** Score midpoint-triangle, pratyantar-lord, and
transit-kernel-on-repaired-substrate through A-3 on the full DR-13-shaped LEL (post-A-1
ingestion). Per-model per-event tables committed as artifacts. Results are DIAGNOSTICS —
baseline for D-5's per-lane mini-checks — NOT the DR-12 adjudication (deferred to D-4b;
recorded in the run header itself).

**A-6 — Doctrine registration.** DR-14/15/16 (texts in DR_14_15_16_TEMPORAL_DOCTRINE_v1_0.md,
native-ratified in substance 2026-07-19; DR-16 carries the honest-clarity principle) registered
in DISAGREEMENT_REGISTER with DIS numbers; DR-12-deferral note; ARC PLAN status → RATIFIED;
governance pointer sweep (CLAUDECODE_BRIEF wave_sequence [.., D-4a, D-5, D-4b], CURRENT_STATE
banner).

**Merge order:** A-0 → A-1 → A-2 (parallel with late A-1) → A-3 → A-4 → A-5; A-6 docs-only,
anytime after open. Carried D-2 findings #2/#4 dispositioned before close (fix or
PARK-with-owner).

## §B — BIND-AT-OPEN slots
- CR-110/CR-111 independent reproduction results (fix scope depends on findings).
- FIX-COV worktree applicability to current main.
- A-2 ontology's event-class enumeration ← LEL HEAD actual classes (enumerate, don't assume).
- A-5 dry-run pre-registration: event set (post-A-1 ingestion), thresholds, control
  construction — committed before scoring.
- Matcher root-cause hypothesis ← C-1-style diagnosis spike (read matcher code + one traced run).
- Prospective-ledger schema ← reuse mimamsa_outcome_record/LEL §9 PPL prior art where possible.

## §G — Gate
1. A-0 live assertions: full-span (birth→birth+100y) windows served, >1 period per lord where
   the table holds them; single disclosed dasha spine; ≥1 TRIGGER-refined 2026–2027 convergence
   window served matching DB; artifacts committed; census == canonical list.
2. Matcher: both named specimens nonzero fit, windfall as interval, chain event per-milestone;
   all 4 native corrections queryable with provenance.
3. Ontology published; matcher + ledger provably consume its shapes (schema-violation test).
4. Controls real; mirroring enforcement demonstrated (a one-sided rule refuses to run).
5. Dry-run complete: 3 models scored end-to-end, per-event tables committed, header carries the
   DR-12-deferral disclaimer.
6. Ledger live: ≥5 falsifier-bearing entries incl. the 3 baseline-arc predictions; append-hook
   demonstrated on a test append; §11 governance text served on the surface itself.
7. Carried findings #2/#4 dispositioned; anti-gaming pass on 1–6; all prior batteries green.

## §C — Close
REPORT_D-4A + STATE_D-4A sealed; promise ledger complete per §F3; `current_wave` → D-5
(INCOMING — BRIEF_D5 §B binds against D-4a's shipped reality); pause for native go.
