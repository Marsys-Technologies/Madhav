---
artifact: BRIEF_D2
type: WAVE BRIEF (two-part: FROZEN + BIND-AT-OPEN)
wave: D-2 — Vidhi Engine + Mechanism wave
version: 1.0
status: FROZEN — §B slots bind at wave open (Fable Binder, per CONDUCTOR_PROTOCOL §2 step 1)
governing: CONDUCTOR_PROTOCOL.md + DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md §5 +
  DOCTRINE_CAMPAIGN_DESIGN_v1_0.md §3/§6/§12
prerequisite: D-1.5b gate GREEN. Track-3 pre-work (vidhi floors/primitives authoring) should be
  COMPLETE before open — if not, V-1 absorbs it (schedule cost, not a blocker).
gate: the design-§8 MASTER ACCEPTANCE TEST run by a FLOOR-MODEL (Sonnet) reading agent (§G).
---

# D-2 — Vidhi Engine + Mechanism

## FROZEN §F1 — Lane map (7 lanes)

### Lane V-0 — Gate harness extension
The master-acceptance harness: (a) the 6 §G.0 load-bearing wealth conclusions as executable
presence checks ("conclusion N traceable to a served signal/verdict in the top-15 of its domain
surface"); (b) census battery (126-tool sweep, PASS/DEGRADED/EMPTY/FAIL counts vs the §F baseline);
(c) completeness-receipt validator; (d) alias-count check vs the declared canonical-face list.

### Lane V-1 — Vidhi registry + compiler
~30 primitives as versioned registry rows (each: definition, live-tool mapping + args, fallback
face, `known_gap` ref into the CR register); per-intent-class acharya floors + machine bands as
data (worked example `floor(wealth_deepdive)` in design §3 is the template); the compiler:
question → scope_tuple(intent, domains, width, depth, horizon, intervention?, entitlement) →
contract. Deterministic: identical tuple → identical contract. Track-3 authored content loads here.
The CR-27 improvisation corpus (the logged Class-9 instances: diff-answered-fresh, supply-shaped
plans, chain missed, nakshatra omitted) is REQUIRED INPUT to floor design — each logged
improvisation must be prevented by a floor item or explicitly noted as out-of-contract.

### Lane V-2 — MCP delivery + receipts
Vidhi registry as MCP resource; compiled plans as MCP prompt (+ `plan_retrieval` meta-tool
fallback); scope tuple echoed for correction before execution; **completeness receipt** on every
synthesis (served/empty/dark per floor item, each dark citing its CR row); `capability_version` +
`tools/list_changed` staleness kill.

### Lane V-3 — Channel: two-pass + capability map
Pass-1 SCAN face (ultra-dense subject-bearing index lines, ~60B/row) + Pass-2 FETCH-by-id; the
capability map gets a live source (closes CR-9 auth); twin-alias dedup 129 → the canonical-face
list, deprecated faces removed from the LLM-visible list (CR-30/51); description-vs-payload CI
audit (CR-44 class); errors-that-teach (return the corrected call — CR-7 class); per-chart
reading-notes (CR-38/71/80) as MCP resources; chart-keyed special-lagna access (CR-16); pact MD-lord
naming (CR-15); holistic bundle sub-tool repair, non-ok on sub-errors (CR-14/39); intent_classify
ratified as the scope-tuple classifier (CR-28).

### Lane V-4 — Mechanism object + CGM elevation
Mechanism = named, valenced CGM subgraph, first-class table + serving face; real edge-strength
formula from the ga_vichara valence pass (retire hardcoded literals, CR-86); arudha + special-lagna
nodes; chain/circuit motifs (the 10→8→12→10 specimen MUST exist as a served mechanism — CR-24);
completed centralities (eigenvector/betweenness/harmonic); CGM astrological salience joining
composite ranking (CR-25 — graph no longer structurally subordinate to MSR); **close both dead
links**: CGM metrics →
MSR `structural_role` (post-CGM re-rank pass, CR-84) and centrality → convergence consumption
stub-removal (CR-85; full consumption is D-3); **retire `bo_anveshana`** (CR-78) — discoveries
become mechanism-derived; D10 lord-placement/karaka joins into the career lens (CR-62 multi-varga
map: wealth {D1,D2,D9,D11}, career {D1,D9,D10}).

### Lane V-5 — Semantic signal classes
Nakshatra-semantic (own-star, dispositor chains, tara bala, end-degree flags — CR-26/64), arudha
(AL conjunctions, A2/A11, AL–bhāva relations — CR-61), special-lagna (Indu/Sree/Ghati/Hora with
domain salience — CR-76), and vargottama + dhana-axis classes (vargottama amplification as a
first-class signal; complete-2/11-axis tenancy — CR-36): each a NEW emitter module folding into the
signal-class registry (append-only; no shared-file edits with V-4's re-rank pass). Salience/priors
per §B binding.

### Lane V-6 — Doctrine completions
Upapada wiring (BPHS ch.30 rules from L0 corpus × computed A12 — CR-101); Nārāyaṇa Daśā (CR-104);
pañcadhā-maitrī compound matrix (CR-105); Chara Daśā wired as a timing witness (CR-77 amended —
wiring, not build); kendrādhipati-doṣa + per-lagna functional-benefic completion (D-1.5 deferral;
**Adjudicator-doctrine required** — extends the valence matrix, DR-n recorded); bespoke detection
for remaining catalog doshas (B9 residue: CR-72/73 completion — every served dosha either
per-chart computed with cancellation or catalog-gated).

**Merge order:** V-1 → V-4 → V-5 → V-6 → V-2 → V-3 → V-0-final-run. (Registry first; graph before
classes that read it; serving last.)

## FROZEN §F2 — must_not_touch
FROZEN orchestrator contract (PARK) · Gate-A/B surfaces' semantics (regression-guarded by re-running
those batteries in this wave's gate) · ka_* convergence internals beyond V-4's stub-removal (D-3) ·
L5 calibration (D-4) · KP sub-lord engine (deferred; consumes B-1's real cusps when it comes).

## §B — BIND-AT-OPEN slots (Fable Binder)
- `known_gap` annotations per primitive ← what D-1.5 actually closed (read both close reports +
  register HEAD).
- Salience constants + class priors for V-5's three classes ← post-D-1.5 MSR distribution (probe
  `bodha_signals_get` tier spread; Adjudicator-doctrine sets values, DR-n).
- V-4 edge-strength formula terms ← the shipped ga_vichara valence output shape (probe
  `ganita_vichara_get`).
- Canonical-face list (the ~30) ← live census at open (Track-3 draft exists; bind against reality).
- Floor content final check ← Track-3 authored floors reviewed against the D-1.5b-added surfaces
  (chalit, Sudarshana, BB, bhava-bala atoms MUST be in the relevant floors — this is §5-of-plan
  step 3: capabilities become contractually consumed here).
- Rollback pin + Gate-A/B regression baseline green.

## §G — Gate (the wave's defining test)
1. **Master acceptance, floor-model:** a FRESH Sonnet reading agent, given only the served surfaces
   + the compiled vidhi (no priors, no register, no hand-derivation), produces the 482012f1 wealth
   reading; harness asserts each of the 6 §G.0 conclusions is traceable to a served top-15
   signal/verdict. 6/6 required. (Floor-model rule: if Sonnet passes, the instrument does the work.)
2. Census battery ≥ baseline (no regressions); alias count = canonical list; completeness receipt
   present on the reading; Gate-A + Gate-B batteries still green (regression).
3. A second synthesis probe on a NON-wealth domain (career) to prove the vidhi generalizes: floor
   items served or honestly dark-cited, receipt validates.
