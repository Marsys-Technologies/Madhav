---
wave: D-3
lifecycle_step: 5  # DEPLOY complete (native GO 2026-07-18): PR #602 (wave/D-3/integration ->
                   # main) merged squash SHA 11377530892799afd8015d3ee9b6ec68efeb0c0d, full CI
                   # green (incl. the ~8.5min Governance Gates pytest step), Deploy to Cloud Run
                   # workflow completed success. Live SHA confirmed via gcloud describe:
                   # amjis-web AND amjis-mcp both @ 11377530... Rebuild determination: NOT
                   # NEEDED — T-1 diff (get_av_transit_gating.ts/index.ts/register_p1_aliases.ts/
                   # test) contains zero migrations and zero new writers, pure read-serving code
                   # over already-populated chart_facts categories; kala_temporal.ts hotfix is
                   # serving-only. No R-5/§8.2 rebuild trigger applies.
                   #
                   # POST-DEPLOY LIVE READS (both run against the deployed connector):
                   # (1) PRIMARY/hotfix routing — kala_temporal_bundle(482012f1, fwd 1yr):
                   #     timeline_excerpt now 7 rows (was 0), timeline_count:7, active_dasha
                   #     resolved (Mercury MD/Saturn AD), kala_readiness.score=0.7. POPULATES ->
                   #     the double-unwrap WAS the root cause. HOTFIX DISPOSITION = KEEP (critical
                   #     path for T-2/T-5). CR-41 writer-supersession disposition is UNCHANGED/
                   #     SEPARATE (this was a serving bug, not the writer's forward-horizon
                   #     defect) — stood down as a rebuild question per the native's routing.
                   #     Note: convergence_windows/obstructions still 0 in this window — not
                   #     probed further, outside this check's scope.
                   # (2) REGRESSION GUARD — judgment_query(wealth, 482012f1): Dhana Yoga
                   #     (dhana_yoga_2_5_9_11, strength 1.0218) STILL present in bearing_yogas —
                   #     matches. But verdict_grade=convergent_moderate (expected
                   #     convergent_strong) and composite_score=2.38 (expected ~2.78) — BOTH
                   #     MOVED. RED per native's explicit stop-and-report instruction. None of
                   #     the 3 merged lanes (T-0 script, T-1 new capability, kala_temporal.ts
                   #     hotfix) touch wealth-verdict computation (scope-warden confirmed on all
                   #     3) — not an obvious lane regression. Hypotheses NOT investigated further
                   #     (no fix-loop per standing rules): (a) time-dependent drift — kala_
                   #     activations/dasha_activation_proximity_score terms feeding the composite
                   #     are date-relative, "today" has advanced since the D-2 baseline reading;
                   #     (b) the two unrelated PRs (#600 docs, #601 perf-batching) that landed on
                   #     main during this cycle. STOPPED HERE, reported to native, no further
                   #     action taken pending disposition.
                   #
                   # Follow-up-audit finding (3 /api/retrieval/capability callers sharing the
                   # possible double-unwrap bug class) logged in DISAGREEMENT_REGISTER_v1_0.md
                   # as a non-DR FINDING entry (register_p1_synthesis.ts, register_p1_reference.ts,
                   # l0_brahmagyan.ts) — out of D-3 scope, not chased.
cycle1_status: CLOSED  # T-0 + T-1 + kala_temporal.ts hotfix deployed clean; wealth guard
                       # re-baselined convergent_moderate/~2.38 (benign, 12/12 yogas fire
                       # structurally). Not re-opened.
status: CLOSED_BLOCKED_RED  # SEALED 2026-07-18 per native closeout directive. D-3 closes
                 # BLOCKED-RED per the standing pre-committed ruling (MEMO_D-3_1.md's Option-C
                 # amendment: "if §G then fails, that's a true red about the kernel, and it
                 # stands"). A1's coverage-matched control (-15.8pp, nearly identical to the
                 # original -16.1pp) establishes the red as a genuine kernel finding, not a
                 # coverage artifact -- this is what makes the closure safe to seal now rather
                 # than wait on a re-run. The standing one-re-run allowance LAPSES WITH THE WAVE;
                 # nothing further re-litigates this gate. current_wave has been ADVANCED to D-4
                 # (status INCOMING -- brief under native/Cowork revision, NOT opened, NOT bound).
                 # Full closure record: REPORT_D-3.md (final). §G RETRODICTION GATE RAN — RESULT: RED (2026-07-18). Full sequence per native's
                 # Option-C-amended disposition executed cleanly: FIX-PSEL fixed the predicate-
                 # selection bug (verified), PERF-TRIGGER-CACHE fixed the resulting rebuild hang
                 # (verified), the chart rebuilt clean, the falsifier confirmed TRIGGER genuinely
                 # fires on real data (16,767 rows). The gate then ran ONCE, blind, full LEL
                 # access, per the native's "run it blind, a red stands" directive. RESULT: RED —
                 # both named-mechanism checks miss their top-decile bar (81%/67% of threshold);
                 # the blind battery misses its 50% floor at 17.5% AND scores WORSE than its own
                 # shuffled-birth negative control (-16.1pp gap, the most damning number in the
                 # run); anti-gaming pass confirms the curves are real/non-degenerate (this is a
                 # genuine miss, not a construction artifact). Root cause (disclosed, not an
                 # excuse): TRIGGER's real footprint on this chart is a narrow 2026-2027
                 # non-wealth window that doesn't reach either historical wealth anchor event; the
                 # served activation surface has zero coverage before 2010 or after ~2032. Per
                 # ESCALATION_POLICY §2.1 (RED INTEGRITY GATE — "the Adjudicator may not
                 # disposition a red integrity gate toward green"), HALTING AND REPORTING to
                 # native now. No further tuning/re-run attempted. Full record: REPORT_D-3.md +
                 # this file's cycle2_gate_G_FINAL_RESULT block. DR-12 (D-4's midpoint-triangle vs
                 # pratyantar-lord vs transit-kernel bakeoff) already anticipated this outcome
                 # class — this RED result is direct evidence for that comparison.
status_prior: ACTIVE  # NATIVE DISPOSITION RECEIVED 2026-07-18 (MEMO_D-3_1.md): Option C, AMENDED.
                 # D-3 stays open; §G waits for a real TRIGGER exercise (not Option B — closing
                 # with the empirical contribution unexercised converts the flagship falsifiable
                 # gate into deferred debt D-4's calibration ignition would then inherit; not
                 # Option D alone — mechanism correctness already established, what's missing is
                 # real-data exercise, a synthetic pass can't substitute). AMENDMENT to C: fix the
                 # disease not just the symptom — two-part FIX-PSEL/T-7 lane: (1) diagnose the
                 # dignity_score=1.0 saturation (4,441 predicates at exact ceiling) — fix at
                 # source if clamp/default-fill bug, document if genuine ceiling-by-construction;
                 # (2) selection hardening regardless of (1) — per-signature-class quota + a
                 # content-hash (not insertion-order) tiebreak, grep estate-wide for the same
                 # antipattern. Full implement->verify->integrate->deploy->L3-rebuild cycle, THEN
                 # check the memo's own falsifier (Mode A/B rows w/ trigger_weights_used present),
                 # THEN run §G once (standing one-re-run rule). GUARD: quota must not be tuned
                 # against §G's outcome — freeze selection, then run the gate blind; a red §G
                 # after that stands as a true red. STOP-and-report if diagnosis traces into
                 # ga_vichara or the core scoring writer (don't let a selection fix silently
                 # become a scoring rework) — bring expanded scope back to native before proceeding.
cycle: 2  # CYCLE-2 OPEN (native directive 2026-07-18): T-2/T-3 transit kernel, T-4/T-5
          # kernel-admission loop, T-6 serving, then the §G retrodiction gate battery
          # (the empirical crux of D-3). Binder pass complete, BOUND — see
          # BIND_D-3_CYCLE2.md. GO received 2026-07-18 (native: run fully autonomously
          # under ESCALATION_POLICY_v1_0.md to the §G gate). RR-fix/T-2/T-3: all 3 lanes
          # receipted ACCEPT, integrated (391 tests clean), merged (PR #603, eb21bb61),
          # deployed (web/sidecar/pipeline-job @ eb21bb61, mcp unchanged @ 11377530 —
          # correct, no platform-mcp touch this cycle), rebuild ruled NOT NEEDED (no
          # writer/schema/serving-surface change yet — T-2/T-3 are unwired new capability).
          # NEXT: T-4/T-5 kernel-admission loop, then T-6 serving (wires T-2/T-3 in),
          # THEN a rebuild trigger will likely apply once T-6 repoints serving surfaces.
cycle2_bind_record: 00_ARCHITECTURE/llm_consumption_audit/briefs/doctrine_waves/BIND_D-3_CYCLE2.md
cycle2_notes:
  - "B-3 correction: cycle-1 BIND_D-3.md recorded test-span start as 2020-01-15; live
     re-verification shows 2021-01-15 (1-char transcription slip, counts unchanged
     21/21). Zero test events fall in calendar-year 2020."
  - "Test-split (21 events) content NEVER inspected this pass — counts/boundary only,
     per the native's blind-construction constraint. Sacrosanct until T-0's single
     scoring pass."
  - "Kernel admission order (B-4) unchanged from cycle-1, strengthened by cycle-1
     delivery (dasha spine + SAV/kakshya now proven-serving, still ranked by expected
     retrodiction lift not implementation readiness). Two T-0 flag-forwards (sparse-
     curve top_decile_fraction=1.0 artifact; localMax grid tie-break on peak_lag_days)
     must be addressed in the kernel/admission-loop scorer before train-delta scoring,
     or they contaminate the staged-admission measurements."
brief_bound: true
bind_record: 00_ARCHITECTURE/llm_consumption_audit/briefs/doctrine_waves/BIND_D-3.md
cycle2_rollback_pin:
  image_sha:
    amjis-web: 11377530892799afd8015d3ee9b6ec68efeb0c0d
    amjis-mcp: 11377530892799afd8015d3ee9b6ec68efeb0c0d
    amjis-sidecar: b536e13b63168839187692b50f94a3d334f5ee1b
    brahma-build-pipeline-job: b536e13b63168839187692b50f94a3d334f5ee1b
  abhisek_build_id: b84c3797-b64a-4956-a431-5f4ccdf9ee55
rollback_pin:
  image_sha:
    amjis-web: e8b1047f56a06591352dbb748373a59b6dea5715
    amjis-mcp: 8c3b21a9afa88934e621a9d525f460a72ed5ca52
    amjis-sidecar: 6487694fe70635e12c84746443ee2359c51b447d
    brahma-build-pipeline-job: 6487694fe70635e12c84746443ee2359c51b447d
  abhisek_build_id: b84c3797-b64a-4956-a431-5f4ccdf9ee55
adjudications:
  - {dr: DR-10, dis: DIS.023, subject: "within-period peak model", ruling: "pratyantar-lord decomposition is classical default; midpoint-triangle DEPRECATED (never bare-served); peak_basis provenance mandatory; transit-kernel supersedes both where computed", recorded: true}
  - {dr: DR-11, dis: DIS.024, subject: "T-0 retrodiction-gate thresholds v1", ruling: "±45d window / top-decile salience / ≥50% blind-battery hit-rate vs shuffled-birth control; DR-revisable only; anti-gaming rule binding (no primary-runner-only green)", recorded: true}
  - {dr: DR-12, dis: DIS.025, subject: "D-4 peak-model adjudication hook", ruling: "D-4 battery MUST score midpoint-triangle vs pratyantar-lord vs transit-kernel against LEL corpus; data retires the loser; forward-binding, inherited by D-4 Binder at its own bind", recorded: true}
carried_item_dispositions:
  - {item: "orchestrator run-rollup race", verdict: "diagnosis confirmed current; §N.2 STOP not triggered (run-machinery-owned, no WriterBase contract change)", disposition: "first-agenda D-3 lane, platform-owned: (A) run-start rollup reconciliation from build_run_assets children, (B) watchdog reconcile-to-truth instead of blind-fail. Both idempotent.", status: "pending spawn as first lane"}
  - {item: "kala_avadhi hollow timeline", verdict: "REVISED — original (a)/(b) classification was itself premature. Rebuild-dispatch agent found via direct DB query (live pipeline DB): kala_avadhi=1571 rows / kala_convergence=1580 / kala_darshana=750 / kala_obstruction=679 for 482012f1, computed_at 2026-07-17T01:47:24Z (build b84c3797, asset_set rebuild, already ran and completed cleanly ~21h before this wave opened). Vimshottari coverage spans 1949-2100. Data was NEVER missing. Underlying capability endpoints (query_dasha_dossier/query_convergence_windows/query_obstruction_periods via /api/retrieval/capability, same principal as the MCP connector) return real rows fine (43/560/679 for 2010-2027).", root_cause: "CODE BUG, not a data gap: platform-mcp/src/tools/retrieval/kala_temporal.ts fetchCapabilityRows() double-content-unwrap defect. /api/retrieval/capability's real response shape is {ok, content:{content:{rows:[...]}, is_error}} (wrapped twice) but fetchCapabilityRows() only unwraps once (reads data.content.rows instead of data.content.content.rows), so rows is always undefined -> silently falls back to [] while still reporting ok:true. Systemic to EVERY chart kala_temporal_bundle is called for, not Abhisek-specific.", disposition: "NO rebuild dispatched (agent correctly aborted once the true cause was found — a rebuild would have been a no-op and risked a false-positive 'fixed' read). One-line fix identified: const rows = (content as {content?:{rows?:unknown}}).content?.rows — applies to all 3 fetchCapabilityRows call sites in computeKalaTemporalBundle plus the snapshot sub-calls sharing the helper. NOT yet applied — new finding, outside the T-0/T-1 spawn scope authorized this cycle; routing to native/conductor for disposition (fold into T-6 now vs. quick standalone hotfix lane) rather than unilaterally spawning new work.", status: "DIAGNOSED, NOT FIXED — awaiting disposition"}
rebuild_scope_ruling:
  scope: pending  # full-wave scope not yet computed; the kala_avadhi scoped rebuild above is a
                  # separate pre-spawn action, not the wave's own T-lane rebuild (lifecycle step 6)
  expected_per_brief: "L3 convergence assets + Taranga service; L2 read-only (no L2 rebuild) — BRIEF_D3 §8.2 per-wave expected scope"
cycle1_lanes:
  - {lane: T-0, branch: wave/D-3/T-0, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T0, base: origin/main@b536e13b, head_sha: 9a67f986, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=40/40, scope_warden=pass, live independent reproduction on 482012f1 matched implementer numbers EXACTLY (check-a intensity0/threshold0 FAIL, check-b 2/6 FAIL, check-c 18/40=0.45 vs 0.50 floor FAIL, control_gap=-0.0464 FAIL). DR-10 peak_basis=dasha_lord_confluence_v1 correctly NOT claimed as a named model. DR-11 anti-gaming independent-reproduction requirement satisfied by this verifier pass itself. All-red retrodiction result judged legitimate v1-proxy weakness, not a defect. 2 flag-forward non-blocking items for T-2/T-3 kernel work: (1) top_decile_fraction=1.0 sparse-curve artifact on check-a (does not flip result), (2) verifier-found localMax grid tie-break makes lead/lag distribution (peak_lag_days=-42 on every hit) uninformative, no pass/fail impact."}
  - {lane: T-1, branch: wave/D-3/T-1, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T1, base: origin/main@b536e13b, head_sha: 3d888223, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests: platform 5687/5687 pass, platform-mcp 515/605 pass (75 pre-existing failures verified byte-identical vs origin/main baseline, none reference AV/kakshya code), new file 25/26 (1 live-integration skipped). scope_warden=pass (exactly 4 files, all in-lane, append-only registrations). CR-87 verified: chart_id required no-default both modes. Live SAV bindus independently re-pulled and re-computed by verifier: 29,29,27,32,30,26,34,32,25,27,23,23 (sum=337 classical total) — exact match; house7=34 amplifying/house10=27 damping confirmed against BRIEF_D3 type specimens. Both v1-tradeoff flags (kakshya duration correction, Lahiri mean-rate sidereal approx) judged RIGHT calls matching the brief's own illustrative framing, not defects. One non-blocking Phase-2 note: kakshya_windows live-serving depends on sidecar transit endpoint, unproven at Phase-1, gate runner should exercise post-deploy."}
cycle1_integration_branch: {branch: wave/D-3/integration, base: origin/main@b536e13b, merges: ["9a67f986 (T-0, fast-forward)", "3d888223 (T-1, merge commit 9166f5ca)", "04643e3a (hotfix-kala-temporal, merge)"], suite: {platform: {tsc: clean, eslint: clean, vitest: "5727 passed / 317 skipped / 1 todo, 0 failed"}, platform_mcp: {tsc: clean, vitest: "517 passed / 75 failed / 15 skipped — pre-existing baseline (+2 new passing from the hotfix), no new failures across all 3 merges"}}, pushed: false, merged_to_main: {pr: 602, sha: "11377530892799afd8015d3ee9b6ec68efeb0c0d"}}
cycle1_hotfix_lanes:
  - {lane: hotfix-kala-temporal, branch: wave/D-3/hotfix-kala-temporal, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-hotfix-katemporal, base: origin/main, head_sha: 04643e3a, status: receipted+merged, verifier_model: opus, merged_into: wave/D-3/integration, receipt_ref: "verdict=ACCEPT, scope_warden=pass, tests: 75 failed/517 passed/15 skipped (own vitest run) vs origin/main scratch-checkout baseline 75/515/15 — delta=+2 new passing tests only, no regression. Wire shape independently traced from source on BOTH ends (route.ts response wrapping + all 4 L3 handlers' is_error-bearing return shape) — diagnosis confirmed, not taken on faith. CR-93/94 precedent independently confirmed as a faithful transplant of an already-accepted identical fix. Judgment call: code-level evidence (source-traced shape + existing precedent + corrected regression test with a negative don't-mis-unwrap case) sufficient for Phase-1 ACCEPT with live confirmation properly deferred to Phase-2 post-deploy gate, per protocol's own two-phase design — verifier explicitly does not consider this risky enough to require live confirmation before merge. FORWARD-POINTER (not blocking, not this lane's scope): 3 other /api/retrieval/capability callers (register_p1_synthesis.ts, register_p1_reference.ts, l0_brahmagyan.ts) return content raw without the is_error descent — may share the same latent bug class depending on downstream consumption; flagged for a follow-up audit, not traced to a conclusion.", integration_note: "merged into wave/D-3/integration cleanly (2 files, 73 insertions/4 deletions); platform-mcp re-run post-merge: tsc clean, vitest 75 failed/517 passed/15 skipped — matches hotfix lane's own count exactly, full wave integration remains clean."}
cycle1_deploy: {done: true, pr: 602, merge_sha: "11377530892799afd8015d3ee9b6ec68efeb0c0d", images: {amjis_web: "11377530892799afd8015d3ee9b6ec68efeb0c0d", amjis_mcp: "11377530892799afd8015d3ee9b6ec68efeb0c0d"}, rebuild: {needed: false, reasoning: "T-1 and hotfix are both pure serving-layer changes, zero migrations, zero new writers, no new fact categories — no R-5/§8.2 trigger"}}
cycle1_gate: {run: false, blocker: null, note: "regression guard RESOLVED BENIGN 2026-07-18 — see carried_item_dispositions / DISAGREEMENT_REGISTER wealth-rebaseline FINDING. ganita_yoga_firings_get confirmed 12/12 yogas fired=true at unchanged strengths (fork check per native directive); the 2.78->2.38 delta is entirely yoga_term, driven by domain-bearing classification 4->3, itself downstream of kala_activations now populating (this wave's own hotfix) + the DR-9 Part B affliction/threat layer going live — not a lost firing. Wealth guard RE-BASELINED to convergent_moderate/~2.38+Dhana Yoga+affliction layer+kala_activations populated; the prior 2.78 pin retired as pre-timing/pre-affliction. Not a D-3 wave-blocker (D-3's gate is the T-0 retrodiction battery). No code changes made."}
cycle2_followups:
  # Non-blocking items surfaced by Phase-1 verifiers, tracked for a later lane/session, not this
  # PR's scope. Native/campaign async-review queue per ESCALATION_POLICY §3.
  - {item: "T-3-followup: wire compute_promise into its actual CR-88 kill sites (services/ka_temporal/date_resolver.py::_proximity_score, pipeline/orchestrator/writers/ka_kalasutra.py:205)", gate: "verifier recommends an Adjudicator-doctrine ruling on the 0.4 NBRY inactive_weight calibration BEFORE this wiring lands (it will scale a real timing weight once consumed)", status: open}
  - {item: "RR-fix Phase-2 follow-up: build_run_assets rescue-UPDATE join (chart_id IS NOT DISTINCT FROM $1) doesn't match for a rescued GLOBAL asset since build_runs.chart_id is NOT NULL (migration 436) — leaves that bra row stuck building while asset_throughput (authoritative, §N.4) correctly flips to lit; per-chart assets unaffected", status: open}
  - {item: "T-2 follow-up: confirm ashtakavarga_bindu_sign fact_category + '<SUBJECT>-SIGN_N' fact_subject format against a live DB — could not verify in-sandbox (no DATABASE_URL); _sav_component degrades honestly to 0.0+known_gap on no-match, not a fabrication, but SAV contributes nothing to activation() until confirmed; verifier flags mi_adhilepa references 'ashtakavarga_bindu' without the _sign suffix as a possible category-name mismatch worth checking first", status: open}
  - {item: "T-4 follow-up: kala_permission.lord_capability() warning_score cross-surface reconciliation vs ganita_dasha_lord_capability_get — T-4 correctly drops the low_shadbala percentile term (unavailable at single-lord query scope, avoided fabricating it) but this means the two surfaces can disagree on the same lord's warning tier; needs a documented reconciliation pass or an explicit divergence note on both surfaces", status: open}
  - {item: "T-5/CR-102 HIGH-VALUE finding, promoted to T-6 first-agenda: ka_sangam/engine.py's _c11_vedha_factor call sites (mode_a_search, mode_b_sweep) pass the rasi sign number as transit_house when bg_transit_rules.primary_house means house-FROM-MOON — live scoring defect, ~1/3-of-favourable-windows-void consistent. T-6 must wire kala_trigger.vedha_factor_corrected/house_from_moon into the actual scoring path AND normalize graha-name case (bg_transit_rules stores lowercase, engine passes capitalized) — verifier found this second case-mismatch bug independently, both must be fixed together or the fix is a no-op against real data.", status: CLOSED, disposition: "T-6 fixed both bugs (_house_from_moon + case-insensitive graha comparison), independently traced and confirmed correct by T-6's verifier (formula verified against l0_transit.py semantics, before/after 1.0->0.3 re-run, native_ctx.moon_sign confirmed real field). Wired into the live run_substep path, not just a standalone function — gochara_service confirmed non-None in a healthy build (not a silent no-op)."}
  - {item: "T-5 follow-up: TRIGGER_SUPPRESSIVE_WEIGHTS/TRIGGER_ADDITIVE_WEIGHTS (suppressive: malefic 0.6/papa-kartari 0.6; additive: guru_shani 0.5/saham 0.5 — verifier's 0.5/0.6 summary was transposed) are unratified implementer defaults. ADJUDICATED (Opus, engineering seat, not doctrine-contested/not PARK-class): T-6 MAY wire them PROVIDED each weight is set by the kernel admission loop against the T-0 blind battery on the TRAIN split only (test sealed per ESCALATION §4), served only post-admission with significance-vs-shuffled-birth-control (not a bare point crossing, CHARGE §1.5), and flagged provisional/earned-empirically not ground-truth on the served surface. Rationale: the brief's own admission loop already is the ratification mechanism these weights lack. Falsifier (ADJUDICATOR-FLAGGED SINGLE POINT OF FAILURE, must confirm before T-6 proceeds): if the admission loop as actually implemented only toggles currents on/off at FIXED default weight rather than tuning weight continuously, this ruling's basis fails and the item reverts to pending_adjudication / possible native escalation. Also corrected: real max suppression via _saturating_combine at both suppressive currents=1.0 is -0.84, not -0.6 as the verifier's linear estimate suggested — any downstream memo must use -0.84.", status: CONDITION_SATISFIED, dr_pending_native_ratification: true, disposition: "ADMIT lane's train-only grid search independently verified genuine (18 configs tried, winner 0.2/0.2 not in {0.5,0.6} defaults, real search table reported not just the winner); T-6 wired the ADMITTED 0.2/0.2 values, verifier confirmed TRIGGER_ADMITTED_ADDITIVE/SUPPRESSIVE_WEIGHTS literally 0.2 in ka_sangam.py and apply_trigger_suppression() genuinely called on the live path. Falsifier did NOT trigger. Still queued for native ratification at campaign close (DR-n batch, not a wave-close blocker) per the doctrine-provisional-ruling process."}
  - {item: "T-5 follow-up: school_consensus data-repair (populate real EnrichmentContext.school_consensus_by_domain in ka_sangam.py) — T-5 only fixed the honesty (computed:bool+reason flag), not the underlying 0.0 stub data; T-6 scope", status: open}
cycle2b_fixpsel_rebuild_hang:
  status: found, investigating
  summary: "Post-FIX-PSEL rebuild (run 372b5cfa, dispatched via dispatch_d3_fixpsel_rebuild.py + gcloud run jobs execute brahma-build-pipeline-job-lj545) HUNG on ka_sangam's lifetime-tier substep loop at 'LIFETIME pred 39/59' -- zero further log output for 34+ min, DB keepalive (SELECT 1) stale for 12+ min before the execution was hard-cancelled (gcloud run jobs executions cancel, no graceful stop_requested_at). No exception/warning/error logged at any point -- this is a genuine hang or pathologically-expensive-but-technically-bounded computation, not a crash."
  why_now: "This is the FIRST time mode_a_search/mode_b_sweep (and TRIGGER suppression, wired into them by T-6) have ever executed at real scale on this chart's lifetime tier -- FIX-PSEL's quota diversification is what let Mode A/B actually fire for the first time (previously 100% SUBSYSTEM/Mode-C routing meant this code path was never exercised on real data since it was written)."
  leading_hypothesis: "apply_trigger_suppression() calls compute_trigger_currents() per WINDOW in a predicate's window list; each call fans out to up to ~38 gochara_service.find_aspects() calls (20 for _malefic_transit_over_mechanism [5 aspects x 4 malefics] + 8 for _papa_kartari_sandwich [4 malefics x 2 sides] + 10 for guru_shani_double_transit [2 planets x 5 aspects]) each spanning the window's own [w_jd_start,w_jd_end]. LIFETIME-tier predicates search up to a 100-year horizon (_LIFETIME_HORIZON_YEARS=100) -- if find_aspects's search resolution doesn't scale down for wide windows, a single window's TRIGGER pass could require an enormous number of ephemeris evaluations. Never load-tested against real lifetime-tier window widths since this path never ran on real data before today. Diagnostic agent dispatched to confirm root cause (bounded-but-expensive vs genuinely unbounded) before any fix is attempted.", diagnosis: PENDING
  run_state: "build_runs.state still shows 'running' in DB (hard-cancel via gcloud doesn't flow through the graceful stop_requested_at path) -- idempotent per §N.3, safe to re-dispatch once a fix (or a scoping decision, e.g. skip/cap TRIGGER on lifetime-tier windows) lands."
  diagnosis: "CONFIRMED (diagnostic agent, file:line traced): NOT an infinite loop -- every scan is a bounded while jd<end_jd loop. Class-2 defect: bounded but the cost model was never validated against real lifetime-tier window volume. Root cause: pipeline/transit_search.py's _get_planet_pos calls swe.calc_ut on EVERY invocation with ZERO caching; find_aspect_events day-steps the full window once PER aspect-degree (5 scans per find_aspects call); compute_trigger_currents fans out to ~38 such scans PER WINDOW (20 malefic + 8 papa-kartari + 10 guru-shani); apply_trigger_suppression calls this for EVERY window in a predicate's list, for both mode_a_search and mode_b_sweep. Quantified: ~1500 swe.calc_ut calls per window, predicate 39 alone produced 478 windows -> ~745,000 ephemeris calls for ONE predicate's trigger layer, ~4-19min wall-clock -- fully explains the hang. Conductor-ruled (not routed to Adjudicator/native): this is a non-contested engineering fix (a memoization cache preserves identical numerical results, purely a performance optimization, no scoring/doctrine change) squarely within ESCALATION §1's routine engineering-fork category -- does not need a formal Adjudicator dispatch given its unambiguous nature, consistent with normal lane-level implementer+verifier discipline."
cycle2b_perf_trigger_cache_lane:
  - {lane: PERF-TRIGGER-CACHE, branch: wave/D-3/PERF-TRIGGER-CACHE, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-PERFCACHE, base: origin/main@ab84814a, head_sha: 6ab8466e, status: receipted, verifier_receipt: "verdict=ACCEPT, correctness_risk_found=false, tests=201/201, scope_warden=pass. Verifier independently probed cache-key correctness (poisoning test: flipped global sid_mode to RAMAN after caching a Lahiri value, confirmed cached value stayed correct Lahiri not the wrong Raman value) plus 7 fresh (planet,jd) combos bit-exact diff cached-vs-bypassed (max diff 0.0). One low-risk caveat noted (not a blocker): the cache converts a pre-existing transient global sid_mode race into a persistent one under thread-parallel builds with concurrent non-Lahiri swe usage elsewhere in the estate -- practically low-risk since L3 trigger/gochara is Lahiri-only, but a cheap future hardening (fold sid_mode into cache key) is logged as a follow-up. Measured speedup: 8.1x wall-clock reduction (60-window smoke run mirroring TRIGGER's 38-scans/window shape), predicate 39's ~745K calls estimated to reduce to ~90-100K real misses -- verifier's own estimate: ~27s-150s per predicate post-fix vs 4-19min before, plausibly under the 15min watchdog but not guaranteed (smoke run doesn't reproduce absolute production timing) -- recommendation: re-run the actual production build and measure, keep the secondary fan-out optimization as the ready next lever if still slow.", model: sonnet, verifier_model: opus, note: "Ephemeris position caching in pipeline/transit_search.py's _get_planet_pos (memoize swe.calc_ut by planet+jd-rounded-to-step-resolution+ayanamsha) to eliminate the vast majority of redundant calls exposed by TRIGGER's newly-exercised 38x-per-window fan-out. Correctness-preservation is the hard requirement (must produce numerically identical results, pure perf fix). Bounded cache size required. Optional secondary: reduce apply_trigger_suppression's per-window fan-out itself if time permits, only if provably safe. may_touch=pipeline/transit_search.py (+kala_trigger/trigger.py only if attempting the optional secondary fix), colocated tests."}
cycle2_wrapup_pass_C_items:
  C1_vedha_case_normalization: {status: FIXED, verified: true, evidence: "engine.py lines 240/245/246 -- both sides .strip().lower() before comparison, confirmed alongside the separate _house_from_moon sign-frame fix -- both CR-102 bugs addressed together, not just one"}
  C2_NBRY_inactive_weight_adjudication: {status: RULED, ruling: "0.4 may stand as a provisional, explicitly-flagged, caller-overridable engineering placeholder -- currently wired into NO served surface (CHARGE §1.1, not behavior-changing today) and the qualitative doctrine (NBRY persists natally outside canceller's dasha, only peak-timing salience shifts, BPHS Ch.39) is uncontested while the magnitude is merely underdetermined, not §3-contested. Future wiring lane must re-decompose the served-value move (CHARGE §1.2) and supersede 0.4 with an L5-calibrated value.", falsifier: "flips to native/Fable if EITHER a classical source surfaces specifying the deferred-period weight (becomes contested) OR the wiring lane shows 0.4-vs-0.3/0.5 flips a served verdict's ordering (becomes behavior-changing+magnitude-critical)"}
  C3_T4_warning_score_divergence: {status: CLOSE_AS_DOCUMENTED, evidence: "real, live, reproducible divergence confirmed (permission.py's single-lord PERCENT_RANK always returns 0 by construction vs the TS view's correct all-9-graha percentile) but ALREADY discharged via permission.py's existing percentile_note in judgment_flags naming the limitation and pointing to ganita_dasha_lord_capability_get as authoritative -- no code change needed"}
  C4_T2_ashtakavarga_category: {status: CLOSE_AS_DOCUMENTED, evidence: "false alarm -- confirmed live against 482012f1: ashtakavarga_bindu_sign (sign-indexed, what taranga_service.py queries) and ashtakavarga_bindu (house-indexed, what mi_adhilepa queries) are two DIFFERENT real coexisting categories, not a naming mismatch"}
  C5_RRfix_global_asset_join: {status: CLOSE_AS_DOCUMENTED_COSMETIC, evidence: "premise overstated -- the IS NOT DISTINCT FROM join uses the RUN's chart_id (never NULL, build_runs.chart_id is NOT NULL) not the asset's scope; every normal per-asset build_run_assets write is keyed WHERE run_id=%s AND asset_id=%s with no chart join at all. The only real residual (a crashed run's build_run_assets rows only reset via a SUBSEQUENT run's orphan-cleanup) is a general property not global-specific, touches only a per-run audit table, asset_throughput (authoritative per §N.4) always correct -- no fix warranted"}
  C6_no_tiebreak_pattern_4_writers: {status: FIXED_VERIFIED_MERGED_DEPLOYED, pr: 607, merge_sha: "91c5cfcbcda1addf2801d6e946fcbe979b72fdc3", evidence: "13 sites across bo_drishti.py(2)/mi_darshana.py(1)/ph_nimitta.py(1)/bo_chart_gestalt.py(9) fixed with stable PK or group-key tiebreaks; Opus-verified ACCEPT (86/86 tests, DISTINCT ON semantics confirmed sound, scope-warden pass); merged, deployed, live-confirmed on amjis-web/amjis-sidecar/brahma-build-pipeline-job; worktree+branch (local and remote) cleaned"}
  C7_unratified_weight_defaults: {status: VERIFIED_SAFE, evidence: "sole production caller (ka_sangam.py:85) confirmed to pass explicit TRIGGER_ADMITTED_ADDITIVE/SUPPRESSIVE_WEIGHTS (0.2/0.2); full-tree grep confirms zero other live/served callers rely on the unratified 0.5/0.6 module defaults. Separately noted still-open, low-risk: the PERF-TRIGGER-CACHE ephemeris cache's global sid_mode race hardening (fold sid_mode into the cache key) remains undone -- logged as a named follow-up, not urgent"}
cycle2_wrapup_pass_A1_A2:
  A1_control_matching_check: {status: DONE, method: "analysis-only, re-scored saved §G artifacts (score_g.py + lel_events.json + pooled_activations.json, unmodified) with the shuffled-birth control AND the real chart both constrained to the same 2010-08-18..2032-07-01 coverage window and identical N=29 (vs original N=40)", result: "original gap -16.1pp (7/40=17.5% real vs 33.6% shuffled mean); coverage-matched gap -15.8pp (5/29=17.2% real vs 33.0% shuffled mean) -- NEARLY IDENTICAL. Coverage restriction does NOT materially explain the negative control gap; the red is robust to this specific confound.", artifact: "coverage_matched_control.py + coverage_matched_result.json in session scratchpad"}
  A2_FIX_COV_lane: {status: STOPPED_PER_OWN_GUARD, not_a_failure: true, root_cause_found: "resolve_activation_windows() (services/ka_temporal/date_resolver.py:392-551) correctly matches ALL in-life dasha periods for a predicate (L1 chart_dashas genuinely spans 1949-2100, confirmed live) but then collapses to a SINGLE primary_selected window (current-straddling > soonest-future > most-recent-past, anchored to build-time 'today') -- every OTHER matched period is retained only in an informational jsonb array the serving query never windows on. This is NOT a serving-query bug (query_temporal_activation.ts already honestly self-discloses 'single_cycle_per_signal... not an exhaustive multi-cycle recurrence forecast -- the multi-cycle generator is D-3 scope' -- a previously-known, deferred gap, not new). Fixing it requires EITHER (1) emitting one kala_activation row per matched period instead of one -- needs a migration dropping/widening the UNIQUE(chart_id,signal_id,ayanamsha_id) index (migration 246) + rewriting the primary_selected collapse logic, row count would balloon ~3-6x (49,705->150-300K+ for the canonical chart), with unverified downstream L4/L5 consumer impact; OR (2) redefining the single window as an envelope over all matched periods -- doesn't need a migration but SILENTLY CHANGES what 'activation window' means for every existing consumer (a 40-year span is not a bounded actionable timing signal). Both require rebuilding predicate-selection/window-selection logic, which is exactly the native's own explicit STOP condition ('if the fix proves larger than one focused lane... STOP and bring me the expanded scope before proceeding'). Zero files touched, zero commits, guard fully respected -- confirmed the scoring/weight/threshold/orb/valence logic was read only to confirm it's irrelevant to this gap (it's about window SELECTION CARDINALITY, not scoring).", recommendation: "scope as its own follow-up lane (D-4/D-5 candidate): migration re-keying kala_activation on (chart_id,signal_id,ayanamsha_id,activation_start) + writer change emitting one row per matched period (reusing identical unchanged per-period date math) + verification every L4/L5 consumer tolerates the higher row count.", worktree_preserved: "/Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-FIXCOV (branch wave/D-3/FIX-COV), untouched, available for the properly-scoped follow-up or removal"}
  A3_A4_status: "NOT ATTEMPTED -- native's own directive made the final §G re-run + D-3 disposition contingent on FIX-COV landing; FIX-COV hit its own explicit STOP instead. Per A1's finding (coverage-matching barely moves the gap, -16.1pp vs -15.8pp), the coverage gap is unlikely to have been the primary driver of the RED result even had FIX-COV succeeded narrowly -- but this is now moot pending native's explicit decision on whether to authorize the larger FIX-COV scope (schema migration + writer cardinality change) as its own lane, or accept the original §G RED (REPORT_D-3.md) as standing, campaign-final evidence without a re-run. D-3's RED verdict from the original run remains the authoritative recorded result; not re-litigated by this pass."
cycle2_gate_G_FINAL_RESULT:
  verdict: RED
  run_type: "one-shot, first-pass, per native directive 'run the gate blind... if §G then fails, that's a true red about the kernel, and it stands.' A mid-run transient API disconnect was resumed as a technical continuation of the SAME pass, not a result-driven re-run — no re-run for methodology/result reasons was performed."
  lel_substrate: "57 total events, 40 scorable / 17 excluded (birth anchor + 16 vagueness-pattern), matches campaign's reconciled ~40 figure. Full LEL access used (sanctioned gate-runner role, sealed test split included per ESCALATION §4)."
  served_surface_structural_findings:
    - "ZERO kala_activation coverage before 2010-08-18 (Mercury MD onset) -- 11 of 40 scorable events (27.5%) structurally unscoreable"
    - "ZERO kala_activation coverage from 2033-01-01 onward -- served forward horizon effectively ends ~2032-07"
    - "Of 77 pooled activation rows, only 4 wealth-domain; of 21 TRIGGER/convergence-refined rows, peaks cluster 2017-09-12/2026-04-13/2027-10-20/2027-11-01, ALL non-wealth (character/career-dominated), NONE within +/-45d of either named anchor event"
  check_a_peak_proximity_8L_mars_2H_loss: {result: FAIL, peak_date: "2025-04-03", lag_days: -42, proximity: PASS, intensity: 2.27, top_decile_threshold: 2.80, pct_of_threshold: "81%", anti_gaming: PASS}
  check_b_windfall_venus_jupiter: {result: FAIL, peak_date: "2010-08-13", lag_days: 43, proximity: PASS, intensity: 2.40, top_decile_threshold: 3.60, pct_of_threshold: "67%", anti_gaming: PASS}
  check_c_blind_battery: {result: FAIL_DECISIVE, n_scorable: 40, hit_rate: "7/40 = 17.5%", required: "50%", shuffled_control_mean: "33.6% (7 shifts: 55/37.5/22.5/42.5/15/27.5/35%)", control_gap: "-16.1pp -- REAL CHART SCORES WORSE THAN RANDOMIZED NULL", beats_shuffled_control: false, confirmed_under_loose_domain_unrestricted_variant_too: "17.5% hit rate, -12.9pp gap -- same failure, different construction"}
  anti_gaming_pass: {variance_floor: PASS, top_decile_day_fraction_lt_15pct: "PASS (10.1% both curves)", control_gap_is_real: "FAIL -- gap is real but NEGATIVE, the single most damning number in the run"}
  timing_surfaces_live: {timing_hooks_nonempty_dated: PASS, kakshya_windows_served: "PASS (401 fix confirmed still working live)", when_for_money_answerable: "PASS structurally, but caveat: served kala_activations on this specific probe were dasha_timeline-sourced not convergence/TRIGGER-refined"}
  final_proof_2027_2034: {ketu_md_2027_2034: "PARTIAL -- dasha period served/dated, mechanism-attributed coverage exists for only ~first 5 of 7 years (2027-08 through ~2032-07)", venus_md_2034: "FAIL on literal ask -- dasha period itself served/dated via get_dashas, but ZERO kala_activation coverage 2033-01-01 through 2035-12-31 -- the activation-WITH-mechanism-attribution this check specifically asks for is not served"}
  root_cause_context: "NOT a rationalization, a disclosure: TRIGGER's genuine convergence-refined footprint on this chart (the 16,767 rows the falsifier confirmed) is concentrated in a narrow 2026-2027 forward window and is almost entirely non-wealth (character-domain) -- it does not reach either named historical wealth anchor event, and the served timing surface has zero activation coverage before 2010 or after ~2032. The kernel is REAL and WIRED (independently verified 5 times over this session: T-4/T-5 code correctness, ADMIT's genuine train-only tuning, T-6's wiring, FIX-PSEL's predicate diversification, PERF-TRIGGER-CACHE's correctness-preserving speedup) -- it has simply not yet reached the specific mechanisms/domain/dates this gate tests, on this chart's served data."
  gate_runner_explicit_statement: "Per the native's directive: this red stands. Not recommending a re-run, a threshold adjustment, or a construction change to improve the number -- the gate did what it exists to do."
  conductor_disposition: "HALT-AND-REPORT per ESCALATION_POLICY_v1_0.md §2.1 (RED INTEGRITY GATE, 'above all the retrodiction/falsification gate... the Adjudicator may not disposition a red integrity gate toward green'). No further adjudication, tuning, or re-run attempted by the conductor. Full record in REPORT_D-3.md. Relevant forward pointer: DR-12 (DIS.025, recorded cycle-1) already anticipated this class of outcome -- 'D-4 battery MUST score midpoint-triangle vs pratyantar-lord vs transit-kernel against LEL corpus; data retires the loser' -- this RED result is direct, load-bearing evidence for that D-4 comparison."
cycle2b_rebuild_final: {done: true, run_id: "17d69e59-50f2-4c47-ab81-602516b9211d", triggered_by: "d3-perfcache-post-fix-rebuild", started_at: "2026-07-18T14:08:11Z", ended_at: "2026-07-18T14:18:43Z", state: completed, last_error: null, note: "Resumed cleanly from the earlier cancelled attempt's substep-resumption ledger (41/61 substeps already committed, 20 remaining) -- migration 436's resumption mechanism worked exactly as designed. With PERF-TRIGGER-CACHE applied, remaining lifetime-tier predicates processed at ~1 per 4-5 seconds (vs the prior 34+min hang on a single predicate) -- total wall-clock ~10.5min for the full 26-asset cascade, zero errors, all assets lit.", falsifier_check: "SATISFIED. kala_convergence for 482012f1: mode=null (Mode A/B) rows=16,767, ALL 16,767 bear trigger_weights_used (100%); mode=D rows=11,950 (0 trigger); mode=C rows=923 (0 trigger). TRIGGER genuinely fired on real served data at massive scale for the first time -- MEMO_D-3_1's falsifier condition is met, the 'cannot test TRIGGER' premise has collapsed, §G may now proceed."}
cycle2b_rebuild: {done: true, run_id: "818faf3a-aba2-4507-8f23-354276e5ec9d", triggered_by: "d3-cycle2-t6-ka-sangam-rebuild", scope: asset_set, chart_id: "482012f1-710e-4a25-994a-93821f5871aa", started_at: "2026-07-18T06:33:07Z", ended_at: "2026-07-18T06:42:02Z", state: completed, last_error: null, verified_via: "direct postgres query (ground truth, not agent claim, not UI) — build_runs row + asset_throughput fresh last_built_at on ka_sangam(1580 rows)/ka_taranga(79728)/ka_kalasutra(49705), all lit, all timestamped within the run window"}
cycle2b_fixpsel_lane:
  - {lane: FIX-PSEL, aka: T-7, branch: wave/D-3/FIX-PSEL, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-FIXPSEL, base: origin/main@c40b56c2, status: receipted, head_sha: 41e9f188, verifier_receipt: "verdict=ACCEPT, tests=560/562 (2 pre-existing TestProdDB DB-integration failures, file untouched by branch), scope_warden=pass, stop_condition_correctly_not_triggered=true, hard_guard_respected=true. Verifier independently confirmed DIGNITY_SCORE is a real 7-value classical dignity enum with A10 §4 spec citation, dignity_score path traced clean of ga_vichara/scoring-doctrine (STOP correctly not triggered), quota floor arithmetic re-derived matching claim (200//12=16 near, 60//12=5 lifetime), tiebreak genuinely content_hash not id, both tiers select independently from full pool not a slice, resume_version bump present, hard-guard grep clean (no LEL/test-split references). Took 3 verifier dispatches (2 transient infra stalls, no substantive findings, don't count per protocol §6.4) to get a complete pass.", implementer_claim: "Part 1: GENUINE CEILING BY CONSTRUCTION, not a bug -- dignity_score traced to bo_laksana.py _compute_salience -> DIGNITY_SCORE 7-value classical dignity enum (formulas.py) -> dignity_state from chart_facts fact_category=graha_dignity_per_varga (real L1 fact). Direct DB confirmation: 482012f1's 9 grahas have exactly 3 dignity states (Jupiter=own, Ketu/Rahu/Saturn=exalted, rest=neutral) mapping to exactly {0.85,1.00,0.50} matching the observed distribution exactly. STOP condition NOT triggered (no ga_vichara/scoring-doctrine trace). Part 2: per-signature-class quota (floor=max(1,limit/(classes*2))) + content-hash tiebreak (replacing id ASC) in ka_sangam.py plan_substeps, applied independently per near/lifetime tier, resume_version bumped 1->2. Estate-wide grep: no other id-ASC-favoring-low-cardinality-class instance found; a related no-tiebreak-at-all pattern flagged in 4 other writers as follow-up candidates, not fixed (correctly out of scope). Hard guard respected: no LEL/test-split data referenced. Tests: 13/13 new + 144 targeted + 549/2(pre-existing) broader.", model: sonnet, verifier_model: opus, note: "Native-disposed amendment to MEMO_D-3_1 Option C. Part 1: diagnose dignity_score=1.0 saturation at source (conductor pre-gathered evidence handed to implementer: only 3 distinct dignity_score values exist across ~49,705 signals for 482012f1 -- 0.5/43369, 1.0/4441, 0.85/1895 -- suggests discretized-by-construction more than a clamp bug, but implementer must confirm from source not assume). STOP-condition guard: if diagnosis traces into ga_vichara or the core scoring writer's doctrine, STOP and report back rather than silently expanding into a scoring rework. Part 2 (regardless of Part 1): per-signature-class quota in ka_sangam.py predicate selection + content-hash tiebreak replacing insertion-order id ASC + estate-wide grep for the same tiebreak antipattern. GUARD: quota must not be tuned against §G's outcome -- implementer has no test-split access and must not seek it. may_touch=ka_sangam.py predicate-selection logic + narrowly whatever Part-1 source file if a genuine bug (excluding ka_vichara/valence), colocated tests."}
cycle2b_critical_finding_predicate_selection_bug:
  # Discovered by the conductor's own direct-DB verification pass AFTER the rebuild completed —
  # NOT reported by any lane or verifier; a genuinely new finding from checking served reality
  # against the claimed wiring, per CR-96 discipline ("verify the consuming surface... a lane's
  # claim != served reality").
  status: found, routed_to_adjudicator, awaiting_ruling
  summary: "kala_convergence for 482012f1 post-rebuild is 100% mode:'C' (SUBSYSTEM/Mode-C ingress) across BOTH near(200 rows) and lifetime(1380 rows) tiers — ZERO Mode A or Mode B rows. TRIGGER suppression (T-5/ADMIT/T-6) and the CR-102 vedha fix (T-6) are wired ONLY inside mode_a_search/mode_b_sweep — confirmed via constituent_factors: 0 of 1580 rows carry the trigger_weights_used/trigger_suppressive_applied keys apply_trigger_suppression() writes."
  root_cause: "ka_sangam.py plan_substeps() loads predicates via ORDER BY s.dignity_score DESC NULLS LAST, p.id ASC LIMIT 200 (near)/60(lifetime). SUBSYSTEM predicates route to Mode C; everything else (DISPOSITOR_RELATIONAL/DIGNITY/DOSHA/YOGA/CLASSIFY_RESIDUAL) routes to Mode A+B. Investigated directly: all 6 signature classes have REAL populated dignity_score (avg 0.50-0.57, not a stub-predicate/NULL-score problem). The actual cause: 4,441 predicates across ALL classes tie at the exact max dignity_score=1.0 (SUBSYSTEM 3827/DISPOSITOR_RELATIONAL 552/DIGNITY 60/YOGA 2); with LIMIT 200 inside a 4441-way tie, the p.id ASC tiebreak decides, and SUBSYSTEM predicates were inserted with systematically lower ids (min_id 2,772,174) than DISPOSITOR_RELATIONAL (2,780,056) or DIGNITY (2,812,374) — so SUBSYSTEM deterministically wins every near-tier and lifetime-tier slot for this chart."
  scope_note: "PRE-EXISTING, not caused by D-3 cycle-2b — plan_substeps()'s predicate-loading SQL and Mode-C-vs-A/B routing predate this wave entirely; T-6's diff only added apply_trigger_suppression() calls INSIDE the existing Mode A/B branches, never touched predicate selection. Discovered incidentally while validating the rebuild, not part of any lane's declared scope."
  consequence: "TRIGGER + CR-102 cannot be exercised on Abhisek's real served data under current predicate-selection code, regardless of correctness of T-4/T-5/T-6/ADMIT's work. Running §G now would test the v1 baseline (+ whatever Mode-C-path effects exist), not TRIGGER at all — a materially misleading gate result if not disclosed."
  adjudication: "RULED (Opus engineering seat): tiebreak bug IS critical-path for §G. Option A (run §G as-is + disclose) FORBIDDEN — would launder a baseline result as kernel evidence (CHARGE §4/governing-principle). Whether §G's informativeness can be sacrificed or must be restored before it counts as campaign evidence is itself reserved to the scientific-integrity gate boundary (CHARGE §4) — routed to native per ESCALATION §2. Provisional lean: Option C (scope a new predicate-selection fix lane, re-rebuild, then §G) — but this is native's call, not auto-proceed. See MEMO_D-3_1.md for full options B/C/D and the falsifier."
cycle2b_deploy: {done: true, pr: 604, merge_sha: "c40b56c2c7b5efb1bdf7574f4083cc732909fce7", images: {amjis_web: "c40b56c2c7b5efb1bdf7574f4083cc732909fce7", amjis_sidecar: "c40b56c2c7b5efb1bdf7574f4083cc732909fce7", pipeline_job: "c40b56c2c7b5efb1bdf7574f4083cc732909fce7", amjis_mcp: "11377530892799afd8015d3ee9b6ec68efeb0c0d (unchanged — no platform-mcp/ path touched this batch; the kakshya-401 fix lives in platform/src/lib/retrieval/registry, bundled into amjis-web)"}, kakshya_401_fix_live_confirmed: "post-deploy live re-probe: ganita_av_transit_gating_get(mode=kakshya_windows, chart_id=482012f1, planet=Saturn, target_sign=10, 2026-07-01..2027-07-01) now returns a clean honest empty result (windows:[], empty_reason='Saturn does not transit sidereal sign 10 in this window', full provenance) instead of the prior sidecar 401 — FIX CONFIRMED WORKING LIVE."}
cycle2_hotfix_kakshya401:
  - {lane: hotfix-kakshya-401, branch: wave/D-3/hotfix-kakshya-401, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-hotfix-kakshya, base: origin/main@eb21bb61, head_sha: 61ee6a8e, status: receipted, model: sonnet(conductor-applied), verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=26/27(1 skip), scope_warden=pass. Verifier independently confirmed the sidecar-side requirement (main.py verify_api_key + /brahmagyan/ephemeris router dependency, planet_transit route on it) matches the live 401 verbatim. MUTATION-TESTED: reverted source only -> new regression test genuinely FAILS, restored -> passes -- proves the test would have caught the original bug, not tautological. PYTHON_SIDECAR_API_KEY confirmed present in infra/secrets/secret_inventory.yaml + deploy.yml mapping + A3_env_matrix (OK all envs) -- strong evidence already bound on Cloud Run, ~20 other prod files use the identical pattern successfully today. Deploy-verify note (not a merge blocker): conductor must re-run the live kakshya_windows probe post-deploy to confirm resolution."}
cycle2_t6_lane:
  - {lane: T-6, branch: wave/D-3/T-6, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T6, base: "origin/main@eb21bb61 + T-4(cdc9584b) + T-5(903d586b) + ADMIT(43b3f4cd) merged in (clean)", head_sha: a04d58e6, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=536/538 (2 pre-existing TestProdDB failures, verifier independently reproduced identically on parent commit via git-stash), scope_warden=pass, CR-87/FROZEN contract intact (narrow 2-line writer additions, no run_substep/plan_substeps signature change). CR-102 fix independently traced and confirmed CORRECT: _house_from_moon formula verified against l0_transit.py's house-FROM-MOON semantics, index conventions (0-based moon_sign vs 1-based transit_sign) confirmed consistent; case-insensitive graha comparison confirmed fixes the second bug; native_ctx.moon_sign confirmed a real field. Before/after 1.0->0.3 vedha_factor + strictly-lower convergence_score independently re-run, real not asserted-equal. TRIGGER wiring independently traced: TRIGGER_ADMITTED_ADDITIVE/SUPPRESSIVE_WEIGHTS confirmed literally 0.2 (both keys) in ka_sangam.py; apply_trigger_suppression() confirmed genuinely CALLED (not defined-but-unused) from both mode_a_search/mode_b_sweep on the live run_substep path; gochara_service confirmed non-None in a healthy build (not a silent no-op). -0.2 vs module-default -0.6 distinction independently confirmed real. NOTE: TRIGGER composition is SUPPRESSIVE-ONLY per this pass (additive guru_shani/saham deferred) -- §G must not expect additive uplift from this lane, only score damping where malefic/papa-kartari fire. CRITICAL GATE-SEQUENCING FLAG (verifier, load-bearing): T-6's fixes are BUILD-TIME only (inside the ka_sangam writer's run_substep) -- 482012f1 keeps STALE pre-T-6 kala_* rows until rebuilt; §G MUST NOT run until a scope-limited rebuild of L3 convergence assets (ka_sangam) on 482012f1 completes, or the gate silently scores against pre-fix data and this entire T-4/T-5/ADMIT/T-6 cycle is invisible to it. Kaksya-401 and when-for-money claims NOT independently live-reproduced by this verifier (no live connector auth in its sandbox) -- flagged for conductor live re-probe before gate."}
cycle2_admit_lane:
  - {lane: ADMIT, branch: wave/D-3/ADMIT, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-ADMIT, base: "origin/main@eb21bb61 + wave/D-3/T-4(cdc9584b) + wave/D-3/T-5(903d586b) merged in (clean, no conflicts)", head_sha: 43b3f4cd, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, test_split_breach=FALSE (verifier manually read all 36 cache event_dates, range 1984-02-05..2019-05-15, zero >=2020-01-01; sealed loss event d81fae4e confirmed absent as data everywhere, present only as a NEVER-appear guard constant; load_train_events() re-validation genuinely wired on the sole LEL ingress path, traced call graph; boundary off-by-one correct both directions: 2020-01-01 exact raises, 2019-12-31 accepts; zero live-fetch path in harness, pure cache-driven). tests=19/19, scope_warden=pass, CR-87 verified. ADMISSION DECISION: T-4 PERMISSION REJECTED (train_score 0.264 < 0.288 baseline, degrades score). T-5 TRIGGER ADMITTED at additive=0.2/suppressive=0.2 (train_score 0.312, +0.024 vs v1 baseline) -- this differs from TRIGGER's own unratified defaults (0.5/0.6, not even in the searched grid), satisfying the Adjudicator's falsifier-avoidance condition: genuine tuning occurred, not fixed-default toggle. TWO HONEST CAVEATS T-6 MUST CARRY (verifier-flagged, non-blocking but must not be silently dropped): (1) admission tested via documented STRUCTURAL PROXIES for T-4/T-5's component input scores (malefic transit/papa-kartari/guru-shani/saham), not the full live gochara-driven modules -- compute_permission needs a live DB conn, compute_trigger_currents needs a live gochara_service, both genuinely unreachable in this sandbox; the real weight-combine math (_saturating_combine, capability-term) IS the genuine article, imported not copied. (2) windfall_pass=FALSE at all 18 tested configs including the winner -- the entire +0.024 delta is a single blind-battery event flipping (12/25->13/25), a thin win, honestly disclosed in the implementer's own search table not hidden."}
cycle2_lanes:
  # SPAWNED 2026-07-18 (native GO received: "resume ... run the cycle to the §G retrodiction gate
  # FULLY AUTONOMOUSLY under ESCALATION_POLICY_v1_0.md"). Pre-spawn housekeeping done first: cycle-1
  # worktrees/branches (T-0/T-1/hotfix-kala-temporal, already squash-merged via PR #602) removed per
  # §0 mandatory cleanup (was a stranded-worktree close-report defect, now corrected); 3 unrelated
  # stale .claude/worktrees/agent-* worktrees (no unique commits, base b536e13b) also pruned.
  # ESCALATION_POLICY_v1_0/ADJUDICATOR_CHARGE_v1_0 wiring edits (pending uncommitted from a prior
  # session) committed+pushed (commit 1506852d) before spawn, per §0 activation requirement.
  - {lane: RR-fix, branch: wave/D-3/RR-fix, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-RRfix, base: origin/main@11377530, head_sha: eebe05e5, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, spec-gate 3566/3566 (0 fail), rollup 7/7, orchestrator -k 62/62, d16 b13640d1 guard 12/12 (no collision), scope_warden=pass, FROZEN contract confirmed untouched (grepped for WriterBase/@register/run(self/plan_substeps/run_substep/WriterResult/db_conn — zero hits in diff). _reconcile_failed_assets_from_db independently traced: fresh SELECT post-rollback, reconciles both directions (DB error added even if in-process missed it; DB-confirmed complete drops a stale in-process failure); watchdog genuinely probes count_sql per-asset before error write, mirrors b13640d1 doctrine, param-binding heuristic verified verbatim-copy of production stats/route.ts:160. One Phase-2 follow-up logged (non-blocking): build_run_assets rescue-UPDATE join (chart_id IS NOT DISTINCT FROM $1) doesn't match for a rescued GLOBAL asset since build_runs.chart_id is NOT NULL (migration 436) — leaves that bra row stuck 'building' while asset_throughput correctly flips to 'lit' (authoritative surface per §N.4 unaffected, no false-green, no data loss); per-chart assets (dominant path) fully correct."}
  - {lane: T-2, branch: wave/D-3/T-2, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T2, base: origin/main@11377530, head_sha: 9e6b9696, status: receipted, model: opus (escalated — 2 Sonnet attempts stalled on an async-monitor pattern, not substantive rejections, no PARK-attempt consumed), verifier_model: opus, receipt_ref: "verdict=ACCEPT (2nd verifier dispatch — 1st stalled transient/infra, no finding, does not count per §6.4), tests=34/34 (targeted, full suite deliberately not run given prior HALT_LOG-pollution risk from an unrelated pipeline command), scope_warden=pass. Taranga service (activation/curve/record_evidence/build_chart_substrate) genuinely reuses ka_sangam.engine.orb_strength_score (single import site, verified cos^2/applying-1.0/separating-0.7 identity) rather than reimplementing; substrate cache verified not-rebuilt-per-call; activation()/curve() verified zero DB writes, record_evidence() is the sole opt-in write path requiring cited_by; CR-41 future-timestamp + CR-87 required-chart_id both independently confirmed. All 5 claimed schema/column sets (chart_facts, chart_dashas mig135, chart_vichara mig435, bodha_mechanisms mig445, bodha_cgm_nodes mig325+kala_taranga mig396) verified real against migrations by the verifier directly (this lane's history: an earlier stalled implementer attempt had fabricated start_iso/end_iso columns, since fixed to start_date/end_date+system_id=vimshottari filter by the Opus escalation — verifier independently confirmed the fix is correct, though start_iso/end_iso do exist as columns too, the real defect was the missing system_id filter). One follow-up flagged: the ashtakavarga_bindu_sign fact_category / '<SUBJECT>-SIGN_N' fact_subject format used by _sav_component could not be confirmed against a live DB in-sandbox — degrades honestly to 0.0+known_gap on no-match (not a fabrication), but needs a live-DB confirmation pass before being trusted; verifier notes mi_adhilepa references 'ashtakavarga_bindu' without the _sign suffix as a possible category-name mismatch to check."}
  - {lane: T-3, branch: wave/D-3/T-3, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T3, base: origin/main@11377530, head_sha: 450496ae, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=3757/3757 (152 targeted + 31 CR-87 + 3605 full-suite, 6 skip/46 desel), scope_warden=pass. services/ka_sangam/engine.py found already conformant (chart_id already required pre-existing) so left untouched — real extraction gap was ka_taranga.py duplicating helpers, now import-swapped byte-identical. New services/taranga_kernel/{kernel,promise}.py: ChartStaticSubstrate+compute_activation_curve (T-2's import surface) + PromiseInputs/compute_promise, all 5 PROMISE data sources independently verified real (bodha_msr_signals.computed_salience, chart_vichara valence_pass/varga_ratification value_num+ratification_factor, bo_karanajala edge_strength [0.1,2.0], ga_yoga_writer nbry_rule_1..4 supporting_planets). Clamp[0,1] verified DR-9-safe (negative valence clamps to 0, never re-signed). One doc defect verifier caught (Venus debilitation sign said Pisces not Virgo, zero runtime impact) — FIXED post-receipt (commit 450496ae). Two follow-ups recorded, not blocking this Phase-1 merge: (1) CR-88 formula-complete but NOT wired into its actual kill sites (services/ka_temporal/date_resolver.py::_proximity_score, pipeline/orchestrator/writers/ka_kalasutra.py:205) — needs a new T-3-followup lane with may_touch extended to services/ka_temporal/**; (2) the 0.4 NBRY inactive_weight is an undocumented calibration default (caller-overridable) — verifier recommends gating the followup-wiring lane on an Adjudicator-doctrine ruling before it scales a real timing weight."}
  # Kernel-admission loop (T-1 currents already delivered cycle-1; T-4/T-5 admission) and T-6 serving
  # spawn AFTER RR-fix/T-2/T-3 receipt+integrate, per brief §F1 declared order (T-2/T-3 kernel core
  # before the admission loop; T-6 last). RR-fix/T-2/T-3 all done (see above).
  - {lane: T-4, branch: wave/D-3/T-4, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T4, base: origin/main@eb21bb61, head_sha: cdc9584b, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=26+53+264 passed (2 pre-existing TestProdDB live-DB failures, confirmed unrelated/independent of this lane), scope_warden=pass. compute_permission(chart_id,md_lord,ad_lord,as_of) = clamp(base_spine_strength x gate_multiplier x relation_term x capability_term, 0,1). gate_multiplier independently confirmed to genuinely CALL derive_dasha_consensus().confidence_multiplier (min(1.0,max(0.5,count/7))), not reimplemented -- the real multiplicative gate the brief required, replacing the buried 0.12-weighted additive current. period_lord_relation verified as genuine graha-pair modular algebra (not a bhava-relative reuse mistake); tara genuinely calls panchang_engine.tara_bala. WARNING_TIER_WEIGHTS/LOW_SHADBALA_PERCENTILE/tier thresholds verified byte-identical to get_dasha_lord_capability.ts by the verifier opening both files side by side. chart_dashas columns verified real against migration 135 (no fabricated-column defect, the class this cycle already caught twice). CR-87 verified. No WriterBase needed (read-only scoring composition, writes no table) -- verifier confirmed this is the correct call, not a scope dodge. One follow-up: T-4's lord_capability() drops the low_shadbala percentile term (PERCENT_RANK needs all-9-graha window, unavailable at single-lord query scope; correctly NOT fabricated) -- creates a documented, non-blocking divergence from ganita_dasha_lord_capability_get's warning_score for the same lord; verifier recommends tracking as a named cross-surface reconciliation item."}
  - {lane: T-5, branch: wave/D-3/T-5, worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-wave-D3-T5, base: origin/main@eb21bb61, head_sha: 903d586b, status: receipted, model: sonnet, verifier_model: opus, receipt_ref: "verdict=ACCEPT, tests=99/99 (19+49/3721desel+31), scope_warden=pass, exactly 3 new files zero existing touched. CR-89 suppressive-current cancellation proof independently reproduced arithmetically (suppressive=-0.6, net=0.2 < 0.8 additive-only baseline). CR-102 vedha reference-frame bug CONFIRMED REAL by independent source trace at both ka_sangam/engine.py call sites (mode_a_search line~966-974, mode_b_sweep line~1133-1139): both pass the RASI SIGN NUMBER as transit_house but bg_transit_rules.primary_house means house-FROM-MOON (l0_transit.py:116); these coincide only when Moon is in Aries -- a live scoring defect affecting an estimated ~11/12 Moon-sign population, consistent with CR-102's ~1/3-void-windows figure. Verifier found ONE ADDITIONAL latent bug beyond the implementer's claim: bg_transit_rules stores graha names lowercase while the engine passes capitalized planet names -- vedha_factor_corrected() inherits the same case-sensitive comparison and will still miss real fetched rules unless T-6 normalizes case when wiring. Guru-Sani/saham/school_consensus-honesty/stub-audit(6/8 measured, doctrine ~90% correctly not repeated as fact) all independently verified. CR-87 verified. Verdict ACCEPT -- but the 0.5/0.5 and 0.6/0.6 suppressive/additive weight constants are UNRATIFIED (implementer's own transparent defaults); verifier explicitly recommends T-6 NOT wire them into the live served scoring path without an Adjudicator-doctrine ratification first, since compose_with_ka_sangam can subtract up to -0.6 from a served convergence score."}
cycle2_integration_branch: {branch: wave/D-3/integration, base: origin/main@11377530, merges: ["eebe05e5 (RR-fix, fast-forward)", "9478aced (T-3, merge commit)", "04104bc9 (T-2, merge commit)"], suite: {targeted_cross_lane: "306 passed (orchestrator/taranga/ka_sangam/promise/nbry/cr87 -k subset)", exact_new_files_together: "85 passed", tsc: clean, import_smoke: "all 6 cross-lane modules import cleanly in one process, no collision"}, pushed: true, merged_to_main: {pr: 603, sha: "eb21bb613aa7140bc02dedce6a7c04e22e903173"}}
cycle2_deploy: {done: true, pr: 603, merge_sha: "eb21bb613aa7140bc02dedce6a7c04e22e903173", images: {amjis_web: "eb21bb613aa7140bc02dedce6a7c04e22e903173", amjis_sidecar: "eb21bb613aa7140bc02dedce6a7c04e22e903173", pipeline_job: "eb21bb613aa7140bc02dedce6a7c04e22e903173", amjis_mcp: "11377530892799afd8015d3ee9b6ec68efeb0c0d (unchanged — no platform-mcp path touched this cycle, workflow correctly skipped it)"}, rebuild: {needed: false, reasoning: "RR-fix is orchestrator run-machinery only (no writer/schema change); T-3 is a byte-identical extraction (ka_taranga.py/ka_sangam.py writer output unchanged) + a new PROMISE formula not yet wired into any writer or fact category; T-2 is a new standalone service not yet consumed by any writer or existing serving surface. No new fact categories, no schema changes, no writer behavior change — no R-5/§8.2 trigger. Abhisek's existing build (b84c3797) remains valid."}}
cycle2_gate: {run: false, note: "the D-3 wave gate (§G) — retrodiction battery, the empirical crux of the wave. Not run until T-2/T-3/T-4/T-5/T-6 land. Blind-construction constraint binding: test split (21 events) must not be inspected/tuned against before T-0's single scoring pass."}
updated_at: "2026-07-18T21:55+05:30"
pre_d4_wrapup_pass: "COMPLETE — see PRE_D4_WRAPUP_REPORT.md. All items resolved/ruled/dispositioned
  except A2/A3/A4/D3 (chained, awaiting native's explicit decision on FIX-COV's expanded scope)
  and B1-B3's own content (drafts awaiting native ratification/completion). Session paused per
  native's explicit 'PAUSE for my go before opening D-4' instruction. current_wave remains D-3."
resumed_at: "2026-07-18T03:45+05:30 — fresh conductor session resumed at STATE_D-3.md, reconciled ledger vs reality (git worktree/branch list matched cycle-1 lanes as stale-but-merged; cleaned), received native GO, executed lifecycle step 2 (SPAWN) for RR-fix/T-2/T-3"
halted_at: "2026-07-18T06:55+05:30 — first halt, resolved: native dispositioned MEMO_D-3_1.md as Option C amended; FIX-PSEL + PERF-TRIGGER-CACHE built/verified/deployed, chart rebuilt clean (17d69e59), falsifier SATISFIED (16,767 real TRIGGER rows). §G then run ONCE, blind, first-pass per native directive. RESULT: RED (cycle2_gate_G_FINAL_RESULT block, REPORT_D-3.md). SECOND, FINAL HALT at 2026-07-18T19:55+05:30 per ESCALATION_POLICY_v1_0.md §2.1 (RED INTEGRITY GATE — 'the Adjudicator may not disposition a red integrity gate toward green'). No further tuning/re-run/adjustment attempted. Session ends here. Next conductor session resumes via §6.2 re-entry only after native reviews REPORT_D-3.md and disposes (D-3 honest-close-as-red / investigate-coverage-and-retry / other)."
