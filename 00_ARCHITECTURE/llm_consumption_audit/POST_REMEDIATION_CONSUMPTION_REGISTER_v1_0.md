---
artifact: POST_REMEDIATION_CONSUMPTION_REGISTER_v1_0
type: LIVING REGISTER (consumption-round findings; feeds MARSYS_DEFECT_GAP_REGISTER on next merge)
version: 1.8
status: LIVING — rows CR-1..CR-89 + CR-107 are this file's system-of-record content (§A-§I, §M).
  CR-90..CR-106 (the §J/K/L range authored in 2026-07-14/15 Cowork sessions) never landed as rows
  in this file's body — formally SUPERSEDED here per §N (D-1.6 Lane S-8, 2026-07-16, closing the
  merge debt this file's own §M flagged): system-of-record for CR-90..106 is
  `DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md §8` (status corrections + wave-routing amendments)
  plus the live doctrine-wave briefs §N's pointer table cites. §I dispositions map CR-1..CR-89 into
  DOCTRINE_CAMPAIGN_DESIGN_v1_0.md §9 (the resolution artifact for that range only — §9 does not
  cover CR-90+). This file stays the living intake for CR-1..89 + CR-107; do not add new
  CR-90..106 rows here — extend the briefs / EXECUTION_PLAN §8 instead (one system of record).
source_sessions: Cowork 2026-07-13 (post-rebuild readings: business/personality/wealth diff,
  full-estate financial reading, chain interpretation, 126-tool deployed census, nakshatra/
  services/CGM interrogation). Chart under consumption: 1c826d5a (Abhinandan), build 762de6e8,
  priors 1.1. Census detail: DEPLOYED_TOOL_CENSUS_2026-07-13.md.
  ADDENDUM same date (§E): Cowork wealth round on 482012f1 (Abhisek), build ee0829d6, priors 1.1 —
  native-corrected reading (missed yogas), post-fix retrieval re-verification.
  ADDENDUM 2 same date (§F): Cowork FULL-ESTATE RETRIEVAL SWEEP on 482012f1 (Abhisek), build
  ee0829d6, priors 1.1 — all 129 read-side tools of the deployed connector exercised once with
  minimal valid args (5 parallel audit agents; 126 exercised, 2 write tools + 1 oversize alias
  excluded). Purpose: establish an accessibility baseline — which tools answer, which answer
  wrongly, which answer emptily. Verdict: 112 PASS / 5 DEGRADED / 8 EMPTY / 1 FAIL.
  ADDENDUM 3 same date (§G): Cowork FRESH FULL FINANCIAL READING on 482012f1 (Abhisek), build ee0829d6 —
  clean-context, tool-first wealth reading (no reliance on prior context/memory), followed by a gap audit
  of what the instrument SERVED vs. what the reading actually NEEDED. Purpose: separate "tools that don't
  answer" (§F) from "tools that answer, and are blind or wrong" (§G). Verdict: all SIX load-bearing
  conclusions of the reading were hand-derived; the engine served none of them, and its ranked wealth
  surface returned 14/15 noise rows. Rows CR-54..CR-71.
  ADDENDUM 4 same date (§H): Cowork ESOTERIC-CONCEPT + SYSTEM-IMPLEMENTATION round on 482012f1
  (Abhisek), build ee0829d6, priors 1.1 — native-directed interrogation of the concepts the readings
  never touched (dosha-label integrity, Kāla-Sarpa verification, KP system, special lagnas, chara
  karakas) plus the two synthesis engines the native suspected were misfiring (bo_anveshana discovery
  engine, L5 Mīmāṃsā calibration). Purpose: register the systems/concepts that are computed-but-dark,
  half-built, or degenerate — the "promise not delivered" surface at the SYSTEM level. Rows CR-72..CR-80.
conventions: CR-n rows; Class per plan §4 taxonomy + IMPROVE/OPTIMIZE/RATIFY for non-defect rows;
  Sev CRIT/HIGH/MED/LOW. Dedupe: rows that UPDATE existing register entries say so explicitly.
---

# Post-Remediation Consumption Register (2026-07-13)

## §A — Serving-plane defects (new or residual)

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-1 | judgment_query `timing_hooks` still entirely empty even though kala_activation now HAS dated rows — the serving join was never wired (R-39 residual; R-45's data fix landed, consumer didn't follow) | 4 | HIGH | judgment career+wealth 2026-07-13; receipt still `timing_anchored:true` | Wire timing_hooks to chart_dashas + kala_activation; receipt honesty | OPEN [PARIŚODHANA A1 ANNOTATION 2026-07-27: ALREADY-FIXED — register stale, for the defect as literally stated. Live probe against production chart 482012f1-710e-4a25-994a-93821f5871aa (`judgment_query`, response_format=v3, verbosity=exhaustive, 2026-07-27): wealth call → `content.checklist.timing_hooks.kala_activations` carries 3 real dated rows (activation_start 2010-08-18, activation_peak 2019-02-16, activation_end 2027-08-18, domains_affected wealth/career/relationship); career call → 1 analogous row. `timing_hooks` is NOT entirely empty — `receipt.timing_anchored:true` is honest for these calls. Confirmed by a THIRD call (domain=marriage) where timing_hooks came back genuinely all-empty (current/lord_mahadasha_windows/karaka_mahadasha_windows/kala_activations/mahadasha_windows_by_graha all `[]`/`{}`) and the receipt correctly self-corrected: `receipt.timing_anchored:false` plus judgment_flag `timing_anchored_forced_false` — proving the honesty mechanism fires both ways, not just when convenient. Code: `platform-mcp/src/tools/registry_bridge.ts`'s `enforceTimingAnchoredHonesty` (WP-S4-fix2, Gate Ś #10) re-derives `timing_anchored` from what actually survived onto the wire and forces it false + flags it when every constituent array is empty — shipped in commit `9bac956f` ("fix(l3-kala): S-4 fix-2 — dasha-aware primary-window selection + timing-hooks receipt honesty (Gate Ś #8/#9/#10)"). Residual not covered by this fix: `lord_mahadasha_windows`/`karaka_mahadasha_windows` (the chart_dashas-sourced half of this row's own fix-direction) remain empty on wealth/career — only the kala_activation-sourced half is wired — but since the receipt is honestly derived from what IS populated, the specific "receipt lies next to an entirely-empty timing_hooks" defect this row names does not reproduce. Disposition: ALREADY-FIXED for this chart's current build.] |
| CR-2 | judgment_query `varga_confirmation.rows` still `[]` while receipt says "D10✓/D2✓" — verdict now weighs varga via sub-scores (R-46 fixed) but row-level evidence remains hollow (R-38 half-fixed) | 5 | MED | same calls | Populate rows or mark receipt "weighted-no-rows" | OPEN |
| CR-3 | `bearing_yogas` now `[]` on BOTH judgment calls — REGRESSION vs 2026-07-11 (Ubhayachari was served pre-rebuild); suspected side-effect of the MSR flood-trim | 4 | MED-HIGH | judgment wealth 07-11 vs 07-13 | Check yoga-signal join post-priors-1.1 | OPEN |
| CR-4 | get_temporal_windows (unfiltered) 119KB over-budget dump; and ALL 50 top rows are ONE identical window — no spectrum/discrimination in convergence output (single dominant peak fanned across ≥30 signals) | 6+7 | HIGH | 2026-07-13 call; kala_windows dedupe check | Budget the tool; emit distinct windows or family-collapse identical ones with member count | OPEN |
| CR-5 | Activation rows: `active_dasha_periods_jsonb: []` on every row; `dasha_activation_proximity` flat 0.5 — dasha join unpopulated in kala_activation | 4 | HIGH | all activation rows | Populate the dasha join at write or serve time | OPEN → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A2, 2026-07-27: live `kala_yoga_activation_get`(482012f1, 2026-07-27→2029-07-27) returns `active_dasha_periods_jsonb` populated with real `chart_dashas` rows (graha/level/start/end, e.g. Saturn antardasha 2024-12-08→2027-08-18) on most signals, and non-flat `dasha_alignment_score` (0.4748, 0.28025, 0.3503, etc.) — the empty-array/flat-0.5-on-every-row defect does not reproduce. Fixed by commit `8dd45fc8` "fix(l3-kala): R-45 iter2 lord-resolution fallbacks + CR-5/CR-12/CR-48 real dasha_alignment" + `9bac956f` (D-1.6 Lane S-4 fix-2, Gate Ś #9/#10 GREEN, STATE_D-1.6.md). Residual: 3 structurally-always-on nabhasa yogas (Gola/Shoola/distribution_yoga_sankhya) and birth-moment panchanga facts still show flat 0.5/empty array by design (no natal dasha-lord to match) — narrow, documented, non-blocking (Gate Ś #8 PARK).] |
| CR-6 | kala_projections_get: no domain filter parameter; all 10 projections are the same event fanned over signal rows; none wealth-tagged | 6 | MED | agent probe 07-13 | Add domain param; family-collapse | OPEN |
| CR-7 | traverse_graph parses `about` to its FIRST CHARACTER ("Could not parse address expression: 'l'"); bodha_graph_subgraph_get seed="VENUS" fails on same DSL parse — while alias bodha_graph_traverse_get works | 4 | HIGH | census slice D; agent probe | Fix arg-mapping in the non-alias tools | CLOSED_WITH_EVIDENCE [D-1.6 S-7/S-8, 2026-07-16: traverse_graph(about="lord_of(bhava 10)") parses+resolves correctly on the deployed connector; did-not-reproduce, matching CR-53's own retest note. SIZE NOTE routed to Lane S-5: depth=1 response is 99KB, over the 64KB Gate Ś ceiling — budget work, not a parse defect] |
| CR-8 | ref_* sidecar family broken ON DEPLOYED: ref_aspects_at_time_get / ref_planet_transit_get / ref_retrograde_periods_get → 401 Invalid API key; ref_planet_position_get passes literal "undefined" date → 500; ref_ephemeris_year_get → 404 unregistered capability URI; ref_transit_rules_get → DB 400 (LCA-13 confirmed on deployed channel + 3 new members) | 4 | HIGH | census slice D | Sidecar key wiring + alias param mapping + URI registration | OPEN → CLOSED — STALE-CORRECTED (PARIŚODHANA Phase-A Prober A6, 2026-07-27): does not reproduce. Live production calls (mcp-direct channel) against all 6 named tools all return clean `ok:true`/valid data — `ref_aspects_at_time_get`(2026-07-27) 2 aspects; `ref_planet_transit_get`(Jupiter, 2026-07-01→08-01) 32 rows; `ref_retrograde_periods_get`(Mercury, 2026) 6 stations; `ref_planet_position_get`(2026-07-27) 9 bodies with `source_citation:"pyswisseph DE441 + Swiss Ephemeris"`; `ref_ephemeris_year_get`(2026) 3,285 rows (budget-trimmed, not 404); `ref_transit_rules_get` 3 rows. No 401/500/404/DB-400 on any call. The "literal undefined date → 500" case specifically re-tested: calling `ref_planet_position_get` with no `date` arg now returns a clean structured error (`"date (YYYY-MM-DD) is required"`), not a crash. Fixed by commit `e2fe0bdd` "fix(w4-loop1): E-5 group1 hard-failures — asset_registry auth, ephemeris ref_* sidecar path, transit_rules schema+whitelist..." (2026-07-13). |
| CR-9 | asset_registry_all / asset_registry_l0 → cockpit 401: the system's own inventory tools are dead to the consuming LLM (P-12 capability map has no live source) | 4 | MED-HIGH | census slice A | Auth wiring for read-only registry access | OPEN → CLOSED — STALE-CORRECTED (PARIŚODHANA Phase-A Prober A6, 2026-07-27): does not reproduce. Live production calls against the named tools (`catalog_assets_all`, `catalog_assets_l0`, plus `catalog_assets_list`) all return clean 200s with real data — `catalog_assets_all` reports `total:107` global assets, `catalog_assets_l0` reports `total:28` L0 Brahmagyan assets — no 401 on any call. Fixed by the same `e2fe0bdd` commit (2026-07-13) whose message names "asset_registry auth" as one of the E-5 group1 hard-failures repaired. |
| CR-10 | Remedy-filter family: planet filter CASE-SENSITIVE ("Venus"→0, "venus"→25 rows); query_remedies + ref_remedies_get IGNORE planet filter entirely (jupiter rows served for Venus); ganita_positions_get planet="Venus" → 0 while unfiltered works (vocab mismatch) | 2+6 | MED-HIGH | census slice B/D | Normalize filter vocab case-insensitively; honor or reject unknown filters loudly | OPEN [PARIŚODHANA Phase-A Prober A3 ANNOTATION 2026-07-27: ALREADY-FIXED — register stale. All three named sub-claims re-tested live against production chart 482012f1 and none reproduce. (1) Case-sensitivity: `ref_remedies_get(planet="Venus")` and `ref_remedies_get(planet="venus")` both return the identical 25-row Venus-only result set (`filters_applied.planet` echoes the caller's casing but the underlying match is case-insensitive) — not "Venus→0". (2) Planet filter ignored: `ref_remedies_get(planet="saturn")` returns 20/29 Saturn-only rows (all `planet:"saturn"`) and `ref_remedies_get(planet="jupiter")` returns 20/31 Jupiter-only rows (all `planet:"jupiter"`) — genuinely different, correctly filtered result sets and result_hashes, not "jupiter rows served for Venus". (3) `ganita_positions_get(chart_id=482012f1, planet="Venus")` returns exactly 9 rows, all `fact_subject:"VEN"` (sign Sagittarius, house 9, nakshatra Purva Ashadha, etc.) — not 0. Consistent with the CR-42/R-19/R-20 D-1.6 S-1 filter-honesty fix wave (Gate Ś #1, REPORT_D-1.6.md) which names this exact remedy-filter family. Row left OPEN (not flipped to CLOSED) pending native/Conductor ratification, per this probe's fix-code-free, read-only mandate.] |
| CR-11 | Empty-without-empty_reason family: query_tantric_remedies, ref_tantric_remedies_get, list_remedies_by_category, ref_remedies_by_category_list, ref_yogas_get, ref_mantras_get/query_mantras, get_positions, phala_predictive_anchors_get — all return bare zero-row payloads with no honesty note (contrast: kala/temporal tools now do this correctly) | 5 | MED | census | Adopt the empty_reason pattern estate-wide | OPEN |
| CR-12 | yoga_activation_by_dasha / kala_yoga_activation_get: 0 activated yogas over a 3-YEAR window — yoga×dasha activation join suspected broken (13 yogas exist; Saturn/Jupiter dashas running) | 4 | HIGH | census slice C/D | Root-cause the join | OPEN → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A2, 2026-07-27: live `kala_yoga_activation_get`(482012f1, 2026-07-27→2029-07-27) returns 16 `activated_yogas` rows over the 3-year window, including Sasa Yoga CURRENTLY ACTIVE (`activation_start`=2024-12-08, `activation_end`=2027-08-18, spans today 2026-07-27, Saturn antardasha) plus Anapha/Vasi/Yuga/Kedara/Gola/Shoola and panchanga-combination rows — "0 activated yogas over 3 years" does not reproduce. Fixed by `8dd45fc8` + `9bac956f` (D-1.6 Lane S-4 fix-2, Gate Ś #9/#10 GREEN, post-rebuild 47/47 assets lit).] |
| CR-13 | Oversize family on deployed: bodha_domain_reading_get 898KB, assess_career 748KB, ephemeris_cache_year 815KB, assess_marriage 537KB, assess_wealth 163KB, get_cgm_subgraph ~100KB, phala_* 26–35KB; judgment_query self-flags >12KB (R-40/LCA-3 class live on the real channel) | 6 | HIGH | census + readings | Extend C1-style budget discipline estate-wide | OPEN |
| CR-14 | phala_outlook_get trim_report `recover_via: "unknown_tool"` (alias self-reference broken); holistic_bundle_chart_facts completes with 5 internal sub-tool errors (partial silently) | 5 | MED | census slice C | Fix recover_via mapping; surface sub-tool failures in bundle verdict | OPEN |
| CR-15 | pact_query names the activation window date (2053-04-23) but NOT the activating graha/dasha — consumer must derive it (Venus MD) from the chain | 6 | LOW-MED | pact probe 07-13 | Name the MD lord in the pact payload | OPEN |
| CR-16 | Special-lagna services birth-keyed, not chart-keyed: chart-entitled consumer cannot obtain special lagnas for a chart whose raw birth data it doesn't hold (Abhinandan) — indu/sree/hora lagna readings blocked | 1 | MED-HIGH | 07-13 attempt + census caveat | Accept chart_id; resolve birth params server-side under entitlement | CLOSED — STALE-CORRECTED (SARVA-SIDDHI, 2026-07-24): shipped PR #594/D-2 (20e2da8e); ganita_special_lagnas_get now accepts chart_id, verified live for chart 482012f1 |

## §B — Data-plane gaps (confirmed absent, not retrieval failures)

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-17 | Karakamsha not stored (chara karakas ARE stored, both schools; AK's D9 sign derivable but no karakamsha fact/analysis) | 1 | MED | agent probe | L1/L2 addition | OPEN |
| CR-18 | Shadbala required-minimum ratios not stored — bare rupas served without the classical normative band (P-5 live instance; consumer supplied BPHS minimums from priors) | 1 | MED | agent probe | Store required_rupa + ratio per graha | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: ganita_chart_facts_get(keyword=required_rupa) → 7 live rows, graha_shadbala_total.*.required_rupa (JUP 6.5, MAR 5.0, MER 7.0, MOON 6.0…), ayanamsha_id=INVARIANT served] |
| CR-19 | L4 phala anchors: ZERO wealth-domain anchors while other domains have rows — the layer meant to answer "when, for money" is empty exactly there | 4 | HIGH | anchors + outlook probes | ph_nimitta domain coverage | OPEN |
| CR-20 | bo_upaya wealth resonances still 0 (LCA-5 residual); phala mitigations exist (618) but ALL programs empty (nothing schedulable) | 4 | MED-HIGH | census + agent | bo_upaya population; program builder | OPEN → CLOSED — STALE-CORRECTED (PARIŚODHANA A5, 2026-07-27): does not reproduce. Live `bodha_remedies_get(chart_id=482012f1, domain=wealth)`: `resonance_count:3` (Jupiter 0.11466/medium, Sun 0.09782/low, Saturn 0.09403/low), each with populated `associated_cdlm_cell_count` (6/13/7) and `prescription_count:9` citation-backed prescriptions (BPHS Ch.88/91-94). Shipped via PR #741 (merged; CR-67/CR-69 lane), live-verified in `PRE_DARPANA_READINESS_v2_0.md §W-3 R-1/R-2`. Secondary sub-claim ("ALL programs empty") also does not reproduce as stated: `phala_mitigation_get(domain=wealth)` returns ≥1 non-empty mitigation row (`mitigation_id 121f7495-9e76-4d7e-8430-192e9da94b32`) — not independently re-verified for full schedulability this session. Residual carried forward as **R-09** (`associated_doshas_array` + `estimated_cost_inr_range_jsonb` still 100% NULL, self-disclosed by the tool's own `data_gap_note`) — separately tracked, not silently closed. |
| CR-21 | Sarvatobhadra fact records nakshatra OCCUPANCY only ("nakshatra_occupancy_recorded") — vedha computation itself appears pending (stub-like); kranti rows carry "ayanamsha_offset_unresolved, W3 refinement" placeholders | 4 | LOW-MED | sensitive_degrees probe | Complete vedha + kranti computation | OPEN |
| CR-22 | Dhana-yoga detection still absent from the yoga engine (Section-1 gap unchanged); NB detection NOW EXISTS (ga_yoga_writer.detect_neecha_bhanga) but VRY still label-only | 1 | MED-HIGH | probes 07-13 | Extend detector family | OPEN |

## §C — Doctrine / method / architecture (the chain-episode cluster)

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-23 | **NB doctrine disagreement**: new engine detector marks Jupiter neecha-bhanga "not_applicable_or_intact" while classical two-ground derivation (Mars-exalted-in-fall-sign in kendra from Moon; debilitated graha in kendra) finds NBRY — first live P-3 adjudication case; neither side may silently win | 9 | HIGH | fact 048698c5 vs derivation 07-13 | Native/doctrine ruling; document grounds the detector checks | OPEN |
| CR-24 | **Chain blindness root cause confirmed in consumption**: the 10→8→12→10 lord-circuit (the chart's engine, native-identified) exists in FOUR layers (L1 dosha label, CGM motif, L4 causal chain, L3 convergence lords) yet appears in NO ranked surface and no reading synthesis — G-6 (no chain signal class) + LCA-9a (no consequence semantics on edges) ELEVATED to top remediation tier with this as the type specimen | 8 | CRIT | chain episode 07-13 | G-6 chain signals with salience; LCA-9a edge semantics | OPEN — ELEVATED |
| CR-25 | CGM carries only topological scores (pagerank/degree/strength) — no astrological salience, never joins composite ranking → graph is structurally subordinate to MSR (native's question confirmed) | 8 | HIGH | subgraph payloads | Salience-score motifs/paths; rank-integrate | OPEN |
| CR-26 | Nakshatra SEMANTICS never rank: trivia (yoni/akshara) was correctly demoted by rebuild, but meaningful nakshatra layer (tara bala, navatara, dasha-lord nakshatra texture, nakshatra dispositors) never surfaces in domain evidence — readings go nakshatra-blind unless the LLM volunteers it | 7 | MED-HIGH | 07-13 readings | Nakshatra-semantic signal class with proper salience | OPEN |
| CR-27 | Class-9 live corpus from this round (for P-1/P-12 design): (a) 4-call diff reading answered a fresh-reading question; (b) supply-shaped evidence plans twice; (c) chain missed until native pointed; (d) nakshatra layer omitted from financial synthesis — each a logged improvisation instance | 9 | — | this thread | Feed P-1 vidhi + P-12 planner requirements | LOGGED |
| CR-28 | intent_classify / util_intent_classify return a classifier PROMPT for the consuming LLM rather than a classification — works-as-built; needs RATIFY-or-redesign decision (is prompt-delegation the intended P-10 contract?) | RATIFY | LOW | census slice C/D | Native ruling | OPEN |

## §D — Optimization / improvement (non-defect)

| ID | Item | Type | Notes |
|---|---|---|---|
| CR-29 | Convergence output should express WINDOW families (one window, N member signals, per-domain flavor) instead of N identical rows | OPTIMIZE | pairs with CR-4 |
| CR-30 | Twin-alias hygiene: for several capabilities the legacy name is broken and the alias works (traverse_graph vs bodha_graph_traverse_get; ref_* vs query_*) — deprecate or repair one face per capability; the capability map must mark the LIVE face | OPTIMIZE | pairs with CR-7/8, P-12 |
| CR-31 | Positive verifications this round (do NOT re-flag): R-45 dates live end-to-end; attribution 0% UNATTRIBUTED; contradictions firing (2); DEFECT-001 0/68,224; varga term moves verdicts (R-46); MSR flood trimmed (9,427); sensitive_degree_check shipped (55 rows incl. per-graha mrityu-bhaga w/ orbs, gandanta, pushkara, kartari, 22nd drekkana); ayurdaya/medical/vastu/kala-priority tools shipped; muhurta 89-day ranges served; bodha_quality healthy | VERIFIED | baseline for re-audit |
| CR-32 | New astrological facts to carry into future readings of 1c826d5a: Mars (8L→12H chain link) in GANDANTA (end-Pisces, fired) + papa-kartari; khareshwara (22nd drekkana) = Scorpio; Jupiter mrityu-bhaga near-miss 0.57° (tolerance 0.25 — not fired, note only); D2 Jupiter+Mercury combust; D11 Saturn exalted in H11 | READING-NOTES | not defects — chart knowledge |

## §E — 2026-07-13 Abhisek wealth-round intake (chart 482012f1, build ee0829d6)

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-33 | ganita_yogas_get v3 verdict-builder self-contradicts: reports `yogas_fired: 0` and "Sasa Yoga is not formed... per its absence from the yoga_label rows served" while the Sasa `yoga_label` row (fact d87dcca9) IS in the same page's rows — verdict counter doesn't read the rows it serves; Pancha-Mahapurusha per-yoga statements are therefore wrong on any chart with a fired PMP yoga | 5 | HIGH | ganita_yogas_get v3, 482012f1, 2026-07-13T10:17Z | Count served yoga_label/yoga_fires rows in verdict; never assert "not formed" from absence-in-page | OPEN |
| CR-34 | NB detector is **D1-scoped only** (UPDATES CR-22/CR-23): `ga_yoga_writer.detect_neecha_bhanga` returns `not_applicable_or_intact` for all 7 grahas — correct for D1 (no rashi debility) but blind to varga NBRY. Type specimen 482012f1: Saturn D9 Aries (debilitated) w/ Sun in D9 lagna (Cancer) kendra + Saturn itself in D9 kendra → NBRY; Venus D9 Virgo (debilitated) w/ exaltation-lord Mercury in D9 kendra → NBRY. Both hand-derived from verified longitudes; both invisible to the detector | 1 | HIGH | facts 93b2aa12/4b3947b3 vs derivation 07-13 | Extend detector per-varga (at minimum D9); store grounds checked per NB verdict (feeds CR-23 doctrine ruling) | OPEN |
| CR-35 | House-lord yoga family still absent on 482012f1 (CONFIRMS CR-22 on second chart): 0 rows for Budha-Aditya (Sun+Mercury H10, non-combust — fires), Saraswati (Jup own-sign trikona + Ven H9 + Mer kendra — fires), Lakshmi (plausible), Dhana (2L Venus + 9L Jupiter conjunct H9 — fires). keyword probes "aditya"/"saraswati"/"dhana" = 0 relevant rows; yoga catalog serves Nabhasa/PMP/Chandra-adhi families only (32 rows) | 1 | MED-HIGH | keyword probes 07-13 | Same detector-family extension as CR-22 | OPEN |
| CR-36 | assess_wealth composite top-10 buried decisive wealth evidence (Class-9 consumption specimen, native-corrected): vargottama Mercury (MD lord!), Rahu exalted H2, Moon H11 (complete 2/11 dhana axis), and KP star-lord chains (Mars→Rahu[H2]→Moon[H11]; Mercury→Sun[H10]) all exist in the fact/signal base but none surfaced in the served reading; MSR "weakest graha = Mercury" verdict ignores vargottama amplification | 7+9 | HIGH | wealth reading 07-13; native correction; positions re-derivation | Vargottama + dhana-axis signal classes with proper salience; KP-paradigm cut in wealth lens; log as P-1 vidhi corpus | OPEN |
| CR-37 | assess_wealth temporal stage empty on 482012f1 too: `activating_dasha.activations: []` over 2026-07-13→2029-07-12, empty_reason cites the known L3 writer defect; yoga_activation_by_dasha serves 10 rows but ALL with null activation_start/end + `active_dasha_periods_jsonb: []` + flat 0.5 alignment (CONFIRMS CR-1/CR-5/CR-12 on second chart) | 4 | HIGH | assess_wealth + yoga_activation calls 07-13 | Same fixes as CR-1/CR-5/CR-12 | OPEN — UPDATE evidence → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A2, 2026-07-27: live `assess_wealth`(482012f1) `activating_dasha.activations` is NOT empty — real dated row served (`activation_start`=2027-10-15, `activation_end`=2027-10-25, `dasha_activation_proximity_score`=0.3503 (not flat 0.5), `active_dasha_periods_jsonb` populated with 8 real `chart_dashas` Ketu-antardasha entries). `kala_yoga_activation_get` (the live alias of `yoga_activation_by_dasha`, which is now a retired/deprecated name per `deprecated_tool_gate.ts` — see `W4_MCP_SURFACE_SERVING_NOTE_v1_0.md` §4a) confirmed under CR-5/CR-12/CR-48 above — no longer "ALL null activation_start/end + flat 0.5". Fixed by `8dd45fc8` + `9bac956f` (D-1.6 Lane S-4 fix-2).] |
| CR-38 | Reading-notes for 482012f1 (not defects — verified chart knowledge for future readings): Mercury VARGOTTAMA Capricorn D1(0.84°)/D9; D9 lagna = Cancer; Saturn D9 Aries + NBRY grounds (Sun in D9 lagna); Venus D9 Virgo + NBRY grounds (Mercury D9 kendra) — Venus is MD lord 2034–2054, so the 2034 activation is an NBRY maturing; Budha-Aditya H10 non-combust; Saraswati structure fires; Rahu Taurus (exalted) H2 Rohini / Moon H11 = full 2/11 axis; KP cash-flow chain Mars(Swati)→Rahu(H2)→Moon(H11), Mercury(U.Ashadha)→Sun(H10); Sasa Yoga H7 (exalted Saturn, 11L); dashas: Mercury–Saturn to 2027-08-18 → Ketu MD (H8, shadbala 0.625) 2027–2034 → Venus MD 2034–2054 | READING-NOTES | — | positions + dasha table 07-13 | Carry into every future 482012f1 reading | LOGGED |

## §F — 2026-07-13 full-estate retrieval sweep (chart 482012f1, build ee0829d6, priors 1.1)

**Method.** All 129 read-side tools on the deployed connector called once with minimal valid args
(chart_id `482012f1-710e-4a25-994a-93821f5871aa`, ayanamsha `lahiri_chitrapaksha`, as-of 2026-07-13),
one retry max on arg errors, executed by 5 parallel audit agents partitioned by layer. Write tools
(`record_outcome`, `mimamsa_outcome_record`) excluded by design; `ref_ephemeris_year_get` skipped as a
declared alias of an already-oversized tool. **126 exercised.**

**Accessibility baseline (the number the native asked for):**

| Verdict | Count | Meaning |
|---|---|---|
| PASS | 112 | callable; returns real, chart-relevant content |
| DEGRADED | 5 | callable; returns wrong or unusable content (silently) |
| EMPTY | 8 | callable; zero rows |
| FAIL | 1 | errors |
| Not exercised | 3 | 2 write-side + 1 oversize alias |

**Per-layer:** L1 Gaṇita 24/26 PASS (1 EMPTY, 1 FAIL) · L2 Bodha **22/22 PASS** · L3 Kāla + L4 Phala
16/20 PASS (4 EMPTY, all in the forward-time surface) · L5 Mīmāṃsā + registry 19/20 PASS ·
L0 reference/remedies 27/37 PASS (4 DEGRADED, 2 EMPTY).

**Headline reading of the baseline:** the *structural* estate is healthy — positions, dignity, strength,
divisionals, dashas, signals, discoveries, domain readings, remedies, citations all serve. The *temporal*
estate is not — every forward-looking surface is either empty, undated, or infra-blocked. A reading of
this chart today can be structurally acharya-grade and temporally mute.

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-39 | **Only hard FAIL in the estate: `holistic_bundle_chart_facts`.** 5 of 8 sub-tools error (`sub_tools_errored: ["MSR","CGM","LEL","PANCHANG","DASHA"]`, MSR + CGM both `error_class: tool_error`); only UCN/RM/CDLM fire, and the bundle still returns `ok`. **UPDATES CR-14** with the named sub-tool list. The one-call "give me everything" surface is the one surface that does not work | 5 | HIGH | sweep 07-13, 482012f1 | Repair the 5 sub-tool calls; bundle verdict must be non-ok when any sub-tool errors | OPEN |
| CR-40 | **Ephemeris/transit sidecar down estate-wide — the single highest-leverage infra defect.** `kala_temporal_bundle` → `sidecar_available: false` ("no transit data"), all 4 sub-assets 0 rows, `kala_readiness: null`; `pact_query` stage 4 TRIGGER unreachable → `pact_status: chain_incomplete_infra` (PROMISE=promised, CONFIRMATION=confirmed, ACTIVATION=active_now all resolve — only the transit gate fails). **Consequence: no transit-gated prediction is currently possible anywhere in the system.** Shares root cause with CR-8 (ref_* 401s) | 4 | **CRIT** | kala_temporal_bundle + pact_query, 07-13 | Restore sidecar + key wiring; any transit-dependent tool must fail loudly, not fail open | OPEN — ELEVATED |
| CR-41 | **All 4,487 `kala_activation` rows are historical — zero overlap the forward year.** `kala_windows_get` returns 0 activations for 2026-07-13→2027-07-13 with an honest `empty_reason` naming the cause; `get_temporal_windows` and `kala_priority_ranking_get` likewise 0. This is *distinct from* CR-1/CR-5/CR-12 (which are join defects): here the rows exist, are dated, and are all in the past. The L3 writer is not projecting forward | 4 | **CRIT** | 3 tools, 07-13 sweep | Root-cause the ka_* writer's forward horizon; activation must span the dasha runway, not just history | OPEN — ELEVATED |
| CR-42 | **`ref_*` filter-fallthrough family — silent wrong answers (worst defect class in the estate).** Four tools accept their documented filter param, silently discard it, and fall back to a generic hybrid citation search, returning **OCR'd index pages instead of the structured rows their descriptions promise**: `ref_dasha_systems_get` (`system` ignored → `query_used:"dasha"`), `ref_dignity_reference_get` (`planet=saturn` ignored → `query_used:"dignity"`), `ref_nakshatra_get` (`nakshatra=purva_bhadrapada` ignored → generic search, no `bg_nakshatra` rows), `ref_remedies_search` (`keyword="Saturn"` dropped, `filters_applied.planet:null`, **returns Jupiter remedies for a Saturn query**). **EXTENDS CR-10** with 4 new members and a new failure mode (fallback-to-citation-search, not merely filter-ignored). No error is raised — a consuming LLM cannot detect this without cross-checking | 2+6 | **CRIT** | sweep slice E, 07-13 | Honor the filter or reject it loudly. A dropped filter must never degrade to a different corpus | OPEN — ELEVATED [PARIŚODHANA Phase-A Prober A3 ANNOTATION 2026-07-27: MIXED — one member ALREADY-FIXED (register stale), three members LIVE-OPEN but under a DIFFERENT failure mode than documented here. (1) `ref_remedies_search` (now `ref_remedies_get`): ALREADY-FIXED — `planet="saturn"` vs `planet="jupiter"` return genuinely distinct, correctly-filtered rows (`filters_applied.planet` honored, no fallback), matching the D-1.6 S-1 fix (register_p1_reference.ts code comment cites "CR-42/R-19/R-20 fix (D-1.6 S-1)"). Case-sensitivity also confirmed fixed — see CR-10 annotation. (2) `ref_dasha_systems_get`, (3) `ref_dignity_reference_get`, (4) `ref_nakshatra_get`: code inspection of `platform-mcp/src/tools/register_p1_reference.ts` (:297-447) confirms the documented CR-42 fix IS present in the working tree — `ref_dignity_reference_get` now queries the structured `bg_dignity_reference` table directly by `graha` (:328-346); `ref_dasha_systems_get`/`ref_nakshatra_get` now route their filter value through `query_text` (top-priority param) instead of being silently out-prioritized by a static `topic` value, with an honest `fallback_reason` disclosed. BUT live production calls to all three (with the documented filter values, with other values, and with NO params at all) uniformly fail — `ref_dignity_reference_get` → `"[p1_reference] platform DB query failed: 400"`; `ref_dasha_systems_get` and `ref_nakshatra_get` → `TimeoutError: The operation was aborted due to timeout` — reproduced twice each, while sibling tools on the same server (`ref_doshas_get`, `ref_yogas_get`) succeeded in the same window, ruling out transient server-wide overload. So the SPECIFIC documented defect ("silently discards the filter, no error raised, consumer cannot detect") does not reproduce — these three now fail LOUDLY, the opposite of silent — but the tools are completely non-functional in production right now, a live, reproducible, and apparently NEW defect (not previously logged in this register, SHODHANA, or SAMAPANA) whose root cause was not established this session (no fix-code changes made, per this probe's read-only mandate). Flagging for a dedicated follow-up investigation (possible candidates: the `platformQuery` direct-SQL path for the dignity table; the `query_text`-keyed branch of `marsys://tool/L0/query_classical_texts` for the other two — `ref_rules_search`'s sibling `keyword`-keyed branch on the SAME capability succeeds live). Row left OPEN — ELEVATED (not flipped) pending native/Conductor ratification and a root-cause pass on the three erroring tools.] |
| CR-43 | `ganita_yogas_get` serves 32 yoga/dosha **catalog** rows, all sampled with `fire_reason: requires_pass` — i.e. presence of a row ≠ the yoga fires in this chart. A consumer that reads the rows as findings will hallucinate yogas. **CONFIRMS CR-33 from the retrieval side** (CR-33 is the verdict-builder half; this is the row-semantics half) | 5 | HIGH | ganita_yogas_get, 07-13 | Separate `catalog` rows from `fired` rows in the payload, or gate catalog rows behind an explicit flag | OPEN |
| CR-44 | `assess_*` payloads (career/health/marriage/wealth) contain **no contradictions, no convergences, no dasha-window sections at all** — the keys are absent entirely (only a QA stub `"contradictions":{"ok":true}`), despite the tool descriptions promising "convergences, tensions, and activating dasha window." The four domain assessors are signal-ref dumps (27/21/32/29 refs) with the synthesis stripped out | 5+6 | HIGH | 4 assess_* calls, 07-13 | Populate the promised sections or amend the tool contract; a description that promises absent keys is a consumer trap | OPEN |
| CR-45 | **Served signal headlines are subject-anonymous field dumps** — e.g. `"graha dignity per varga: dignity state = exalted"` with **no graha and no house named**. Chart-relevance is recoverable only via `chart_id`, `citation_human`, or CGM node labels. Every ranked-signal surface in the estate inherits this. This is the mechanical reason readings drift generic (pairs with CR-36's Class-9 specimen) | 7 | HIGH | bodha_signals_get, get_signals, assess_*, 07-13 | Headline must name subject + house + varga. Salience without subject is unusable evidence | OPEN |
| CR-46 | `phala_anchors_get` returns `anchor_count: 100` that are **98 duplicates of 2 real anchors** (all `career_discovery_event`, conf 0.322). `event_anchors` dedups them correctly and states the fix belongs in the `ph_nimitta` writer — so the dedup logic exists but sits downstream of the tool that reports the count. The inflated count also propagates into `phala_outlook` summaries | 4 | HIGH | phala_anchors_get vs event_anchors, 07-13 | Dedup at the ph_nimitta writer; anchor_count must be post-dedup | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: phala_anchors_get(2020-2030) → anchor_count=4, all 4 distinct event types (no 98-dup flood; post-dedup). Bonus: a call without date_range now REJECTED loudly (422), not silently defaulted. Payload 20KB (was 421KB)] |
| CR-47 | `phala_rectification_get`: **`lel_fit_score: 0` across all 185 candidates — 0 of 36 LEL training events matched.** The rectification engine is not discriminating between birth-time offsets at all; the "best time 10:43" it returns is the input, not a finding. Silent zero-signal failure — it looks like it worked | 4 | HIGH | phala_rectification_get, 07-13 | Root-cause the LEL↔candidate matcher; a 0-fit run must self-report as non-discriminating | OPEN |
| CR-48 | `kala_yoga_activation_get` / `yoga_activation_by_dasha` serve 5 yogas with **`activation_start: null` on every row** (`undated_activation_count: 5`) — so "which yogas are ripening now" is not actually time-gated. **CONFIRMS CR-12 + CR-37 on 482012f1 in the L1-sweep context** (5 rows here vs 10 in the wealth round — the count varies but the null-date defect is identical) | 4 | HIGH | 07-13 sweep | Same fix as CR-12 | OPEN — UPDATE evidence → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A2, 2026-07-27: live `kala_yoga_activation_get`(482012f1) `total_count`=16, `undated_activation_count`=9 — NOT "every row" as originally claimed. The 7 dated rows are exactly the single-graha-lord `yoga_label` signals (Anapha/Sasa/Vasi/Yuga) plus one Jaimini flag; the 9 still-undated rows are structurally-always-on nabhasa yogas (Kedara/Gola/Shoola, `distribution_yoga_sankhya`, 3 rows) and birth-moment panchanga facts (`arambha_iso`/`name`/`number`/`inauspicious_flag`/`combination_name`/`constituent_facts_jsonb_atomic`, 6 rows) with no natal dasha-lord to match against — a narrower, by-design residual matching the documented Gate Ś #8 PARK (`STATE_D-1.6.md`: "these 74 rows... are structurally different from the now-working DIGNITY/SUBSYSTEM classes... needs new `dasha_eligibility_rule` construction work"), not the original "null on every row" defect. The dasha-matchable class is fixed by `8dd45fc8` + `9bac956f`; the always-on/birth-moment residual is a bounded, evidenced, still-open design gap (see live PARK note) rather than a reproduction of CR-48 as stated.] |
| CR-49 | **Oversize family re-confirmed on 482012f1 at near-default args** (UPDATES CR-13 with a second chart + new members): `bodha_domain_reading_get` **4.1MB**, `assess_career` 724KB, `ephemeris_cache_year` 814KB (32,862 lines), `assess_marriage` 533KB, `assess_wealth` 161KB, `traverse_graph` 180KB, `get_cgm_subgraph` 98KB, `get_projections` 68KB. Six of these spill to disk and are unusable inline. `ganita_tajaka_get` is a **new** member: 245 hadda + 48 varsha rows with no default limit | 6 | HIGH | 07-13 sweep | C1-style budget discipline estate-wide; `ephemeris_cache_year` needs date-range/pagination; `ganita_tajaka_get` needs a default limit | CLOSED_WITH_EVIDENCE (budgeted members) + RESIDUAL routed to Lane S-5 [D-1.6 S-7, 2026-07-16: bodha_domain_reading_get 41KB, assess_career 47KB (was 724KB), event_anchors 14KB, ganita_tajaka_get 56KB, phala_anchors 20KB (was 421KB) — all under the 64KB Gate Ś ceiling. RESIDUAL still >64KB: get_cgm_subgraph 99.7KB, phala_mitigation_get 99.9KB, get_projections 70KB, traverse_graph 99KB at depth=1 — Lane S-5] |
| CR-50 | `ganita_positions_get` **unfiltered leads with aprakasha/upagraha rows** (PIDAA/PATALA/…) rather than the nine grahas — the default call for "show me the planets" does not show the planets. A `planet` filter is required to reach graha rows. (Distinct from CR-10's case-sensitivity bug, though same family) | 6 | MED-HIGH | ganita_positions_get, 07-13 | Default ordering must lead with the nine grahas + lagna; upagrahas behind a facet | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: ganita_positions_get default page → categories=["graha_position"] only; 9 grahas + Lagna; include_upagrahas=false by default, upagrahas served AFTER, never interleaved] |
| CR-51 | **Alias-contract violation: `query_calibration` and `mimamsa_calibration_get` are documented as aliases but return different payloads.** `query_calibration` → `rows: []`, row_count 0. `mimamsa_calibration_get` → 9 LL1 family multipliers + 141 QA checks. A consumer trusting the alias claim gets an empty answer to a question the system can answer. Pairs with CR-30 (twin-alias hygiene) | 5 | MED-HIGH | both tools, 07-13 | One face per capability, or make the alias real | OPEN |
| CR-52 | Tantric-remedy corpus is **empty on both filter axes** — `ref_tantric_remedies_get(planet=saturn)` → 0 rows, `query_tantric_remedies(deity=Durga)` → 0 rows. Both now emit an explicit `empty_reason` (so CR-11's honesty fix has landed here — **positive delta**), but the underlying corpus has admitted nothing. Data gap, not code fault | 1 | MED | 07-13 sweep | Populate the tantric corpus or retire the tools | OPEN |
| CR-53 | **Positive verifications from this sweep (do NOT re-flag).** L2 Bodha is the healthiest layer in the estate: 22/22 PASS, 9,401 MSR signals served, 319 CGM nodes / 739 edges, 84.44% two-pass verified, DEFECT-001 clean (0/68,165 orphan refs). L1: 24/26 PASS, FORENSIC 7/7 re-verified live via `ganita_natal_positions_compute` (Sun Cap · Moon P.Bhadrapada · Lagna Aries 12.4311° · Shukla Tritiya · Ravivara · Shiva · Garaja). `sensitive_degrees` 55 rows firing correctly (Jupiter papa-kartari fired; gandanta/mrityu-bhaga correctly not fired). `get_graha_yuddha` → 0 pairs is **correct**, not a defect (no tara-graha pair within 1° in this chart). `traverse_graph` **worked** in this sweep with a valid seed — CR-7's first-character DSL parse bug did not reproduce; **re-test CR-7 before spending fix effort on it**. Empty-reason honesty pattern now present across kala/temporal/tantric tools | VERIFIED | — | 07-13 sweep | Baseline for the next re-audit | LOGGED |

### §F.1 — Remediation priority implied by this sweep

The sweep resolves the estate into **three tiers**, and they are not equally urgent:

1. **Silent wrong answers (fix first).** CR-42 (ref_* returns Jupiter for a Saturn query), CR-43
   (catalog rows readable as fired yogas), CR-44 (promised keys absent), CR-51 (alias returns empty
   for an answerable question), CR-47 (rectification reports success on zero signal). These are worse
   than failures: a consuming LLM cannot detect them, and each one manufactures a confident falsehood.
2. **Temporal blackout (fix second).** CR-40 (sidecar down → no transit gating anywhere) + CR-41
   (all activation rows historical) + CR-48 (yoga activations undated) + CR-1/CR-5 (dasha joins
   unpopulated). Together these mean **the system currently cannot answer "when."** It can answer
   "what" and "why" at acharya grade. Predictive claims made today are hand-derived from L1
   longitudes, not read from a working L3 surface — and any reading must say so.
3. **Usability / budget (fix third).** CR-49 oversize family, CR-45 anonymous headlines, CR-50
   default ordering. Painful, not dangerous.

## §G — 2026-07-13 fresh financial reading, capability gap audit (chart 482012f1, build ee0829d6)

**Method.** A clean-context, tool-first, full-estate financial reading of 482012f1 — no reliance on prior
conversation context or memory. Tools called live: `judgment_query(wealth, v3, max_signals=40)`,
`bodha_signals_get(domain=wealth, top_k=15)`, `ganita_condition_get(dignity + karakas)`,
`ganita_strength_get`, `ganita_dashas_get(L2, 2027–2040)`, `ganita_chart_facts_get(D2/D11 dignity;
divisional_chart=D2)`, `lel_query`. Then: audit of **what the instrument served vs. what an acharya-grade
reading actually required**.

### §G.0 — The finding that governs this entire section

The reading rested on **six load-bearing conclusions.** The engine served **zero** of them. Every one was
hand-derived by the consuming LLM from L1 longitude facts:

| # | Load-bearing conclusion | Engine's answer |
|---|---|---|
| 1 | **Dhana Yoga: 2L Venus + 9L Jupiter conjunct in the 9th, in Jupiter's own sign** — a textbook Parāśara dhana yoga | `bearing_yogas: []`, **zero dhana yogas detected** |
| 2 | **Varga collapse:** 2L + 11L BOTH debilitated in D9; 2L, 11L, Mercury, Moon ALL in the **12th house of D2** | No signal. No varga-divergence class exists |
| 3 | **Nīcha-Bhaṅga (D9), two grahas:** Saturn (Sun in D9 lagna kendra + Saturn in kendra); Venus (Mercury in D9 kendra) | `not_applicable_or_intact` × 7 — detector is D1-scoped |
| 4 | **The wealth-loss mechanism:** exalted Rahu IN the 2nd + 8L Mars aspecting the 2nd at strength **1.00** + Mars–Saturn in the 7th (partners) | Served as `valence: neutral`, `salience 0.575`, headline `"lord aspects = neutral_link"` — **ranked as noise** |
| 5 | **Venus is the WEAKEST graha by shadbala (4.64) AND is 2L AND is MD lord for 20 years from 2034** | No surface joins these three facts. Digest instead reports `weakest_graha: "Mercury"` — **factually wrong** (Mercury 7.55) |
| 6 | **Ketu MD 2027–2034: 7 years under an 8th-house lord with shadbala 0.625** — the lean stretch | `lord_natal_shadbala_total: 0.625` is served as a bare number; nothing interprets it |

**The instrument is a superb fact store and a failing judgment engine.** The L1 retrieval was fast, clean,
fully grounded, and correct on every value I checked. The L2+ judgment surface ranked the chart's central
wealth mechanism at salience 0.575 with valence `neutral`, and returned 14 of 15 top wealth signals as
`neutral_link` boilerplate out of a 1,594-signal pool.

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-54 | **Functional-lordship VALENCE is broken — the single worst judgment defect found to date.** `bodha_signals_get(domain=wealth)` serves `"Lord of H8 (Mars) aspects H2 in D1: neutral_link"` with `valence: neutral`, `computed_salience: 0.575`, `signature_tier: supporting`, rank 9957/9401. **An 8th-lord aspect on the 2nd house is the chart's core wealth-DESTRUCTION mechanism** (type specimen: this native's May-2025 fraud, LEL `d81fae4e`). The `link_type` vocabulary has only `neutral_link` / `trikona_link` — **there is no dusthana_link, no maraka_link, no kendra_link, no functional-malefic class.** Any lord-to-house link that isn't a trikona is silently flattened to "neutral". The system cannot distinguish a benefic yoga from a destructive one at the link level | 7+2 | **CRIT** | signals `b16fd407`/`bbc1c7cf`, facts `1ea14404`/`82cc6f52`, 07-13 | Extend `link_type` taxonomy to the full functional-lordship matrix (trikona/kendra/dusthana/maraka/badhaka); derive valence from functional lordship, not from a default | **CLOSED (D-2 GATE GREEN 2026-07-17, DR-9/DIS.022)** — valence now derived from natural × functional × dignity × contact-type (signed 4-way), consumed by all emitters. The 8L-Mars→H2 link is no longer flattened to neutral; G0-4 amended assertion serves the Rahu/Mars-on-H2 wealth-loss mechanism in a signed adverse/threat layer, gate-verified. See REPORT_D-2.md §3 (G0-4) / §4 row 3. Residual granular-surface asymmetry (raw valence_pass Rahu-aspect rows vs synthesized affliction_mechanisms offset) carried to D-3 (REPORT_D-2 §6.2). |
| CR-55 | **`weakest_graha` in the chart digest is FACTUALLY WRONG.** `vw_chart_digest` reports `weakest_graha: "Mercury"` for 482012f1. Actual shadbala: **Venus 4.64** < Moon 5.65 < Mars 5.57 < Mercury **7.55** < Jupiter 7.80 < Saturn 7.83 < Sun 8.47. **Mercury is the 4th-strongest graha; Venus is the weakest.** The digest is served at the top of `judgment_query`, `get_chart_orientation`, `bodha_chart_digest_get` and every orientation surface — so a wrong headline fact is injected into the top of nearly every reading. (This is the mirror-image of CR-36, where MSR's "weakest graha = Mercury" verdict was flagged for ignoring vargottama — the deeper issue is that it isn't computing shadbala at all) | 5 | **CRIT** | digest vs `ganita_strength_get`, 07-13 | Root-cause what `weakest_graha` is actually computed from; it does not match shadbala. Either fix it or rename the field to what it measures | OPEN — ELEVATED |
| CR-56 | **Dhana-yoga family still absent — now with an unambiguous type specimen (CONFIRMS CR-22/CR-35 for the third time).** 482012f1: **2L Venus (H9) conjunct 9L Jupiter (H9, own sign Sagittarius)** = Dhana Yoga by BPHS's plainest formulation (lord of dhana joined to lord of bhagya in a trikoṇa). Engine: `bearing_yogas: []`, `yogas_fired: 0` for wealth. Also absent on this chart: Budha-Āditya (Sun 22°11′ + Mercury 1°09′ H10, 21° apart → non-combust, **fires**), Sarasvatī, Lakṣmī. Yoga catalog serves Nabhasa/PMP/Chandra-adhi families only. **Three consecutive readings on two charts have now had to hand-derive the yoga layer.** This is no longer a gap; it is the system's defining deficiency | 1 | **CRIT** | judgment_query wealth + ganita_yogas_get, 07-13 | Build the house-lord yoga detector family (dhana, raja, Budha-Āditya, Sarasvatī, Lakṣmī, Vipareeta). This is the #1 acharya-grade blocker | OPEN — ELEVATED |
| CR-57 | **No VARGA-DIVERGENCE signal class — the promise/ripening gap is structurally invisible.** 482012f1 type specimen: rāśi promise is large (2L+9L conjunct trikoṇa, 11L exalted kendra, exalted Rahu in 2, full 2/11 axis) but **fails to ratify in every operative varga** — D9: Venus **debilitated**, Saturn **debilitated**, Jupiter falls to the 12th. D2: Venus, Saturn, Mercury, Moon **all in the 12th house**. This divergence IS the reading's central structural finding — "wealth promised, visible, and does not settle" — and **no signal, no discovery, no contradiction row fires on it.** The system stores D1 and stores D9/D2 and never compares them | 7 | **CRIT** | facts `f764a762`, `0386b8eb`, `bc957315`, `d1fc6cef`, `f70f865a`, 07-13 | New signal class: `varga_ratification_divergence` — for each bhāveśa/kāraka, compare D1 dignity vs operative-varga dignity; fire when the sign flips. High salience by construction | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: bodha_signals_get(signal_type_class=varga_ratification_divergence) → live signals, e.g. "SATURN: D1 exalted vs D9 debilitated — wealth/career/health ratification fails in D9", salience 1.2, signature_tier=major, valence=malefic; constituent facts resolve. Shipped by D-1 Night-1 Lane 4 + ga_vichara LANE2] |
| CR-58 | **D2 HORA has no semantic layer — the wealth varga is served as raw dignity only.** `ganita_chart_facts_get(divisional_chart=D2)` returns 690 rows of which the overwhelming majority are `aspect_jaimini_per_varga` rasi-drishti boilerplate. The classical Hora doctrine — **Sun's hora (Leo) = self-earned / active wealth; Moon's hora (Cancer) = inherited / passive / accumulated wealth** — is not computed anywhere. On 482012f1 this is decisive: Sun (own), Jupiter, Rahu sit in **Leo/Sun-hora**; Venus (2L), Saturn (11L), Mercury, Moon, and a **debilitated Mars** all sit in **Cancer/Moon-hora, D2 house 12**. "Both wealth lords are in the passive hora, in the 12th of the wealth chart" is the sharpest single sentence in the reading and the system cannot say it | 1+7 | **HIGH** | D2 dignity facts, 07-13 | Compute + store hora-class (`surya_hora` / `chandra_hora`) per graha; add a D2 wealth-semantics reader | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: ganita_chart_facts_get(divisional_chart=D2) → varga_hora_class rows live with surya_hora/chandra_hora + hora_d2_house per graha. Type specimen reproduced: Sun/Jupiter/Rahu(+Ketu,Lagna)=surya_hora D2H1; Venus/Saturn/Mercury/Moon/Mars=chandra_hora D2H12] |
| CR-59 | **Varga-scoped NBRY still blind (CONFIRMS CR-34 — second reading, same chart, decisive impact).** The detector's D1-only scope means it misses **both** cancellations that reverse this chart's financial verdict: Saturn debilitated D9 Aries → **cancelled** (exaltation-lord Sun in D9 lagna Cancer = kendra; Saturn itself in D9 kendra) and Venus debilitated D9 Virgo → **cancelled** (dispositor Mercury in D9 Capricorn = 7th = kendra). Without these, the reading's verdict is "wealth promise fails." With them, it is "wealth promise is delayed and then delivers disproportionately in Venus MD 2034." **The detector's blindness inverts the answer** | 1 | **CRIT** | facts `f764a762` (D9_SAT debilitated), `4814c825` (D9_SUN Cancer H1), `856875fd` (D9_MER Capricorn H7); derivation 07-13 | Extend `ga_yoga_writer.detect_neecha_bhanga` per-varga (D9 minimum); store grounds-checked per verdict | **TEXT CORRECTED (D-2, 2026-07-17): NBRY DETECTION IS LIVE — the "blindness inverts the answer" framing is superseded.** `ga_yoga_firings_get(482012f1)` fires `neecha_bhanga_raja_yoga` with `bhanga_active:true` + a per-varga `grounds_jsonb` ledger (BPHS Ch.39) naming BOTH D9 cancellations (Venus/Virgo via Mercury-kendra; Saturn/Aries via Mars-kendra-from-Moon + Sun-H1) — live since D-1.6 S-3, gate-verified at D-2 §G.1 G0-3. The residual was never detection: it was **routing/ranking surfacing** — the wealth vidhi's `nbry_scan` primitive routed to the catalog face that doesn't evaluate NBRY, fixed to the firings surface in D-2 (PR #596). Remaining D-3 residual: L2 salience ranking of NBRY signals. Disposition: detection CLOSED; surfacing/ranking → D-3. |
| CR-60 | **No DASHA-LORD CAPABILITY index — the system serves the number and never reads it.** `ganita_dashas_get` correctly enriches every row with `lord_natal_house_d1`, `lord_natal_dignity_d1`, `lord_natal_shadbala_total` (excellent, keep). But **nothing interprets the join.** 482012f1: **Ketu MD 2027-08-18 → 2034-08-18 — 7 years, lord in the 8th house, shadbala 0.625** (near-nil). And **Venus MD 2034–2054 — 20 years, lord is the WEAKEST graha in the chart (4.64) and simultaneously the 2L carrying the entire dhana promise.** "Your next 7-year mahadasha lord is your second-weakest graha in a dusthana" and "your 20-year wealth dasha is ruled by your weakest graha" are the two most actionable sentences available about this native's finances, and no surface produces either | 7 | **HIGH** | dasha rows + shadbala, 07-13 | New derived surface: `dasha_lord_capability` = f(shadbala percentile, house class, functional lordship, varga ratification). Emit a warning tier when a long MD is carried by a weak/dusthana lord | CLOSED_WITH_EVIDENCE [D-1.6 S-7, 2026-07-16: ganita_dasha_lord_capability_get live → 9 lords with house_class, shadbala_percentile, functional_lordship, ratification_factor, deterministic warning_tier; fact_ids resolve; honest judgment_flags for Rahu/Ketu/Moon gaps. Ketu percentile 0.125→watch; Venus 4.64 rupa/percentile 0.25→watch] |
| CR-61 | **Arudha layer stored but never ranked (arudha's own CR-26).** `arudha_pada` facts ARE present and correct (`ARUDHA_A1.house_d1 = 9` → **Arudha Lagna in the 9th, conjoined the Venus–Jupiter dhana yoga**; `ARUDHA_A11.house_d1 = 2` → gains-arudha in the wealth house). "Your public image sits on top of your wealth promise" is a first-order Jaimini reading and it never surfaces. `bodha_signals_get(frame=arudha)` annotates a frame_context but does not rank arudha-derived signals | 7 | **HIGH** | facts `88b2ed6c`, `3488c508`, 07-13 | Arudha-semantic signal class (AL-conjunction, A2/A11 placement, AL–bhava relationships) with real salience | CLOSED — STALE-CORRECTED (SARVA-SIDDHI, 2026-07-24): shipped via V-5 emitter (PR #585); bodha_signals_get(signal_type_class=arudha) returns 5 ranked rows live for chart 482012f1 |
| CR-62 | **D11 (Labha) is never consulted for wealth.** `judgment_query(wealth)` hardcodes `operative_varga: "D2"` and stops. But **Venus is EXALTED in D11 (Pisces)** — decisive counter-evidence to its D9 debilitation and its D2 12th-house placement, and the reason the reading could conclude "delivers, late and large" rather than "fails." Saturn (11L) sits in the **11th of D11**. The gains chart is the natural third witness for any wealth judgment and the shastra map does not know it exists | 6 | **HIGH** | facts `a12f022d` (D11_VEN exalted), `a7c69a03` (D11_SAT H11), 07-13 | Extend the §28.5 shastra map: wealth → operative vargas **D2 + D11** (and arguably D9 for ratification). Multi-varga confirmation, not single | OPEN |
| CR-63 | **`judgment_query` wealth receipt claims `timing_anchored: true` while `timing_hooks` is `{current: [], lord_mahadasha_windows: [], karaka_mahadasha_windows: [], mahadasha_windows_by_graha: {}}` — completely empty.** The receipt LIES. Also `varga_confirmation.rows: []` while receipt says `D2✓`, and `varga_term: 0` / `varga_moved_verdict: false` — so the D2 confirmation contributed **nothing** to the verdict despite the receipt claiming it was weighted. **RE-CONFIRMS CR-1 + CR-2 on 482012f1/wealth; both remain fully open** | 4+5 | **HIGH** | judgment_query wealth v3, 07-13 | Wire timing_hooks; make the receipt honest (a receipt that reports success on an empty payload is worse than no receipt) | OPEN — UPDATE evidence |
| CR-64 | **Nakshatra semantics still never rank (CONFIRMS CR-26 on 482012f1).** Financially load-bearing and entirely absent from every served surface: **Venus in Purva Ashadha — its OWN nakshatra** (the 2L in its own star, in the 9th); **Rahu in Rohini** (exalted Rahu in the Moon's star, in the 2nd — the wealth-deception signature); **Mercury in Uttara Ashadha** (MD lord 17y); **Moon in Purva Bhadrapada at 29°46′** (end-degree). The LLM must volunteer all of it or the reading goes nakshatra-blind | 7 | **HIGH** | positions + 07-13 reading | Nakshatra-semantic signal class (own-star, dispositor chains, tara bala, end-degree flags) with real salience | CLOSED — STALE-CORRECTED (SARVA-SIDDHI, 2026-07-24): shipped via V-5 emitter (PR #585); bodha_signals_get(signal_type_class=nakshatra_semantic) returns 9 ranked rows live for chart 482012f1; residual data-quality footnote (16.7% orphan constituent_facts_array) tracked separately as new CR-132 |
| CR-65 | **MSR ranked-wealth surface is 93% noise.** `bodha_signals_get(domain=wealth, top_k=15)`: **14 of 15** served rows are `bhava_significance_link:lord_aspects` = `neutral_link` or `lord_in_house_per_varga` boilerplate, at salience 0.575–0.69, drawn from a 1,594-signal wealth pool. The only `major`-tier row served (salience 1.15) is the Mars→H2 aspect — **and it is mis-valenced as neutral** (CR-54). Net: **the top-15 wealth signals contain zero usable judgments.** `signature_tier` distribution confirms the shape: supporting **95.7%**, major 2.9%, chart_defining **0.2%** | 7 | **CRIT** | bodha_signals_get, 07-13 | The salience formula does not discriminate. A signal class where 95.7% of rows are `supporting` is not a ranking — it is a dump. Re-derive salience from functional weight, not structural presence | OPEN — ELEVATED |
| CR-66 | **Phala wealth anchors still ZERO (RE-CONFIRMS CR-19 on the reading path).** The layer built to answer "when, for money" has no wealth-domain anchors on this chart. The entire timing spine of the reading (Mercury MD ends 2027-08-18 → Ketu MD 7y → **Venus MD 2034-08-18, 20y**) was assembled by hand from `ganita_dashas_get` L2 rows. L4 contributed nothing to a financial timing question | 4 | **HIGH** | phala probes + reading, 07-13 | `ph_nimitta` wealth-domain coverage | OPEN — UPDATE evidence |
| CR-67 | **Remedy engine contributed nothing to the two interventions (RE-CONFIRMS CR-20/LCA-5).** `bo_upaya` wealth resonances remain 0. The reading's two interventions — (1) strengthen Venus specifically, routing the NBRY through Mercury's significations, formalizing the native's *already-spontaneous* 2025 turn toward Venkaṭeśvara/Lakṣmī-Nārāyaṇa as the Śukra remedy; (2) ring-fence the 2nd house against the 8L-Mars/Rahu mechanism, separating 8H-flavoured extraction businesses from the wealth-holding vessel — **were constructed entirely by hand.** Note the specific miss: the system holds the LEL spiritual arc (Śani sādhana 2002+, Ugratārā 2010+, Mahādev 2015+, **Viṣṇu/Venkaṭeśvara 2025**) AND the graha weakness (Venus 4.64) and never joins them. **The native's own devotional history contains the prescription the remedy engine failed to produce** | 4+7 | **HIGH** | reading 07-13 | bo_upaya population; join remedy selection to (weakest load-bearing graha × existing sādhana history × dasha runway) | OPEN — UPDATE evidence → CLOSED — STALE-CORRECTED (PARIŚODHANA A5, 2026-07-27): "`bo_upaya` wealth resonances remain 0" does not reproduce. Live `bodha_remedies_get(chart_id=482012f1, domain=wealth)` returns 3 real, domain-joined, cited resonances (Jupiter/Sun/Saturn) with populated `associated_cdlm_cell_count` and 9 citation-backed prescriptions; the response's own `data_gap_note` states "CR-67 CLOSED: associated_cdlm_cells_array is now populated." Shipped PR #741 (merged), live-verified in `PRE_DARPANA_READINESS_v2_0.md §W-3 R-1/R-2` (2026-07-2x) and independently re-confirmed live this session. Note: the register's specific worked example (Venus/Venkaṭeśvara-Lakṣmī-Nārāyaṇa/Mercury-routed remedy joined to LEL sādhana history) was NOT itself re-derived this session — disposition covers the "contributes nothing / resonances=0" defect, not a claim that the exact hand-built intervention now auto-generates. Residual carried forward as **R-09** (`associated_doshas_array` + `estimated_cost_inr_range_jsonb` still 100% NULL, self-disclosed by the tool) — separately tracked, not silently closed. |
| CR-68 | **LEL is walled off from mechanism-confirmation, not just from prediction.** The no-leakage rule (correct for prediction generation) currently means LEL contributes **nothing** to a reading. But 482012f1's LEL contains the exact retrodictive confirmations of the chart's wealth mechanism: `finance/family_windfall` 2010-07, **`loss/financial_deception` 2025-05** (the 2H-Rahu/8L-Mars signature firing), `career/business_stalled` 2021→2026 (the second quarry, 8H/Ketu). A calibration-side surface — *"this chart's 2H mechanism has fired N times, here they are"* — would be enormously strengthening and violates nothing | 8 | **MED-HIGH** | lel_query + reading, 07-13 | New surface: `mechanism_retrodiction` — join LEL events to the signal/mechanism that predicts them, served as CONFIRMATION (never as prediction input). Feeds L5 calibration honestly | CLOSED — STALE-CORRECTED (SARVA-SIDDHI, 2026-07-24): shipped PR #688 (5f27d9d2, 2026-07-21); mechanism_retrodiction_get is live and verified for chart 482012f1 (7 mechanisms fired) |
| CR-69 | **Composite ranking has no "load-bearing × capability gap" axis.** The reading's sharpest move was a three-way join no ranking axis expresses: *graha X is (a) the weakest by shadbala, (b) the carrier of the domain's central promise, and (c) the lord of a 20-year upcoming MD* → therefore **X is the highest-leverage remedial target in the chart, and the intervention window is the years BEFORE its dasha opens.** The composite_4d formula (class_prior × topic_relevance × intrinsic_strength × structural_role × temporal_activation) has no term that rewards *the gap between a graha's importance and its capability* — which is precisely where remedy belongs | 8 | **HIGH** | reading 07-13 | Add a `leverage_index` = domain_load_bearing_weight ÷ capability(shadbala, dignity, varga_ratification), forward-weighted by dasha runway. This is the number remedy should be ranked on | OPEN |
| CR-70 | **Positive verifications from the reading path (do NOT re-flag).** L1 retrieval was **excellent throughout** and is the reason the reading was possible: `ganita_dashas_get` L2 window query returned 13 clean rows with full natal enrichment (`lord_natal_house_d1` / `dignity` / `shadbala`) — this enrichment is the single most useful serving feature in the estate, keep and extend it. `ganita_chart_facts_get` with `fact_subject` CSV + `category` filter is fast, exact, and fully grounded (`grounding_score: 1`, all fact_ids resolve). `ganita_condition_get(karakas)` correctly serves the full arudha_pada set. `lel_query` served all 57 events cleanly with a proper provenance envelope. `judgment_query` resolution_chains ("House 2 from lagna (Aries) = Taurus → ruled by Venus → 2nd lord Venus in the 9th") is genuinely good acharya-shaped output. DEFECT-001 clean (0/15 orphan refs). **The fact layer is sound. The judgment layer is what needs building** | VERIFIED | — | 07-13 reading | Baseline | LOGGED |
| CR-71 | **Reading-notes for 482012f1 — verified chart knowledge for all future financial readings** (not defects). Structure: **Dhana Yoga** = 2L Venus (H9, Purva Ashadha, shadbala 4.64) conjunct 9L Jupiter (H9, own Sagittarius, Mula, 7.80) — **Arudha Lagna also in H9**, on top of the yoga. **Śaśa Yoga** = Saturn exalted H7 Libra (10L+11L, 7.83). **Budha-Āditya** = Sun 22°11′ + Mercury 1°09′ H10 Capricorn, 21° apart, **non-combust → fires**. Mercury **vargottama** (Capricorn D1/D9). **Wealth-loss mechanism** = exalted Rahu (Rohini) IN H2 + **8L Mars full aspect on H2 at strength 1.00** (fact `82cc6f52`) + Mars–Saturn conjunct H7 (partners). Full **2/11 axis** tenanted (Rahu H2, Moon H11 at 29°46′). **Varga collapse:** D9 → Venus **debilitated** (Virgo), Saturn **debilitated** (Aries), Jupiter → 12th; **both debilities carry NBRY grounds** (Sun in D9 lagna kendra; Mercury in D9 kendra). D2 → Venus/Saturn/Mercury/Moon **all H12, Chandra-hora**; Sun (own)/Jupiter/Rahu in Leo H1, Sūrya-hora; Mars **debilitated** D2. **D11 → Venus EXALTED (Pisces), Saturn in H11.** Shadbala order: Sun 8.47 > Saturn 7.83 > Jupiter 7.80 > Mercury 7.55 > Moon 5.65 > Mars 5.57 > **Venus 4.64** > Ketu 0.625 > Rahu 0.375. **Dasha spine:** Mercury MD (3L/6L, no dhana participation) 2010-08-18 → **2027-08-18**; **Ketu MD (H8, shadbala 0.625) 2027-08-18 → 2034-08-18** — sub-windows: Ketu-Venus 2028-01-15→2029-03-16 (first preview), **Ketu-Rahu 2030-07-19→2031-08-06 (highest loss/fraud risk)**, Ketu-Jupiter 2031-08→2032-07 and Ketu-Saturn 2032-07→2033-08 (the two constructive windows); **VENUS MD 2034-08-18 → 2054-08-18 (20y)**, Venus-Venus to 2037-12-18 = **the dhana dasha, the activation of full financial potential** | READING-NOTES | — | 07-13 fresh reading | Carry into every future 482012f1 financial reading | LOGGED |

### §G.1 — What this section changes about remediation priority

§F said: *fix silent wrong answers first, temporal blackout second.* §G does not overturn that — it **adds a
third axis that outranks the usability tier and arguably rivals the first**:

**The judgment layer does not exist yet.** L1 is excellent. L2 stores 9,401 signals of which 95.7% are
`supporting`-tier boilerplate, cannot tell a dhana yoga from a lord-aspect, cannot tell a benefic link
from a destructive one (CR-54), reports the wrong weakest graha (CR-55), and never compares a rāśi promise
against its varga ratification (CR-57). **A reading produced from this system today is only as good as the
LLM's ability to ignore what the system ranked and re-derive the chart from raw facts.** That is exactly
what happened in this session, twice.

**The five that would most change the output, in order:**

1. **CR-56 — house-lord yoga detector family** (dhana / raja / Budha-Āditya / Sarasvatī / Lakṣmī / Vipareeta).
   Third confirmation across two charts. Nothing else moves the needle this much.
2. **CR-54 — functional-lordship valence + link_type taxonomy.** Until an 8L→2H aspect stops being
   `neutral`, every ranked surface in the estate is structurally unable to surface a chart's core danger.
3. **CR-57 — varga-ratification divergence signal class.** The promise/ripening gap is the single most
   diagnostic thing in Jyotiṣa and the system is blind to it by construction.
4. **CR-59 — per-varga NBRY.** Escalated: on this chart the blindness **inverts the financial verdict.**
5. **CR-55 — fix `weakest_graha`.** Cheapest fix on this list, and it is currently injecting a false fact
   into the header of nearly every reading in the system.

CR-60 / CR-62 / CR-69 (dasha-lord capability, multi-varga wealth confirmation, leverage index) are the
"next level" items — they are what would take the instrument from *acharya-grade* to *beyond-acharya*,
because they are joins no human holds in working memory. But they are worthless on top of a judgment layer
that cannot yet see a dhana yoga.

## §H — 2026-07-13 esoteric-concept + system-implementation round (chart 482012f1, build ee0829d6)

**Method.** Native-directed interrogation of the concepts and whole systems the prior readings never
touched, plus verification of two synthesis engines the native suspected were misfiring. Tools called
live: `ganita_structural_get(dosha_fires v3 + dispositors + yoga_fires)`, `ganita_positions_get`,
`ganita_condition_get(dignity + karakas)`, `ganita_special_lagnas_get`, `query_chart_facts(special_lagna
+ nakshatra_dispositor_chain)`, `mimamsa_calibration_get`, `mimamsa_insight_get(wealth)`,
`bodha_discoveries_get(wealth)`. Purpose: the "promise-not-delivered" ledger at the **system/concept**
level — computed-but-dark data, half-built systems, and degenerate engines — as distinct from §F (tools
that don't answer) and §G (tools that answer but are blind/wrong on the served surface).

### §H.0 — The governing finding

Two independent things were established. **(1) The `dosha_label` surface is decorative, not computed** —
all 22 labels share ONE generic constituent fact and fire on `requires_pass`, and at least one (`kala_sarpa`)
directly contradicts the genuinely-computed per-varga fact. **(2) Whole classical systems are computed into
L1 and then dropped, or half-built, or degenerate** — the Jaimini chara-karaka axis, the special lagnas
(incl. the two wealth-specific ones), the KP system, the discovery engine, and the L5 calibration loop are
each either dark data or running in a null mode. On this chart these are not cosmetic: the Kāla-Sarpa
false-positive and the Kemadruma false-positive both **survived into the first reading** and had to be
retracted; the special-lagna and chara-karaka layers carry decisive wealth testimony (Indu Lagna in the
8th on exalted Ketu; AmK = exalted 11L Saturn) that no synthesis surface expresses.

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-72 | **The `dosha_label` surface is decorative, not computed (EXTENDS CR-43 to the whole dosha family).** All 22 `dosha_label` rows on 482012f1 (Kāla-Sarpa, Kemadruma, Daridra, Mrityu-Bhāga, Pitru, Graha-Yuddha, …) share **one identical `constituent_facts_array: ["e2b47b2c6d457725"]`** and fire on `fire_reason: "requires_pass"` with generic `classical_tradition` citations — i.e. these are catalog **stubs asserting the dosha exists in the catalog**, NOT detections run against this chart's placements. A consuming LLM that reads a `dosha_label` row as a finding will assert doshas the chart does not have | 5 | **CRIT** | ganita_structural_get(dosha_fires) 07-13; all 22 rows share fact e2b47b2c | Every dosha must carry a genuine per-chart detection with its own constituent facts + a boolean `fires`; retire or gate the shared-stub `dosha_label` rows exactly as CR-43 asks for yogas | OPEN — ELEVATED [PARIŚODHANA A4 ANNOTATION 2026-07-27: ALREADY-FIXED — register stale. Live probe against production chart 482012f1 (`ganita_structural_get` facet=dosha_fires, all=true, called 2026-07-27) now returns exactly 1 dosha_label row total (manglik, `fire_reason:"bespoke_detector:manglik"`), with `catalog_only_rows_in_page:0` and `dosha_label_gate.excluded_total:0` — the 22-row shared stub (`constituent_facts_array:["e2b47b2c6d457725"]`, `fire_reason:"requires_pass"` vacuous-pass) does not reproduce. Root cause per `ga_structural_writer.py`: `BESPOKE_DOSHA_DETECTORS` (kemadruma/daridra/kala_sarpa/manglik + 12 named Kāla-Sarpa variants) replaced the vacuous generic catalog evaluator for those dosha families, and `_evaluate_catalog_rule` now fails closed (`rule_shape_unimplemented`) instead of vacuously returning `requires_pass=True`; a bespoke dosha that genuinely does not form is skipped (`continue`), never stubbed. Shipped via commits 4bebb622 (Doctrine Campaign Night-1), 051a7a50 + 1dd8d632 (D-1.6 S-2c/S-2d, explicitly noting the 2026-07-16 live chart still showed the pre-fix stub pending rebuild), 78d9f070 (EL-18 Manglik unreachable-code fix, 2026-07-25). NOT independently verified per-dosha for all 22 catalog entries beyond manglik/kemadruma/daridra/kala_sarpa — see the CR-73/CR-74 annotations for those four specifically; the other ~18 (Mrityu-Bhāga, Pitru, Graha-Yuddha, Shakata, …) simply did not fire for this chart in the all=true probe, which is consistent with either genuine non-formation or still-unimplemented rule shapes (fail-closed either way, so no decorative false-positive was observed, but per-entry logic quality was not inspected). Disposition: ALREADY-FIXED for this chart's current build.] |
| CR-73 | **Dosha labels are astrologically FALSE, not merely unverified — no negative-condition / cancellation check exists.** Type specimens on 482012f1: (a) `kemadruma` label fires while **Anapha Yoga genuinely fires** (Mercury, a non-luminary, in the 12th-from-Moon — verified) — the two are mutually exclusive by definition, so Kemadruma is provably false here; (b) `daridra` fires despite an exalted 11L, own-sign dhana-kāraka, and 2L in the 9th. The label engine tests a positive trigger and never checks the cancelling condition. **This is the doctrine-level twin of CR-72** | 2 | HIGH | ganita_structural_get(dosha_fires + yoga_fires) 07-13; Anapha fact eb51fdc4 vs kemadruma fact 0e838f88 | Dosha detectors must evaluate cancellation/bhaṅga conditions before firing; a dosha that its own cancelling yoga contradicts must not serve | OPEN [PARIŚODHANA A4 ANNOTATION 2026-07-27: ALREADY-FIXED (register stale) for both cited type specimens — tested individually, not generalized. (a) kemadruma: `ganita_structural_get(facet=dosha_fires, all=true)` for 482012f1 (2026-07-27) returns ZERO kemadruma rows — the bespoke `_detect_kemadruma` now genuinely evaluates "not formed" (Jupiter/Venus sit in the 10th-from-Moon, a kendra-support bhanga ground) and the writer `continue`s past it rather than stubbing a finding. Independently confirmed `ganita_yoga_firings_get(yoga_canonical_id=anapha)` still fires:true (moon+mercury, houses 11/10) — Anapha and Kemadruma no longer contradict because Kemadruma is honestly absent. `ga_structural_writer.py`'s `_cancel_kemadruma` docstring explicitly names chart 482012f1 and this exact Jupiter/Venus-10th-from-Moon omission as the bug this fix closes. (b) daridra: the same live call returns ZERO daridra rows for 482012f1 — bespoke `_detect_daridra`/`_cancel_daridra` now evaluate real 11L/2L dignity plus `ga_vichara` wealth-ratification plus `ga_yoga_firings` dhana-yoga grounds (own detector code, not re-derived); the claimed false-positive does not reproduce. Also spot-checked manglik (same dosha family, not one of the two original specimens): fires:true with `bhanga_active:false` and a genuine BPHS `cancellation_citation_ref` (own/exalt/Jupiter-aspect/sign-specific check actually run) via commit 78d9f070 (2026-07-25) — previously unreachable dead code per that commit's own root-cause note. Commits: 4bebb622, 9b026aee/4de0ee17 (Kemadruma-bhanga kendra-support restoration, CR-73), 051a7a50. NOT tested: the remaining ~18 non-bespoke dosha_label catalog entries — none fired for this chart in the all=true probe so no false positive was observed, but their cancellation logic (if any) was not individually inspected. Disposition: ALREADY-FIXED for kemadruma and daridra specifically.] |
| CR-74 | **Kāla-Sarpa: the served label contradicts the computed fact; and the genuine per-varga verdict has no serving surface.** Genuinely-computed `kala_sarpa_per_varga` D1 = `{fires:false, rahu_house:2, ketu_house:8}` — **not a Kāla-Sarpa chart in the rāśi** (Mars + Saturn in H7 break the nodal hemming). Yet the `kala_sarpa` `dosha_label` row serves (per CR-72). The real signal is that KS **fires in 18 divisionals** (D2-Hora Rahu-H1, D10 Rahu-H1, D6, D12, D16, D20, D24 … — verified per-varga facts), which is a distinct, wealth/career-relevant phenomenon that **no tool surfaces**. The consumer gets the false rāśi label and not the true varga pattern | 5+7 | HIGH | kala_sarpa_per_varga natal fact cfcc4ad0 (false) + 18 divisional_fired facts, 07-13 | Serve the computed per-varga KS verdict (natal + divisional map); suppress the stub label. Divisional-KS deserves its own signal class | OPEN [PARIŚODHANA A4 ANNOTATION 2026-07-27: ALREADY-FIXED (register stale). Live probe against 482012f1 (2026-07-27): `ganita_structural_get(facet=dosha_fires, response_format=v3)`'s `verdict.kala_sarpa_dosha` reports `natal_verdict:"not_formed"` (rahu_house=2, ketu_house=8) plus `divisional_charts_with_fires_true` listing 18 vargas (D2,D4,D6,D8,D10,D12,D14,D16,D20,D24,D32,D40,D50,D54,D60,D108,D150,D2700) — the exact pattern this row describes, now properly served rather than absent. `content.kala_sarpa_reconciliation` = `{dosha_label_row_served:false, dosha_label_fires:false, per_varga_d1_fires:false, agrees:true}` — the `kala_sarpa` `dosha_label` row is no longer served at all (bespoke detector honestly returns "does not form" and the writer skips it rather than stubbing), and an explicit reconciliation receipt confirms agreement with the D1 per-varga fact; no label/fact contradiction reproduces. A dedicated `facet=kala_sarpa` call independently serves the FULL per-varga map (natal + all 29 divisional rows, `total:29`) — satisfying this row's fix-direction verbatim ("serve the computed per-varga KS verdict; suppress the stub label"). Shipped via commit 1dd8d632 (D-1.6 S-2c, live `kala_sarpa_reconciliation` receipt on `get_yoga_dosha`) + 4bebb622 (direct wiring of the label to the computed per-varga function) + the dedicated `facet=kala_sarpa` (D-1.5b). Disposition: ALREADY-FIXED.] |
| CR-75 | **KP SYSTEM is half-built and fully dark (native-flagged for implementation).** Three-part gap: **(a)** there is **no KP sub-lord engine** at all — the 249-fold sub-lord division that is the entire cornerstone of KP is absent, so no cuspal sub-lord, no sub-lord significator, no KP "yes/no" ruling can be produced; **(b)** the facts that DO exist — `kp_cuspal_significators` and `kp_ruling_planets_natal` (seen in `ganita_condition_get` karakas facet) — have **no dedicated serving tool and no synthesis wiring**, they are dark L1 data; **(c)** a `vimshottari_kp` dāśa system exists but the KP 4-level significator hierarchy (occupants-of-star → occupant → owners-of-star → owner) is not built. Consequence: the native's KP cash-flow chain can only be hand-reconstructed from Parāśari `nakshatra_dispositor_chain` facts (Mars→Rāhu→Moon→Jupiter→Ketu→Mercury→Sun, verified) — the KP layer itself answers nothing | 1+7 | **HIGH** | ganita_condition_get(karakas) kp_* categories present but unserved; no sub-lord facts anywhere; 07-13 | Build the KP sub-lord engine (L1); expose kp_cuspal_significators + kp_ruling_planets via a serving tool; wire KP significators into a KP-paradigm judgment cut. Whole-system elevation | OPEN — ELEVATED |
| CR-76 | **Special-lagna wealth signatures computed but never ranked (synthesis-wiring gap; distinct from CR-16's birth-key access blocker).** All 8 special lagnas resolve cleanly in L1 `special_lagna` facts on 482012f1, including the two wealth-specific ones the readings must use: **Indu Lagna (the Dhana Lagna) = Scorpio, 8th house, with exalted Ketu tenanted** (fact 32f67f15) → wealth via 8th-house channels; **Sree Lagna (Lakṣmī point) = Libra, 7th house, conjunct exalted Saturn (11L), lord Venus (2L)** (fact c178ff37); plus **Ghati Lagna = Sagittarius H9 on the Jupiter+Venus dhana yoga**. **None is ingested into the MSR corpus, `assess_wealth`, or `judgment_query`.** A wealth reading that omits Indu + Sree lagna is missing core instruments the system already holds | 7 | **HIGH** | special_lagna facts (INDU/SREE/GHATI/HORA…) 07-13 | Ingest special lagnas into the MSR signal corpus with wealth-relevant salience (Indu + Sree + Hora + Ghati especially); add a special-lagna reader; wire into the wealth lens | OPEN |
| CR-77 | **Jaimini chara-karaka axis dropped at the L2 synthesis boundary (UPDATES CR-17).** `karaka_chara_position` (both schools) and `karakamsa_position` ARE computed — **AmK = Saturn** (the wealth/career minister-kāraka = the chart's exalted 11L, forming Śaśa + NBRY), **AK = Moon** (H11, exalted in D10), **DK = Mercury**, **Karakāṁśa = Gemini** (AK-Moon's D9 sign). But `assess_wealth` / `judgment_query` use a purely Parāśari recipe (bhāva + bhāveśa + kāraka) and **consult none of it**; Chara Dāśā is not served for timing. The single strongest Jaimini wealth testimony in the chart (AmK = exalted gains-lord) is invisible to every judgment surface | 7 | **HIGH** | karaka facts 3d8de38d(AmK=Sat), b0c5e213(AK=Moon), 07-13 | Wire chara-karakas (esp. AmK) + Kārakāṁśa into the wealth/career judgment; serve Chara Dāśā as a timing witness. Closes the "Jaimini computed then dropped" gap | OPEN |
| CR-78 | **The `bo_anveshana` discovery engine is running in a degenerate univariate mode — it is not the cross-domain "Bimba" synthesis it is described as (native-suspected, confirmed).** All 71 wealth discoveries on 482012f1 are `discovery_class: "distributional_anomaly"` (per-subsystem z-scores only), **`cross_subsystem_root: false` on every single row** (zero genuine cross-subsystem syntheses found), `consequence_score: 1` uniform, `corroboration_count: 1`, `calibration_hook: "empty"`, and an **identical boilerplate `why_an_acharya_misses_it` string** templated across all rows. The engine that is supposed to surface patterns "only visible when multiple L1 streams are synthesized" is computing single-variable outliers and labelling them synthesis | 8 | **HIGH** | bodha_discoveries_get(wealth) 07-13, 71 rows | Implement genuine cross-subsystem discovery (multi-stream joins, real corroboration counts, per-discovery reasoning); a discovery with cross_subsystem_root=false is not a discovery | OPEN — ELEVATED |
| CR-79 | **L5 Mīmāṃsā has never ingested an outcome, and its verdict logic is degenerate (native-suspected, confirmed).** `mimamsa_calibration_get`: `verdict_distribution: []`, `reliability_curve: []`; **all 9 LL1 multipliers `promotion_status: prior_only`, `n_observations: 0`, `gate_passed: false`, `kill_switch_state: active`**; 141 QA checks nearly all scoring exactly **0.5** with negative controls `not_implemented`. `mimamsa_insight_get(wealth)`: **"Major Financial Gain" AND "Major Financial Loss" are BOTH returned `denied, grade 1.7/10`, citing the SAME five generic yoga signals** — the verdict engine cannot discriminate opposite outcomes. Structural-mode is by design, but the outcome-recording loop (`mimamsa_outcome_record`) has demonstrably never been fed and the verdict function is null-degenerate | 8+5 | **HIGH** | mimamsa_calibration_get + mimamsa_insight_get(wealth) 07-13 | Seed the outcome loop from the LEL (feeds CR-68); fix the verdict function so opposite event-classes cannot share identical evidence + identical grade; QA that always returns 0.5 must report itself as non-discriminating | OPEN |
| CR-80 | **Reading-notes addendum for 482012f1 (this round; extends CR-71, CR-38 — verified chart knowledge, not defects).** **Kāla-Sarpa: D1 = NONE** (Rahu H2 / Ketu H8; Mars + Saturn in H7 break the hemming) — the chart is NOT Kāla-Sarpa; it fires only in 18 vargas (D2/D10 Rahu-H1, D6, D12, D16, D20, D24…). **Anapha Yoga genuinely fires** (Mercury in 12th-from-Moon) → **Kemadruma is FALSE.** Also firing: Śaśa (PMP), Vasi. **Chara karakas:** AK=Moon, AmK=Saturn, BK=Sun, MK=Venus, PiK=Mars, GK=Jupiter/Rahu, DK=Mercury; **Karakāṁśa = Gemini.** **Special lagnas:** Indu(Dhana) Scorpio H8 (exalted Ketu on it, lord Mars), Sree(Lakṣmī) Libra H7 (on exalted Saturn 11L, lord Venus 2L), Ghati Sagittarius H9 (on Jupiter+Venus dhana yoga), Hora Gemini/H2-axis, Bhava Pisces H12, Varnada Cancer, Vighati Libra H7. **Nakshatra-lord (Parāśari) chain:** Mars→Rāhu→Moon→Jupiter→Ketu→Mercury→Sun→Moon(cycle); everything funnels through Jupiter↔Ketu + Mercury→Sun→Moon-AK(H11). Rāhu in **Rohiṇī** (prosperity star, exalted, H2). | READING-NOTES | — | ganita_structural + special_lagnas + karakas + positions 07-13 | Carry into every future 482012f1 reading | LOGGED |

### §H.1 — What this round adds to remediation priority

§G established that **the judgment layer barely exists**. §H establishes that **the same is true one level deeper — at the whole-system and data-integrity level**:

- **Data integrity (fix alongside §F.1 tier-1 "silent wrong answers").** CR-72 + CR-73 + CR-74: the `dosha_label` surface manufactures false findings (Kāla-Sarpa, Kemadruma both provably false on this chart) with no per-chart computation and no cancellation check. This is the same failure mode as CR-42/CR-43 — a confident falsehood a consuming LLM cannot detect — and belongs in the fix-first tier.
- **Whole systems dark or half-built (new tier, native-directed).** CR-75 (KP: no sub-lord engine + dark cuspal facts), CR-76 (special lagnas unranked), CR-77 (Jaimini chara-karaka axis dropped). These are not signal-tuning items — they are **entire classical methods the instrument computes-then-discards or never finishes.** The native's directive is explicit: these are to be *included for implementation, fixed, and elevated*, not merely noted.
- **Synthesis engines degenerate (elevate with §G's judgment-layer tier).** CR-78 (discovery engine = univariate z-scores, zero cross-subsystem) and CR-79 (L5 calibration never fed, verdicts non-discriminating) mean the two layers meant to lift the instrument *beyond* acharya-grade are presently null. They sit above CR-56/CR-54/CR-57 in ambition and below them in readiness — build the judgment layer first, then these become worth turning on.

## §I — 2026-07-13 asset-elevation deep-review intake (MSR / CGM / convergence writer internals)

**Method.** Two Explore code-reviews of the writer layer (`platform/python-sidecar/`) during the
beyond-acharya design brainstorm: (1) combinatorial-sweep inventory of `ga_structural`; (2) deep
mechanics review of MSR (`bo_laksana.py` + `bodha_writers/formulas.py`), CGM (`bo_bimba`/`bo_karanajala`/
`bo_cgm_paths`/`bo_cgm_motifs`), and convergence (`ka_yojaka`/`services/ka_sangam/engine.py`/`ka_kalasutra`).
These are **code-verified writer-internal defects** (file:line), not consumption-surface observations — they
explain the *mechanical root* of the §E–§H symptoms. All ABSORBED into DOCTRINE_CAMPAIGN_DESIGN §11–§13.

| ID | Finding | Class | Sev | Evidence | Fix direction | Status |
|---|---|---|---|---|---|---|
| CR-81 | **MSR class-prior term is INERT.** `salience_formula_v2` multiplies by `class_prior`, but `bo_laksana.py:1305` passes the literal `1.0` and the `brahma_class_priors` table is **never queried** (confirmed obs 35827). The first factor of the entire ranking formula does nothing — every signal's class contributes equally regardless of astrological weight | 5 | HIGH | bo_laksana.py:1301-1306; formulas.py:525-586 | Query brahma_class_priors as data; make class-prior a live, calibratable term | OPEN → D-1/§11 → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A8, 2026-07-27: code-verified — `bo_laksana.py` now carries `_load_class_priors()` (:917-935) which queries `brahma_class_priors` live (`WHERE prior_version='1.0' AND signal_tradition='*' AND fact_kind='*'`) and `_resolve_class_prior()` (:938+) resolves a real per-signal value at the substep-2052-2061 call site (`class_prior=class_prior_val`); the literal `1.0` survives ONLY as the documented per-row miss-fallback (brief-mandated: "priors are optional by design"), not as the universal hardcode the finding describes. Matches `NIGHT1_LANE4_MSR_ELEVATION.md` §2 Change 1 (register_rows: CR-81) verbatim. Fixed by commit `4bebb622` "Doctrine Campaign Night-1: D-1 judgment substrate + CR-87 hotfix + rebuild infra (resumption)" (2026-07-14, git blame-verified on the retiring comment block). Does not reproduce.] |
| CR-82 | **MSR tier-ceiling is the mechanical root of the 95.7%-supporting mush (CR-65).** `_tier_ceiling_for` (`formulas.py:556-569`) force-caps ANY `*_per_varga` category, any `_FLOOD_PRONE_FAMILIES` member, and any non-D1 varga fact at `supporting` — so the varga layer, where the chart's whole story lives, is **structurally forbidden from ranking chart_defining/major.** The 95.7% supporting distribution is not emergent; it is imposed | 7 | HIGH | formulas.py:538-569 | Replace fixed thresholds + varga ceiling with percentile-in-distribution tiers + **ratification-aware weighting** (a D9-confirmed fact ranks higher, not capped) | OPEN → D-1/§11 → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A8, 2026-07-27: code-verified — `bo_laksana.py:_tier_ceiling_for` (:723-739) now docstrings its own retirement: "the flat 'any non-D1/`_per_varga` fact capped at supporting' rule is RETIRED... Replaced by ratification-aware weighting (`_resolve_ratification_factor`) + percentile tier assignment (`_assign_tiers_by_percentile`)"; only `_FLOOD_PRONE_FAMILIES` members still ceiling at `supporting` (the one exception the design brief itself ratifies — un-capping genuine divisional-matrix rollups would recreate CR-65 in mirror image). `_assign_tiers_by_percentile` (:750-791) implements the percentile-in-distribution tiers (chart_defining=top 1.5%, major=next 8.5%) exactly as prescribed. Matches `NIGHT1_LANE4_MSR_ELEVATION.md` §3 Change 2 (register_rows: CR-82 → CR-65 root) verbatim. Fixed by commit `4bebb622` "Doctrine Campaign Night-1: D-1 judgment substrate + CR-87 hotfix + rebuild infra (resumption)" (2026-07-14, git blame-verified). Does not reproduce.] |
| CR-83 | **MSR valence is a keyword-substring heuristic (deepens CR-54's root).** `_infer_valence` (`bo_laksana.py:269-282`) assigns benefic/malefic/neutral by frozenset category membership + substring scan of `fact_value_text` — **no dignity, no functional lordship, no numeric computation.** Every downstream valence (CGM edges, convergence, readings) inherits this heuristic | 2 | HIGH | bo_laksana.py:269-282 | Valence from the D-1 functional-lordship pass (actor's functional nature × target class), not keywords | OPEN → D-1/§4/§11 |
| CR-84 | **CGM never feeds MSR ranking — graph influence on salience is zero by wiring.** `bo_laksana` runs BEFORE CGM in the DAG and reads no `bodha_cgm_*` table; salience/tier are computed with **zero graph input.** The `structural_role` term that should carry centrality/mechanism-membership is not sourced from the graph at all | 8 | HIGH | bo_laksana fetch SQL (no cgm join); DAG order | Feed CGM centrality/mechanism-membership into MSR's structural_role (requires a second-pass or a post-CGM re-rank asset) | OPEN → D-2/§12 → CLOSED_WITH_EVIDENCE [PARISHODHANA Phase-A Prober A8, 2026-07-27: code-verified — exactly the fix direction named in this row's own "Fix direction" cell shipped: a SEPARATE `@register("bo_laksana_rerank")` `BoLaksanaRerankWriter` (`bo_laksana.py:3232-3358+`, migration 446) is scheduled strictly AFTER `bo_karanajala` in the DAG (comment :3236-3244 explains the second-asset design explicitly avoids a DAG-cycle on the original `bo_laksana`/CLAUDE.md §N.2 FROZEN-contract concern) and UPDATE-only enriches `bodha_msr_signals.structural_role_score` from REAL `bodha_cgm_nodes` centrality (`_fetch_graha_centrality` :3288-3300, blended 40% pagerank + 25% eigenvector + 20% betweenness + 15% harmonic via `_structural_role_from_centrality` :3269-3285). The ORIGINAL `bo_laksana` writer still runs pre-CGM and still has zero graph input at its own compute time (that half of the literal claim is unchanged) — but CGM now demonstrably feeds MSR ranking via this post-CGM second pass, so "graph influence on salience is zero" no longer holds structurally. Does not reproduce as stated.] |
| CR-85 | **CGM→convergence bridge is a dead passenger field.** `ka_yojaka._fetch_cgm_pagerank` (`:182-219`) reads `bodha_cgm_nodes.pagerank_score` and stamps `cgm_centrality_weight` onto every predicate — but `ka_sangam`/`engine.py` **never reads it** (not in `SUPPORTING_WEIGHTS`, not in any current). The one built CGM→timing link is wired and unused | 8 | MED-HIGH | ka_yojaka.py:182-219 vs engine.py currents | Either consume cgm_centrality_weight as a real current or remove the dead stamp; prefer consume (mechanism graph-weight in PROMISE lock) | OPEN → D-2/§12/§13 |
| CR-86 | **CGM edge strengths are hardcoded literals; the formula version is a label with no formula.** argala 0.5, dispositor 0.6, occupancy 0.7, bhava_aspect 0.5, yoga_member 0.5 are constants (`bo_karanajala.py:351,424,505-512,760`); `weight_formula_version:"edge_weight_v1.0"` labels literals. A declared `sade_sati` edge type has **no builder** (dead type). Only pagerank computed; betweenness/eigenvector/harmonic NULL | 5 | MED-HIGH | bo_karanajala.py edge builders | Real edge-strength formula from the valence/salience pass; build or remove sade_sati edge; complete the specced centralities | OPEN → D-2/§12 |
| CR-87 | **CRITICAL: one native's natal constants are hardcoded into SHARED convergence-engine code.** `engine.py:253` `_NATIVE_JANMA_NAK_IDX=24` (Purva Bhadrapada); `:1146-1149` Mode-C sade-sati signs Cap/Aqu/Pis for Aquarius Moon; `ka_sangam.py:35-39` `_NATIVE_LOCATION` = Bhubaneswar lat/lon/tz. **Tara-bala (C_tara), sade-sati (Mode C), and panchanga (C_panchanga) currents are therefore computed against chart 482012f1 for EVERY chart** — Abhinandan's and every future native's activation scores are silently wrong. This is a cross-chart correctness bug hiding in a single-native build | 1+4 | **CRIT** | engine.py:253,1146-1149; ka_sangam.py:35-39 | Resolve janma-nakshatra, sade-sati signs, and location per-chart from chart_facts; add a multi-chart regression guard (Abhinandan must score differently from Abhisek) | OPEN — ELEVATED → D-3/§13 |
| CR-88 | **Convergence promise-side is severed from the chart's judged structure (native-suspected, code-confirmed).** `ka_sangam` selects **only `dignity_score`** from `bodha_msr_signals` (`:97,137-158`), defaulting to 0.5 when NULL. **`computed_salience`, `valence`, varga-ratification, `signature_tier`, and dasha-lord shadbala are ALL ignored.** The two richest MSR outputs (salience, valence) never reach timing — convergence times the sky almost independently of the chart. This is the deepest cause of "activation doesn't reflect the reading" | 7 | **CRIT** | engine.py:454-486; ka_sangam.py:97,137-158 | PROMISE lock (§13): salience × valence × varga-ratification × NBRY-deferral × mechanism-weight as the structural input to activation | OPEN — ELEVATED → D-3/§13 |
| CR-89 | **Convergence has no suppression model + missing classical delivery currents.** `1 − Π(1−wᵢsᵢ)` over 12 currents is strictly additive-saturating (all wᵢ>0); only vedha (×0.3) damps, on the gate side → **destructive interference is mathematically impossible** ("strong dasha, adverse sky, nothing happened" cannot be modeled). Missing entirely: **Guru-Śani double-transit** (grep double_transit/dual/jointly = nothing), **saham currents** (sahams computed as facts, zero currents), **period-lord relational algebra** (AD-lord kendra/trikoṇa/tāra from MD-lord — absent). Inert: `school_consensus` always 0.0 (`ka_sangam.py:514` leaves it empty); ~90% of predicates are non-functional template stubs (obs 24742) | 7 | HIGH | engine.py:454-486,394-422; ka_yojaka binder stubs | TRIGGER lock (§13): signed suppressive currents + double-transit + saham + period-lord algebra; repair or retire school_consensus + stub predicates under the staged-admission LEL test | OPEN → D-3/§13 |

### §I.1 — Amendments to earlier rows (code-review corrected the fix-direction)

- **CR-54 AMENDED.** The premise "link_type has only neutral_link/trikona_link" is **wrong**: `bhava_significance_link` already emits a **4-way** vocabulary (`kendra/trikona/dusthana/neutral_link`, `ga_structural_writer.py:5463`), and `lord_aspects_lord_per_varga` carries **no** link_type (strength-valued). The real defect: the classifier grades the **target house's class**, never the **acting lord's functional nature** — so an 8L-Mars→2H aspect reads `neutral` because H2 isn't kendra/trikona/dusthana. **Corrected fix:** derive valence from the actor's functional lordship × target class, not by extending the vocabulary.
- **CR-76 / CR-77 AMENDED.** Special-lagna / chara-karaka / arudha / KP / tajaka facts are **NOT dropped** — MSR ingests every category. They are **ingested-then-starved**: position-class facts carry no graha/house resolution → default shadbala 1.0 / dignity 0.5 / bindus 4 → hit the CR-82 varga ceiling → sink to background. **Corrected fix:** subject-resolution + un-capping, not "add ingestion."
- **CR-41 SUPERSEDED (not patched).** The all-historical-activation-rows defect is **dissolved by construction** in the Kāla Taraṅga stateless service (no writer horizon exists). Do not spend effort repairing the L3 forward-projection writer.

*Disposition index: every row CR-1..CR-89 is dispositioned exactly once in DOCTRINE_CAMPAIGN_DESIGN_v1_0.md §9
(ABSORBED→D-1..D-4 / INFRA-PREREQ / DEFERRED-EXPLICIT / BASELINE) — completeness verified by enumeration in
the 2026-07-13 spec-review loop (CR-32→BASELINE, CR-78→D-2 were caught missing on the first pass and placed).
The §9 table is authoritative for CR-1..80; §I rows CR-81..89 also carry inline "→ D-n/§m" Status annotations
above. That design is the resolution map; this register remains the living intake, kept in sync at every edit.*

*Merge protocol: next Claude Code session dedupes CR-rows against MARSYS_DEFECT_GAP_REGISTER_v2_0.md
(several UPDATE existing rows: CR-1→R-39, CR-2→R-38, CR-8→LCA-13, CR-13→R-40/LCA-3, CR-20→LCA-5,
CR-24→G-6/LCA-9a; from §E: CR-34/35→CR-22/CR-23, CR-37→CR-1/CR-5/CR-12; from §F: CR-39→CR-14,
CR-42→CR-10, CR-43→CR-33, CR-48→CR-12/CR-37, CR-49→CR-13, CR-51→CR-30, CR-52→CR-11,
CR-53 REOPENS-FOR-RETEST CR-7; from §G: CR-56→CR-22/CR-35, CR-59→CR-34 (ESCALATED), CR-63→CR-1/CR-2,
CR-64→CR-26, CR-66→CR-19, CR-67→CR-20/LCA-5, CR-69→CR-25/G-6; from §H: CR-72→CR-43 (EXTENDS to dosha
family), CR-74→CR-72, CR-77→CR-17 (UPDATES), CR-72/CR-73 are the dosha-label twins of CR-33/CR-43,
CR-76 is DISTINCT from CR-16 (synthesis-wiring vs birth-key access), CR-75/CR-78/CR-79 are NEW
whole-system rows with no prior register entry) and assigns permanent IDs. This file
remains the living intake for consumption-round findings until then.*

*Changelog: v1.4 (2026-07-13) — §I added: asset-elevation deep-review (MSR/CGM/convergence writer
internals, code-verified file:line); rows CR-81..CR-89; 3 rows CRIT/ELEVATED (CR-87 hardcoded-natal-
constants cross-chart bug, CR-88 convergence promise-side severance); §I.1 amends CR-54 (valence from
actor lordship, not vocabulary), CR-76/77 (ingested-then-starved, not dropped), CR-41 (superseded by
Kāla Taraṅga). All rows CR-1..CR-89 dispositioned in DOCTRINE_CAMPAIGN_DESIGN_v1_0.md §9. v1.3 (2026-07-13)
 — §H added: esoteric-concept + system-implementation round on 482012f1;
rows CR-72..CR-80; 3 rows ELEVATED (CR-72 dosha-label decorative-not-computed, CR-75 KP system half-built,
CR-78 discovery engine degenerate); new whole-system rows (KP, discovery engine, L5 calibration, special
lagnas, chara karakas); §H.0 governing finding + §H.1 data-integrity/whole-system/synthesis-engine tiers.
v1.2 (2026-07-13) — §G added: fresh full financial reading of 482012f1 + capability gap audit;
rows CR-54..CR-71; 5 rows ELEVATED (CR-54, CR-55, CR-56, CR-57, CR-59 escalated, CR-65); §G.0 six-conclusion
table; §G.1 judgment-layer priority axis. v1.1 (2026-07-13) — §F added: full-estate retrieval sweep of all
126 exercisable read tools on 482012f1; rows CR-39..CR-53; 3 rows ELEVATED (CR-40, CR-41, CR-42); §F.1
priority tiering. v1.0 (2026-07-13) — §A–§E, rows CR-1..CR-38.*

---

## §M — 2026-07-14/15 Cowork addenda (APPENDED — reconciliation flag; CLOSED by §N below)

**⚠ STATE NOTE (2026-07-15).** This file was at **v1.4** on disk. Cowork sessions on 2026-07-14/15 authored
**§J (CR-90/91), §K (CR-92..96 + the §K.2 12-assertion D-1.5 gate + DR-1 doctrine ruling), and §L
(CR-97..106 capability-elevation intake)** — those sections were **read and absorbed into
`DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md` + the Doctrine-Waves briefs** (the campaign brief's gate table
cites "register §K.2 ×12"), but the register FILE was subsequently rebuilt to v1.4 and did **not** carry
them. **A merge was owed:** re-integrate CR-90..107 into this file, or formally designate the
Doctrine-Waves briefs as the system of record and mark this register SUPERSEDED. Do not assume CR-90..107 are
captured here just because the plan references them — verify against whichever file is authoritative
(the exact CR-96 lesson). CR-107 is appended below so it is not lost in the interim.

| ID | Finding | Class | Sev | Evidence | Fix direction | Disposition |
|---|---|---|---|---|---|---|
| CR-107 | **`muhurta_finder` serves a composite auspiciousness score with NO classical decomposition** — the reasons behind the number are unreachable, so consumers hand-derive them and get them wrong. Live specimen (482012f1, business, Jul 2026): July 16 2026 → `score 0.616` (`panchanga 0.475 / dasha 0.72 / transit 0.69 / signal 0.72`), tithi/nakshatra/yoga/vara named but **nothing decomposed**: no **transit-navamsa**, no **janma-tithi-return** flag (Jul 16 afternoon = Shukla Tritīyā = native's birth tithi — first-order, unsurfaced), no **Sade-Sati state** (transit Saturn ~20° Pisces = 2nd-from-natal-Moon, final leg, near-stationary — unsurfaced), no **nakshatra-nature** tag (Ashlesha = tīkṣṇa — the reason panchanga scored 0.475, unexplained). **Observed twice in the wild:** two expert muhurta write-ups for this date both hand-derived exalted Jupiter's navamsa and both got it WRONG (claimed Cancer-1st / Taurus-11th; **correct = Virgo, 3rd from D9 lagna** — Jupiter 9.3° Cancer → movable-sign N3 = Virgo, verified against ephemeris). The engine holds the data to prevent this and serves none of it | 5+7 | HIGH | live b97b6eb0: muhurta_finder(business, 2026-07-10..25) + query_planet_transit 2026-07-16 (Jup 9.3°Can/Pushya, Moon Ashlesha, Sun 29.7°Gem, Sat ~20°Pis) | Muhurta output must decompose: per-graha transit sign + nakshatra + **navamsa**; janma-tithi / janma-nakshatra return flags; Sade-Sati / kaṇṭaka-śani state; panchanga-element nature tags with the element that drove each sub-score; Abhijit / Rahu-Kalam / Gulika intraday windows. **Fixture: July 16 2026 / 482012f1.** | **→ D-3** (temporal lane; pairs with CR-99 AV-gating, CR-100 Sudarśana, CR-102 vedha) |

---

## §N — D-1.6 Lane S-8 governance reconciliation (2026-07-16) — closes §M's merge debt

**Ruling (S-8, per BRIEF_D1_6.md §F1 Lane S-8 + native scope_ruling): option (b) — this register's
§A-§I + §M (rows CR-1..CR-89 + CR-107) stays the system-of-record INTAKE for those rows; the
Doctrine-Waves briefs + `DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md §8` become the formally-designated
system-of-record for CR-90..106.** Re-typing CR-90..106's full finding text into this file was
rejected as the fix: those rows were authored directly INTO the brief/plan corpus in the 2026-07-14/15
sessions (§J/§K/§L existed only as Cowork-session prose that was absorbed at authoring time, never
committed to this file's git history — confirmed: `git show <D-1.5b HEAD>:<this file>` carries no §J/K/L
content) — re-transcribing them here from secondary references would itself violate the "one system of
record" goal by creating a second, potentially-drifting copy. Instead: **no dangling row range** is
achieved by this pointer table, verified against live grep of the doctrine_waves briefs directory
(2026-07-16):

| CR-range | Live pointer(s) (file:line-ish, verified 2026-07-16 grep) | Notes |
|---|---|---|
| CR-90 | `BRIEF_D1_6.md` Lane S-5 (PARK-#4); `STATE_D-1.5b.md` d1_5a_carryforward note | Residual `keyword_heuristic_v1` valence rows — routed to D-1.6 Lane S-5 |
| CR-91 | `BRIEF_D1_5A.md` Lane A2 | Ingest `aspect_parashari` facts into `VicharaFactIndex` — SHIPPED (D-1.5a) |
| CR-92 | `BRIEF_D1_5A.md` Lane A3; `EXECUTION_PLAN §8` "CR-92 → PARTIALLY STALE" | `bearing_yogas` / `ga_yoga_firings` wiring |
| CR-93 | `BRIEF_D1_5A.md` Lane A4; `EXECUTION_PLAN §8` "CR-93/94 → RE-SCOPED" | PMP narrator position-fetch, deploy-path defect |
| CR-94 | `EXECUTION_PLAN_v1_0.md` line 73 + §8 "RE-SCOPED"; `SESSION_LOG.md` 2026-07-15 entry | dishonest `zero_rows_returned` / coverage counter |
| CR-95 | `EXECUTION_PLAN_v1_0.md` §8 "CR-95 → STALE / CLOSED-BY-REBUILD" | verify-on-deploy at Gate A |
| CR-96 | `BRIEF_D1_6.md` line 155 (this lane's own governing lesson); `BRIEF_D1_5A.md` (migrations-in-platform/migrations note) | meta-lesson: "verify against whichever file is authoritative"; this §N is the CR-96 lesson discharged for this register |
| CR-97 | `BRIEF_D1_5B.md` Lane B-4 (Bhavat Bhavam) | SHIPPED (D-1.5b) |
| CR-98 | `BRIEF_D1_5B.md` Lane B-1 (Bhāva-chalit + real cusps, DR-2) | SHIPPED (D-1.5b) |
| CR-99 | `BRIEF_D1_6.md` §M CR-107 cross-ref ("pairs with CR-99 AV-gating"); `BRIEF_D1_5B.md` CR-99a (Aṣṭakavarga completion, Lane B-2) | AV-gating class; CR-99a sibling shipped in B-2 |
| CR-100 | `BRIEF_D1_5B.md` Lane B-3 (Sudarśana Chakra) | SHIPPED (D-1.5b); S-7 probe (BIND_D-1.6 K-3-Sudarshan) CLOSED_WITH_EVIDENCE |
| CR-101 | `BRIEF_D2.md` (Upapada wiring, BPHS ch.30) | Routed to D-2 |
| CR-102 | `BRIEF_D3.md` (Guru-Śani double-transit + saham currents + gochara vedha filter) | Routed to D-3 |
| CR-103 | `BRIEF_D1_5B.md` Lane B-2 (Bhāva Bala) | SHIPPED (D-1.5b) |
| CR-104 | `BRIEF_D2.md` (Nārāyaṇa Daśā) | Routed to D-2 |
| CR-105 | `BRIEF_D2.md` (pañcadhā-maitrī compound matrix; Chara Daśā as timing witness) | Routed to D-2 |
| CR-106 | `EXECUTION_PLAN_v1_0.md` §8 (un-numbered §L.2 retrogression-doctrine trap, DEFERRED-EXPLICIT) | No live tabular row found beyond the plan's own note that "§L table ends at CR-106" and its content is most-plausibly the retrogression trap; EXECUTION_PLAN is the authoritative disposition, not this file |
| CR-107 | This file §M (verbatim, preserved above) + `BRIEF_D1_6.md` line 19 (routed to D-3 suppression model) | Only CR-90..106 member with an explicit table row surviving in THIS file (§M); kept in place rather than duplicated |

**Verification method:** live `grep -rn "CR-<n>"` across `00_ARCHITECTURE/llm_consumption_audit/**`
+ `00_ARCHITECTURE/DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md` + `SESSION_LOG.md`, 2026-07-16, this
session. Every CR-90..107 ID resolves to at least one live artifact; none is orphaned. Gate Ś item 14
("no OPEN row in either register left un-dispositioned... CR-90..107 range resolved to one system of
record") is satisfied by this table + the frontmatter `status` field above.

**§E CR-33..38 permanent-ID assignment (per the merge-protocol note preserved above, "assigns
permanent IDs" — the obligation this §N discharges for the one sub-range S-8's brief calls out by name):**

| CR-row | Permanent ID / disposition | Rationale |
|---|---|---|
| CR-33 | = `MARSYS_DEFECT_GAP_REGISTER_v2_0.md` **Y-12** | Same finding verbatim (v3 verdict-builder fabricates "not formed" from a truncated page, Sasa-Yoga specimen) — CR-33 is the intake copy, Y-12 is the permanent register row. No new ID needed. |
| CR-34 | = `MARSYS_DEFECT_GAP_REGISTER_v2_0.md` **Y-3** (NBRY effectively absent) + this file's own **CR-22/CR-23** (per CR-34's own text, "UPDATES CR-22/CR-23") | D1-scoped NB detector blindness — Y-3 already carries this defect at the permanent-register level; CR-34 stays cross-referenced, not duplicated |
| CR-35 | = `MARSYS_DEFECT_GAP_REGISTER_v2_0.md` **Y-4** (house-lord yoga family undetected) + confirms this file's **CR-22** | House-lord family (Budha-Aditya/Saraswati/Dhana) — Y-4 is the permanent row |
| CR-36 | No new permanent ID — composite finding fully covered by existing rows: `S-1` (vargottama not joined), `CR-55`/`D-5` (weakest_graha, now S-1 verify-then-close per BIND_D-1.6), `G-6` (chain signal class, D-2), `CR-69`/leverage_index (D-2) | Cross-reference note added here so the finding is traceable without a redundant ID |
| CR-37 | = this file's own **CR-1 / CR-5 / CR-12** (per CR-37's own text, "CONFIRMS CR-1/CR-5/CR-12 on second chart") + `MARSYS_DEFECT_GAP_REGISTER_v2_0.md` **T-4** (kala_activation empty) | Second-chart confirmation of an already-permanent-ID'd defect (R-45 writer date-population fix, D-1.6 Lane S-4) |
| CR-38 | READING-NOTES (not a defect) — no permanent defect ID assigned; filed as chart-knowledge appendix, status LOGGED | Per the row's own type; carried forward per its own "Carry into every future 482012f1 reading" instruction |

*Changelog: v1.5 (2026-07-16, D-1.6 Lane S-8) — §N added: closes §M's merge debt. Ruling: §A-§I+§M
(CR-1..89+CR-107) remain this file's system of record; CR-90..106 formally SUPERSEDED here, pointer
table to DOCTRINE_CAMPAIGN_EXECUTION_PLAN_v1_0.md §8 + live doctrine-wave briefs verified via
2026-07-16 grep sweep (no orphaned ID). §E CR-33..38 permanent-ID assignment table added (CR-33=Y-12,
CR-34=Y-3, CR-35=Y-4, CR-36=cross-ref only, CR-37=T-4, CR-38=READING-NOTES/no-ID) per the file's own
merge-protocol note. Frontmatter `status` field rewritten to state the CR-90+ supersession explicitly
so a fresh session cannot assume CR-90..107 are captured in this file's body.

## §O — SARVA-SIDDHI register-reconciliation (2026-07-24) — standing note on recurring drift

**Disposition this session:** CR-16, CR-61, CR-64, CR-68 flipped `OPEN` → `CLOSED —
STALE-CORRECTED` in their row's rightmost column above. All four were independently confirmed
by a W-0 truth-pass as shipped-and-live in code (PR #594/D-2 for CR-16, PR #585's V-5 emitter
for CR-61/CR-64, PR #688 for CR-68) — verified against the deployed connector for chart
482012f1 as part of this reconciliation. The register itself (this file, `cr_status.ts` in both
`platform` and `platform-mcp`, and `registry_data.ts`'s `known_gap` column) was the only thing
still calling them open; `registry_data.ts` and `cr_status.ts` are corrected in the same lane,
plus a new surgical migration (`464_sarva_siddhi_cr_reconciliation.sql`) carries the fix into
the DB-seed third copy. A narrow residual surfaced by CR-64's re-verification (nakshatra_semantic
signals carry a 16.7% orphan `constituent_facts_array` rate on chart 482012f1, per the tool's own
live self-report) was filed as a new, separately-tracked `CR-132` rather than re-opening CR-64 —
the ranking itself, which CR-64 was about, works.

**Standing note (recurring drift class):** this is the **third** confirmed instance of the same
failure mode — a fix ships in code and is verified live, but the register's status column is
never flipped, so the planner/completeness-receipt machinery keeps citing a `known_gap` for a
capability that has actually shipped. Prior instances: **CR-56** (dhana_yoga_scan house-lord
family — corrected in VIDHI-PŪRṆATĀ, 2026-07-23, migration 463) and **CR-54/CR-59** (functional-
lordship valence / NBRY detection — corrected at the D-2 gate, 2026-07-17, folded into migration
462's authoring). Three occurrences in roughly a week of active development is enough to call
this a class, not a one-off: whenever a wave *ships* a fix for a tool/route a primitive's
`known_gap` cites, closing the code-side ticket without a corresponding registry flip leaves a
false negative sitting in the completeness receipt's `dark` bucket — understating what the
instrument can actually do, the opposite failure from B.10's fabrication concern but just as
much a fidelity defect.

**Recommendation:** a lightweight periodic reconciliation pass — grep every `known_gap: 'CR-N'`
citation in `registry_data.ts` against the CR's live disposition in whichever wave/brief closed
it, on a cadence (e.g. every third session, piggybacking on the existing red-team cadence per
CLAUDE.md §M) or as a standing checklist item at any wave's session-close when that wave shipped
a fix for a tool a primitive names. This does not need new tooling — the `isCitableKnownGap` /
`isClosedCr` machinery in `cr_status.ts` already exists to make a wrong citation loud
(`registry_completeness.test.ts`'s "never CLOSED" test); the gap is procedural (nobody re-checks
an OPEN citation against ships that land after it was written), not mechanical.

*Changelog: v1.6 (2026-07-24, SARVA-SIDDHI register-reconciliation lane) — §O added: CR-16,
CR-61, CR-64, CR-68 rows flipped OPEN → CLOSED — STALE-CORRECTED (all four independently
verified shipped-and-live against chart 482012f1); standing note on this being the third
confirmed instance of the ships-but-register-never-flips drift class (after CR-56, CR-54/CR-59)
with a periodic-reconciliation recommendation. New CR-132 minted (cr_status.ts) for CR-64's
narrow orphan-constituent-refs residual — not a re-open of CR-64.*

*Changelog: v1.7 (2026-07-27, PARIŚODHANA Phase-A Prober A4, decorative-data cluster) — CR-72,
CR-73, CR-74 disposition cells annotated in place (original OPEN/OPEN — ELEVATED text preserved,
append-only) with ALREADY-FIXED findings from live probes against production chart 482012f1:
the 22-row shared-stub `dosha_label` pattern (CR-72) does not reproduce; the kemadruma and
daridra false-positive type specimens (CR-73) do not reproduce (both bespoke detectors now
honestly report non-formation); the Kāla-Sarpa label/per-varga-fact contradiction (CR-74) does
not reproduce (`kala_sarpa_reconciliation.agrees:true`, dedicated `facet=kala_sarpa` serves the
full natal+divisional map). Not re-verified: the ~18 non-bespoke dosha_label catalog entries
beyond the four named above. Rows themselves left OPEN/OPEN — ELEVATED (not flipped to CLOSED)
pending native/Conductor ratification of the ALREADY-FIXED disposition, per this probe's
fix-code-free, read-only mandate.*

*Changelog: v1.8 (2026-07-27, PARIŚODHANA Phase-A Prober A3, filter-fallthrough cluster) — CR-10
and CR-42 disposition cells annotated in place (original OPEN/OPEN — ELEVATED text preserved,
append-only) from live probes against production chart 482012f1. CR-10: ALREADY-FIXED — all
three named sub-claims (case-sensitive planet filter, `ref_remedies_get` ignoring planet,
`ganita_positions_get(planet="Venus")` → 0) do not reproduce. CR-42: MIXED — the
`ref_remedies_search`/`ref_remedies_get` member is ALREADY-FIXED (filter honored, correctly
different result sets per planet); the other three named members (`ref_dasha_systems_get`,
`ref_dignity_reference_get`, `ref_nakshatra_get`) do NOT reproduce the documented silent-fallthrough
behavior, but all three are currently LIVE-OPEN under a DIFFERENT, newly-observed failure mode —
every live call to any of the three fails outright (400 / TimeoutError) regardless of params,
while sibling tools on the same server succeed in the same window. Working-tree code inspection
confirms the documented D-1.6 S-1 fix IS present for all three; the live failure is something
else and its root cause was not established (read-only, fix-code-free probe). Separately
re-confirmed the campaign's highest-priority item, `mimamsa_lel_query`'s `query`/`offset`
parameters: 5 live calls (3 distinct `query` strings × 2 distinct `offset` values) against
chart 482012f1 all returned the byte-identical `result_hash:"sha256:f91bcc2d…"` — LIVE-OPEN,
confirmed exactly as documented across four prior campaigns. Root cause identified by code
read: `platform-mcp/src/tools/register_p1_aliases.ts`'s `mimamsa_lel_query` alias forwards
`query`/`offset` verbatim to the `lel_query` platform primitive
(`platform/src/lib/retrieval/registry/layers/L5_mimamsa/query_life_events.ts`), whose handler
never reads `args['query']` or `args['offset']` — only `chart_id`/`category`/`domain`/
`significance`/`start_date`/`end_date`/`limit` participate in the query. This is a distinct
item from CR-10/CR-42 (not one of their four named members) and is not itself annotated in a
register row in this file — see PARIŚODHANA Phase-A Prober A3's disposition report for the
Conductor. Rows left OPEN/OPEN — ELEVATED (not flipped to CLOSED) pending native/Conductor
ratification, per this probe's fix-code-free, read-only mandate.*

*Changelog: v1.7 addendum (2026-07-27, PARIŚODHANA Phase-A Prober A1, receipt-honesty cluster
CR-1/CR-2/CR-63/AS-1) — CR-1 disposition cell annotated in place (original OPEN text preserved,
append-only): ALREADY-FIXED. `timing_hooks` is no longer entirely empty (kala_activations
populated with real dated windows on wealth/career, 2026-07-27 live probes) and the receipt's
`timing_anchored` is honestly derived either way, confirmed by `enforceTimingAnchoredHonesty`
(registry_bridge.ts, commit 9bac956f) correctly forcing it false + flagged on a genuinely-empty
marriage-domain call. **CR-2 and CR-63 do NOT get the same disposition — re-confirmed LIVE-OPEN,
NOT annotated as fixed here per the append-only-on-ALREADY-FIXED-only convention, but recorded for
the Conductor:** `checklist.varga_confirmation.rows` reproduced empty on THREE live domain calls
(wealth D2, career D10, marriage D9 — including the debilitated-Venus/D9 case where
`varga_moved_verdict:true`), while the receipt still marks `varga_confirmed:"D2✓"/"D10✓"/"D9✓"`.
This directly REFUTES `PROGRAM_LEDGER_AND_ELEVATION_ROADMAP_v1_0.md`'s "CR-2 strong false-alarm
candidate" hypothesis (§85) — that hypothesis appears to have conflated the always-empty
`checklist.varga_confirmation.rows` array with the separately-added, genuinely-populated
`verdict.varga_subscores` field (WP-1.8), which carries per-graha dignity but is a different
field under a different path. Root cause: `registry_bridge.ts` has an honesty-enforcement
function for `timing_anchored` (`enforceTimingAnchoredHonesty`) but no equivalent for
`varga_confirmed` covering the "never-populated-pre-trim" case — only
`reconcileReceiptWithTrimReport`, which fires solely when the budget trimmer's own trim_report
shows the rows path cut to zero (not when the capability itself never emitted rows). See
`MARSYS_DEFECT_GAP_REGISTER_v2_0.md` R-38 (this file's permanent-ID mapping for CR-2) for the
parallel annotation on that register's "REMEDIATED-PENDING-W4 / 7/7 prod-verified" claim, which
this reprobe also contradicts. AS-1 (R-38/R-41 blind-anchor-test pair): R-41
(`ganita_yogas_get` envelope-vs-content) independently reconfirmed ALREADY-FIXED live (verdict
counts now demonstrably match served content, `catalog_only_rows_present` disclosure present);
R-38 reconfirmed LIVE-OPEN per the above — so of the two historical "misses," one has since
closed and one has not.*
