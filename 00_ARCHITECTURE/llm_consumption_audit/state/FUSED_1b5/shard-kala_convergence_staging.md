# FUSED Lane 1b+5 shard — kala_convergence_staging

channel: served-only-by-down-pipeline (ALL families)
family_count: 8
DB-truth rows: Abhisek=0, Abhinandan=0
serving tool (DEAD): temporal | serving tool (full-pipeline-only): get_temporal_windows via ask_madhav
wire probe: NONE (channel unreachable — cannot diff)

| family_key | channel | retrievability_verdict | fidelity_verdict | derivation |
|---|---|---|---|---|
| convergence_id | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| chart_id | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| window_start | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| window_end | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| convergence_score | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| constituent_factors | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| source_citation | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
| computed_at | served-only-by-down-pipeline | UNREACHABLE (data-plane empty: 0 rows all charts + no alive surgical tool) | NO-WIRE-PROBE (served-only-by-down-pipeline: no reachable surgical channel; no wire value to diff) | path-grade(exemplar=convergence_id) + member-confirmation. Serving tool: temporal (DEAD surgical, curl 500 'Retrieval tool not found in registry: temporal') + get_temporal_windows (full-pipeline via ask_madhav only, curl reject 'not in surgical whitelist'). DB-truth: kala_convergence_staging EMPTY — 0 rows across ALL charts (transient staging/scratch table, cleared post-promotion to kala_convergence). No wire probe (unreachable). Class 1/4. |
