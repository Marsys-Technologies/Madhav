# FUSED 1b+5 shard — bodha_cgm_nodes

- table_name: bodha_cgm_nodes
- family_count: 55
- channel: served-only-by-down-pipeline
- exemplar: node_id
- derivation: path-grade(exemplar=node_id) + member-confirmation
- retrievability_verdict: UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav.
- fidelity_verdict: N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire.

## Per-family rows (Condition 1: one row PER FAMILY)

| family_key | channel | retrievability_verdict | fidelity_verdict | derivation |
|---|---|---|---|---|
| node_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| chart_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| ayanamsha_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| build_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| snapshot_type | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_type | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_subject | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_label_human | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| position_in_chart_jsonb | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| strength_score | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| dignity_state | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| source_subsystem | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| degree_in | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| degree_out | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| betweenness_centrality | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| eigenvector_centrality | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| pagerank_score | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| clustering_coefficient | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| closeness_centrality | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| harmonic_centrality | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| core_number | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| articulation_point_flag | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| domain_affiliations_jsonb | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| cluster_membership_array | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| cgm_subgraph_cluster_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| msr_signal_id | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| configuration_constituents_array | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| configuration_lifecycle_state | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| hub_flag | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| hub_score | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| hub_edge_types_array | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| present_in_traditions_array | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| cross_ayanamsha_presence_score | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_embedding_vec | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| ephemeris_audit_jsonb | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| msr_salience_version_used | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| cdlm_version_used | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| graph_compute_library | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| graph_compute_library_version | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| verification_pass_status | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| citation_ref | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| citation_human | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| computed_at | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| engine_version | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_type=domain | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_type=graha | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| node_type=bhava | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=career | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=health | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=wealth | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=character | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=spirituality | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=relationship | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
| primary_domain=general | served-only-by-down-pipeline | UNREACHABLE-via-surgical: only surgical front get_cgm_subgraph is dead-wired to missing cgm_graph_walk registry entry (500). Data EXISTS (140 rows/chart, both charts). Reachable only via full-pipeline ask_madhav. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=node_id) + member-confirmation |
