# FUSED 1b+5 shard — bodha_cgm_sub_graphs

- table_name: bodha_cgm_sub_graphs
- family_count: 18
- channel: truly-UNREACHABLE
- exemplar: subgraph_id
- derivation: path-grade(exemplar=subgraph_id) + member-confirmation
- retrievability_verdict: UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row.
- fidelity_verdict: N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire.

## Per-family rows (Condition 1: one row PER FAMILY)

| family_key | channel | retrievability_verdict | fidelity_verdict | derivation |
|---|---|---|---|---|
| subgraph_id | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| chart_id | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| ayanamsha_id | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| build_id | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| subgraph_type | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| subgraph_label | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| node_ids_array | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| edge_ids_array | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| subgraph_density | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| subgraph_centroid_node_id | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| representative_path_jsonb | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| classical_archetype_match | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| graphml_export_jsonb | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| gexf_export_jsonb | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| verification_pass_status | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| citation_ref | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| citation_human | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
| computed_at | truly-UNREACHABLE | UNREACHABLE-by-nonexistence: bodha_cgm_sub_graphs is EMPTY (0 rows, all charts) — data plane never written. Only surgical front get_cgm_subgraph also dead (routes to missing cgm_graph_walk). Families are schema/column-derived, not backed by any row. | N/A — no wire value retrievable; Lane 5 diff impossible (retrieval gap precedes fidelity). Four Lane-5 failure modes untestable because nothing arrives over the wire. | path-grade(exemplar=subgraph_id) + member-confirmation |
