---
artifact: AD4_CALL_SITES_INVENTORY.md
canonical_id: AD4_CALL_SITES_INVENTORY
version: 1.0
status: CURRENT
authored_at: 2026-05-14
authored_by: AIOPS_AD_4
purpose: >
  Exhaustive inventory of every streamText / generateText call site in
  platform/src and platform/scripts as of AD.4 execution. Drives the
  migration in §3.4 and is the reference for AC.AD4.2 verification.
---

# AD.4 Call-Site Inventory

Generated by:
```bash
grep -rn "streamText\|generateText" platform/src platform/scripts \
  --include="*.ts" | grep -v "__tests__" | grep -v "node_modules"
```

---

## Section 1 — Sites requiring migration

### 1a — generateText → runAdapter (simple single-shot)

| # | File | Line | CallType | ModelOverride | Notes |
|---|---|---|---|---|---|
| 1 | `platform/src/lib/aiops/probe/runner.ts` | 56 | from `opts.callType` | from `opts.stack` | uses AbortController; replaced by `timeoutMs` |
| 2 | `platform/src/lib/checkpoints/checkpoint_4_5.ts` | 69 | `checkpoint_4_5` | `getEffectiveModel(...)` result | marsys stack |
| 3 | `platform/src/lib/checkpoints/checkpoint_5_5.ts` | 92 | `checkpoint_5_5` | `getEffectiveModel(...)` result | marsys stack |
| 4 | `platform/src/lib/checkpoints/checkpoint_8_5.ts` | 83 | `checkpoint_8_5` | `getEffectiveModel(...)` result | marsys stack |
| 5 | `platform/src/lib/conversations/title.ts` | 43 | `worker` | `TITLE_MODEL_ID` | has inline observability; adapt to ModelInteraction.usage |
| 6 | `platform/src/lib/models/health.ts` | 66 | `worker` | explicit `modelId` arg | result unused (error detection only) |
| 7 | `platform/src/lib/pipeline/planner_context_builder.ts` | 85 | `worker` | `workerModelId` arg | reads `result.text` → `interaction.finalText` |
| 8 | `platform/src/app/api/consume/suggestions/context/route.ts` | 56 | `worker` | `TITLE_MODEL_ID` | reads `completion.text` |
| 9 | `platform/src/app/api/performance/judge/route.ts` | 83 | `worker` | `gemini-2.5-flash-lite` | reads `result.text` |
| 10 | `platform/src/lib/synthesis/panel/member_runner.ts` | 120 | `synthesis` | `config.model_id` | reads `r.text` → `interaction.finalText` |
| 11 | `platform/src/lib/synthesis/panel/adjudicator.ts` | 88 | `synthesis` | `adjConfig.model_id` | reads `r.text` → `interaction.finalText` |

### 1b — generateText → runAdapter (with toolChoice)

| # | File | Line | Entry point | Notes |
|---|---|---|---|---|
| 12 | `platform/src/lib/pipeline/pipeline_planner.ts` | 226 | `runAdapter` | `toolChoice: 'required'`, `disableSdkRetry: true` (planner owns retries); reads `result.toolCalls[0]` → `interaction.intermediate` tool_call event |

### 1c — streamText → streamAdapterRaw (SSE passthrough, rawOnFinish)

| # | File | Line | Entry point | Notes |
|---|---|---|---|---|
| 13 | `platform/src/lib/synthesis/single_model_strategy.ts` | 412 | `streamAdapterRaw` | multi-step, smoothStream, `toolChoice: 'none'`; tools have no execute handler needed; `rawOnFinish` carries full onFinish logic; returns `result` to orchestrator |
| 14 | `platform/src/lib/synthesis/panel_strategy.ts` | 120 | `streamAdapterRaw` | passthrough: system + user message; `rawOnFinish` carries telemetry+audit logic |

### 1d — Dead import removal

| # | File | Notes |
|---|---|---|
| 15 | `platform/src/app/api/chat/consume/route.ts` | Imports `streamText` (line 2) but never calls it. Remove import. |

### 1e — streamText → build_bridge (complex raw tools + convertToModelMessages)

| # | File | Line | Notes |
|---|---|---|---|
| 16 | `platform/src/app/api/chat/build/route.ts` | 91 | Uses `buildTools` (AI SDK tools with execute handlers) + `convertToModelMessages`; incompatible with `QueryRequest.tools: ToolDefinition[]`. Bridge function `streamBuildRaw` lives in `adapters/build_bridge.ts` (excluded from AC.AD4.2 grep). |

---

## Section 2 — Sites EXCLUDED from migration (not in may_touch)

| File | Reason |
|---|---|
| `platform/scripts/retrieval/test_classify.ts:47` | Out of `may_touch` (`scripts/retrieval/**`). Uses `createAnthropic` directly (diagnostic script). Excluded from AC.AD4.2 scope. |
| `platform/scripts/cutover/stage1_smoke.ts:129,157,204` | `streamText` is a **field name** in a struct (`{ streamText: string }`), NOT an AI SDK call. No import from `'ai'`. No migration needed. |

---

## Section 3 — sites already in adapters/ (excluded from AC.AD4.2)

These are the adapter implementations themselves — they call `streamText` internally and are the destination of the migration, not the source:

- `platform/src/lib/adapters/providers/adapter_anthropic.ts`
- `platform/src/lib/adapters/providers/adapter_deepseek.ts`
- `platform/src/lib/adapters/providers/adapter_gemini.ts`
- `platform/src/lib/adapters/providers/adapter_nim.ts`
- `platform/src/lib/adapters/providers/adapter_openai.ts`
- `platform/src/lib/adapters/raw.ts`
- `platform/src/lib/adapters/legacy_runAdapter.ts` ← NEW in AD.4

---

## Section 4 — Comment-only occurrences (no migration needed)

These files contain the string `streamText` or `generateText` only in comments or type-docstrings. AC.AD4.2 uses import-pattern grep (not raw string match) so these pass automatically.

- `platform/src/lib/config/feature_flags.ts:40` — comment
- `platform/src/lib/llm/observability/observe_ai_sdk.ts:1-2` — comment (not in may_touch)
- `platform/src/lib/models/nvidia.ts:119,149` — comment (not in may_touch)
- `platform/src/lib/models/registry.ts:36` — comment (not in may_touch)
- `platform/src/lib/models/resolver.ts:14,18,22` — comment (updated in §3.5, in may_touch)
- `platform/src/lib/synthesis/token_caps.ts:34` — comment (in may_touch; update)
- `platform/src/lib/synthesis/types.ts:133,136,151` — comment (in may_touch; update)
- `platform/src/lib/adapters/types.ts:44,58,66,76` — inside adapters/ (excluded)
- `platform/src/lib/trace/types.ts:101` — comment (not in may_touch)

---

## Section 5 — AC.AD4.2 verification command

```bash
# Check for remaining streamText/generateText IMPORTS (not comments) outside adapters/legacy
grep -rn "^import.*\(streamText\|generateText\)" \
  platform/src platform/scripts \
  --include="*.ts" \
  | grep -v "__tests__\|/adapters/\|legacy_run"
```

Expected: 0 hits after migration.

---

*End of AD4_CALL_SITES_INVENTORY.md v1.0*
