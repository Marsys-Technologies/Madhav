---
lane: X-4
wave: PG-2 diagnostic
status: CLOSED
audit_target: "PG-1's 14 unverifiable findings + 3 unaudited assumptions — the 8 items named in BIND_PG-2.md for this lane"
findings_file: 00_ARCHITECTURE/pg2_diagnostic/deliverables/pg2_findings_X-4.jsonl
findings_count: 8
worktree: /Users/Dev/Vibe-Coding/Apps/Madhav-pg2-X-4
branch: pg2/X-4
---

# PG2 Lane X-4 — close-out summary

All 8 charged items were closed with real probes (live DB queries against the `amjis` Postgres instance, source-code reads, and one live CLI invocation of `chart_agnostic_gate.ts`). No item was left silently unaddressed; every finding carries non-empty evidence and cites the specific PG-1 finding(s) it resolves. Two items surfaced their own PG-1-style dispatch-brief mislabeling (A-13→A-14 for the memoization item, same failure class PG-1 caught with A-26→A-08) — both were corrected in-flight rather than audited under the wrong label.

## Item-by-item status

1. **PG1-R1-0010 / A-10 (provenance stamp) — CLOSED, partially_resolved.** A per-turn provenance-shaped stamp DOES exist (`{model, queryId, planning_model_id, planning_latency_ms, disclosure_tier, query_class, stack, style, pipeline, conversationId}`, built in `run_adapter_dispatch.ts:472` and written via `conversation_writer.ts`'s `writeConversationMessages`), but it lives in the same `conversation_messages.metadata_json` column as everything else, written via `ON CONFLICT (id) DO UPDATE` — i.e. mutable/UPSERT-able, not a separate immutable ledger table. A-10 as restructured by D-16 explicitly wants "copied immutably into ledger rows — not session state"; the schema as built does not enforce that distinction. `conversation_messages` confirmed live at 7 columns, no dedicated provenance column, no separate ledger table anywhere in the schema.

2. **PG1-R2-0005 / A-2 (chart_agnostic_gate, omit chart_id) — CLOSED, resolved.** Could not reach a live raw-HTTP endpoint from this worktree (no dev server running, no `PLATFORM_URL`/DB env configured locally, no reachable staging URL) — this is an honest, documented gap, not a skipped probe. In its place, did a decisive source read of `/api/retrieval/capability` (`route.ts:190-206`): chart_id resolves only from `args.chart_id` or the `X-MCP-Chart-Id` header, defaults to `null`, and the route returns `400 CHART_REQUIRED` if both are absent — no default/native UUID substitution exists anywhere in the resolution chain. Corroborated by a second, independent per-handler defense (`register_d6_synergy.ts`, `router_registration.ts`, `dprofiles_registration.ts` all re-check and reject) and by the FROZEN `chart_agnostic_gate.ts` CI gate (Rule 1 + Rule 4 forbid a missing/defaulted `chart_id`), which this lane ran live (`npx tsx chart_agnostic_gate.ts` → PASS, though 0 capabilities checked outside Next's bootstrap — a noted residual, not a fail signal).

3. **PG1-S1-0003 (citation persisted shape) — CLOSED, resolved.** Settled: "different shape," not "no citations." `platform/src/lib/citations/citation_data_part.ts` defines the actual shape — a `{type:'citation', index, signal_id, layer, snippet}` data-part extracted from assistant text via `SIG\.MSR\.\d{3}` regex matching, embedded inside `parts_json`'s array, never a separate structured table. PG-1's ILIKE-for-'signal_id' scan found 0 matches; this lane re-ran the same query and also found 0 — but discovered why: `conversation_messages` has 0 rows and `conversations` has 1 row in this DB instance. The 0-match result is a DB-traffic artifact, not evidence the mechanism is unused in production.

4. **PG1-S1-0004 / PG1-S1-0005 / A-30 / A-32 (F-25c stub confirmation) — CLOSED, resolved for A-32/F-25c; A-30 remains its own follow-up.** `platform/src/app/api/conversations/[id]/feedback/route.ts` confirmed live as a genuine stub — its own header comment: "message_feedback table dropped in WS-0. Endpoint returns empty/ok stubs." POST discards the rating it receives; GET always returns `{feedback: []}`. Three DB tables superficially match the grep terms (`mcp_disagreements`, `school_disagreements`, `mimamsa_resonance_feedback`) but none is wired to this endpoint and none is the intended conversational correction-capture mechanism — they're a governance-register mirror, a classical-school-disagreement data asset, and a distinct client-learning-signal capture, respectively. F-25c is confirmed genuinely absent, not merely unqueried. A-30 (calibration collect-only gating) was correctly out of scope for both PG-1's S-1 lane and this item's evidence — flagged as its own residual, needs a lane scoped to `mimamsa_calibration`/`mimamsa_calibration_snapshot`.

5. **PG1-D1-0001 / A-08 (mislabeled A-26) — CLOSED, resolved (confirmation only).** Already fully closed within PG-1's own D-1 lane: PG1-D1-0002 (class: confirmed) verified the exact same 7-column `conversation_messages` schema this lane independently re-queried and got byte-identical results for. No new work needed — this is a citation-and-confirm, not a re-audit.

6. **A-13 (uncovered by any PG-1 lane) — CLOSED, refuted, with a mislabel correction.** The dispatch brief's "A-13" (memoization/content-visibility) does not match the architecture doc — the doc's real A-13 is "three-region turn layout" (unrelated). The actual ruling ("No virtualization. Frozen-block memoization + content-visibility instead.") is **A-14** (§12.7, line 199) — and PG-1 itself already flagged this exact ruling as unaudited under the same A-13 mislabel in `PG1-Z1-A0001`. This lane closes it under the corrected label: confirmed NOT built, and worse — the current code does the *opposite* of A-14's mandate. `VirtualizedMessageList.tsx` is confirmed live (transitively imported by `AdaptiveMessageList.tsx`, matching PG1-C1-0006's earlier finding), while `content-visibility` and `React.memo`/frozen-block memoization both return zero hits anywhere in `platform/src`.

7. **A-31 (uncovered by any PG-1 lane) — CLOSED, refuted.** PG-1's own `PG1-Z1-A0010` already flagged A-31 ("Compliance decay is designed for: batch resolution, LEL-drafted outcomes, non-shameful lapsing...") as NOT AUDITED THIS WAVE. This lane closes it: no batch/decay/lapse/expire logic exists anywhere in `mimamsa_outcome.ts` or `ppl_writer.ts`. `mimamsa_predictions.lifecycle_status` has only 5 defined values (`open|matched|confirmed|falsified|withdrawn` — `prospective_ledger.ts:81`), no 'lapsed'/'expired' state, and the only code that transitions it does so per-row on manual event match, never as a scheduled sweep over predictions whose `observation_window` has closed. No prediction-related cron/scheduled job exists in the repo.

8. **PG1-R1-0005/0006/0008/0009 (target-architecture unverifiables) — CLOSED, reclassified confirmed-not-built.** Re-ran all four of PG-1's original greps live with identical zero-hit results: `marsys_drill` (only in architecture markdown), `mutation` field on `CapabilityDescriptor` (absent), `PlanReceipt` type (absent anywhere), `*model_plane*` file / `OpenRouter` string (both absent). Reclassified from "unverifiable" to a closed "confirmed not-built" verdict per the dispatch brief's instruction, since these are simple existence checks where a clean re-confirmed zero-hit result is itself decisive. PG1-R1-0008's narrower low-confidence residual (whether an equivalent-but-differently-named `PlanReceipt`-shaped object already exists inside `vidhi/` or the 3-4 planner files) was NOT independently chased by this lane — left explicitly open for a follow-up with time budget to open those files.

## Headline findings

- **The A-26→A-08 mislabel pattern recurs.** This wave found a second instance (A-13→A-14 for the memoization/virtualization ruling) of PG-1's exact failure class: a dispatch brief or intermediate finding citing the wrong assumption ID from `PARIPRASHNA_TARGET_ARCHITECTURE_v0_1.md`. Worth a structural fix (e.g. a lint check cross-referencing assumption IDs cited in dispatch docs against the doc's own table) rather than catching this ad hoc every wave.
- **A-14 is not just unbuilt, it's inverted.** The target explicitly says "No virtualization... instead" and the codebase runs the virtualizer live while lacking both prescribed replacements. This is a stronger finding than a simple "not yet built" — it's an active architectural contradiction for whoever picks up the block-protocol rendering work.
- **This DB instance carries almost no real conversational history** (`conversations`=1, `conversation_messages`=0 rows). Several PG-1 "0 matches" results (S1-0003 in particular) are DB-emptiness artifacts, not shape-absence evidence. Future citation/message-shape audits should target whichever instance actually carries production chat traffic.
- **F-25c (feedback stub) is now airtight-confirmed**, not just self-reported by the architecture doc — the route's own code comment documents its own regression ("message_feedback table dropped in WS-0").
- **The live raw-HTTP probe for A-2 could not be executed** in this diagnostic worktree (no server, no env, no reachable deployed URL) — closed instead via a decisive two-layer code read (route-level + per-handler validation) plus a live CLI run of the FROZEN CI gate. This is an honest partial-methodology substitution, documented as a residual in finding PG2-X4-0002, not a silently skipped probe.

## Worktree discipline

All work was performed from `/Users/Dev/Vibe-Coding/Apps/Madhav-pg2-X-4` on branch `pg2/X-4`. No `cd` into `/Users/Dev/Vibe-Coding/Apps/Madhav` or any other `Madhav-pg2-*` directory occurred at any point in this session. DB access was via the `mcp__postgres__query` tool (external connection, not a local server in this worktree); the one live code execution (`chart_agnostic_gate.ts`) was run via `npx tsx` from inside this worktree's `platform/` directory.
