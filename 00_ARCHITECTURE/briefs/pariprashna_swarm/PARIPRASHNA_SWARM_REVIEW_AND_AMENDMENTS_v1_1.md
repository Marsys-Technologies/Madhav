---
artifact: PARIPRASHNA_SWARM_REVIEW_AND_AMENDMENTS_v1_1
canonical_id: PARIPRASHNA_SWARM_PLAN_AMENDMENTS
version: 1.1
status: CURRENT-FOR-EXECUTION — Fable-5 adversarial review of the v1.0 swarm plan;
  its amendments BIND the execution. Where this file and the v1.0 plan disagree,
  this file wins.
produced_during: PARIPRASHNA-SWARM-REVIEW (Cowork, Fable 5, 2026-08-19)
date: 2026-08-19
authoritative_side: claude
role: >
  (1) Review findings RF-1..RF-12 — gaps and challenged assumptions that would have
  become roadblocks or velocity sinks, each with its binding fix. (2) The Delegated
  Decision register DD-1..DD-10 — every human gate closed by native delegation
  (this conversation, 2026-08-19), so the build runs with zero human gates.
  (3) The merge/deploy train protocol. (4) The swarm scaling policy. (5) The
  live tracker specification. (6) Amended phase structure (P0 IGNITION added).
relates_to:
  - PARIPRASHNA_PHASED_SWARM_IMPLEMENTATION_PLAN_v1_0.md (amended by this file)
  - PARIPRASHNA_IMPLEMENTATION_ROADMAP_v1_0.md (lane inventory)
  - 00_ARCHITECTURE/PARIPRASHNA_ARCHITECTURE_v1_0.md (the PPR requirements)
changelog:
  - "1.1 (2026-08-19): full adversarial review; P0 IGNITION added; all human gates
    closed via DD register under explicit native delegation."
  - "1.1 (2026-08-21, PARIPRASHNA-P2-CLOSE, item 8, in place — no version bump,
    living append-only register): DD-11 -> ADVISORY; DD-12 -> CLOSED (evidence:
    PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md); DD-20 -> CLOSED (fix shipped/deployed/
    live-verified); new DD-21 (observed-delivery-artifact rule, binding P3
    onward), DD-22 (item 4 carried, approach (c) approved), DD-23 (remedial
    guidance under-governed, OWNED-UNDATED)."
  - "1.1 (2026-08-21, PARIPRASHNA-P2-CLOSE follow-up, in place — no version
    bump): DD-13 RE-DATED to 2026-09-03, hard date, native ruling — branch
    (c) of its own binary condition exercised openly at the P2-close
    boundary, not a silent re-date."
  - "1.1 (2026-08-21, PARIPRASHNA-P3-PREFLIGHT-2026-08-21, in place — no
    version bump, living append-only register): new DD-24 (parity proves
    sameness not correctness — P3-D's receipt-hash parity assertion bounded
    to an enumerated web-door-gap baseline, BINDING ON P3); DD-22 amended
    in place with the DD-22-before-P3-B sequencing ruling and rationale.
    Companion docs-only edits (not in this file): DD-21 observed-delivery
    clause folded into P3's gate criteria and P3-D's parity-hashing subject
    named, both in
    PARIPRASHNA_PHASED_SWARM_IMPLEMENTATION_PLAN_v1_0.md §Phase III; P3-B/
    P3-D lane annotations in tracker/PLAN.yaml."
---

# Swarm Plan — Fable-5 Review & Binding Amendments

## §1 — Review findings (assumptions challenged, gaps closed)

### RF-1 · CRITICAL · The file-disjointness assumption is FALSE at the plan's center
v1.0 assumed the collision map would yield mostly independent lanes. It will not:
**`app/api/pariprashna/route.ts` is 1,179 lines and is touched by at least six
lanes** across P1/P2 (safety gate, injection scan, block typing, S-3 wiring,
honest controls, receipt assembly). The collision map would serialize the heart
of the build behind one file — the single largest hidden velocity sink in v1.0.
**FIX (binding): P0 IGNITION includes a PORTS REFACTOR** — decompose route.ts
into the §6.2 typed-port stage modules (`safety_gate.ts` · `plan_stage.ts` ·
`evidence_stage.ts` · `synthesis_stage.ts` · `validation_stage.ts` ·
`reading_parts.ts` · `receipt_stage.ts` · `persistence_stage.ts`, thin route
shell composing them), proven behavior-identical by a golden-stream semantic-
equality harness over the 12-fixture corpus + captured real streams BEFORE any
phase lane opens. After P0, each later lane owns its own stage file and the
six-way collision dissolves. This is the single biggest velocity unlock
available and it must come first.

### RF-2 · HIGH · Migration-number collisions between parallel lanes
Multiple lanes author migrations concurrently → sequence-number collisions and
nondeterministic apply order. **FIX: conductor-owned migration allocator** —
numbers reserved centrally at lane-brief time; **expand/contract discipline**:
mid-phase migrations are additive-only (new tables/columns/roles), destructive
changes cluster at phase close; the schema train always merges and deploys
before the app train.

### RF-3 · HIGH · Micro-collisions on shared files beyond route.ts
Barrels/`index.ts`, shared `types.ts`, `feature_flags.ts`, `package.json`/
lockfile, and `protocol/events.ts` will be touched by many lanes. **FIX:
ownership rules** — protocol/event changes are ONE designated lane per phase
(additions batched, event schema versioned once); barrel and lockfile edits are
integrator-only; lanes add new files in preference to editing shared ones;
`feature_flags.ts` additions go through a conductor-reserved flag registry.

### RF-4 · HIGH · The real merge bottleneck is CI wall-time, not agent count
Ten lanes × full CI serially consumes the day. **FIX:** the train protocol
(§3): batched speculative merges with ONE full-CI run per batch and bisect-on-
red; lane-level CI runs impacted suites only; the full battery runs at train
and gate level.

### RF-5 · MED · Test-database contention between parallel builders
Concurrent DB-integration suites against one dev DB corrupt each other's state.
**FIX:** per-lane database clones from a template DB (`CREATE DATABASE lane_x
TEMPLATE …`), allocated/dropped by the conductor; DB-heavy suites tagged and
also run once at train level against the shared instance.

### RF-6 · MED · Static 1:1 role allocation starves the pool
Verification (live probes) is slower than building; late-phase, builders idle
while verifiers queue. **FIX: roles are hats, not headcounts** — one
oversubscribed work-stealing queue; any free agent slot takes the next task of
any role, priority order: unblock-merge > verify > build > refute > scout-next-
phase. This is also the direct answer to "agent availability must never
throttle velocity": **the queue, not the pool, is the constraint** (§4).

### RF-7 · MED · Main is moving underneath the phases
The log shows active concurrent work landing on main (EKAVĀKYATĀ, night-tick
BOARD refreshes, engine fixes). Long-lived integration branches will drift.
**FIX:** integration branch rebases onto origin/main at every train close; a
**conflict-forecast task** (cheap, recurring) diffs incoming main deltas
against the collision map and re-orders the queue when a collision approaches;
phase lease registered in CAMPAIGN_COORDINATION.

### RF-8 · MED · A night-time halt wastes the run
v1.0 halts cleanly but immediately. **FIX: auto-triage tier** — before any
halt, a diagnostician agent gets ONE bounded remediation attempt (flaky-suite
rerun · bisect-and-park the offending lane · revert the last train batch).
Halt only after that fails; halts always end the phase with pinned rollback +
resume state + report. Never hang, never retry-loop.

### RF-9 · LOW · TDD-everything taxes trivial lanes
**FIX: three verification tiers** — integrity lanes: TDD + adversaries;
behavior lanes: tests-with-code + verifier; content/fixture lanes (lexicon,
reader text, docs): verifier review only.

### RF-10 · MED · The gate battery is a barrier that idles the whole pool
**FIX: prefetch** — during FREEZE-1 and the gate run, the pool executes the
NEXT phase's scouting and lane-brief drafting (read-only, zero risk). No idle
slots anywhere in the run.

### RF-11 · Assumption "deploy per wave is safe" — refined, not accepted
Continuous flag-OFF deploys stand. **Added canary discipline:** every deploy
creates a tagged Cloud Run revision at 0% traffic → smoke battery against the
tagged URL (demonstrated-can-fail) → traffic shift to 100% → post-shift smoke
→ any red = automatic traffic shift back + auto-triage. Rollback is a traffic
command, not a build.

### RF-12 · Assumption "the operator packet must be human" — DISPROVEN
PG-1 ran live `gcloud sql instances describe` from a session: the machine has
authenticated gcloud. PITR enable, scratch-instance creation, restore drill,
and credential rotation are all automatable via gcloud/psql under existing
IAM. The one genuinely non-automatable item (a secret nobody possesses) is
closed by decision DD-2. Residual risk is IAM refusal on a specific call —
handled per DD-3 (park that lane, never block the phase).

## §2 — The Delegated Decision register (all human gates CLOSED)

Authority: explicit native delegation, this conversation, 2026-08-19 — "take
the decisions on my behalf and close all human gates." Each DD is a native
decision made through that delegation, recorded here as the ADR; the swarm
treats them as ruled. DD-1 and DD-3 supersede prior sequencing rulings and are
flagged for the Decision Register at next docs close.

| # | Gate closed | Decision |
|---|---|---|
| **DD-1** | **AC-15 / W-4 (the week-of-use verdict)** | Converted from a BLOCKING gate to an ASYNC signal. P3→P4 proceeds when the automated feel-proxy battery passes — the design plan's own machine metrics at their stated targets (M1 CLS=0 above the tail · M2 caret orphaning 0 · M3 <300ms first signal · M5 internal-ID leakage 0 · M6 chip transmutation 0 · M8 mobile 100% · M9 axe clean) plus the edge-state fixture suite. **W-4's soul is preserved: the swarm never records an AC-15 PASS** — it records "waived-as-blocking by native directive 2026-08-19; native verdict welcome asynchronously." A negative verdict at any time spawns a remediation wave against the then-current phase. |
| **DD-2** | ANTHROPIC_API_KEY provisioning | **Delist the anthropic stack** from pickers and config (it has failed-instantly-but-masked since 2026-08-01 anyway; production default is Gemini). Re-enlisting later = one config change + the qualification suite. No secret required from anyone. |
| **DD-3** | Console/infra operator packet (PITR enable, scratch instance, restore drill, `amjis_app` rotation) | **Automated via gcloud/psql under existing IAM.** If IAM refuses a specific call, THAT LANE parks with the exact failing command in the report and the phase CONTINUES; the gate records the park honestly (downgraded from phase-blocking to lane-parking for the IAM-refused subset only). |
| **DD-4** | The irreversible retirement commit + flag deletion (P4) | **Pre-authorized**, contingent on: verifier line-by-line deletion-diff PASS against the refreshed census (W-2 intact) + seven consecutive green smokes already recorded (W-1 intact) + rollback pin committed before the retirement commit. |
| **DD-5** | Build-run spend | Per-phase build budgets: P0 $40 · P1 $150 · P2 $200 · P3 $80 · P4 $150 · P5 $80 (API-cost equivalent). Ceiling hit = clean halt at the next lane boundary with resume state. (Runtime app caps stay $2/turn · $40/day per NCD-8 — unrelated to build spend.) |
| **DD-6** | Taste calls (the Seal, typography, arrival-line voice, empty-state) | **Judge-panel majority decides**; the native may dispute any post-hoc as a remediation item. No mid-phase design review. |
| **DD-7** | The seven-smoke hold (P3) | Autonomous wall-clock wait; the conductor sleeps between smoke runs; any red resets the counter (W-1 intact); green×7 declared by the CI history, no human. |
| **DD-8** | Q-2 reading grading + docs seal (P4) | Gate-runner grades the three readings against the §J rubric, honestly labeled MACHINE-GRADED; the design-plan RATIFIED-AS-BUILT flip proceeds on it. |
| **DD-9** | G0 PR merge | Pre-authorized: kickoff step 0 opens and merges the `pariprashna/g0-close` PR (doc-only diff) if not already merged, before anything else. |
| **DD-10** | CAMPAIGN_COORDINATION dirty-file conflict | Never touch another workstream's dirty file: register the phase lease the moment the file is clean; until then log the deferral each phase (the G0 precedent). |
| **DD-11** | Dead observatory during a lane transition (added 2026-08-20, PARIPRASHNA-CLOSEOUT session, item 2; status corrected 2026-08-20, same session, item 2 follow-up; deadline consolidated into DD-12, 2026-08-20, PARIPRASHNA-CLOSEOUT-FINAL session, item 1; **downgraded to ADVISORY 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 8, per DD-12's own binary-outcome rule branch (b)**) | **Status: ADVISORY.** The `lane` wrapper DD-12 proposed was not built before P2 closed — branch (b) of DD-12's own binary outcome. Per-lane discipline in this campaign remains agent-remembered, not mechanically enforced, and stays that way until a future phase revisits it. The historical record below (why it was IN FORCE — NOT YET WIRED through P1/P2) stands unedited. The rule: the conductor calls `tracker-health-check` (`00_ARCHITECTURE/briefs/pariprashna_swarm/tracker/tracker-health-check`) at every lane transition; a non-zero exit is a HALT condition, same shape as every other DD-5-style halt (halt at the next lane boundary, pin rollback, write resume state, restore the observatory, report, then resume). **Nothing calls it mechanically yet** — a P2 lane-transition entry on `origin/campaign-coordination` (2026-08-20, the presentation-truth wave) records one manual, read-only invocation (`OBSERVATORY HEALTHY`) and a commitment to keep invoking it by hand for the rest of P2, which is real signal but still agent-remembered, not structural. This is the tier that actually closes the loop the tracker-v2 observatory (§7) built: T1–T4 keep the *observer* alive, but every one of them is itself something that can be down; the subject (the conductor) checking the observer is the only watcher guaranteed to be running at the exact moment it matters. The 2026-08-19 23m37s blind window (tracker-v2 README, "The 2026-08-19 incident") would have been caught in seconds by this, had it existed then. **Deadline and resolution: see DD-12** — the structural finding DD-11's own investigation surfaced, and the single P2-close decision point that now governs both rows; DD-11 no longer carries its own separate deadline clause. |
| **DD-12** | Structural finding: per-lane discipline in this campaign is agent-remembered, not mechanized (added 2026-08-20, PARIPRASHNA-CLOSEOUT-FINAL session, item 1 — arising directly from the DD-11 investigation; **CLOSED 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 8**) | **Status: CLOSED, branch (b).** The `lane` wrapper was not built before P2 closed. Per this entry's own binary-outcome rule, DD-11 is downgraded to ADVISORY (see DD-11's own row above) with the written acknowledgement that per-lane discipline in this campaign is agent-remembered, not mechanically enforced, and stays that way until a future phase revisits it. **Evidence for this close: `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md`** — the P2 pre-close audit whose central finding, in the native's own words, was "seven lanes merged, flagged live, and reported done while delivering nothing observable to a reader," discovered only by a dedicated post-hoc audit, not by any per-lane mechanism this campaign had running. The new binding rule this finding motivates going forward is filed separately as **DD-21** (below), effective P3 onward. **Finding (original text, unedited):** lane transitions in this campaign are **prose-only**. There is no deterministic per-lane hook anywhere in the swarm plan or its tooling — not for tracker event emission, not for `tracker-health-check` (DD-11), not for `may_touch` territory/lease enforcement, not for budget accounting against the DD-5 per-phase ceilings. Every one of those depends on an agent remembering to perform it at each transition, not on a mechanism that runs regardless of whether anyone remembers. **This is the narrated-versus-derived problem one layer up**: the tracker-v2 observatory (§7) was rebuilt specifically to DERIVE its state from git/`gh`/filesystem ground truth rather than trust agent self-report (CLAUDE.md §N.8) — but the *emission points feeding that observatory* are still sentences in a kickoff prompt, not code. The observatory's completeness is therefore bounded by agent diligence at exactly the boundary where the 2026-08-19 23m37s blind window occurred: a bare `launchctl bootout` run outside any tracked mechanism. **Proposed shape:** a single `lane(...)` wrapper every transition is invoked through — called at a lane's start and at its close — which performs tracker event emission, a `tracker-health-check` invocation (halt on non-zero, per DD-11), a `may_touch` scope assertion, and budget-ceiling recording, all as **side effects of being called**, not as separate steps an agent has to remember on top of doing the actual work. Once this hook exists, **DD-11 wires itself**: halt-on-non-zero becomes a property of the wrapper, not a discipline layered on top of it. **Deadline: P2 CLOSE** — the same boundary DD-11 already carried; this entry is now the one decision point for both rows. **Binary outcome, both branches acceptable:** (a) the `lane` wrapper is built and adopted before P2 closes → DD-11's status moves to **WIRED**; or (b) it is not → DD-11 is downgraded to **ADVISORY**, with a written acknowledgement (this entry, updated in place) that per-lane discipline in this campaign is agent-remembered, not mechanically enforced, and stays that way until a future phase revisits it. **A third pass that leaves DD-11 at NOT YET WIRED with a freshly pushed-out deadline is explicitly not an acceptable outcome at P2 close** — the choice must be made, not deferred again. |
| **DD-13** | Fast-follow: two disclosed residuals in the mortality phrasing-scan gate, accepted-as-scoped at go-live (added 2026-08-20, PARIPRASHNA-GOLIVE-EXECUTE session, item 1 — native ruling on 2e: `PARIPRASHNA_SAFETY_GATE_ENABLED` → ON now, both residuals accepted as scoped, with this ticket as the mandatory fast-follow; deadline ruled 2026-08-20, PARIPRASHNA-GOLIVE-CLOSEOUT session) | **Status: CLOSED 2026-08-22 (native ruling, both residuals resolved — see the closure paragraph at the end of this entry).** Was: OWNED — DEADLINE SET, due **2026-09-03, or the next phase-close boundary, whichever comes first** (native ruling, PARIPRASHNA-GOLIVE-CLOSEOUT session, 2026-08-20). **Binary condition at that boundary** (the DD-11/DD-12 pattern — a choice must be made, not deferred again): at the boundary, either (a) both residuals below are closed, or (b) the streaming-buffer-vs-first-paint-citation tension is explicitly resolved one way or the other with a stated reason (buffer wins and the P2 guarantee is knowingly narrowed, or the guarantee wins and the un-send asymmetry stays open on stated grounds), or (c) DD-13 is re-ruled openly by the native — **never silently re-dated a second time.** Both residuals are stated in `platform/src/lib/pariprashna/safety/phrasing_scan.ts`'s own module header, not newly discovered: **(a) the third-sentence pairing gap** — the mortality-term/date cross-sentence scan (`CROSS_SENTENCE_WINDOW = 2`) only pairs a term and a date within two sentences of each other; a pair separated by an intervening third sentence is, in the module's own words, "a KNOWN residual, not a covered case," and is not detected. **Scope of fix:** widen `CROSS_SENTENCE_WINDOW` past two sentences, or add an independent same-sentence-count scan that doesn't share the two-sentence assumption — whichever closes the gap without regressing the existing round-3 (M-3/H-4) boundary-sweep tests (`phrasing_scan_round3.test.ts`), which fail if either constant shrinks. **(b) the streaming un-send asymmetry** — nothing can retract a token already streamed to the reader; if a mortality term is emitted before its paired date arrives, the gate can withhold the date but not the already-sent term. The module's own framing: this is a real reduction ("a reader may see a mortality term whose date is then withheld") not an elimination ("a reader may see a complete mortality claim"), and is written down as such rather than rounded up. **Scope of fix:** evaluate a short pre-display buffer for mortality-adjacent spans specifically (hold suspect prose back one span-width before emitting it, mirroring how `StreamingMortalityScanner` already holds prose back to the last sentence boundary for HS-1), weighed explicitly against the P2 first-paint-citation guarantee (`PARIPRASHNA_FIRST_PAINT_CITATIONS_ENABLED`) — **do not silently regress that guarantee to close this gap**; if the two are in real tension, that tension is itself part of what this ticket needs to report back, not something to resolve unilaterally. **Cross-reference added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session:** a separate, unrelated defect in the sibling file `classifier.ts` (input query classification, not this file's output phrasing redaction) was found and closed the same day — see **DD-14**. That fix does not touch `phrasing_scan.ts`, does not close either residual (a) or (b) above, and does not count against this entry's deadline; DD-13 remains open at its full original scope. **RE-DATED 2026-08-21, PARIPRASHNA-P2-CLOSE session, branch (c) of this entry's own binary condition — DD-13 re-ruled openly by the native at the P2-close boundary, not silently re-dated:** the P2-close boundary was reached with both residuals unchanged from this entry's original scope; re-dating is recorded because both remain narrowly scoped, both were disclosed by `phrasing_scan.ts`'s own module header rather than discovered late, and neither worsened during P2. **New due date: 2026-09-03 — hard, no further boundary clause attached.** **Residual (a) CLOSED 2026-08-22, PARIPRASHNA-P3-PREFLIGHT-PART-F session:** `EXTENDED_DATE_SHAPE_WINDOW = CROSS_SENTENCE_WINDOW + 1` added as a separate, narrower pass (not a widening of `CROSS_SENTENCE_WINDOW` itself — see that constant's own note), scoped to `mortality_term_x_date_shape` only; `duration_to_end` at the same separation remains an open, disclosed residual by design (over-redaction control, stated in the constant's own doc comment). PR #1465, merged via merge queue, deployed (`main@94e20a8c7`), 672/672 safety-suite tests green. Live-verified per DD-21 with an honest scope caveat: a benign probe turn confirmed the deployed code path executes on real production synthesis (`enforced: true`, no false positive); a second probe deliberately asking about maraka-dasha longevity was correctly intercepted upstream by the query classifier (`hs1_date_of_death`+`hs4_mortality_window`, `seal_pending_signoff`) before ever reaching synthesis, which is real evidence the two controls compose correctly but is not itself a live redaction event of the exact adversarial pattern — full account in `campaign-coordination`, entries `ae102adf3`/`ab580d8b0`. **Residual (b) INVESTIGATED, NOT RESOLVED, 2026-08-22, same session — reported per this entry's own instruction not to resolve the tension unilaterally:** a genuine, real tension is confirmed to exist, not merely hypothesized. `phrasing_scan.ts`'s `StreamingMortalityScanner` sits downstream of `CitationStreamRewriter` in the SAME per-delta pipeline (`synthesis_stage.ts`, `mortalityScanner.push(rewritten)` / `push(cleanDelta)`) — the mortality gate is the last thing between citation-resolved prose and the wire, and it already forces sentence-wise (not token-wise) delivery once armed, a pre-existing, disclosed latency trade. A "pre-display buffer for mortality-adjacent spans" as this ticket's own scope describes would be a NEW, categorically different mechanism: instead of holding back a sentence only once a match is already confirmed, it would hold back ANY sentence merely CONTAINING a `MORTALITY_OUTPUT_TERMS` match — before knowing whether a later sentence pairs a date with it — for up to `CROSS_SENTENCE_WINDOW`/`EXTENDED_DATE_SHAPE_WINDOW` sentences of forward lookahead. That fully closes the term-leak residual within the existing window widths, at a real, now-measured cost: `MORTALITY_OUTPUT_TERMS` includes common, legitimate classical-Jyotish vocabulary (`maraka`, `ayurdaya`, `longevity`, `life ?span`, `life expectancy`, among others), and a direct production query (`message_parts` × `conversation_messages`, all persisted assistant text) found 2 of 36 real assistant messages (~5.6%; SMALL SAMPLE, early-stage instrument, stated as such rather than rounded up) contain at least one such term — and both are ordinary benign Saturn/Rahu dasha-and-career readings with no adversarial content, not near-misses. A pre-display buffer would delay part of that ~5.6% (and any future turn matching the same vocabulary) by an unpredictable multi-sentence model-generation lag before ANY of the qualifying sentence reaches the client, working directly against the premise `PARIPRASHNA_FIRST_PAINT_CITATIONS_ENABLED` was built for (citations resolve within a 64-byte/400ms budget specifically so content reaches the reader fast; the mortality gate already sits downstream of that resolution in the same chain). **Options laid out, none selected — this is the native's call, per this entry's own "not something to resolve unilaterally" instruction:** (i) STATUS QUO — keep current shipped behavior (term-half may leak, date-half is withheld, already tested and disclosed), zero further latency cost, residual (b) stays open on stated grounds — satisfies this entry's binary condition branch (b) if the native accepts it as the stated resolution; (ii) FULL PRE-DISPLAY BUFFER — closes the term-leak residual completely within the existing window widths, at the measured ~5.6%-of-turns-and-growing latency cost against the P2 guarantee described above — also satisfies branch (b), the opposite direction; (iii) a narrower middle-ground buffer (e.g. gated on term co-occurrence with some additional contextual cue rather than bare vocabulary presence) was considered but not designed past this sketch — it trades its own new false-negative risk for reduced over-triggering and would need its own dedicated review before being a real option, not a paper one. No code was changed for residual (b) in this session; this paragraph is the report the entry's own scope asked for. **RULED 2026-08-22, native ruling (interactive session, DD-13 residual (b) call) — option (i) STATUS QUO selected, DD-13 CLOSES:** the native picked branch (b) of this entry's own binary condition — the tension is explicitly resolved, not left open by default. Shipped behavior is kept unchanged: the term-half of a mortality-adjacent pairing may still reach the reader before its paired date is withheld; no pre-display buffer is added. Residual (b) is recorded as a **permanently accepted, disclosed limitation** — it stays documented in `phrasing_scan.ts`'s own module header — not as a fixed defect, not as a further deferral. **Stated reasons, verbatim:** (a) the harm is a reduction, not an elimination — the dangerous artifact is the term AND its paired date together, and that composite pairing is still blocked; both real production hits measured during the investigation above are benign Saturn/Rahu dasha-and-career readings, not near-miss leaks; (b) defense-in-depth already covers the adversarial path upstream, independently demonstrated by this same session's live verification of DD-13 residual (a): a deliberate maraka-dasha longevity probe never reached synthesis at all — the query classifier intercepted it (`hs1_date_of_death`+`hs4_mortality_window`) before `phrasing_scan.ts` was ever in the loop; the phrasing scan is the second layer, not the only one; (c) option (ii)'s cost compounds with adoption while its benefit does not — the measured 2/36 (~5.6%) sample is an early-stage instrument with effectively one user, and longevity/health questions are a core Jyotish use case, so that share is expected to rise as usage grows; waiting for a larger corpus makes this decision worse, not clearer, so it was made now rather than deferred again. **Standing rider, recorded as a future review trigger, not an open obligation:** if the serving population changes materially (cohort subjects admitted, or public access reached at P3-F), this ruling is to be revisited on its own terms, since reason (a)'s "benign in practice" premise is population-dependent. **DD-13 is now CLOSED in full** — both residual (a) (code fix, merged/deployed/live-verified, PR #1465) and residual (b) (native-ruled status quo, this entry) are resolved; nothing remains open under this ticket. |
| **DD-14** | HS-4 death-anniversary false positive in `classifier.ts`, confirmed live via the probe harness (added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session) | **Status: CLOSED.** A defect in `platform/src/lib/pariprashna/safety/classifier.ts` (input query classification — `HS4_PATTERNS`' `hs4.mortality_terms` phrase pattern, and the separate Tier-A `MORTALITY_TERMS_A` combination-rule lexicon) — **explicitly NOT part of DD-13's scope**, which is `phrasing_scan.ts` (a downstream, output-side pre-wire redaction scan over the model's own generated prose). The two files sit in the same `safety/` directory and are both cited in DD-13's own header as siblings with deliberately different, non-shared contracts (`classifier.ts` decides whether to hold an incoming QUESTION; `phrasing_scan.ts` redacts the model's OUTGOING prose) — confirmed by direct read, zero shared code between them. DD-13's two named residuals (third-sentence pairing gap, streaming un-send asymmetry) are specific to `phrasing_scan.ts`'s `CROSS_SENTENCE_WINDOW`/streaming-buffer mechanism and do not describe this defect in any part. **This entry closes 0% of DD-13** — DD-13's own deadline and binary condition are untouched by this closure. Reproduced live (pre-fix): "My father's death anniversary is coming up next month — what does that period look like astrologically for the family?" held under full HS review, identical to a genuine mortality-timing question. First fix attempt (narrowing the shared `MORTALITY_TERMS_A` lexicon directly) was caught by a dedicated adversarial review BEFORE merge — a controlled before/after comparison found it created a strictly worse regression: a compound temporal/duration mortality ask sharing "death anniversary" phrasing (e.g. "How many years until my death anniversary — this year or a future one?") went from a correct `hs1_date_of_death`/`hard_stop` (pre-fix) to `classes: []`, completely undetected — never pushed, never merged. Corrected fix: `MORTALITY_TERMS_A` reverted to its original unmodified form (still gates the hard-stop combination rules unchanged); a new, separate `MORTALITY_TERMS_A_STANDALONE_REVIEW` constant (identical lexicon + an `(?!\s+anniversar\w*)` lookahead) added, used ONLY to gate the narrow `mortality_term_present` review-level push. PR #1376, merged via merge queue, deployed to `amjis-web-01552-cb7` (100% traffic, independently confirmed via `gcloud run services describe`). Live-reverified post-deploy via the probe harness (`platform/scripts/probe/ask.sh`): the original phrase now clears cleanly (turn `3c6e9610-6abc-4895-8d06-2f8534501a7c`, normal domain-clarifying response, no hold) and the regression-guard compound phrase still correctly hard-stops (turn `8e2312bf-1c34-4e74-985c-b6768f768e8d`, safety-seal hold, `hs1_date_of_death`). 671/671 classifier tests, 1953/1953 full pariprashna suite, `tsc`/`naming_lint`/`drift_detector`/`check_earned_signal` all clean. |
| **DD-15** | Block-kind rendering gap (dasha table/verse not rendering as structured blocks) — corroborating live evidence for the MCP↔web namespace gap (added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session) | **No new decision opened — cross-reference only.** This is a live, user-facing SYMPTOM of the namespace gap already measured and CLOSED at `00_ARCHITECTURE/briefs/retrieval_residual/NAMESPACE_COVERAGE_v2_0.md` (RC-10/R-9, v2.1: 20/23 `VIDHI_PRIMITIVES` `live_tool` names bridged + 3 deferred). Precision on the link, so this doesn't read as reopening RC-10's own closed measurement: RC-10 measures one SPECIFIC 23-name set; Paripraśna's own receipt-coverage floor (`receipt_stage.ts`, `floor_item_id` values like `positions_snapshot`, `arudha_read`, `mechanism_read`, `bhava_condition`, …) is a DIFFERENT, larger floor-item list that shares the same underlying resolution mechanism (`resolveLiveTool` / `LIVE_TOOL_TO_RETRIEVAL` / the generated web-tool bridge, both sites literally emit the string `web_namespace_gap`) but is not RC-10's identical 23-name set — this entry corroborates the GAP CLASS with a fresh, real symptom, it does not reopen RC-10 itself. Live evidence: two real production turns via the probe harness, with `PARIPRASHNA_SEMANTIC_BLOCKS_ENABLED` independently confirmed live (`true`, via `gcloud run services describe`), both persisted their entire response as one `semantic_kind: "paragraph"` block despite containing dasha-window dates and a bulleted transit list — never decomposed into `table`/`list`. Both turns' own receipts disclosed a large namespace gap on their own floor (2/5 and 7/21 floor items served, the remainder `web_namespace_gap`). Turn evidence, queried directly from `conversation_messages.metadata_json->'acharya_reading_receipt'`: assistant messages `32ca9a07-1ae5-414f-9008-4b14a0777496` (`coverage.served=2/5`) and `fb9f1794-f3da-40c7-b326-45397ed3f4e3` (`coverage.served=7/21`). |
| **DD-16** | Crash-kill/outbox recovery — untestable in production, blocked on a migration decision (added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session; RULED and CLOSED 2026-08-20, PARIPRASHNA-DD16-DD17-EXECUTE session) | **Status: CLOSED.** Native ruled: open the migration, additive only, flag stays off. Migration `578_pariprashna_persistence_outbox.sql` applied to production via `platform/scripts/migrate.ts` — exactly the table/2-index/RLS-policy shape this entry's own prior text specified from `durable_outbox.ts`'s header, nothing else; no GRANT statements (disclosed as owed to a future real cutover, not this migration's scope). Applied 2026-08-20T17:41:21Z, tracked in `_migrations_applied`, sha256 independently recomputed and confirmed to match the on-disk file (not trusted from the runner's own claim alone) — the deploy-time bulk runner is not trusted blindly, per CLAUDE.md §N.4. Independently re-verified against the live table post-apply, not just the runner's exit code: `to_regclass` confirms the table exists with the exact specified columns; `pg_indexes` confirms both indexes; `pg_class.relrowsecurity = false` confirms RLS is created-not-enabled; `pg_policies` confirms the one `chart_id = app_chart_context()` policy scoped to `role_web_serve`. `MARSYS_FLAG_PARIPRASHNA_DURABLE_PERSISTENCE_ENABLED` independently confirmed absent from the live `amjis-web` env (off by default) both before and after. **The crash-kill/outbox recovery replay test, against the REAL table, real result:** `platform/scripts/probe/dd16_outbox_recovery_test.ts` (committed, reusable for a future real cutover) imports and exercises the REAL exported functions (`writeAheadTurn`, `claimPendingPersistenceEntries`, `replayPendingPersistence` — not mocked, not re-implemented) via a real `pg.Pool` against production. Sequence: (1) `writeAheadTurn` enqueued a write-ahead intent — independently re-read via a separate query, confirmed `status='pending'`, `applied_at=null` — this is the exact crash state (`turn.close` told the reader, the real write never happened); (2) a separate `replayPendingPersistence` call (simulating an independent recovery-sweep process) claimed the row and invoked a real `applyFn` whose payload was asserted, byte-for-byte, to match what was enqueued — proving the JSONB payload round-tripped correctly through real Postgres storage, not an in-memory reference; (3) independently re-read post-replay: `status='applied'`, `applied_at` set, `last_error=null`; (4) idempotency check: a second claim sweep found 0 pending rows. **PASS.** Test row deleted on exit (0 rows remain for this test's `message_id`, independently confirmed) — the table is empty in production, exactly as it was before this test, same as before the migration in every respect except the schema now existing. This closes the previously-untestable half of the gate; the reducer-level "visible incomplete state" half was already covered (see this entry's prior text, unchanged). Test script: `platform/scripts/probe/dd16_outbox_recovery_test.ts`. |
| **DD-17** | Receipt/waiver audit — 100% `interpretation_sets` waiver rate, blocked on model-spend policy (added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session; RULED 2026-08-20, PARIPRASHNA-DD16-DD17-EXECUTE session; SUPERSEDED 2026-08-21, PARIPRASHNA-DD16-DD17-EXECUTE session diagnostic follow-up) | **Status: SUPERSEDED — by the native's broader model-tier policy (`gemini-3.1-pro-preview` for deep-reasoning call sites, `gemini-3.7-flash` for light ones), which supersedes this entry's original single-call-site tier ruling. Not closed.** The `gemini-2.5-flash` (`mid` tier) upgrade merged and deployed (PR #1396, `amjis-web-01557-bh2`). Per the native's explicit instruction, the post-deploy probe was run as a DIAGNOSTIC on the tier hypothesis, not as closure evidence. **Real result:** a repeat of the exact same repro question against the exact same canonical chart, post-upgrade, still shows **3/3 detected significant judgments waived**, every one still `waiver_reason: "model produced no entry for this judgment_id"` — byte-identical failure mode to the pre-upgrade 16/16 baseline (turn `b9d55fd0-cfed-43da-910b-b092fbcdb798`, assistant message `cc0b3459-06b3-4481-9e3c-f9aaa3e744cd`). **Branch (b): the tier was NOT the cause.** Root cause, found by directly replicating `worker.ts`'s exact system prompt + response schema + a real judgment against the real deployed `gemini-2.5-flash` model and inspecting the RAW response before any parsing: the model's actual content is genuinely excellent and exactly on-task (3 substantively distinct candidates, a selected reading, a real rationale, a real falsifier) — **but wrapped in the wrong JSON shape** (`{"judgments":[...]}` with `candidate_readings`/`reading_id`/`strongest_reading` field names, no `status` field, wrapped in a ` ```json ` markdown fence) instead of the required `{"sets":[...]}` shape with `candidates`/`selected_index`/`status`. The markdown fence is itself evidence Gemini's native structured-output mode (`responseMimeType: 'application/json'`, which never fences) was not actually in effect for this call, despite `adapter_gemini.ts` wiring `responseFormat: { type: 'json', schema: req.responseSchema }` and its own code comment claiming the AI SDK forwards this into `generationConfig`. `worker.ts`'s extraction (`parsed.sets ?? []`) then silently treats this valid-but-wrong-shaped JSON exactly like an empty response — a real, complete, high-quality model answer is discarded and mislabeled "no entry." This is a genuine, distinct code defect (schema-binding, not model capability) — filed separately as **DD-20**, not fixed in this diagnostic pass. **This diagnostic result is recorded as evidence informing the native's broader model-tier migration decision**: moving this call site to `gemini-3.1-pro-preview` under the new policy would not, on its own, fix this specific failure, since the defect is upstream of model capability — DD-20 is the load-bearing fix regardless of which model tier eventually serves this call site. No further model change made pending the native's own review of the separately-delivered call-site classification report. |
| **DD-18** | Planted-contradiction corpus fixture collided with the HS-3 safety gate as authored (added 2026-08-20, PARIPRASHNA-P2-GATE-BATTERY session) | **Status: CLOSED.** The corpus's own canonical Gate-3 evidence fixture (`cross-domain-001-mercury-career-vs-6l`, `platform/src/lib/pariprashna/corpus/fixtures.ts`, grounded in `UCN_v4_0 §IX.2`) as originally authored asked about "...6th house of obstacles and disease...", which the safety classifier's `DOMAIN_CLASS_RULES` correctly (not a lexical false-positive like DD-14 — a real domain-classifier judgment, not a near-miss) routed to `hs3_health_crisis`/review before contradiction-surfacing logic ever ran — reproduced live against the real native's canonical chart, turn `8770e216-979d-4c22-95d5-fc12e90a2f3c` (safety-seal hold, `safety-postplan-0` block). **Fix (this PR, one line):** the word "and disease" is dropped from the fixture's `queryText`, preserving the actual planted tension (Mercury as confirmed primary career instrument, `SIG.MSR.413`, vs. its 6th-house rulership of "obstacles") without the domain-classifier-triggering word. **Confirmed working, live, before filing this closed:** the reworded query text (byte-identical to what now ships in the fixture) was run live against the canonical chart pre-fix and returned a real, substantive reading that explicitly named and engaged the tension rather than smoothing it over ("Concurrent Outputs... not a liability that undermines its power, but the mechanism that ensures its power is constantly applied") — turn `64bb96f5-b020-4c5b-a700-14220ffca44f`, assistant message `32ca9a07-1ae5-414f-9008-4b14a0777496`. One open note, not blocking this closure: that turn's receipt `cross_domain.domains` recorded only `["career"]`, not the fixture's own `expectedDomains: ["career","health"]` — the corpus scoring dimension (`cross_domain_contradiction_surfaced.ts`) may or may not pass on a real corpus run depending on how strictly it reads that field; the narration-level "surfaced, not smoothed" behavior the gate criterion actually asks for is confirmed working by direct read of the prose, independent of that one structured field. |
| **DD-19** | `interpretation/worker.ts`'s LLM call site has no per-call usage/cost logging (added 2026-08-20, PARIPRASHNA-DD16-DD17-EXECUTE session, arising from DD-17's own investigation; **DATED 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-MODEL-TIER-RULING session, native ruling item 5**) | **Status: OWNED — DEADLINE SET.** Due **2026-09-03**. DD-17's cost estimate had to be approximated from system-prompt/schema character counts (`/4` heuristic) rather than measured, because this call site is entirely absent from `llm_usage_events` — the project's own real per-call usage/cost table, which `planner` and `title` calls from the exact same turns ARE logged into. Root cause (not yet fixed): the `QueryRequest` built in `worker.ts`'s `callOnce` never sets a `pipeline_stage`, the field `platform/src/lib/llm/observability/persist.ts` keys its `llm_usage_events` row on. **Scope of fix:** set a real `pipeline_stage` value on this call site's `QueryRequest` (matching how `planner`/`title` calls already do it) so it starts appearing in `llm_usage_events` with real `input_tokens`/`output_tokens`/`computed_cost_usd`, so a future cost question about this call site (or a monitored fleet-wide waiver rate, DD-17's own disclosed gap) is answered with real data instead of an approximation. **DATED per the native's own read that its blocking scope is now precisely known and narrow** (confirmed live 2026-08-21 via a direct `llm_usage_events` query: `pipeline_stage` values are exactly `planner`/`title`/`synthesize` — zero rows for `interpretation_sets`): it blocks ever answering a real cost question about the `interpretation_sets` call site specifically. **It does NOT block the NCD-8 spend ceiling** — `synthesize` is separately instrumented (see DD-25) — so this is not itself a launch precondition, but P5's calibration work will need real interpretation_sets cost data, so it is not indefinitely deferrable either. Same date as DD-25, not coincidentally: both are cost-observability gaps surfaced by the same query. |
| **DD-20** | `interpretation/worker.ts`'s structured-output schema is not actually being enforced on the model call — the real cause of the 100% waiver rate, found by a DD-17 diagnostic (added 2026-08-21, PARIPRASHNA-DD16-DD17-EXECUTE session diagnostic follow-up; **fix shipped, deployed, and live-verified 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 1 — see DD-12's close evidence, `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §2**) | **Status: OWNED — UNDATED.** A direct diagnostic (see DD-17's amended entry for the full trace) replicated `worker.ts`'s exact request against the real deployed model and captured the RAW response before parsing: the model answers with genuinely excellent, on-task content — 3 distinct candidates, a selected reading, a real rationale, a real falsifier — but in the wrong JSON envelope (`{"judgments":[{"candidate_readings":[...],"strongest_reading":{...}}]}`, no `status` field, wrapped in a ` ```json ` markdown fence) instead of the schema's required `{"sets":[{"candidates":[...],"selected_index":...,"status":...}]}` shape. The markdown fence is itself the tell: Gemini's native `responseMimeType: 'application/json'` structured-output mode never fences its output, so this call was not actually running in schema-constrained mode despite `adapter_gemini.ts:54-56` wiring `responseFormat: { type: 'json', schema: req.responseSchema }` and that file's own comment claiming the AI SDK forwards this into `generationConfig.responseSchema`. `callOnce`'s extraction (`parsed.sets ?? []`, `worker.ts`) has no path to detect a valid-but-wrong-shaped response — it silently treats it exactly like an empty one, and the resulting waiver reason ("model produced no entry for this judgment_id") misleadingly reads as "the model had nothing to say" when the model in fact answered fully and well. **Scope of fix:** determine why the schema constraint isn't reaching the model for this call site specifically (candidates: an AI-SDK-version-specific wiring gap between `responseFormat` and Gemini's native schema parameter, a `reasoning: 'disable'` interaction, or something specific to how `streamText` vs. the AI SDK's `generateObject`/`streamObject` primitives handle Gemini schema enforcement) and fix the wiring so the model is genuinely schema-constrained — not a prompt-only "please match this shape" instruction, which is demonstrably insufficient on its own (the model got the CONTENT right without getting the SHAPE right, consistent with prompt-only guidance rather than mechanical constraint). A secondary, independent hardening worth doing alongside the real fix: `callOnce` should distinguish "valid JSON, wrong shape" from "no sets at all" so a future recurrence surfaces as a loud, specific error rather than the same silent, misleadingly-worded waiver. Not attempted in this diagnostic pass — this is a real fix candidate for a dedicated session, same discipline as every other DD entry filed OWNED rather than fixed inline. No deadline invented. **CLOSED 2026-08-21 (PARIPRASHNA-P2-CLOSE, item 1):** `parseAndValidateSets` (real Zod validation distinguishing wrong-shape from empty) plus a corrected, explicit repair-retry message — PR #1398 (`369719a99`), merged, deployed (`amjis-web-01572-m95`), live-verified via a real probe turn (repair-retry fired and produced genuine `status: "generated"` entries) and reconfirmed in a later browser turn (`detected_count: 4, covered_count: 4, waived_count: 0`). The secondary hardening (distinguishing wrong-shape from empty at the error-surfacing layer) shipped as part of the same fix, not deferred. Full account: `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §2. |
| **DD-21** | New binding rule, effective P3 onward: no lane may be marked closed without an observed-delivery artifact (added 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 8 — arising directly from DD-12's close) | **Status: IN FORCE, effective P3 onward.** DD-12's own finding — seven lanes merged, flagged live, and reported done while delivering nothing observable to a reader, discovered only by a dedicated post-hoc audit — is the evidence for this rule (`PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md`). **The rule:** a lane may be marked closed / delivered ONLY when accompanied by an observed-delivery artifact — a captured probe transcript, a live browser observation, or a database read showing the capability reaching a user. **Code review, passing tests, a merged PR, and a live feature flag are all insufficient on their own** — none of them establish that a reader can actually see or receive the capability; P2's own audit found multiple lanes satisfying every one of those four and still delivering nothing observable. This report's own §2 (items 1–7 + Lane K) is the first application of the rule: every item there closes on a real probe turn, a real browser observation with CDP device metrics, or a direct database read — never on CI-green alone. No exception carved out for small or "obviously correct" changes; the rule binds uniformly. |
| **DD-22** | Item 4 — table-in-prose block promotion (semantic-block detection for a markdown table embedded inside a larger prose block, not occupying the whole block) — ruled OUT of P2-close scope, approach (c) approved (added 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 8; **sequencing vs. P3-B ruled 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-2026-08-21 session**) | **Status: CARRIED — APPROVED APPROACH, NOT BUILT.** Full finding, native ruling, rejected approach (a), and the approved approach (c)'s viability evidence (direct read of `commitBlock()`'s scan-then-classify ordering, `committedBlocks` cardinality, and every real consumer's field-level access pattern) recorded in full at `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §5 — read that section before starting this work rather than re-deriving it. **Approach (c) ("annotate rather than split"):** keep one block per role-shift; attach table metadata (offset ranges + parsed structure) to the existing block; let the renderer draw a real table from a span inside it. Zero change to `committedBlocks`' cardinality. Approach (a) (mid-stream detection touching the streaming loop) is REJECTED — do not revisit; the per-block path runs HS-1, the register lint, and the voice lint, and destabilising those for a formatting fix is not an acceptable trade. **Acceptance criteria for whoever builds this** (native-set, verbatim): (1) byte-exact reconstruction — emitted pieces concatenated must equal the original block text exactly, the ports refactor's own golden-equality discipline; (2) regression proof against #1399 — citations still extract correctly, verified by a live probe turn showing `citation.define` events, not unit test alone; (3) regression proof against #1400 — `facts_consumed` still populates the receipt correctly, verified by a live turn plus a DB read; (4) proof that the safety scans genuinely ran on the full text before any split or annotation — already discharged in the P2-close report's own direct code read, but the builder should re-verify against whatever the code looks like at build time, not trust this entry's snapshot indefinitely. No deadline invented. **Sequencing vs. P3-B (added 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-2026-08-21 session):** DD-22 and P3-B (headless-loop extraction) both annotate/touch `commitBlock()`'s region of `reading_parts.ts` and must never run concurrently — either strictly before P3-B opens or strictly after P3-B merges. **Ruled: BEFORE.** DD-22 lands first. Rationale: DD-22 is small, fully scoped, has its approach ruled (c) and acceptance criteria attached above — nothing about it depends on P3-B's extraction — while landing it first means P3-B's headless-loop extraction inherits a `commitBlock()` that already carries correct table-span annotation, rather than needing a second pass to re-thread that annotation through the extracted loop. Building it after P3-B would mean re-deriving `commitBlock()`'s scan-then-classify ordering against whatever shape the headless-loop extraction leaves it in — pure avoidable rework. Whoever opens P3-B must confirm DD-22 has merged before starting; whoever picks up DD-22 must confirm P3-B has not yet opened. |
| **DD-23** | New finding: remedial guidance is under-governed as a content class (added 2026-08-21, PARIPRASHNA-P2-CLOSE session, item 8 — arising from the DD-20 fix's own live-verification turns) | **Status: OWNED — UNDATED. Does not block P2 close.** Full evidence at `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §4: in one live browser turn, TWO independently-built honesty controls flagged the SAME remedial content — the voice-lint imperative detector (`voice_imperative_detected`, on real label-prefixed bare imperatives like `"* **Practice:** Chant this mantra 108 times..."`) and the citation gate (`citation_gate_error`, `"prescriptive query (remedial) produced 0 citations"`, determined in the same session to be case (b) — real sources exist and were fetched, simply not cited, not case (a) — see the same report's §3). **Scoped as an investigation, not a fix.** Open questions for whoever picks this up: does remedial/prescriptive guidance need its own voice register, distinct from the imperative-phrasing conventions built for descriptive/diagnostic prose? Does it need its own citation policy — a dedicated remedial-source-citation requirement, given `remedial_codex_query` already returns real, structured, scripture-cited material the model is evidently not obligated to attach? Or both? No deadline invented — same honest undated handling as DD-13 carried before its own deadline was separately ruled, and as DD-19 still carries. |
| **DD-24** | Parity proves sameness, not correctness — P3-D's receipt-hash parity assertion is bounded to an enumerated baseline of the web door's known gaps (added 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-2026-08-21 session, item 1) | **Status: BINDING ON P3.** P3-B propagates the web door's real behavior onto the MCP door (headless-loop extraction, `prashna_ask` re-based onto it with all gates); P3-D then asserts receipt-hash equality between the two doors' outputs (normalized receipt-hash equality, `platform/tests/**`). The web door still carries known, filed defects at P3 open — most sharply the citation coverage gap `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §3/§4 found and filed as **DD-23**: `remedial_codex_query` genuinely fetches real Remedial Codex content and real `RM.*` signals enter the citable pool, yet the entire remedial/prescriptive span of the model's own output carries zero `[n]` citation markers (case (b) — sources exist, not cited; the gate is functioning correctly, not a false positive). P3-B will propagate this faithfully onto the MCP door; P3-D will then certify the doors identical — and they will be: identically wrong. A green parity check on a known defect converts a tracked gap into an untracked one, which is worse than no check at all. **The rule:** before P3-B propagates web-door behavior to the MCP door, the web door's known gaps must be ENUMERATED in the P3-B lane brief and each explicitly marked either propagated-knowingly or fixed-first. P3-D's parity assertion is valid only against that enumerated baseline. Parity is evidence that two doors agree; it is never evidence that either is correct. No parity pass may be cited as delivery evidence (per **DD-21**'s observed-delivery-artifact rule) for any capability listed in the enumeration. **Seed enumeration, current as of this filing (RE-DERIVE at P3 open — do not inherit this list verbatim; it will have changed by then):** (1) Lane B's remedial citation coverage gap, filed **DD-23** (`PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §3/§4) — remedial spans ungrounded despite real sources fetched; (2) **DD-22** — table-in-prose block promotion, approach (c) approved, not built; (3) **DD-23** — remedial guidance under-governed as a content class, scoped as investigation, not fix; (4) **N** (`P2-N`, gate `G3-F`) — the quality corpus (12 query classes, 13 scored dimensions) never run against the live route, carried not delivered per `PARIPRASHNA_P2_CLOSE_REPORT_v1_0.md` §6; (5) **O** (`P2-O`, gate `G3-G`) — model qualification suites never run live, carried not delivered per the same §6; (6) **H's Seal motion** — not observed, not inferred, never marked delivered, per the same §6. This seed list is drawn from the P2-close report's own §6 Lane disposition and DD-22/DD-23's own filed text, provided as a concrete starting point, not a substitute for re-deriving it — the P2→P3 boundary is itself a place new gaps could surface, and this entry does not claim completeness beyond what was known at filing time (2026-08-21). |
| **DD-25** | `computed_cost_usd` is null on every `synthesize` row in `llm_usage_events` — a real row exists (unlike DD-19's gap), the cost field on it is empty (added 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-MODEL-TIER-RULING session, native ruling item 4, discovered during the item-5 decision-packet research) | **Status: OWNED. DATED 2026-09-03 AND marked a HARD PRECONDITION for any public access, whichever comes first.** Distinct from **DD-19**: DD-19 is "no rows at all" for `interpretation_sets`; this is "the row exists, one field on it is empty" for `synthesize`. Confirmed live 2026-08-21 via direct `llm_usage_events` query, both before and after this session's 3 real probe turns (verifying PR #1402) — all 4 `synthesize` rows on record (`gemini-2.5-pro`, real `input_tokens`/`output_tokens` populated: 52,366–132,719 in, 3,248–4,117 out) carry `computed_cost_usd: null`. `input_tokens`/`output_tokens` populate correctly; only the derived cost field is empty — narrows the defect to whatever computes `computed_cost_usd` from those two fields plus the model's registry cost rates, not to the usage-logging path itself (that path is confirmed working, per PR #1402's live-verified verdict this same session). **Consequence, stated plainly:** `PARIPRASHNA_LIMITS_ENABLED` cannot be enabled while `computed_cost_usd` reads null on every synthesis turn — the NCD-8 spend ceiling (`platform/migrations/574_ncd8_spend_ceilings_channel_attribution.sql`'s `channel`-scoped columns on `llm_usage_events`) reads this field to enforce the $2/turn · $40/day pre-dispatch ceiling; a null cost field means every turn evaluates as $0 spent, so the ceiling is structurally inert and public traffic would be uncapped. **This is therefore a HARD PRECONDITION for any public access decision, not merely a nice-to-have fix** — distinct from DD-19, which is real but not launch-blocking. Same 2026-09-03 date as DD-19 — both are cost-observability gaps surfaced by the same query pass, dated together rather than independently re-derived. **Scope of fix (not yet investigated in depth — filed as OWNED, not root-caused):** trace `computed_cost_usd`'s write path in `platform/src/lib/llm/observability/persist.ts` for the `synthesize` pipeline_stage specifically; confirm whether the cost-computation step reads the resolved model's `costPer1MInput`/`costPer1MOutput` from `lib/models/registry.ts` correctly for `gemini-2.5-pro`, or whether the computation is skipped/fails silently for this call site. **ROOT-CAUSED 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-PART-D session — scope corrected, wider than this entry's own original framing.** `computed_cost_usd` is null on **every** `llm_usage_events` row, for every provider (gemini/anthropic/nim/deepseek) and every pipeline_stage (planner/title/synthesize) observed in the last 30 days — not only `synthesize`'s. The write path never reads `registry.ts`'s `costPer1MInput`/`costPer1MOutput` at all: `computeCost()` (`platform/src/lib/llm/observability/cost.ts`) queries a separate table, `llm_pricing_versions`, which has had **zero rows** since its creation (migration 038) — confirmed directly against production. Every call throws `PricingNotFoundError`; `safeComputeCost()` in `observe.ts` catches it, logs to `console.error`, returns `null`. Not a migration that silently no-op'd — a feature shipped with its data source never seeded, ever, for any provider. **Fixed:** migration 583 (`583_llm_pricing_versions_seed.sql`) seeds the table from `registry.ts`'s current flat rates, translated through the existing `google→gemini`/`nvidia→nim` provider-vocabulary map (`platform/src/lib/pariprashna/observability/provider_map.ts`). **Disclosed limitation, not fixed in this pass, filed as DD-26:** the table's schema holds one flat rate per model; Google's real pricing is tiered by input size for `gemini-2.5-pro`/`gemini-3.1-pro-preview` — this seed uses the ≤200K-input tier, so any call whose input exceeds that threshold will be under-priced until DD-26 lands. Status of this entry moves from OWNED/not-root-caused to **FIX LANDED, pending merge + deploy + live re-verification** that `computed_cost_usd` populates and the NCD-8 ceiling is no longer inert (two distinct claims per §N.8 — populated ≠ enforcing). |
| **DD-26** | `llm_pricing_versions`'s schema holds one flat `price_per_million_usd` per `(provider, model, token_class)`; Google's real Gemini pricing is tiered by input size (registry.ts's own comments — e.g. `gemini-2.5-pro`: "$1.25 up to 200K input; $2.50 above 200K") (filed 2026-08-21, PARIPRASHNA-P3-PREFLIGHT-PART-D session, disclosed limitation of migration 583 rather than silently absorbed) | **Status: OWNED, no deadline set.** Migration 583 seeds the ≤200K tier rate for every tiered model; any call whose input exceeds the tier threshold is under-priced by `computeCost()` until this lands — a real, bounded, disclosed gap, not a launch precondition (DD-25's own consequence — the NCD-8 ceiling reading *some* non-null cost, even if occasionally under-priced at the tier boundary, is a materially different risk than reading `null`/$0 on every turn). **Scope of fix (not yet built):** extend `llm_pricing_versions`'s schema to support an input-token-range column (or a second row per model keyed by a threshold), thread threshold-aware lookup through `computeCost()`, and re-seed the affected models (`gemini-2.5-pro`, `gemini-3.1-pro-preview`) with both tiers. Native-scoped out of Part D (a schema-change brief, not Part D's trace-and-fix brief) — carried here as its own dated, owned item so it isn't lost. |
| **DD-27** | Deploy pipeline runs a full `Build & Deploy Web` for docs-only PRs that touch no `platform/**` path (first observed 2026-08-22, PARIPRASHNA-P3-PREFLIGHT-PART-F close, PR #1467 — a two-file `00_ARCHITECTURE/**` change; filed as its own entry 2026-08-22 per native ruling, superseding the Part-F session's own disposition of "not filed as a DD, a resource-efficiency observation") | **Status: OWNED — UNDATED, gating clause added for Part H.** The Part-F session that first observed this (PR #1467: `main@49209b067` → `CI — Ganga Quality Gate` → `Deploy to Cloud Run`, full `Build & Deploy Web` job ran and succeeded for a change touching only `00_ARCHITECTURE/CAPABILITY_MANIFEST.json` and `PARIPRASHNA_SWARM_REVIEW_AND_AMENDMENTS_v1_1.md`, neither under `platform/`) treated it as a non-blocking resource-efficiency note, not a defect, and did not file it as a DD. **Native ruling, 2026-08-22 (this session):** the observation is not cosmetic — whatever gates `Build & Deploy Web` apparently triggers on more than a `platform/**` path filter, or unconditionally on every push to `main`. **Binding consequence for Part H:** Part H's batched governance-registry write (`CURRENT_STATE`, `SESSION_LOG`, `FILE_REGISTRY`, `CAPABILITY_MANIFEST`, `NATIVE_DIRECTIVES`) is itself docs-only by content, but per this entry it must be treated as a REAL production deployment of whatever is on `main` at merge time — not skipped or downgraded because "it's only docs." Whoever runs Part H must: (1) check what is actually on `main` immediately before merging (including any work concurrently landed by the other live campaign, PARIŚEṢA-RĀTRI-V4); (2) announce the resulting Cloud Run revision tag per X-6; (3) run the full tagged-revision-at-0%-traffic → smoke battery → traffic shift → post-shift smoke canary discipline described in §10 of the P3-preflight master prompt, exactly as for a code change. **Scope of fix (not yet investigated):** determine what `Deploy to Cloud Run`'s trigger condition (workflow path filter, or absence of one) actually is in `.github/workflows/`, and whether it should be narrowed to `platform/**`-only changes, or left as-is if there's a real reason (e.g. governance docs are read at runtime by some service and do need to ship). No deadline invented — this is a pipeline-efficiency question, not a safety gap; the safety-relevant consequence (treat every merge to `main` as a real deploy) is already binding as of this entry regardless of when the underlying trigger condition itself gets investigated. |

**Effective at the next phase boundary (P1 close), not mid-phase — landed,
but landing is not the same as wired.** DD-11 was written as a new standing
rule, not a hotfix to something broken right now — the P1 conductor was
mid-phase when it was authored, and inserting a new mandatory
per-lane-transition call into a running phase would have risked exactly the
kind of disruption DD-11 exists to prevent (an interruption at an unplanned
moment). P1 finished under the rules it started under; P2 opened under
DD-11's *text*, but not its *enforcement* — investigated 2026-08-20 (same
session, item 2 follow-up): the live P2 conductor session was found running
from a worktree branched before this rule (or the script it calls) existed
on `main`, and there is no deterministic per-lane hook in this campaign for
any session to attach a check to short of editing the conductor's own
in-flight files, which risks the disruption this rule was written to avoid
in the first place. Editing prose a live session has already loaded into its
own context does not change what that session does — only a session that
starts fresh (or is deliberately resynced) after this text lands actually
picks it up. Landing the rule's *text* is not the same claim as landing its
*enforcement*, and the row above says so plainly rather than reading as
closed.

**2026-08-20 update (PARIPRASHNA-CLOSEOUT-FINAL, item 1):** that investigation's
root cause — no deterministic per-lane hook exists for *any* per-lane
discipline in this campaign, not just DD-11's — is now its own entry, DD-12,
immediately above. DD-11's deadline clause has been removed in favor of
DD-12's single P2-close decision point, so this file carries one obligation
here, not two.

**2026-08-20 (PARIPRASHNA-GOLIVE-CLOSEOUT session): cross-tenant denial on
`/pariprashna`, live-verified — no new obligation needed.** The prior
go-live report (PARIPRASHNA-GOLIVE-EXECUTE) flagged cross-tenant denial on
the Pariprashna route as code-verified only, not live-tested, for lack of a
second identity. One already existed: `probe-service-account`, the
provisioned read-only QA principal documented in `R5_RUN_LEDGER_v1_0.md`
(no `profiles` row; used for prior MCP-layer canary probes) — also present
in `chart_grants` with `view` permission on a subset of charts. A session
cookie was minted for it via `platform/scripts/dev/mint_session_cookie.ts`
(no new production account created). Result: `GET
/clients/482012f1-.../pariprashna` (a chart it IS granted on) → `200`
(positive control); `GET /clients/fdee25b5-.../pariprashna` and `GET
/clients/e36ba429-.../pariprashna` (two charts it holds NO grant on) → both
`307` to `/dashboard`, matching `resolveChartPageAccess`'s documented
`permission === 'deny'` behavior exactly. Cross-tenant denial on the live
Pariprashna route is now live-confirmed, not just code-read. The minted
session cookie was deleted after the test; no credential persisted.

**Result: zero human gates.** The only stops are halt conditions, and every
halt ends with rollback pinned, state resumable, and a report — never a hang.

## §3 — The merge & deploy train protocol (the "smart merge queue")

```
 lane branch ──verifier PASS + refuters clear + impacted-suite CI──▶ ADMISSIBLE
                                                                        │
   ┌────────────────────────────────────────────────────────────────────┘
   ▼
 TRAIN BATCHING (integrator): collect all currently-admissible lanes (≤5),
   order by conflict-prediction (shared-type/protocol touchers FIRST, leaf
   lanes last), speculative merge onto the integration branch
   │
   ├─ ONE full-CI run per batch
   │     red → BISECT by halving → park the offender (finding written) → rerun
   │     green ↓
   ├─ integration branch REBASES onto origin/main (RF-7), re-verify quick suite
   ├─ PR → main via merge queue  (only Paripraśna merges during FREEZE-3)
   ▼
 DEPLOY (canary): tagged Cloud Run revision @ 0% → tagged-URL smoke
   (demonstrated-can-fail) → 100% traffic → post-shift smoke
   red at any step → AUTO traffic-rollback → auto-triage → halt if unresolved
```

Two trains per phase, strictly ordered: **T-SCHEMA** (migrations + grants +
RLS; merges and deploys FIRST, expand-only mid-phase) then **T-APP**
(everything else, batched). Trains run repeatedly as batches fill — a lane
verified at hour 2 is on main by hour 3; nothing waits for the phase to end.
Gate batteries run under FREEZE-1 against the deployed artifact, per v1.0.

## §4 — Swarm scaling policy ("availability never throttles velocity")

- **One oversubscribed work-stealing queue; roles are hats** (RF-6). Every
  slot always holds the highest-priority available task.
- **Adaptive concurrency:** start N=10 concurrent worker sessions; every 15
  minutes, if p95 task latency and API error rate are under threshold, N+=2
  (cap 24 — beyond that, local CPU and provider rate limits invert the gain);
  on 429s or latency spikes, N−=4 immediately, recover gradually. The
  conductor logs N over time in the tracker so throttling is visible, not
  suspected.
- **Honest physics:** hundreds of agents per phase are QUEUED and PIPELINED;
  ~10–24 execute at any instant. Velocity comes from zero idle (work-stealing
  + prefetch + trains that ship hourly), not from pretending simultaneity.
- **Builder oversupply rule:** the lane queue is always ≥2× the concurrency
  cap deep (pulling forward next-wave lanes whose leases are already free),
  so no slot ever waits for work to be defined.

## §5 — The live tracker (real-time, never stale)

**Files** (in `00_ARCHITECTURE/briefs/pariprashna_swarm/state/`):
- `SWARM_TRACKER.json` — machine state: phase · wave · per-lane
  {id, role-stage: queued|building|verifying|refuting|admissible|merged|parked,
  worktree, branch, last_event_ts, verifier_verdict, refuter_votes} · trains
  {batch, ci_status, merged_sha} · deploys {revision, traffic, smoke} · gate
  results · budget {spent, ceiling} · concurrency N · **heartbeat_ts**.
- `tracker_data.js` — the same state as `window.TRACKER = {...}` (file://-safe).
- `tracker.html` — self-contained dark dashboard (no server needed: open the
  file, it includes tracker_data.js and reloads itself every 30s): phase strip,
  lane board grouped by stage, train/deploy timeline, budget bar, concurrency
  sparkline, and a **staleness banner that turns the header red if
  heartbeat_ts is older than 15 minutes** — a stale view announces itself.
- `TRACKER.md` — human digest, regenerated at wave boundaries.

**Update discipline (binding on the conductor):** write JSON+js on EVERY lane
state transition; heartbeat write every 10 minutes even when nothing changed;
commit tracker state to the phase branch every 30 minutes and at every train
close (visible remotely via GitHub as well as locally); final loud line +
tracker commit at phase close or halt.

## §6 — Amended phase structure

```
 P0 IGNITION (new)      env + ports refactor + tracker + G0 absorption
 P1 FOUNDATION          as v1.0, minus the operator packet (DD-3), 10 lanes
 P2 READING MADE TRUE   as v1.0, 15 lanes (now collision-free thanks to P0)
 P3 ONE ENGINE/DOOR     as v1.0 + DD-7 autonomous smoke hold + DD-1 feel-proxy battery
 (no human seam — DD-1) P4 proceeds on the proxy battery; native verdict async
 P4 OLD DIES/REMEMBERS  as v1.0 + DD-4 pre-authorized retirement + DD-8 machine-graded Q-2
 P5 EARNED CALIBRATION  as v1.0
```

**P0 IGNITION lanes** (small phase by count, foundation by nature):
P0-A merge G0 (DD-9) · P0-B environment (worktree farm, gh/gcloud auth checks,
cloud-sql-proxy, template test-DB, migration allocator, flag registry) ·
P0-C **ports refactor of route.ts** with the golden-stream equality harness
(RF-1 — the phase's centerpiece; verifier + 3 adversaries on the equality
claim) · P0-D tracker scaffold (§5) live from minute one · P0-E design-plan
grounding pass (docs) · P0-F DD-2 anthropic delist + DD-3 infra automation
probes (find IAM refusals NOW, not mid-P1). Gate: golden streams
byte/semantically identical through the decomposed route on the deployed
artifact; tracker live; every DD-3 command proven or parked.

## §7 — Addendum (2026-08-19, post-P0 close): §5's tracker superseded going forward

P0 (including P0-D, the tracker scaffold this §5 specified) closed before this
addendum was written — `origin/campaign-coordination @ 3bdcc752a`, PR #1349
merged as `9db457dcc`, all 5 P0 lanes (Step 0 + P0-B/C/D/E/F) closed, deploy
verified live. §5's tracker was built and merged exactly as specified:
`00_ARCHITECTURE/briefs/pariprashna_swarm/state/{SWARM_TRACKER.json,
tracker.html, tracker_data.js, TRACKER.md}`. So the standing instruction this
addendum's own kickoff brief carried — "P0-D reduces to wiring lane
transitions into tracker_emit.py" — no longer applies verbatim: there was no
still-open P0-D to redirect. What follows instead.

**The Paripraśna Execution Observatory (tracker-v2)** —
`00_ARCHITECTURE/briefs/pariprashna_swarm/tracker/` (code, versioned) +
`~/.pariprashna-tracker/` (runtime state, outside the repo) — was built as a
**separate, standalone session**, retargeted at **P1 onward** rather than at
P0: derived-not-narrated lane state (git/`gh`/`gcloud`/filesystem ground
truth, never a self-report, per CLAUDE.md §N.8 applied to the instrument
itself), an out-of-process daemon (`trackerd.py`) with a three-tier dead-man's
switch (20s heartbeat · launchd watchdog restart on >90s staleness · client-
side staleness computed independently of the backend), append-only per-writer
event logs, a declarative `PLAN.yaml` carrying the real 53-lane inventory from
`PARIPRASHNA_PHASED_SWARM_IMPLEMENTATION_PLAN_v1_0.md §2/§4` (not the
approximate "30 lanes" this kickoff brief itself cited), and a falsifiable
`--selftest` suite exercised against deliberately broken inputs before being
trusted (README + session report carry the full acceptance evidence,
including the observed-failing runs).

**§5 of this file is superseded going forward.** The P1+ conductor does not
build its own tracker: whatever spawns P1's lane agents wires one
`tracker_emit.py` call per lane-state transition (`kind: "lane_state"`,
`evidence_class: "CLAIMED"` for states with no artifact, e.g. `BUILDING`
before a branch exists — the observatory itself derives `MERGED`/`CLOSED`
independently from git/GitHub once a branch or PR exists, and flags an
`anomaly` if a claim and the derived evidence disagree). §5's file paths
(`state/SWARM_TRACKER.json` etc.) are retained in place as the historical P0
record, not deleted, not updated further.

*End addendum. §1–§6 above remain the historical record of the P0-era
review — unedited except for this pointer.*

*End SWARM_REVIEW_AND_AMENDMENTS v1.1 — this file binds execution; v1.0
remains the narrative plan of record beneath it.*
