# Stream charter — {{stream_id}}

- Owner: {{stream_lead}}
- Independent verifier: {{verifier}}
- Baseline SHA: {{baseline_sha}}
- Worktree/branch: {{worktree}} / {{branch}}
- Approved ceiling: {{ceiling}}
- Entry gate and dependencies: {{entry_gate}} / {{dependencies}}

Freeze the scenario denominator before execution. Record evidence URIs for every accepted
work item. Park A3 decisions; label delegated product calls `SURROGATE DECISION — not native
acceptance`. Closure needs stream regression, a result packet, independent verification, and
integrator acceptance.
