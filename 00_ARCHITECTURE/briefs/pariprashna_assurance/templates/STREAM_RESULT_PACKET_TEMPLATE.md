# Stream result packet — {{stream_id}}

- Scenarios planned/executed: {{planned}} / {{executed}}
- Findings and root causes: {{findings}}
- Remediations verified/rejected: {{remediations}}
- Regression evidence: {{regression_evidence}}
- Independent verifier verdict: {{verifier_verdict}}
- Open A3 decisions and residual risks: {{residuals}}

This packet is a link set to primary evidence. It is not acceptance until an authorised
integrator emits `result_packet_accepted` with its evidence URI.
