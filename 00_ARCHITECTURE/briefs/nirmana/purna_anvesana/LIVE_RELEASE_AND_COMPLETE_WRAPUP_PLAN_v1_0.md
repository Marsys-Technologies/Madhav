---
artifact: MADHAV_PURNA_ANVESANA_LIVE_RELEASE_AND_COMPLETE_WRAPUP_PLAN
version: "1.0"
status: FROZEN_FOR_EXECUTION
campaign_id: madhav-purna-anvesana
session_id: MADHAV-PURNA-ANVESANA-LIVE-WRAPUP-20260915
authority: CCD-012
protected_base: 731e311f0b8f5f84db2f152b93951e1d3d50d89a
wave7_source_candidate: facccfe4b518b997357c08ff7798b703459326df
produced_on: 2026-09-15
---

# Pūrṇa Anveṣaṇa live release and complete wrap-up

## 1. Terminal contract

Bring the complete planner-knowledge and inquiry capability onto Madhav's approved production
surfaces and prove the original requirements against the exact delivered system. Completion means
PA-R01 through PA-R13 have accepted dispositions; the aggregate source candidate is independently
reviewed and protected-merged; required migrations and dedicated signing/runtime configuration are
verified; Portal, managed MCP and raw MCP pass the same deployed inquiry; the frozen empirical
protocol passes with real eligible experts and data; production serves the expected revision and
remains healthy through the bounded observation window; compatibility retirement, if undertaken,
has separate downstream and rollback evidence; and governance/handoff records agree.

No source, local, disposable, PR, CI, staging, timestamp or dashboard proxy earns a stronger gate.

## 2. Immutable inputs and preservation

- Protected base at open: `731e311f0b8f5f84db2f152b93951e1d3d50d89a`.
- Immutable Wave 7 source candidate: `facccfe4b518b997357c08ff7798b703459326df`.
- Existing PR stack: #2597 through #2605; none is merged at open.
- Preserve all Wave 0–7 events, negative acceptance, refutations and exact-head receipts.
- Build one aggregate candidate from current protected main. Do not merge the stack one PR at a
  time because intermediate protected-main states would trigger incomplete deployment/migration.
- Do not edit migrations 1035/1036 or Data Plane L0–L3 source.

## 3. Execution gates

### G0 — authority, session and live stocktake

Pass when CCD-012, the remote lease, validated session-open handshake, protected-tip/stack census,
deployment oracle, migration history, runtime configuration metadata and gap matrix are current.
Read-only discovery must not print credentials, tokens, keys or sensitive chart data.

### G1 — aggregate candidate and requirements reconciliation

Fast-forward the isolated aggregate branch to the exact Wave 7 candidate, then inspect the original
brief, R1–R12, PA-R01–PA-R13, the unchanged 34-route corpus and all generated projections. Repair only
evidence-backed gaps. Preserve 34/34 without weakening the corpus. Resolve migration-number/location,
current migration dependency order and release-topology issues by additive forward change only. The
known estate-wide empty-database baseline conflict is separately evidenced and must not be smuggled
into this release by reinterpreting already-applied historical migrations.

### G2 — local/disposable verification and independent review

Run the frozen acceptance, focused inquiry/MCP/security/concurrency suites, codegen checks, TypeScript,
lint, complete platform unit gate, qualified MCP gate and Pūrṇa migration replay on a production-
compatible disposable schema. Reproduce any
failure before repair. Obtain independent read-only semantic/planner, security, migration and terminal
reviews of one exact candidate; zero unwaived HIGH or MEDIUM findings may advance.

### G3 — protected review surface

Push the aggregate branch, open one focused protected-main pull request, verify the remote head and
required check set, and obtain current review evidence. Do not administer or weaken the ruleset. Keep
the candidate immutable after final approval; any source change invalidates downstream receipts.

### G4 — staging/shared schema and revision-pinned canary

After the conflicting shared-operation lease is absent, acquire a fresh exclusive operation lease.
Capture rollback/backup references. Apply the reviewed migration set to an approved staging or
production-compatible target through the established runner; prove migration hashes, RLS, ownership,
least privilege and security-definer search paths. Provision/rotate the dedicated inquiry signing ring
without exposing material. Deploy the exact candidate to a no-traffic or revision-pinned canary with
only the reviewed timeout/CPU/runtime changes. Red reverts to the prior revision and parks the gate.

### G5 — deployed three-door and overlay acceptance

Using one consented test principal/chart and frozen fixture, execute Portal, managed `prashna_ask` plus
status, and raw inquiry start/execute/finalize. Prove normalized semantic equivalence, first/middle/final
pagination and exhaustion, failure identity, active-build overlay/hash selection, dark-state refusal,
restart/cross-instance continuity and redacted receipts. No sensitive payload enters repository logs.

### G6 — empirical answer-quality acceptance

Execute `EMPIRICAL_ANSWER_QUALITY_PROTOCOL_v1.json` unchanged only when the eligible held-out corpus,
privacy/consent custody, two independent domain experts, conflicts, blinding, randomized pairing and
acceptance owner are real and recorded. Seal scores before unblinding. All six metrics and all stop
conditions govern. Missing participants/data/approval remains `NOT_RUN`; it cannot be self-certified.

### G7 — protected integration, production promotion and observation

Only after the strict pre-production gates that can be run before main integration pass, admit the
immutable PR through the existing merge queue. Verify the merge commit/protected tip and release run.
Inspect Cloud Run traffic and the deployed revision directly. Promote canary traffic only on green
health, then repeat production three-door/overlay probes and observe the bounded health window. Roll
back through the governed release path on regression.

### G8 — compatibility, governance and maintenance close

Retire no flat-manifest or compatibility path without a separate reviewed downstream-consumer census,
deployed proof and restoration path. Reconcile campaign state/events, CURRENT_STATE, CCD, SESSION_LOG,
PR/deployment/migration/empirical receipts and maintenance runbooks. Release the coordination lease
remotely and validate the session close. `FULLY_COMPLETE` is allowed only if the exact delivered
revision satisfies every applicable gate and no material residual remains.

## 4. Concurrency and ownership

There is one Writer: this root session. Independent agents are read-only reviewers and may not edit,
commit, push, approve, merge, deploy, migrate, rotate keys or mutate campaign state. All shared or
production operations are serialized under a fresh exclusive lease. Foreign worktrees and unrelated
campaign state remain untouched.

## 5. Failure semantics

Each failing gate gets a bounded, distinct repair attempt with preserved failing evidence. Source
repairs return to G1/G2; migration or canary failures roll back and park G4; deployed trace failures
return to the responsible source/config gate; empirical stop conditions quarantine the run and require
a new protocol version. A genuine unavailable human/data/authority dependency is reported precisely
after all independent work is exhausted; it is never rewritten as technical success.
