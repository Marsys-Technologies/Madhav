# TEST ARTEFACT — L1 INVENTIONS (Nikaṣa Phase 4, layer L1)

Flat register of every invention made while deriving `L1_INSTANCE_SKELETON.md`,
`GA_TAJAKA_BRIEF_TEST.md`, `GA_PRASHNA_BRIEF_TEST.md` from tiers 1–3 + census only.
Format: `INVENTION | <file §section> | <what> | <template clause quoted verbatim> | <proposed text>`

INVENTION | L1_INSTANCE_SKELETON §0.1 (and both briefs row 1) | The per-layer necessity set over P01–P24 / V01–V13 — which rows *cannot be answered without* L1; neither tier maps P/V to layers by necessity | "List the P-needs and V-journeys for which this layer is **necessary** — not "involved in", not "contributes to", but *cannot be answered without*." (tier 3 §0.1) | Add to tier 2 §3.1 (or §11 of tier 1) a per-layer column "necessary for P/V: …" derived from the DP-contract ↔ P/V mapping; a layer instance then narrows, never invents | INV-L1-01
INVENTION | L1_INSTANCE_SKELETON §0.3 | The seed and migration-pin readings of the DAG; the census emits only the live-registry view, so the mandated three-way reconciliation cannot be performed | "measured_by: registry depends_on reconciled across seed, migration pin AND live asset_registry — state all three and any disagreement" (tier 3 §0.3) | Extend the inspector census with `depends_on` as read from (a) registry seed, (b) migration pin, (c) live asset_registry, and emit the disagreement set per asset | INV-L1-02
INVENTION | L1_INSTANCE_SKELETON §1.1 / briefs row 7 | The deployed-vs-current-code half of the three-way baseline, per asset; the census carries registry + run history but no code-head comparison | "whether current code on any live head differs from what is deployed" (tier 3 §1.1) | Census gains a per-asset field: deployed schema hash vs newest live-head writer hash, with the commits named | INV-L1-03
INVENTION | L1_INSTANCE_SKELETON §2.1 / briefs row 3 | L1's life-event switch behaviour — what a *computation* layer emits when the switch is OFF; tiers define the switch but not per-layer ON/OFF semantics | "The life-event switch: what this layer may do when ON, what it emits when OFF, and the storage separation that makes OFF a selection rather than a rebuild." (tier 3 §2.1) | State in tier 2 §9.2, per layer: L1 computes identically under both states; the switch binds consumers (L2+), not the fact layer; detector: birth-fact immutability per (chart_id, input revision) | INV-L1-04
INVENTION | L1_INSTANCE_SKELETON §2.2 / briefs row 4 | The assignment of §3.4 presentation rows to L1 — tier 2 maps fields→contracts, never rows→layers; the reader derived it through the contract mapping | "including which §3.4 presentation rows this layer carries and which fields it hands onward for them" (tier 2 §13.3 item 6) | Add to tier 2 §3.4 a "carried by layer" column per row (conventions → L1 via DP01/DP03; intermediate quantities → L1 via DP03/DP04; dignity components → L1 via DP04; method/school, passed clauses, competing readings, typed chains, clock rows → L0/L2/L3 as named) | INV-L1-05
INVENTION | L1_INSTANCE_SKELETON §2.4 / briefs row 6 | The enumerated list of coverage obligations L1 owns; Product §3 names substance, not owned obligations; census reports width NOT_GENERIC on all 19 | "Which of the product's coverage obligations this layer owns, with prerequisites, variants, exceptions, negative cases and uncertainty. Each ends in one of the five states." (tier 3 §2.4) | Data plane §5 gains a per-layer obligation enumeration; the inspector declares each asset's universe so width is measurable | INV-L1-06
INVENTION | L1_INSTANCE_SKELETON §2.6 | Which of the sixteen entity classes L1 emits/accepts; the per-class independent-map census (`local_map_candidates: -1` is a non-result) | "Which of the sixteen entity classes this layer emits or accepts; for each, whether every name resolves through the controlled set … how many independent maps the layer's code carries (permitted: one)" (tier 3 §2.6) | Census emits per-class alias coverage and the map census per class (never a layer-level scalar); tier 2 names each layer's emitted/accepted classes | INV-L1-07
INVENTION | L1_INSTANCE_SKELETON §2.7/§4.4 row 13; both briefs row 13 | The per-asset named Jyotish concepts with the carriage check each invites. **C-9 CONFIRMED for L1**: census Carr.detector NO_DETECTOR ×19 ("which check applies is per-asset semantics"); tiers supply only the layer-level hint "Carr D3 dominates" (tier 4, L1 row) | "**the relevant Jyotish concepts** the asset touches, named, with the domain detector each invites (2.7, 5.2's menu)" (tier 3 §4.4) | The layer instance must carry, per asset, a (concept, check ∈ {D1,D2,D3}) pair; proposed L1 default: every pure-computation asset D3, any asset restating a classical clause D1, dual-authority assets D2 — stated as instance content, not left to brief authors | INV-L1-08
INVENTION | L1_INSTANCE_SKELETON §4.4 rows 8/12; both briefs rows 8/12 | Per-asset disposition (P/I/E/Q/C/H/R/U) and the preserved kernel; no rule maps census evidence to a disposition, and no tier states what must survive a rebuild | "- its disposition and its "must add" list (3.2, 3.3) … - **the preserved kernel** — what of the asset must survive any rebuild unchanged (from 3.2)" (tier 3 §4.4) | Tier 3 §3.2 gains an evidence→disposition decision rule (e.g. exercised + served + history FAIL ⇒ E; exercised + 0 rows + 0 modules ⇒ U pending §0.1); and §3.2 names each preserved kernel explicitly (table span/key, algorithm, conventions) | INV-L1-10
INVENTION | L1_INSTANCE_SKELETON §2.2 / §5.4 | Whether the presentation-parity acceptance test is the layer plan's own work: §12.2 marks the row [TRANSFERS] while tier 3 §5.4 test 4 requires it before acceptance — a layer plan cannot tell | "A [TRANSFERS] obligation is not a data-plane layer's to build alone, and a layer plan does not inherit it as its own work." (tier 2 §1) vs "4. **Presentation parity** holds for the layer's served surface." (tier 3 §5.4) | Tier 3 §5.4 test 4 gains: "run where the surface exists; where the owning plane is not built, record [TRANSFERS]-pending — never a pass, never a block" | INV-L1-11
INVENTION | L1_INSTANCE_SKELETON §1.1 | Ownership/counting/idempotency rules for one table with several producers (`chart_facts` ← 7 L1 writers; identical 421,096-row figure reported under each). **C-10/R06/R10 CONFIRMED for L1** | "For each: target table(s) — a **set**, not one pointer, for multi-table assets — row counts in production, columns, contract fields live, last build, and whether current code on any live head differs from what is deployed." (tier 3 §1.1) | Add the inverse rule: where several assets share a target, the layer plan names the table's counting owner, scopes count_sql per producer key, and bars any writer's Build check 6 from answering for the whole table | INV-L1-12
INVENTION | L1_INSTANCE_SKELETON §4.4 gate map (Idem) | The shared-table natural key for L1's delete-then-insert convention; `chart_facts` is keyed on fact_id but written by 7 writers — ga_nakshatra already carries ON CONFLICT against the layer convention (census Idem PARTIAL) | "`Idem` is delete-then-insert on chart × natural key; the load-bearing rule is "consumers refer to L1 facts, they do not recompute them"" (tier 4, Adapting per layer, L1 row) | Define per shared table: natural key = (chart_id, ayanamsha_id, fact-type/producer scope); each writer's delete scopes to its own fact-type set; state the scope set in the layer plan | INV-L1-13
INVENTION | L1_INSTANCE_SKELETON §1.1 | The `kind` of a writer-backed data asset with no declared target_table (ga_strength, ga_structural — census Build.target N/A); the closed kind list covers data/service/multi-table/rider/static, none of which fits "declares no target" | "kind: data | service (no table by design) | multi-table | rider (producer_covered) | static (migration-seeded)" (tier 4 §0) | Add `kind: data (target set undeclared)` as a defect-kind, or require check 3 to FAIL (not N/A) when asset_kind=data, has_writer=true, target_table=null | INV-L1-14
INVENTION | GA_TAJAKA_BRIEF_TEST §1.1 (also ga_prashna §1.1) | The declared universe for a varṣaphala year-lord computation (charts × years × lord-set); nothing in the documents declares it; census NOT_GENERIC | "Declare the universe first, from a source or from the ontology: how many instances the concept has … and which dimensions each instance should carry." (tier 4 §1.1) | The layer instance §2.4 names each asset's universe source (ontology class or text); the asset brief cites it, never declares it ad hoc | INV-L1-16
INVENTION | GA_PRASHNA_BRIEF_TEST §1 / §4 Build | The verdict for an EMPTY data table with state=lit, rows_written=0: check 6 covers populated tables and services; an empty writer-backed data asset is the uncovered third case (ga_prashna: 51 runs, 0 rows, 0 modules) | "**completion honesty** | the build record agrees with the live count. `rows_written = 0` against a populated table is a status with no measurement behind it, and for a service it is indistinguishable from a writer that produced nothing" (tier 4 §4.2 check 6) | Check 6 gains: for a data asset, rows_written=0 with 0 live rows is PARTIAL unless the layer plan declares the asset empty-by-design; "empty by design" is a layer-instance claim with its own detector | INV-L1-17

Observations (recorded, not inventions — no document failed to provide these):
- Census-internal contradiction on every asset: Build.count_integrity PASS ("count_sql=yes") alongside Build.completion N/A ("no count_sql") — inspector defect, not a template gap.
- `local_map_candidates: -1` — the census's own non-result; the template gap it exposes is INV-L1-07.
- Earn.build_record / Cost.baseline FAIL on all 19 L1 assets (rows_per_second NULL): tier 4 §1 says "not instrumented" is a legal value, the census scores it FAIL — verdict-scale disagreement between census and tier 4, to Phase 5.
- 17 inventions total (INV-L1-01 … 08, 10 … 14, 16, 17; INV-L1-09 and -15 were folded into -03/-observations and not used).
