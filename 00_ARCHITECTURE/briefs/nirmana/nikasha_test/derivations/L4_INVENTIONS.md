# L4 — Invention register (TEST ARTEFACT, campaign nikasha-test Phase 4)

Format: `INVENTION | where | what had to be invented | template clause quoted verbatim | proposed text`
Layer: L4 Phala. Census: L4_prod_20260926.json (production read-only), cross-checked against L4_sandbox_20260926.json (identical verdicts).

INVENTION | L4_INSTANCE_SKELETON.md §0.1 | the P-need and V-journey rows L4 is necessary for, and the loss-distinction per row | "inherits:    Product §2 (P01-P24), Data plane §2 (V01-V13)" (tier-3 §0.1) | Add to tier-2 §13.3 (or a new §13.4): a per-layer assignment table mapping each P01–P24 need and V01–V13 journey to the layer(s) necessary for it, so 0.1 is a lookup, not a judgement.
INVENTION | L4_INSTANCE_SKELETON.md §0.2 | the "what it computes that existing software does not" claim | "Name what it computes that existing software does not, and what it hands the reasoning layer to read across." (tier-3 §0.2) | Tier-2 §6.5 (Phala narrative) should state the distinct-from-existing-software claim explicitly per layer.
INVENTION | L4_INSTANCE_SKELETON.md §0.3 | which DP contracts L4 produces (vs consumes), and the edge type of each L4 edge | "- **Receives from:** upstream layers, by edge type, by DP contract.\n- **Hands onward to:** downstream layers, by edge type, by DP contract." (tier-3 §0.3) | Tier-2 §7.1 gains a per-layer rollup: for each layer, contracts produced and contracts consumed, each with its §3.2 edge type.
INVENTION | L4_INSTANCE_SKELETON.md §1.1 | per-asset "current code on any live head differs from deployed" and the residual/shared/historical kind classification | "For each: target table(s) — a **set**, not one pointer, for multi-table assets — row counts in production, columns, contract fields live, last build, and whether current code on any live head differs from what is deployed." (tier-3 §1.1) | Name the instrument (a git-head scan + registry field for kind) in §1.1's measured_by, and have the census emit both.
INVENTION | L4_INSTANCE_SKELETON.md §2.1 | a named detector per correctness rule (e.g. for "L4 must not rewrite the original forecast") | "**For each rule, the detector.** A rule with no detector is a wish." (tier-3 §2.1) | Tier-3 §2.1 should state, per layer, which existing detector discharges the layer's §8.1 row (for L4: the DP15a emission-freeze check), or record NO_DETECTOR explicitly as the expected fill.
INVENTION | L4_INSTANCE_SKELETON.md §2.2 | which §3.4 presentation rows L4 carries and which fields it hands onward | "including which §3.4 presentation rows this layer carries and which fields it hands onward for them" (tier-2 §13.3 item 6) | Tier-2 §3.4 gains a sixth column assigning each presentation row to its carrying layer(s).
INVENTION | L4_INSTANCE_SKELETON.md §2.2 | whether the presentation-parity test is L4's own work, given it is marked [TRANSFERS] | "measured_by: presentation-parity test (Data plane §12.2): both renderings from the consumed reading package, no recomputation, identical finding / confidence / uncertainty" (tier-3 §2.2) | Tier-3 §2.2 must carry the [TRANSFERS] marking: the layer states the fields it carries; the parity test itself belongs to the retrieval/conversation plane's artefact.
INVENTION | L4_INSTANCE_SKELETON.md §2.3 | the declared use of every consumed contract | "Every consumed input declares its use — calculation, applicability, counter-evidence, uncertainty, interpretation, exclusion, navigation, evaluation. **A citation with no declared use is not a contract.**" (tier-3 §2.3) | Tier-2 §7.1 gains a declared-use column per contract, so instances inherit rather than assign.
INVENTION | L4_INSTANCE_SKELETON.md §2.4 | which product coverage obligations L4 owns | "Which of the product's coverage obligations this layer owns, with prerequisites, variants, exceptions, negative cases and uncertainty." (tier-3 §2.4) | Tier-2 §5 gains a per-layer ownership column; layer instances also declare each asset's width universe (feeds census Complete.width, register R22).
INVENTION | L4_INSTANCE_SKELETON.md §2.5 | a topological order and the frozen definition revision for egate scoping | "measured_by: topological sort of the reconciled depends_on from 0.3; cycle check; cross-layer gates via egate.sql scoped to the CURRENT frozen definition revision" (tier-3 §2.5) | Depends on the 0.3 three-source reconciliation; the census should emit the reconciled DAG and the revision id.
INVENTION | L4_INSTANCE_SKELETON.md §2.6 | which of the sixteen entity classes L4 emits or accepts | "Which of the sixteen entity classes this layer emits or accepts; for each, whether every name resolves through the controlled set" (tier-3 §2.6) | Tier-2 §4.1 gains a layer × entity-class matrix (emit / accept / untouched).
INVENTION | L4_INSTANCE_SKELETON.md §2.7 + §4.4 row 13 (C-9/R09 — CONFIRMED for L4, occurrences five and six) | the per-asset carriage check (a/b/c) and the per-asset Jyotish concepts | "For each obligation the layer owns (§2.4), state which of a–c applies, the detector, and its current result" (tier-3 §2.7); and "**the relevant Jyotish concepts** the asset touches, named, with the domain detector each invites (2.7, 5.2's menu)" (tier-3 §4.4) | Per register R09's proposed reopen: §2.7 gains a per-asset assignment table, or the template states the brief author chooses and records why. Census confirms operationally: Carr.detector NO_DETECTOR on all 9 L4 assets.
INVENTION | L4_INSTANCE_SKELETON.md §4.4 rows 3, 8, 12 | L4's switch behaviour; per-asset dispositions; per-asset preserved kernels | "- its correctness rules and switch behaviour (2.1)" and "- its disposition and its \"must add\" list (3.2, 3.3)" and "**the preserved kernel** — what of the asset must survive any rebuild unchanged (from 3.2)" (tier-3 §4.4) | Rows 8/12 are transitively blocked by the absent ablation harness (1.2–1.4); the template should state the fallback fill ("harness absent — disposition provisional") so it is recorded, not invented. Switch behaviour needs one sentence per layer in tier-2 §9.2.
INVENTION | PH_MUHURTA_BRIEF_TEST.md §0 | role: manifestation | "role:             manifestation | temporal | neither (supplies what both rest on)   # from layer §4.4" (tier-4 §0) | §4.4's thirteen rows contain no role row; either add row 14 (role per asset) to tier-3 §4.4 or drop the "# from layer §4.4" citation.
INVENTION | PH_MUHURTA_BRIEF_TEST.md §4 (both briefs) | the Null-gate verdict — the inspector runs no Null check, so the gate cannot be filled from the census | "| **Null** · honest null | always | an underivable value is emitted as null, not as a plausible default (§N.7 item 6) | | | |" (tier-4 §4) | The inspector gains a Null check (e.g. default-valued columns vs null proportion), or the gate map states the detector is per-asset and the brief must name it.
INVENTION | PH_MUHURTA_BRIEF_TEST.md §4 / PH_RECTIFICATION_BRIEF_TEST.md §4 | the Narr-gate applicability reading | "| **Narr** · narration fidelity | if it emits prose | prose restates cited facts and never re-derives them (§N.7) | | | |" (tier-4 §4) | The census should emit "emits prose: yes/no" per asset so the N/A-with-reason is measured, not inspected by hand.
INVENTION | PH_MUHURTA_BRIEF_TEST.md §5 (both briefs) | gap_id namespace and owner values for ledger rows | "`asset · gap_id · kind · criterion · what · change · detector · owner · gate · state · ts`" (tier-4 §5) | State the gap_id allocation rule (per-layer prefix? next-free integer?) in tier-4 §5 or the ledger _schema line.
INVENTION | PH_RECTIFICATION_BRIEF_TEST.md §0 | a kind value for "writer exists but is packaged (__init__.py) and invisible to the inspector" | "kind:             data | service (no table by design) | multi-table | rider (producer_covered) | static (migration-seeded)" (tier-4 §0) | Not a template defect primarily — the inspector's registered check must search writer packages (fix in asset_census.py); if packaged writers remain legal, add kind value or a writer_shape field.

## Non-invention findings recorded while deriving (system findings, not register rows here)

- C-10/R06/R10 (shared multi-producer tables): REFUTED for L4 — 9 distinct target_tables; `grep -l "INSERT INTO phala_"` over platform/python-sidecar/pipeline/orchestrator/writers/** matches only each table's own ph_* writer.
- ph_rectification Build.registered FAIL is an inspector FALSE POSITIVE: @register("ph_rectification") exists at writers/ph_rectification/__init__.py:246. Explains registered_ids=8 vs registry_has_writer=9 for L4.
- ph_muhurta: Build.count_integrity PASS ("count_sql=yes") contradicts Build.completion N/A ("no count_sql") — inspector internal inconsistency.
- [TRANSFERS] test: an L4 plan cannot tell that Dens.served FAILs (serving modules without density_contract) are not L4's own work — tier-3 §5.2 lists Dens as the layer's gate while tier-2 §1 says [TRANSFERS] obligations are not the layer's to build. Ambiguity confirmed.
