# L2 — INVENTIONS (flat register-row source)

**TEST ARTEFACT.** One line per invention across `L2_INSTANCE_SKELETON.md` (A),
`BO_DRISHTI_BRIEF_TEST.md` (B), `BO_SAMVADA_BRIEF_TEST.md` (C). Format:
`INVENTION | <file §section> | <what had to be invented> | <template clause quoted verbatim> | <proposed text>`.
Clause quotes are verbatim from `LAYER_DEFINITION_AND_STRATEGY_TEMPLATE_v1_0.md` (tier 3),
`ASSET_ELEVATION_TEMPLATE_v2_0.md` (tier 4), or tier 2 as marked. Inspector-side defects (R20, R42,
R44, R45, R46) are cited as caveats in A–C, not listed here — they already have register rows.

INVENTION | I-01 | A §0.1 (also B/C §0.1 row 1) | The P-needs/V-journeys L2 is *necessary* for — the necessity assignment itself; tiers 1–2 enumerate P01–P24 and V01–V13 but assign none to any layer | tier-3 §0.1: "List the P-needs and V-journeys for which this layer is **necessary** — not "involved in", not "contributes to", but *cannot be answered without*." | tier 2 gains a layer × P/V necessity table (or a derivation rule from §3.1's owned questions); skeleton then narrows, never assigns
INVENTION | I-02 | A §0.2 | The paragraph "what it computes that existing software does not, and what it hands the reasoning layer" for L2 — only the §3.1 table rows exist; no L2 narrative was derivable | tier-3 §0.2: "In one paragraph: the distinctions this layer makes **earnable** that were not earnable before it. Name what it computes that existing software does not, and what it hands the reasoning layer to read across it." | tier-2 §6.3's Bodha narrative promoted to canonical content the instance quotes (this reader had tier-2 §6.x at summary level only)
INVENTION | I-03 | A §0.3, §1.1 | The three-source dependency reconciliation; the census reads the live registry only, emits edge counts not edge lists, and never sees the seed or the migration pin | tier-3 §0.3: "measured_by: registry depends_on reconciled across seed, migration pin AND live asset_registry — state all three and any disagreement" | the census emits per-asset `depends_on` edge lists plus a seed/pin/live three-way diff per layer
INVENTION | I-04 | A §1.1 | Expression of a shared table with several producers: `bodha_msr_signals` has SEVEN registered L2 producers (bo_arudha, bo_laksana, bo_laksana_rerank, bo_nakshatra_semantic, bo_special_lagna, bo_sudarshana, bo_vargottama_dhana; the middle two share one writer file) and the census credits each with all 150,724 rows / 85 columns. **C-10/R06/R10 CONFIRMED for L2, sharper than L0's four-producer case** | tier-3 §1.1: "For each: target table(s) — a **set**, not one pointer, for multi-table assets — row counts in production, columns, contract fields live, last build" | register R06's own proposal, confirmed as sufficient in scope: "a shared table declares its producers and each producer's `count_sql` is scoped to its own rows" — add: two assets sharing one writer file must declare the split
INVENTION | I-05 | A §1.1, §4.4 row 7 (B/C row 7) | "deployed vs current code" — the risk half of the three-way baseline; the census reads one head and live tables, never the comparison | tier-3 §1.1: "whether current code on any live head differs from what is deployed" | the census gains a deployed-vs-head diff (schema fingerprint + writer file hash per asset)
INVENTION | I-06 | A §1.3/§1.5 (B/C row 10) | L2's synergistic term — the layer's *defining* term per the adaptation table — has no harness and no synergy binding; any fraction would be invented, and without it the 1.5 shortfall (the elevation delta) is uncomputable | tier-3 §1.3 inherits: "the layer's synergy binding if one exists"; §1.5: "Record the synergistic term as a fraction of the total **only where an ablation harness exists to produce it** … Do not invent a fraction to fill the field."; adaptation table: "L2 Bodha | … 1.3 is where whole-chart reading lives or fails" | tier 3 prescribes a minimum synergy harness per chart-product layer (e.g. the seam-by-seam measurement named in §1.5 made mandatory content, with a named instrument)
INVENTION | I-07 | A §1.4 (B/C row 11) | Consumer-verified evidence states (consumed / traceably transformed / value-evaluated); the census measures at the producer and the module list only | tier-3 §1.4: "measured_by: for each produced contract, its position on source-present → qualified → consumed → traceably transformed → served → value evaluated, verified at the consumer" | a consumer-side probe per produced DP contract (a query at the L3/inquiry reader confirming the fields it actually read)
INVENTION | I-08 | A §2.1 (B/C row 3) | L2's life-event-switch behaviour: what the layer emits ON vs OFF and its storage separation; parents define the switch globally only | tier-3 §2.1 inherits: "Product §8.1 (this layer's row), §13; Data plane §9.2 (the switch and the storage separation)" | tier-2 §9.2 gains a per-layer switch-behaviour table (six rows), or tier-3 §2.1 states the instance defines it under named constraints
INVENTION | I-09 | A §2.2 (B/C row 4) | Which §3.4 presentation rows L2 carries; partially derivable via contract ownership (competing readings → DP06+DP02, typed chains → DP06) but the row-to-layer boundary is judgment | tier-2 §13.3 item 6: "including which §3.4 presentation rows this layer carries and which fields it hands onward for them" (demanded of the layer, stated for none) | tier-2 §3.4's field→contract table gains a third column: carrying layer
INVENTION | I-10 | A §2.3 (B/C row 5) | Per-asset mapping to DP contracts with declared use; the census carries no asset→contract field and "declared use" exists nowhere as data | tier-3 §2.3: "Every consumed input declares its use — calculation, applicability, counter-evidence, uncertainty, interpretation, exclusion, navigation, evaluation. **A citation with no declared use is not a contract.**" | the registry gains `produces_contracts` / `consumes_contracts` columns per asset; the census reports them
INVENTION | I-11 | A §2.4 (B/C row 6) | Which product §3/§5 coverage obligations L2 owns; no per-layer assignment exists; census confirms from below (Complete.width NOT_GENERIC 23/23, no declared universes) | tier-3 §2.4: "Which of the product's coverage obligations this layer owns, with prerequisites, variants, exceptions, negative cases and uncertainty." | tier-2 §5's domain obligations carry an owner-layer column (joins R22: universes need a place to be declared)
INVENTION | I-12 | A §2.6 | Which of the sixteen entity classes L2 emits/accepts, and the per-class independent-map census (census header: `local_map_candidates: -1`, not measured) | tier-3 §2.6: "Which of the sixteen entity classes this layer emits or accepts; for each, whether every name resolves through the controlled set … how many independent maps the layer's code carries (permitted: one)" | tier-2 §4.1 or the registry assigns entity classes per layer; the inspector's map census is implemented (replace −1)
INVENTION | I-13 | A §2.7, §4.4 row 13 (B row 13: chose D1; C row 13: chose D3) | The per-asset carriage-check assignment. **C-9/R09 CONFIRMED for L2 — fifth and sixth confirmations, first outside L0.** Census corroborates: Carr.detector NO_DETECTOR 23/23 with "which check applies is per-asset semantics" — the inspector says per-asset, the template provides only layer scope | tier-3 §2.7: "For each obligation the layer owns (§2.4), state which of a–c applies, the detector, and its current result"; §4.4: "the relevant Jyotish concepts the asset touches, named, with the domain detector each invites (2.7, 5.2's menu)" | register R09's proposal stands and is now confirmed on a second layer: "§2.7 gains a per-asset assignment table, or the template states the brief author chooses and records why"
INVENTION | I-14 | A §2.5 | The intra-layer topological order and the "CURRENT frozen definition revision" the gate check must be scoped to — edge lists and the revision id are in no provided source | tier-3 §2.5: "measured_by: topological sort of the reconciled depends_on from 0.3; cycle check; cross-layer gates via egate.sql scoped to the CURRENT frozen definition revision" | the census emits the edge list it already resolves, and names the frozen definition revision it evaluated against
INVENTION | I-15 | A §4.4 row 1 | Narrowing 0.1's P/V rows per asset — impossible while 0.1 itself is invented (I-01) | tier-3 §4.4: "the P-needs and V-journeys the asset serves (from 0.1, narrowed)" | resolved transitively by I-01's fix
INVENTION | I-16 | A §4.4 row 8 (B/C row 8) | Per-asset dispositions and must-add lists; require Part 1 measurements, and 1.2/1.3 are unmeasured/unmeasurable (I-06) — any P/I/E/Q/C/H/R/U letter is supplied, not derived | tier-3 §4.4: "its disposition and its 'must add' list (3.2, 3.3)"; §3.2: "For every asset and service in 1.1, one of the data plane's eight dispositions, with the evidence from Part 1 that justifies it" | a minimum Part-1 measurement floor below which dispositions are recorded U (unresolved) by rule, not by author choice
INVENTION | I-17 | A §4.4 row 12 (B/C row 12) | The per-asset preserved kernel; sourced "(from 3.2)" which is itself underivable (I-16); nothing in tiers or census defines what must survive a rebuild | tier-3 §4.4: "**the preserved kernel** — what of the asset must survive any rebuild unchanged (from 3.2)" | tier-4 §0.1 row 12 is re-sourced: the kernel is declared in the registry per asset (grain + upsert/delete key + immutable columns), the instance reads it
INVENTION | I-18 | A §2.1 | A detector per correctness rule — none exists for any L2 rule; the template's measured_by cannot be satisfied, and naming one would be inventing it | tier-3 §2.1: "measured_by: a detector per rule, named here, that can report the rule violated … **For each rule, the detector.** A rule with no detector is a wish." | a detector registry per layer-rule (even if every entry opens as NO_DETECTOR, the template already scores that honestly)
INVENTION | I-19 | A §4.4 row 2 | Per-asset weighting of L2's two scored obligations; the row is provided at layer altitude only | tier-3 §4.4: "the ten obligations it is scored on (from 0.2)" | acceptable as-is if the template says layer-uniform scoring is the default; today neither permitted nor forbidden
INVENTION | I-20 | A [TRANSFERS] assessment | The separation of L2's own work from transferred work: §13.3 items 1 and 6 and template §2.2's measured_by embed [TRANSFERS] material (DP10/DP11 live in tier-2 §8; Presentation parity is a [TRANSFERS] §12.2 row; tier-4 §1.2 adds a third location) without carrying the mark at the point of obligation | tier-2 §1: "A [TRANSFERS] obligation is not a data-plane layer's to build alone, and a layer plan does not inherit it as its own work." | every §13.3 item, DP contract row and §12.2 row carries its [TRANSFERS] tag at the point where a layer plan reads it, not only where it is defined
INVENTION | I-21 | B/C §0 identity block | The `role` field value ("neither") — no tier assigns manifestation/temporal/neither per L2 asset | tier-4 §0: "role: manifestation | temporal | neither (supplies what both rest on)   # from layer §4.4" | layer §4.4 gains the per-asset role assignment it is cited as sourcing (L2 is "neither" for most assets, but that is currently the author's inference)
INVENTION | I-22 | B/C §0.2 | The asset-purpose paragraph beyond one line derivable from the §3.1 layer row and census shape | tier-4 §0.2: "written so a reader who knows the product but not this asset can say why removing it would cost something" | the registry carries a one-line purpose per asset (it has catalog_status; purpose is the missing twin)
INVENTION | I-23 | B/C §1 consumers | Declared-vs-actual consumer comparison; the census has module lists but no grep population over readers | tier-4 §1: "Consumers, declared vs actual: `depends_on` edges in; code that actually reads it, writer-side and serving-side, grep population stated. **The gap between the two is a finding, not a footnote**" | the inspector emits both edge directions and a code-reader census per asset
INVENTION | I-24 | B/C §1.1 | The declared universe the width census divides by — 23/23 L2 assets read NOT_GENERIC; there is no place to declare one | tier-4 §1.1: "Declare the universe first, from a source or from the ontology"; census text: "no declared universe for this asset — declaring one is the first width gap" | R22's fix, confirmed needed layer-wide on L2: a registry/instance field for the declared universe
INVENTION | I-25 | B §3 | The testable content of "interpretive fidelity" for this asset; the parents define obligations at layer altitude | tier-4 §3: "what satisfied means for this asset specifically"; tier-1 §11 names the obligation but not its per-asset test | layer §3.1 carries a per-obligation test template the brief instantiates
INVENTION | I-26 | C §0 | The `kind` value for a view-target asset — the closed list has no place for it (not data/service/multi-table/rider/static) | tier-4 §0: "kind: data | service (no table by design) | multi-table | rider (producer_covered) | static (migration-seeded)" | the kind vocabulary gains `view` (projection over other assets' tables; idempotency and count checks re-scoped accordingly) — extends R14's service clause
INVENTION | I-27 | C §1 storage | Whether the asset is empty-by-design or populated: the census carries both (live_rows 0 via stub count_sql vs Complete.depth 15 rows) | tier-4 §1: "rows by the asset's own `count_sql` (the cockpit instrument), floor, delta; … whether `count_sql` and `target_table` are truthful" | R46's fix (count the view), plus the inspector flags stub count_sql (`SELECT 0`) as a finding class of its own
INVENTION | I-28 | C §1 producer | What the writer actually builds (the view, or a table it reads) — Idem.pattern PARTIAL "likely delegates" and delegation is not followed | tier-4 §1: "Producer: writer module and `@register` id · or the asset it rides on · or 'static, migration N' · or 'service'" | R20's fix (follow delegation into the seeder), confirmed load-bearing for L2's three delegating writers (bo_bimba, bo_pramana_mapa, bo_samvada)
INVENTION | I-29 | C §4/§1 | Verdicts for Ldgr and Vocab — the census emits NO ROW for either on this asset (layer aggregates: Vocab 22/23, Ldgr 16/23), silently exempting a view asset instead of N/A-with-reason | tier-4 §4: "A conditional gate that does not apply is disposed of with an explicit `N/A` and one line of reason. It is never silently dropped." | the inspector emits every gate row for every asset, N/A-with-reason included (a missing row is a harness bug, not a verdict)
INVENTION | I-30 | C §3 | The testable content of "completeness" for a digest: a view-vs-source parity check no template defines | tier-4 §3 (as I-25) + tier-3 §2.4 five-states rule | for `kind: view` assets, §3 specialises completeness as a parity query between the view and its source tables
