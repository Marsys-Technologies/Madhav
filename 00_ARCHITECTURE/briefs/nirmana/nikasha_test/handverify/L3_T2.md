# L3 (Kāla) — T2 hand-verification

- Date: 2026-09-26. Census: `census/L3_sandbox_20260926.json` (23 assets, generated 2026-09-26T19:48+05:30,
  run against the local sandbox, not production — see sampling caveat).
- Production environment (ground truth for code/registry/run-history):
  `source /Users/Dev/madhav-l3/dbenv.sh && export PGPORT=5433`; `SHOW default_transaction_read_only;` → **on**
  (verified first). All production queries read-only.
- Sandbox environment (the DB the census actually measured):
  `env PGOPTIONS= /opt/homebrew/bin/psql -h …/nikasha_test/.sandbox -p 54329 -U sandbox -d nikasha_sandbox`.
- Repo greps against `/Users/Dev/madhav-nikasha/platform` (writers in
  `platform/python-sidecar/pipeline/orchestrator/writers/` and `platform/python-sidecar/services/`,
  capability modules in `platform/src/lib/retrieval/registry/layers/L3_kala/`).
- The L3 census could not run on production (R40/R41: Vocab.identity duplicate-count over kala_field
  exceeds the 180 s psql timeout and kills the layer). Manual production ground truth already established:
  kala_field duplicates under (chart_id, event_class, segment_index) = 0.
- **Sampling caveat**: sandbox tables >50k rows were deterministically sampled to 5% — includes L3's
  kala_activation, kala_activation_predicates, kala_field, kala_taranga. Count-dependent cells
  (Complete.depth, Cost.baseline rows_written, Build.completion) for those tables are verified against
  sandbox contents and marked "sampled".

## Sampling

L3 kinds present: **writer-backed data** (19 assets with target_table), **writer-backed service with no
table** (ka_dasha_kala, ka_graha_sancara, ka_muhurta_seva, ka_tulana — `asset_kind='service'`,
target_table NULL), **multi-asset shared table** (kala_gochara_windows is the target of both ka_gochara
and ka_gochara_sweep), and **registered-FAIL** assets (12 of 23, writer discovered via services/ package
or retired).

Kinds **absent** in L3: no-writer static/migration-seeded (registry `has_writer=t` for all 23),
empty-by-design (no live-0 data table), has-writer-but-never-run (layer field
`never_exercised_with_writer = []`). Vocab.alias and Count.floor checks are absent from the census output
for every L3 asset (alias is L0-only per protocol; floor is a coverage gap — see observations).

Sampled **11 assets**: ka_avadhi (registered PASS, latest-run error), ka_bhavishya_lekha (dep_liveness
FAIL), ka_kalasutra (sampled table kala_activation), ka_kshetra (registered FAIL via services/ package +
CONSTANT indirection, kala_field), ka_yojaka (sampled table kala_activation_predicates), ka_gochara +
ka_gochara_sweep (shared table; one PASS-registered, one RETIRED/tombstoned), ka_dasha_kala +
ka_muhurta_seva + ka_tulana (service, no table; registered FAIL), ka_taranga (sampled table, dep FAIL).

## Asset × check verdict table

A = AGREE, D = DISAGREE (verdict-level), D* = verdict defensible but census `measured` string factually
wrong, — = check absent. "sampled" marks count-dependent cells on 5%-sampled tables.

| asset | registered | contract | Idem | target | dag | count_integ | completion | Earn | Cost | depth | identity | Ldgr | Dens | exercised | history | dep_live | width | Carr | Reach |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ka_avadhi | A | A | A | A | A | A | **D** (R42) | A | A (R44) | A | A | — | A | A | A (D* err-text) | A (D*) | A | A | A |
| ka_bhavishya_lekha | A | A | A | A | A | A | **D** (R42) | A | A (R44) | A | A | A | A | A | A | A | A | A | A |
| ka_kalasutra | A | A | A | A | A | A | **D** (R42) | A | A | A (sampled) | A (sampled) | A (sampled) | A | A | A | A (D* list) | A | A | A |
| ka_kshetra | **D** (R43) | **D** (R43) | **D** (R43) | A | A | A | **D** (R42) | A | A | A (sampled) | A (sampled) | — | A | A | A (D* err-text) | A (D*) | A | A | A |
| ka_yojaka | A | A | A | A | A | A | **D** (R42) | A | A (R44) | A | A | — | A | A | A | A (D*) | A | A | A |
| ka_gochara | A | A | A | A | A | A | **D** (R42) | A | A | A | A | — | A | A | A | A | A | A | A |
| ka_gochara_sweep | A (D*) | A | A | A | A | A | **D** (R42) | A | A | A | A | — | A | A | A | A | A | A | A |
| ka_dasha_kala | **D** (R43) | **D** (R43) | A (D*) | A | A | A | A | A | A | — | — | — | A | A | A | A | A | A | A |
| ka_muhurta_seva | **D** (R43) | **D** (R43) | A (D*) | A | A | A | A | A | A | — | — | — | A | A | A | A | A | A | A |
| ka_tulana | **D** (R43) | **D** (R43) | A (D*) | A | A | A | A | A | A (R44) | — | — | — | A | A | A | A | A | A | A |
| ka_taranga | A | A | A | A | A | A | **D** (R42) | A | A (R44) | A (sampled) | A (sampled) | — | A | A | A | A (D* list) | A | A | A |

Cells checked: **194**. Agreements: **177**. Verdict disagreements: **17** (4 registered + 4 contract +
1 Idem + 8 completion). Evidence-inaccurate-but-verdict-defensible: 11 (see notes).

## Per-check evidence

- **Build.registered** (11 cells: 7 A, 4 D): `grep -rn "@register(['\"]ka_<id>" platform/ --include=*.py`.
  Hits in `writers/ka_avadhi.py:175`, `ka_bhavishya_lekha.py:78`, `ka_kalasutra.py:23`, `ka_yojaka.py:61`,
  `ka_gochara.py:180`, `ka_taranga.py:74` (all match registry has_writer=t → PASS ✓). Census FAILed
  ka_kshetra / ka_dasha_kala / ka_muhurta_seva / ka_tulana as "no @register found", but live decorators
  exist: `services/ka_kshetra/writer.py:268` (`@register(ASSET_ID)`, ASSET_ID='ka_kshetra' — constant
  indirection), `services/ka_dasha_kala/writer.py:113` and `services/ka_tulana/writer.py:87` (inside
  `_build_writer_class()`, invoked at module level: `KaDashaKalaWriter = _build_writer_class()`), and
  `services/ka_muhurta_seva/writer.py:72` (module level). Each `writers/ka_*.py` shim side-effect-imports
  the services writer, so registration fires on orchestrator import. Registry has_writer=t agrees → truth
  is PASS → **4 DISAGREE, all R43 instances** (both R43 sub-patterns present: CONSTANT indirection and
  package-directory writers). For ka_gochara_sweep the census FAIL is **correct in substance**: the
  decorator exists (`services/ka_gochara_sweep/writer.py:256`) but MR-09 deliberately removed the import
  (shim `writers/ka_gochara_sweep.py:45,49` is a tombstone, `__all__=[]`, package `__init__` no longer
  imports writer.py; only tests import it), while the registry row still says has_writer=t and
  catalog_status=RETIRED → FAIL verdict AGREE; measured string "no @register found" is literally false
  (D* evidence note).
- **Build.contract** (11 cells: 7 A, 4 D): all six PASS-verdict writers subclass WriterBase, define
  `run(ctx)` (ka_gochara additionally has plan_substeps/run_substep with `run()` as a thin driver over
  them — census "declares both entry points (note, not a violation)" accepted), zero `db_conn.commit/close`,
  zero `INSERT INTO asset_throughput`. Census N/A "no writer file" for ka_kshetra/ka_dasha_kala/
  ka_muhurta_seva/ka_tulana is wrong — the services writers exist and are conformant (WriterBase subclass;
  run(ctx) or plan_substeps+run_substep; ka_tulana writer even comments "DO NOT call conn.commit()") →
  truth PASS → **4 DISAGREE (R43-derived)**. ka_gochara_sweep N/A accepted (writer retired; reason string
  "no writer file" still inaccurate — file exists, unwired).
- **Idem.pattern** (11 cells: 10 A, 1 D): PASS verdicts reproduce — `DELETE FROM` in ka_avadhi.py:325,
  ka_bhavishya_lekha.py:403, ka_kalasutra.py:33, ka_yojaka.py:445, ka_gochara.py:336, ka_taranga.py:180
  (delete-then-insert; several also have ON CONFLICT safety nets). Census N/A for ka_kshetra is wrong:
  `services/ka_kshetra/writer.py` has 12+ `ON CONFLICT … DO UPDATE` sites plus DELETE FROM (lines 639,
  771, 836, 985, 2433, …) → truth PASS → **1 DISAGREE (R43-derived)**. Service-asset N/As (ka_dasha_kala,
  ka_muhurta_seva, ka_tulana) are semantically apt (service writers emit 0 data rows) though the stated
  reason "no writer file" is false (D* note).
- **Build.target** (11/11 A): registry `target_table` = census value for all; `to_regclass('public.<t>')`
  resolves in the sandbox for all 8 data targets; services correctly N/A (target NULL, asset_kind
  service, has_writer=t confirmed in registry).
- **Build.dag** (11/11 A): `depends_on` cardinalities match census edge counts exactly
  (3/4/3/9/7/8/1/1/0/3/5); every dep of the 11 sampled assets resolves in `asset_registry` (unnest …
  NOT IN (SELECT asset_id …) → empty). No cycle in chains ≤ 4 deep.
- **Build.count_integrity** (11/11 A): count_sql AND integrity_check_sql non-null for all 8 data assets;
  the 3 services have count_sql NULL + integrity_check_sql non-null (service_health probe) → PARTIAL ✓.
  This directly contradicts the same census's Build.completion "no count_sql" on the data assets.
- **Build.completion** (11 cells: 3 A, 8 D): census N/A "no count_sql" for all 8 data assets, but
  `SELECT count_sql IS NOT NULL FROM asset_registry` = t for all 8 (e.g. ka_avadhi
  `SELECT COUNT(*) FROM kala_avadhi WHERE chart_id=$1`) → stated precondition false → **8 DISAGREE, all
  R42 instances** (N/A-despite-count_sql). Additionally the quoted build states are non-latest for some
  assets (ka_avadhi quoted 'lit' but latest throughput row is error@2026-09-11). Multi-table note
  (R42 second half): ka_gochara and ka_gochara_sweep share kala_gochara_windows with different
  generation filters ('4.0' vs 'v1') — count_sql self-comparison hazard confirmed present. The 3 service
  N/As are correct (count_sql genuinely NULL) → AGREE.
- **Earn.build_record** (11/11 A) and **Cost.baseline** (11/11 A): `asset_throughput` (both DBs; sandbox
  is a near-identical snapshot with minor date skew) — `rows_per_second` NULL and `measurement_count=0`
  on every row of all 11 → FAIL verdicts reproduce. **R44 instances**: census quotes non-latest rows for
  ka_avadhi (lit/1160/2026-08-12 vs latest error/1169/2026-09-11), ka_bhavishya_lekha (quoted 08-12 vs
  latest 08-13), ka_yojaka (lit/50171/08-12 vs latest stale/50678/09-10), ka_tulana (08-12 vs 08-13),
  ka_taranga (08-12 vs 08-13). Verdicts unaffected. ka_kalasutra (335403@08-13), ka_kshetra
  (error/1183134@09-11), ka_gochara (87@09-10), ka_gochara_sweep (error/1705@08-12), ka_dasha_kala
  (lit/0@09-11), ka_muhurta_seva (lit/0@08-02) match the latest row.
- **Complete.depth** (8/8 A): full per-column census (information_schema-generated
  `SELECT count(*), count(col), …`) on the sandbox reproduces census numbers **exactly**: kala_avadhi
  3620/12/12/never []; kala_bhavishya 100/18/17/never [outcome_notes]; kala_activation 16925/15/10/never
  [] (sampled); kala_field 549797/23/22/never [refinement_residual] (sampled);
  kala_activation_predicates 7525/11/11/[] (sampled); kala_gochara_windows 40117/34/21/never
  [threshold_lambda, threshold_percentile, implied_density, base_rate_cited] (identical for ka_gochara
  and ka_gochara_sweep, as expected for a shared table); kala_taranga 13963/9/9/[] (sampled).
- **Vocab.identity** (8/8 A): duplicate-group queries on each declared key return 0 in the sandbox:
  kala_avadhi (chart_id,system_id,level_n,period_start); kala_bhavishya, kala_activation,
  kala_activation_predicates, kala_gochara_windows (id); kala_field (chart_id,event_class,segment_index)
  — sandbox 0 dups over 549797 rows (sampled), consistent with the manual production ground truth of 0
  over the full 10.3M rows; kala_taranga (chart_id,month,scope_kind,scope_id).
- **Ldgr.source_presence** (2/2 A): kala_bhavishya `count(source_citation)`=100/100; kala_activation
  16925/16925 (sampled). Correctly absent where no citation column exists.
- **Dens.served** (11/11 A): grep over `L3_kala/*.ts` (excluding `*.test.ts`) reproduces every module
  list exactly — kala_avadhi→query_dasha_dossier.ts (1); ka_bhavishya_lekha→index.ts,
  query_projections.ts, query_temporal_activation.ts (3); ka_kalasutra→call_service_wrappers.ts,
  index.ts, query_temporal_activation.ts (3); ka_yojaka→index.ts, query_temporal_activation.ts (2);
  ka_gochara→call_service_wrappers.ts, index.ts (2, via asset-id refs); ka_dasha_kala/ka_muhurta_seva/
  ka_tulana→call_service_wrappers.ts, index.ts (2 each); ka_taranga→query_activation_waveform.ts (1);
  ka_kshetra and ka_gochara_sweep→0 modules → N/A ✓. `grep -c density_contract` = 0 in every module →
  FAIL verdicts ✓.
- **Build.exercised / Build.history** (11+11 A): `build_run_assets ⋈ build_runs ON br.id = bra.run_id`
  on the **sandbox** reproduces every census number exactly (n / scopes / last date / errors / aborts /
  latest state): ka_avadhi 98/4 scopes/2026-09-11/19/9/**error** → FAIL ✓; ka_kshetra 158/2026-09-11/
  98/12/error → FAIL ✓; ka_gochara_sweep 94/2026-08-12/51/31/error ("no writer registered for
  ka_gochara_sweep") → FAIL ✓; the other 8 latest=complete with errors on record → PARTIAL ✓ (BLOCKED-
  upstream error texts confirmed, 123 such rows across 6 assets). **Caveat**: sandbox run history is not
  identical to production (e.g. ka_bhavishya_lekha 119/33-err in sandbox vs 114/28-err in production;
  ka_avadhi has a 2026-09-11 error row in sandbox that production dates 09-10) — the census numbers are
  the sandbox's; verdicts agree under both DBs. Evidence note (D*): the sample error text quoted by the
  census is not the latest run's error for ka_avadhi (quoted "UndefinedColumn: column d.dasha_start does
  not exist" — 1 historical row; latest is "post-write integrity check failed: integrity_check_sql →
  False") nor for ka_kshetra (quoted "ValueError: a timeline spec needs at least one track"; latest is
  "worker_crash: OperationalError: the connection is lost"). Verdicts correct.
- **Build.dep_liveness** (11 cells: 11 verdict-A, 5 evidence-D*): latest `asset_throughput` state per dep
  (sandbox; identical in production): census FAIL lists reproduce for ka_bhavishya_lekha
  [ka_kala_darshana, ka_vighnakara, ka_sangam all stale] ✓ and ka_tulana [same 3] ✓. Lists incomplete for
  ka_kalasutra (census [ka_sangam]; ka_yojaka is also stale — likely an R44-style non-latest read) and
  ka_taranga (census [ka_sangam]; ka_avadhi is error and bo_pratijna stale). PASS claims "all declared
  dependencies are lit" are literally false for ka_avadhi (bo_pratijna stale), ka_kshetra (bo_pratijna,
  bo_upaya stale), ka_yojaka (bo_pratijna stale) — PASS defensible only under the "lit-reachable /
  has_writer exists" reading (R45-adjacent), as in L2. ka_gochara, ka_gochara_sweep, ka_dasha_kala are
  truly all-lit; ka_muhurta_seva N/A (0 deps) ✓.
- **Complete.width / Carr.detector / Reach.fields** (11/11 A each): constant verdicts; preconditions
  confirmed — no declared-universe field in `asset_registry` (22/23 L3 rows do set
  `expected_volume_formula`/`expected_volume_inputs`, but those are volume estimates, not width
  universes — same caveat as L2); D1/D2/D3 grep hits in `L3_kala/` are divisional-chart references, not
  carriage detectors; no per-asset exposure census exists.

## Per-check false-positive counts (over 11 sampled assets)

| check | census non-pass verdicts sampled | verdict DISAGREE | evidence-inaccurate (verdict holds) |
|---|---|---|---|
| Build.registered | 5 FAIL | 4 (R43: ka_kshetra, ka_dasha_kala, ka_muhurta_seva, ka_tulana) | 1 (ka_gochara_sweep) |
| Build.contract | 5 N/A | 4 (R43-derived) | 1 (ka_gochara_sweep) |
| Idem.pattern | 5 N/A | 1 (ka_kshetra) | 3 (services: right verdict, wrong reason) |
| Build.target | 3 N/A | 0 | 0 |
| Build.dag | 0 | 0 | 0 |
| Build.count_integrity | 3 PARTIAL | 0 | 0 |
| Build.completion | 11 N/A | 8 (R42) | 0 |
| Earn.build_record | 11 FAIL | 0 | 5 (R44 non-latest rows) |
| Cost.baseline | 11 FAIL | 0 | 5 (R44) |
| Complete.depth | 4 PARTIAL | 0 | 0 |
| Vocab.identity | 0 | 0 | 0 |
| Ldgr.source_presence | 0 | 0 | 0 |
| Dens.served | 9 FAIL + 2 N/A | 0 | 0 |
| Build.exercised | 0 | 0 | 0 |
| Build.history | 8 PARTIAL + 3 FAIL | 0 | 2 (quoted error text ≠ latest error) |
| Build.dep_liveness | 4 FAIL + 1 N/A | 0 | 5 |
| Complete.width / Carr.detector / Reach.fields | 11 NOT_GENERIC / NO_DETECTOR / NOT_GENERIC | 0 | 0 |

**Cells checked: 194 · agreements: 177 · verdict disagreements: 17** (all instances of already-registered
defects R42/R43).

## DISAGREE list with reproduction

1. **Build.registered FAIL on ka_kshetra, ka_dasha_kala, ka_muhurta_seva, ka_tulana** (census:
   "registry says has_writer=true and no @register found"). Truth: PASS. Repro:
   `grep -rn "@register" platform/python-sidecar/services/ka_{kshetra,dasha_kala,muhurta_seva,tulana}/writer.py`
   → live decorators (ka_kshetra via `@register(ASSET_ID)` constant; dasha_kala/tulana via module-level
   `_build_writer_class()` call; muhurta_seva module-level); shims in `writers/` side-effect-import them;
   registry has_writer=t. **R43 instance** (both sub-patterns).
2. **Build.contract N/A on the same 4** (census: "no writer file"). Truth: PASS — files exist and conform
   (WriterBase, run(ctx) or plan_substeps+run_substep, no commit/close, no asset_throughput INSERT).
   **R43-derived**.
3. **Idem.pattern N/A on ka_kshetra**. Truth: PASS — `services/ka_kshetra/writer.py` has 12+ ON CONFLICT
   DO UPDATE sites and DELETE FROM statements. **R43-derived**.
4. **Build.completion N/A on all 8 sampled data assets** (census: "no count_sql"). Truth: count_sql IS
   NOT NULL for all 8 (registry query above), contradicting the census's own Build.count_integrity PASS
   ("count_sql=yes") on the same rows. **R42 instances**.

## Observations (not verdict disagreements)

- Sandbox vs production skew: the sandbox's `build_run_assets`/`asset_throughput` copies differ slightly
  from production (extra rows, dates shifted ±1 day: ka_bhavishya_lekha 119 vs 114 runs; ka_avadhi latest
  error 2026-09-11 sandbox vs 09-10 production). All census run-history numbers are the sandbox's;
  verdicts agree under either DB, but the census JSON does not record that its history evidence is a
  snapshot that has since diverged.
- ka_gochara_sweep is a genuinely tombstoned writer (MR-09) whose registry row still says has_writer=t
  with catalog_status=RETIRED — the census FAIL there is a true positive; the measured string is wrong.
- **Count.floor is entirely absent from the L3 census** although `target_floor` is set on 20/23 L3 assets
  (incl. kala_field floor 8,599,775). Not covered by R40–R47 → **new coverage gap (candidate defect)**.
- **Build.history quotes a non-latest error text** while describing "most recent run error" (ka_avadhi,
  ka_kshetra). Not covered by R40–R47 → **new evidence-binding defect (candidate)**.
- ka_kalasutra census Complete.depth verdict is PASS despite only 10/15 columns fully populated (no NEVER
  column); reproduced as measured — noted for verdict-rule review, not counted as disagreement.
