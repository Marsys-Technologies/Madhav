# T2 — Proving Journeys Baseline

**Canonical chart:** `482012f1-710e-4a25-994a-93821f5871aa` (Abhisek Mohanty). All DB queries below
are read-only aggregates (`count()`, `GROUP BY chart_id`, boolean state lookups) against this chart
or a `GROUP BY chart_id` breakdown; no narrative/interpretive row content was ever selected.

Method note: budget-capped at ≤40 tool calls; built directly on prior-cycle `_work/F5.md` (consumer-
path trace), `_work/F7.md` (physical data census), `_work/DOMAIN_F.md` (consumer surfaces),
`_work/T5.md` (strategy tensions) — none of that work was redone, only cited and extended with 4 new
read-only queries and 2 code reads that those packets did not reach.

---

## F02's exact chain (quoted)

`00_ARCHITECTURE/briefs/nirmana/MADHAV_DATA_PLANE_FOUNDATION_CONTRACT_AND_GATES_v1_0.md:29`:

> F02 | Preserve the end-to-end path: question → applicable concept → qualified rule → canonical
> fact → structural relationship → temporal mechanism → manifestation or explicit gap → delivered
> finding → protected evaluation. | Trace with IDs, operators, exact fields, lineage and honest gap
> state.

Task brief specifies the first 8 of these 9 links as the trace scope; "protected evaluation" (the
9th) is folded into Journey 3 below where it is the operative link (C3 adjudication).

---

## THE HEADLINE FINDING (stated up front; all three journeys converge on it)

`kala_activation` (owned by `ka_kalasutra`) and `kala_convergence` (owned by `ka_sangam`) — the two
tables nearly every L3 "when does X activate" consumer surface reads, directly or by join — have
**zero rows for the canonical chart**, confirmed live:

```sql
SELECT count(*) FROM kala_activation   WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa';  -- 0
SELECT count(*) FROM kala_convergence  WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa';  -- 0
SELECT chart_id, count(*) FROM kala_activation  GROUP BY chart_id ORDER BY 2 DESC;
--  1c826d5a-41cb-4450-b4dc-59d440e5f75a | 336093
--  cb73cd3d-9eba-4220-9902-0de91566e980 |   1055
SELECT chart_id, count(*) FROM kala_convergence GROUP BY chart_id ORDER BY 2 DESC;
--  1c826d5a-41cb-4450-b4dc-59d440e5f75a | 17957
--  cb73cd3d-9eba-4220-9902-0de91566e980 |  2540
```

This corroborates F7 §1/§4 (which already flagged both tables as canonical-chart-empty among its 9
zero-row tables) but goes one step further: `asset_throughput` (the orchestrator's own build-state
table, per CLAUDE.md §N.8/ORCHESTRATOR_CONVERGENCE_CLOSE) shows both writers ran for the canonical
chart and *did* write rows, once:

```sql
SELECT asset_id, state, rows_written, last_built_at, last_error FROM asset_throughput
WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa' AND asset_id IN ('ka_kalasutra','ka_sangam');
-- ka_kalasutra | stale | 335403 | 2026-08-13 01:15:50+00 | (null)
-- ka_sangam    | stale | 14868  | 2026-08-13 01:07:13+00 | (null)
```

So on 2026-08-13 the orchestrator recorded writing 335,403 rows (`ka_kalasutra`→`kala_activation`)
and 14,868 rows (`ka_sangam`→`kala_convergence`) for the canonical chart — figures of the same order
of magnitude as what the *other* charts hold today — yet the live tables now show 0 rows for this
chart. State is honestly `stale` (not falsely `lit` — no §N.8 no-op-completion violation on the
*signal* itself), and `last_error` is null, so nothing in `asset_throughput` explains the
disappearance. **Whether this is a mis-scoped delete-then-insert on a later run for a different chart
(a §N.3 idempotency-contract violation — the exact hazard the per-chart-scoped-delete rule exists to
prevent), a deliberate purge, or something else was not diagnosed within this packet's budget — flag
for a dedicated follow-up, stated as COULD NOT VERIFY beyond the two facts above.** Either way, the
present-tense fact is unambiguous: these two tables are empty for the canonical chart right now, and
that is what any live query answers with today.

Every L3 "temporal mechanism" tool identified by F5/DOMAIN_F ultimately depends on one or both of
these tables:
- `kala_now_get`, `kala_windows_get`, `kala_ahead_get` → `query_temporal_activation` → `kala_activation` (DOMAIN_F rows 2-4; F5 `ka_kalasutra` row).
- `kala_priority_get`/`kala_priority_ranking_get` → `call_priority_ranking` → `kala_activation` (F5 `ka_tulana` NOT-FOUND row — the wrapper reads `ka_kalasutra`'s table, not `ka_tulana`'s own output, compounding the emptiness).
- `kala_yoga_activation_get` → `yoga_activation_by_dasha` → joins `bodha_msr_signals` × `kala_activation` directly (traced fresh this packet, see Journey 2).
- The one documented fallback (`kala_windows_get`'s empty-activation fallback → `kala_bhavishya`, per DOMAIN_F's F-176 note) is *also* empty for the canonical chart (F7: `kala_bhavishya` 0 rows canonical, 100 rows all on chart `1c826d5a-…`).

So for the canonical chart, the primary path and its one documented fallback are both empty.

---

## Journey 1: Financial promise and relief

Source: `MADHAV_PRODUCT_DEFINITION_v3_0.md §12.1` + `VALUE_ARCHITECTURE_v2_0.md §12.1`, quoted in
full in the task brief. Chain per VAL:394 — L0 rules → L1 positions/conditions/formation/clocks → L2
constructs resource-creation/retention mechanisms (incl. derived-house relations + counter-evidence)
→ **L3 searches the same configuration's qualified activation routes** → L4 distinguishes
manifestations/emits forecast → L5 evaluates the claim later.

| F02 step | Status | Evidence |
|---|---|---|
| question | N/A | Product-surface concern, not L3. |
| applicable concept (L0) | Out of this packet's scope | L0 is sealed per CLAUDE.md §E; not independently re-verified this pass. |
| qualified rule (L0) | Out of this packet's scope | Same. |
| canonical fact (L1) | **PASS** | `chart_dashas` (L1) holds 483,870 rows for the canonical chart (F7 §4c). `ga_dashas`/`ga_positions`/`ga_strength` are L1-closed assets per CLAUDE.md §E. |
| structural relationship (L2) | Assumed PASS, not independently re-verified | L2 Bodha is `✓ BUILT` per CLAUDE.md §E; `bo_laksana`/`bo_sangati`/`bo_upaya` construction of resource-creation/retention mechanisms and Bhāvat Bhāvam is out of this L3-scoped audit's direct evidence base this cycle. |
| **temporal mechanism (L3) — FIRST FAILING BOUNDARY** | **FAIL** | `kala_activation` (`ka_kalasutra`) is the table every "which qualified clocks engage" tool (`kala_now_get`, `kala_ahead_get`, `kala_windows_get`) reads (DOMAIN_F rows 2-4) — **0 rows for the canonical chart** (query above). Its one documented fallback, `kala_bhavishya`, is also 0 rows canonical (F7 §1). Compounding, secondary defect on the same step: even where dasha-clock qualification is asked for directly, `call_dasha_eligibility` (the live wrapper behind `kala_dasha_kala`-flavoured queries) bypasses `KaDashaKalaService`/`ka_dasha_kala`'s own writer output entirely and queries raw L1 `chart_dashas` instead — "**CONFIRMED — direct `chart_dashas` query, bypasses `KaDashaKalaService`. Confidence: high.**" (F5.md:32-37). So even setting the empty-table finding aside, the qualification layer VA:433 requires ("which qualified clocks engage those same participants and conditions") is not the layer actually being served for this call path. |
| manifestation or explicit gap | **Cannot be honestly reached today for the canonical chart** | VA:433's "Where the manifestation bridge is not qualified, stop at supported activation and identify the missing link" presumes a supported-activation baseline exists to stop at. With `kala_activation` empty, there is no activation baseline to qualify or gap-report against — the honest state is "no L3 temporal data for this chart," not a graded partial finding. The PACT/promise-gate retention-constraint check (`promise_spine.ts`'s `interpretPactJoin`, confirmed LIVE by DOMAIN_F) is itself sound machinery, but is only invoked on `kala_windows_get`'s fallback path (F-176, DOMAIN_F row 3) — a documented, intentional trade-off, not a bug — yet that fallback path is *also* empty for this chart, so the retention-constraint check has nothing to gate either. |
| delivered finding | Not reached | Downstream of the above; not independently traced further. |

**First failing boundary: temporal mechanism, owned by `ka_kalasutra` (`kala_activation`), compounded by `ka_tulana`/`ka_dasha_kala`'s consumer-side bypasses already documented in F5.**

---

## Journey 2: Named yoga / NBRY

Source: `MADHAV_PRODUCT_DEFINITION_v3_0.md §12.2` + `VALUE_ARCHITECTURE_v2_0.md §12.1` ("Named
yoga…"), quoted in full in the task brief. Requires: formation + cancellation checked BEFORE
searching time (L1/L2); then L3 lists eligible activation routes, closest contact, better-supported
windows, opposing conditions; explicitly warns "a participant's nearest transit contact is not
automatically the activation of the whole configuration."

New trace this packet (not in F5/DOMAIN_F, which only flagged `kala_yoga_activation_get`'s target
table as "COULD NOT VERIFY"): its handler (`register_p1_aliases.ts:1408-1436`) calls
`marsys://tool/L-TIMING/yoga_activation_by_dasha`, implemented at
`platform/src/lib/retrieval/registry/layers/register_d8_assess_domain.ts:1787-1916` as
`yogaActivationByDashaCapability`.

| F02 step | Status | Evidence |
|---|---|---|
| question | N/A | Product-surface concern. |
| applicable concept / qualified rule (L0) | Out of scope | Same as Journey 1. |
| canonical fact (L1) | Assumed PASS | Formation/dignity facts are L1-closed. |
| structural relationship (L2) — formation + cancellation | Assumed PASS, not independently re-verified | `bodha_msr_signals` with `signal_type_class = 'yoga'` is the catalog this capability joins against (`register_d8_assess_domain.ts:1796,1887,1912`); whether NBRY's specific cancellation-condition qualification is correctly modeled at the L2 signal level was not re-checked this pass (out of L3 scope). |
| **temporal mechanism (L3) — FIRST FAILING BOUNDARY** | **FAIL** | `yogaActivationByDashaCapability`'s own query joins `bodha_msr_signals m` to `kala_activation ka` on `chart_id`+`signal_id` (`register_d8_assess_domain.ts:1885-1916`, conditions at `:1909-1916`). With `kala_activation` at 0 rows for the canonical chart, this join returns **zero activated yogas for chart 482012f1 regardless of NBRY's formation/cancellation state** — the tool cannot surface an activation window for *any* named yoga on this chart today, not just NBRY specifically. Two *already-fixed* historical defects are documented in the same code block as in-line comments (R-45 "~all yoga activation rows carry NULL activation_start/end", now handled by admitting undated rows rather than silently dropping them; WP-S4-fix2, an undated-row ranking-inflation bug, now fixed by sorting dated rows first) — these are evidence the join mechanism itself has been actively hardened, which makes the present emptiness more clearly attributable to the missing `kala_activation` build than to residual join-logic bugs. Separately, `ka_sangam`'s `kala_convergence` (0 rows canonical) is the table other "eligible activation route" assets chain from (T5 Tension 6) — so the configuration-level convergence search VA:12.2 calls for ("list eligible activation routes… with scope and uncertainty") is also empty at its source for this chart. |
| "nearest transit ≠ whole configuration" safeguard | **COULD NOT VERIFY** | No participant-count/whole-configuration-vs-single-transit distinction was found in `yogaActivationByDashaCapability`'s join (it correlates one `signal_id` to dasha-period overlap only, via `dasha_activation_proximity_score` — no explicit multi-participant model surfaced in the code read this pass). Whether this distinction lives elsewhere (e.g. inside `bodha_msr_signals`' own signal construction, or in `ka_vighnakara`'s obstruction layer) was not traced within budget — flagged, not asserted absent. |
| manifestation or explicit gap | Cannot be honestly reached for the canonical chart | Same reasoning as Journey 1 — no activation baseline exists to grade a manifestation against. |
| delivered finding | Not reached | Downstream of the above. |

**First failing boundary: temporal mechanism, owned by `ka_kalasutra` (`kala_activation`, read directly by `yogaActivationByDashaCapability`) and `ka_sangam` (`kala_convergence`, feeding the wider "eligible activation routes" search) — the identical boundary as Journey 1, reached by a different tool.**

---

## Journey 3: Historical challenge

Source: `MADHAV_PRODUCT_DEFINITION_v3_0.md §12.3` + `VALUE_ARCHITECTURE_v2_0.md §12.1`
("Historical challenge…"), quoted in full in the task brief. Requires: an independent event-free
structural/temporal re-analysis, compared against the frozen original claim and the admitted
historical log; inspection of non-matching events and unmatched activations; no hindsight leakage;
protected C3 adjudication routed separately from provider-facing explanation (the "protected
evaluation" 9th F02 link).

| F02 step | Status | Evidence |
|---|---|---|
| question | N/A | Product-surface concern. |
| applicable concept / qualified rule (L0) | Out of scope | Same. |
| canonical fact (L1) | Assumed PASS | Same L1-closed basis. |
| structural relationship (L2) | Assumed PASS, not independently re-verified | Out of L3 scope this cycle. |
| **temporal mechanism (L3) — FIRST FAILING BOUNDARY, same root cause** | **FAIL** | The "independent event-free structural/temporal analysis" and the "unmatched activations" inspection both require re-deriving what L3 activation windows existed/exist — the same `kala_activation`/`kala_convergence` substrate as Journeys 1-2, empty for the canonical chart. There is no live-computed activation set for chart 482012f1 to compare against a frozen historical claim; the comparison side of the analysis (what *did* activate vs. what was *claimed*) is starved at the same point. |
| manifestation or explicit gap | Cannot be honestly reached | Same reasoning. |
| delivered finding | Not reached | Downstream. |
| **protected evaluation (9th F02 link, C3 adjudication)** | **COULD NOT VERIFY** | `mimamsa_lel_query`, `mimamsa_outcome_record`, `standing_predictions_read`, `mechanism_retrodiction_get` all exist in the catalogued MCP tool surface (per the deferred-tool listing available this session), consistent with L5 Mīmāṃsā being sealed per CLAUDE.md §E — but none was opened/traced this pass (out of budget, and L5's own machinery is outside this L3-scoped packet's remit). Whether the L5 side of Journey 3 (frozen-claim retrieval, fit/misfit adjudication) is independently sound is a separate, un-audited question; this packet only establishes that the L3 half it depends on (the activation/convergence baseline) is not currently available for the canonical chart. |

**First failing boundary: temporal mechanism, same root cause as Journeys 1 and 2 — `ka_kalasutra`/`kala_activation` and `ka_sangam`/`kala_convergence` empty for the canonical chart. This is the earliest point at which Journey 3's live L3-side inputs fail; whether L5's separate frozen-claim/adjudication machinery has its own independent defects further downstream was not assessed.**

---

## Cross-journey pattern

All three proving journeys hit their first failing boundary at **the identical F02 link — temporal
mechanism — for the identical underlying reason**: `kala_activation` (`ka_kalasutra`) and
`kala_convergence` (`ka_sangam`) hold zero rows for the canonical chart `482012f1-…`, despite being
the tables nearly every L3 "when does X activate" consumer tool reads (directly, by join, or via
their one documented fallback, which is also empty for this chart). `asset_throughput` shows both
writers *did* run for this chart on 2026-08-13 and recorded writing hundreds of thousands of rows
(335,403 / 14,868) — the data existed once and is gone now, with `state='stale'` and no
`last_error`, an unexplained discrepancy flagged for follow-up rather than diagnosed here (budget).

This is not three independent defects; it is one substrate gap that every downstream "when" tool —
across financial-promise timing, yoga activation, and historical-challenge re-derivation alike —
inherits identically. Journey-specific secondary defects exist beneath this shared floor (Journey 1's
`ka_dasha_kala`/`ka_tulana` consumer-wrapper bypasses per F5; Journey 2's unverified whole-configuration-
vs-single-transit safeguard) but none of them can be observed to matter in practice for the canonical
chart today, because the query never gets past the empty `kala_activation`/`kala_convergence` floor
to exercise them.
