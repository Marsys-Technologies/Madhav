# F5 — Consumer-Path Trace, All 23 `ka_*` Identities (22 active + protected `ka_gochara_sweep`)

**Method note:** the first full-scope attempt at this packet stalled a 600s no-progress watchdog
(unconstrained multi-file exploration — the same failure mode cycle 1 hit on 3 other packets) and
was killed with no output. Retried split into two independently-bounded, budget-capped subagents
("Part A": divergence settlement + 11 assets; "Part B": remaining 11 assets + retired
`ka_gochara_sweep`), each with an explicit ≤25-tool-call ceiling and "grep-with-`--include`-and-
`head` only, no open-ended browsing" instruction. Both completed cleanly (22 and 7 tool calls
respectively). This confirms cycle 1's lesson: split + hard-cap large scan packets from the start
rather than dispatching them whole.

Two of the resulting rows (`ka_tulana`, `ka_dasha_kala`) are genuinely unresolved findings, not
scan gaps papered over — flagged per CLAUDE.md §N.8, not asserted either way.

---

## Settled divergences (the two specifically named in the brief)

### `call_ephemeris_at_t` — separate swisseph integration, NOT `services/ka_graha_sancara/engine.py`

`call_ephemeris_at_t`'s handler (`platform/src/lib/retrieval/registry/layers/L3_kala/call_service_wrappers.ts:221-257`)
`fetch()`es the sidecar's `/api/compute/ephemeris_at_t` route
(`platform/python-sidecar/routers/ephemeris.py:241`). That router does `import swisseph as swe`
directly (`ephemeris.py:5`) and defines its **own** `_AT_T_AYANAMSHA_MAP` (`:225-231`), with an
explicit in-file comment (`:211-212`) stating this is "rather than a second swisseph integration" —
i.e. it deliberately does not import `services/ka_graha_sancara/engine.py`, even though that module
exists and is docstring-labeled "Core ephemeris-at-T engine for ka_graha_sancara"
(`services/ka_graha_sancara/engine.py:2`). `grep -n "ka_graha_sancara" routers/ephemeris.py` → zero
import/call hits, only the naming comment.
**Verdict: CONFIRMED — separate swisseph integration.** Confidence: high.

### `call_dasha_eligibility` — queries `chart_dashas` directly, NOT `KaDashaKalaService`

`call_dasha_eligibility`'s handler (`call_service_wrappers.ts:271-364`) runs a raw SQL query
(`:346-364`) — `SELECT ... FROM chart_dashas WHERE chart_id = $1 AND ayanamsha_id = $2 ...`. No
`KaDashaKalaService` import anywhere in the file.
**Verdict: CONFIRMED — direct `chart_dashas` query, bypasses `KaDashaKalaService`.** Confidence: high.

---

## Full 23-identity consumer-surface trace

| asset | writer file | consumer surface file(s) | verdict | evidence | confidence |
|---|---|---|---|---|---|
| `ka_gochara` | `pipeline/orchestrator/writers/ka_gochara.py:180`; `TABLE = "kala_gochara_windows_v2"` (`:120`) | `platform-mcp/src/tools/retrieval/register_gochara_windows.ts` | **DIVERGENT (real finding)** | Writer's own table constant is `kala_gochara_windows_v2`, and the file's header explicitly frames this as its "OWN table, structurally outside" the legacy `kala_gochara_windows`. But the consumer's `buildSourceCitation()` (`register_gochara_windows.ts:573-598`) queries/cites the un-suffixed `kala_gochara_windows` and labels generation=`3.0` rows "W6.4 cutover ka_gochara materializer" (`:581`) — the serving surface still reads the pre-cutover table name, not the table the currently-registered writer populates. Corroborates cycle 1's F3 finding (seed vs strategy disagree on `ka_gochara`'s target table) from the consumer-surface side. | medium — table-name mismatch is clear; whether a separate sync/view reconciles the two tables was not traced |
| `ka_kshetra` | `services/ka_kshetra/writer.py` | `platform-mcp/src/tools/kala_views/{ahead,priority,ritual,story}.ts` | same-code (surface-level) | asset_id string appears repeatedly across all 4 files | low-medium — asset_id co-occurrence only, target table not independently verified |
| `ka_bhavishya_lekha` | `pipeline/orchestrator/writers/ka_bhavishya_lekha.py:78` | `.../L3_kala/query_projections.ts`; `kala_views/ahead.ts` | same-code | asset_id cited in query file header/body | medium |
| `ka_gochara_resonance` | `services/ka_gochara_resonance/writer.py:502`; `INSERT/DELETE gochara_resonance_map` (`:417,420`) | `register_gochara_windows.ts:1007` (`FROM gochara_resonance_map`) | same-code | exact table-name match, writer→consumer | high |
| `ka_yojaka` | `pipeline/orchestrator/writers/ka_yojaka.py:61` | `.../L3_kala/query_temporal_activation.ts:6` | same-code | asset_id in file header | low-medium — single-line citation, table not verified |
| `ka_kala_darshana` | `pipeline/orchestrator/writers/ka_kala_darshana.py:9` | `.../L3_kala/query_temporal_view.ts` | same-code | asset_id referenced twice | medium |
| `ka_sudarshana_varsha` | `services/ka_sudarshana_varsha/writer.py:116` | `.../L3_kala/query_sudarshana_varsha.ts`; `kala_views/now.ts` | same-code | asset_id repeated in a dedicated query file named after the asset | medium-high |
| `ka_jivana_parva` | `pipeline/orchestrator/writers/ka_jivana_parva.py:53` | `.../L3_kala/query_life_arc.ts`; `kala_views/story.ts` | same-code | asset_id in dedicated query file + narrative surface | medium-high |
| `ka_taranga` | `pipeline/orchestrator/writers/ka_taranga.py:73` | `.../L3_kala/query_activation_waveform.ts` | same-code | asset_id in header + body | medium |
| `ka_kota_chakra` | `services/ka_kota_chakra/writer.py:206` | `.../L3_kala/query_kota_chakra.ts`; `kala_views/now.ts` | same-code | asset_id in dedicated query file | medium-high |
| `ka_sangam` | `pipeline/orchestrator/writers/ka_sangam.py:224` | `.../L3_kala/query_convergence_windows.ts`; `query_temporal_activation.ts`; `kala_views/ahead.ts` | same-code | asset_id in dedicated query file + secondary fan-out reference | medium |
| `ka_moorti_nirnaya` | `services/ka_moorti_nirnaya/writer.py:174` | `.../L3_kala/query_moorti_nirnaya.ts`; `kala_views/now.ts` | same-code | `FROM kala_moorti_nirnaya` — direct table read | high |
| `ka_tithi_pravesha` | `services/ka_tithi_pravesha/writer.py:234` | `.../L3_kala/query_tithi_pravesha.ts`; `kala_views/now.ts` | same-code | `FROM kala_tithi_pravesha` — direct table read | high |
| `ka_muhurta_seva` | `services/ka_muhurta_seva/writer.py:72` | `call_service_wrappers.ts` (`call_muhurta_score`); `platform-mcp/src/tools/muhurta_finder.ts` | same-code | in-file comment: reuses the same `score_muhurat()` primitive, not a reimplementation | medium — `score_muhurat()` source itself not opened to confirm identical function object |
| `ka_vighnakara` | `pipeline/orchestrator/writers/ka_vighnakara.py:147` | `.../L3_kala/query_obstruction_periods.ts` | same-code | `FROM kala_obstruction` — direct table read | high |
| `ka_gochara_v3_century_materialize` | `pipeline/orchestrator/writers/ka_gochara_v3_century_materialize.py:1717` | `register_gochara_windows.ts` | same-code | comment block explicitly names it authoritative writer for the shared `kala_gochara_windows` table (`:576,588`) | high |
| `ka_kalasutra` | `pipeline/orchestrator/writers/ka_kalasutra.py:23` | `.../L3_kala/query_temporal_activation.ts`; `kala_views/ahead.ts`, `now.ts` | same-code | reads writer's own `kala_activation` table plus a documented L2 join | high |
| `ka_graha_sancara` | `pipeline/orchestrator/writers/ka_graha_sancara.py:47` | `call_service_wrappers.ts` (`call_ephemeris_at_t`) | same-code (live service call, no table) | compute service, `target_table = null`; consumer wires a matching health-probe id | medium — HTTP route handler itself not traced to confirm it invokes the exact `@register`'d class |
| `ka_vedha_gochara` | `services/ka_vedha_gochara/writer.py:263` | `.../L3_kala/query_vedha_gochara.ts`; `L0_brahmagyan/query_transit_vedha.ts`; `kala_views/now.ts` | same-code | `FROM kala_vedha_gochara` — direct table read | high |
| `ka_gochara_sweep` (retired) | `services/ka_gochara_sweep/writer.py:256` — decorator import removed at the orchestrator shim so `@register` never fires | `register_gochara_windows.ts`; `kala_views/story.ts` | orphan (protected-history, by design) | consumer still labels historical rows with this asset_id for provenance display, but no longer calls the writer — confirms CLAUDE.md's "never rebuilt" status | high |
| `ka_tulana` | `services/ka_tulana/writer.py:87` | `call_service_wrappers.ts` (`call_priority_ranking`); `kala_views/priority.ts`; `register_p1_aliases.ts` | **NOT-FOUND — reads a neighboring asset's table (real finding)** | consumer's "(ka_tulana service)" SQL (`:615-689`) selects from `kala_activation`, the table `ka_kalasutra` owns; `grep target_table\|INSERT INTO` on `ka_tulana/writer.py` found no hits | low — genuinely unresolved: could not confirm within budget whether `ka_tulana` co-writes `kala_activation` by design, or the consumer silently substitutes a neighbor's data |
| `ka_dasha_kala` | `services/ka_dasha_kala/writer.py:113` | `call_service_wrappers.ts` (`call_dasha_eligibility`) | **NOT-FOUND — reads a different (L1) table (real finding)** | consumer's "(ka_dasha_kala service)" SQL is `FROM chart_dashas` — an L1 Gaṇita table, not any `kala_*` L3 table; `grep target_table\|INSERT INTO` on `ka_dasha_kala/writer.py` found no hits | low — same gap class as `ka_tulana`; unclear whether `ka_dasha_kala`'s own writer output is consumed anywhere at all |

---

## Summary

- **20 of 23** identities show same-code wiring at least at the asset_id / table-name level (11 at
  high confidence with a confirmed matching table name, 9 at medium/surface-level confidence —
  asset_id co-occurrence checked, target-table cross-check not independently done for all of them
  given the per-subagent tool-call budget).
- **1 retired** (`ka_gochara_sweep`) confirmed correctly inert — decorator import removed, never
  fires, survives only as a provenance label.
- **3 real divergence/gap findings**, none hypothetical:
  1. **`ka_gochara`** — writer targets `kala_gochara_windows_v2`; the live consumer surface reads
     and cites the un-suffixed `kala_gochara_windows`, labeling it as the current materializer's
     output. Corroborates cycle 1's F3 seed-vs-strategy disagreement on this exact table from the
     serving side.
  2. **`ka_tulana`** — its consumer wrapper reads `kala_activation` (owned by `ka_kalasutra`), with
     no evidence found that `ka_tulana`'s own writer persists anywhere.
  3. **`ka_dasha_kala`** — its consumer wrapper reads `chart_dashas` (an L1 table), with no evidence
     found that `ka_dasha_kala`'s own writer output is served anywhere.
- **Reachability implication for `CONSUMER_INTEGRATED` (T3):** for the 2 NOT-FOUND assets, if their
  writers genuinely have no consumer that reads their own output, `CONSUMER_INTEGRATED` is not
  currently reachable for `ka_tulana`/`ka_dasha_kala` under the CURRENT registered writer without
  either (a) discovering an undiscovered real consumer, or (b) treating the neighbor-table read as
  an intentional co-writer/shared-surface design that needs explicit documentation. This is exactly
  the kind of orphan-obligation-or-orphan-work question T1's traceability matrix needs to resolve,
  not guess at.
