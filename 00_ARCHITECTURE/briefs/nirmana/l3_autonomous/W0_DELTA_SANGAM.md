---
artifact: KALA_W0_DELTA_SANGAM
version: "1.0"
status: CURRENT
date: 2026-09-22
addressed_to: the ka_sangam brief session
effect: delta to your existing prompt; supersedes anything in it that conflicts; base branch is main
---

# W0 delta — ka_sangam

Your prompt named one prior artifact to reconcile. There are **three**, one of them carrying
native-ratified decisions. And W0 established facts about Sangam's edges and blast radius that
change its output contract. Base branch: **`main`** — W2 explicitly preserved `ka_sangam`
unchanged (*"unrelated `ka_sangam` … remain preserved"*), so `main`'s current Sangam code *is* the
accepted source.

## Read now

- `00_ARCHITECTURE/CONDUCTOR/cleanup/L3_KA_SANGAM_TRANSIT_MODEL_REDESIGN.md` — status *"DESIGN …
  NO BUILD until native ratifies"*; **§4.5 "RATIFIED DECISIONS (native 2026-06-22)"**; §4.6
  per-signature transit-planet model; §7 sequence. Then the two implementation prompts beside it:
  `CLAUDE_CODE_PROMPT_KA_SANGAM_TRANSIT_REDESIGN.md` and `..._PER_SIGNATURE_FIX.md` (both READY).
- `00_ARCHITECTURE/briefs/CLAUDECODE_BRIEF_L3_KA_SANGAM_v1_0.md` (you already have this)
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_CURRENT_STATE_AND_DISPOSITION_v1_0.md` §4.1 (`ka_sangam`,
  `ka_vedha_gochara`, `ka_gochara` rows), §4.2, §6.3
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_W0_FIELD_CONTRACT_REGISTER_v1_0.md` — census #15; fence 7
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md` §5 row P4; L3-U02, L3-U07
- `l3_autonomous/KALA_ASSET_BRIEF_INSTANTIATION_GUIDE_v1_0.md`

## What W0 already established — inherit, do not re-derive

1. **Your registry edges are wrong, and W0 says "corrected before build"** (CURRENT_STATE §4.1).
   Sangam *declares* materialized `ka_gochara` but does not read it — it invokes the on-demand
   Gochara **service** (§4.1, §6.3). Sangam *reads* `ka_vedha_gochara` but does not declare it
   (§4.1 "undeclared by Sangam"). Both closures are yours; the brief specifies them as computation
   edges with F12 roles, not as registry cosmetics.
2. **`kala_convergence` is a cascade SOURCE, not only a victim** (CURRENT_STATE §4.2): deleting
   convergence cascades `kala_obstruction` and `kala_darshana`, nulls Bhavishya's convergence
   reference, and *cascades through `phala_anchors` into multiple L4 tables*. So a Sangam rebuild
   deletes downstream L3 rows and reaches sealed L4. Your generation-binding design must be
   **bidirectional** — survive an L2 rebuild above, and not destroy L3/L4 below.
3. **Fence 7 binds you**: outcome-bearing or `phala_anchors`-referenced Bhavishya rows are
   immutable absent a generation schema; a content change fails before DML. A Sangam rebuild that
   nulls those references violates it.
4. **P4 is your named redesign candidate** (Strategy §5): *"Reuse geometric search results across
   compatible predicates/modes, then apply distinct semantic scoring and eligibility. Mode identity
   and full applicable predicate coverage. Savings fund broader coverage; reduced caps cannot pass
   as equivalent."* The last sentence is a constraint on your top-500/750 fix, not a suggestion.
5. **L3-U02 (concordance) is your interface packet**: *"Two disjoint supporters never become
   simultaneous consensus; an inapplicable method is not dissent. Zero supporting evidence cannot
   become one independent witness; another representation of the same origin adds no independent
   support."* This is the doctrinal form of your inherited-independence problem.

## The three-artifact reconciliation

The June transit-model redesign diagnosed *"the bug, correctly stated (three layers deep)"* —
that `ka_sangam` scanned ephemerides with a fixed planet instead of evaluating rules over
pre-computed transit events — and §4.5 records native rulings on it. Markers from that redesign
(`per_signature`, `transit_trigger`, `slow_transit`) appear in `main`'s `services/ka_sangam/engine.py`
(7) and `writers/ka_sangam.py` (5): **some of it landed.** Your brief must establish, at the code,
which of §4.5's ratified decisions are implemented, which are not, and whether any conflicts with
the current Strategy. A June native ruling stands unless the native supersedes it — if you believe
one should be superseded, say so as a named decision, never by silently designing past it.

## What is genuinely new since W0 — carry forward

Top 500 and top 750 by score are **100% Mode C**, so `ka_vighnakara` and `ka_kala_darshana` never
receive a daśā×transit convergence — the score cut is a mode filter. Mode D is 70% of the table
and ~13× duplicated; the guard is vacuous on the lifetime path (`pred_dicts=[pred]`).
`confidence_score = ICC/13` with a tautological transit term; `confidence_label` is
**anti-correlated** with evidence count. `independent_current_count` is read by zero `ka_*`
consumers. The discriminating tier is stored and never served.

## What changes in your brief

Reconcile three artifacts, not one; close both registry edges as typed computation edges (1);
design generation binding in both directions and respect fence 7 (2, 3); treat "reduced caps
cannot pass as equivalent" as binding on the mode-stratification fix (4); make inherited
independence the U02 packet's core (5); and carry the June rulings forward explicitly.
