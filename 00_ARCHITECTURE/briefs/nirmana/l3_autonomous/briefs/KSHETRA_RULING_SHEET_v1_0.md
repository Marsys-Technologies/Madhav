---
artifact: KSHETRA_RULING_SHEET
canonical_id: KSHETRA_RULING_SHEET
version: "1.0"
status: AWAITING_NATIVE_RULING
date: 2026-09-23
rules_on: KSHETRA_ECOSYSTEM_ELEVATION_PLAN_v1_0.md v1.4 §7 · KSHETRA_ELEVATION_BRIEF_v1_0.md v4.3
prepared_by: the Kshetra brief session (madhav-d2), on the native's delegation to "do the rest" — the rulings themselves are NOT delegable (contract §8: a packet never approves itself; APPROVED_FOR_EXECUTION comes only by native record)
how_to_rule: >
  One word per line in the "Native ruling" field — AFFIRM (adopt the recommendation), or a written
  alternative. When every line carries a ruling, the brief's status moves to APPROVED_FOR_EXECUTION
  with this sheet's commit as approval_record. Until then nothing in the packet is authorised.
independent_reviewer: madhav-fc (assigned by delegation; overridable)
implementation_owner: Execution — Data Plane task (codex/madhav-data-plane-execution), per Strategy §8 (assigned by delegation; overridable)
---

# Kshetra — ruling sheet

Ten decisions. Each carries the plan's recommendation, the one-line reason, and what the
alternative costs. Evidence is in the plan and brief at the cited sections; Kimi K3's independent
review (effort=max) and its reconciliation are beside them. **Nothing below is decided until the
native writes in the last field.**

| # | Decision | Recommendation | Why, in one line | Alternative and its cost | **Native ruling** |
|---|---|---|---|---|---|
| 1 | **Product configuration** — 6-class calibrated or 25-class incomplete? (plan §7.1) | **The calibrated 6-class run is the product** until PK-P4 sources more priors; the 25-class rows are substrate. | Six classes have a real λ⁰; nineteen run on a synthetic constant the row cannot declare. The 6-class configuration is a *demonstrated* path (the other chart's published manifest). | 25 classes now: publishes an assertion 85.7% synthetic, untagged on the row — §N.8. | |
| 2 | **Disposition** — complete, re-scope, or park? (plan §7.2) | **Re-scope; build fresh at W7** (`L3-W7-KSHETRA-COHERENT-PUBLICATION-01`). | Completing the old rows is unavailable (resume ≥9 rejects them; DHARA 1.2 superseded their id; their null layer is pre-`87cc8c9baf` under an accepted HIGH finding). Park is already in force by `KshetraReplacementHeld`. | Attempting to resume: refused by the writer. Parking indefinitely: no consumer ever reaches the field. | |
| 3 | **σ_T source** (plan §7.3; brief §4.3) | **L1 birth-time precision as the admitted immutable artifact**; interim `default_120s_assumption` + F06 `unavailable`. **Never** a live `phala_rectification` read. | A rectification posterior is LEL-fitted — event-derived into an event-free prospective field (Strategy §6.2; L3-U09). The default branch already exists (`uncertainty.py:170-173`). | Granting the read: a firewall breach; the build works but the field is contaminated by outcomes. | |
| 4 | **Ecosystem ingestion** (plan §7.4, §4 S1) | **Consume `ka_vedha_gochara` and `ka_moorti_nirnaya`; retire Kshetra's internal derivations after one cross-check generation; AV gate from the same source as Sangam E2.** | Today two vedha and two moorti verdicts can coexist in one layer for one instant (0 reads of either asset; internal re-derivation). One verdict per instant. | Keep re-deriving: the duplicate-support hazard stands (L3-U02, VA §10.2). | |
| 5 | **P1 / P2 / P6 order** (plan §7.5; brief §4.6) | **P6 → P2 → P1**, P1 gated on the ablation (#10). | The pin rule makes publication the only order that preserves capital; P1 is ~4× everything else and its equivalence proof needs the exhaustive-shift oracle first. | P1 first: optimises an unpublishable artefact; may be wasted if the ablation parks the asset. | |
| 6 | **Enrichment order** (plan §5, §7.6) | **E0 (counterfactual-self, Q04) and E1 (mechanism fingerprint) first** — no new doctrine, existing columns; then E2 three-arm null; E3–E8 each on a cited source, all after the ablation. | E0/E1 cost a query and answer questions no surface owns; the rest are new `config_pin`s needing B.3/B.10 sources. | Model changes before the ablation: pays for enrichment of a shape not yet shown to earn its place. | |
| 7 | **Node frame** (plan §6a, §6a.1, §7.7) | **Under a MEAN hub ruling: disposition (b)** — keep true-node knots, derive mean in the **L0 ephemeris service**, `node_mode`/`epoch_convention` declared on the row beside `ayanamsha_id`. Never Kshetra-side. | Store is true-node at noon-UT knots, Swiss-exact; contract says mean; L1 natal is mean. (b) leaves frozen L0 untouched and makes the disagreement inspectable. **Reading-visible:** Rāhu moves Rohiṇī pāda 3 (mean, L1's fact) ↔ pāda 4 (true, author computation, 0.045° margin; L1 serves no `RAH_TRUE`, so if the ruling goes true, L1 must serve it). | (a) rebuild L0 mean-node: touches every consumer. (c) mixed-frame contract per consumer: pushes the seam onto every reader. | |
| 8 | **Shared vedha/moorti source + uniform admission + G-9** (plan §6a, §7.8) | **Ratify `ka_vedha_gochara` / `ka_moorti_nirnaya` as the layer's single producers.** Consumer admission uniform and mechanical: F06 `applied` iff the producer's `corpus_verifiable = true` and geometry passes; else `unqualified` — **today `unqualified` for every gochara-vedha row, house_vedha included** (Phaladīpikā Ch.26, the only vedha citation surviving the BPHS Ch.29 strike, is not in the admitted corpus). **Rule G-9's direction:** admit Phaladīpikā (and Sārāvalī / Jātaka Pārijāta if rows are to stay verse-cited), or re-grade every gochara-vedha row as cited-outside-admitted-corpus for all three consumers. | Same text, one reason, one verdict — the inconsistency the Gochara session caught. Geometry checkability is computational correctness, not source qualification (F24). Adopted identically by Gochara (v2.0 §5.4) and Sangam (82477d0a7). | Admitting house_vedha as `applied` on geometry alone: a source-unqualified rule serves as if qualified — B.3. | |
| 9 | **G3 — which suppression semantics is the contract?** (plan §2 item 0, §7.9; brief §2.4, §3) | **Route-scoped SM-R-7 Option B** (the documented contract; Kimi's and the plan's recommendation) — hoist the `suppressed_keys` filter into `hazard.evaluate`, passed explicitly; **and** require a byte-equality test field ≡ null ≡ projection before any `null_p` is served from the W7 build. | The stored field and its null are both chart-wide today; only `layer1.project_layer1` (a projection that never writes `kala_field`) honours the contract. Whichever semantics is chosen, the three must agree. **Rank 0.** | Chart-wide as the contract: an unrelated obstruction can suppress a class — the discipline the model's own docstring calls "structurally impossible" is given up. Either way, without the equality test the significance test is on a different model than the served one. | |
| 10 | **The ablation's judge and timing** (plan §7.10; brief §4.1) | **Three arms** (Sangam · Sangam+Taranga · +segments) over several adjacent-MD chapter pairs incl. one ordinary period; **pre-registered rubric keyed to Q06's own distinction text**; **judge named by the native**; **runs BEFORE the S1-ingestion packet** so a failed ablation parks the asset without that spend. | The asset's entire unique claim (Q06) rests on this test; without a judge and rubric fixed in advance it is prose, not a falsifier (§N.7 item 5). | Judge chosen after the arms are read: unfalsifiable. Ablation after ingestion: pays S1 for a shape that may be parked. | |

**Also delegated and done, overridable by a line here:** implementation owner = the Execution —
Data Plane task (Strategy §8, the sole execution destination); independent reviewer = madhav-fc
(strategic session, not the author). **Native ruling on the owners:** ______

**On signing.** When all ten lines and the owners line carry a ruling, the brief's `status` becomes
`APPROVED_FOR_EXECUTION` and its `approval_record` cites this sheet's commit. Any line ruled
against the recommendation re-opens only the plan sections it names; the packet is otherwise
executable as written. Until then: no code, migration, build, grant or deployment is authorised by
anything in this packet, and populated-chart replacement remains held by `KshetraReplacementHeld`.
