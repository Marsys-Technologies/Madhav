# Kimi K3 (effort=max) review — Gochara family elevation plan v2.0
# generated 2026-09-23T02:00:16+05:30 via: kimi -m kimi-code/k3 -p <KIMI_REVIEW_PACKET_GOCHARA_v1_0.md>; k3 default_effort=max, thinking.effort=max; cwd=/Users/Dev/madhav-l3/integration (branch codex/madhav-l3-claude-code)

• Using **dispatching-parallel-agents** to structure the multi-source evidence gathering for this review.

• I'll read the plan under review myself, in full, and dispatch four parallel explore agents for the supporting documents, code verification, governing texts, and classical corpus — each with a focused brief.

• All evidence gathered, plus two things I verified directly: the Clear execute route (`execute/route.ts:160-183`) and the `GocharaTransitService` consumer set. I now have everything needed for the review.

• # Review of `GOCHARA_FAMILY_ELEVATION_PLAN_v2_0.md` — independent review (Kimi K3)

  I read the plan in full, the v1.0 ratified plan, brief v1.2 (Appendices B–E), the Astra review, the E1–E8 evidence file, every code path cited, the four governing texts, and the classical corpus at `00_ARCHITECTURE/SOURCE_DATA/classical_texts/`. Labels: **[D]** doctrine (text named), **[P]** practice, **[J]** my judgment, **[U]** could not verify. No number below is asserted as a threshold to adopt — all weights, orbs and tolerances remain proposals for your ruling.

  ---

  ## A. Verdict and blocking gaps

  **Verdict: PROCEED WITH AMENDMENTS.** The plan is the strongest artifact of this campaign I have seen evidence of: the findings register is measured, the design is internally coherent, the runbook is sequenced correctly, and it is unusually honest about what it does not know (§1.6, §13). It is *not yet* complete enough to execute as written. I found no contradiction of D-1, D-3 or R1–R10, and the one D-2 deviation (N-1) is disclosed and correctly routed to you as a ruling, not smuggled in. There are **3 blocking gaps, 6 major gaps, and a set of minor ones**. All are closable inside the plan's own architecture.

  ### BLOCKING

  **B-1. §6.3's `clear_tables` mechanism does not exist in the Clear route — the ledger and coverage relations would be orphaned by every Clear.** The plan (§6.3 row 3, §6.4) says setting `ka_gochara.clear_tables = [kala_gochara_windows, kala_gochara_contacts, kala_gochara_coverage]` means "the ledger/coverage are cleared with the windows." I verified the execute route end to end: `platform/src/app/api/cockpit/clear/execute/route.ts:160-183` resolves clear operations as `EXPLICIT_CLEAR_OPS` → `deriveDeleteSqlFromCountSql(count_sql)` → `target_table` fallback. `clear_tables` is **read nowhere in either clear route** — it feeds only the Atlas display (`atlas/schema/route.ts:19`). Consequence: after the re-pin, a chart-owner Clear of `ka_gochara` deletes `'4.0'` windows (via `count_sql`) but leaves every row of `kala_gochara_contacts` and `kala_gochara_coverage` behind — orphaned testimony rows pointing at windows that no longer exist, a direct §N.3/idempotency violation, and a live inconsistency for integrity conjunct (i). The plan's own statement "needs none unless a JOIN enters `count_sql`" (§6.4, citing `assetClearSpec.ts:52`) is wrong on the code as it stands. **Closes:** either an `EXPLICIT_CLEAR_OPS['ka_gochara']` entry with three generation-scoped DELETEs (windows, contacts, coverage), or wiring `clear_tables` into both preview and execute routes; plus a §10 ecosystem proof row that Clear of `'4.0'` leaves zero rows in all three relations. This belongs in §6.3/§6.4 and WP7. Severity is blocking because the schema (WP6) will otherwise be built around a cleanup contract the runtime does not honor.

  **B-2. S-1's blast radius is understated: `GocharaTransitService` has at least two more consumers than the plan's reader inventory admits.** §2.1 and §6.1 list only Saṅgam (`ka_sangam/engine.py:464`, `:1383`). But `services/kala_trigger/trigger.py:87-199` calls `gochara_service.find_aspects(...)` under an explicit duck-type contract ("Uses the same gochara_service.find_aspects(...) duck-type contract", `:87`), in three call sites (`:96,:150,:199`), and `platform/python-sidecar/scripts/kala_admission/currents.py:59` requires a live `find_aspects` engine. S-1 (§6.2) changes the service's return shape to "kernel episodes with grain, coverage and `contact_id`" — a breaking change for these callers, and `kala_trigger` is the trigger layer for predicate windows (per `STATE_D-3.md:166-168`, it already fans out ~38 `find_aspects` calls per window — S-1 could actually *fix* that cost, but only if scoped). **Closes:** add `kala_trigger` and `kala_admission/currents.py` to §6.1 with an owning packet each (or an explicit backward-compatibility shim commitment in S-1), and add them to the §10 ecosystem proof row. Blocking because a serving-contract change with unenumerated consumers is exactly the F-18 class of defect this plan exists to eliminate.

  **B-3. The H-1 kakṣyā fix as stated may repair the citation without repairing the astrology.** F-09/§8.1 H-1 say "real BPHS Ch.66 kakṣyā boundaries from L1 replace the equal-eighths fixture." Two problems. First, doctrine: in the aṣṭakavarga tradition the kakṣyā divisions of a sign **are** equal eighths — 8 × 3°45′, lords Saturn→Jupiter→Mars→Sun→Venus→Mercury→Moon→Lagna [D — standard BPHS ch.66/Phaladīpikā aṣṭakavarga doctrine as received; **[U] in this corpus**: my corpus survey found *zero* kakṣyā text in the OCR'd BPHS Santhanam vols 1–2 (ch. 66 exists at `BPHS/bphs_vol2_rsanthanam_djvu.txt:35553` ff., but no kakṣyā subdivision passage was found — possibly OCR loss; this must be settled at WP1 against a cleaner edition)]. So the fixture at `primitives.py:664-719` is likely *geometrically correct but uncited*, and what is chart-specific is not the boundary grid but the **bindu content** of each kakṣyā per transiting planet (Bhinnāṣṭakavarga). Second, the count: L1 holds 120 rows × 3 keys for `ashtakavarga_kakshya_boundary` (App. A 22) — 12 signs × 8 kakṣyās = 96, so 120 encodes something else (per-planet? per-lord?). WP1 must establish what those rows actually are. The real defect in F-09 is that 147/202 sentences are kakṣyā crossings computed **without bindu qualification**, which is why served activity saturates at [0.99964844, 1.0]. Swapping an equal-eighths fixture for equal-eighths L1 rows changes provenance but not saturation. **Closes:** restate H-1 as "kakṣyā crossings qualified by the chart's real BAV bindu for the transiting planet in that kakṣyā (no bindu → no sentence, or a distinctly-weighted sentence), with the classical citation verified at WP1"; if the corpus cannot substantiate ch.66, the citation claim must be downgraded until the source is ingested (see M-3 below). Blocking because WP5's exit gate ("served-path behaviour matches its documentation") would otherwise certify a fix that leaves the F-09 mechanism intact.

  ### MAJOR

  **M-1. Published-generation protection gap.** Runbook step 3 (§9) guards `'v1'` and `'3.0'`; Clear of `ka_gochara` is *by design* able to delete `'4.0'` (§6.4). But if a chart owner Clears while `kala_gochara_authority.authoritative_generation='4.0'`, the authority row and the `published` manifest now point at a void — serving returns empty under a `published` label, which is precisely the "incomplete full-horizon asset appears built" failure the Strategy §5 forbids. **Closes:** Clear refuses (or requires super_admin + typed confirmation) when the target generation is the authoritative one, or cascades an authority reset + manifest `cleared` status; add an integrity conjunct "authority generation must have ≥1 window row and a `published` manifest." §6.3/§9 step 3.

  **M-2. P-1a as designed is label-brittle against the plan's own rebuild mechanism.** §4.7 says a rebuild after publication is a *new label* (`'4.1'`, …), and §4.7/§6.2 P-1a adds "an explicit `'4.0'` branch" in `buildSourceCitation` (`register_gochara_windows.ts:573-598`). A hardcoded `'4.0'` branch sends `'4.1'` to the v1 fall-through — the exact bug P-1a exists to fix, recurring on the first rebuild. **Closes:** make the branch *manifest-driven*: for any generation with a `kala_gochara_publication` row, provenance is read from the manifest (`writer_asset_id`, `convention_id`, `manifest_id`); string branches remain only as legacy fallbacks for `'v1'`/`'3.0'`/`g3_*`. Same for `computeGocharaCoverage` `:966`. This is cheaper than it sounds because N-10 already creates the manifest table. [J]

  **M-3. The classical corpus does not contain the texts this family's overlays cite.** The corpus holds BPHS (Santhanam OCR, ~88/97 chapters), Jaimini (Adhyāyas 1–2 only), and KP Readers 5–6. **Phaladīpikā, Sārāvalī, Bṛhat Saṃhitā, Jātaka Pārijāta, Praśna Mārga, Uttara Kālāmṛta are all absent as texts** (cited only, by translators and KP). Yet the schema and plan lean on them: `bg_phaladeepika_latta` and the "Phaladeepika malefic-count→effect-grade scale" (`context.py:553`), Moorti nirṇaya (absent from corpus entirely — every `murti` hit is the surname "Krishnamurti"), Sade-Sati (absent from the BPHS OCR; present only in KP vol5 at `KP/kp_reader_vol5_djvu.txt:15105-15113`, a modern source), the gochara-from-Moon house effects and vedha tables (KP vol5 `:18082-18277`, `:1310-1347`, explicitly citing Phaladīpikā at `:18274`). Per your priority (1), an acharya-grade elevation cannot keep running overlays whose named classical basis is not in the corpus to verify. **Closes:** an L0/source-ingestion action (Phaladīpikā gochara + moorti chapters, Sārāvalī gochara, Bṛhat Saṃhitā transit chapters) as a declared prerequisite of WP9, with the overlay writers' citations re-grounded; until then every Moorti/latta/vedha claim carries `uncited_extension=true`. [J] — **[D]** for the absence statement: corpus inventory verified by direct search.

  **M-4. The century writer remains registered `is_active=true` and dual-writes production `'3.0'`; the "hold" is procedural only.** `ka_gochara_v3_century_materialize.py` is `@register`ed (`:1717`), seeded active (`supabase/migrations/560_…:31-51`), and its `run_substep` DELETE+INSERTs `kala_gochara_windows` `'3.0'` (`:2253-2261, :2328-2357`) in the same transaction as its staging write. The step-3 guard (§9) will make those DELETEs fail loudly — which enforces the hold but turns any accidental century run into a crashing writer, and until step 3 lands the rollback surface is one orchestrator misfire away from being rewritten, falsifying §4.7's "rollback = re-point to `'3.0'` (its rows are never touched)". **Closes:** state in §9 that post-step-3 the century writer is expected to fail on its prod write; set `is_active=false` for the century at step 3 (reversible at WP10), preserving `g3_*` staging as the calibration corpus the plan wants. §2.2/§9.

  **M-5. Moon partitions never enter the coverage manifest.** §4.4 keys coverage by (body × target_type) / event class, but §4.2 keeps the Moon on-demand and unpersisted (R7). A served answer whose underlying testimony is a Moon contact then has no manifest row, and L3-Q08's "is no window found a real negative?" cannot be answered for Moon-dependent classes. **Closes:** the on-demand Moon search writes (or derives at serve time) a coverage record for the requested interval — P-4's read capability should expose `searched_horizon` for on-demand partitions too. §4.2/§4.4. [J]

  **M-6. The §10 "Value" row depends on artifacts that are not on main.** The plan itself discloses (§13) that `KALA_COST_PROFILE_v1_0.md` and `KALA_BASELINE_v1_0.md` exist only in the `setup` worktree. Without them the §10 Value row and WP4's cost profile are NOT_RUN, not PASS — the plan says this honestly but does not gate WP1 on landing them. **Closes:** add "land both artifacts on main (or formally withdraw the rows)" to WP0/WP1 exit. Minor as a plan edit, major if ignored: per §N.8 a value claim with no detector behind it is null, and these two rows are the value claim.

  ### MINOR

  - **m-1.** §4.2 spike (App. C) used `sid = tropical − ayanamsa_ut` rather than `FLG_SIDEREAL`; the plan discloses this (App. C setup) and D-1 pins `FLG_SIDEREAL`. WP3a's exit gate must re-run the whole spike matrix under `FLG_SIDEREAL` with `retflag 258` asserted *and checksummed*, and the spike must be committed — "scratch; not committed" (App. C header) means the evidence is not reproducible. Closes at WP3a as written; flagging so it is not negotiated away.
  - **m-2.** E8 counterexamples not yet in the spike: 360/0 wrap tangency, close-station pairs ([1.4,1.6] d), start-inside episode. WP2 lists them as fixtures — good — but App. C's "retrograde and directed cases exercised" should not be read as seam/tangency coverage. [X from E8; plan consistent]
  - **m-3.** `contact_id` (§4.3) hashes `round(t_exact, 60 s)`: stable under re-partitioning (tested in §10), but an M-1 ruling that changes declared orbs changes `t_in`/`t_out` while `t_exact` holds — ids survive, good; however a *tolerance* change under the same `convention_id` could shift `t_exact` by <60 s and silently keep the id while the row changed. Add `tolerance_arcsec` (or a method-version) to the hash input, or declare tolerance changes always imply a new `convention_id`. §4.3.
  - **m-4.** Arc reuse across charts is never stated. Arcs are pure geometry (body × knot table), chart-independent; building them once globally and solving contacts per chart is allowed under Strategy §5 P3 ("shared geometry requires identical complete astronomical/target context") and is likely the single largest build-efficiency lever after context-fetch-once. §4.5 should say which. [J]
  - **m-5.** Registry re-pin (§6.3 step 5) lands before the candidate build (step 6): between them, cockpit stats read 0 for `ka_gochara`. Harmless but should be stated so it is not misread as a regression during the window. §N.4 cockpit-truth.
  - **m-6.** F-12's fix (R-5: first-root retention + `afflicted` qualifier) is listed under G-R but does not appear in WP3c's exit gate by name; add it.
  - **m-7.** Sign discrepancy in the historical record, not the plan: Astra F4 reports the 2020/2026 convention gaps as −16.518″/+5.522″, brief v1.2/plan as +16.516″/−5.523″ (magnitudes agree, signs flipped). Whichever is right, the D-1 rationale is unaffected, but WP0's "re-validate at implementation" should pin the sign convention in the registered finding.

  **Internal-consistency and ratified-decision check.** No contradiction with D-1 (the plan's spike convention is disclosed and gated), D-3 (the H/M split is carried correctly; N-12's classification of target corrections as honesty fixes is a *legitimate* extension of D-3's own distinction — the code claims a sensitivity the cited fact denies), R1–R10 (R2 one-way dependency kept; R3 honored via `timestamptz` + `claim_grain`; R7 kept; R9 honored for `bg_gochara_arcs`). N-1 deviates from D-2's ratified sequence *and says so* (§1.1, §1.3) — that is a proposal to you, not a violation. Binding constraints: no invented computed values (F-19/F-20/F-21 are removals, not inventions); L1 authority respected (`target_longitude_deg` is explicitly a "reference copy", §4.3); no confidence scalar (§4.8); no forbidden shortcut (§4.5 states it twice); `v1`/`'3.0'` untouchable; nothing retired; WriterBase contract untouched. One tension worth naming, not a violation: §N.3 delete-then-insert vs §4.7 immutability is reconciled by the candidate/published label rule, but the writer must refuse delete-then-insert against a `published` generation — put that in WP6's exit gate.

  ---

  ## B. Astrological review

  ### B.1 Contact object and episodes (§4.2–4.3)

  Are `t_in / t_exact / t_out`, orb, applying/separating, station branch and dwell the right primitives? **Yes — as an honest superset of what the texts support, and the plan is right to store them as geometry with declared orbs rather than as doctrine.** Detail:

  - **Sign-ingress and residence (rāśi gochara):** the classical core. Gochara in the received tradition is judged by sign counted from the natal Moon (and per-house results per planet) — [D] as reproduced in KP vol5 (`kp_reader_vol5_djvu.txt:18082-18277`, explicitly attributing the per-planet house results to Phaladīpikā at `:18274`) and reflected in BPHS ch.31's translator note on gochara vedha (`bphs_vol1_rsanthanam_djvu.txt:24414-24424`). **[U] caveat:** the Phaladīpikā text itself is not in this corpus (M-3 above).
  - **Transit to natal planet positions (not just Moon):** [D] BPHS ch.66–72 aṣṭakavarga transit doctrine judges transits relative to *each natal planet's* position (`bphs_vol2_rsanthanam_djvu.txt:35830` ff.: houses from the natal Sun etc.); ch.70 derives nakṣatras from rekha counts and times events by Saturn's transit over them and their trikoṇas (`:40770-40809`). This is the strongest classical anchor for the family's whole target architecture — transit-to-natal-point, per-planet — and the plan should cite ch.66–72 explicitly as the doctrinal spine, not only for kakṣyā (H-1).
  - **Conjunction to a degree with an orb:** [P]. Classical gochara is sign-based; degree-exact transit-to-natal-point with orb is the universal living practice (and KP's entire method, e.g. transit to cusps, `kp_reader_vol5_djvu.txt:1089-1092`). The plan's `orb_source` field (§4.3) is exactly the right honesty mechanism — the orb is declared practice, cited or `uncited_extension=true`.
  - **Dṛṣṭi in transit:** the per-graha special aspects (Mars 4/8, Jupiter 5/9, Saturn 3/10, full 7th for all) are [D] BPHS ch.26 (`bphs_vol1_rsanthanam_djvu.txt:16496-16505`) — correctly implemented at `primitives.py:189-196` and exercised on 379/914 served rows (F-22). Applying them in *transit* specifically is [P] (the chapter is about natal charts; KP vol5 does use transit aspects, `:1134`). Notably, ch.26 ślokas 6–8 define **dṛṣṭi-kona**, graduated aspect strength by longitudinal separation (`:16514-16529`) — this is genuine [D] support for degree-sensitivity and hence for orb-decay-shaped activity (M-1), though it prescribes no cutoff orb.
  - **Applying/separating, station branch, dwell:** [P] (Tajika-influenced practice; no statement in this corpus). Keeping them as geometry fields costs nothing and is what lets M-2 be evidence-ruled later. Correct.
  - **Return (e.g. Saturn return):** [P]/modern; harmless as a relation, must carry `uncited_extension`.
  - **Kakṣyā crossing:** doctrinally grounded in the aṣṭakavarga tradition [D, with the corpus caveat of B-3] — but only meaningful when bindu-qualified; see B-3. **Nakṣatra ingress:** [D] via BPHS ch.70 as above.

  ### B.2 Target-resolution contract (§5.3)

  This contract is the best section of the plan. Per type:

  (a) **Ārūḍha as sign-level — correct, and doubly so.** Doctrinally, ārūḍha is a Jaimini construct and Jaimini's aspects/contacts are **rāśi-to-rāśi** [D — Jaimini sūtra tradition; the corpus's KVA edition covers Adhyāyas 1–2, which is where ārūḍha is defined]; the degree-bearing "ārūḍha longitude" is a later computational habit. Empirically, F-20 shows every stored `arudha_pada.longitude_sidereal` is a sign-cusp placeholder (30·(sign−1)), so a degree-level treatment would fabricate contacts at cusps — a B.10 violation. Treating arudha as a residence span is the only honest option. **[J]**

  (b) **Negative sensitive-degree checks as non-targets — correct.** `not_gandanta` is the *absence* of a condition; there is no point on the wheel to be contacted. The 22 positive rows (papa/śubha kartari, pushkara) being merged into the subject graha's `independence_group` is exactly L3-U02-compliant (Strategy §7:442 — "another representation of the same origin adds no independent support"). N-12 as honesty fix: I agree. **[J]**

  (c) **`lord` via whole-sign house lordship — correct.** Standard Parāśari whole-sign convention, matches `enrichment.py`'s existing LAGNA-offset convention (`:193-217`), deterministic. [D/P]

  (d) **Yoga constituents as one `independence_group` per yoga — correct.** A yoga is one configuration; its constituents are not independent witnesses (L3-U02, F05). Carrying `bhanga_active` as qualifier-not-weight is the right restraint. [J]

  (e) **Mechanism nodes as residence spans — honest, and the right interim.** Double-transit-style mechanisms (Jupiter/Saturn sign triggers) are [P] — a living-tradition technique with no statement in this corpus. Making them interval objects pending M-4's operand audit (R5) is correct; the trap to avoid is letting sign-ingress residence silently masquerade as a *point* contact in served prose (§N.7). [J]

  **Where bhāva-madhya matters (M-5):** in the classical whole-sign tradition, nowhere — there is no transit-to-cusp doctrine in BPHS or in this corpus at all; transit-to-cusp is **KP doctrine** [D — KP vol5 `:1089-1092`, `:1132-1134`]. My recommended M-5 ruling below reflects this.

  **Doctrine for Gulika/Māndi, Prāṇapada, special lagnas, nakṣatra pādas, bhāva-ārūḍhas (E-1/M-6):** Corpus findings: Gulika — one rule, quoted by KP vol5 from the tradition: "expect the death whenever Saturn transits in Rasi and Navamsa occupied by (A) the lord of the 8th house (B) Gulika (C) Saturn or (D) the lord of the 2nd decanate from the ascendant" (`kp_reader_vol5_djvu.txt:1737-1740`) — [D] via KP's citation of tradition, and BPHS ch.3/ch.25 establish Gulika/Māndi as computed natal points. Prāṇapada and the special lagnas: **natal-only** in BPHS ch.3/ch.5; no transit-contact doctrine anywhere in the corpus [U-absent]. Bhāva-ārūḍhas: same Jaimini logic as (a) — sign-level. Nakṣatra pāda sensitive points: natal-sensitivity doctrine only. **Which E-1 class to admit first: Gulika/Māndi** — it is the only class with a transit doctrine that names it, the L1 degrees are real (F-20 contrast), and the classical use (Saturn contact for difficult timing) is exactly what a contact ledger serves. Second: bhāva-ārūḍhas as interval targets (free, once arudha is re-typed). Defer special lagnas, Prāṇapada, nakṣatra-pāda points until a text is in hand — admitting them now would repeat F-19's sin in reverse.

  ### B.3 Node convention (N-4/N-4a)

  The texts are silent — node computation models postdate all of them [U-absent from corpus]. Living practice is genuinely split: the Lahiri ephemeris tradition and most North-Indian practice/software default to the **mean** node; Jagannātha Hora and a vocal minority use **true**. The decisive argument here is not tradition but **internal consistency**: the served L1 natal is mean (`RAH_MEAN`, pāda 3 of Rohiṇī, fact `c520713087b97470`), and comparing a true-node transit against a mean-node natal mixes two definitions of the same point — the 6.3″ knife-edge in brief App. C (noon knot → pāda 3, birth instant → pāda 4) shows the choice is reading-visible. **Ratify N-4 (mean).** [J]

  On N-4a: deriving mean analytically from stored true-node knots is *acceptable* engineering — the convention vector makes the doctrinal choice visible, so nothing is hidden — but it inserts a statistical model (regressing out the true node's oscillation) between the data and the answer, with a residual bound you must then carry. Since the kernel already calls Swiss for bisection at the instant (§4.2, App. C), the cleaner option is **(b′): the kernel computes the mean node directly from Swiss (`SE_MEAN_NODE` under `FLG_SIDEREAL`) at arc-build and solve time** — no L0 change, no regression model, no new ephemeris pass, identical doctrinal content, smaller tolerance budget. This does not contradict D-1 (it *is* the D-1 method applied to the node body). If knot-table purity is preferred for reproducibility, (b) stands, but then WP1 must publish the regression spec and its residual bound as part of the convention vector. Falsifier for either: arc/solve residuals vs direct Swiss mean exceed declared ε. **[J]**

  ### B.4 Scoring projection (§4.5, §8)

  Keeping λ_v3's algebra as a projection over the ledger **preserves** the astrology, provided the §4.5 equivalence contract (no missed narrow/station/retrograde events, complete boundary joins, same finite-value policy) is enforced by the WP4 decomposed comparison — because the algebra's inputs (which contacts, when, how strong) are unchanged; only their enumeration becomes complete and honest. The distortion risk is not in projection but in **saturation**: with activity ∈ [0.99964844, 1.0] on every populated served row (F-09/F-14), the activity factor currently transmits ~nothing, and the projection will faithfully reproduce that nothing until H-1 is fixed *properly* (B-3).

  **Which H is not purely an honesty fix:** **H-1**, as written — it changes computed activity values, not just their documentation (and per B-3 it may not even change them in the right way). H-5 (removing the 3-peak cap) also changes served content, but it restores what the code already claims, so it stays honest. H-6 changes activity numerically wherever duplicate targets exist — defensible as honesty (L3-U02 is a contract, not a preference), but the native should see a before/after λ distribution for the affected rows at WP5 even though pre-approval stands. [J]

  **Rule first: M-5, then M-1** (see §E). M-1 gates the actual user-visible value of the whole elevation (F-02's 13 envelope-only classes become timed only when activity is orb-shaped instead of a ±5 d box).

  **Classically load-bearing factors missing from the list:**

  1. **Aṣṭakavarga bindu weighting of the transited sign/kakṣyā** — the classical transit-quality layer par excellence [D — BPHS ch.66–72; transit results by bindu-bearing houses from each planet, `:35830` ff.; ch.72 rekha-count auspiciousness, `:42582-42786`]. The PERMISSION factor reads `bg_transit_av_gates` (`context.py:355`) — 8 rows of thresholds — but the F-08/Astra finding that W21 substitutes `min_sav_score` for chart bindus shows the real per-chart BAV is not wired. This is the single most classical enrichment available and belongs in this elevation via M-4's audit. **[D][J]**
  2. **Vedha specifics:** vedha is present as an overlay, but the corpus (KP vol5 `:1310-1347`, `:1341-1342`) records the **exceptions** — no vedha between Sun and Saturn, nor between Moon and Mercury — and BPHS ch.31's note records **vipareeta vedha** (a second planet joining the transiting planet cancels the obstruction, `bphs_vol1_rsanthanam_djvu.txt:24436-24441`). Whether `ka_vedha_gochara` implements either is [U] — an audit belongs in WP9. An obstruction that cannot be cancelled is not the classical vedha. **[D]**
  3. **Sade-Sati phase distinction:** quality gates already read `sade_sati_phase` (`context.py:405`; 4,492 rows, 41 keys), so phases exist as inputs — but the classical/living-tradition distinction (rising/peak/setting, and the dhajjā-based refinement of practice) should appear as *qualifiers*, not blended weights. [P; absent from classical corpus — U]
  4. **Retrograde/station weighting:** no classical transit statement in the corpus [U]; practice diverges. Keep as geometry (branch field) and let M-2-style evidence decide. Do not add a doctrinal weight. [J]
  5. **Latta:** the table `bg_phaladeepika_latta` exists but is unwired, and Phaladīpikā is not in the corpus — citation verification first (M-3), then M-4. [U]
  6. **Tārā-bala:** already a factor (tārā) — the corpus's tārā material is daśā/longevity-context (BPHS ch.46 Tara Dasa, `bphs_vol2:8854-8893`; ch.44 maraka tārās) rather than transit-strength, so the tārā factor's citation should be checked at WP5 as part of the H-fix documentation sweep. [U]

  ### B.5 Overlays (§5.4)

  - **Moorti at the true ingress instant: right.** Mūrti nirṇaya is *defined* by the Moon's position relative to the natal Moon at the moment a planet enters a sign — a day-grade is an approximation of an instant-defined quantity [P; classical basis [U] — the doctrine is absent from this corpus, reinforcing M-3]. Reporting the day-grade misclassification rate (§9 step 9 gate) is the right honesty.
  - **Vedha `independence_group`: right** — one obstruction attenuates once (L3-U02); this is H-6 applied to overlays and is consistent.
  - **Kota proposed-use gate: right** — Kota chakra transit doctrine is specialized practice with no corpus anchor; operand audit (M-4) before any scoring use is exactly R5's discipline. [J]

  ---

  ## C. Engineering review

  ### C.1 Kernel (§4.2, App. C)

  The design — monotone-arc index over splined noon-UT knots for *candidate bracketing*, direct Swiss bisection for *refinement*, explicit station/wrap structure, no station coalescing — is sound and is the correct shape for this problem: the spline is a search accelerator, never the authority; Swiss at the instant is. The spike (App. C) is genuinely encouraging: stored knots reproduce Swiss to 0.002″, all contacts within ≤0.96″/seconds of direct solves, retrograde triple-pass at the 270° dṛṣṭi exercised, clean negative control. **[X]**

  What is missing before WP3a can be trusted:

  1. **The spike is uncommitted scratch** (App. C header). It must land as a reproducible test with pinned inputs, or it is a claim, not evidence (§N.8). [J]
  2. **Convention**: spike used tropical-minus-ayanamsa; D-1 pins `FLG_SIDEREAL` (≤16.5″ nutation gap). Re-run under the pinned convention — residuals quoted "against the same convention it used" prove spline fidelity, not D-1 compliance. [X][J]
  3. **The E8 counterexample classes**: wrap tangency at 0/360 (w2g found 0 of ≥1), close-station pairs ([1.4, 1.6] d, w2g kept one), start/end-inside episodes (w2g dropped them). WP2 lists these as fixtures — the kernel must demonstrate them on *real* arcs, not only synthetic cubics. [X]
  4. **The Moon at spike scale**: two Moon cases is a smoke test; the Moon is where the arc index earns or loses its keep (fast, always direct, frequent crossings). WP3a should include a Moon sweep with an independently enumerated candidate set — the exit gate already says "candidate set independently enumerated"; keep it. [J]
  5. **`near_station_unresolved` semantics** (§4.2): define what happens downstream — a flagged episode must propagate `completeness_state=unqualified` into the ledger and the manifest, not just a boolean. Otherwise the flag is a signal with no detector behind it (§N.8). [J]

  The ε/tolerance contract (`tolerance_arcsec` + `bracket_seconds` per row, recorded backend + checksums, `NOT_RUN` on `retflag & 4`) is sufficient and is one of the plan's best engineering features — it converts F-14's silent Moshier fallback into a structural impossibility for the gate. One addition: per **relation-class** tolerance floors (a dṛṣṭi exact and a conjunction exact need not share one ε). [J]

  ### C.2 Ledger and publication (§4.3, §4.7)

  - **Schema gaps:** (i) no column ties a contact to the *overlay state* at the contact instant (vedha active/moorti grade) — the projection recomputes these as interval sets, which is fine, but then §10 should prove overlay-at-instant reproducibility; (ii) `comparable_with` is declared but its vocabulary is not pinned in WP1 — pin it or it becomes a free-text field; (iii) the coverage manifest (§4.4) lacks an explicit `moon_on_demand` partition marker (M-5 above). [J]
  - **`contact_id` stability:** sound under re-partitioning by construction (SHA-256 over identity fields + rounded `t_exact`); the §10 identity test is a real detector. See m-3 for the tolerance-change edge.
  - **`'4.0'` label vs serving routing:** the plan's analysis is *verified correct at source*: `buildSourceCitation` (`register_gochara_windows.ts:573-598`) branches only `'3.0'`/`g3_*`, and the authority filter (`:641-643`) defaults to `'v1'` — a bare `'4.0'` falls through to the v1 citation branch. The label choice plus P-1 is the right call, **with the M-2 amendment (manifest-driven branch, not a hardcoded `'4.0'` string)** so that `'4.1'` works without another serving-code edit.
  - **Immutability rule:** candidate/published/superseded/rolled_back with new labels after publication is correct and cleanly reconciles §N.3 (delete-then-insert within a candidate) with immutability (never against published). Add the writer-side refusal to WP6's gate (A-final note above).
  - **Size estimate:** 2–5×10⁵ rows/chart at ~400 B → 100–200 MB/chart is plausible arithmetic [I as labeled]; the Moon-on-demand default is what keeps it bounded. Measured at WP4 — acceptable. One unpriced item: **index cost** — every serving query hits (chart_id, generation, time-range); the WP6 migration should include the covering indexes, and WP4 should measure query latency, not just bytes. [J]

  ### C.3 Upstream (§5)

  The inventory is complete for what the kernel reads: `ephemeris_daily` knots (825,084 rows, 1900–2150 — comfortably over the century), L1 positions/AV/Sade-Sati/daśās/yoga-firings, overlays, rules tables. Three notes:

  1. **The tropical arc table is correctly abandoned, not consumed** (F-07: `w2g/db_source.py:35,80,178` tropical, confirmed; E1's 763-day Saturn error is the measured cost). R9's register-label-don't-delete for `bg_gochara_arcs` is right.
  2. **Input generation vector (§5.5)** is sufficient to catch F-21-class drift — build ids + row counts + `substrate_version` + convention id, and "differs → new candidate" is the right semantics. One strengthening: include **content digests**, not just counts, for the small reference tables (`bg_transit_rules` 75 rows, `bg_transit_av_gates` 8 rows) — a count does not catch an in-place edit of a threshold row. Cheap. [J]
  3. **Not sourced, and should be named:** the per-graha **orb table** the kernel will declare (`orb_source`) — WP1 must say where orb values come from (practice table, `uncited_extension=true`), or the ledger ships with an undocumented doctrine input. Same for the kakṣyā lord order (B-3). [J]

  ### C.4 Downstream (§6)

  The reader inventory is accurate for the three MCP tools, checklist, D8/D9, Kṣetra, Saṅgam, L5, cockpit — **but incomplete for the service consumers** (B-2: `kala_trigger/trigger.py:87-199`, `kala_admission/currents.py:59`). That is the only inventory miss I found, and it is blocking-adjacent because S-1 changes the contract those callers duck-type against.

  - **P-1** correctly scoped; amend per M-2 (manifest-driven). **P-2** correctly scoped (`reading_checklist.ts:1063-1092` drops ids today — carrying `contact_id`/`completeness_state` through the capped page is the fix, and §N.6's density layering belongs there). **P-3** (L5 `contact_id`) correctly scoped and required for frozen-claim identity. **P-4** is the right new capability; give it the Moon-coverage requirement (M-5/§4.4). **S-1** correctly scoped in content, wrong in blast radius (B-2). **V-1/K-1** correctly scoped and correctly assigned to their owners; K-1's pin-by-(generation, id) is the right answer to F-18's row-id edges, and §9's Kṣetra note (old edges keep pointing at `'3.0'` ids, which persist) is verified-safe because `'3.0'` is never deleted.
  - **Registry re-pins (§6.3):** `count_sql`/`target_table`/integrity conjuncts (a)–(j) are complete and the N-9 (iii) conjunct (`target_table` = `count_sql` relation) is a genuinely good invariant. **But** the `clear_tables` row is inoperative (B-1), and conjunct (i) (window `active_sentences` ids resolve in the ledger) needs the orphaned-ledger case handled after a Clear — B-1 again. `depends_on` re-pin to `[bg_ephemeris, ka_gochara_resonance, ka_vedha_gochara, ka_moorti_nirnaya, ga_positions, ga_dashas, ga_yoga]` is correct; consider adding `bg_transit_rules` explicitly (mechanism_node resolution reads it through resonance, but the kernel's Vedha house rules read it directly per §5.1). Digest 1018 handling (retain for `'2.0'` residue until N-11, new spec for `'4.0'`) is safe.
  - **Clear route safety after Phase 1.1:** the `is_active=false` exclusion makes the retired sweep unreachable, and the (table, generation) trigger makes `'v1'`/`'3.0'` undeletable even by direct SQL — defense in depth restored in the form migration 588's own note recommended. Verified against `execute/route.ts:84-108,160-183`: with the sweep withheld from scope, its `count_sql` can no longer be derived. Correct — *for the assets that exist today*. Add a regression test that a future asset with `target_table=kala_gochara_windows` and no generation predicate in `count_sql` fails registration (the trigger is the backstop; the registry linter is the fence). [J]

  ### C.5 Cutover and rollback (§9)

  The ten-step ordering is correct, and the two resequencing risks are the ones the plan itself names:

  - Step 0 (Close Clear path) must precede 1–3 — N-1's whole point; if step 3 (guard) ran before step 0, the route could still reach `'v1'` via the sweep's `count_sql` and the trigger would be the only barrier; if step 2 (drill) ran before step 1 (grant), it fails on privileges (F-05). Correct as ordered. **[S-verified]**
  - Step 5 (re-pin) before step 6 (candidate build): a transient 0-count window on cockpit (m-5) — acceptable, but if reversed, the integrity conjuncts would be scoped to `'2.0'` while `'4.0'` rows exist, breaking conjunct (f). Current order is right.
  - Step 7 (four flip gates) before step 8 (flip): right, and each gate is a test against deployed tools — this is what makes the flip boring.
  - **Rollback is genuinely provable**: authority re-point to `'3.0'` + manifest `rolled_back`; the serving filter is authority-driven (`:641-643`), `'3.0'` rows are never touched and are trigger-protected from step 3 onward, and step 7 *rehearses* rollback through the real adapters before the flip. That is a rehearsal, not a hope — the strongest rollback discipline in the document. One residual: post-flip Kṣetra builds read `'4.0'` and pin new edges; a rollback leaves Kṣetra's *new* edges pointing at `'4.0'` rows that still exist but are no longer served — coherent, but the §9 Kṣetra note should say so explicitly. [J]

  ### C.6 Tests (§10)

  Mostly real detectors. Assessment per row:

  - **Vacuous as written: none entirely — two are conditional.** The **Value** row is NOT_RUN until `KALA_BASELINE_v1_0.md` lands on main (M-6); the **Delivery** row is honestly marked NOT_RUN until Pūrṇa. Both are fine *if* the NOT_RUN is enforced by CI rather than by memory — put the artifact-presence check in the test itself. [J]
  - **Boundary/precision** ("runs only where `retflag & 2`; on `retflag & 4` the run is NOT_RUN") — the model for every gate in this project; non-vacuous by construction. 
  - **Duplication** ("one physical contact via 3 targets → one `independence_group`; activity unchanged") — real only if the WP2 factorized oracle computes the expected activity *independently*; otherwise "unchanged" compares the system to itself. WP2 already promises a hand-specified oracle; hold it to that. [J]
  - **Revision** ("same inputs on two dates → byte-equal") kills F-06 properly; add the century writer's birth-epoch defect class (#2534) as a second-chart fixture at step 9 — the runbook names it, the proof matrix should too. [J]
  - Missing row: **Clear/cleanup proof** (B-1) — Clear of `'4.0'` must leave zero rows in all three owned relations and a coherent authority/manifest state. Add it to the Ecosystem row.

  ### C.7 Efficiency (§4.5)

  The design credibly removes the per-instant search without any forbidden shortcut: the measured cost structure (64 ms/evaluation, ~92 % repeated search, kakṣyā 59 % of it, context fetched ×270 substeps) maps one-to-one onto the fixes (precomputed episodes → closed-form 0.013 ms/instant [X]; per-chart context once; span-aware algebra). No cap, grid, or horizon reduction is anywhere in the mechanism — the §5 P3 equivalence contract is stated as a gate, not a hope. For the speedup claim to be honest, `KALA_COST_PROFILE_v1_0.md` must measure: (i) prepare/search/query separated, cold and warm; (ii) **kernel arc-build + contact-solve time per chart** (the new cost the plan adds — currently unpriced); (iii) ledger write + index time at WP6 measured scale; (iv) end-to-end wall for the canonical chart vs the 58.2 min best-run baseline, with the same workload — and it must *not* compare against the 20–25 h figure, which §1.6 honestly marks [U]. Arc reuse across charts (m-4) is the legitimate multiplier to claim next. [J]

  ---

  ## D. Elevation, enrichment, improvement

  What would make this family better than a senior ācārya's transit reading is already latent in the design: **complete enumeration with honest negatives**. An ācārya samples; the ledger exhausts — every station, every retrograde re-contact, every seam, with a coverage manifest that makes "no window" a *searched* negative (L3-Q08) rather than an unexamined one. That is the superhuman part, and §4.2–4.4 already buy it. What the ācārya still has that this family lacks is *qualitative discrimination* — which contacts matter, how much, and what obstructs them. Ranked enrichments:

  1. **Bindu-qualified transit quality (BAV/SAV per sign and kakṣyā).** [D — BPHS ch.66–72, `:35830` ff., `:42582-42786`.] Replaces the saturated activity channel with the classical quality gradient; simultaneously fixes F-09 *in substance* (B-3). Cost: medium (L1 already holds 9,880 AV rows; the work is wiring per-planet bindus into activity, via M-4's audit). **This elevation.**
  2. **Vedha completeness: exceptions + vipareeta cancellation.** [D — KP vol5 `:1310-1347`, `:1341-1342`; BPHS ch.31 note `:24436-24441`.] Audit `ka_vedha_gochara` in WP9; an uncancellable obstruction overstates difficulty, a missing exception overstates it differently. Cost: low. **This elevation.**
  3. **Sade-Sati phase qualifiers on Saturn contacts** (rising/peak/setting as testimony qualifiers, not weights). Inputs exist (`sade_sati_phase`, `context.py:405`). [P]. Cost: low. **This elevation.**
  4. **Gulika/Māndi as degree targets** (E-1 first class). [D — KP vol5 `:1737-1740` citing the tradition; BPHS ch.3/25 for the points natally.] Cost: low (real L1 degrees exist). **This elevation, via M-6.**
  5. **Bhāva-ārūḍhas as interval targets.** [D — Jaimini rāśi logic.] Nearly free once arudha is re-typed (§5.3). **This elevation, via M-6.**
  6. **Classical-source ingestion for the overlays** (Phaladīpikā, Sārāvalī, Bṛhat Saṃhitā transit chapters) — not an algorithm enrichment but the citation foundation for Moorti/latta/vedha/gochara-from-Moon. Cost: an L0 ingestion task. **This elevation (prerequisite for WP9's claims), per M-3.**
  7. **Latta wiring** — wait: no text in corpus, table unwired. After (6), through M-4.
  8. **Special lagnas / Prāṇapada / nakṣatra-pāda points as targets** — wait: no transit-contact doctrine found [U-absent]; admitting them now is F-19 in reverse.
  9. **KP-school cusp contacts as a named school variant** — wait: real doctrine but a different school (KP vol5); belongs to the multi-school track with a school label, not blended into the Parāśari projection (F08: never average incompatible schools).

  ---

  ## E. Per-decision recommendations

  **N-1 (re-sequence D-2: Clear path → grant → drill → guard) — APPROVE.** The ratified order cannot execute (the drill is privilege-blocked, F-05) while the deletion path is live and ordinary-user-reachable (F-3; verified `execute/route.ts:160-183` derives the sweep's `count_sql` DELETE with no generation predicate). Closing the path first reduces exposure *during* the drill. Cost: negligible; one route PR + tests. Falsifier: none credible — unless the route fix itself introduces a regression, which the preview-hash mechanism and a route test bound. Not deciding: how the 2,667 uncovered archive ids (F-05) are re-archived — that is a separate backup-policy task. This amends a ratified decision; it needs your explicit ruling, and the plan correctly asks for one.

  **N-4 (mean node) — APPROVE.** Texts silent [U]; practice split; the deciding fact is that served L1 natal is mean (`RAH_MEAN`, pāda 3, `c520713087b97470`) and mixing conventions compares two different points. Blast radius measured (232/914 served rows). Cost: a one-line convention plus the N-4a mechanism. Falsifier: a governing-text or school mandate for true nodes applying to this product's declared school — none exists in the corpus. Not deciding: L0's receipt discrepancy (G-6) or L1's nutation offset (G-4).

  **N-4a — AMEND to (b′): kernel computes mean node directly from Swiss (`SE_MEAN_NODE`, `FLG_SIDEREAL`) at arc-build/solve time; the true-node knots are retained untouched; the L0 receipt discrepancy routes to L0's owner (G-6) as proposed.** Same doctrinal content and same "no L0 change" property as the plan's (b), but removes the analytic-regression layer between knots and answer — one less model to specify, bound, and audit (App. C already proves direct Swiss solves at ≤0.96″). Cost: trivial — the kernel already calls Swiss at the instant. Falsifier: kernel-vs-direct-Swiss residuals exceed declared ε on node contacts (the WP3a gate measures exactly this). If you prefer knot-table purity (no Swiss call except refinement), the plan's (b) is acceptable — but then WP1 must publish the regression spec + residual bound in the convention vector. Not deciding: whether L0 should *also* store mean-node knots (L0's owner, with G-6).

  **N-5 (`ka_gochara` owns the served product) — APPROVE.** It is already the registry authority (`sweep.superseded_by='ka_gochara'`), W2-eligible, and its `target_table` already names the production relation; the century writer donates the engine and stays held. Cost: the re-pin list of §6.3. Falsifier: a consumer shown to depend on century-writer identity rather than generation — none found; F-18's Kṣetra pins are by row id and `'3.0'` persists. Not deciding: century retirement (R1's full gate set, G-1 denominator).

  **N-6 (century = qualified compact substrate + projection; hold stands) — APPROVE.** Consistent with R1 (variants as named projections), preserves the calibration corpus (`g3_*`), and makes no retirement claim. Add M-4's `is_active=false` at guard time so the hold is structural, not behavioral. Falsifier: evidence the `'3.0'` rows are needed as *served* fallback beyond the authority re-point — the rollback rehearsal at step 7 tests exactly this.

  **N-7 (persist the Contact ledger) — APPROVE, with B-1 as a condition.** The ledger is what converts F-01 (computed-and-discarded) into Strategy §3's Contact object. Conditions: the Clear/cleanup fix (B-1), the tolerance-in-hash decision (m-3), `comparable_with` vocabulary pinned at WP1, indexes measured at WP4/WP6. Cost: 100–200 MB/chart estimate [I], measured at WP4. Falsifier: WP4 measures a size/latency that breaks the serving budget with Moon excluded — then partition strategy is revisited, not the ledger itself.

  **N-8 (Kota proposed-use; Tithi edge retired as an edge) — APPROVE.** Kota unwired (`w25` exists, not read) and doctrine-uncited; M-4's operand audit is the right gate (R5). Tithi's declared edge has no reader (`ClassContext.fetch` doesn't consume it [S]) — retiring the *edge* while preserving the asset is honest bookkeeping. Falsifier: a live reader of either is discovered — the §6.1 audit says none; B-2's lesson says re-grep once more at WP0.

  **N-9 (registry-hold release conditions) — APPROVE, plus one condition.** The three conditions (Clear `is_active` filter; co-owned seed correction; `target_table`=`count_sql` conjunct) are right. Add: **(iv) a clear-spec resolution exists for every owned relation** — i.e., B-1's fix is a release condition, because without it the re-pinned registry makes Clear *more* dangerous (correctly deletes windows, silently orphans the ledger). Cost: one `EXPLICIT_CLEAR_OPS` entry. Falsifier: n/a — it is a completeness check.

  **N-10 (generation `'4.0'` + publication manifest; `evidence_ref = manifest_id`) — APPROVE, with the M-2 amendment.** The label analysis is verified correct (`:573-598` branches, `:641-643` default, `:966/:977` coverage routing). The manifest is the right publication object, and writing the manifest id into the existing `evidence_ref` column (App. A 28 confirms the column) without touching 527's table is clean. Amendment: P-1a's serving branch must be manifest-driven (or `4.`-family-matching), not a literal `'4.0'` string, or the plan's own rebuild-as-`'4.1'` rule breaks provenance on first rebuild. Cost: the manifest read in serving is one indexed lookup. Falsifier: the step-7 provenance gate. Not deciding: the digest-spec numbering for `'4.0'`.

  **N-11 (delete `_v2 '2.0'` at WP10; alternative HISTORICAL_RESTRICTED) — APPROVE the plan's recommendation, conditioned.** The rows are unserved, regenerable, and fingerprinted; deletion at WP10 *after* `'4.0'` is verified and digest 1018 is retired is clean and honors "nothing is retired" (nothing *served* is lost; the asset `ka_gochara` lives). The alternative (keep as HISTORICAL_RESTRICTED) is defensible but stores dead weight whose only value — a comparison baseline — expires when `'4.0'` is verified. Condition: deletion happens *after* step 9's soak, not before, and 1018's retirement is recorded in the same migration. Falsifier: WP4/WP10 reveals a residual consumer of `'2.0'` (today only `scripts/w2g_equivalence_report.py` [S]) — then HISTORICAL_RESTRICTED instead. Not deciding: the medium-term fate of `g3_utkarsha` staging (calibration corpus; revisit at R1's retirement review).

  **N-12 (target corrections F-19/F-20/F-21 as honesty fixes) — APPROVE.** This fits D-3's own distinction precisely: the targets *claim* a sensitivity the cited facts deny (154 negative-result rows; cusp-placeholder longitudes; dangling yoga ids) — removing/re-typing them makes the code do what it claims, and R-6 stores the honest null (`target_resolution_state`) so nothing masquerades as evaluated (F06; §N.7 item 6). The plan correctly keeps E-1 *admissions* as method calls (M-6). Cost: the 27-class gate must still pass (WP3c gate covers); 208 honest nulls will read as "fewer targets" — the coverage manifest's `targets_unresolved` field answers that. Falsifier: a *positive* sensitive check among the 154 misclassified — the App. A 24 census (`not_gandanta` 44 / `not_fired` 44 / `not_pushkara` 40 / `kartari none` 26) says no; the R-1 value-filter makes misclassification structurally impossible going forward.

  **M-1 (contact-interval activity) — RULE FIRST among M-1..M-4 (after M-5), and rule for orb-scaled activity.** Every timing class in F-02 (13 of 27) becomes a real window only when activity decays with orb instead of filling a ±5 d box; BPHS ch.26's dṛṣṭi-kona [D — `:16514-16529`] is the classical warrant for *graduated* strength by separation. Recommendation: orb-decayed activity within the declared orb (shape — linear, cosine, or dṛṣṭi-kona-derived — decided on the WP8 comparison evidence, not here). Cost: one projection change + comparison PR. Falsifier: the WP8 ablation shows orb-scaling does not improve discrimination on the baseline walkthroughs (marriage 2013, ordinary quarter). Not deciding: the decay shape or orb values.

  **M-2 (dwell weighting) — DEFER, lean skeptical.** No transit-dwell doctrine in the corpus [U]; stations matter geometrically (branch), but a dwell *weight* is a modern intuition. Let the WP8 evidence speak; do not pre-admit. Falsifier: dwell-weighted λ outperforms on held-out chronology. Not deciding: the weight.

  **M-3 (Moon in the score) — RULE: the Moon participates, as a separate testimony layer, not blended into the same λ as slow-body contacts.** The Moon is the classical anchor of gochara (sign-from-Moon doctrine, vedha, moorti — [D] as cited in B.1) and ADJ-14 already keeps it in evidence under every option; removing it from scoring would amputate the classical core. But blending Moon contacts (several per day) with Saturn contacts (one per years) in one noisy-OR is the F-09 saturation mechanism. Recommendation: Moon-scored classes exist (emotion, daily tenor) and Moon *qualifies* slow-body windows (tārā/moorti at the instant), but the century-scale activation score is computed on slow bodies. Cost: a channel split in the projection. Falsifier: baseline walkthroughs show Moon-blended λ discriminates better — I expect the opposite. Not deciding: the exact channel algebra.

  **M-4 (mechanism admission) — APPROVE the plan's process unchanged** (operand audit first, two-step admission, W21 not first, per R5 and the Astra disposition). The only amendment: bindu-qualified AV weighting (D-1 enrichment) should be *studied in the same audit*, since W21's `min_sav_score`-for-bindus substitution was the flagged defect. Not deciding: which mechanisms pass — that is what the audit is for.

  **M-5 (sign-level target anchoring) — RULE FIRST of all M-calls, and rule: whole-sign residence is the primary object; cusp-point contact is admitted only as a KP-school variant with a school label.** Classical warrant: whole-sign is the Parāśari convention this chart's L1 already uses (`enrichment.py:193-217`); transit-to-cusp exists only in KP [D — `kp_reader_vol5_djvu.txt:1089-1092`]. The plan's interim (interval objects, never point contacts) is already the right answer; ruling it now unblocks §5.3's 229 interval targets and WP1's point-vs-span semantics. Cost: none — it ratifies the interim. Falsifier: a classical transit-to-bhāva-madhya text surfacing in the ingested corpus (M-3) — none known. Not deciding: whether `bhava_cusps` ever feeds a *KP-track* product feature (Saṅgam's M-1a shares the question).

  **M-6 (new target classes) — RULE: admit in the order Gulika/Māndi → bhāva-ārūḍhas; defer the rest.** Basis per D-4/D-5 above: Gulika/Māndi has the only transit doctrine naming it [D — KP `:1737-1740`]; bhāva-ārūḍhas inherit arudha's Jaimini sign-level logic at near-zero cost. Defer special lagnas, Prāṇapada, nakṣatra-pāda points — no transit-contact doctrine found in the corpus [U], and admitting them uncited repeats F-19. Every admitted row carries its `classical_citation` or `uncited_extension=true` (§4.3 already has both fields). Cost: small — real L1 degrees exist for all five classes (App. A 22). Falsifier per class: no citation at WP1 → not emitted, per the plan's own gate.

  ---

  **Summary of what I ask you to change before ruling the plan FINAL:** the three blocking items (B-1 clear-spec reality, B-2 S-1 consumer inventory, B-3 H-1 restated as bindu-qualification with a verified citation), the M-2 manifest-driven serving branch, M-4's structural century hold, and the M-6 gating of WP1 on the cost/baseline artifacts. Everything else in the plan survives review as written. Where I have amended, I have said so plainly; where the corpus is silent, I have said [U] rather than borrow authority. The architecture — kernel, ledger, coverage manifest, publication model, runbook — is sound and, once these are folded in, executable.

  Two closing disclosures: (1) my corpus-based doctrine claims are limited by the corpus itself — Phaladīpikā, the natural primary source for gochara/vedha/moorti, is absent from it, so several [D] labels above rest on KP vol5's citations of the tradition plus BPHS; M-3 exists precisely to fix that. (2) One historical-record discrepancy (the sign of the 2020/2026 convention gaps between Astra F4 and brief v1.2, m-7) should be pinned at WP0; it changes nothing doctrinally.

