---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_GRAHA_SANCARA
version: "1.0"
status: ISSUED
date: 2026-09-24
brief_under_review: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_GRAHA_SANCARA_ELEVATION_BRIEF_v1_0.md (v1.0)
reviewed_at: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD 4d8c6aa9b; brief's source_revision 9feac52d7 is an ancestor and the five cited code files are byte-identical between the two (git diff --stat empty)"
reviewer: "Fable 5.1 review agent, fresh context, read-only; one pure in-process swisseph computation run (no DB, no network, no repository write except this file)"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed; every file:line re-read with sed -n / grep -n; prior audit artifacts treated as claims, not proof"
---

# Independent review — `KA_GRAHA_SANCARA_ELEVATION_BRIEF_v1_0`

## 1. Verdict

**REWORK.** The brief's three load-bearing premises — that the engine's live path is instant-grain
(`event_instant` / `instant_grain`), that node frame is a label to *assert* rather than a
computation that *differs* between the two implementations, and that the engine is the position
substrate of four overlay writers — are each contradicted at source; the proof matrix's Value row
cannot go red under the brief's own §10 recommendation, and its DEMANDS row rests on L0 columns that
do not exist on the brief's stated revision.

## 2. Findings

| id | sev | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| F1 | BLOCKER | §2.2 "Live path `_compute_live`"; §2.4 live instant positions `time_basis='event_instant'`; §4.4 `claim_grain` "`instant_grain` live"; §5 QUALIFY_LIMIT applied to PATH-A only; §7 Duplication row "same instant via PATH-A and live"; §0/§11.1 "arbitrary-instant" | The engine's live path does not compute at the instant. `engine.py:312-313` passes `query_dt` as the *first* positional arg of `get_transit_states(birth_dt, query_date, ...)` (`compute_transits.py:158-161`) and `q_date` as `query_date`; positions are computed at `jd = _sidereal_jd_ut(query_date)` (`:183`) = `swe.julday(d.year, d.month, d.day, 0.0)` (`:77-79`) — **00:00 UT of the query date**, time-of-day discarded. Pure swisseph run (Lahiri, Moshier): Moon 1984-02-05 00:00 UT = **324.4787°**; 05:13 UT (10:43 IST birth) = **327.0550°**; 12:00 UT knot = 330.4060° (Pisces). 324.4787° is exactly the value the writer (`ka_graha_sancara.py:172`), the probe comment (`service_probes.py:546-548`) and `tests/l3/test_m3_graha_sancara_defects.py:71-74` call "the exact birth instant". The forensic check passes by coincidence of sign; the "instant-precision path" claim is an unearned signal (§N.8). Also `compute_transits.py:79-80` hard-codes `SIDM_LAHIRI` and `:83` forces `FLG_MOSEPH` (`moshier=True` default). | Rewrite §2.2/§2.4/§4/§5/§7: both engine paths are date-grain (`noon_ut_knot` PATH-A; a 00:00-UT knot on PATH-B, which is **not** in the B1 `time_basis` enum). The parity test "N instants → identical longitudes" against the router is unpassable at any non-midnight instant until the engine's live path actually computes at the instant — that is the real delta, and it changes `may_touch` (F5). Name the existing test/comment/probe as a false "instant" detector to be corrected. |
| F2 | BLOCKER | §4.4 "`node_mode='mean'` (D6)"; §6 lens C invariant "node = mean"; §7 Relevant-influence "`node_mode` unchanged"; §10.1 engine as reference; §3 Non-claim "no numerical divergence … none was measured" | The two implementations differ in node frame, readable at source: engine `engine.py:19-20` "TRUE_NODE everywhere"; `compute_transits.py:54,63` `swe.TRUE_NODE`; L0 `l0_ephemeris.py:77` `{"name": "Rahu", "swe_id": 11}` and `:287` `swe.calc_ut(jd, 11, ...)` — 11 is `swe.TRUE_NODE` (verified `MEAN_NODE=10, TRUE_NODE=11`) although the comment reads "Mean North Node"; router `routers/ephemeris.py:82` `('Rahu', swe.MEAN_NODE)` and `:48` `"node_mode": "mean"`. Measured true−mean at the birth instant: **1.0213°**. So the "convention divergence" exists today for Rahu/Ketu on every instant, and asserting `node_mode='mean'` on the engine would be a fabricated label (§N.7 items 3/6) unless the engine's computation changes. | §4 must state the TRUE→MEAN change as a **computation delta** with its ripple (F3 callers; and PATH-A depends on what L0 stores — raise the `l0_ephemeris.py:77` mislabel to L0). Until changed, the engine asserts `node_mode='true'` honestly. §7 Relevant-influence and Boundary rows must tolerance or exclude Rahu/Ketu explicitly. Drop "no divergence measured" — one is readable without measurement. |
| F3 | MAJOR | goal_objective "the position substrate four overlay writers … stand on"; §2.3 kota/moorti/sudarśana/vedha read "engine (`NAKSHATRAS` ordering, transit states) — the hub"; "The engine is live for four overlay writers at build time"; §5 hub rule | The four writers import **constants only**: `ka_kota_chakra/writer.py:56` (`ALL_GRAHAS, NAKSHATRAS, NAK_SIZE_DEG`), `ka_moorti_nirnaya/writer.py:49` (`NAKSHATRAS, NAK_SIZE_DEG, SIGNS`), `ka_sudarshana_varsha/writer.py:34` (`SIGNS`), `ka_vedha_gochara/writer.py:60`. Their positions come from their own SQL on `ephemeris_daily` (`kota writer.py:96`, `moorti :77`, `vedha :106`). Non-test callers of `get_ephemeris` are: writer self-test (`:120,176,232`), `service_probes.py:618`, `pyjhora_adapter/transits.py:82-86` (`force_live=True`, comment "precise live computation" — F1 applies; no caller of this adapter found within sidecar non-test scope), and **L4** `brahmagyan/phala/muhurta.py:737` (PATH-A with `db_conn`, literal `"lahiri"`). | Rewrite §2.3 with the real callers and roles; state "no L3 writer consumes the engine's computation; constants fan-out only"; re-scope the goal sentence; note the two real callers lie outside the brief's declared search boundary and one is a cross-layer (L4) consumer that inherits every defect above. |
| F4 | MAJOR | §0/§3 "never asserts which ephemeris backend answered"; §4.4 "`ephemeris_backend ∈ {swieph, moshier}` asserted from the call's own `retflag`"; §7 Context row ".se1 removed → `ephemeris_backend='moshier'` asserted" | True of the engine only. The served route already asserts per call: `routers/ephemeris.py:12-24` `_ephemeris_backend(retflag)`, `:154-156` per-body `retflag` capture, `:27-73` `_service_context` emitting `node_mode`, `frame`, `ayanamsha_id`, `input_precision`, **`ephemeris_backends_observed`** ∈ `{jpl_file, swiss_ephemeris_file, moshier_analytic_fallback}`, `backend_observation_state`, `backend_qualification_state`, `failure_contract`. The same vocabulary is the L0 probe contract's (`nirmana_probe_contracts.json` `allowed_ephemeris_backends: ["swiss_ephemeris_file"]`; `service_probes.py:292,342-347`). The brief's `ephemeris_backend`/`{swieph, moshier}` is a rival name and vocabulary (DP01); the §7 Context row is already the route's behaviour and cannot evidence elevation there. | Adopt the existing field name and enum, or declare the alias mapping. Re-scope §4.4 to the engine. |
| F5 | MAJOR | §4.4 backend "from the call's own `retflag` (never inferred from a global)"; may_touch list | On the engine's live path this is impossible within may_touch: `compute_transits.py:84` `pos, _ = swe.calc_ut(...)` discards `retflag`; `:83` forces `FLG_MOSEPH`; `:216` returns the constant `"ephe_mode": "moshier"`. `platform/scripts/temporal/compute_transits.py` is in neither `may_touch` nor `must_not_touch`. | Either add it to `may_touch` (and trace its other consumers — it is a CLI/script with its own contract) or state that the engine stops delegating to it — which, with F1, is the actual smallest sufficient change and must be sized as such. |
| F6 | MAJOR | §10.1 recommends the **engine** as reference; §2.2 the route has "its own swisseph integration" | The route's computation is the L0 `bg_ephemeris_engine` surface reused as-is (`routers/ephemeris.py:211-213` "rather than a second swisseph integration"; `_service_context.service_asset_id = "bg_ephemeris_engine"`, `registry_generation = bg_ephemeris_engine@migration-624`, `:11-12,45-46`): Swiss-file requested with observed fallback, MEAN node (D6-conformant), true instant, 422 on unknown id, `SWISS_STATE_LOCK` serialised. The engine's live path is a Moshier-forced, TRUE-node, 00:00-UT script (F1/F2/F5). The recommendation would make the unqualified path the reference for the qualified one. | Present §10.1 with these facts side by side; the honest recommendation on this evidence is the router's computation (or a new shared kernel) as reference, with the engine's PATH-A retained for stored-knot reads. |
| F7 | MAJOR | frontmatter `accepted_upstream_contract` "migrations 1075–1079 applied to production"; §1 L0 items 6–7 row; §4.4 "`epoch_convention` from the row (L0 item 7)"; §7 DEMANDS "L0's row declarations (items 6–7, applied)" | No migration numbered 1075–1079 exists in `platform/migrations` (highest 1072) or `platform/supabase/migrations`; no migration adds `node_mode`/`epoch_convention`/`node_frame` to `ephemeris_daily` (grep → only `624_nirmana_l0_ephemeris_probe_contract.sql`); blueprint `:383` says "L0 repair PR #2727 open and RED". Blueprint `:189,:287` say "applied" — the blueprint contradicts itself; the code on `9feac52d7` has neither the columns nor the migrations. | DEMANDS becomes a named blocker with the PR state, or the vector is asserted from the engine's own constants (noon UT is `l0_ephemeris.py:277`; node frame is F2). Do not cite "applied" without a migration file on the base. |
| F8 | MAJOR | §4.2 "Unknown → `unavailable` with the offending name, never a bare exception at the route boundary"; §7 Negative row `unavailable(unknown_ayanamsha)` | The route deliberately fails loud: `routers/ephemeris.py:250-258` HTTP 422 "never silently falls back" (design note §3 item 5), pinned in `_service_context.failure_contract.unknown_ayanamsha = "HTTP_422"` (`:66`) and tested by `tests/l3/test_ephemeris_at_t_sidecar_route.py:83-86`. An unknown vocabulary token is a caller error, not F06 `unavailable` ("method silent for lack of input"). Replacing 422 with a 200-and-`unavailable` is a guard weakening (context §11) and an F06 misuse. | Keep 422 at the route; inside the engine a typed `ValueError` with the valid set is already the behaviour (`engine.py:376-379`). Reserve `unavailable` for out-of-range/derivation failures. |
| F9 | MAJOR | §7 Value row "mutation: alter one `SIDM_*` mapping in the router → parity test fails"; §4.9 ablation | Under the brief's own §10.1 design the router delegates to the engine, so `_AT_T_AYANAMSHA_MAP` (`:226-232`) is no longer on the live path — the mutation target vanishes; if the map becomes the shared alias map both import, mutating it moves both sides equally and parity stays green. The detector cannot go red under the recommended design. §7 Relevant-influence also asserts "`node_mode` unchanged" while node_mode differs today (F2). | Re-specify Value as a mutation of the *reference kernel* observed at the *served envelope* (a golden-value fixture, not a two-implementation parity), and keep a true two-implementation parity only for the transition commit. |
| F10 | MAJOR | §4.2 "legacy names accepted through one declared alias map … that the router already has (`:215-223`) — moved to one place both import" | A canonical-plus-legacy resolver already exists in L0 (must_not_touch): `brahmagyan/l0_ephemeris.py:91-119` `AYANAMSHA_MAP` holds `lahiri, raman, kp, krishnamurti, yukteshwar, surya_siddhanta` **and** `lahiri_chitrapaksha, true_chitra, surya_siddhanta_classical` (EL-39), with `_resolve_read_ayanamsha` `:139-156` and `_DEFAULT_READ_AYANAMSHA='lahiri_chitrapaksha'` `:137`; `derive_sidereal` (`engine.py:253`) already accepts the canonical id. The engine's `SUPPORTED_AYANAMSHAS` `:64` is a stale hand-copied subset. A "new place both import" would be a fourth vocabulary. | The engine validates against `l0_ephemeris.AYANAMSHA_MAP` (import, no L0 edit) and defaults to `DEFAULT_AYANAMSHA`; the router keeps its map or reads the same. Note `AYANAMSHA_MAP` is `{}` when swisseph is absent (`:118-119`). |
| F11 | MINOR | §5 PRESERVE "the memo cache"; §4.4 `instant_grain` live | Cache key is date-only: `engine.py:151-152` `(d.isoformat(), ayanamsha)`; with a caller-supplied `_cache` (`:396-407`) any later instant on the same date returns the first instant's grahas from either path. Inconsistent with an instant-grain claim even after F1. | QUALIFY_LIMIT the cache as date-keyed, or key on the instant when `force_live`. |
| F12 | MINOR | §6 lens H "idempotent … no credentials"; L3-A01 "arbitrary-chart input" | `engine.py:381-391` assumes **Asia/Kolkata** for any naive datetime — a global-scope service with a native-specific default (B1 tz rule: the birth instant's offset, never a fixed zone). `q_date = dt.date()` (`:393`) is then taken in that zone, shifting which PATH-A knot is read. Unnamed in the brief. | Reject naive datetimes (the route already does, `:264-268`) or name the default as a wrong-context hazard with a live-path statement. |
| F13 | MINOR | §7 Irrelevant-control "identical payload"; parity "identical longitudes" | `is_retrograde` for Rahu/Ketu is forced `False` on PATH-A (`engine.py:263`) and on the live path (`compute_transits.py:187,197-198`), while the router reports `retrograde = speed < 0` (`:128`) — nodes retrograde on the route, direct on the engine. Parity on the flag fails by construction; the field is not in §7. | Add `is_retrograde` to the parity contract with the node convention stated. |
| F14 | MINOR | `file:line` ranges in §1/§2/may_touch | `engine.py:173-260` (function ends `:279`), `:284-313` (ends `:339`), `:344-378` (ends `:438`), `:141-167` (`:143-168`); `routers/ephemeris.py:215-223` for the "same ayanamsha" note (actually `:223-225`; map `:226-232`, default `:233`); `routers/ephemeris.py:200-260` — the decorator is `:241` and the route body runs to `:291`, so `may_touch`'s range excludes the route's own return and the shared helpers `_calculate_sidereal_positions` `:140-173` / `_service_context` `:27-73` that the natal endpoint `:176-197` also uses; `call_service_wrappers.ts:170-240` (descriptor to ~`:262`). Test paths are swapped: actual `tests/test_ka_graha_sancara.py` and `tests/l3/test_m3_graha_sancara_defects.py`; the route's tests `tests/l3/test_ephemeris_at_t_sidecar_route.py`, `tests/test_ephemeris_ayanamsha.py` are absent from `may_touch` though a parity test would live there. `asset_registry_seed.ts` is `platform/scripts/seed/asset_registry_seed.ts:2230-2245`. | Correct the ranges; widen `may_touch` to the whole route and its helpers with the natal endpoint named as a co-user; fix test paths. |
| F15 | MINOR | §1 "Synergy audit §2 / blueprint G4 — backend process-global and unowned; `panchang_engine` forces Moshier" | `KALA_SYNERGY_AUDIT_v1_0.md` §2 has no backend content (grep `backend|Moshier|ephemeris` → only `:59`, the node row). The claim is blueprint chronicle §11.18 (changelog v2.6), and **G4 was RETRACTED** (blueprint changelog v2.6, v3.5, v4.0: "the .se1 files are on this host"). `panchang_engine/__init__.py:71,149,252,347` `set_ephe_path(None)` supports "forces Moshier". Separately the audit `:59` says "Kṣetra reads mean via `ephemeris_daily`" while the brief says TRUE node stored — the artifacts disagree; code sides with the brief (F2) but L0's own comment is wrong. | Cite §11.18 not G4; drop "G4"; record the audit/brief node disagreement as an L0 question. |
| F16 | MINOR | §2.1 "`estimated_seconds: null`"; §2.5 "(`estimated_seconds` null — correct)" | Guide §3 / context §10: never cite `asset_registry.estimated_seconds`. | Delete both mentions. |
| F17 | MINOR | §0 "a self-test that certifies the one nobody calls" vs §2.3 "live for four overlay writers" | Internally inconsistent; after F3 the true statement is "live for no L3 writer; called by the pyjhora adapter (no caller found within scope) and by L4 muhurta". | Rewrite both sentences to match. |
| F18 | MINOR | §4.6 "`service_health` then has four ways to read `unhealthy`"; §2.3 `service_probes.py` "DB-free probe" | Today the writer already runs six checks (`ka_graha_sancara.py:118-248`) and raises on unhealthy (`:94`); the gap is instant coverage and F1. Uncited: the TS availability contract for `call_ephemeris_at_t` is gated on the **engine** probe `graha_sancara_forensic` (`call_service_wrappers.ts:215-219`: `probe_contract_sha256`, `max_age_seconds: 900`; digest `nirmana_probe.py:69,107`), i.e. the served route's availability is certified by the other implementation — the strongest evidence for the brief's thesis. And `service_probes.py:582-583` rejects any `forensic_ayanamsha` but `'lahiri'`, so moving the probe to the canonical id changes `nirmana_probe_contracts.json#ka_graha_sancara` and the digest pinned in the must_not_touch wrapper. | Cite the availability contract; add the probe-contract/digest re-pin to §5 as a coordinated packet with Pūrṇa. |
| F19 | NOTE | §4.5 failure semantics | PATH-A returns `None` on any failure (`engine.py:228,248,256`) → falls to the live path (`:425-427`) → `NotImplementedError` for a non-lahiri *in-range* request (`:306`): a stored-read failure surfaces as a vocabulary error. §4.5 covers only the out-of-range case. | Add the in-range fall-through as a typed `unavailable(reason=path_a_read_failed)`. |
| F20 | NOTE | §2.1 identity | The served route self-identifies as `service_asset_id: "bg_ephemeris_engine"` (`routers/ephemeris.py:45`), not `ka_graha_sancara`; `EphemerisResult.source` documents a `'mixed'` value never produced (`engine.py:134`). | State in §2.1; drop `'mixed'` or produce it. |

## 3. Citations verified

| citation (as in brief) | resolved? | what is actually there |
|---|---|---|
| `routers/ephemeris.py:5` `import swisseph as swe` | yes | `:5` |
| `routers/ephemeris.py:200-260` "the route" | partly | comment block `:200-213`, map `:226-232`, default `:233`, decorator `:241`, body `:242-291` |
| `routers/ephemeris.py:215-223` "same ayanamsha under two names" | partly | note is `:223-225`; `:215-222` is the vocabulary comment |
| `routers/ephemeris.py:244` docstring "(ka_graha_sancara service)" | yes | `:244` |
| `grep ka_graha_sancara routers/ephemeris.py` → comment only | yes | `:200`, `:244` only; no import |
| `engine.py:64` `SUPPORTED_AYANAMSHAS` | yes | `:64`, six names |
| `engine.py:84` `applying_or_separating` | yes | `:84` |
| `engine.py:141-167` cache | ~ | class `:143-168` |
| `engine.py:173-260` PATH-A | ~ | `:173-279` |
| `engine.py:220` `WHERE date = %s AND ayanamsha_id = 'tropical'` | yes | `:220` |
| `engine.py:253` `derive_sidereal(...)`; `:255` warning | yes | `:253`, `:255` |
| `engine.py:284-313` `_compute_live` | ~ | `:284-339` |
| `engine.py:306-309` `NotImplementedError` gate | yes | `:306-310` |
| `engine.py:313` `get_transit_states(query_dt, q_date, ayanamsha=...)` | yes | `:313` — and this is F1 |
| `engine.py:344-378` `get_ephemeris` | ~ | `:344-438` |
| `engine.py:346` default `"lahiri"` | yes | `:346` |
| `engine.py:376` `Unknown ayanamsha` | yes | `:376-379` |
| no `set_ephe_path`/`retflag` in `engine.py` | yes | grep empty |
| `writers/ka_graha_sancara.py:1-30` | yes | docstring `:1-21`; behaviour confirmed `:60-103`, `:253-268` |
| `call_service_wrappers.ts:170-240`; `:227` `DEFAULT_AYANAMSHA`; `:239` `PYTHON_SIDECAR_URL` guard | yes | `:170-262`; `:227`; `:235-240` |
| `asset_registry_seed.ts:2236-2244` | yes (path unstated) | `platform/scripts/seed/asset_registry_seed.ts:2230-2245`, `storage_type: 'service'`, `depends_on: ['bg_ephemeris']`, `scope: 'global'`, `asset_kind: 'service'`, seed text "TRUE_NODE throughout" |
| REGISTER:136 (contribution register §5 row) | yes | `MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md:136` |
| Strategy §6.1 L3-A01 | yes | `MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md:271` |
| Elevation plan §6 item 5 "unverified by me" | yes | `MADHAV_L3_ASSET_ELEVATION_PLAN_v1_0.md:206` |
| STATE.md ★ | yes | `STATE.md:219-235` |
| Blueprint §3.5 row 1; §16.2 row; §17.2 row 2 | yes | `KALA_ELEVATION_BLUEPRINT_v1_0.md:317`, `:899`, `:950` (§16.2 carries the same truncated ranges) |
| Blueprint §3.3 SC-8 / SC-10 | yes | `:287`, `:289` |
| "Synergy audit §2 (backend/G4)" | **no** | audit §2 (`:37-51`) has no backend content; G4 retracted (blueprint changelog `:30,:31,:40`) |
| Lane F §2a `engine.py:302-306` | superseded, as the brief says | now `:306-310` |
| `chart_facts` id set [A] | yes, by code | `routers/jaimini.py:76-80`, `retrieval/registry/constants.ts:2`, `l0_ephemeris.py:107-113`; **not** in `00_ARCHITECTURE/CHART_FACTS_SCHEMA.json` (no id list there — §11.5's pointer is wrong) |
| `47131772b` W2 source accepted | commit exists | "fix(data-plane): identify L3 frontier writer results" — its stat does not name `ka_graha_sancara`; "service payload frozen" not verified from the commit |
| `f6fed12c7` L0 accepted | commit exists | "docs(data-plane): accept L0 producer readiness" |
| migrations 1075–1079 / PR #2727 applied | **no** | not in any migrations directory; blueprint `:383` "open and RED" |
| tests `tests/l3/test_ka_graha_sancara*.py`, `tests/test_m3_graha_sancara_defects.py` | swapped | `tests/test_ka_graha_sancara.py`; `tests/l3/test_m3_graha_sancara_defects.py` |

## 4. What I could not verify, and why

- Production state: whether migrations 1075–1079 or PR #2727 are applied, `asset_registry.service_health` / `selftest_detail` today, `.se1` presence on the production host, live incidence of any failure class — no DB access, no GitHub/network (read-only mandate).
- Whether `pyjhora_adapter.transits.compute_transits` has a live caller — no caller found within `platform/python-sidecar` non-test scope by grep; not asserted "unused".
- The "service payload frozen" claim attached to `47131772b` — the commit does not name this asset in its stat.
- The row content of `ephemeris_daily` (which node is physically stored) — inferred from `l0_ephemeris.py:77,287` (`swe_id` 11 = TRUE_NODE) only; not measured.
- Per-call latency — not measured (the brief does not claim it).
- The swisseph figures above were computed with Moshier in this environment (`retflag` MOSEPH bit set when SWIEPH was requested — no `.se1` on this host); Swiss-file values would differ at the arc-second level, not the degree level that the findings rest on.

## 5. Conformance to the binding

- **B1** `time_basis`: brief uses `noon_ut_knot` / `event_instant` (in enum) but the engine's live path is a 00:00-UT knot (F1) — no enum member; must be added to the binding or the path fixed. `claim_grain` `date_grain` / `instant_grain` in enum. `inclusivity` declared n/a — acceptable for a point query. tz rule violated by the engine (F12), unnamed.
- **B2** `epistemic_class='COMPUTED_FACT_CONFIGURATION'` — Foundation `:65`, conformant. `completeness_state='unavailable'` — in enum but misapplied to an unknown-token rejection (F8). `operator_role='computation'` — Foundation `:39`, conformant. `comparable_with`, `tier_basis`, `independence_group` — absent; the brief should state n/a with a reason (a service with no score), noting that Q09 convention sensitivity is exactly `comparable_with='different_convention'`.
- **Fields named outside B1–B7**: `ephemeris_backend` (rival to the code's `ephemeris_backends_observed`, F4), `node_mode` (matches router; SANGAM_STAGE3_STATE.md:160 spells `node_convention`), `ayanamsha_application` (SANGAM spells `ayanamsa_application`), `epoch_convention`, `ayanamsha_id`, and the reason syntax `unavailable(unknown_ayanamsha)`. Three spellings across three artifacts for the same convention vector; the brief must declare its mapping or the binding must be amended with SC-8's field names before the brief adopts them.
- **B6** node longitude: binding says mean; the engine is TRUE on both paths (F2). The brief offers the label without the change.
- **B7**: the §7 Value row is a green that cannot go red under the recommended design (F9) — non-conformant with "a green run that could not go red does not count".
