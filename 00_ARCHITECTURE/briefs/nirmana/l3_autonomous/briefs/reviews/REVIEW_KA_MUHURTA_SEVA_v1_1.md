---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_MUHURTA_SEVA
version: "1.1"
status: ISSUED
date: 2026-09-24
brief_under_review: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_MUHURTA_SEVA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.1")
supersedes: REVIEW_KA_MUHURTA_SEVA_v1_0.md (20 findings, F1 BLOCKER, REWORK)
reviewed_at_revision: "27b0146f3 (l3/kala-layer-briefs; brief committed in this commit; `git diff --stat 9feac52d7 HEAD` and `git diff --stat bf70a3477 HEAD` on every cited source file are both empty)"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no external service, no git write"
method: "every v1.0 finding re-checked at source, not against §12; every file:line in v1.1 re-read with sed/grep; [R] markers treated as claims and re-verified — five of the reviewer's own v1.0 line numbers were wrong and are corrected below"
---

# Re-review — `ka_muhurta_seva` elevation brief v1.1

## 1. Verdict

**ACCEPT_WITH_CORRECTIONS.** The v1.0 BLOCKER is closed at source — the three-engine call graph, the zero-value-receiving-caller statement and the always-raising `ka_sangam/engine.py:722` call all re-trace exactly as the brief now says — but the rewrite introduces six MAJOR defects local to the delta design and scope: the finder lives at `muhurat/finder.py`, not under `panchang_engine/`, so the §10.1 fence dichotomy the native is asked to rule on is false; the recommended "build the verdict from the returned `breakdown`" option cannot produce the B5 coverage the brief specifies because `find_muhurat` drops vetoed days and cuts to `top_n` before returning; a tāra-Vadha veto is presented as existing when the finder treats Vadha as a 0.00 grade; the ablation bound "≤ 10" is wrong for three of eight events (native weight 0.20/0.25); `may_touch` lists three Pūrṇa-owned TS files the Context forbids designing changes to; and IP-10's "route `kala_elect_get` through this service" branch is infeasible without a vocabulary decision the codebase has explicitly declined. None reopens the consumer map; all are correctable in place — but §4.2, §4.4, §7, §10.1 and the frontmatter must be corrected before the native rules on §10.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| N1 | **MAJOR** | frontmatter `may_touch`: "`platform/python-sidecar/panchang_engine/muhurat/finder.py:101-141`"; `must_not_touch`: "`panchang_engine/**` except the fence decision"; §0 "(1) `service.py` → `panchang_engine/muhurat/finder.py`"; §2.2 "Finder `panchang_engine/muhurat/finder.py`"; §5/§10.1 "build the verdict … (no `panchang_engine` edit) — recommended; or lift the fence on `finder.py:101-141`" | No such file. The finder is `platform/python-sidecar/muhurat/finder.py` (`finder.py:4-5`: "Moved from panchang_engine/muhurat.py (P2 re-arch, 2026-06-09). Scoring/judgement lives here. panchang_engine/ holds only the deterministic core"). `panchang_engine/muhurat.py` is a 38-line re-export shim (`:1-8`). `service.py:110,157` imports `from muhurat.finder import …`. No governance text names an owner or fence for `muhurat/` (grep of blueprint, Context, Strategy for `muhurat/` → 0 hits). | Correct the path in all four places. Restate §10.1: editing the finder touches no `panchang_engine/**` file, so the "no L0 pañcāṅga-authority edit" premise does not distinguish the two options; the real question is whether `muhurat/` (scoring/judgement, sidecar-local) is inside this asset's fence — propose it is, or ask. |
| N2 | **MAJOR** | §4.4 `coverage = {…, exclusions: [{window_id, reason ∈ {vetoed, below_top_n}}], …}` + counters `candidates_evaluated, vetoed, returned, top_n, truncated`; §4.8 "Missing: all vetoed → empty with coverage"; §9 "`exclusions` = the two rikta days with `reason='vetoed'`"; §10.1 recommended option "build the verdict in the service from the finder's *returned* `breakdown` (no finder edit)" | `find_muhurat` (`finder.py:242-261`) appends a window only `if s > 0` (`:246`) — knockout days and true-zero days are dropped indistinguishably, with no count — and returns `candidates[:top_n]` (`:261`); the service (`service.py:159-169`) receives only the surviving top-N windows. Nothing returned carries vetoed days, below-`top_n` days, a vetoed count, or whether a cut happened (a return of exactly `top_n` rows is ambiguous). For the single-day path, `score_muhurat` returns a bare float (`:148-196`); no breakdown is returned at all — the service would have to call `_score_breakdown` itself (private; re-exported by the shim at `panchang_engine/muhurat.py:16`). | The recommended option as written cannot deliver §4.4/§4.8/§9. State the three real options: (a) finder edit (returns all candidates + counts); (b) no-edit variant: call `find_muhurat` with `top_n = len(horizon)` (≤ 90 on the route, `routers/muhurat.py:131`) and apply the cut in the service — yields `below_top_n` exclusions and `truncated` exactly, and `dropped_by_finder` = horizon − candidates as an *approximate* vetoed count (knockout ∧ zero-quality conflated — say so); (c) service re-runs the per-day loop (rejected: a second engine iteration, doubling `compute_panchang`). For `score()`, say the service calls `_score_breakdown` directly. |
| N3 | **MAJOR** | §4.2 "`vetoes` hard, listed (`_in_inauspicious`; **tāra vadha when the overlay ran**)"; §10.4 "the overlay is a veto + a grade carried as testimony" | In this engine Vadha is a grade, not a veto: `tara_bala.py:44` `7: {"name": "Vadha", … "score": 0.00}`, `:116` "0.0 (Vipat/Vadha) to 1.0"; `finder.py:190-194` adds `weights["native"] * 0.0` and the day still ranks. The only veto is `_in_inauspicious` (`:58-76`). A Vadha `hard_veto` exists on the **phala/`elect.ts`** path (`elect.ts:202-204` comment) — a different engine (F1). | Either drop "tāra vadha" from `vetoes` or declare it as a NEW rule (`ENRICH_CORRECT`, DP02 source required) whose effect is that the *returned set* now depends on `native_chart` — and add that case to §4.8 (today: Vadha day may be top-ranked with `tara_bala=0.0`; new: excluded with `reason='vetoed_tara_vadha'`). §10.4 must then read as a proposal to add a veto, not a description. |
| N4 | **MAJOR** | §2.4 "tāra-bala overlay … weight 0.10"; §4.10 "`legacy_score` drops by ≤ 10 (tāra weight 0.10) — bounded by construction"; §7 Relevant-influence row "Δ`legacy_score` ≤ 10 … detector fails when … Δ > 10" | `muhurat_weights.yaml`: `native: 0.10` for vivah (`:48`), griha_pravesh (`:63`), vyapara (`:79`), yatra (`:96`), property_purchase (`:114`); **`native: 0.20`** mantra_initiation (`:127`); **`native: 0.25`** upaya_ritual (`:148`) and sadhana_initiation (`:169`) — the two events added "specifically for ka_muhurta_seva" (`finder.py:43-45`; `routers/muhurta_score.py:29-32`). `compute_tara_bala_score` ranges 0.0..1.0 (`tara_bala.py:116`), so Δ ≤ 25 for those two. | Bound per event: Δ`legacy_score` ≤ 100 × `weights['native']`; the §7 detector reads the effective weight from `get_weights_for_event(event)` rather than hard-coding 10, or the fixture is pinned to vivah and says so. |
| N5 | **MAJOR** | frontmatter `may_touch` last entry: "IP-10 (proposed, Pūrṇa-owned TS + L3 sentinel): `platform-mcp/src/tools/kala_views/elect.ts`, `platform-mcp/src/tools/muhurta_finder.ts`, `L4_phala/query_muhurat.ts`"; `must_not_touch`: "`platform-mcp/src/tools/kala_views/**` except as an interface packet" | Context §5 (`KALA_ASSET_BRIEF_CONTEXT_v1_0.md:103-105`): "`platform-mcp/src/tools/kala_views/*` belongs to the live Pūrṇa Anveṣaṇa campaign. **Do not design changes to it.** … express it as an interface packet … and let Pūrṇa own the code." Guide `:153` lists "edit `kala_views/` or any Pūrṇa-owned surface" among the forbidden acts. An interface packet is an obligation + an L3-owned sentinel test (blueprint `:751`), not a file-scope grant. `query_muhurat.ts` is L4-retrieval-owned (`platform/src/lib/retrieval/registry/layers/L4_phala/`). | Remove the three TS paths from `may_touch`; keep IP-10 as a proposed §12.2 row plus the L3-owned sentinel test file (which *is* in scope). Drop the "except as an interface packet" qualifier from `must_not_touch` — it is self-contradictory. |
| N6 | **MAJOR** | §4.10 / §10.2 IP-10: "`kala_elect_get` obtains its election from this service (or `phala/muhurta.py` adopts the verdict and `location_used` from a per-chart location) … recommendation: yes, both" | Branch A is not a re-pointing: `kala_elect_get`'s `undertaking` flows to `MuhurtaFinderInputSchema.action_type ∈ {marriage, travel, business, medical, education, property, general}` (`muhurta_finder.ts:83-85`), served by `phala/muhurta.py` with dasha/gochara sub-scores (`:1329-1332`), hora ladder and `lane_f` (`elect.ts:302-336`) — none of which this service produces; `general` ∉ `EVENTS_MVP` (`finder.py:36-46`) — the exact mismatch that makes `ka_sangam/engine.py:722` raise. `routers/muhurta_score.py:9-13,25-29` records the decision *not* to fabricate a marriage→vivah-style mapping ("medical/ceremony have no clean EVENTS_MVP analog"). A partial TS bridge exists (`ACTIVITY_RULES[].event_class`, `muhurta_finder.ts:450-542`) but maps `education → 'vidya_arambha'` (`:498`, not in EVENTS_MVP) and `medical → 'upaya_ritual'` (`:490`). Branch B is an L4 change (`phala/muhurta.py` in `must_not_touch`, L4-owned), so it is not an "L3-U04/U11 packet owed to Pūrṇa" in form. | Split IP-10 honestly: (i) **disclosure packet** (Pūrṇa): `kala_elect_get` envelope carries `location_used = {…, source:'panchanga_daily_single_observer'}` and `personal.completeness_state` derived from the existing `lane_f.tara_bala_status` — sentinel L3-owned; (ii) **per-chart pañcāṅga item** (L0 `panchanga_daily` + L4 `phala/muhurta.py` owners) — not a packet, a cross-asset item as §3b already says; (iii) drop branch A, or scope it explicitly to the five `action_type`s with declared EVENTS_MVP analogs and name the vocabulary decision as a native ruling. |
| N7 | MINOR | §4.3 "`location_used = {lat, lon, tz_offset_minutes, source ∈ {caller, canonical_default}}` on the route body of `/api/compute/muhurta_score` and on `call_muhurta_score`'s `content` — a descriptor change (`:463` inputs, `:502` body)"; §7 Context row "`location_used.tz` typed with `source`" | `/muhurta_score`: request model `:61-69` has no location, so `source='caller'` is unreachable; the response is an untyped `dict` (`:73`, `:125-143`) so a new field needs no model change; `call_muhurta_score` returns `{ content: data }` (`call_service_wrappers.ts:509`) — a response field reaches `content` with **no** descriptor change (`:463 inputs`/`:502 body` are request-side; only `outputs:` at `:463` is optionally affected). `/muhurat`: `lat`/`lon` are `Field(...)` required (`routers/muhurat.py:32-33`) — always caller — and only `tz_offset_minutes` defaults (`:34`); one `source` per location cannot express "coords caller, tz defaulted". | Say: response-side typing needs no descriptor change; a descriptor + request-model change is needed only if `/muhurta_score` is to *accept* a location — decide and name it, or restrict its enum to `canonical_default`. Type `location_used = {lat, lon, tz_offset_minutes, coords_source, tz_source}`. |
| N8 | MINOR | citations marked [R]: `finder.py:216` (inclusive), `:243` (`while current <= date_to`), `:257` (date step), `:163-166` (event → table/weights); `routers/muhurat.py:44` (`breakdown`); §0 chain link `muhurta_finder.ts:32-34` | Actual: `finder.py:215` "[date_from, date_to]", `:219` "Inclusive search window", `:242` `while current <= date_to:`, `:258` `current = current + timedelta(days=1)`, `:164-166`; `routers/muhurat.py:46` `"breakdown": window.breakdown` (`:44` is `star_rating`). These were wrong in v1.0 (files unchanged since `bf70a3477`) — reviewer's error, carried by [R]. `muhurta_finder.ts:32-34` is a **stale comment** naming "sidecar POST /api/compute/muhurat (Phase 4C-6 muhurat router)" — the finder router — whereas the code path is `callPlatformPrimitive` (`:41`) → `query_muhurat.ts:7-8,124` → `POST /api/compute/phala/muhurta_finder`. The brief's chain is right; its cited line contradicts it. | Fix the five line numbers; cite `muhurta_finder.ts:41` + `query_muhurat.ts:124`; add the stale `:32-34` comment to the §5 catalog-correction list (it would mislead the next reader into the v1.0 error). |
| N9 | MINOR | §9 walkthrough "`exclusions` = the two rikta days with `reason='vetoed'`"; §4.8 "Missing: all vetoed → empty" | `_in_inauspicious` (`finder.py:64-75`) requires `rahu_kalam ∧ yamagandam ∧ tithi ∈ {4,8,9,14,30} ∧ Saturday`. `rahu_kalam` and `yamagandam` are emitted for every day (`timings.py:196,200`; `panchang_engine/__init__.py:102-103,168`), so the veto ≡ worst-tithi **on a Saturday**; the set is {4,8,9,14,30}, not "rikta" {4,9,14}; a rikta weekday is graded low, not vetoed. "All vetoed → empty" is constructible only for a horizon consisting of such Saturdays (a 1-day fixture). | Rewrite the walkthrough's exclusion example (e.g. "one Saturday Ashtami vetoed"); name the 1-day fixture for the Missing row. Record in §2.2 that two of the four conjuncts are always true on the live path. |
| N10 | MINOR | §7 "Duplication | a factor in two families | one contribution | no double weight | doubles" | No fixture is defined, and by design Pushya contributes through `nakshatra_raw` *and* Guru-Pushya through `yoga_raw` (`finder.py:105-115`; `muhurat_weights.yaml:73-76,149`) — "one contribution" fails by design. A detector that can fail exists only if the row means Σ`calendar.contributions` (+ `personal.value`) == `legacy_score` (to rounding). | Define the row as the contribution-sum identity, or delete it. |
| N11 | MINOR | §7 header "tier `[S]` service-level, `[R]` route-level, `[X]` cross-asset/IP-10"; §12 "F20 accepted (§7 tiers + Evaluation)" | v1.0 F20 asked for the **F24** tier per row: `COMPUTATIONAL_CORRECTNESS / EXPLANATORY_DISCRIMINATIVE_VALUE / EMPIRICAL_OUTCOME_PERFORMANCE` (guide `:133`). `[S]/[R]/[X]` are scope labels, not verdict tiers. | Add the F24 tier column: Value row → `EXPLANATORY_DISCRIMINATIVE_VALUE`; all others → `COMPUTATIONAL_CORRECTNESS`; none `EMPIRICAL` (consistent with the Evaluation row's `not_applicable`). |
| N12 | MINOR | §4.6 "Horizon `inclusivity='closed_closed'` as the finder is today … window instants `t_start=sunrise_utc, t_end=sunset_utc`" | Binding B1 (`KALA_SYNERGY_BINDING_v1_0.md:30`): `inclusivity` is "**declared on every row**" — the row is the window (`t_start/t_end` = sunrise/sunset, `finder.py:250-251`), not the search horizon. The horizon's inclusivity belongs in B5 `requested_horizon`. The window's own inclusivity is undeclared. | Declare `inclusivity` on the window (`closed_open` if sunset is exclusive — say which) and carry the horizon's `closed_closed` inside `coverage.requested_horizon`. |
| N13 | NOTE | §4.2 "`weights_hash: sha256(loaded weights dict)`"; §7 Irrelevant control "weights dict reordered → same `weights_hash`"; Revision "change one YAML weight → `weights_hash` changes" | `config_loader.py` (read in full, `:1-79`): no version or hash surface; `load_muhurat_weights` is `@lru_cache` (`:24`) — a YAML edit is invisible until `invalidate_weights_cache()` (`:72-79`) or restart; `get_weights_for_event` returns the merged `defaults ∪ events[event]` dict (`:66-69`). "Loaded weights dict" is ambiguous (raw file vs merged per-event). | Hash the merged per-event dict with canonical serialization (`json.dumps(sort_keys=True)`); the Revision fixture must clear the cache; say raw-vs-merged. |
| N14 | NOTE | §4.7 "`routers/muhurta_score.py` and `routers/muhurat.py` call `KaMuhurtaSevaService` and serve the verdict" | `/muhurat` → `service.find_windows` is a drop-in (same kwargs, `service.py:159-169` vs `routers/muhurat.py:139-148`); `_fetch_native_chart` (`:50-119`, DB) stays on the route. `/muhurta_score` → `service.score` loses `panchang_context` (`:137-142`: tithi/nakshatra/vara/yoga names) because `score()` returns a float and does not expose the `Panchang` (`service.py:112-119`); `ayanamsha_id` validation (`:89-99`) has no service parameter. | Either the verdict carries the aṅga names (recommended: `calendar.context`) or the route computes `compute_panchang` a second time — name it; state that `ayanamsha_id` validation stays on the route. |
| N15 | NOTE | §0 "`ka_sangam/engine.py:722`, the sole `.score(` call in production, which always raises" | Confirmed: `event='general'` ∉ `EVENTS_MVP` (`finder.py:36-46`) → `score_muhurat` raises `ValueError` (`:161-162`); only `.score(` call outside tests (grep). But `service.score()` runs `_validate_location` **and a full `compute_panchang`** (`service.py:107-117`) *before* the raise; the exception is caught at `:723-735` and the term returns `None`. | Add to the Saṅgam-owned item: every Mode A/B row pays a Swiss computation for a guaranteed exception. |
| N16 | NOTE | §7 binding "B3 (`window_id`) … **B4 n/a** (no testimony aggregation …) — stated" | B3 also requires `generation` and `window_ref = {asset_id, generation, id}` (`binding:53-56`) — absent; `weights_hash` could serve as `generation`. B4's downstream rule (`:63`) binds "any projection carrying a score"; the brief exempts itself. | Add `generation = weights_hash` and a `window_ref` shape; move the B4 exemption to §10 as a decision for the binding owner, not a statement. |
| N17 | NOTE | not in brief | `panchang_engine/__init__.py:437-475` exposes a fourth `find_muhurat` wrapper with `tz_offset_minutes` default 330 (`:457`) inside the L0 package; no non-test caller found (grep). | Record in §11 beside `routers/muhurat.py:34`. |
| N18 | NOTE | §9 "Campaign: `ANALYZED → ENRICHED`" | `PRODUCER_READY` resolves (template `:36`), `CONSUMER_INTEGRATED` resolves (Strategy `:397`); `ANALYZED`/`ENRICHED` not found in template, guide, Strategy or `KALA_ACCEPTANCE_RECORD_CONTRACT_v1_0.md` (grep). | Cite the campaign-ladder source or drop the pair. |

## 3. Citations verified

Format: citation → resolved? → what is there. All source files: `git diff --stat 9feac52d7 HEAD` and `… bf70a3477 HEAD` empty.

**Service / writer / finder**
- `service.py:6` never silently assumed → yes. `:37-60` `_validate_location` → yes. `:78-119` `score -> float` → yes; `:93-96` un-floor → yes; `:101` 0..100 → yes; `:107-117` validate + `compute_panchang` before `score_muhurat` `:119` → yes. `:121-169` `find_windows(top_n=10)` → yes; `:159-169` delegates to `find_muhurat` → yes. `:178-211` module delegates → yes. `:110,:157` `from muhurat.finder import` → yes (**not** `panchang_engine.muhurat`).
- `writer.py:65-69` five anchors → yes. `:137-142` `WriterResult(rows_inserted=0)` → yes (`:137`). `:220-221` `score_muhurat` direct → yes. `:237-273` Check 4 knockout → yes (`:237` header).
- `panchang_engine/muhurat/finder.py` → **does not resolve.** Actual `platform/python-sidecar/muhurat/finder.py`: `:4-5` moved-from note; `:36-46` `EVENTS_MVP` (8); `:58-76` `_in_inauspicious`; `:101-141` `_score_breakdown`, `:139-140` `tara_bala` iff `native_chart`; `:148-196` `score_muhurat` (0..100, `:196`), `:161-162` raises on unknown event, `:164-166` weights/table; `:203-261` `find_muhurat`, `:215,:219` inclusive (brief `:216`), `:242` `while current <= date_to` (brief `:243`), `:246` `if s > 0`, `:247-255` breakdown attached, `:250-251` sunrise/sunset, `:258` date step (brief `:257`), `:261` `[:top_n]`.
- `panchang_engine/muhurat.py:1-38` → shim; `:16` re-exports `_score_breakdown`.
- `panchang_engine/types.py:258-280` `NatalChart`, `MuhuratWindow.breakdown` (`:280`) → yes.
- `muhurat_weights.yaml:19` "Version: 1.0" comment → yes; `:41,48` vivah sum / `native: 0.10` → yes; `:127` mantra_initiation `native: 0.20`; `:148`, `:169` `native: 0.25`; `:12-13` source abbreviations → yes; no machine-readable citation field → yes.
- `panchang_engine/config_loader.py:1-79` → read in full: `@lru_cache` `:24`, merge `:66-69`, `invalidate_weights_cache` `:72-79`; no version/hash surface.
- `panchang_engine/tara_bala.py:40,44` Vipat/Vadha score 0.00; `:104-122` `compute_tara_bala_score` 0.0..1.0.
- `panchang_engine/timings.py:196,200` rahu_kalam/yamagandam unconditional; `panchang_engine/__init__.py:102-103,168` live `inauspicious` built from that dict (label `yamagandam` — knockout is live-reachable); `:437-475` wrapper `find_muhurat` with tz default 330 (`:457`).

**Routes / serving**
- `routers/muhurta_score.py:9-13,25-32` (two engines, vocabulary decision), `:34-39` default-location docstring, `:48-50` engine-direct imports, `:54-58` constants, `:61-69` request (no location/chart), `:73` `-> dict`, `:89-99` ayanamsha gate, `:115-117` `compute_panchang` at default, `:123` `score_muhurat`, `:129` `score`, `:131-136` `location.note`, `:137-142` `panchang_context` → all yes. `main.py:102-108` router mounted → yes.
- `routers/muhurat.py:20` `from muhurat.finder import` → yes; `:32-34` lat/lon required, tz default 330 → yes; `:46` `breakdown` (brief `:44` → `star_rating`); `:50-119` `_fetch_native_chart`; `:131` 90-day cap; `:139-148` `find_muhurat` direct → yes.
- `elect.ts:62` import `handleMuhurtaFinder` → yes; `:122` `chart_id` required; `:134` `native_janma_nakshatra` input; `:202-204` Vadha `hard_veto` comment; `:229-233` `defaultDateRange` `new Date()`+90d → yes; `:286-290` thesis; `:296-298` falsifier; `:314-318` `tara_bala` coverage (`:317` `honestEmptyCoverage`); `:862-869` finder input, no location; `:872` `handleMuhurtaFinder` call → yes. grep `lat|lon|location` → only `chart_id`.
- `muhurta_finder.ts:32-34` → resolves to a **stale comment** naming `/api/compute/muhurat`; `:41` `callPlatformPrimitive`; `:83-85` `action_type` enum; `:280` score [0,1]; `:333` `tara_bala_status`; `:450-542` `ACTIVITY_RULES.event_class` bridge (`:490` medical→upaya_ritual, `:498` education→vidya_arambha, `:514` general→general).
- `platform/src/lib/retrieval/registry/layers/L4_phala/query_muhurat.ts:7-8,124` `POST /api/compute/phala/muhurta_finder` → yes.
- `brahmagyan/phala/muhurta.py:1317` `_fetch_panchanga_row` → yes; `:1329-1331` `_panchanga_quality_for_action` → yes; `:1893-1894` route → yes; `:1178-1185` reads `panchanga_daily`; grep for `score_muhurat|muhurat.finder|ka_muhurta_seva|find_muhurat` → 0 hits.
- `call_service_wrappers.ts:415-416` description ("reuses the same score_muhurat() … ph_muhurta/muhurta_finder … calls internally") → yes; `:463` `inputs`/`outputs`; `:489-490` comment; `:502` body; `:509` `{ content: data }` passthrough → yes.
- `asset_registry_seed.ts:2263-2276`; `:2267` "Location mandatory — no silent Bhubaneswar default"; `:2274` `depends_on: ['ka_graha_sancara']` → yes.
- `useMuhuratFinder.ts:62-73` body; `:71` tz explicit → yes.

**Writers / probes / tables**
- `ka_sangam.py:40` import, `:329` instantiate → yes. `ka_sangam/engine.py:700-712` docstring, `:722` `.score(… event='general')`, `:723-735` caught → `None` → yes; grep `\.score(` outside tests → only `:722` (+ service's own docstring/delegate).
- `ka_vighnakara.py:197-198` instantiate; `:613-621` `compute_panchang` direct; `:622-624` tithi only → yes.
- `ph_muhurta.py:134-136` proxy formula + comment; `:160` `source:'ka_muhurta_seva_proxy'` → yes. `migrations/968_…:75-76` → yes.
- `service_probes.py:975-1010` header; `:1097,:1142,:1150-1151` calls `score_muhurat` direct (not `.score`) → yes. `routers/nirmana_probe.py:26` mapping only.
- `ka_graha_sancara.py:91` comment → yes.
- `scripts/panchanga_daily_writer.py:42-46` Bhubaneswar constants, `:205` `compute_panchang(current, NATIVE_LAT, …)` → yes. `platform/supabase/migrations/427_panchanga_daily_reprovision.sql:41-43` single-observer → yes.
- `platform/migrations/676_…:42` `depends_on = '{}'` → yes.
- Tests: `tests/test_ka_muhurta_seva.py`, `tests/l3/test_muhurta_score_sidecar_route.py` exist (F13); no `-k verdict` test exists yet (planned; F23 applies). `services/ka_temporal/date_resolver.py` exists (B1 resolver statement is accurate).

**Governance**
- Register §5 → `MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md:158` → yes, verbatim.
- Strategy `:71` L3-Q10, `:97` no universal ranking, `:273` L3-A03, `:397` `CONSUMER_INTEGRATED` → yes.
- Product (`00_ARCHITECTURE/MADHAV_PRODUCT_DEFINITION_v3_0.md`, the file guide `:31` names): §3.11 `:168-172` → yes ("reusable partial capital" `:172`); §5.2 `:220-223` → yes; §10 `:365` "date/location/time-zone fidelity" → yes.
- VA `MADHAV_DATA_PLANE_VALUE_ARCHITECTURE_v2_0.md:152-158` §4.4 "Global scope also does not mean location-free … Muhūrta lattice uses a reference location" → yes.
- W0 `MADHAV_DATA_PLANE_L3_W0_FIELD_CONTRACT_REGISTER_v1_0.md:33`, `:640-644` → yes.
- Lane D `readiness/_work/LANE_D_ASSET_REGISTER.md:532-566` §10, `:553` "not independently re-confirmed" → yes. Lane E `LANE_E_LAYER_VALUE_MODEL.md:232` Q-K09, `:961-967` §5.8 → yes. Lane F `LANE_F_DEMAND_LEDGER.md:157` P11, `:164` orphan U-id → yes.
- DAG `audit/KALA_DAG_RECONCILIATION_v1_0.md:33` → yes.
- Blueprint `:671` Q7 `{nearest, strongest, robust}`; `:748` §12.1; `:751-763` IP-1…IP-9 (IP-10 is the next free number; no collision in any sibling brief) → yes.
- Binding `:19-22` rule of adoption; `:30` `inclusivity` on every row; `:32` `time_basis`; `:53-56` B3; `:63` B4 downstream; `:69` B5 shape → yes.
- Context `:103-105` §5 serving; `:137-139` live-path wording; `:184-190` §11 → yes. Guide `:90` never cite `estimated_seconds` (brief now clean — grep 0 hits outside the changelog); `:133` F24 tiers; `:135` packet form; `:153` forbidden acts → yes.
- Tulana brief `:71,:79,:162,:170` same criterion vocabulary → yes. `KALA_DELEGATED_DECISIONS` / `NATIVE_RULING_SHEET` grep `muhurta|criterion|Q7` → 0 (brief §11.7 accurate).
- Commits `793972c75`, `f6fed12c7`, `47131772b` → exist; `47131772b` subject "fix(data-plane): identify L3 frontier writer results" (content not inspected — brief marks [A], correct). `KALA_COST_PROFILE_v1_0.md` → does not exist (brief says "pending" — correct).

## 4. What I could not verify

1. **Live registry state** (`depends_on`, `service_health`, migration 676/850 applied) — no DB; migration text only.
2. **Live incidence** of the route default, `> top_n` cuts, latency — no runtime; the brief does not claim them.
3. **`47131772b` content** — subject line only.
4. **Whether any Pūrṇa surface already discloses `panchanga_daily`'s observer** — `elect.ts` grep only; not read end-to-end (brief §11.5 says the same).
5. **Campaign ladder `ANALYZED → ENRICHED`** — source not found (N18).
6. **Owner/fence of `platform/python-sidecar/muhurat/`** — no governance text names it (N1); I could not determine whether it is inside or outside this asset's fence, only that it is not `panchang_engine/**`.

## 5. Conformance to the binding (B1–B7)

**Conformant:** `t_start/t_end` (`timestamptz`), `time_basis='event_instant'`, `claim_grain='date_grain'`, `resolver` honestly stated as not `date_resolver`; `completeness_state` values within the six; `epistemic_class`, `operator_role`, `comparable_with ∈ {self, different_convention}`, `tier_basis='relative_uncalibrated'`, `source_qualification` per family; B5 `coverage` in the seven-key shape with `exclusions[]` as the alias for `vetoed` (declared); `criterion ∈ {nearest, strongest, robust}` (Q7); B2 overclaim withdrawn (`corpus_verifiable`, R-6 four not named — correct).

**Non-conformant or incomplete:**
- **B1 `inclusivity`** declared on the horizon, not on the row/window (N12).
- **B3** `window_id` present; `generation` and `window_ref` absent (N16).
- **B4** self-exempted ("n/a") although the verdict carries scores (`calendar.value`, `legacy_score`) — a decision for the binding owner, not the brief (N16).
- **B5** shape conformant, but the recommended fence option cannot populate `exclusions[]`/`truncated` (N2) — a conformant schema with no detector behind two of its fields would be an N.8-class signal.
- New asset-local names (each needs a dossier row per Context §8): `location_used`, `legacy_score`, `weight_source`, `weights_hash`, `contributions`, `vetoes`, `criterion`, `vetoed_excluded`, `candidates_evaluated`, `vetoed`, `returned`, `top_n`, `truncated`, `window_id` — listed in §7; `coords_source`/`tz_source` would be added by N7.

## 6. Disposition of the twenty v1.0 findings (verified at source, not from §12)

| v1.0 | v1.1 status | evidence |
|---|---|---|
| F1 BLOCKER consumer map | **RESOLVED** | Three engines re-traced (§3 above); zero value-receiving callers confirmed by grep; `:722` always raises (N15). Residuals: wrong finder path (N1) and stale `muhurta_finder.ts:32-34` link (N8). |
| F2 route default / hazard location | **RESOLVED** | §3a "declared global … typing gap"; §3b hazard placed at `panchanga_daily` + `phala/muhurta.py:1317`, cross-asset, not owned. |
| F3 `breakdown` as existing capital | **RESOLVED** | §1, §2.2, §2.4, §4.2 alias `breakdown.tara_bala → personal.value`; `'tara_bala' in breakdown` detector. |
| F4 criterion vocabulary | **RESOLVED** | §4.5 `{nearest, strongest, robust}` + `vetoed_excluded`; matches blueprint `:671,:748` and Tulana. |
| F5 fictional float readers | **RESOLVED** | §2.3/§5: `ph_muhurta`, `ka_vighnakara`, `ka_sangam` dropped; real readers `routers/muhurat.py` (UI hook) + `muhurta_score.py:129`. |
| F6 Negative-row split / descriptor change named | **PARTIAL** | Split done (§7 Negative PRESERVE + delta). The named "descriptor change (`:463`, `:502`)" is request-side; the response reaches `content` via `:509` passthrough with no descriptor change; `caller` unreachable on `/muhurta_score` (N7). |
| F7 Delivery sentinel path | **RESOLVED** | Delivery restated on `routers/muhurat.py` + UI hook; IP-10 row gated. IP-10's own feasibility is N6. |
| F8 `undertaking` component | **RESOLVED** | §4.2 `weight_source` replaces it; constraint set `unexplored` with DP02 source. |
| F9 "chart-less requests" | **RESOLVED** | §2.2 "no chart or location input". |
| F10 0.71 scale | **RESOLVED** | 0..100 throughout (grep `0.71` → 0). |
| F11 inclusivity | **PARTIAL** | Horizon `closed_closed` declared and correctly cited in substance; but B1 inclusivity belongs on the window row, still undeclared (N12); line numbers off by one (N8). |
| F12 `estimated_seconds` | **RESOLVED** | Only mention is the changelog entry recording its removal. |
| F13 test paths | **RESOLVED** | Both files exist. |
| F14 `47131772b` [A] | **RESOLVED** | §2.5 "[A] (subject line only)". |
| F15 weights version surface | **RESOLVED** | `weights_hash` proposed; `config_loader` now read by this review — no version field, so a hash is the only surface; canonicalization + cache note (N13). |
| F16 five of seven | **RESOLVED** | §2.2. |
| F17 effect size | **PARTIAL** | Stated, but wrong for mantra_initiation (0.20), upaya_ritual/sadhana_initiation (0.25) (N4). |
| F18 tz default 330 | **RESOLVED** | §2.2, §4.3, §7 Context, §11.6. |
| F19 catalog correction | **RESOLVED** | §5 seed `:2267` + descriptor `:415-416,489-490`. Add the stale `muhurta_finder.ts:32-34` comment (N8). |
| F20 Evaluation row + F24 tiers | **PARTIAL** | Evaluation row present (`not_applicable`, reasoned). Tiers added are scope tiers `[S]/[R]/[X]`, not the F24 verdict tiers (N11). |

Binding items from v1.0 §5: B5 shape → addressed (schema) but see N2; B2 overclaim → withdrawn; B3 → `window_id` only (N16); B1 `time_basis`/`resolver` → addressed; B4 → self-exempted (N16).
