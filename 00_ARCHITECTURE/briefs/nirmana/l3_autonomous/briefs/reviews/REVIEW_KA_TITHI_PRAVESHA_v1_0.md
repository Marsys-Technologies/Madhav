---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_TITHI_PRAVESHA
version: "1.0"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_TITHI_PRAVESHA_ELEVATION_BRIEF_v1_0.md
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD 27b0146f3; the brief's source_revision 9feac52d7 is an ancestor and `git diff --stat 9feac52d7 HEAD` on services/ka_tithi_pravesha/, query_tithi_pravesha.ts and kala_views/now.ts is empty, so citations checked at HEAD hold at 9feac52d7"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no git write, no external service"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed as written; every file:line re-read with sed/grep at HEAD; the engine's timezone convention checked down to PyJHora (site-packages jhora/panchanga/drik.py); prior audit artifacts (Lane D, T1, F2, context §4) treated as [A] and re-checked at source where they carry a code claim"
---

# Review — `KA_TITHI_PRAVESHA_ELEVATION_BRIEF_v1_0`

## 1. Verdict

**REWORK.** The diagnosis of the timestamp defect is correct and well-cited (`raw[:19]` → naive `instant` → `timestamptz` INSERT), and the recommendation's direction (tz-aware instants, unsourced-with-receipt, one declared root, a voice not a weight) is right — but the brief mis-states what the defect corrupts (the stored lagna and graha positions are cast at the *correct* absolute instant; only `window_start/window_end` are mislabeled — which inverts the rebuild's equivalence invariant), two proof rows carry expected values that a *correct* implementation would fail (Relevant-influence, Boundary), the Delivery sentinel is a constant default that cannot prove per-row propagation, the Positive fixture is not the production input shape, a registered candidate consumer with a contradictory citation record is omitted, Q09 is overclaimed, and the offered `coverage` is not the B5 shape.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| F-01 | **MAJOR** | §2.4: `pravesha_lagna_*`, `graha_positions_jsonb` *"inherit the instant's error"*; §3 Impact: *"fatal for the praveśa **lagna** … can be off by two or three signs … and for every `graha_positions_jsonb` value"*; §5: *"the corrected instants change every stored row"*. | `writer.py:130-146` `_moon_longitude` builds `jd` from the naive wall-clock and passes `tz=float(bp["tz_offset_hours"])` (`:141`); `:149-156` `_annual_chart` passes `birth_params` (incl. `tz_offset_hours`) with the naive `instant.isoformat()`; `pyjhora_adapter/compute.py:30-33` strips tzinfo and `:58-66` uses `inputs["tz_offset_hours"]` — the identical local-jd+Place(tz) pattern the FORENSIC-verified L1 path uses; PyJHora `panchanga/drik.py:1773` `jd_utc = jd - place.timezone / 24.` Writer test `test_ka_tithi_pravesha_writer.py:189-196` confirms year-1 return within 600 s of the naive birth_dt against the L1 natal Moon. So the cast is computed at the **correct** absolute instant; the only error is that the naive `instant` is handed to a `timestamptz` column at INSERT (`:81-98`, DDL `531:49-50`). | Restate the impact: the row *asserts* an instant 5.5 h late; the lagna/positions/audit are correct. Make that the rebuild's equivalence contract — `pravesha_lagna_*`, `graha_positions_jsonb`, `ephemeris_audit_jsonb`, `natal_moon_longitude_deg` **byte-identical** pre/post; `window_start/end` move by exactly −`tz_offset_hours`; nothing else changes. Drop "two or three signs". A downstream that *re-casts* from the served instant would be wrong — say that instead. |
| F-02 | **MAJOR** | §7 Relevant influence: *"shift the birth offset by +05:30 → +00:00 → `t_start` shifts by exactly 5.5 h; the cast changes"*; detector *"unchanged"*. | The natal Moon is read from L1 by `fact_id` (`:72-77`) and is fixed; the lunar return is an astronomical event. Under a **correct** UTC implementation a tz relabel moves the anniversary 5.5 h but leaves the nearest return (and the cast at that absolute instant) unchanged, barring the nearest-return knife-edge. A 5.5 h shift in `t_start` is exactly what **today's** naive code produces (`:187,:193` operate on wall-clock). The row's detector fires on the correct implementation and passes the defective one. | Expected: `t_start` (UTC) and the cast **unchanged**; the relevant-influence fixture should perturb what the asset actually depends on — the natal Moon fact (+13.176° → `t_start` moves ≈1 day; `logic.py:67`) or the birth date (anniversary moves; return follows). Keep the tz-relabel as the *irrelevant-control* row. |
| F-03 | **MAJOR** | §7 Boundary: *"a return at 23:55 IST → UTC date is the previous day"*; detector *"off by a day"*. | 23:55 IST = 18:25 UTC the **same** day (IST = UTC+05:30); the previous-UTC-day case requires a return before 05:30 IST. As written the detector flags a correct implementation. *"`pravesha_lagna` from the true instant"* is already true (F-01). | Fixture 03:00 IST (→ 21:30 UTC, previous day) or restate; the boundary assertion becomes "UTC date and the served local date differ, both correct under their declared tz". |
| F-04 | **MAJOR** | §7 Delivery: sentinel `source_qualification='unsourced'` reaches `query_tithi_pravesha.ts` and `kala_now_get`; detector *"absent"*. | A constant on every row is the default, not *"a decisive non-default sentinel"* in *"a low-ranked, non-default field"* (`…BRIEF_CONTRACT_v1_0.md:112`; BINDING B7; blueprint §12.3). It cannot distinguish "the column propagates per row" from "the wrapper emits the literal". Separately, `query_tithi_pravesha.ts:83-90` is an explicit column list and `now.ts:620-628,650-658` picks named fields, so nothing new reaches either surface without the Pūrṇa packet — fine as a today-fails detector, but the sentinel choice is wrong. | Sentinel = a per-row value the wrapper cannot synthesize: `completeness_state='unavailable'` seeded on one year via a non-converging fixture, or `independence_group.roots[0] == moon_fact_id` (the real L1 id). Assert it in `kala_now_get`'s `tithi_pravesha` object and in the saved reading. |
| F-05 | **MAJOR** | §2.3: *"any `ka_*` writer — none"*; *"No integrator consumes the asset"*; §3 secondary (G-1) treats the writer's `not_in_corpus` string as the only citation record. | Omitted: `services/gochara_v3/mechanisms/w27_annual_stack.py:306-372` `compute_tithi_pravesha` (`MECHANISM_ID_B = "w27b_tithi_pravesha"` `:74`), registered in `mechanisms/registry/w27b_tithi_pravesha.yaml` (`admission_state: candidate`; `data_source: "ClassContext.tithi_pravesha_rows (from kala_tithi_pravesha…)"`; `modifier_schedule favourable 1.25 / adverse 0.80`; **`citation_status: cited`, `classical_citation: "Kṛṣṇa Miśra / Tājaka tradition … Sudarśana Cakra Paddhati"`**), `mechanism_register.yaml:215-223`, `grammar_v3_registry.yaml:212-214`. Live path: nothing populates `tithi_pravesha_rows` (grep: only w27 reads it; `context.py:114` `ClassContext` has no such field), `engine.py` has no w27 reference — so "no live integrator" stands. But (a) it is a declared candidate that treats the praveśa chart as a **λ-product modifier** — the exact thing §4 item 7 / blueprint Q2 (`:667`) forbids before an M-4 operand audit; (b) its yaml asserts a **citation** the writer's `:64-70` denies — two repo records disagree on the brief's own G-1 question; (c) its expected keys (`event_class`, `tone`, `varsha_start_iso`) match no `kala_tithi_pravesha` column. | Add the w27b row to §2.3 as "candidate, unwired, no populator (scope: `services/gochara_v3`)"; §10 decision 2 must say whether w27b's modifier schedule is superseded by the voice or held under M-4; the D-M receipt must adjudicate the yaml's citation string as well as the writer's absence string. |
| F-06 | **MAJOR** | §4 item 1: *"Q09 (what changes with convention — it carries `ayanamsha_id`)"*; *"Q05/Q-K06 (an annual method as a distinct voice …)"*. | One hardcoded convention: `CANONICAL_AYANAMSHA` (`:61,:109,:272`), engine pinned `'lahiri'` (`:62`); no variant computed, and the delta adds none. Strategy L3-Q09's proof is *"Recompute declared variants; match the same propositions and boundary changes"* (`STRATEGY:70`). A constant column does not serve Q09. Q05 exists only after §4 item 7 is ruled (decision 2); today none of Q05 is served. | Q09 → *cannot serve (single convention; a second ayanāṃśa partition would be a separate delta)*; Q05 → *partial, conditional on decision 2*. Q06 stands. |
| F-07 | **MAJOR** | §7 binding line / §4 item 8: OFFERS B5 with `coverage = {years_requested, years_computed, unavailable_years[]}`. | `KALA_SYNERGY_BINDING_v1_0.md` §B5 fixes `coverage` to `{requested_horizon, completed_horizon, resolution, partitions_searched[], exclusions[], unsearched_regions[], completion_detector}` and rules a differently-named field *"non-conformant until it renames or aliases with a declared mapping"*; blueprint §12.1 Clock-interval row expects `clocks_consulted / clocks_unavailable`. | Rename (`requested_horizon`=years 1..120, `completed_horizon`, `exclusions[]`=non-converged years with reason, `completion_detector`=the contiguity check) or declare the alias map in §7. |
| F-08 | **MAJOR** | §7 Positive fixture *"canonical birth params **with** offset"*; §2.2 *"discarding any offset"*; §4 item 2 *"Parse `datetime_iso` with its offset (or the chart's declared tz …)"*; §7 Negative *"`datetime_iso` without offset and no chart tz → no-op"*. | Production shape: `pipeline/orchestrator/birth_params.py:96-106` builds `datetime_iso` from `datetime.combine(birth_date, birth_time).isoformat(timespec="seconds")` — **naive, never carries an offset**; the offset is `tz_offset_hours` (`:97-101,:109`, from `timezone_id`, required by `_REQUIRED` `:33`). `raw[:19]` discards nothing on any orchestrator-built input; the writer already holds the tz and uses it for the engine (`:141`) — it only fails to attach it before INSERT. `test_ka_tithi_pravesha_writer.py:105-120` pins the naive shape. | The primary path is "attach `tz_offset_hours` (fixed offset at birth) to the wall-clock and convert to UTC"; the Positive fixture must be the production shape (naive + `tz_offset_hours`), the "with offset" string a second fixture; the Negative row must say the orchestrator cannot produce "no chart tz" (`:33`) and remains a unit-level guard. |
| F-09 | MINOR | §3: *"~2.7° from `natal_moon_longitude_deg` (5.5 h × ~0.5°/h)"*; §4 item 11 *"~2.7° off"*. | Mean Moon speed 13.176°/day (`logic.py:67`) = 0.549°/h → 5.5 h ≈ **3.0°** (true speed 11.8–15.4°/day → 2.7°–3.5°). Conclusion (≫ 0.01°) unchanged. | Write "≈3°". |
| F-10 | MINOR | §1 row / header `accepted_upstream_contract`: *"W2 source `47131772b` (writer prepares its candidate before the chart-partition DELETE)"*. | `git show --stat 47131772b` touches only `writers/ka_avadhi.py`, `writers/ka_yojaka.py` and two tests; it is not an ancestor of HEAD. The prepare-before-DELETE block (`writer.py:281-294`) landed in `fa9857f00` (2026-09-16, #2607) per `git log -S'replacement begins only after full preparation'`. | Cite `fa9857f00` (#2607). |
| F-11 | MINOR | §7 Duplication: detector *"counted twice"*. | No code path counts this asset's testimony: no integrator (brief §2.3), SC-6 ledger *unowned* (blueprint `:285`), IP-5 not built (`:12.2`). A planned test with no detector (F23). | Today's provable invariant is row-level: persisted `independence_group.roots == [moon_fact_id]` and equal to the `chart_facts` id `ga_dashas` seeds from; mark the served double-count test as "detector exists once IP-5/SC-6 lands". |
| F-12 | MINOR | §7 Value: fixture *"frozen Q06 / ordinary-period"*. | `KALA_BASELINE_v1_0.md` does not exist on this base (only `KALA_BASELINE_FREEZE_EXECUTION_PROMPT_v1_0.md`; blueprint changelog 4.0: *"KALA_BASELINE_v1_0.md was NEVER CREATED"*). No ordinary-period fixture is named. | State the fixture is pending the freeze; name an ordinary praveśa year (no dramatic event) as the ordinary-period case. |
| F-13 | MINOR | §7 Irrelevant control: *"reorder `graha_positions_jsonb` keys → identical row"*. | JSONB has no key order; the row cannot fail. | Control on graha **array** order (assert canonical `GRAHA_ORDER`, `positions.py`) or on cosmetic `birth_params` fields (`place_name`, `subject_label`, `birth_params.py:110-111`) leaving every computed column identical. |
| F-14 | MINOR | §7 has 10 rows, no command / evidence-path columns. | Contract §6 lists eleven rows incl. **Evaluation** (`…CONTRACT_v1_0.md:105-115`); instantiation guide §7 requires *"fixture/data boundary, command, expected result, invariant, detector, evidence path"*. | Add Evaluation as `not_applicable` (no governed outcome set) plus the two columns. |
| F-15 | MINOR | §4 item 5: `independence_group = [{family, roots, basis}]`. | BINDING §B4: `[{group_id, family, roots[], members[], basis:'declared_lineage'}]`. | Add `group_id` and `members[]`. |
| F-16 | MINOR | Template v2.0 §2.1 latent-value (a)–(d) classification, §2.5 consumer walkthrough (a Product §9 experience incl. the ordinary period), §2.6 knowledge-time pin — absent. | `KALA_ASSET_ELEVATION_PLAN_TEMPLATE_v2_0.md` §2.1/§2.5/§2.6; `MADHAV_PRODUCT_DEFINITION_v3_0.md:334` §9. The DDL's `computed_at` (`531:63`) is the only knowledge-time field and is unmentioned. | Add a short experience-4 walkthrough (`kala_now_get` → `tithi_pravesha` object: what changes for the reader after the fix — honestly, a correct instant and a declared root, no new distinction until the voice lands); classify each material field (a)–(d); pin event vs knowledge time. |
| F-17 | MINOR | §5 interface packet: *"serve `t_start` with `claim_grain`"*. | `query_tithi_pravesha.ts:84-85` serves `to_char(window_start,'YYYY-MM-DD"T"HH24:MI:SS')` — offset stripped. In a UTC session today's mislabeled rows render as the *correct-looking* IST digits; after the fix they render as UTC digits 5.5 h earlier — a consumer comparing the served string to local wall-clock reads the fix as the regression. `:70` defaults `as_of` to `new Date()` and `:90` casts a bare date `$4::timestamptz` at session-tz midnight — B1 tz-source items on the serving path. | The packet must serve the offset (`…SSOF` or `AT TIME ZONE` with the declared tz), name the `as_of` default and the cast, and the Delivery detector must compare **instants**, not strings. |
| F-18 | NOTE | §5 / §6 lens I: *"one migration ≥ 1082"*. | Highest migration on this base: `platform/supabase/migrations/1072_kala_convergence_episodes.sql`; blueprint 4.0 says floor 1075; "≥ 1082" is the Yojaka brief's reasoning (`KA_YOJAKA_ELEVATION_BRIEF_v1_0.md:231`: 1080–1081 claimed by Gochara WP0-7) — not stated here. | Cite the numbering source, or say "next free after the Gochara packet". |
| F-19 | NOTE | §2.2 / §5: `ENGINE_AYANAMSHA = "lahiri"` is *"SC-10's"* and fenced with `pyjhora_adapter`. | The literal is at `writer.py:62`, inside `may_touch`; SC-10 (blueprint `:289`) targets exactly *"no bare `'lahiri'` default anywhere on the L3 path"*. | Either import the alias map in the delta or state why the in-scope literal stays. |
| F-20 | NOTE | §2.4: `verification_pass_status` *"can and does read `divergent_flagged`"*. | "Can" verified (`:207-211`, DDL CHECK `531:60`); "does" (any production row) is unverifiable here — Lane D §13 and context §4 carry no count. | "Can". |
| F-21 | NOTE | §0 / §3: *"no `count(*)` was ever run against `classical_text_chunks` … has **never been asked by predicate**"*. | An absence claim about history with no receipt — the class the brief indicts. The writer docstring `:31-35` says an ingestion work item *"is filed"*; I found no such item to check. | Mark [A]/unverified, or cite the L0 lane record that shows no predicate was run. |
| F-22 | NOTE | §2.3: *"`ClassContext.fetch` never read it [A: Gochara plan §5.4]"*; *"declared edge retired (N-8)"*. | `GOCHARA_FAMILY_ELEVATION_PLAN_v0_3.md`: 0 hits for `tithi`, `ClassContext.fetch`, `N-8`. N-8 resolves only through blueprint `:323,:325,:975`. At source `context.py:114` `ClassContext` has no `tithi_pravesha_rows` field and nothing populates one — the *claim* is true; the citation does not resolve. | Cite `context.py:114` + the w27 grep (F-05); point N-8 to where it is actually defined. |
| F-23 | NOTE | §5 Rollback / §7 Revision: `formula_version` bump; *"no accretion"*; lens C invariants `t_end(N) = t_start(N+1)`, roots converge. | The applied contract `platform/migrations/670_…:1413-1515` already enforces tiling (a), verification re-derivation (b), fact join (d), single anchor (e), contiguity (h), 338–393 d band (i); none pins `formula_version` or the citation string, so the bump is safe — but (b) re-derives from the same naive-computed audit and therefore **cannot** detect the tz error; it is the only live detector today. Tests that must flip: `test_ka_tithi_pravesha_writer.py:117` (naive equality), `:196,:205` (aware − naive raises `TypeError`), `test_w2_first_frontier_writer_preservation.py:289`. The code-digest pin `platform/src/generated/nirmana-writer-digests.json:100` (consumed by `dispatch_frozen_rebuild.py:29,58,113`) must be regenerated — not in `may_touch`. | Cite 670 as the existing detector set and its blind spot; list the tests that must flip; add the digest regeneration to the packet. |
| F-24 | NOTE | Header `parent_layer_contract: "… / blob 793972c754b…"`. | `git cat-file -t 793972c754b…` → `commit`. | Write "commit". |
| F-25 | NOTE | §2.1 `asset_registry_seed.ts:2536-2544`; §2.2 `:203-211`, `:240-275`; §3 "15 texts / 10,651 chunks (Gochara G-9 count)". | Seed: `asset_id` at `:2530`, `target_table` `:2537`, `depends_on` `:2544` — range starts after the id. Verification: comment `:201-205`, code `:206-211`. `run` spans `:240-303`. G-9 at blueprint `:325` is a *kota* corpus count; 10,651 appears in `MADHAV_PRODUCT_DEFINITION_v2_0_PROPOSAL.md:277,368` as **16** texts / 10,651 chunks and in `L0_W1_ANALYSIS_BATCH_B.md:15`. | Tighten ranges; cite the real corpus-count source and reconcile 15 vs 16. |

Not findings, confirmed at source: constants `:60-70` and docstring `:35`; `_FETCH_NATAL_MOON_SQL` `:72-77` by `fact_id` at the canonical ayanāṃśa (§N.5 correct); `raw[:19]` at `:125`; `_compute_one_year` `:181-231` with naive `window_start/window_end` at `:215-216` and the two-pass verification `:206-211` (`LUNAR_RETURN_TOL_DEG = 0.01`, `logic.py:73`); `_DELETE_SQL` `:79`, `ON CONFLICT … DO NOTHING` `:97`, INSERT `:81-98`; honest no-op `:249-253`; `DEFAULT_MAX_PRAVESHA_YEAR = 120` (`logic.py:81`); `pravesha_anniversary` `logic.py:102-109`; `lunar_return` never raises `:139-142`; DDL `531:44-72` (`window_start/window_end TIMESTAMPTZ`, natural key, CHECK on `verification_pass_status`); the two served readers (`query_tithi_pravesha.ts`, `now.ts:613-660`) and no `ph_*`/`mi_*`/`ka_*` writer reading the table (repo-wide grep, all code types); seed row `:2530-2544`; Register `:146` verbatim; Strategy `:66-70`, `:91`, `:279`, `:352-353`; Blueprint `:217,:280,:285,:323,:395,:649,:667,:905`; G-1 (`§15`); D-M (`KALA_DELEGATED_DECISIONS:411-417`); elevation plan Q2 `:188`; context §4 5.5 h [A]; Lane D §13 `:655-676` (120 rows, `lit`, single build 2026-09-07 [A]); T1 `:305`; F2 `:125` t0 freeze [A]. No confidence scalar invented; no question outside L3-Q adopted; no `kala_views` edit beyond a packet; no orchestrator change; the source-qualification stamp is proposed pending the delegated D-M lane, not decided; no `estimated_seconds` cited as cost; the simpler baseline ("today's rows") is the actual current behaviour; the §4 item 11 ablation (Moon re-evaluated at the stored instant read as UTC) genuinely distinguishes the fix from plumbing.

## 3. Citations verified

| citation (as in brief) | resolved? | what is there |
|---|---|---|
| `writer.py:13` | yes (13-14) | "~3.4ms/row" |
| `writer.py:35` | yes | "honestly `not_in_corpus` on every row" |
| `writer.py:60-70` / `:64-70` | yes | `FORMULA_VERSION`, `CANONICAL_AYANAMSHA`, `ENGINE_AYANAMSHA`, `CLASSICAL_SOURCE_CITATION` |
| `writer.py:72-77` | yes | `_FETCH_NATAL_MOON_SQL` |
| `writer.py:79`, `:97` | yes | `_DELETE_SQL`; `ON CONFLICT … DO NOTHING` |
| `writer.py:81-98` | yes | `_INSERT_SQL` incl. `window_start, window_end` |
| `writer.py:116-127` / `:125` | yes | `_birth_dt_and_params_from_config`; `datetime.fromisoformat(raw[:19])` |
| `writer.py:181-230` | yes (181-231) | `_compute_one_year` |
| `writer.py:203-211` | yes (201-211) | two-pass verification |
| `writer.py:240-275` / `:252` | partially (`run` = 240-303) | no-op notes at `:252` |
| `logic.py:73` | yes | `LUNAR_RETURN_TOL_DEG = 0.01` |
| `logic.py:102-109` | yes | `pravesha_anniversary` |
| `logic.py:121-139` | yes (121-142) | `lunar_return`; "never raises" at `:139` |
| `asset_registry_seed.ts:2536-2544` | yes (2530-2544) | seed row |
| `query_tithi_pravesha.ts` (table read) | yes | `:83-94` SQL over `kala_tithi_pravesha` |
| `now.ts` (table read; "computed") | yes | `:613-660` fetch; `:2040-2049` coverage |
| `REGISTER:146` | yes | verbatim |
| Strategy §6.1 L3-A09 | yes (`:279`) | verbatim |
| Strategy §3 Clock interval | yes (`:91`) | verbatim |
| Strategy L3-Q05/Q06/Q09 | yes (`:66,:67,:70`) | Q09 proof = "Recompute declared variants" (F-06) |
| KALA_ASSET_BRIEF_CONTEXT §4 | yes | "stored **5.5 hours late** in production" [A] |
| Lane D §13 / T1 Frontier row | yes (`LANE_D_ASSET_REGISTER.md:655-676`; `T1_FRONTIER.md:305-327`) | 120 rows, `lit`; served-vs-Q2 tension |
| Elevation plan Q2 | yes (`:188`) | three assets consumed by nothing |
| KALA_DELEGATED_DECISIONS D-M | yes (`:411-417`) | second L0 lane pass |
| Blueprint §3.5 row 7 / §4 row / §9.1 D4 / §9.2 Q2 / §16.2 / SC-1 / SC-6 / SC-10 / §15 G-1 / §12 | yes (`:323,:395,:649,:667,:905,:280,:285,:289,§15 G-1,:732-775`) | as the brief says |
| Gochara plan §5.4 "ClassContext.fetch never read it"; N-8 | **no** (F-22) | 0 hits in `GOCHARA_FAMILY_ELEVATION_PLAN_v0_3.md`; N-8 only in blueprint |
| "Gochara G-9 count 15 texts / 10,651 chunks" | **no** (F-25) | G-9 at blueprint `:325` is a kota count; 10,651 is 16 texts elsewhere |
| W2 source `47131772b` | resolves, but not to this writer (F-10) | avadhi/yojaka + tests |
| `source_revision 9feac52d7` | yes | ancestor of HEAD; no diff on cited code files |
| header "blob 793972c…" | resolves as a **commit** (F-24) | — |
| DDL (not cited by line) | `supabase/migrations/531_kala_tithi_pravesha.sql:44-72` | `TIMESTAMPTZ`, natural key, CHECKs |
| `birth_params.py` (not cited) | `:33,:96-111` | naive `datetime_iso` + `tz_offset_hours` (F-08) |
| `compute.py` / `positions.py` / PyJHora (not cited) | `compute.py:30-33,58-66`; `positions.py:45-56`; `jhora/panchanga/drik.py:1773` | local wall-clock + Place(tz) → `jd_utc = jd - tz/24` (F-01) |
| `w27_annual_stack.py` / `w27b_tithi_pravesha.yaml` (not cited) | `:306-372`, `:74`; yaml whole | candidate consumer + contrary citation (F-05) |
| `migrations/670:1413-1515` (not cited) | yes | applied integrity contract (F-23) |

## 4. What I could not verify, and why

- The 5.5 h production figure, the 120-row count, `asset_throughput = lit`, whether any row is `divergent_flagged`, and the builder connection's session `TimeZone` — no DB access. No `SET TIME ZONE` / `options=-c timezone` appears in `pipeline/orchestrator/*.py` (grep), so "UTC on the builder" is carried [A] from context §4, not confirmed.
- Whether `classical_text_chunks` carries lunar-return / varṣa-praveśa doctrine, and whether any predicate was ever run against it — no DB; the brief's "never asked" is itself unreceipted (F-21).
- Whether the "ingestion work item" the docstring (`writer.py:31-35`) says is filed exists — no tracker record found in the worktree.
- `KALA_COST_PROFILE_v1_0.md` / any end-to-end timing for the 120-row build — not present on this base; the brief's cost claim is the writer's own docstring, correctly labelled.
- The N-8 ruling text and the Gochara plan §5.4 passage — not found where cited (F-22).
- The knife-edge case for F-02 (a return equidistant between two anniversaries under a 5.5 h relabel) — not computed; stated as a measure-zero caveat, not a fact.

## 5. Conformance to the binding

Fields named in the B1–B7 vocabulary and used correctly: `t_start`/`t_end` (with the declared one-generation alias to `window_start/end`), `inclusivity='closed_open'`, `time_basis='event_instant'`, `claim_grain='instant_grain'` (B1); `completeness_state` ∈ F06 six, `source_qualification='unsourced'`, `corpus_verifiable`, `comparable_with='different_convention'`, `epistemic_class='COMPUTED_FACT_CONFIGURATION'` (F04, `FOUNDATION:65`), `operator_role='computation'` (F12, `FOUNDATION:39`), `tier_basis` n/a (B2); `independence_group` (B4).

Non-conformant:
- `coverage = {years_requested, years_computed, unavailable_years[]}` — not the B5 shape; no alias map declared (F-07).
- `independence_group` entries lack `group_id` and `members[]` (F-15).
- B3 (`window_ref`, `generation`, content-addressed id) is neither offered nor declined; the brief should say `not offered` with reason (rows keyed by natural key; no cross-asset citation until the voice exists).
- B1 tz-source rule on the serving path (`new Date()` default, session-tz cast) is not named in the interface packet (F-17).
