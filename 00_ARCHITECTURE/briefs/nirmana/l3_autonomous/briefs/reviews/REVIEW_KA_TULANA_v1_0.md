---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_TULANA
version: "1.0"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_TULANA_ELEVATION_BRIEF_v1_0.md
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD bf70a3477; brief's source_revision 9feac52d7 is an ancestor and `git diff 9feac52d7 HEAD` on every cited code file is empty, so citations checked at HEAD hold at 9feac52d7"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no external service"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed as written; every file:line re-read with awk/sed/grep; prior audit artifacts (Lane D/E, T1 Spine, F2) treated as [A] and re-checked at source where they carry a code claim"
---

# Review — `KA_TULANA_ELEVATION_BRIEF_v1_0`

## 1. Verdict

**REWORK.** The recommendation's direction (three named comparators, declared incomparability, declared missingness, independence in / confidence out, IP-4 honesty) is right and mostly well-cited, but the brief's own failure evidence contains a claim the code refutes (`rank_windows` does not rank Mode C "as one scale" — it raises at `ranker.py:61-62`), its `INTEGRATE` half names a sidecar route that does not exist and is outside `may_touch`, its "no migration" claim collides with an applied, sha256-pinned probe contract that hard-codes the composite it proposes to retire, its "one-line honesty fix" is at least seven source sites plus generated projections (the one file:line range it may touch excludes the description it wants to change), its Delivery sentinel cannot fail in the way that matters, and its offered `coverage` shape is not the binding's B5 shape.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| F-01 | **MAJOR** | §3 Observed behavior: *"Feed two Mode-C windows and one Mode-A window to `rank_windows`: they are ranked on `convergence_score` as if one scale."* §7 Negative row: today's failure = *"a rank is emitted"*. | `ranker.py:61-62`: `if window.mode not in {"A", "B"}: raise ValueError(f"unknown mode: {window.mode!r}")`, called from `_validate_window_set` at `:288` before any scoring; `test_w2_first_frontier_service_contracts.py:161` pins `{"mode": "NOT_A_MODE"} → "unknown mode"`. Meanwhile `kala_convergence.mode` has held `'C'` since `supabase/migrations/360_kala_convergence_mode_c.sql:12` dropped the CHECK, `services/ka_sangam/engine.py:2115,2134` emit `'C'` and `:2201` `'D'`, and Lane E §0.1 (`:96-97`) says the top-750 is all Mode C. | The defect class is not "flattened" for cross-mode input; it is **excluded at the door**: a `kala_convergence` adapter would be refused for every Mode C/D row today. Rewrite §3's third sentence and §7 Negative's "today" column to say the kernel rejects C/D outright; the delta becomes "admit all modes, partition by `comparable_with`", and the Negative detector must distinguish `incomparable` from `ValueError`. |
| F-02 | **MAJOR** | §4 item 9 / §5 `INTEGRATE` / §10 decision 2: `kala_priority_get` should *"call the sidecar's Tulana route with `WindowInput`s"*. | No sidecar route exposes `rank_windows`/`compare`/`attention_map`: `grep -rn 'tulana\|rank_windows' platform/python-sidecar/routers/*.py` hits only `routers/nirmana_probe.py:24` (the fixed-fixture health probe, `run_health_probe` at `:111`). `service_manifest.json:664-668` records `call_priority_ranking` as *"Direct Postgres query … NOT a sidecar HTTP call"*. `platform/python-sidecar/routers/**` is not in `may_touch`. | Either add a router file to `may_touch` (and say a new FastAPI route is code the brief authorizes at stage 3) or downgrade `INTEGRATE` to "description honesty now; wrap is a separate packet once a route exists". As written the INTEGRATE disposition is unexecutable inside the declared scope. |
| F-03 | **MAJOR** | §5: *"No table, no migration"*; `RETIRE_AFTER_MIGRATION` (one generation) of the I-11 composite; `PRESERVE` *"the forensic fixture in `service_probes.py`"*. `must_not_touch` includes *"applied migrations"*. | `platform/migrations/849_nirmana_l3_w3_tulana_health_probe.sql:78-104` writes `asset_registry.health_probe` for `ka_tulana` with `forensic_expected_composite_a: 0.795`, `_b: 0.4234`, `forensic_expected_decisive_factor: "proximity_factor"` — the composite and the proximity term are the contract. `routers/nirmana_probe.py:63-69,106-107` rejects any request whose `probe_contract_sha256` ≠ sha256(JCS(health_probe)); the same JSON is mirrored in `scripts/nirmana_probe_contracts.json` ("ka_tulana"). `service_probes.py:804-811,847-848` assert those exact numbers. | Retiring the composite (or making proximity non-decisive) requires a **new migration** re-pinning `health_probe`, a change to `nirmana_probe_contracts.json`, and a probe rewrite — add them to `may_touch`, drop "no migration", and state that `legacy_composite` must keep producing 0.795/0.4234 for the fixture until that migration lands. The `PRESERVE` of the fixture and the `RETIRE` of the composite are the same object today; the brief must say which wins in the interim. |
| F-04 | **MAJOR** | §4 item 9 / §8 / §10 decision 2: the description fix is *"one line"*; `may_touch` limits the wrapper to `call_service_wrappers.ts:660-720`. | The wrap claim lives at: `call_service_wrappers.ts:10` and `:524-535` (description, **outside 660-720**; the SQL the brief cites runs `:672-730`, the descriptor is `:518-833`); `priority.ts:8-10` (header) and `:329-331` (tool description); `register_p1_aliases.ts:1017,1022-1024`; `authority_basis_census_seed.ts:147-151`; `producer_editorial_review.ts:384-387`; `service_manifest.json:664-668`; `L3_kala/index.ts:27`; plus regenerated `platform/src/generated/projections/*.generated.json` (5 files), `capability_knowledge.snapshot.json`, `capability_estate_census.json`, `platform-mcp/src/generated/mcp_surface_profiles.generated.ts`. | Replace "one line" with the enumerated blast radius; widen the interface packet to name `register_p1_aliases.ts`, `authority_basis_census_seed.ts`, `producer_editorial_review.ts`, `service_manifest.json`, `index.ts` and the regeneration step; fix the `may_touch` range to `:518-535` (description) or the whole descriptor. |
| F-05 | **MAJOR** | §7 Delivery (IP-4): sentinel `criterion='strongest'` *"from the service"* reaching the envelope; detector *"absent → the surface did not call the service"*. | `criterion` is a proposed top-level, constant-valued field; nothing in the row makes it service-computed. A TS wrapper that never calls Python can emit `criterion: 'strongest'` literally. Contract §6 Delivery (`…CONTRACT_v1_0.md:112`) and binding B7 require *"a decisive **non-default** sentinel"* in a *"low-ranked, non-default field"*. | Make the sentinel a value only the kernel can produce from a fixture the wrapper does not know — e.g. a `decisive_factor`/`tie_on` on a low-ranked row of a two-window fixture whose ordering flips under `strongest` vs `legacy_composite` — and pair it with the F28 ablation already in §4 item 12 (remove the call → served order changes). |
| F-06 | **MAJOR** | §7 binding line / §4 item 8: *"OFFERS … B5 (`coverage`)"* with `coverage = {candidates_supplied, candidates_ranked, excluded: [{window_ref, reason}]}`. | `KALA_SYNERGY_BINDING_v1_0.md` §B5 fixes `coverage` to `{requested_horizon, completed_horizon, resolution, partitions_searched[], exclusions[], unsearched_regions[], completion_detector}` and says a stream that *"emits a differently-named field for the same concept is non-conformant until it renames or aliases with a declared mapping"*. | Rename to the B5 keys (`exclusions[]` not `excluded`; add `requested_horizon`/`completed_horizon` = the candidate set supplied, `completion_detector` = the validator that refused rows) or declare an explicit alias map in §7. |
| F-07 | **MAJOR** | Header `accepted_upstream_contract`: *"Saṅgam brief v1.5 / plan v1.0 (typed testimony: independence group, comparability, R-6 four components) on sangam/stage3 — DEMANDED"*; §4 item 6 adapter reads `activity`, `independence_group`, `comparable_with`, `generation`, `window_ref` *"from real `kala_convergence` columns"*. | `git show sangam/stage3:…/writers/ka_sangam.py` INSERT column list (`:990-999`) is byte-identical to HEAD — no `independence_group(s)`, `comparable_with`, `window_ref`, `activity`, `generation` column is persisted on any branch; `services/ka_sangam/exposure.py:65-82` on stage3 still uses the pre-binding spelling `comparability_class`; the Saṅgam brief itself spells the field `independence_groups` (`SANGAM_ELEVATION_BRIEF_v1_0.md:184`, plural). `9feac52d7` is not an ancestor of `sangam/stage3` (merge-base `39af7d29f`). No `kala_convergence` DDL change exists on stage3 (`git diff 9feac52d7 sangam/stage3 -- platform/migrations` empty). | State plainly: *"no branch persists any DEMANDED column today; every §7 row that consumes one runs on synthetic `WindowInput`s only until Saṅgam's merge gate lands the columns"*, and declare the `independence_groups`→`independence_group` / `comparability_class`→`comparable_with` mapping the adapter will apply. |
| F-08 | MINOR | Header: *"W2 service payload shape frozen (47131772b: Tulana rejects non-finite, duplicate or unqualified inputs)"*. | `git show --stat 47131772b` touches `ka_avadhi.py`, `ka_yojaka.py` and two test files — not Tulana. `git log -S'_validate_window' -- ranker.py` → `fa9857f00 feat(data-plane): deliver governed L0-L3 source execution (#2607)`. | Cite `fa9857f00` (#2607). |
| F-09 | MINOR | §0/§2.2: *"a confidence default of 0.2 when the input is missing (`:192-194,:222`)"*; §1 (Lane E row, carried [V]): *"`rarity_norm=0.5`, `confidence=0.2` on every row while the rationale string reports them as measured"*. | `:222` `.get(w.confidence_label, 0.2)` is unreachable: `:80` raises on any label outside `_CONFIDENCE_NUMERIC` before `_composite` runs. The mechanism for "missing confidence" is the dataclass default `confidence_label: str = 'speculative'` at `:135` (→ 0.2), and the rationale then prints `(speculative, weight 20%)` — a label, not an invention. For rarity, `:245` prints `~unknownyr cycle` when `rarity_years` is None, so the rationale does disclose absence while the composite still uses 0.5. | Cite `:135` as the confidence default; drop "reports them as measured" — say "the composite uses 0.5/0.2 while the rationale discloses `unknown`/`speculative`". The invented-neutral defect stands; the narration claim overreaches. |
| F-10 | MINOR | §2.3 Live-path statement: *"No live path reaches the kernel."* | Two do, with synthetic inputs: `services/ka_tulana/writer.py:55` runs `rank_windows` on every orchestrator build (raises at `:119` on failure), and the deployed authenticated route `routers/nirmana_probe.py:24,111` → `service_probes.py:797,844`. The brief's own §0 acknowledges the probe. | "No live path carries chart-derived inputs to the kernel; two live paths (build self-test, `/nirmana_probe`) exercise it on fixed fixtures." Add `routers/nirmana_probe.py` to the §2.3 consumer table. |
| F-11 | MINOR | §2.3: *"`WindowInput` requires `rarity_years` and `confidence_label`"*. | `ranker.py:129-143`: required positionals are `window_id`, `mode`, `peak_date`; `rarity_years: Optional[float] = None` (`:136`); `confidence_label` defaults `'speculative'` (`:135`). | "`WindowInput` accepts `rarity_years`/`confidence_label`; `kala_activation` carries neither (DDL `supabase/migrations/246_l3_ka_kalasutra.sql:3-33`: `orb_strength`, `convergence_score`, dates only), so a wrap over it would run on the invented defaults." |
| F-12 | MINOR | §2.4 / §5: `CompareVerdict.decisive_factor` is *"the genuinely valuable part"* — `PRESERVE`. | `:328-334`: `decisive` = argmax of **unweighted** per-factor deltas. Rarity Δ0.5 × 0.25 = 0.125 loses to convergence Δ0.4 × 0.40 = 0.16 in the composite, yet `decisive_factor` names rarity. The explanation can name a factor that did not decide. The applied probe (`849:…decisive_factor: "proximity_factor"`) pins this behaviour. | Preserve the *concept* (named decisive factor), not the current computation; under `strongest` define `decisive_factor` as the first key in the declared lexicographic order that differs, which is both explainable and weight-free. |
| F-13 | MINOR | §7 has 10 rows; instantiation guide §7 requires *"all eleven contract §6 rows"* and each with *"fixture/data boundary, command, expected result, invariant, detector, evidence path"*. | `…CONTRACT_v1_0.md:105-115` lists eleven; the brief omits **Evaluation** (`:115`, "only if separately governed") and has no `command` or `evidence path` column. | Add the Evaluation row as `not_applicable` with reason (no governed outcome set for a pure comparator), and add command + evidence-path columns. |
| F-14 | MINOR | Template v2.0 §2.5 consumer walkthrough (*"one Product §9 experience end to end … include the ordinary-period case"*) — absent; no ordinary-period fixture in §7. | `KALA_ASSET_ELEVATION_PLAN_TEMPLATE_v2_0.md` §2.5; every §7 fixture is dramatic (2019 vs 2027, Mode A vs C). | Add a §9-experience-5 (choice comparison) walkthrough and one ordinary-period fixture (two unremarkable Mode A windows, equal activity → `tie`). |
| F-15 | MINOR | §4 item 5 / §7 Negative: *"`incomparable` iff `comparable_with` differs"*. | Binding B2 defines `comparable_with` as a **relational** enum `{self, same_convention_same_inputs, same_convention_newer_inputs, different_convention}` — a value relative to a reference, not a partition label. "Differs" between two rows is undefined without saying what each is relative to. | Define the partition key explicitly (e.g. the pair is comparable iff each is `same_convention_same_inputs` relative to the other's `{mode, kernel_version, generation}`) and state how `mode` maps onto it. |
| F-16 | NOTE | §2.3: *"(`priority.ts:330-331`, description `:525` [A: Lane E])"*; §11 item 1. | `priority.ts:330-331` resolves (tool description, "Wraps the same ka_tulana priority-ranking service"). `priority.ts:525` is the file's closing `}`; the description at `:525` is `call_service_wrappers.ts:525`. Both resolve; the citation is ambiguous. | Write `call_service_wrappers.ts:525` and mark both [V] — they were verifiable in one `grep`. |
| F-17 | NOTE | §2.5: *"t3: a t2 `asset_frozen` exists and is inadmissible"* — no source given. | Repo-wide, the only record is the F2 audit tally `audit/_work/F2.md:126` (`ka_tulana | 1 | t2 | yes (t2)`) and `:140`, a 2026-09-22 ledger measurement [A]. `EVENTS.jsonl` carries no freeze for `ka_tulana`. | Cite `F2.md:126` and label [A]. |
| F-18 | NOTE | Line ranges: `ranker.py:38-45` (weights), `:307-389` (compare), `:390-405` (attention_map); `call_service_wrappers.ts:670-712` (SQL). | Weights `:38-43`; `compare` `:307-388`; `attention_map` `:390-428` (`:405` is mid-validation); SQL `:672-730`, JOIN/WHERE `:707-714`. | Tighten; none changes meaning. |
| F-19 | NOTE | §2.2: *"a confidence-label comparison (`:365-366`)"*. | `:365-366` assign `win_label`/`lose_label` and never use them — dead code, not a comparison. `CompareVerdict.confidence_label` is set from `winner.confidence_label` at `:384`. | Cite `:384` for the label on the verdict; note `:365-366` as dead code the amendment-9 migration deletes. |
| F-20 | NOTE | §7 Context row and §4 item 4 change behaviour that existing tests pin; `may_touch` includes the tests but the brief does not name them. | `tests/l3/test_ka_tulana.py:212` asserts `_normalise_rarity(None) == 0.5`; `:114-133` pin `confidence_label` semantics; `test_w2_first_frontier_service_contracts.py:160` pins `"unknown confidence_label"`, `:184-200` pin the composite values; `test_service_probes.py`, `test_migration_849_tulana_health_probe.py`, `test_nirmana_probe_route.py` pin the probe contract. | List them under §5 as tests that must flip, so a reviewer at stage 3 can tell an intended inversion from a regression. |
| F-21 | NOTE | §10 decision 3: drop the unread `ka_vighnakara`/`ka_kala_darshana` edges *"by registry packet"*. | `platform/scripts/seed/asset_registry_seed.ts:2438` holds `depends_on`; the seed file is not in `may_touch`; DAG edge guards (`dag_edge_guard.py`, migration 224 cycle per `9feac52d7`'s own message) constrain edge edits. | Say the registry packet is out of this brief's scope and names its own owner. |
| F-22 | NOTE | Header `parent_layer_contract: "… / blob 793972c754b…"`. | `git cat-file -t 793972c754b…` → `commit`, not `blob`. | Write "commit". |

Not findings, confirmed at source: the composite/weights/floor/`date.today()` defaults (`:38-43,:49,:80,:192-194,:207-208,:225-230,:281,:287`); the wrapper is inline SQL with no sidecar call and emits `orb_strength_available` (`:703-706`); the 99.6% NULL figure is the code comment at `:689` [A]; the writer shim (`writers/ka_tulana.py:1-19`) and self-test writer conform to §N.2; registration (`asset_registry_seed.ts:2427-2441`) matches §2.1 verbatim; the caller grep (all file types, `platform/` + `platform-mcp/`, excluding `services/ka_tulana/` and tests) finds exactly the probe, the writer shim, and prose/registry references — no ranking caller; Register `:157`, Strategy `:63,:66,:97,:274,:444`, Product `:270`, Blueprint `:222,:320,:349-352,:671,:758,:902`, Amendments `:32`, Elevation plan `:193`, W0 register `#19` (`:38,:648-654`), Lane D §14 (`:692-710`), Lane E §3.4 (`:689-725`) and §0.1 (`:87,:96-97`), T1 Spine §9 (`:389-393`) all say what the brief says they say. `estimated_seconds` appears only as the reported seed value (`null`), not as a cost basis. No question outside L3-Q01–Q13 is adopted; no confidence scalar is invented (`tier_basis='relative_uncalibrated'` extended); no `kala_views` edit beyond an interface packet; no orchestrator change; the simpler baseline (I-11 composite; inline SQL) is the actual current behaviour; the date-swap ablation under `strongest` genuinely distinguishes the elevation from plumbing.

## 3. Citations verified

| citation (as in brief) | resolved? | what is there |
|---|---|---|
| `ranker.py:268,307,390` | yes | `def rank_windows` / `def compare` / `def attention_map` |
| `ranker.py:38-45` | yes (38-43) | `I11_WEIGHTS` |
| `ranker.py:49` | yes | `_CONFIDENCE_NUMERIC` |
| `ranker.py:80` | yes | `raise ValueError(f"unknown confidence_label…")` |
| `ranker.py:166` | yes | `class CompareVerdict` |
| `ranker.py:192-194` | yes | `None → 0.5` |
| `ranker.py:198-215` / `:207-208` | yes | `_proximity_factor`; `days < 0: return 0.05  # past window` |
| `ranker.py:222-229` | yes (222-230) | `.get(label, 0.2)`; weighted sum |
| `ranker.py:247` | yes | `proximity=… weight 15%` rationale part |
| `ranker.py:268-281` / `:281` | yes | signature; docstring "Defaults to date.today()" (actual default `:287`) |
| `ranker.py:307-389` | yes (307-388) | `compare` |
| `ranker.py:332` | yes | `'proximity_factor': abs(…)` in `factor_deltas` |
| `ranker.py:365-366` | yes, **but dead code** (F-19) | unused `win_label`/`lose_label` |
| `ranker.py:390-405` | yes (390-428) | `attention_map` |
| `ranker.py:61-62` (not cited by brief; F-01) | — | `mode not in {"A","B"} → ValueError` |
| `service_probes.py:655-846` / `:659-663` / `:783-843` | yes | probe block; "no architecture question, unlike `ka_dasha_kala`" at `:663`; imports + `rank_windows` + `compare` |
| `call_service_wrappers.ts:660-712` / `:670-712` | partially | `:660-661` = `top_k` param push; SQL `:672-730`; JOIN `kala_activation` `:708`; WHERE `:711-714` |
| `call_service_wrappers.ts:688-697` | yes (687-702) | comment: NULL on 669,964/672,551; DROPPED convention |
| `call_service_wrappers.ts:525` | yes | "(ka_tulana service)" in description |
| `priority.ts:330-331` | yes | "Wraps the same ka_tulana priority-ranking service" |
| `priority.ts:525` | resolves to `}` (F-16) | — |
| `asset_registry_seed.ts:2432-2440` | yes (2427-2441) | `storage_type:'service'`, `depends_on`, `estimated_seconds: null`, `asset_kind:'service'` |
| `writers/ka_tulana.py:1-19` | yes | shim, `WriterResult(rows_inserted=0)` per docstring |
| `services/ka_tulana/writer.py` | yes | self-test `:25-62`; raises `:119` |
| `REGISTER:157` | yes | verbatim |
| Strategy L3-Q02 / Q05 / A04 / U04 / §3 Comparison-election | yes | `:63`, `:66`, `:274`, `:444`, `:97` |
| Product §7.1 | yes | `:270` "'Strongest' is not an unexplained scalar" |
| Blueprint §3.5 row 4 / reading 1 / §9.2 Q7 / §12.2 IP-4 / §16.2 | yes | `:320`, `:343-352`, `:671`, `:758`, `:902` |
| Amendments Saṅgam item 9 | yes | `KALA_SYNERGY_AMENDMENTS_v1_0.md:32` |
| Elevation plan Q7 | yes | `MADHAV_L3_ASSET_ELEVATION_PLAN_v1_0.md:193` |
| W0 register #19 | yes | `…W0_FIELD_CONTRACT_REGISTER_v1_0.md:38,:648-654` |
| Lane D §14 / T1 Spine 9 / Lane E §3.4 / §0.1 | yes | `LANE_D_ASSET_REGISTER.md:692-710`; `T1_SPINE.md:389-393`; `LANE_E_LAYER_VALUE_MODEL.md:689-725`, `:87,:96-97` |
| `source_revision 9feac52d7` | yes | ancestor of HEAD; zero diff on cited code files |
| `47131772b` | resolves, **wrong file set** (F-08) | `ka_avadhi.py`, `ka_yojaka.py`, two tests |
| `793972c754…` | resolves as a commit (F-22) | — |
| `sangam/stage3` | exists locally and on origin | writer INSERT identical to HEAD (F-07) |

## 4. What I could not verify

- **Any live database figure**: `kala_convergence` = 0 rows for chart `482012f1` (Lane E, 2026-09-22 [A]; consistent with applied migration `981:25`, which live-counted 17,957 + 2,540 rows for two other charts and none for the canonical one); `orb_strength` NULL on 669,964/672,551 (code comment [A]); Mode A/B ≤ 0.3805 / C ≥ 0.70 (Lane E §0.1 [A]). No DB access; not queried.
- **The t2 `asset_frozen` ledger row** for `ka_tulana`: only the F2 audit tally exists in the repo (`F2.md:126,140`); the ledger itself is a database.
- **Whether `origin/sangam/stage3` has moved** past the local `sangam/stage3` tip (`90435d96a`); not fetched.
- **Whether any non-Git caller** (deployed notebooks, cron, external MCP clients) invokes the kernel; search scope was the worktree (`platform/`, `platform-mcp/`, all file types).
- **Test outcomes**: no test suite was run (read-only mandate); assertions above are from reading the test source.

## 5. Conformance to the binding (B1–B7)

Conformant names used: `t_start`, `t_end`, `t_peak` (B1); `inclusivity` value `closed_open` (B1); `reference_instant` in place of `date.today()` (B1 tz rule); `epistemic_class='INTERPRETIVE_INFERENCE'` (F04 spelling at Foundation `:66`), `completeness_state='unavailable'` (F06), `operator_role ∈ {relevance_navigation, computation}` (F12 spelling at Foundation `:39`), `comparable_with`, `tier_basis='relative_uncalibrated'` (B2); `window_ref`, `generation` (B3); `independence_group` (B4).

Non-conformant or undeclared:

1. **`coverage` shape (B5)** — brief keys `candidates_supplied / candidates_ranked / excluded[]`; binding keys `requested_horizon / completed_horizon / resolution / partitions_searched[] / exclusions[] / unsearched_regions[] / completion_detector`. No alias map declared. (F-06)
2. **`comparable_with` used as a partition class**, whereas B2 defines a relational enum; the reference is undeclared. (F-15)
3. **Producer spelling mismatch undeclared** — the DEMANDED source (Saṅgam brief `:184`) spells `independence_groups`; stage3 code emits neither spelling and still uses `comparability_class`. The brief must declare the mapping it will apply. (F-07)
4. **Fields outside B1–B7, introduced by this brief and not flagged as such**: `criterion`, `decisive_factor` (existing), `tie_on`, `verdict`, `factors_available`, `legacy_composite`, `strength` (reported beside `nearest`), `candidates_supplied`, `candidates_ranked`. Allowed for a per-asset payload, but §7's binding line should list them as asset-local so the census does not read them as binding vocabulary.
5. **B7 sentinel discipline** — the Delivery row's `criterion='strongest'` is not a "decisive non-default sentinel in a low-ranked, non-default field". (F-05)

*End of REVIEW_KA_TULANA_v1_0.*
