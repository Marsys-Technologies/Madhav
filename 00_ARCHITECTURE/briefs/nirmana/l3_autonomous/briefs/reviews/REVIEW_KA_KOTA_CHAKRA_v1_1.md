---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_KOTA_CHAKRA
version: "1.1"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_KOTA_CHAKRA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.1", source_revision 9feac52d7)
prior_review: REVIEW_KA_KOTA_CHAKRA_v1_0.md (ACCEPT_WITH_CORRECTIONS, 21 findings)
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD e82dd34a3; `git diff --stat 9feac52d7 HEAD -- platform/ platform-mcp/` is empty, so every code citation below is against the brief's own base"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no network, no git write"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md as written; every v1.0 finding re-checked in the v1.1 text AND at source (sed -n / grep -n / cat -n), never from §12; every new v1.1 claim re-read at source; the seven caller-flagged axes (stateless coverage, successor (a), edge tolerance, resolver claim, janma ORDER BY, closed vocabularies, zone proof row) checked at source"
---

# Re-review — `KA_KOTA_CHAKRA_ELEVATION_BRIEF` v1.1

## 1. Verdict

**REWORK** (scoped to §4.2, §4.3, §4.4, §4.6, §4.8, §1/§4.9's w43/w44 claim, and §10 decisions 1, 4, 5 —
the disposition `QUALIFY_LIMIT` + `ENRICH_CORRECT` and the four stamps stand). Grounds: v1.1 resolves
14 of 21 v1.0 findings cleanly, but the mechanisms it now specifies for the native to rule on do not do
what the brief says they do — the stateless `unsearched_regions` cannot resolve a stale `window_ref`
(the ref carries no date and the `generation` carries no horizon), the successor conjunct (a) passes
vacuously on any row whose `ring_table_version` is NULL or unknown, `stale_partition` has no registry
slot to live in, the `window_edge_tolerance_days` rule stamps `0` where the shift is provably non-zero
and the per-day alternative is mis-costed nine-fold, the B1 mapping points at a resolver function that
does not exist, `as_of` "from `ctx.config`" needs a config key the orchestrator never populates, and one
[R]-adopted claim hardened in v1.1 ("neither file exists") is false at source. Three of the five §10
decisions cannot be ruled on as posed.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| N-01 | **MAJOR** | §1: `mechanism_register.yaml:145` … *"evidence link to `w43_ablation_runner.py` and `w44_weight_fitting.py` — **neither file exists**"*; §4.9: *"evidence files that do not exist"*; changelog same; §1 cites `grammar_v3_registry.yaml:148`. | Both files exist: `platform/python-sidecar/scripts/kala_admission/w43_ablation_runner.py` (commit 6c4c9d6ef, PR #1176) and `…/w44_weight_fitting.py`. `w44_weight_fitting.py:66-71` lists `"w25_kota_chakra"` in `ADMITTED_MECHANISM_IDS` ("10 with weights; 2 structural-only"); `:163` `MECHANISM_ENGINE_WIRED["w25_kota_chakra"] = False`. `w43_ablation_runner.py:69,:112` loads `grammar_v3_registry.yaml` (which says `candidate`), and its own docstring `:6-30` says it is a *"STRUCTURAL PROXY approach, not a full per-toggle re-run"*. `grammar_v3_registry.yaml`: w25 block starts `:129`, `admission_state: candidate` at **`:150`**, not `:148`. No Python reader of `mechanism_register.yaml` found (grep over `platform/python-sidecar`). v1.0 F-06 said "neither file exists **in `gochara_v3`**" — v1.1 dropped the qualifier and made the claim false. | Restate: the files exist under `scripts/kala_admission/`; the disagreement is 2-vs-2 (`mechanism_register.yaml:145` + `w44:71` say admitted; `w25_kota_chakra.yaml:13` + `grammar_v3_registry.yaml:150` say candidate); and the ablation w43 performs is a structural proxy by its own admission, so it is **not** the per-toggle ablation N-8 requires. Fix the line number. |
| N-02 | **MAJOR** | §4.8, §7 Binding: *"`t_start/t_end` are derived views at the chart's tz-aware midnight via `services/ka_temporal/date_resolver` (B1's 'resolver' row)"*; §6 lens A/B silent on it. | `services/ka_temporal/date_resolver.py` defines `normalize_graha`, `extract_lords_from_text`, `lord_from_house_varga`, `sign_lord`, `extract_lords_from_config`, `DashaPeriod`, `ActivationWindows`, `resolve_birth_date`, `load_dasha_timeline`, `resolve_activation_windows`, `_peak_cluster_dates`, `_dasha_period_dates`, `_proximity_score` — a daśā-predicate→date resolver. `grep -n "tz\|zone\|instant\|utc"` over the file returns nothing; `resolve_birth_date` (`:295-322`) returns a naive `date`. No SQL view exists. The blueprint's SC-1 packet (`KALA_ELEVATION_BLUEPRINT_v1_0.md:882`) plans `date_resolver.py:325-390, :404-405, :417/:500` changes — none is a date↔instant conversion. `services/ka_qualification/` (SC-2/3/5 shared modules, blueprint `:883-886`) is absent. `ka_temporal` is not in `may_touch`. | Do not claim a derived view exists. State: "`t_start/t_end` are **not served** until the SC-1 packet lands a date↔instant function in `ka_temporal`; until then the DATE columns are the declared alias and `time_basis='noon_ut_knot'` is the only instant claim." Name the dependency in §5 fences and §11. |
| N-03 | **MAJOR** | §4.2: coverage is *"stateless — `unsearched_regions = [ <requested_horizon.start, >requested_horizon.end ]`, which names any dropped span … A consumer resolves a stale `window_ref` by comparing its date against the current generation's coverage"*; §4.10 Negative: *"coverage `exclusions` recording it"*; §7 Context: *"new generation; coverage `unsearched_regions` names the spans outside the new horizon"*; §10.1. | (a) **Semantics.** Strategy §3 *Search coverage* (`STRATEGY:97`): *"failed/unsearched regions … separates absence **within scope** from unavailable or unexplored"*; SC-5 (`BLUEPRINT:284`) the same. `[<start, >end]` is the set-complement of `requested_horizon` — it is derivable from `requested_horizon` on the same row and carries zero information; nothing in-tree defines `unsearched_regions` at all (grep over `platform`, `platform-mcp`: no hits; `kala_gochara_coverage` named at `BLUEPRINT:742` has no `CREATE TABLE`). (b) **Resolution.** §4.3 defines `window_ref = {asset_id, generation: ring_table_version + FORMULA_VERSION, id: sha256(…)}`. The id is a hash — no date is recoverable from the ref; the generation string is **identical** across rebuilds that only move the horizon. A consumer holding a ref from build D therefore has exactly what §3 describes today: *"no row and no reason"*. "Comparing its date" presumes the consumer stored `window_start` beside the ref, which B3 does not require. (c) **Refusal.** `writer.py:252-261` writes nothing and preserves the prior partition; under "coverage on every row" the refused build's `exclusions` have **no home**, and the surviving rows carry the *previous* build's coverage — B5's *"a row and coverage, never 'no row'"* (`BINDING:70`) is unmet. (d) §7 Context says "new generation" while §4.3's generation is unchanged. | Put the horizon in the generation: `generation = ring_table_version + '|' + FORMULA_VERSION + '|' + requested_horizon_start + '..' + requested_horizon_end` — then a stale ref names its own horizon and a consumer compares that to the current rows' `requested_horizon` (both are data the consumer already holds). Keep `unsearched_regions` for sub-spans **inside** the requested horizon (today: empty on success; the refused grahas' full span on refusal). Give coverage a home that survives a refusal: one `kala_kota_chakra_coverage` row per `(chart_id, generation)` in `may_touch`, written on **both** paths. Re-pose §10.1 with these consequences. |
| N-04 | **MAJOR** | §4.3: successor (a) *"joins `r.table_version = k.ring_table_version`, so it measures 'is this row's ring what the partition it names says'"*; §10.4 *"without the pin, (a) cannot be written correctly"*. | Today's (a) is `NOT EXISTS (SELECT 1 FROM kala_kota_chakra k JOIN bg_kota_chakra_rings r ON r.ring_position = k.count_from_janma WHERE r.ring_name IS DISTINCT FROM k.kota_ring)` (`670:894-899`). Adding `AND r.table_version = k.ring_table_version` to an **inner** join makes every row whose `ring_table_version` is NULL (all existing rows after the additive DDL) or names a version absent from `bg_kota_chakra_rings` drop out of the join — the contract goes **TRUE vacuously**. The brief says nothing about backfill or NULL semantics; the §7 Positive row cannot catch this (its fixture rows are all stamped). | Specify: `LEFT JOIN … WHERE r.ring_position IS NULL OR r.ring_name IS DISTINCT FROM k.kota_ring`, **or** add `FOREIGN KEY (ring_table_version, count_from_janma) REFERENCES bg_kota_chakra_rings (table_version, ring_position)` (the composite PK exists, `523:67`) with `ring_table_version NOT NULL` — and state that the contract is expected FALSE for every chart between the DDL and its rebuild (no backfill is honest: nothing records which version graded existing rows). Add a proof row: one row with `ring_table_version=NULL` → contract FALSE. |
| N-05 | **MAJOR** | §4.3, §5, §10.4: *"a separate **`stale_partition`** signal (`k.ring_table_version <> MAX`) marks rows graded under a superseded version without failing the contract"*; frontmatter: *"one NEW registry migration: … plus a stale_partition signal"*. | `asset_registry` columns: `167_asset_registry.sql:7-40` (`count_sql`, `size_sql`, `target_floor`, `expected_volume_*`, …) + `integrity_check_sql text` + `health_probe jsonb` (`202:17`, services) + writer flags/timeouts. There is exactly **one** SQL slot and it yields one boolean `integrity_passed` (`670:891-916`). No column exists for a second, non-failing signal (`grep -rhoi "ADD COLUMN IF NOT EXISTS [a-z_]*_sql"` → only `integrity_check_sql`). Storing it per row is wrong — `MAX(table_version)` moves after the row is written. | Pick and say: (i) fold staleness into the served envelope as a computed field (`stale_partition := ring_table_version <> (SELECT MAX…)`) — an L3-U04 packet item; or (ii) an `asset_registry` schema change (a new `advisory_check_sql`) — outside `may_touch`, native decision. §10.4 as posed ("yes") is not implementable as a "registry successor". |
| N-06 | **MAJOR** | §4.6: *"for a body moving < ~0.05°/day … a 0.015° error at a nakṣatra edge can move `window_start`/`window_end` by **one calendar day**. So either compute the offset per day (… ~9 × 460 swisseph calls …), or stamp `window_edge_tolerance_days = 1` for slow bodies and `0` otherwise"*; §6 lens G *"~4,140 swisseph calls"*; §10.5 *"stamp now"*; §7 Boundary detector *"no tolerance stamp"*. | (i) At date grain a crossing moves by one knot whenever the crossing-day knot lies within ε of the edge — for **any** body, with probability ≈ ε / daily motion: Sun ≈ 0.015/0.986 ≈ 1.5 %, Moon ≈ 0.1 %, Jupiter ≈ 18 %, nodes ≈ 28 %, Saturn ≈ 45 %. `0 otherwise` is a precision claim with no detector (F28 `FOUNDATION:55`; §N.8). (ii) A mean-motion threshold ignores stations: Mercury, Venus, Mars, Jupiter, Saturn all reach |speed| → 0, where ε = 0.015° shifts an edge by ε/|speed| days (3 days at 0.005°/d); `1` is not a bound for them. (iii) A real detector is cheap: `ephemeris_daily.speed_dps` is stored per knot (`platform/migrations/ws2_l0_ephemeris.sql` DDL; `l0_ephemeris.py:298,306`), so `edge_shift_days = ceil(ε(d) / |speed_dps(d)|)` at each run boundary is one division per row. (iv) The per-day cost is **461 calls, not 4,140**: `derive_sidereal` is `set_sid_mode` + `get_ayanamsa_ut(jd)` (`l0_ephemeris.py:208-209`) — the offset depends on the date only, never on the body; the writer's "9 × 460" (`writer.py:31-32`) assumed per-(body, day) and the brief adopted it uncritically. | Re-cost §10.5: per-day offset = 461 `get_ayanamsa_ut` calls (serialized under `@serialized_swiss_state`) and it **eliminates** the defect class; if the stamp is kept, it must be per-row and computed from `speed_dps` (unbounded → `NULL` with `completeness_state='unqualified'` for that edge), never a per-body constant. Make the Boundary row's detector compare per-day-offset edges to single-offset edges on a Saturn station fixture, not "stamp present". |
| N-07 | **MAJOR** | §4.2: *"`as_of` from `ctx.config` (chart-zone today by default, echoed) — never `date.today()` read inside the writer"*; §5 rollback: *"the writer reads `as_of` with a config default"*; §5 `ENRICH_CORRECT`. | `asset_runner.py:985`: `config={'chart_id': chart_id, 'birth_params': birth_params}` — the only keys ever populated; `ContextSpec.config` (`writers/__init__.py:27`) is a plain dict. An `as_of` key therefore either (a) is populated by the orchestrator — a change to the FROZEN driver (§N.2: STOP and raise; guide §11) — or (b) defaults inside the writer to a `now()` read, which B1's tz-source row forbids (*"never `datetime.now()`; no `date.today()` anywhere"*, `BINDING:33`). The brief does not say which. `birth_params` does carry `tz_offset_hours` (`birth_params.py:106-111`) — B1's "birth instant's offset" — so a chart-zone default is computable, but only as a clock read. | State the honest form: `as_of = ctx.config.get('as_of')`, else `datetime.now(timezone(timedelta(hours=tz_offset_hours))).date()` stamped `as_of_source ∈ {declared, build_clock}` on every row (the clock read disclosed, not hidden), and raise "a new optional `config['as_of']` key populated by the build entry point" as the one orchestrator-adjacent item for the native — a config-dict key, not a `WriterBase` change, but it touches `asset_runner.py`, which is not in `may_touch`. |
| N-08 | **MAJOR** | §2.4 (via §7 Binding): *"`operator_role='testimony'`"*; §9: *"testimony voice only"*. | F12 (`FOUNDATION:39`): the operator vocabulary is `computation, applicability, counterevidence, uncertainty, interpretation, exclusion, relevance_navigation, evaluation` — **eight values, no `testimony`**. B2 (`BINDING:42`) binds `operator_role` "as the Foundation defines". "Testimony" is Strategy §3's *object* name and the blueprint's prose (`:667`, `:907`), not an F12 role. v1.0 §5 passed this as "conformant names" — it is not. | `operator_role='computation'` for rings/windows (they compute geometry), `'interpretation'` for `posture`/`severity`; keep "testimony voice" as prose. |
| N-09 | **MAJOR** | §4.4: *"`source_qualification`: `kota_ring → 'verse_cited'` carrying L0's tier-iii citation"*; `completeness_state` named in §7 OFFERS but assigned **nowhere** (grep: appears only in the frontmatter globs and the OFFERS list). | The L0 citation text itself: *"tier-(iii) secondary-source transcription … **NOT YET traced to a primary ingested classical text**"* (`l0_kota_chakra_rings.py:76-80`); `corpus_status='not_in_corpus'` (`523:64`). B2's vocabulary is `{verse_cited, algorithmic_approximation, unsourced}` (`BINDING:45`). SC-7 (`BLUEPRINT:286`): *"consumers admit uniformly: `completeness_state='applied'` iff `corpus_verifiable=true` ∧ geometry passes, else `unqualified`"* — so under the brief's own derived `corpus_verifiable=false`, **every** kota row is `unqualified` to every consumer, which the brief never states. | Either stamp `kota_ring → 'unsourced'` (no verse is cited; a tier-iii transcription is not a verse) or make the tier-iii = `verse_cited` argument explicitly to the native. Assign `completeness_state` on the producer row (`applied` on the geometry; `unavailable` on the refusal path) and state the SC-7 consequence in §9's non-claims. |
| N-10 | MINOR | §4.3: natural key stays `(chart, ayanamsha, graha, window_start)`; widening is needed "only under retain". | B3 (`BINDING:54`): `generation` *"on every row; **part of the PK**"*. Under replace it never collides, so adding it to the UNIQUE is free and required. | Widen `kala_kota_chakra_natural_key` to include `generation` in the additive DDL. |
| N-11 | MINOR | §5 / §7 janma row: *"a total `ORDER BY`/`LIMIT 1` on the janma-Moon fetch"* — column unnamed; v1.0 proposed `build_id DESC`. | `chart_facts.build_id` is `UUID` (`204_chart_facts.sql:14`) — `ORDER BY build_id DESC` is not chronological. `computed_at TIMESTAMPTZ NOT NULL` exists (`204:28`); `fact_id TEXT PRIMARY KEY` (`204:11`) is a total tiebreak. | `ORDER BY computed_at DESC, fact_id LIMIT 1`. |
| N-12 | MINOR | §4.3: `window_id = sha256(chart, ayanamsha, graha, window_start, FORMULA_VERSION, ring_table_version)` as a stable id. | For a `start_truncated` run `window_start == horizon_start` (`logic.py:166`), which moves with every `as_of` — the **same physical run** gets a new `window_id` on each rebuild even when it lies wholly inside the new horizon; a ref to it never resolves. | State that truncated rows' ids are horizon-bound, or hash truncated rows on `(graha, nakshatra_idx, ring_table_version, FORMULA_VERSION, requested_horizon_start)` and declare them non-portable across horizons. |
| N-13 | MINOR | §9 walkthrough: Saturn in `prakara` → *"`posture='attacking'`, `severity='moderate'` (closed set)"*. | `_READING_TABLE["prakara"]` = `("breaching_the_wall", "elevated", …)` (`logic.py:108`); `attacking` is bāhya's malefic posture (`:107`); `moderate` is in **no** set and would fail the CHECK the brief proposes. The brief's own illustrative row violates its own constraint. | `posture='breaching_the_wall'`, `severity='elevated'`. |
| N-14 | MINOR | §2.2: *"`count_from_janma` (`logic.py:47`)"*; §4.4: *"`inclusivity='closed_closed'` (`logic.py:121` uses `<=`)"*; §1: `grammar_v3_registry.yaml:148`. | `logic.py:47` is docstring prose; `count_from_janma` is `:86-89`. `logic.py:121` is the tuple-unpack in `attack_defence_reading`; the `<=` is `run_containing_date` `:182`. Grammar `:150` (N-01). | Correct the three. |
| N-15 | MINOR | §4.1: *"**Q04** verbatim (`STRATEGY:65`): 'activity coexisting with strain' — supporting and inhibiting paths can overlap; proof: 'a decisive second-domain opposition survives compilation'"*. | `STRATEGY:65`: question *"Why can activity coexist with strain?"*; distinction *"Receipt/creation versus retention/relief; supporting and inhibiting paths can overlap"*; proof *"A decisive second-domain opposition survives compilation **and projection**"*. None of the three is verbatim; the distinction's first half (receipt/creation vs retention/relief) is not something a ring posture can supply. | Quote verbatim; say Q04 is **partially** served (the inhibiting-path half) and Q03 not at all (a ring is not an activation route). |
| N-16 | NOTE | §7 Irrelevant control [S] *"two `as_of` inputs resolving to the same chart-zone date"*; Relevant influence (zone) [S] *"the served `as_of` echoes the chart-zone date"*. | `query_kota_chakra.ts` takes `as_of` as a date string (`:83`), has no instant input and no chart-tz lookup (`grep "timezone\|tz\|birth"` → none); both rows require the Pūrṇa wrapper to change. Only the Delivery row is marked gated. | Mark both wrapper rows "gated on the IP packet; L3 owns the test", as F-18 did for Delivery. |
| N-17 | NOTE | §2.4: `is_natural_malefic` classed `INTERPRETIVE_INFERENCE`. | `NATURAL_MALEFICS/BENEFICS` (`logic.py:95-96`) is the classical classification the codebase repeats — a `QUALIFIED_RULE` input; only the ring×nature → label step is inference. | Split the row. |

## 3. Citations verified

| citation (as in v1.1) | what is there | status |
|---|---|---|
| `writer.py:239-241`, `:243`, `:90`, `:305-309`, `:318`, `:292`, `:73-74`, `:76-81`, `:132-147`, `:142`, `:87-92`, `:94-99`, `:101`, `:115`, `:119-129`, `:252-261`, `:157-162`, `:175-177`, `:25-32`, `:31-32` | all resolve as described | resolved |
| `logic.py:45-55`, `:114-117`, `:106-111`, `:114-124`, `:95-96`, `:68-83`, `:65`, `:166-167` | disclosure; template (8 postures / 6 severities — **match the brief's §2.2 sets exactly**); readings; malefics; ring_for_count; names; truncation flags | resolved |
| `logic.py:47` (count_from_janma) | docstring prose; function at `:86-89` | **wrong** (N-14) |
| `logic.py:121` (`<=`) | tuple unpack; `<=` at `:182` | **wrong** (N-14) |
| `520:33-56`, `:34`, `:43-44`, `:51`, `:53`, `:54-55` | `platform/supabase/migrations/520_kala_kota_chakra.sql` | resolved |
| `523:37-39`, `:44-48`, `:64`, `:67` | `platform/supabase/migrations/523_bg_kota_chakra_rings.sql` | resolved |
| `670:890-917`, `:894-899`, `:900-908`, `:909-914`, `:1837` | `platform/migrations/670_…`: kota block `890-917`; (a) has no `table_version` predicate; `target_floor = 588` at `:1837` | resolved |
| `853:27-30` | `platform/migrations/853_nirmana_l3_w3_kota_chakra_expected_volume.sql:27-37` (Moon 442 … total 588) | resolved |
| `asset_runner.py:742-792` | `_run_substeps` aggregates `rows_inserted/rows_updated` only; no `.notes` handling in `pipeline/orchestrator/*.py` | resolved |
| `asset_runner.py:985` (this review) | `config={'chart_id': …, 'birth_params': …}` | resolved (N-07) |
| `216_chart_facts_partial_indexes.sql:22,28` | both unique indexes include `build_id` | resolved |
| `204_chart_facts.sql:14,:28` (this review) | `build_id UUID`; `computed_at TIMESTAMPTZ` | resolved (N-11) |
| `ga_writers/_idempotency.py:66` | `DELETE FROM chart_facts WHERE chart_id = %s AND fact_category = ANY(%s)` | resolved |
| `query_kota_chakra.ts:26`, `:83`, `:103-109`, `:122-123` | `MAX_LIMIT`; UTC-date default; SELECT whitelist `:102-109`; `total_matching`/`truncated` | resolved |
| `now.ts:418-445`, `:430`, `:432-445` | `fetchKotaChakraNow` `:418`; `current_only: true` `:430`; the mapped whitelist runs `:436-449` | resolved (±4) |
| `w25_kota_chakra.py:9-10`, `:19-20`, `:26-29`, `:48-51`, `:163-181` | as described | resolved |
| `mechanism_register.yaml:145` | `admission_state: admitted`; `:155` evidence_link names w43/w44 | resolved |
| `mechanisms/registry/w25_kota_chakra.yaml:13` | `candidate`, `ablation_evidence_link: null` | resolved |
| `grammar_v3_registry.yaml:148` | `admission_state: candidate` is at `:150` | **off by 2** (N-01) |
| "neither file exists" | `scripts/kala_admission/w43_ablation_runner.py`, `w44_weight_fitting.py` exist | **false** (N-01) |
| `engine.py:100-101` | imports only w23, w30 | resolved |
| `test_w25_kota_chakra.py:61-67`, `:84` | `_FakeContext.kota_chakra_data`; `{"Saturn": "stambha"}` | resolved |
| `tests/l3/test_ka_kota_chakra_writer.py:59-87` | fetch-helper tests only; no `run()` test | resolved |
| `asset_registry_seed.ts:2472-2487`, `:2486` | `depends_on: ['ga_positions','bg_ephemeris','bg_kota_chakra_rings']` | resolved |
| `l0_kota_chakra_rings.py:73` | `TABLE_VERSION = "kota_chakra_rings_v01"` | resolved |
| `l0_ephemeris.py:160-161`, `:271-278`, `:208-209` | noon-UT jd; `_compute_positions_for_date`; `get_ayanamsa_ut(jd)` | resolved |
| `REGISTER:142`, `STRATEGY:65`, `:86-98`, `:277`, `BLUEPRINT:325`, `:397`, `:907`, `:284`, `:286`, `:742`, `:882-886` | as described | resolved |
| `ADJUDICATIONS_NIGHT3:572` | *"0 hits across 10,651 chunks"* | resolved |
| `FOUNDATION:31`, `:39`, `:51`, `:55`, `:63-66` | F04, F12 (eight roles, no `testimony`), F24, F28, class names | resolved (N-08) |
| `BINDING:28-34`, `:41-46`, `:53-55`, `:62`, `:69-70` | B1–B5 rows quoted above | resolved |
| `birth_params.py:93-111` (this review) | `tz_offset_hours` from `charts.timezone_id` | resolved (N-07) |
| `date_resolver.py` (whole-file function list; `:295-322`; `:473`) | no date↔instant/tz function; `date.today()` at `:473` | resolved (N-02) |
| `ws2_l0_ephemeris.sql` DDL, `l0_ephemeris.py:298,:306` (this review) | `speed_dps NUMERIC(10,7)` stored per knot | resolved (N-06) |
| `w44_weight_fitting.py:66-71`, `:163`; `w43_ablation_runner.py:6-30`, `:69`, `:112` (this review) | w25 in `ADMITTED_MECHANISM_IDS`; engine-wired False; structural-proxy runner reading the grammar registry | resolved (N-01) |

## 4. What I could not verify

1. **Live state** — the live 670 result, `table_version` set, row count, whether any `_v02` set exists, and whether the four registry records have been reconciled outside the tree. No DB access. In-tree: one version (`_v01`); 853's per-graha sum (588) equals `target_floor`.
2. **The corpus count** — DB-resident (`classical_chunks`); two in-tree attestations (ADJ-9 `:572`; `523:37-39`); not re-runnable here.
3. **N-8 / M-4 text** — no Gochara plan in this worktree defines them; carried from the blueprint as [A].
4. **Whether w44's `ADMITTED_MECHANISM_IDS` was ever executed to produce a fitted weight for w25** — the list and the `MECHANISM_ENGINE_WIRED=False` entry are read; no fit artefact was searched for beyond `scripts/kala_admission/`.
5. **Ayanāṃśa drift rate** (~50.3″/yr) — writer docstring + general knowledge; the ≈0.015° at +400 d and the per-body crossing probabilities in N-06 are arithmetic on mean motions (Sun 0.986°/d, Moon 13.2°/d, Jupiter 0.083°/d, Saturn 0.0335°/d, mean node 0.053°/d), not measured against `derive_sidereal` or `ephemeris_daily`.
6. No test run; no build run; no migration applied.

## 5. Conformance to the binding (B1–B7)

- **B1**: `inclusivity`, `time_basis`, `claim_grain` — conformant names. `t_start/t_end` — **non-conformant**: the "declared mapping" points at a resolver capability that does not exist (N-02); an honest declaration is "DATE alias; instants not served until SC-1". tz source — the brief's `as_of` default is either an orchestrator change or a clock read (N-07); the row is not yet satisfied. Resolver row: **cannot be adopted** — nothing to import.
- **B2**: `epistemic_class`, `completeness_state`, `source_qualification`, `corpus_verifiable` (derived), `comparable_with='self'`, `tier_basis='relative_uncalibrated'` — names conformant; `comparable_with`/`tier_basis` now present (v1.0 gap closed). **Non-conformant**: `operator_role='testimony'` is outside F12 (N-08); `completeness_state` is named but never valued (N-09); `source_qualification='verse_cited'` on an uncorpused tier-iii transcription is at least contestable and triggers SC-7's `unqualified` on every consumer (N-09).
- **B3**: `window_id` sha256 and `window_ref` shape — conformant names. **Non-conformant**: `generation` not in the PK (N-10); `generation` omits the horizon, so the ref cannot express the one thing the brief's coverage design needs it to (N-03); truncated-row ids are horizon-bound (N-12).
- **B4**: `independence_group` with `basis='declared_lineage'` and the register's "not an independent adverse vote" clause — conformant (F-09 closed).
- **B5**: seven field names — conformant; **semantics non-conformant** (`unsearched_regions` outside the requested horizon), no persistence on the refusal path (N-03).
- **B6**: not applicable (no vedha/mūrti/contact/node verdict produced) — unchanged.
- **B7**: the Positive, Duplication, Duplication (janma), Revision and Negative rows now have detectors that can go red. The Boundary row's "no tolerance stamp" is field-presence, not correctness (N-06); the two wrapper rows are Pūrṇa-gated and unmarked (N-16); no row exercises the NULL-`ring_table_version` hole (N-04).
- **Asset-local names** (`ring_table_version`, `requested_horizon_start/end`, `stale_partition`, `uncited_extension`, `start/end_truncated`, `window_edge_tolerance_days`) — declared; `stale_partition` has no home (N-05); `window_edge_tolerance_days` as specified is a constant, not a measured field (N-06). The `requested_horizon` = `coverage.requested_horizon` invariant asked for in v1.0 §5 is still not stated.

## 6. Disposition of the 21 v1.0 findings (verified at source, not from §12)

| v1.0 | disposition | evidence |
|---|---|---|
| F-01 | **RESOLVED** | §0/§3/§4.3/§7 Revision/§10.4 now describe (a) as version-blind (`670:894-899` confirmed: no `table_version` predicate) and the pin as a precondition. The successor's own hole is a new finding (N-04), not a regression of F-01. |
| F-02 | **RESOLVED** | §7 Positive fixture `2025-12-31 … 2027-04-05` with `as_of=2026-03-01`; −60 d = 2025-12-31 and +400 d = 2027-04-05 re-derived; the gate (`writer.py:119-129`) is satisfied by an inclusive fixture of exactly that span. |
| F-03 | **RESOLVED** | `now.ts`/`query_kota_chakra.ts` moved to `interface_packet_targets_not_may_touch`; `must_not_touch` carries `kala_views/**`, `1033–1070`, `deploy.yml`, `generation='v1'`; paths `platform/supabase/migrations/520_…`, `platform/migrations/670_*`, `853_*` all correct. |
| F-04 | **PARTIAL** | A form is chosen and the retain alternative's key consequence is named (§4.2, §10.1, §10.2), but the chosen form does not resolve a stale ref and has no refusal-path home (N-03). |
| F-05 | **RESOLVED** | §0, §2.4, §4.4 derive `corpus_verifiable` from `bg_kota_chakra_rings.corpus_status` (`523:64`); §11.2 records the two attestations. |
| F-06 | **PARTIAL** | The disagreement is recorded and handed to Gochara (§1, §4.9), but v1.1 hardened "not in `gochara_v3`" into "neither file exists", which is false; w44's ADMITTED list is a fourth record; grammar line off by 2 (N-01). |
| F-07 | **RESOLVED** | `:239`, `:243` throughout; no `:2xx` remains. |
| F-08 | **PARTIAL** | The four cited lines corrected (`logic.py:45-55`, `:114-117`, `:106-111`, `:114-124`, `:95-96`, `:68-83`; `writer.py:76-81`, `:132-147`) and both vocabularies spelled out **match `_READING_TABLE` exactly** (8 postures, 6 severities); but two new mis-citations were introduced (`logic.py:47`, `:121` — N-14) and §9's example row contradicts the sets (N-13). |
| F-09 | **RESOLVED** | L3-A07 quoted verbatim (`STRATEGY:277` matches character-for-character); "Contact overlay" attributed to `BLUEPRINT:325`; object mapped to *Temporal testimony* over a *Contact*; register clause (`:142`) carried into B4. |
| F-10 | **RESOLVED** | §0: current horizon inferable from row extremes, previous not recorded; `WriterResult.notes` not persisted (`:318`; `asset_runner.py:742-792`). |
| F-11 | **RESOLVED** | `kota_chakra_rings_vNN` in §4.3; `kota_chakra_rings_v02` in §7 Revision; `'kota_chakra_rings_v01'` in §9. |
| F-12 | **PARTIAL** | Q04 named with a distinction and proof, Q-K06 demoted to the baseline question; but "verbatim" is a paraphrase, half the distinction and the proof's tail are dropped, and the service is partial, not full (N-15). |
| F-13 | **RESOLVED** | "UTC calendar date" in §0, §2.3, §3, §4.7 (`query_kota_chakra.ts:83` confirmed). |
| F-14 | **PARTIAL** | The edge-shift framing is adopted (§4.6), but the stamp rule (`1` slow / `0` otherwise) is wrong on both branches, the per-day alternative is mis-costed 9×, and a stored `speed_dps` detector was not considered (N-06). |
| F-15 | **PARTIAL** | Hazard stated (§2.2), `ORDER BY`/`LIMIT 1` promised (§5), two-build fixture added (§7); the ordering column is unnamed and the obvious candidate (`build_id`) is a UUID (N-11). |
| F-16 | **RESOLVED** | §4.10 and §7 Duplication compare on natural key + payload, excluding `id`/`computed_at` (`520:34`, `:53`). |
| F-17 | **PARTIAL** | Split into a zone-normalised control and a zone-relevant row; instant arithmetic (2026-03-01T22:00Z = 03-02 03:30 IST) correct; but both are wrapper tests the wrapper cannot run today and are not marked packet-gated (N-16). |
| F-18 | **RESOLVED** | §7 Delivery marked gated on the Pūrṇa packet; whitelists at `query_kota_chakra.ts:102-109` and `now.ts:436-449` confirmed. |
| F-19 | **RESOLVED** | "cannot reach the table through `ClassContext`" in §1 and §2.3; test lines confirmed. |
| F-20 | **RESOLVED** | 853's per-graha figures cited in §1; all counts labelled [A] in §3/§11. |
| F-21 | **RESOLVED** | §2.2 and §7 Negative state that `run()` has no test and `date.today()` is nowhere frozen. |

Tally: 14 RESOLVED · 7 PARTIAL · 0 UNRESOLVED. Seventeen new findings (N-01–N-17): 9 MAJOR, 6 MINOR,
2 NOTE. The disposition and the four stamps are unchanged by any of them; §10 decisions 1, 4 and 5
must be re-posed before the native can rule.
