---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_KOTA_CHAKRA
version: "1.0"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_KOTA_CHAKRA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.0", source_revision 9feac52d7)
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD 3387c9ac3"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no network, no git write"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed as written; every file:line re-read with sed/grep/cat -n at HEAD; prior audit artifacts not trusted for code claims; the seven caller-flagged axes (a)–(g) checked at source"
---

# Review — `KA_KOTA_CHAKRA_ELEVATION_BRIEF` v1.0

## 1. Verdict

**ACCEPT_WITH_CORRECTIONS.** The disposition (`QUALIFY_LIMIT` + one `ENRICH_CORRECT`) and the four
stamps it asks for are backed by the code as it stands at HEAD — `date.today()` at `writer.py:239`,
`MAX(table_version)` at `:90`, free-`TEXT` `posture`/`severity` at `520:43-44`, `corpus_status=
'not_in_corpus'` on the L0 ring rows — but the brief mis-describes the 670(a) detector it builds
its version-pin argument on, ships one proof row whose fixture cannot produce rows, leaves the
coverage object without a persistence home under its own recommended "replace" policy, hard-codes a
corpus basis the L0 row already carries, and lists a Pūrṇa-owned `kala_views` path in `may_touch`.
None of these changes the recommendation; all of them must be corrected before native ruling.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| F-01 | **MAJOR** | §0, §3: *"670(a) compares old rows to the **new** partition — a detector that fires on the wrong thing"*; §3 Observed: *"670(a) then reports the **un-rebuilt** charts as violating"*; §7 Revision row expects the successor to flag only "old rows". | `platform/migrations/670_nirmana_l3_w3_integrity_contracts.sql:895-899`: `JOIN bg_kota_chakra_rings r ON r.ring_position = k.count_from_janma WHERE r.ring_name IS DISTINCT FROM k.kota_ring` — **no `table_version` predicate at all**. `bg_kota_chakra_rings` PK is `(table_version, ring_position)` (`523:67`) and revisions land by APPEND (`523:44-48`), so once a `_v02` set exists every `kala_kota_chakra` row of every chart — freshly rebuilt or not — is joined against **both** versions; (a) fails globally wherever v01 and v02 disagree at any position. The detector does not "fire on staleness"; it fires on the existence of a second version. Not superseded: no later migration updates `integrity_check_sql` for `ka_kota_chakra` (853 sets `expected_volume_formula` only; 900 lists it in a digest spec). | Rewrite §3's failure description and the §7 Revision row: today's (a) is a *version-blind* join that cannot pass once two versions coexist; the successor must join `r.table_version = k.ring_table_version` (hence the pin is a precondition of the successor, not an optional add-on), and `stale_partition` is `k.ring_table_version <> (SELECT MAX(table_version) …)`. |
| F-02 | **MAJOR** | §7 Positive row: fixture ephemeris `2026-01-01..2027-03-31`, `as_of=2026-03-01`, expected `requested_horizon=[2025-12-31, 2027-04-05]`, rows written, 670 TRUE. | The horizon arithmetic is right (−60 d = 2025-12-31; +400 d = 2027-04-05), but the fixture does not cover 2025-12-31 or 2027-04-01..05. `writer.py:246-261`: `_has_complete_daily_series` (`:119-129`) requires one row per inclusive horizon day, else the writer **refuses** ("incomplete daily ephemeris coverage … prior partition preserved") and writes nothing. As written the Positive row can only produce the Negative row's outcome. | Fixture must span at least `2025-12-31..2027-04-05` (or `as_of` must move inside the fixture by ≥ 60 d / ≤ −400 d). State the fixture boundary explicitly per §7 "fixture/data boundary". |
| F-03 | **MAJOR** | frontmatter `may_touch`: *"interface packet only: … `platform-mcp/src/tools/kala_views/now.ts:415-440` (item 16)"*; `must_not_touch` omits the guide's fixed entries. | `KALA_ASSET_BRIEF_CONTEXT_v1_0.md` §5: *"`platform-mcp/src/tools/kala_views/*` belongs to the live Pūrṇa Anveṣaṇa campaign. Do not design changes to it."* Guide §2 header lists `platform-mcp/src/tools/kala_views/**`, `applied migrations 1033–1070`, `.github/workflows/deploy.yml`, `kala_gochara_windows WHERE generation='v1'` as fixed `must_not_touch` entries; §11 forbids editing `kala_views/`. Same defect class as REVIEW_KA_MUHURTA_SEVA_v1_1 N5 (MAJOR). Also: `must_not_touch` cites *"supabase/migrations/520_kala_kota_chakra.sql, migration 670"* — 520 is at `platform/supabase/migrations/520_kala_kota_chakra.sql`; 670 is at `platform/migrations/670_nirmana_l3_w3_integrity_contracts.sql` (a different directory). | Move `now.ts` (and the `query_kota_chakra.ts:83` default, which is `platform/src/lib/retrieval`, Pūrṇa-served) out of `may_touch` into an "interface-packet targets (Pūrṇa-owned; L3 owns the sentinel test)" line under L3-U04/U11; add the four fixed `must_not_touch` entries; correct both migration paths. |
| F-04 | **MAJOR** | §4.2: *"the per-build `coverage = {requested_horizon, completed_horizon, …, unsearched_regions:[] …}`. A rebuild that moves the horizon is a new generation with **the old one's** `unsearched_regions` = the dropped span, so a saved `window_ref` resolves to 'outside the current horizon' rather than to nothing"*; §10.1 recommends **replace**. §5 Migration: *"one additive DDL (stamp columns or a `qualification` JSONB + the two CHECKs)"*. | `writer.py:305-309`: `DELETE FROM kala_kota_chakra WHERE chart_id = %s` then `executemany(INSERT)`. Under "replace", the old generation's rows — and any per-row coverage on them — are gone; nothing in `may_touch` provides a table or partition where a *per-build* coverage record could survive the delete. `WriterResult.notes` (`:318`, `horizon=…`) is not persisted: `asset_runner.py:742-792` aggregates only `rows_inserted`/`rows_updated` (no `notes` handling anywhere in `pipeline/orchestrator/*.py` outside writers). A `window_ref` whose `generation` names the old horizon (§4.3) therefore resolves to *no row*, not to "outside the current horizon", unless the consumer compares the ref's date against the **current** generation's coverage. | Two implementable forms exist; the brief must pick and say so: (i) **replace, stateless** — the new generation's `coverage.unsearched_regions = [ <requested_horizon.start, >requested_horizon.end ]` (this inherently names any dropped span without remembering the old horizon), stored either on every row (denormalised) or in one additive coverage partition/JSONB row per chart that the delete-then-insert also rewrites; a consumer resolves a stale `window_ref` by date against it; or (ii) **retain** prior generations keyed by `requested_horizon` (decision 10.1's other branch), in which case the natural key `(chart, ayanamsha, graha, window_start)` (`520:54-55`) must gain `requested_horizon_start` or the second generation collides on `ON CONFLICT DO NOTHING` (`:115`). Name the coverage stamp home in §5/§10.2. |
| F-05 | **MAJOR** | §0, §4.4: *"`corpus_verifiable=false` stamped with `basis='gochara_g9_count_0'`"*; §3 Expected contract: *"§N.7 item 3 (no wrapper-local constant shadows the L0 partition — satisfied)"*. | The L0 row already carries the corpus verdict: `523:64` `corpus_status TEXT NOT NULL DEFAULT 'not_in_corpus'`, `523:37-39` *"the ingested corpus has ZERO hits for kota/kotachakra terms (confirmed by ADJUDICATION-9's own corpus finding)"*, and `SHAD_DARSHANA_ADJUDICATIONS_NIGHT3_v1_0.md:572` *"0 hits across 10,651"* (2026-08-01). The writer already copies `citation` verbatim off the L0 row rather than holding a literal (`writer.py:157-162, :175-177`). A writer-local `basis='gochara_g9_count_0'` string is exactly the wrapper-local constant §N.7 item 3 forbids — it would shadow `bg_kota_chakra_rings.corpus_status`, and drift from it the day the L0 owner ingests Muhūrta-Cintāmaṇi (523:39-42 files that work item). | Derive `corpus_verifiable` from the L0 row's `corpus_status` (`= 'in_corpus'`), carried the same way `citation` is; record the G-9/ADJ-9 counts as *attestations* in the brief's §11, not as a stored basis string. |
| F-06 | **MAJOR** | §1, §2.3, §4.7: Gochara `w25_kota_chakra` is *"proposed-use only"*, *"admission_state: candidate"*, multipliers *"remain proposed-use until N-8's ablation"*. | Code fact holds: `engine.py:100-101` imports only `w23_tara_bala` and `w30_nodal_drishti`; no Python reads any mechanism registry (`grep admission_state\|mechanism_register` over `gochara_v3/*.py` hits only docstrings); `context.py` has no kota field. **But the registries disagree:** `mechanisms/registry/w25_kota_chakra.yaml:13` `admission_state: candidate`, `ablation_evidence_link: null`; `grammar_v3_registry.yaml:148` `candidate`, `ablation_evidence_link: "W4.3"`; **`mechanism_register.yaml:145` `admission_state: admitted`, `uncited_extension: false`, `weight_type: fitted`, `evidence_link: "W4.3 ablation runner (w43_ablation_runner.py) … W4.4 weight fitting (w44_weight_fitting.py)"** — neither file exists in `gochara_v3` (only `w25_kota_chakra.py` and its test mention w25). The brief cites the module docstring (`:9-10`) and omits the `admitted` entry. | Add to §1/§2.3: the mechanism is unwired at the engine, but `mechanism_register.yaml:141-158` records it as `admitted`/`fitted` with an evidence link to files not in-tree — a GA.1-class registry disagreement in Gochara's fence; hand it to the Gochara stream as a named item alongside N-8, since a future loader reading that register would admit the multipliers without the ablation the brief relies on. |
| F-07 | MINOR | §0: *"`writer.py:73-74,:2xx today = date.today()`"*; §2.2/§3: *"`:2xx`"* twice. | `writer.py:239` `today = date.today()`; `:240-241` horizon; `:243` `_compute_ayanamsha_offset(today)`. Placeholders never resolved. | Replace `:2xx` with `:239` (and `:243` for the offset reference date). |
| F-08 | MINOR | §0: *"`uncited_extension=TRUE` … (`:2xx`, `logic.py:7-14`)"*; §2.2/§4.5: *"closed template … (`logic.py:56-70`)"*, *"`logic.py:66-70`"*; §2.2: *"`_fetch_janma_nakshatra_idx` (`:76-81`)"*. | `logic.py:7-14` is the "what Kota-Chakra is" prose; the uncited-extension disclosure is `logic.py:45-55` and `:114-117`; `writer.py:292` sets it. The closed template is `_READING_TABLE` at `logic.py:106-111`, `NATURAL_MALEFICS/BENEFICS` at `:95-96`, `attack_defence_reading` at `:114-124`; `logic.py:65` is `ALL_RING_NAMES`, `:68-83` is `ring_for_count`. The janma SQL is `writer.py:76-81`; the function is `:132-147`. | Correct the four citations. The value set for the CHECKs is: `posture ∈ {attacking, reinforcing_perimeter, breaching_the_wall, guarding_the_wall, inside_the_middle_ward, settled_in_the_middle_ward, at_the_heart, anchoring_the_heart}`, `severity ∈ {watch, supportive, elevated, high, acute, strong_support}` (`logic.py:106-111`) — spell it out in §4.5 so the migration is derivable. |
| F-09 | MINOR | §1: *"Strategy §3 *Contact overlay* — posture over a contact; convention scope explicit"*; §1 first row cites *"Register §5 (ka_kota_chakra row)"* only. | Strategy §3 (`MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md:86-98`) defines *Contact* and *Temporal testimony* — there is no "Contact overlay" object; the label is the blueprint's (`KALA_ELEVATION_BLUEPRINT_v1_0.md:325`). The Strategy §6.1 row the guide (§1 item 4) requires is **L3-A07** (`:277`): *"Preserve ring testimony, exact version and applicability; **replace mutable latest-version selection with pinned authority**. Current v3 implementation does not consume it: qualify and prove any proposed integration."* — the version pin is already a ruled Strategy obligation, which strengthens §4.3 and is uncited. Register row `:142`: *"not an independent adverse vote by default"* — a constraint the brief's B4 `independence_group = per graha` should carry. | Cite L3-A07 verbatim in §1; attribute "Contact overlay" to blueprint §3.5; map the object to Strategy *Temporal testimony* (posture as testimony) over a *Contact*; carry the register's "not an independent adverse vote" clause into §7 B4. |
| F-10 | MINOR | §0/§3: *"nothing records that the horizon moved"*; *"the earlier windows simply vanish"*. | True for persisted state (F-04), but overstated: because the complete-series gate forces every graha's first run to start on `horizon_start` and last run to end on `horizon_end` (`logic.py:166-167`, `writer.py:246-251`), `MIN(window_start)`/`MAX(window_end)` per chart **is** the current horizon and `start_truncated`/`end_truncated` mark it. What is unrecoverable is the *previous* horizon and any per-row declaration. | Reword: "the current horizon is recoverable only by inference from row extremes; the previous horizon is not recorded anywhere persisted (`WriterResult.notes` at `:318` is not stored by `asset_runner.py`)". |
| F-11 | MINOR | §7 Revision row: *"`bg_kota_chakra_rings` v02 → v03"*; §9 walkthrough: *"`ring_table_version='v02'`"*; §11.2: *"which `table_version` … is live, and whether more than one exists"*. | The version vocabulary is `kota_chakra_rings_v01` (`l0_kota_chakra_rings.py:73`; `523:45`), not bare `vNN`; the module defines exactly one version and 523 mandates APPEND for the next. The writer's `MAX(table_version)` is a string max over that form (`writer.py:84-86` explains the zero-padding). | Use the real form in §7/§9; §11.2 can be narrowed to "whether any `_v02` set has been appended in production" (in-tree there is one). |
| F-12 | MINOR | §4.1: *"Q03/Q04 … Q-K06"*; §7 Value row: *"frozen Q-K06 question"*. | L3-Q03 (`STRATEGY:64`) is "complete activation route"; L3-Q04 (`:65`) is "activity coexisting with strain". `Q-K06` is a Lane E readiness question (`readiness/_work/LANE_E_LAYER_VALUE_MODEL.md:191`: "Which clocks disagree about the second half of 2028?"), not L3-Q01–Q13; Context §2 forbids adopting a local question. The blueprint uses the label (`:298,:324`), so the brief inherits it — but §4.1 gives neither the *required added distinction verbatim* nor the *primary proof* from Strategy §2 that guide §5.1 demands, and the Value row's frozen question should be an L3-Q one. | State Q04's distinction ("supporting and inhibiting paths can overlap") verbatim and its proof ("a decisive second-domain opposition survives compilation"); cite Q-K06 only as the KALA_BASELINE question the Value row is run against, with the L3-Q it instantiates. |
| F-13 | MINOR | §2.3/§3: `as_of` default is *"the server's date"*; §3 Impact: *"off-by-one … around midnight UTC"*. | `query_kota_chakra.ts:83`: `new Date().toISOString().slice(0, 10)` — the **UTC** calendar date, independent of server zone. For an IST chart the UTC date is the previous day between 00:00 and 05:30 IST — the impact claim is right, the mechanism is UTC not "server". | "UTC calendar date" in §2.3/§3. |
| F-14 | MINOR | §0/§3 Non-claim: *"the < 0.02° single-offset approximation is accepted as disclosed"*. | Bound verified: reference at `today`, farthest horizon day is +400 d ≈ 1.10 yr × ~50.3″/yr ≈ 55″ ≈ 0.015° < 0.02° (`writer.py:25-32`). But the writer's own comparison ("negligible next to a 13.33-deg nakshatra span") is the wrong denominator: the material effect is on **boundary-crossing dates**, and for a slow body (Saturn ~0.033°/d, mean nodes ~0.053°/d) a 0.015° error at a nakṣatra edge can move `window_start`/`window_end` by one calendar day. Correctly bounded as an angle; unbounded as a date-grain precision claim. | Stamp the approximation as `algorithmic_approximation` with an explicit edge tolerance (`window_edge_tolerance_days = 1` for bodies with daily motion < ~0.05°/d, 0 otherwise), or compute the offset per day (the writer says this costs ~9×460 swisseph calls — cheap by its own §2.1 cost claim). |
| F-15 | MINOR | §2.2 Inputs: natal Moon fact via `_FETCH_JANMA_MOON_SQL`. | `writer.py:76-81` pins `fact_key` but has **no `ORDER BY`/`LIMIT 1`/`build_id`**; `:142` `fetchone()`. `chart_facts` uniqueness includes `build_id` (`216_chart_facts_partial_indexes.sql:22,28`), so two builds' MOON rows can coexist and `fetchone()` is order-undefined — §N.7 item 2's stated build_id caveat. Mitigated in normal flow by `ga_writers/_idempotency.py:66` deleting the whole `fact_category` across builds. | Note the hazard in §2.2 and add `ORDER BY build_id DESC LIMIT 1` (or a total order) to the brief's `may_touch` writer change; add a two-build_id fixture to the Duplication row. |
| F-16 | MINOR | §7 Duplication row: *"rebuild twice, same `as_of` → identical rows"*. | `520:34` `id BIGSERIAL`, `:53` `computed_at DEFAULT now()` — rows are never byte-identical across rebuilds. A whole-row equality detector would always fire; a detector that ignores these columns is needed. | Compare on natural key + payload columns (exclude `id`, `computed_at`). |
| F-17 | MINOR | §7 Irrelevant-control row: *"`as_of` in UTC vs chart zone at the same instant → same current row … differs (today)"*. | Windows are `DATE` (`520:45-46`) and `as_of` is a date string; the same instant near IST midnight legitimately *is* two different calendar dates in the two zones. "Same current row" is not zone-invariance — it is the assertion that the chart-zone date is the one used. As phrased the row cannot distinguish a correct chart-zone answer from a coincidental match. | Fix the instant (e.g. 2026-03-01T22:00Z = 2026-03-02 03:30 IST) on a day where the Moon changes nakṣatra; assert the served `as_of` echoes the chart-zone date and the Moon row is the 03-02 run. |
| F-18 | NOTE | §7 Delivery row: sentinel `corpus_verifiable=false` *"reaches `now.ts` item 16 and `query_kota_chakra` envelope"*. | Both surfaces select an explicit column whitelist — `query_kota_chakra.ts:103-109`, `now.ts:432-445` — so no new column reaches either without the Pūrṇa packet. The detector correctly fails today; the row should say it is gated on the L3-U04/U11 packet, not L3-runnable. | Mark the row "blocked on IP packet; L3 owns the test". |
| F-19 | NOTE | §1 w25 row: *"the mechanism **cannot read** the table today"*. | Precisely: it cannot *reach* it through `ClassContext` (no kota field, `context.py`); the mechanism does read `kota_chakra_data` when a context carries it (`mechanisms/tests/test_w25_kota_chakra.py:61-67,:84`). | "cannot reach the table through `ClassContext` today". |
| F-20 | NOTE | §2.1: *"585 rows live [A]"*. | `670:1837` `target_floor = 588`; `853_…_expected_volume.sql:27-30` records a per-graha live measurement (Moon 442, Mercury 41, Venus 35, Sun 35, …) as of that migration. Neither is the live count; the [A] label is honest. | Cite 853's per-graha figures as the most recent in-tree measurement. |
| F-21 | NOTE | §7 Negative / Context rows treated as regressions of existing behaviour. | No test exercises `run()`: `tests/l3/test_ka_kota_chakra_writer.py` covers the two fetch helpers only (`:59-87`); `date.today()` is nowhere frozen. The refusal path (`:252-261`) and the horizon are untested today. | Both rows are genuinely new detectors; say so (they are not "unchanged" behaviour with tests behind them). |

## 3. Citations verified

| citation (as in the brief) | what is there | status |
|---|---|---|
| `writer.py:73-74` | `HORIZON_BACK_DAYS = 60`, `HORIZON_FORWARD_DAYS = 400` | resolved |
| `writer.py:2xx today = date.today()` | `:239` | **placeholder, resolves at `:239`** (F-07) |
| `writer.py:88-91` (MAX table_version) | SQL `:87-92`, `MAX(table_version)` at `:90` | resolved |
| `writer.py:23-32` (single offset, < 0.02°) | docstring `:25-32` | resolved |
| `writer.py:41-45` (single ayanāṃśa) | `:41-45` | resolved |
| `writer.py:76-81` `_fetch_janma_nakshatra_idx` | SQL `:76-81`; function `:132-147` | SQL resolves; function line wrong (F-08) |
| `writer.py:18-22` (same formula as `derive_sidereal`) | docstring `:18-21` | resolved |
| `writer.py:84-91`, `:94-99` | ring SQL; ephemeris SQL | resolved |
| `writer.py:118-128` `_has_complete_daily_series` | `:119-129` | resolved (±1) |
| `writer.py:101, :115` | `_DELETE_SQL`; `ON CONFLICT … DO NOTHING` | resolved |
| `logic.py:7-14` (uncited_extension) | Kota prose; disclosure is `:45-55`, `:114-117` | **wrong lines** (F-08) |
| `logic.py:56-70`, `:66-70` (closed template) | `:65` `ALL_RING_NAMES`, `:68-83` `ring_for_count`; template `:106-111` | **wrong lines** (F-08) |
| `logic.py:17-35` (ADJUDICATION-9) | `:16-35` | resolved |
| `520:33-56`, `:43-44`, `:51`, `:55` | DDL; `posture TEXT NOT NULL`/`severity TEXT NOT NULL` no CHECK; `uncited_extension … DEFAULT TRUE`; UNIQUE natural key | resolved (path is `platform/supabase/migrations/520_kala_kota_chakra.sql`) |
| `670:890-1016`, `:892-905` (a)–(c) | `platform/migrations/670_…:890-917` — (a) `:894-899`, (b) `:900-908`, (c) `:909-914` | resolves; (a) has **no version filter** (F-01); brief's `:890-1016` overruns into `ka_kshetra`'s contract (`:920+`) |
| `query_kota_chakra.ts:83` | `as_of` default `new Date().toISOString().slice(0,10)` | resolved (UTC date, F-13) |
| `query_kota_chakra.ts:113-130`, `MAX_LIMIT 50` | `MAX_LIMIT` at `:26`; `total_matching`/`truncated` `:122-123`; envelope `:124-140` | resolved |
| `now.ts:415-440` (item 16, `current_only: true`) | `fetchKotaChakraNow` `:418-445`; `current_only: true` at `:430` | resolved |
| `w25_kota_chakra.py:9-29`, `:19-20`, `:50`, `:165-178` | admission text `:9-10`; multipliers `:26-29`; "never consumed" `:19-20`; ClassContext note `:48-51`; degrade `:163-181` | resolved |
| `asset_registry_seed.ts:2472-2487` | `platform/scripts/seed/asset_registry_seed.ts:2472-2487`; `depends_on` at `:2486` | resolved |
| Blueprint `:325`, `:397`, `:907` | §3.5 row 9; §4 row 9; §16.2 row | resolved |
| `l0_kota_chakra_rings.py` (tier-iii, `vNN`) | `:50`, `:59`, `:73` `TABLE_VERSION = "kota_chakra_rings_v01"` | resolved; form is `kota_chakra_rings_vNN` (F-11) |
| Strategy §3 "Contact overlay" | no such object (`:86-98`) | **unresolved** (F-09) |
| Register §5 ka_kota_chakra row | `MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md:142` | resolved |
| Gochara G-9 "count = 0" | `KALA_ELEVATION_BLUEPRINT_v1_0.md:35,:325`; `KALA_ELEVATION_CHRONICLE_v1_0.md:895`; independently `SHAD_DARSHANA_ADJUDICATIONS_NIGHT3_v1_0.md:572` ("0 hits across 10,651") and `523:37-39` | resolved as [A] attestations; not a query re-runnable in-tree (§4) |

## 4. What I could not verify

1. **Live 670 result, live row count, live `table_version` set** — no DB access. In-tree: exactly one version (`_v01`) is defined; 853 records per-graha counts as of its cycle.
2. **The corpus count itself.** The corpus is DB-resident (`classical_chunks`, 10,651 chunks per ADJ-9 `:572` and `KA_TITHI_PRAVESHA_ELEVATION_BRIEF:154`). Two independent in-tree *attestations* of 0 rows exist (ADJ-9 2026-08-01; blueprint chronicle 2026-09-23 "established the way the rule requires"), and `grep -rli kota 08_CLASSICAL_CROSS_REFERENCE/corpus` returns nothing — but that directory holds only `ingestion/` and `raw/`, not the chunk table, so this is corroboration, not verification. The brief's `[A]` label is correct; it should cite both attestations (F-05).
3. **N-8 and M-4.** No Gochara plan in this worktree defines them (`GOCHARA_FAMILY_ELEVATION_PLAN_v0_2/v0_3` do not contain the labels; the "Gochara plan v2.0" other briefs cite is absent). The brief carries them from the blueprint as [A]; I could not check their text.
4. **Whether `mechanism_register.yaml`'s `admitted` entry is ever loaded at runtime** — no Python reader found in `gochara_v3/`; a dynamic loader elsewhere (e.g. a TS or CLI path) was not searched beyond `platform/python-sidecar/services/gochara_v3`.
5. **Ayanāṃśa drift rate** (~50.3″/yr) is taken from the writer's own docstring (`:27-28`) and general knowledge, not measured against `derive_sidereal` at two dates.
6. No test was run; no build was run.

## 5. Conformance to the binding (B1–B7)

- **B1**: `inclusivity='closed_closed'`, `time_basis='noon_ut_knot'` (ephemeris knots are noon UT — `l0_ephemeris.py:160-161,:271-277`, verified), `claim_grain='date_grain'` — conformant names. The brief keeps `window_start/window_end` as `DATE`; B1 requires the *served* type to be `timestamptz` UTC (`t_start/t_end`) — the brief neither converts nor declares an alias mapping. **Non-conformant until it says `t_start/t_end` are derived views (chart-tz midnight via `ka_temporal/date_resolver`) or names the DATE columns as a declared alias.** The brief also does not adopt `date_resolver` for its `as_of`→horizon conversion (B1 "resolver" row: MUST ADOPT).
- **B2**: `epistemic_class`, `completeness_state`, `source_qualification`, `corpus_verifiable`, `operator_role='testimony'` — conformant names. `comparable_with` and `tier_basis` are **absent** (B2 requires both on any producer row carrying a graded quantity; `severity` is one). Add `comparable_with='self'` and `tier_basis='relative_uncalibrated'`.
- **B3**: `window_ref` with `generation` — conformant; but the per-asset stable id must be a content-addressed sha256 over natural key + method version, and the brief proposes none (rows are addressed by `id BIGSERIAL`). Add `window_id` = sha256(chart, ayanamsha, graha, window_start, FORMULA_VERSION, ring_table_version).
- **B4**: `independence_group` with `basis:'declared_lineage'` — conformant; carry the register's "not an independent adverse vote by default" (F-09).
- **B5**: `coverage` object with the seven fields — conformant names; persistence home unspecified (F-04).
- **B6**: not applicable (no vedha/mūrti/contact/node verdict produced).
- **B7**: the brief names detectors for tests 5–7 and 9 but two of them cannot go red as written (F-02, F-16, F-17).
- **Asset-local names** (`ring_table_version`, `requested_horizon`, `stale_partition`, `uncited_extension`, `start/end_truncated`) are declared as such — acceptable; `requested_horizon` duplicates `coverage.requested_horizon` and should be the same value by construction (state the invariant).
