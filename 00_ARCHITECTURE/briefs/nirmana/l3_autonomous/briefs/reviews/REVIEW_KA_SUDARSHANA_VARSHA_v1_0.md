---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_SUDARSHANA_VARSHA
version: "1.0"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_SUDARSHANA_VARSHA_ELEVATION_BRIEF_v1_0.md
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD 3387c9ac3; the brief's source_revision 9feac52d7 is an ancestor and `git diff --stat 9feac52d7 HEAD` on services/ka_sudarshana_varsha/, query_sudarshana_varsha.ts, kala_views/now.ts, services/gochara_v3/, orchestrator/birth_params.py and migrations 521/670 is empty, so every citation checked at HEAD holds at 9feac52d7"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no git write, no external service"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed as written; every file:line re-read with sed/grep at HEAD; the lockstep-convergence invariant and the birthday-eve zone arithmetic re-computed with a throwaway read-only python snippet (no repository write); prior audit artifacts (blueprint v5.0 rows, STRAT A10, the W0 field register) treated as [A] and re-checked at source where they carry a code claim"
---

# Review — `KA_SUDARSHANA_VARSHA_ELEVATION_BRIEF_v1_0`

## 1. Verdict

**REWORK.** The brief's direction is right on every axis it names — one temporal current, not three witnesses; anniversary windows, not a solar return; a chart-zone `as_of`; a dangling Gochara declaration — and its source citations resolve. But it stops one step short of the fact that decides its own central claim: because every frame adds the *identical* offset, `tri_lagna_convergence` is a **per-chart constant across all 120 rows** (a natal property restated 120 times; migration 670's own conjunct (c) already records this), so "convergence years" do not exist and one proof row demands a behaviour the arithmetic forbids; the `as_of` defect is equally live on `kala_now_get` (`now.ts:1643` has the same server-UTC default and passes it explicitly, so the proposed wrapper-only fix never reaches the primary served surface) while `now.ts` sits in `may_touch` against the guide's prohibition; four of the six `[S]` proof rows are either not executable through the inputs that exist or have no detector today; the w27c decision offers a false binary (the mechanism's expected row shape matches neither namesake table, and two registry records disagree on its admission state); and the birth-date hazard is hedged on a 112-line file the brief could have read and that closes the hazard.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| F-01 | **MAJOR** | §0/§3: `tri_lagna_convergence` is *"a property of the natal sign spacing, not three independent testimonies"*; §4.2: the flag *"keeps its meaning"*; §7 Relevant influence: *"convergence years move"*. | `logic.py:74` `(varsha_year - 1) % 12` is the one offset; `:110-112` add it to each natal index; `:117` `jl == cl == sl`. Hence `jl_N == cl_N == sl_N` ⇔ `jl_natal == cl_natal == sl_natal` for every N — the flag is **constant per chart** over all 120 rows (re-computed exhaustively over 1728 natal triples × 120 years: no triple changes state). The repository already knows this: migration 670 (c) comment, `platform/migrations/670_nirmana_l3_w3_integrity_contracts.sql:1252-1254` — *"The flag is constant per chart because all three references share one offset — a known, recorded finding"*; and `tests/l3/test_ka_sudarshana_varsha.py:124-134` asserts the lockstep invariant. For the canonical chart (Aries / Aquarius / Capricorn, test `:115-117`) it is `false` in all 120 rows. So there are no "convergence years"; the served wording *"a classically notable alignment year"* (`query_sudarshana_varsha.ts:38-39`; DDL COMMENT `platform/supabase/migrations/521_kala_sudarshana_varsha.sql:66-67`) reads a natal constant as an annual event. | State the invariant outright: the flag carries **zero temporal information**; its honest qualification is `convergence_basis='natal_constant'` (or equivalent) plus `declared_current_count=1`, and the "alignment year" wording on the wrapper/DDL is an interface-packet correction. Cite 670 (c) as the prior record rather than presenting the finding as new. Rewrite the Relevant-influence row (F-08). |
| F-02 | MINOR | §4.2: `independence_group = {basis:'declared_lineage', root:'natal_signs', members:['jl','cl','sl'], root_fact_ids:[lagna, moon, sun]}`; §0: *"one natal root"*. | `KALA_SYNERGY_BINDING_v1_0.md:64` fixes the shape to `[{group_id, family, roots[], members[], basis:'declared_lineage'}]` — an array, `roots[]` plural, `group_id` and `family` required; `root` and `root_fact_ids` are not binding names (`:18-20`: differently-named field = non-conformant). Blueprint Q3 (`KALA_ELEVATION_BLUEPRINT_v1_0.md:668`): independence between currents = disjoint `roots[]`; *"sudarśana's three frames = one root"*. Astronomically the three natal signs are **three** L1 facts (`writer.py:46-52`, three `fact_id`s) of one chart; what makes the frames one witness is the shared temporal current — one anchor (`birth_date`, `writer.py:151`) and one rule (`:74`) — not a shared fact. | One group: `{group_id, family:'sudarshana_varsha', roots:[<birth-date anchor>, lagna_fact_id, moon_fact_id, sun_fact_id], members:['jl','cl','sl'], basis:'declared_lineage'}`; say explicitly that `declared_current_count` counts **groups** (1), and that the moon fact in `roots[]` is what makes this asset non-independent of `ka_tithi_pravesha` for a consumer of both. Rename to the binding's fields. |
| F-03 | **MAJOR** | §2.3 live-path: *"the server-date `as_of` default is live on the direct wrapper call"*; §4.5: *"`now.ts` already passes its own date — unchanged"*; §2.3 table: `now.ts` *"with its own `asOfDate`"*. | `platform-mcp/src/tools/kala_views/now.ts:1643` `const asOfDate = args.as_of ?? new Date().toISOString().slice(0, 10)` — the **identical** server-UTC default — passed explicitly to the wrapper at `:474-478`, so `query_sudarshana_varsha.ts:71` is never reached through `kala_now_get`; `:2201-2205` documents *"default: today"*. A wrapper-only fix leaves the primary served surface with the defect; and the default is shared by every item `kala_now_get` assembles (`:1645-1650`), so it is layer-wide, not asset-local. Same pattern already noted for `query_tithi_pravesha.ts:70` in `REVIEW_KA_TITHI_PRAVESHA_v1_0.md` F-17. | Carry SC-1 for this asset as an **interface packet** (L3-U04/U11 form; blueprint §12.2 has no IP for the `as_of` default — propose one) covering both `now.ts:1643` and the wrapper default, owned by Pūrṇa, with the L3-owned test of F-05. State honestly that the wrapper fix alone changes nothing a `kala_now_get` reader sees. |
| F-04 | **MAJOR** | Header `may_touch` includes *"platform-mcp/src/tools/kala_views/now.ts:466-480 (item 17)"* (*"interface packet only"*); `must_not_touch` omits `kala_views/**`. | `KALA_ASSET_BRIEF_INSTANTIATION_GUIDE_v1_0.md` §2 header template puts `platform-mcp/src/tools/kala_views/**` in `must_not_touch`; §11 and `KALA_ASSET_BRIEF_CONTEXT_v1_0.md` §5: *"Do not design changes to it … express it as an interface packet"*. The cited lines are also not where the change would go: `:466-468` is a docstring, `fetchSudarshanaVarshaNow` is `:469-495`, and the default lives at `:1643`. | Move `kala_views/**` to `must_not_touch`; the `as_of` and field-projection changes become the IP of F-03/F-07. |
| F-05 | **MAJOR** | §7 Irrelevant control: *"same `as_of` instant expressed in UTC vs chart zone → same `is_current` row"*; §7 Boundary: *"`as_of` = birthday eve 23:30 chart-local → year N; `+1 h` → N+1"*; §4.8 boundary. | The wrapper's `as_of` is a `YYYY-MM-DD` string (`query_sudarshana_varsha.ts:46,:71`; `now.ts:2203` regex `^\d{4}-\d{2}-\d{2}$`): no instant or zone can be passed, so neither row is executable as written. Read as a date pair (Feb 4 → N, Feb 5 → N+1) they exercise `:88` `window_start <= $4::date AND window_end > $4::date`, which passes today and distinguishes nothing. The only zone-dependent path is the **omitted-`as_of` default**. Arithmetic checked: 2026-02-04 23:30 IST is 2026-02-04 UTC; +1 h is 2026-02-05 IST but still 2026-02-04 UTC. | One fixture serves both rows: fake wall-clock at `2026-02-04T18:30:00Z` (= 2026-02-05 00:00 Asia/Kolkata) with `as_of` omitted — today's code marks year 42 (`2026-02-04`), chart-zone code marks year 43 (`window_start 2026-02-05`); a second clock at `18:29:59Z` → 42 under both. Declare the zone source as `charts.timezone_id` (the birth zone, B1 *"the birth instant's offset"* `BINDING:33`), not residence, and echo the resolved date. |
| F-06 | **MAJOR** | §7 Duplication: *"a convergence year to a concurrence consumer → counted as one … detector fails when counted 3"*; §4.10 ablation: *"today it counts 3; after, 1"*. | No concurrence consumer exists on this base: SC-6 ledger *unowned* (blueprint `:285`), IP-5 not built (`:759`), w27c not wired (`gochara_v3/engine.py` has no `w27`/`annual_stack` reference; `ClassContext` `context.py:114-186` has no `sudarshana_rows`), no `ka_*` reader (brief §2.3, re-grepped). Nothing counts 3 today, so the "3 → 1" ablation is not measurable — a planned test with no detector (F23). | Today's provable invariant is row-level: one group persisted, `declared_current_count=1`, three `members`, re-derived by a new integrity conjunct (F-13). Mark the served-count test *"detector exists once IP-5/SC-6 lands"* and the ablation as conditional on it. |
| F-07 | **MAJOR** | §7 Delivery: sentinel `approximates='solar_return_instant'` *"reaches `now.ts` item 17 … fails when absent"*. | A constant on every row is the default, not *"a decisive non-default sentinel"* in *"a low-ranked, non-default field"* (`MADHAV_DATA_PLANE_ASSET_INTERFACE_EXECUTION_BRIEF_CONTRACT_v1_0.md:112`; blueprint §12.3). `now.ts:484-493` copies seven named fields; the existing test `platform-mcp/src/__tests__/kala_now_get_kota_sudarshana_w3.test.ts:139-164` asserts only those. Nothing new reaches `kala_now_get` without the Pūrṇa packet — a fine today-fails detector, but the sentinel cannot prove per-row propagation. | Sentinel = a per-row value the wrapper cannot synthesise (e.g. `independence_group[0].roots` containing the real `moon_fact_id`, or a `completeness_state` seeded on one year in a fixture); assert it in `kala_now_get.sudarshana_varsha` and in the saved reading; the row's precondition is the IP of F-03. |
| F-08 | **MAJOR** | §7 Relevant influence: *"change natal Moon sign by 1 → `cl_active_*` shift by 1 every year; `jl/sl` unchanged; convergence years move"*. | Per F-01 convergence cannot "move": for any unequal natal triple it is `false` in every row before and after; from an equal triple, a one-sign change flips **all 120** rows to `false`. As written the row can never pass under correct code. | Expected: `cl_active_*` +1 in every row; `jl_*`/`sl_*` byte-identical; `tri_lagna_convergence` unchanged (all `false`) — or, from an equal-triple fixture, all 120 flip. That is the honest statement of the flag's dependence. |
| F-09 | MINOR | §4.3 / §10 decision 1: none of `{event_instant, noon_ut_knot, date_grain_midpoint}` fits; *"add `calendar_anniversary`"*; option (ii) re-base on the true return. | `time_basis` exists in **no code** on this base (grep across sidecar, `platform/src`, `platform-mcp/src`, both migration dirs: only the unrelated `lifetime_basis`); the binding's "OFFERS" for Gochara is prose. The binding's own Saṅgam rule (`BINDING:27`) converts a DATE to *"instant at the chart's tz-aware midnight"* — a fourth derivation basis the enum does not name either. The gap is therefore **binding-wide** for every DATE-grain producer (sudarshana, kota, moorti, jivana, avadhi …), and `calendar_anniversary` names the astrological rule (already carried by `approximates` and the technique) rather than how the bound instant is derived. Rejecting (ii) is right: a root-found return is a different technique (`logic.py:41-46`). `time_basis=null` with reason is defensible under §N.7 item 6 but B1 says *"declared on every row"* (`:30-31`). | Raise **one** amendment to the binding owner for a generic value (e.g. `date_grain_local_midnight`), applicable to every DATE-grain asset, keeping `approximates='solar_return_instant'` asset-local; state the null as transitional with the amendment id. |
| F-10 | MINOR | §2.2: birth date = *"the calendar date component of an ISO string whose zone is whatever the orchestrator supplied"*; §3 non-claim / §11.2: *"`birth_params.py` — not read; if it is UTC, a near-midnight birth could shift the anniversary by a day"*. | `pipeline/orchestrator/birth_params.py:96` `birth_dt = datetime.combine(row["birth_date"], row["birth_time"])`, `:106` `"datetime_iso": birth_dt.isoformat(timespec="seconds")` — **naive local wall-clock**, never converted to UTC; the zone travels separately as `tz_offset_hours` (`:99,:109`). So `raw[:10]` (`writer.py:111`) equals `charts.birth_date` exactly; the writer test `tests/l3/test_ka_sudarshana_varsha_writer.py:45` pins that shape. The hazard is **over-stated**: it does not exist on the FROZEN path and the brief could have closed it by reading the file it names. What is **under-stated**: nothing detects a change of that convention — 670 (h) (`:1305-1313`) anchors on `min(window_start)`, so a wheel built from a wrong date passes all eight conjuncts; `charts.birth_date` is the only external anchor. | Record the convention as verified (naive local; `[:10]` = `charts.birth_date`). Add a conjunct (h′) `min(window_start) = charts.birth_date` (via a new migration that re-`UPDATE`s `integrity_check_sql`, not by editing 670) or persist `birth_date_basis` on the row. |
| F-11 | MINOR | §4.7 / §10 decision 4: w27c *"must either consume `kala_sudarshana_varsha` by `window_ref` … or drop the declaration"*; §2.3: the dangling declaration. | Dangling confirmed (`w27_annual_stack.py:31`; no `sudarshana` in `context.py`; engine unwired). But `compute_sudarshana` (`w27_annual_stack.py:436-460`) reads rows shaped `{graha, house_number, window_start_iso, window_end_iso}` — a **transit body's house** in a composite chart; `kala_sudarshana_varsha` has no graha or house column (521 `:32-57`) and `bo_sudarshana` is static — **neither namesake supplies the shape**, so "consume by `window_ref`" is not available without a new Gochara-owned transform, and that transform would be a λ-product modifier (`_SD_HOUSE_MODIFIERS` `:102-115`), the operand class blueprint Q2 (`:667`) bars before an M-4 audit. Two registry records disagree: `mechanisms/registry/w27c_sudarshana.yaml:15` `admission_state: candidate` vs `mechanism_register.yaml:237` `admission_state: admitted`; both assert `citation_status: cited` / *"BPHS Sudarśana Cakra chapter"* (yaml `:6-7`; register `:242-244`) — a repository record bearing on the brief's own decision 3, unmentioned. The W0 field register `MADHAV_DATA_PLANE_L3_W0_FIELD_CONTRACT_REGISTER_v1_0.md:215-218` lists *"gochara-v3 annual stack"* as a receiver — stale; guide §1 says record which record was stale. | Decision 4 becomes three-way (drop; re-scope to a Gochara-owned transform under M-4; or leave candidate with the declaration corrected) and names the admission-state contradiction; decision 3 must adjudicate the yaml's citation string alongside the writer's prose; §1 marks the field-register receiver as stale. |
| F-12 | MINOR | §7 Negative: `varsha_year_for_date(birth, birth−1d)` raises. | Real detector, no production path: the writer imports only `DEFAULT_MAX_VARSHA_YEAR, compute_tri_lagna_year, varsha_window` (`writer.py:35-39`); grep finds no caller of `varsha_year_for_date` outside `logic.py` and its tests. The live pre-birth path is the wrapper SQL (`:88` → no `is_current` row) and `now.ts:483` `current: null`. | Target the live path (as_of before birth → all 120 rows `is_current=false` → `current: null`, coverage honest-empty); keep the unit test as secondary. |
| F-13 | MINOR | §7 Positive: *"120 rows; 670 TRUE; stamps present … fails when any conjunct FALSE / stamp absent"*; §5: *"670's conjuncts unaffected"*. | 670 (a)–(h) (`:1221-1318`) re-derive tiling, arithmetic, convergence, labels vs `reference_signs`, fact-id resolution + natal index vs L1 value, contiguity, single anchor, anniversary windows — all real detectors (each has a row pattern that makes it FALSE). None tests a stamp; and the brief correctly forbids editing the applied 670. So "stamp absent" has no detector on this base. | A new migration re-`UPDATE`s `asset_registry.integrity_check_sql` adding (i) every row carries the stamps with vocabulary-valid values and (j) `declared_current_count = 1`; note that (h) is anchor-relative (F-10). |
| F-14 | MINOR | §7 Value: fixture *"frozen Q-K06 question"*. | `KALA_BASELINE_v1_0.md` does not exist on this base (`find 00_ARCHITECTURE -name KALA_BASELINE_v1_0.md`: none; only the freeze execution prompt). | State the fixture is pending the freeze; name the ordinary-period case (year 43, no convergence, no dramatic event). |
| F-15 | MINOR | §4.1: *"L3-Q served. Q05 (an annual frame beside other clocks — as one voice); Q-K06"*. | Q05's primary proof is *"Remove a method; show actual changed and unchanged evidence"* (`MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md:65`); with no ledger and no integrator (F-06) nothing consumes the voice today, so none of Q05 is served until SC-6 / Q2 is ruled and built. | *"Q05: partial, conditional on SC-6/Q2; today the asset offers the voice's qualification, not the voice."* |
| F-16 | MINOR | §4.3 window `source_qualification='algorithmic_approximation'`; §4.4 rule `source_qualification='verse_cited'` or `'unsourced'`. | B2 (`BINDING:44`) names **one** `source_qualification` *"on the producer row"*; the brief assigns two values to one row (window vs rule). | The row's `source_qualification` is the rule's; the window basis goes in a declared asset-local field (e.g. `window_basis='calendar_anniversary'` alongside `approximates`), with the alias map in §7. |
| F-17 | MINOR | §7: `window_ref={asset_id, generation:FORMULA_VERSION, id:(chart, varsha_year)}`. | B3 (`BINDING:52-54`): id **content-addressed sha256** over natural key + method version; `generation` on every row **and in the PK**. `formula_version` is a column (521 `:52`) but not in the natural key (`:54-55`); a `(chart, varsha_year)` tuple is not a sha256 id. | Declare the alias mapping explicitly, or add the sha256 id and put `generation` in the key. |
| F-18 | MINOR | §1 "Already established" cites the register row, strategy, blueprint, binding, 521/670, seed. | Guide §1 requires also: CURRENT_STATE §4.1 row (`MADHAV_DATA_PLANE_L3_CURRENT_STATE_AND_DISPOSITION_v1_0.md:130` — *"no L3 input; annual evidence \| preserve; receiving operator owed"*), the field-register partition (`…FIELD_CONTRACT_REGISTER_v1_0.md:35, 215-…`), FOUNDATION_SAFETY (`:79`). Template v2.0 §2.1 (a)–(d) latent-value classification and §2.6 knowledge-time pin are absent; `computed_at` (521 `:53`) is the only knowledge-time field and is unmentioned. §9's walkthrough covers §2.5 adequately. | Add the three records; classify each material field (a)–(d); pin event vs knowledge time (`window_start` is event-time calendar; `computed_at` knowledge-time). |
| F-19 | NOTE | Citation drift. | `writer.py:157-160` cited for fact-id carriage → fact ids are `:168-170` (`:157-160` are natal indices); `:124-136` refusals → `:130-142`; `:140-165` rows → `:148-172`; `logic.py:22` → `:21`; `logic.py:70-77` → `:69-74` and `:77-80`; `now.ts:466-480` → `:469-495`. Migration paths in `must_not_touch` lack the `platform/` prefix (`platform/supabase/migrations/521_…`, `platform/migrations/670_…`). `ka_taranga.py:92` resolves (`SELECT timezone_id FROM public.charts …`) but is a writer-side **fallback** after `birth_params.tz_offset_hours` (`:85-88`), not a wrapper convention. Blueprint `:906` still says *"day-grade declared as `precision_regime`"* — stale against `BINDING:32` (`claim_grain`); the brief correctly uses `claim_grain`. | Fix the line numbers and paths; note the stale blueprint wording. |
| F-20 | NOTE | Header `target_state_data_plane`: *"PRODUCER_READY (stage 3); DATA_ACCEPTED when …; CONSUMER_INTEGRATED when …"*. | `KALA_ASSET_ELEVATION_PLAN_TEMPLATE_v2_0.md` §4: *"no brief may claim past `PRODUCER_READY`"*; context §9: `CONSUMER_INTEGRATED` has no admissible event type. The conditionals are honest but the field should hold one state. | `PRODUCER_READY`; the conditions stay in §9. |
| F-21 | NOTE | §7 Revision: *"L1 sign fact regenerated → rows replaced; `root_fact_ids` new … fails when stale ids"*. | Delete-then-insert is real (`writer.py:54,:184`) and 670 (e) resolves ids. But "new ids" presumes an L1 rebuild regenerates `chart_facts.fact_id` (TEXT PK, `platform/supabase/migrations/204_chart_facts.sql:11`); whether `ga_positions` derives ids deterministically was not verified here. If deterministic, the expected "new ids" cannot occur and the row reduces to 670 (e). | State which; if ids are stable, the Revision invariant is "rows replaced, ids re-resolved, natal indices equal to the regenerated fact values". |

## 3. Citations verified

All read with `sed -n` / `grep -n` at HEAD 3387c9ac3 (identical to 9feac52d7 for every file below).

| citation (as in brief) | resolves | note |
|---|---|---|
| `logic.py:8-19` namesake ruling | yes | |
| `logic.py:22` "Per Maharishi Parashara…" | yes (`:21`) | off by one |
| `logic.py:27-33` sub-daśā out of scope | yes (`:26-33`) | |
| `logic.py:41-46` "not a true … solar-return instant" | yes | |
| `logic.py:57` `DEFAULT_MAX_VARSHA_YEAR = 120` | yes | |
| `logic.py:60-66` `varsha_year_for_date` raises before birth | yes | no production caller (F-12) |
| `logic.py:69-74` / `:70-77` offset rule | yes (`:69-74`) | |
| `logic.py:77-80` `progressed_sign_index` | yes | |
| `logic.py:84-92` `varsha_window` | yes (`:83-91`) | |
| `logic.py:117` `jl == cl == sl` | yes | per-chart constant (F-01) |
| `writer.py:19-22` SIGNS from engine | yes (`:20-23,:34`) | |
| `writer.py:44-52` natal facts SQL | yes (`:44,:46-52`) | |
| `writer.py:54`, `:184` DELETE | yes | |
| `writer.py:57-70` INSERT `ON CONFLICT DO NOTHING` | yes | |
| `writer.py:80-95` fetch + honest None | yes (`:81-95`) | |
| `writer.py:98-112` `datetime_iso[:10]` | yes (`:98-113`) | |
| `writer.py:124-136` refusals | partly (`:130-142`) | |
| `writer.py:140-165` 120 rows | partly (`:148-172`) | |
| `writer.py:157-160` fact ids carried | no — `:157-160` are natal indices; fact ids `:168-170` | F-19 |
| `writer.py:184-187` | yes | |
| `521:32-56`, `:56` window check | yes (`platform/supabase/migrations/521_…`) | path prefix missing |
| `670:1221-1320` (a)–(h) | yes (`platform/migrations/670_…:1221-1318`) | (c) comment records the constant flag |
| `query_sudarshana_varsha.ts:26` `MAX_LIMIT` | yes | |
| `query_sudarshana_varsha.ts:71` `new Date()` default | yes | same default at `now.ts:1643` (F-03) |
| `now.ts:466-480` item 17 | partly (`:469-495`) | |
| `w27_annual_stack.py:10,31,75,82` | yes | row shape `:436-460` (F-11) |
| `context.py` no `sudarshana` read | yes (grep: none; `ClassContext` `:114-186`) | |
| `asset_registry_seed.ts:2498-2513` | yes (`:2498-2514`) | `estimated_seconds: null`, not cited by brief |
| `ka_taranga.py:92` `charts.timezone_id` | yes | fallback after `tz_offset_hours` `:85-88` |
| `BINDING:30-32` | yes (inclusivity `:30`, time_basis `:31`, claim_grain `:32`) | |
| `STRATEGY:91` Clock interval | yes | |
| STRAT A10 | yes (`STRATEGY:280`) | |
| blueprint `:324`, `:396`, `:906` | yes | `:906` says `precision_regime` (stale) |
| Register §5 row | yes (`…CONTRIBUTION_REGISTER_v2_0.md:145`) | |
| `source_query_availability.ts` | yes (`:2944-2961`) | |
| `birth_params.py` (brief: not read) | read here: `:96,:106,:109` | naive local; no UTC (F-10) |
| `KALA_BASELINE_v1_0.md` | does not exist | F-14 |

## 4. What I could not verify

- Any live database state: the 670 contract's production result, the 120-row count, `asset_throughput` — no DB access (brief marks these [A]; consistent).
- No test was run (read-only); the unit-test file contents were read, not executed.
- Whether the L0 classical corpus holds a verse-grain Sudarśana-cakra source (no DB; the only repository mention of a citation is the w27c yaml string, F-11).
- Whether `ga_positions` regenerates `chart_facts.fact_id` on rebuild (bears on F-21).
- Gochara branches other than this base (brief §11.4 — same limitation).
- Node's IANA-zone date formatting on the deployed runtime (the chart-zone `as_of` proposal is implementable with `charts.timezone_id`, which the TS registry already reads from `charts` at `chart_header.ts:78`, `get_tajik.ts:187`, `get_graha_yuddha.ts:148`; the runtime's ICU availability was not checked).

## 5. Conformance to the binding

- **Non-conformant names:** `root` (singular) and `root_fact_ids` in `independence_group` — B4 requires `roots[]`, plus `group_id` and `family`, as an array of groups (F-02).
- **Two values for one B2 field:** `source_qualification` assigned to both the window (`algorithmic_approximation`) and the rule (`verse_cited`/`unsourced`) on one row (F-16).
- **B3 `window_ref`:** `id` is a natural-key tuple, not the content-addressed sha256; `generation` (`FORMULA_VERSION`) is not in the PK (F-17).
- **B1 `time_basis`:** `null` pending a ruling — B1 says declared on every row; the proposed enum value is at the wrong level (F-09).
- **Declared asset-local fields** (`convergence_basis`, `approximates`, `root_fact_ids`) — permitted by the binding's alias rule when the mapping is declared; `root_fact_ids` should fold into `roots[]`.
- **Conformant:** `inclusivity='closed_open'`, `claim_grain='date_grain'`, `declared_current_count`, `completeness_state='applied'`, `coverage` (all seven B5 keys), `epistemic_class` values `COMPUTED_FACT_CONFIGURATION` / `QUALIFIED_RULE` (Foundation `:64-65`).
