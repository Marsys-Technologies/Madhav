---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_TITHI_PRAVESHA
version: "1.1"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_TITHI_PRAVESHA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.1")
prior_review: briefs/reviews/REVIEW_KA_TITHI_PRAVESHA_v1_0.md (REWORK, 25 findings)
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD a99300bb7; the brief's source_revision 9feac52d7 is an ancestor and `git diff --stat 9feac52d7 HEAD` on services/ka_tithi_pravesha/, query_tithi_pravesha.ts and kala_views/now.ts is empty, so every code citation checked at HEAD holds at 9feac52d7"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no git write; no DB (local 5432 not running, the postgres MCP endpoint 127.0.0.1:5433 refused); no external service"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md as written. Every v1.0 finding re-verified at source against v1.1 (§12 of the brief was not trusted). Then a fresh hunt on the equivalence contract (re-derived from writer.py → compute.py/positions.py → PyJHora drik.py:1773), the Irrelevant-control row (knife-edge quantified from logic.py's own kernel), the 03:00 IST Boundary, the Delivery sentinel, the B3 declination, the `to_char … OF` serving render (Node parse tested; Postgres semantics from formatting.c, not run), the writer-digest regeneration (generator + CI check read), and the w27b registry facts (both registry files read at the cited lines)."
---

# Review — `KA_TITHI_PRAVESHA_ELEVATION_BRIEF` v1.1

## 1. Verdict

**ACCEPT_WITH_CORRECTIONS.** The diagnosis is now right and fully grounded (the cast is computed at the correct absolute instant; only `window_start/window_end` are mis-asserted and the served render strips the offset), and 17 of the 25 v1.0 findings are resolved at source (8 partial, none unresolved) — but four correctable defects remain that must be fixed before stage 3: the rewritten Irrelevant-control row still carries an expected value a *correct* implementation would fail (the knife-edge is ~1 flip per 120-year chart, not measure-zero, and the bisection's 30 s stop makes "every computed column identical" false by construction); one of the two serving-render forms the brief recommends (`AT TIME ZONE` + `OF`) re-creates the very defect class being fixed, and the other (`SSOF`) emits `+00` in a UTC session, which V8 rejects; the w27b registry facts are contradicted at the cited lines (`mechanism_register.yaml:219` says `admitted`, `fitted`, with an evidence link — not `candidate`); and §7 has no Context/missingness row (the "eleven" is reached by a second Positive row). None requires re-analysis; each correction is specified below.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| N-01 | **MAJOR** | §7 Irrelevant control: relabel +05:30 → +00:00 *"under a correct implementation"* → *"the nearest return and the cast are unchanged … barring a measure-zero knife-edge; every computed column identical"*; §11.5 *"measure-zero knife-edge"*. | The kernel is `logic.py:168-173`: `days_est = ang_diff(natal, m0)/13.176` with `m0` evaluated at the anniversary; a +5.5 h relabel moves the anniversary's absolute instant by 5.5 h, so `m0` advances ≈3.02° and the estimate wraps to the *other* return whenever `diff0 ∈ (−180°, −177°]` — probability 3.02/360 = **0.84 % per year, ≈1.0 expected flip per 120-row chart** (computed here from the writer's own constant). Not measure-zero. Second, for the non-flipping years the bisection stops at `BISECTION_TIME_TOL = 30 s` (`logic.py:90,:194-206`) from a different starting bracket (`estimate_offset_days` shifts by ≈ −0.229 d), so the converged `instant` differs by up to 30 s: Moon ≤ 0.0046°, **lagna ≤ 0.125°** (`pravesha_lagna_degree` is stored at 4 dp, `writer.py:221`), and `ephemeris_audit_jsonb.{estimate_offset_days, iterations, diff_deg}` differ by construction (`logic.py:210-217`). *"Every computed column identical"* fails a correct implementation on essentially every row. | Run the control on **one fixture year** whose `estimate_offset_days` is far from ±13.66 (year 1: the writer test shows d ≈ 0, `test_ka_tithi_pravesha_writer.py:196`). Expected: stored UTC instant within `BISECTION_TIME_TOL`; Moon within `LUNAR_RETURN_TOL_DEG`; lagna within 0.13°; `estimate_offset_days` shifted by ≈ −0.229 d and otherwise excluded from the identity check; `verification_pass_status` unchanged. Replace "measure-zero" in §7 and §11.5 with the 0.84 %/yr figure. The cosmetic-field half of the row (`place_name`, `subject_label`) can keep "identical". |
| N-02 | **MAJOR** | §4.4 / §7 Delivery: serve the offset via `to_char(… 'YYYY-MM-DD"T"HH24:MI:SSOF')` **or** `AT TIME ZONE` with the declared zone. | (a) `OF` renders in the **session** TimeZone and, per PostgreSQL's `formatting.c` `DCH_OF`, prints minutes only when non-zero: a UTC session yields `2026-02-28T21:30:00+00`. Tested here in Node 24: `new Date("2026-03-01T03:00:00+00")` → **Invalid Date** (`+05:30`, `+0530` parse fine). (b) `x AT TIME ZONE 'Asia/Kolkata'` returns `timestamp without time zone`; `OF` on that prints `+00` — IST digits labelled UTC, i.e. the defect class the brief is repairing, re-created on the serving path. Neither form is what the brief wants. The pool sets only `statement_timeout` (`platform/src/lib/db/client.ts:35`), so the session tz is the server default, unverified. Could not run SQL (no DB); the `OF` semantics are from source, the Node parse is measured. | Recommend one unambiguous form: `to_char(window_start AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')` (session-independent, ISO-parseable) or `…SSTZH:TZM` if a session-tz render is wanted; forbid `AT TIME ZONE` + `OF`. Keep the Delivery detector's "compare instants, not strings". |
| N-03 | **MAJOR** | §1 w27b row, §2.3, §10.3: *"a registered candidate (`admission_state: candidate`)"* citing `mechanism_register.yaml:215-223`; *"two repository records disagree"* (§4.6, §10.2). | `mechanisms/registry/w27b_tithi_pravesha.yaml:14` = `admission_state: candidate`, `:15-16` null evidence/ruling, `:6` citation "… Sudarśana Cakra Paddhati". But **`mechanism_register.yaml:219` = `admission_state: admitted`**, `:227` `uncited_extension: false`, `:228` `evidence_link: "W4.3 ablation runner … W4.4 weight fitting"`, `:229` `weight_type: fitted`, `:224-226` citation "… **BPHS Tithi Pravesha chapter** contextualizes domain tone". So the cited range contradicts the brief's "candidate"; the register asserts an *admitted, fitted, evidenced* mechanism whose data source nothing populates (`context.py:114` `ClassContext` has no `tithi_pravesha_rows`; the only definition is the test stub `mechanisms/tests/test_w27_annual_stack.py:96`). Three citation records disagree (writer `not_in_corpus`; yaml Sudarśana Cakra Paddhati; register BPHS chapter), not two. | Restate: "candidate in the per-mechanism yaml, **admitted/fitted** in `mechanism_register.yaml:219,:229` with an evidence link to an ablation that cannot have run on real rows". The D-M lane adjudicates three records. §10.3's "Gochara's call" must name the register inconsistency explicitly, since an `admitted` state is the M-4 operand-audit bar already asserted as passed. |
| N-04 | **MAJOR** | §7 has *"eleven rows incl. Evaluation"* (§12 F-14 "accepted"). | Contract §6 (`…BRIEF_CONTRACT_v1_0.md:105-115`) and guide §7 (`:129`) enumerate Positive · Negative · Relevant · Irrelevant · Duplication · **Context/missingness** · Boundary · Delivery · Revision · Value · Evaluation. §7 reaches eleven with **two Positive rows** and has **no Context/missingness row** — the row where "wrong chart/generation isolates; zero/absent/unavailable/unqualified remain distinct" lives, i.e. exactly where §4.11 "Missing: a non-converged year → `unavailable`, not a silent gap" and §4.5's F06 mapping belong. | Add Context/missingness: fixture = one non-converging year (`lunar_return` never raises, `logic.py:139-142`; force `moon_longitude_fn` → None) → `completeness_state='unavailable'` with `reason`, `start_converged=false`, no row dropped, 670(h) contiguity still holds; detector fails when the year is absent or stamped `applied`. Keep the second Positive row as a sub-row. |
| N-05 | MINOR | §4.3 / §10.1: *"`window_start`/`window_end` move by exactly `−tz_offset_hours`; nothing else changes"*. | Stated relative to today's rows, which presumes the builder session TimeZone was UTC when the naive `instant` was inserted (`writer.py:215-216` → `_INSERT_SQL:90`) — §3 non-claim and §11.1 both say that is unverified (no `SET TIME ZONE` in `pipeline/orchestrator/*.py`, only `statement_timeout`). Under a non-UTC builder session the delta is not −5.5 h. Separately, the contract holds only if the engine keeps receiving the **naive local wall clock**: `compute.py:30-33` strips tzinfo, so a UTC-aware `instant` passed to `_annual_chart` (`:149-156`) would be cast 5.5 h wrong (an offset-aware `+05:30` instant would be safe). The brief says "convert to UTC before the INSERT" without pinning where. | State the contract absolutely: `window_start = (naive instant − tz_offset_hours) AS UTC`, verified against a recomputation, with "−5.5 h vs today's production rows" as the corollary under the UTC-session assumption. Pin the conversion at the row-dict boundary (`:215-216`), applied identically to `instant` and `next_instant` so 670(a) tiling (`670:1424-1431`) still holds; `_moon_longitude`, `lunar_return` and `_annual_chart` continue to receive naive local wall clock. |
| N-06 | MINOR | §4.4 *"`AT TIME ZONE` with the declared zone"*; §7 Boundary *"the served local date"*; B1 tz-source rule *"on the serving path"*. | No carrier for the zone exists on the serving path: `kala_tithi_pravesha` has no tz column (`531:44-72`), `query_tithi_pravesha.ts` joins nothing (`grep charts|timezone` → only the `$4::timestamptz` cast at `:90`), and §5's migration is described only as "qualification columns". `charts.timezone_id` exists (`0001_brahma_baseline.sql:1598`) but is not read here. Also unresolved: the engine used the **fixed birth offset** (`birth_params.py:109`, ZoneInfo at birth) for all 120 years; an IANA-zone render would differ from that wall clock in DST seasons for non-Indian charts. | Name the carrier: either persist `tz_offset_hours` (and/or `timezone_id`) on the row in the additive migration, or have the packet join `charts.timezone_id`. Say which zone "declared" means (fixed birth offset = what the cast used; IANA = civil time) and note they coincide for the canonical chart. |
| N-07 | MINOR | §7 Delivery sentinel alternative (b): *"`independence_group[0].roots[0]` = the real `moon_fact_id`"* as *"a per-row value the wrapper cannot synthesise"*. | `moon_fact_id` is already a served column (`query_tithi_pravesha.ts:88`); a wrapper can build `roots[0]` from it without the new `independence_group` column ever propagating. Alternative (a) (`completeness_state='unavailable'` on a seeded year) does meet the criterion — `now.ts:649-657` picks named fields, so nothing new reaches `kala_now_get` without the Pūrṇa packet, which the brief correctly scopes as L3-owns-the-test-only. | Keep (a) only, or make (b) a value not derivable from any existing column (e.g. `independence_group[0].group_id`, a content hash the wrapper cannot guess). |
| N-08 | MINOR | §7 Relevant influence: *"perturb the natal Moon longitude by +13.176° → `t_start` moves ≈1 day"*. | Same wrap as N-01: `diff0 + 13.176°` crosses +180° for 13.176/360 = 3.7 % of years (≈4 of 120), where the nearest return becomes the *previous* one and `t_start` moves ≈ −26.3 d, not +1 d. The detector ("unchanged") still fires correctly; the expected value is wrong on those rows. | Pin to one fixture year with small `estimate_offset_days`, or state "+1 d, or −26.3 d where the wrap occurs". |
| N-09 | MINOR | §2.1 *"Latent-value classes (template §2.1): (a) served and correct; (b) served and mis-asserted; (c) computed, unserved; (d) absent"*. | Template §2.1 (`…TEMPLATE_v2_0.md:47-50`) defines the four as **(a) persisted but discarded by consumers; (b) computed but discarded before persistence; (c) genuine source/computation gap; (d) available but unqualified for the intended use** — "these need four different fixes". The brief re-labelled (a)–(d) with different meanings, so F-16 is only partly closed. | Re-map: `window_start/end` → (d); the verification status → (d) (served at `now.ts:655`, so not (a)); a source adjudication → (c); nothing here is (b). |
| N-10 | MINOR | §5 *"Tests that must flip: `test_ka_tithi_pravesha_writer.py:117` (the naive-equality pin), `:196`, `:205`, `test_w2_first_frontier_writer_preservation.py:289`"*. | `:117` is `assert result is not None`; the naive-equality pin is **`:119`** (`birth_dt == datetime(1984, 2, 5, 10, 43, 0)`) — and it need not flip if `_birth_dt_and_params_from_config` keeps returning the naive wall clock (N-05 says it should). `:196` and `:205` (`row["window_start"] - birth_dt`) flip only if `_compute_one_year` returns an aware `window_start` — conditional on N-05's placement. `:289` stubs `birth_params={}` and replaces `_compute_one_year` wholesale (`:292-296`), so it flips **only** if `run()` itself reads `tz_offset_hours`. | List the tests as *conditional on where the conversion lands*, with the correct line (`:119`), rather than as a fixed flip set. |
| N-11 | NOTE | §2.2 *"`DEFAULT_MAX_PRAVESHA_YEAR = 120` (`logic.py:81`)"*. | `logic.py:82`. | `:82`. |
| N-12 | NOTE | §1 *"Register §5 (`REGISTER:146`) — annual praveśa frame"*. | Resolves to `MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md:146`: *"Return bracketing, annual-chart and numerical search kernels. — P/Q/E: named method/equation needs source adjudication; current Moon-longitude return is not accepted merely from name. DP02/07."* Not "annual praveśa frame"; the file is never named. | Name the file and quote it — it is the strongest existing warrant for §4.6/§10.2. |
| N-13 | NOTE | §4.9 / §7 *"B3: not offered — rows are addressed by the natural key"*; §4.2 *"`t_start/t_end` aliasing `window_start/window_end` for one generation"*. | B3 (`BINDING:53-56`) requires a content-addressed id (never a bigint surrogate) and `generation` on every row; the table's PK is `id BIGSERIAL` (`531:45`) and there is no `generation` column. Declining B3 while phrasing the alias "for one generation" borrows B3's vocabulary. The declination itself is legitimate (no cross-asset citation yet) and is stated, not faked. | Say: "B3 not offered: surrogate PK retained, no `generation` column; the `t_start/t_end` alias is a naming alias. B3 becomes due the moment the SC-6 voice (§10.3) cites a praveśa window." |
| N-14 | NOTE | §7 column *"command / fixture"*. | Every cell is a fixture description; no row names a command (pytest path/marker, the ablation script) as guide §7 (`:129`) requires. | Add the command per row (e.g. `pytest tests/l3/test_ka_tithi_pravesha_writer.py -k …`; the ablation as a named script under the evidence path). |
| N-15 | NOTE | §5 *"the code digest … is regenerated as part of the packet"*. | Accurate, and citable harder: generator `pipeline/orchestrator/provenance_inventory.py:23-33` (`get_writer_source_hash`), `:40-58` (`--check` mode), CI enforces currency at `.github/workflows/ci.yml:1409`; the hashed closure follows local imports (`asset_runner.py:367-401`), so a `logic.py` change also changes the digest. | Cite those; state that a `logic.py` edit regenerates too. |
| N-16 | NOTE | §4.13 / §7 Value: ablation *"re-evaluate the Moon at each stored instant read as UTC: today ~3° from natal"*. | Correct under the UTC-builder-session assumption (N-05). If the builder session was not UTC the "today" leg reads differently; the "after" leg (within `LUNAR_RETURN_TOL_DEG`) is unconditional. | Add "today's figure presumes the UTC builder session context §4 measured". |

Not findings, re-confirmed at source for v1.1: the correct-instant chain (`writer.py:130-146` builds `jd` from the naive wall clock and passes `tz=float(bp["tz_offset_hours"])` at `:141`; `:149-156` passes `birth_params` with the naive `isoformat()`; `compute.py:30-33` strips tzinfo, `:58` reads `tz_offset_hours`, `:63-66` builds `jd_ut` from the local wall clock; `positions.py:54` `_place(lat, lon, tz)`; PyJHora `drik.py:1773` `jd_utc = jd - place.timezone / 24.`) — so the cast columns are computed at the correct absolute instant and the §4.3 contract (cast byte-identical under the production-shape fixture, provided N-05's placement) is sound; the Boundary fixture (03:00 IST = 21:30 UTC previous day) is arithmetically right and the assertion "both correct under their declared zones" is the right shape (subject to N-06 for the zone carrier); the B3 declination is stated not faked; the production input shape (`birth_params.py:33,:96-111`: naive `datetime_iso`, `tz_offset_hours` from `timezone_id`, `place_name`/`subject_label` cosmetic); `raw[:19]` at `:125` discards nothing on that shape; the ~3° figure (5.5 h × 0.549°/h = 3.02°; 2.70–3.53° across 11.8–15.4°/day); `fa9857f00` (#2607) touches `services/ka_tithi_pravesha/writer.py` and is the `git log -S` hit for the prepare-before-DELETE comment; `793972c754…` is a commit; migration 670 `:1413-1515` conjuncts (a)–(i) as listed in §1 and its (b) at `:1437-1442` re-derives from `ephemeris_audit_jsonb` and so cannot see the tz assertion; Q09 cannot be served (`STRATEGY:70`; one convention `writer.py:61-62`); B5 shape matches `BINDING:69`; B4 shape matches `BINDING:62`; F06 six values (`FOUNDATION:33,:72`); in-tree migration max 1072 (`supabase/migrations`) / 1070 (`platform/migrations`); corpus figure 16 texts / 10,651 at `MADHAV_PRODUCT_DEFINITION_v2_0_PROPOSAL.md:277,368`; seed `:2530-2544`; `now.ts:613-660`, `:2040-2049`; `query_tithi_pravesha.ts:70,:83-90,:90`; `context.py:114`; `w27_annual_stack.py:74,:306-372`; no `ph_*`/`mi_*`/`ka_*` writer reads the table; elevation plan Q2 `:188`; blueprint `:285,:289,:323,:325,:667,:743,:975`; D-M `:411-417`; Lane D `:655-676`; T1 `:305`; context §4 `:96-99`. No confidence scalar invented; no question outside L3-Q; no `kala_views` edit beyond a packet; no orchestrator change; no `estimated_seconds` cited; the simpler baseline is today's rows; no live incidence claimed beyond context §4 [A]; the receipt discipline (§4.6, §11.2) is honest.

## 3. Citations verified

| citation (as in brief v1.1) | resolved? | what is there |
|---|---|---|
| `source_revision 9feac52d7`; diff to `e82dd34a3` and to HEAD empty on the three files | yes | ancestor of a99300bb7; both diffs empty |
| `writer.py:60-70`, `:61`, `:62` | yes | constants; `ENGINE_AYANAMSHA = "lahiri"` at `:62` |
| `writer.py:72-77` | yes | `_FETCH_NATAL_MOON_SQL` by `fact_id` |
| `writer.py:79`, `:81-98`, `:97` | yes | DELETE; INSERT incl. `window_start, window_end`; `ON CONFLICT … DO NOTHING` |
| `writer.py:116-127`, `:125` | yes | `datetime.fromisoformat(raw[:19])` |
| `writer.py:130-146`, `:141` | yes | `_moon_longitude`; `tz=float(bp["tz_offset_hours"])` |
| `writer.py:149-156` | yes | `_annual_chart` passes `instant.replace(microsecond=0).isoformat()` |
| `writer.py:181-231`, `:201-211`, `:206-211`, `:215-216` | yes | `_compute_one_year`; two-pass; naive `window_start/end` |
| `writer.py:240-303`, `:249-253`, `:281-294` | yes | `run`; honest no-op; prepare-before-DELETE |
| `writer.py:31-35` | yes | citation-honesty docstring |
| `logic.py:67`, `:73`, `:81`, `:102-109`, `:121-142`, `:139` | `:81` **no** (constant is at `:82`); others yes | `MEAN_MOON_SPEED_DEG_PER_DAY = 13.176`; `LUNAR_RETURN_TOL_DEG = 0.01`; `pravesha_anniversary`; `lunar_return`, "never raises" at `:139` |
| `birth_params.py:33`, `:96-106`, `:97-101`, `:109`, `:110-111` | yes | `_REQUIRED`; naive `datetime_iso` (`:106`); `tz_offset_hours` (`:109`); cosmetic fields |
| `pyjhora_adapter/compute.py:30-33`, `:58-66` | yes | strips tzinfo; `tz_offset_hours`; local `jd_ut` |
| PyJHora `drik.py:1773` | yes (`.venv/.../jhora/panchanga/drik.py:1773`) | `jd_utc = jd - place.timezone / 24.` |
| `test_ka_tithi_pravesha_writer.py:117`, `:189-196`, `:196`, `:205` | `:117` **partly** (naive pin is `:119`); others yes | see N-10 |
| `test_w2_first_frontier_writer_preservation.py:289` | yes (`:287-299` is the test) | conditional flip, N-10 |
| `531:44-72`, `:49-50`, `:60`, `:63` | yes | DDL; TIMESTAMPTZ; CHECK; `computed_at` |
| `platform/migrations/670_…:1413-1515` | yes | contract (a)–(i) |
| `query_tithi_pravesha.ts:70`, `:83-90`, `:84-85`, `:88`, `:90`, `:83-94` | yes | `as_of` default; `to_char` without offset; `moon_fact_id` served; `$4::timestamptz` |
| `now.ts:613-660`, `:2040-2049` | yes | fetch + coverage |
| `asset_registry_seed.ts:2530-2544`, `:2537`, `:2544` | yes (`platform/scripts/seed/asset_registry_seed.ts`) | seed row |
| `w27_annual_stack.py:74`, `:306-372` | yes | `MECHANISM_ID_B`; `compute_tithi_pravesha` |
| `mechanisms/registry/w27b_tithi_pravesha.yaml` | yes | `candidate`; `cited`; 1.25/0.80 |
| `mechanism_register.yaml:215-223` | resolves, **contradicts** the brief (N-03) | `admission_state: admitted` at `:219`; `fitted` `:229`; evidence link `:228` |
| `grammar_v3_registry.yaml:212-214` | yes | w27b `data_source: ClassContext.tithi_pravesha_rows` |
| `context.py:114` | yes | `ClassContext` dataclass; no `tithi_pravesha_rows` field (repo grep: only w27 + its test stub `:96`) |
| `engine.py` has no w27 reference | yes | grep empty |
| `nirmana-writer-digests.json:100`; `dispatch_frozen_rebuild.py:29,58,113` | yes | `ka_tithi_pravesha` digest; path constant; loader; consumer |
| `REGISTER:146` | resolves (file unnamed) | `MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md:146` — kernels + "needs source adjudication" (N-12) |
| `STRATEGY:70`, `:91`, `:279` | yes | Q09 proof; Clock interval; L3-A09 |
| `KALA_DELEGATED_DECISIONS:411-417` | yes | D-M |
| elevation plan Q2 `:188` | yes (`MADHAV_L3_ASSET_ELEVATION_PLAN_v1_0.md`) | three assets consumed by nothing |
| blueprint `:285`, `:289`, `:323`, `:325`, `:667`, `:975`; §12.1 `clocks_consulted` | yes (`:743`, `:761`) | SC-6; SC-10; row 7; row 9; Q2; N-8 pointer; Clock-interval coverage names |
| Lane D `:655-676`; T1 `:305` | yes | 120 rows, `lit`, 2026-09-07 [A]; frontier row |
| context §4 (5.5 h) | yes (`:96-99`) | [A] |
| `MADHAV_PRODUCT_DEFINITION_v2_0_PROPOSAL.md:277,368` | yes | 16 texts / 10,651 chunks |
| `KALA_BASELINE_v1_0.md`, `KALA_COST_PROFILE_v1_0.md` absent | yes | only `KALA_BASELINE_FREEZE_EXECUTION_PROMPT_v1_0.md` exists |
| `fa9857f00` (#2607); `47131772b` not the writer | yes | writer.py in fa9857f00's stat; `git log -S` hit |
| `793972c754…` is a commit | yes | `git cat-file -t` → commit |
| in-tree migration max 1072 | yes | `1072_kala_convergence_episodes.sql` |
| no `SET TIME ZONE` in `pipeline/orchestrator/*.py` | yes | grep: only `birth_params.py` tz derivation and `asset_runner.py:150` UTC stamping |
| `BINDING:28,:33,:62,:69`; B3 `:53-56`; `FOUNDATION:33,:72` | yes | B1 tz source; B4; B5; B3; F06 |
| `TEMPLATE_v2_0.md:47-50`, `:81-92`; guide `:129`; contract `:105-115` | yes | latent-value classes (N-09); walkthrough; knowledge time; eleven rows (N-04) |
| `provenance_inventory.py:23-58`; `ci.yml:1409`; `asset_runner.py:367-413` (not cited) | yes | digest generator, CI `--check`, hashed closure (N-15) |

## 4. What I could not verify, and why

- Anything requiring a database: the 5.5 h production figure, the 120-row count, `asset_throughput = lit`, whether any row is `divergent_flagged`, the builder connection's session TimeZone, the serving pool's session TimeZone, and the corpus predicate — local Postgres is not running and the postgres MCP endpoint (127.0.0.1:5433) refused the connection.
- The Postgres `OF` / `AT TIME ZONE` rendering in N-02 is stated from PostgreSQL source semantics (`formatting.c` `DCH_OF`: sign + two-digit hours, `:MM` only when non-zero; a `timestamp without time zone` has zero gmtoff → `+00`), not executed. The Node parse result (`+00` → Invalid Date; `+05:30` valid) was executed here (Node 24, tz Asia/Calcutta).
- The knife-edge and tolerance figures in N-01/N-08 are computed from the writer's own constants (mean speed 13.176°/day, 30 s stop); the true-speed range widens the per-year probability by ±15 %. Not run against the engine.
- Whether the "ingestion work item" the constant (`writer.py:69`) says is filed exists — no tracker record in the worktree.
- End-to-end timing — no `KALA_COST_PROFILE_v1_0.md` on this base; the brief labels the docstring figure correctly.
- No test was run; nothing was written to the repository except this file.

## 5. Conformance to the binding

Fields used in the B1–B7 vocabulary and correctly: `t_start`/`t_end` (declared alias to `window_start/end`), `inclusivity='closed_open'`, `time_basis='event_instant'`, `claim_grain='instant_grain'` (B1); `completeness_state` ∈ the six F06 values with `reason`, `owner`, `evidence_ref`, `next_eligible_action`, `epistemic_class`, `source_qualification='unsourced'`, `corpus_verifiable`, `comparable_with='different_convention'`, `operator_role='computation'` (B2); `independence_group` as `[{group_id, family, roots[], members[], basis:'declared_lineage'}]` with `declared_current_count` (B4); `coverage` with the seven B5 keys plus the blueprint §12.1 `clocks_consulted`/`clocks_unavailable` for the served projection (B5); B3 declined with a stated reason (allowed, but see N-13 on borrowing "generation").

Non-conformant or under-specified:
- B1 **tz source on the serving path**: named as a rule but with no carrier for the zone on the row or in the query (N-06).
- B1 **resolver** row (`BINDING:34`: every stream imports `services/ka_temporal/date_resolver` for date↔instant; "no private conversion") — the brief proposes a writer-local naive→UTC conversion and does not mention the shared resolver; state whether the fixed-offset conversion goes through it or why it cannot (the resolver's `date.today()` defect is context §4's own example).
- No field is named outside the B1–B7 vocabulary.

## 6. Disposition of the v1.0 findings against v1.1 (verified at source, not from §12)

| v1.0 id | status | where / why |
|---|---|---|
| F-01 | **RESOLVED** | §0, §2.4, §3, §4.3, §7 Positive — the correct-instant chain re-verified end to end (`writer.py:141,:154` → `compute.py:33,:58-66` → `drik.py:1773`); impact restated; equivalence inverted. (N-05 refines its wording.) |
| F-02 | **PARTIAL** | Relevant-influence now perturbs the natal Moon (right); the tz relabel moved to Irrelevant control — but that row's expected value fails a correct implementation (N-01), and the +13.176° row's "≈1 day" wraps on ~3.7 % of years (N-08). |
| F-03 | **RESOLVED** | §4.11, §7 Boundary — 03:00 IST = 21:30 UTC previous day; assertion reshaped correctly. The zone carrier for "served local date" is a new gap (N-06), not a regression of F-03. |
| F-04 | **PARTIAL** | §7 Delivery — sentinel (a) is per-row and non-synthesisable; sentinel (b) is derivable from the already-served `moon_fact_id` (N-07). |
| F-05 | **PARTIAL** | w27b added to §1/§2.3/§4.6/§10.2-3 with the unwired-populator evidence (`context.py:114`) — but the register's state is mis-stated at the cited lines (`admitted`, `fitted`, evidence link) and a third citation string exists (N-03). |
| F-06 | **RESOLVED** | §4.1 — Q09 cannot be served; Q05 partial and conditional; Q06 stands. |
| F-07 | **RESOLVED** | §4.8 — the seven B5 keys, plus `clocks_consulted`/`clocks_unavailable` for the served projection. |
| F-08 | **RESOLVED** | §1, §2.2, §4.2, §7 Positive/Negative — production shape (naive + `tz_offset_hours`), "attach, don't un-discard", Negative as a unit-level guard (`_REQUIRED:33`). |
| F-09 | **RESOLVED** | ≈3°, range 2.7–3.5° (re-computed: 3.02°, 2.70–3.53°). |
| F-10 | **RESOLVED** | `fa9857f00` (#2607) — confirmed in that commit's stat and by `git log -S`. |
| F-11 | **RESOLVED** | §7 Duplication — row-level invariant; served test gated on SC-6/IP-5. |
| F-12 | **PARTIAL** | §9 states the fixture is pending the freeze (`KALA_BASELINE_v1_0.md` absent — confirmed); no specific ordinary praveśa year is named for the ordinary-period case. |
| F-13 | **RESOLVED** | §7 Irrelevant control — cosmetic `birth_params` fields (`birth_params.py:110-111`; `compute.py:59-60` reads them only as labels). |
| F-14 | **PARTIAL** | Evaluation row and the two columns added; but Context/missingness is missing and the "command" column carries no commands (N-04, N-14). |
| F-15 | **RESOLVED** | §4.7 — `group_id`, `family`, `roots[]`, `members[]`, `basis`, `declared_current_count`. |
| F-16 | **PARTIAL** | §2.1 knowledge-time pin (`531:63`) and §9 experience-4 walkthrough present; the (a)–(d) latent-value classes are not the template's four (N-09). |
| F-17 | **PARTIAL** | The serving half is named everywhere it should be (§0, §2.3, §3, §4.4, §7 Delivery, header) and the detector compares instants — but both recommended render forms are defective as written (N-02). |
| F-18 | **RESOLVED** | §5 — next free slot verified at execution; in-tree max 1072 confirmed. |
| F-19 | **RESOLVED** | §2.2, §4.10, §10.4 — the `'lahiri'` literal at `:62` is in scope; decision to resolve via the alias map. |
| F-20 | **RESOLVED** | §2.2, §2.4 — "can", with "does" listed under §11.1 as unverified. |
| F-21 | **RESOLVED** | §4.6, §11.2 — both absence claims marked unreceipted; the v1.0 claim withdrawn. |
| F-22 | **RESOLVED** | §2.3 cites `context.py:114` and the grep; N-8 pointed at blueprint `:323,:325,:975`. |
| F-23 | **PARTIAL** | 670's (a)–(i) and its (b) blind spot correctly stated in §1; digest regeneration in `may_touch` with its consumer named (verified, N-15 adds the generator/CI cites); the tests-that-flip list has a wrong line and two conditional entries (N-10). |
| F-24 | **RESOLVED** | header says "commit"; `git cat-file -t` → commit. |
| F-25 | **RESOLVED** | seed `:2530-2544`, verification `:201-211`, `run` `:240-303` — all tightened correctly; 16 texts / 10,651 recorded at §11.6 with the source. |

Tally: 17 RESOLVED · 8 PARTIAL (F-02, F-04, F-05, F-12, F-14, F-16, F-17, F-23 — of which F-12 and F-16 are minor) · 0 UNRESOLVED. New: 4 MAJOR (N-01–N-04), 6 MINOR (N-05–N-10), 6 NOTE (N-11–N-16).
