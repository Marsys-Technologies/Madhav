---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_MOORTI_NIRNAYA
version: "1.1"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_MOORTI_NIRNAYA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.1", source_revision 9feac52d7)
prior_review: briefs/reviews/REVIEW_KA_MOORTI_NIRNAYA_v1_0.md (REWORK; 1 BLOCKER + 5 MAJOR + 14 MINOR/NOTE)
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD e82dd34a3; `git diff 9feac52d7 e82dd34a3` is empty for services/ka_moorti_nirnaya/, pipeline/orchestrator/asset_runner.py, migrations 670/525, services/gochara_v3/, ka_kshetra/stage1_symbolization.py, brahmagyan/l0_ephemeris.py, now.ts and query_moorti_nirnaya.ts — every citation below is checked against the same code v1.1 was written against"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no network, no git write; §12 of the brief was not trusted — each v1.0 finding re-verified at source"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md as written, plus the six re-review focus items: the two guard options traced through asset_runner.py's post-writer path (integrity gate → dormant/lit decision → no-op reclassification); the lag re-derived from logic.py's run detection and l0_ephemeris.py's 12:00-UT knot; the pure-function proof row checked against today's code shape; the root-find claim checked against every ingress/bisection primitive in the sidecar; the B1 resolver claim checked against services/ka_temporal/date_resolver.py; the Gochara contradiction checked against all three mechanism registers"
---

# Re-review — `KA_MOORTI_NIRNAYA_ELEVATION_BRIEF` v1.1

## 1. Verdict

**REWORK.** v1.1 correctly disposes 15 of the 20 v1.0 findings and its account of the lag is now right in sign and size; but the option it *recommends* for the guard (§10.1 (b), "refuse with the prior partition preserved") does not produce the F06 state §0 promises — traced through `asset_runner.py`, a refusal on a previously built chart ends as `state='lit'` with `last_error=NULL`, the prior partition served as current and the `notes` reason discarded (the orchestrator never reads `WriterResult.notes`), which is the §N.8 "unearned lit" class and breaches the B5 coverage row the brief itself OFFERS; option (a) is described without its destructive consequence (a transient grant outage replaces a chart's graded partition with an ungraded one) and with a successor conjunct bound to a column the brief's own DDL entry does not add; the date-grain Boundary proof row has a false expected result (23:50 vs 00:10 UT do not move `start_date` under a noon-UT knot — 11:50 vs 12:10 do); the root-find is called "new code owned by this writer" while `pipeline/transit_search.py:506-560` already bisects sign ingresses over its own swisseph calls; the B1 `t_start/t_end` mapping ("chart-tz midnight via `date_resolver`") names a conversion that module does not contain and contradicts the brief's own `time_basis='noon_ut_knot'`; and the Gochara contradiction is mis-counted and mis-directed (two of the three registers say `candidate` — the `admitted` entry is the outlier, and the w22 docstring is stale in the *other* direction).

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| N-01 | **BLOCKER** | §4.2: *"**(b)** refuse with the prior partition preserved and the reason in `notes` — exactly the writer's existing incomplete-series behaviour (`:211-225`), no migration, no gate conflict. … **Either way** the build no longer raises an untyped exception, and the asset's own F06 state (with `reason`, `owner`, `evidence_ref`, `next_eligible_action`) is what a consumer sees instead of only `asset_throughput.last_error`"*; §10.1 recommends **(b)**; §0: *"a missing grant producing an F06 state rather than a build outage"*; §7 Negative: *"(b) a refusal with the prior partition intact and `notes` naming the reason … no post-write rollback"*; §5 PRESERVE: *"the honest whole-build refusal on an incomplete series"*. | Trace of (b) on a chart with a prior partition: the writer returns `WriterResult(rows_inserted=0, notes=…)`. `_drive_substeps` reads only `rows_inserted`/`rows_updated` (`asset_runner.py:767-768`); **`WriterResult.notes` (`writers/__init__.py:39`) has no reader anywhere in `asset_runner.py`** (grep `.notes`/`notes=`: none) — the reason is dropped. The 670 gate at `:1064-1072` runs over the *prior* partition and passes. `:1091-1098`: live `target_floor` is **72** (`670:1839`; `855:5,:41`; the seed's upsert keeps the existing value, `asset_registry_seed.ts:3349`), so `zero_rows_is_complete=False` → `final_state='dormant'` → `:1109-1164` no-op reclassification: `_data_rows_present` (`count_sql`, seed `:2567`) > 0, `has_substeps` false → `plan_complete=True` → **`final_state='lit'`, `rows_written=present`, `last_error=NULL`**, an `asset.noop_completion` event. The orchestrator's own comment at `:1111-1117` names this shape the D-1.6 "unearned lit". Downstream `ka_gochara_v3_century_materialize` (`seed:2218-2225`, `_DEP_ASSERT_MODE='enforce'`, `:44`) sees a satisfied dep. On a first build (no prior rows) → `'dormant'`, reason nowhere. So under (b) the outage is **masked, not typed**: no row, no coverage, no `last_error`, no F06 state — and the B5 rule the brief OFFERS in §7 (*"empty result: a row **and** coverage, never 'no row'"*, binding B5) is breached. Today's incomplete-series refusal (`writer.py:217-225`), listed under PRESERVE as "honest", has exactly this shape at the orchestrator level. | §10.1 must be re-decided with this trace in front of the native. (b) as "refuse silently" cannot be recommended. Honest variants: **(b′)** raise a *typed* exception → `'error'` + structured `last_error` (F06 lives on `asset_throughput`, not the asset table — the same outage as today, only named; say so); or **(a)+successor** — the only option that gives the asset's own F06 state and a B5-conformant row. Delete *"either way … the asset's own F06 state … is what a consumer sees"*; re-word the §7 Negative (b) expected result to what the orchestrator actually records; strike "honest" from the PRESERVE line or qualify it ("honest at the writer, masked at the orchestrator — see §10.1"). |
| N-02 | **MAJOR** | §4.2 (a): *"write the sign-runs with `completeness_state='unavailable'`, grade fields null … only available with a 670-successor migration that amends (f) to admit interior rows carrying `completeness_state='unavailable'`; that migration is in `may_touch`"*; §10.1: *"(a) is defensible but costs a migration and re-opens an applied contract … unless the native wants the ingress facts served without a grade"*; `may_touch[3]`: *"one additive DDL migration on kala_moorti_nirnaya (requested_horizon, grading_basis, reason, window_id)"*. | (i) Under (a) the candidate is *complete* (every run present, all ungraded), so `writer.py:305-311` DELETE+INSERT **replaces a chart's graded partition with an ungraded one on a transient grant outage**; §5 "assemble-before-delete" protects against an *empty* candidate (`:299-306`), not an ungraded one — the brief never states this cost. (ii) The successor conjunct binds to `completeness_state`; the DDL entry in `may_touch` does not add that column — an unstated ordering dependency on out-of-scope DDL (see N-09). (iii) The successor's shape is not given: it must keep (f)'s teeth for interior rows *not* marked `unavailable`, and — since `_fetch_moorti_table` is one-shot (`:196`) — a mixed partition (some graded, some `unavailable`) is impossible from this writer and should remain a violation. (iv) 525's CHECK (`:91-102`, must_not_touch) forbids `moon_nakshatra_idx_at_ingress`/`nakshatra_offset` with `moorti_computed=false`, so "(a) … the ingress facts served without a grade" = **windows only**; the Moon index, computable without the grant, cannot be stored. (v) 670's conjuncts are not chart-scoped in SQL (`_probe_asset` runs `integrity_check_sql` unparameterised, `:816-820`; (f) at `:1083-1089` correlates over the whole table) — a successor that admits persisted `unavailable` rows must be written knowing one chart's rows are visible to every chart's gate. | State (i)–(v) in §4.2/§10.1 as the real cost of (a); write the successor conjunct's shape (interior ∧ ¬computed ∧ `completeness_state<>'unavailable'` → violation; per-chart uniformity); add `completeness_state` + its CHECK to the DDL entry; correct "ingress facts served" to "windows served". |
| N-03 | **MAJOR** | §7 Boundary `[U]`: *"ingress at 23:50 UT vs 00:10 UT of the next day → the run's `start_date` and hence the keyed Moon knot shift by one day; `grading_lag` stamped in both"*. | Knots are at **12:00 UT** (`l0_ephemeris.py:271`, `:278` `swe.julday(…, 12.0)`); a run starts on the first date whose noon knot shows the new sign (`logic.py:109-113`). Ingress 23:50 UT of D: noon D old sign, noon D+1 new → `start_date=D+1`. Ingress 00:10 UT of D+1: noon D old, noon D+1 new → `start_date=D+1`. **Same `start_date`; nothing shifts.** The pair that shifts is **11:50 vs 12:10 UT of D** → `start_date` D vs D+1 (lag 0 h 10 min vs 23 h 50 min). A test written to the row as stated fails against correct code, or is "fixed" by moving the knot to midnight — the Graha Sañcāra brief's PATH-A 00:00-UT defect in reverse. Also: `detect_sign_runs` takes a daily sign series, not instants, so the fixture is a two-knot series and the row as scoped re-tests run detection (already covered by `tests/l3/test_ka_moorti_nirnaya.py`), not the lag stamp. | Fixture 11:50 / 12:10 UT of D; expected `start_date` D / D+1; assert `grading_lag` stamped and the knot used is 12:00 UT of `start_date`. |
| N-04 | **MAJOR** | §4.4 / §5 / §10.2: *"The bisection of the graha's sign crossing between noon(D−1) and noon(D) over that kernel is **new numerical code owned by this writer** (precedent: `ka_tithi_pravesha/writer.py`'s Moon-return root-find) … this brief adds **no second swisseph integration** (SC-8)"*; §4.4: *"with its bracket and tolerance declared as part of the DEMAND"*. | **`pipeline/transit_search.py:506-560` `find_ingress_events`** already bisects sign-cusp crossings (`:534-543`, 20 iterations over a `_step_size` scan), over `_get_planet_pos` (`:242-272`) — its own `swe.calc_ut` with `SIDM_LAHIRI`, `FLG_SIDEREAL`, TRUE node, Ketu = Rahu+180°; exported `:951`, served by `routers/transit_search.py`. So an ingress root-find exists, is not owned by this writer, and is already a *further* swisseph integration whose conventions (true node, `FLG_SIDEREAL`) differ from `ephemeris_daily`+`derive_sidereal` (the Graha Sañcāra brief's N1/N2 splits). "No second swisseph integration" is false at codebase level; the brief can promise not to add a third. The bracket (noon D−1, noon D] can hold three crossings for a retrograde re-entry (in/out/in); bisection returns one, unspecified. Tolerance is promised, not stated (`ka_tithi_pravesha/logic.py:69-72` pins a 30-s / degree tolerance as the precedent). | Name `find_ingress_events` in the DEMAND: reuse its bisection over the reference kernel, or state why a new one; define "the ingress" as the last crossing before the knot; state bracket, iteration cap and tolerance; re-word SC-8 to "no third integration". |
| N-05 | **MAJOR** | §4.7: *"`t_start/t_end` are declared **resolver-derived views** over the DATE key (chart-tz midnight via `services/ka_temporal/date_resolver`), not a second conversion here"*; §7 Binding OFFERS B1 *"`t_start/t_end` as declared resolver-derived views"*. | (i) `services/ka_temporal/date_resolver.py` contains **no date↔instant conversion**: its functions (`:92-674`) are graha normalisation, daśā-timeline loading and predicate→window resolution; grep `midnight|timezone|tzinfo|instant`: none. Binding B1's "resolver" row (`KALA_SYNERGY_BINDING_v1_0.md:33`) names a capability the module does not have; the brief inherits that and adds a mapping it cannot supply. (ii) The mapping **contradicts the brief's own `time_basis='noon_ut_knot'`**: the first in-sign observation is 12:00 UT of `window_start`, the ingress lies in (noon D−1, noon D]; IST midnight of D = 18:30 UT of D−1 is neither. For `window_end` E the last in-sign knot is 12:00 UT of E and the exit lies in (noon E, noon E+1] — a "midnight of E" `t_end` *precedes* the last observation in the sign. Chart-tz midnight is B1's rule for Saṅgam's calendar-day daśā DATEs (`:26`), not for a UT-knot observation. | Offer `t_start` = 12:00 UT of `window_start`, `t_end` = 12:00 UT of `window_end`, declared as knot instants (`time_basis='noon_ut_knot'`, `inclusivity='closed_closed'`), `t_exact` NULL until the DEMAND lands (B1 `:27`: "timestamptz or NULL, never a sentinel"). Raise the resolver gap to the binding's owner rather than citing it as existing. |
| N-06 | **MAJOR** | §4.8 / §10.4 / §8: *"the contradiction to hand its owner is **three-way** (`mechanism_register.yaml:87-103` says `admitted`; `context.py` prefetches; `engine.py` never invokes; the module docstring says not plumbed)"*. | Four items are listed as three, and the two registers that settle the question are omitted: **`grammar_v3_registry.yaml:52-75`** `admission_state: candidate`, `admission_ruling_id: pending`; **`mechanisms/registry/w22_moorti_nirnaya.yaml:14-15`** `candidate` / `null`; `context.py:183` (*"admission_state stays 'candidate' in its own registry entry"*); `w44_weight_fitting.py:160` w22 → `False`. So `mechanism_register.yaml:91` `admitted` is the **outlier of three registers** — the hand-off is "correct one register", not "adjudicate a four-way". And the docstring at `w22:162-163` is stale in the *opposite* direction from how the brief reads it: `context.py:186/:252/:274` **does** plumb `moorti_rows`, `w22:172` reads `getattr(context, "moorti_rows")`, and `gochara_v3/tests/test_context_moorti_wiring.py:151` exercises that end-to-end live. The only missing link is the engine call (`engine.py:100-101` imports w23/w30 only; `:632` no mūrti term). | State: plumbed (context) → readable (w22:172) → never called (engine) → registers split 1:2 on admission → docstring stale on plumbing. Hand Gochara two concrete edits (the outlier register; the stale docstring/comment `:162-171`) plus the Ch.27 citation. |
| N-07 | MINOR | §7 Grading-direction `[U]`: *"`grade(moon_lon_at_ingress, janma_idx)` as a pure function with the Moon-longitude oracle injected … detector fails when the two agree, or the instant is 'later'"*; §12: *"a pure function with an injected oracle, **runnable today**"*. | No such function exists. `nakshatra_offset_from_janma` (`logic.py:126-134`) takes an *index*; lon→idx is `int(lon // NAK_SIZE_DEG) % 27` inline at `writer.py:229`; the date-keying is `:268` inside `run()`. The row is runnable **after** the factoring in `may_touch` (`logic.py`) — a change this brief proposes, not today's shape. The detector's second clause cannot fire from code: the direction is a property of the fixture (the author chose "6 h earlier"); only "the two agree" (oracle ignored) is a code-path failure. | "Runnable once `grade()` is factored (this brief's own change)". Detector: "under `grading_basis='ingress_instant'` the stored idx ≠ the oracle's idx". Keep the one-sidedness invariant with the corrected Boundary row (N-03), where it is a property of run detection. |
| N-08 | MINOR | §0 / §3 / §4.11: *"≈49 % of graded rows are expected to store an offset one step ahead … uniform on (0, 24 h] … a zero result is **not possible** under a one-sided lag unless every ingress happens to fall within a nakṣatra's interior for the whole 24 h"*. | Re-derived: lag = noon(D) − t_ingress ∈ **[0, 24 h)** (`logic.py:109-113` + 12:00-UT knots); the Moon is never retrograde, so idx(knot) ≥ idx(ingress) mod 27 — **direction correct, one-sided forward**. P(boundary crossed) = E[motion]/13.33° = (13.18°/day × 0.5 d)/13.33° ≈ **0.494** under uniform ingress phase and mean motion — the figure holds. Caveats absent: (i) **+2 steps** is possible when motion > 13.33° (Moon > 13.33°/day near perigee with lag > ~21 h) — well under 1 %, but "one step" is not exact; (ii) **per-chart variance**: ≈64 graded rows (72 − 8 horizon-edge) → binomial sd ≈ 6 pp, so the single-chart §4.11 ablation measures 49 ± ~12 pp (95 %) — a 40 % or 58 % result *confirms*; (iii) "not possible" → "≈ 0.5^64"; (iv) a **sub-24 h excursion** (ingress, then retrograde exit before the next knot) is not a lag but an *invisible run* — a third date-grain defect the account does not name; (v) "(0, 24 h]" → "[0, 24 h)". | Add (i)–(v) to §3 Non-claim / §4.11; state the ablation's acceptance band, not a point. |
| N-09 | MINOR | `may_touch[3]`: *"one additive DDL migration on kala_moorti_nirnaya (requested_horizon, grading_basis, reason, window_id)"*; §10.3 recommends **columns**. | §4.3/§4.5/§4.7/§7 OFFER, as row fields: `claim_grain`, `time_basis`, `inclusivity`, `grading_lag`, `completeness_state` + `owner`/`evidence_ref`/`next_eligible_action`, `epistemic_class`, `source_qualification`, `corpus_verifiable`, `comparable_with`, `tier_basis`, `operator_role`, `generation`, `window_ref`, `independence_group`, `coverage` — none in the DDL entry. Under "columns" the fence is under-scoped by ~15 columns, one of which the N-02 successor binds to. | List the columns (with CHECKs for the closed vocabularies) or say which are constants stamped at serve time. |
| N-10 | NOTE | §1: the false "century v3 actual input" claim is *"inherited from blueprint `:326`"*. | The parent contract carries it too: `MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md:276` (L3-A06) *"Actual v3 input."* | A strategy-row correction is a strategy amendment to raise (context §2), not only a blueprint note. |
| N-11 | NOTE | §1 R6 row: *"no `GRANT` naming `bg_transit_moorti` exists in either migration tree"*. | Literally false: `platform/migrations/632_nirmana_evidence_server_writer_guard.sql:39` lists it in `detector_read_relations`, granted SELECT at `:143-145` — to `nirmana_evidence_ingress_writer`, not the builder. `1070_data_plane_builder_orchestrator_grants.sql:63-73` is the builder-role grant migration and omits it — the natural precedent for G21. | "No grant to `data_plane_builder`; 632 grants the evidence role; 1070 is the precedent." |
| N-12 | NOTE | Header: `source_revision: "9feac52d7 … git diff 9feac52d7 3387c9ac3 -- services/ka_moorti_nirnaya/ is empty"`. | Short SHA (guide `:67`: full commit). The quoted path does not exist from the repo root (`platform/python-sidecar/services/…`), so the quoted command is vacuously empty. Re-run on the real paths against HEAD `e82dd34a3`: empty (see `reviewed_on`). `implementation_owner` still unnamed (acceptable at DRAFT). | Full SHA; real path; owner at PROPOSED. |
| N-13 | NOTE | §2.2: *"Tests: `tests/l3/test_ka_moorti_nirnaya_writer.py:90-115` covers only the empty-table and keyed-dict cases of `_fetch_moorti_table`"*. | True of that function. The inventory omits `tests/l3/test_ka_moorti_nirnaya.py` (logic) and `test_ka_moorti_nirnaya_property.py:87-175` (offset totality, FORENSIC anchor `482012f1`). | List all three files. |
| N-14 | NOTE | §2.1 registration. | The `discover_all()` registration path is the shim `pipeline/orchestrator/writers/ka_moorti_nirnaya.py:23` importing the services writer; the `@register` itself is `writer.py:174`. Correctly outside `may_touch`. | Name the shim in §2.1. |
| N-15 | NOTE | §2.3: `w22_moorti_nirnaya.py:18-19`. | The sentence is at `:19`; `:17-18` are the "ADMISSION STATE" header. | `:19`. |

## 3. Citations verified

| citation (brief v1.1) | resolved? | actual |
|---|---|---|
| `writer.py:164-171` (unguarded fetch) | yes | bare `conn.cursor(row_factory=…)`, no SAVEPOINT/try |
| `writer.py:196-201`, `:203-205`, `:207`, `:208`, `:211-225`, `:227-230`, `:232-297`, `:243`, `:268`, `:292-295`, `:299-306`, `:305-308`, `:103`, `:65-66`, `:69-73`, `:76-80`, `:107-117`, `:120-135`, `:138-142`, `:145-161` | yes | as cited |
| `writer.py:174` (`@register`) | — | shim at `pipeline/orchestrator/writers/ka_moorti_nirnaya.py:23` |
| `logic.py:22-30`, `:37-55`, `:64-66`, `:97-115`, `:126-134` | yes | as cited (run detection body `:96-116`) |
| 525 `:64-103`, CHECK `:91-102`, `:76-77` | yes | `platform/supabase/migrations/525_kala_moorti_nirnaya.sql` |
| 670 `:1017-1117`, (f) `:1079-1089`, comment `:1081-1082`, `:1839` | yes | `platform/migrations/670_…`; (f) SQL `:1083-1089`; floor 72 at `:1839` |
| `asset_runner.py:759-765` | yes (±1) | SAVEPOINT `:758`, try/except/raise `:759-763`, RELEASE `:764` |
| `asset_runner.py:1041`, `:1047-1055`, `:1064-1072`, `:523-528`, `:44` | yes | defer-commits, error path, post-write gate, upsert, dep-assert |
| `asset_runner.py:767-768`, `:1091-1098`, `:1109-1164`, `:1111-1117`, `:816-820`, `:473-500` (not cited by the brief) | — | rows consumed; dormant/lit; no-op reclassification; `_probe_asset`; `_data_rows_present` |
| `writers/__init__.py:32-39` (not cited) | — | `WriterResult.notes: str = ''`, no orchestrator reader |
| `asset_registry_seed.ts:2218-2225`, `:2557-2574`, `:2567`, `:3349` | yes / — | dependency; asset row; `count_sql`; upsert preserves `target_floor` |
| `l0_ephemeris.py:271`, `:278` | yes | "at 12:00 UT"; `swe.julday(…, 12.0)` |
| `context.py:186`, `:252`, `:274`, `:486-531`, `:183` | yes | `moorti_rows` field; fetch; ctor; `_fetch_moorti_rows`; "stays 'candidate'" |
| `engine.py:100-101`, `:632` | yes | imports w23/w30 only; λ formula without mūrti term |
| `w22_moorti_nirnaya.py:9`, `:18-19`, `:162-163`, `:172` | yes / ±1 | Ch.27 at `:9`; sentence at `:19`; NOT-plumbed at `:162-163`; `getattr(context,"moorti_rows")` at `:172` |
| `mechanism_register.yaml:87-103` | yes | `admission_state: admitted` (`:91`) |
| `grammar_v3_registry.yaml:52-75`; `mechanisms/registry/w22_moorti_nirnaya.yaml:14-15`; `w44_weight_fitting.py:160`; `tests/test_context_moorti_wiring.py:151` (not cited) | — | `candidate`/`pending`; `candidate`/`null`; w22 → `False`; live end-to-end test |
| `stage1_symbolization.py:222-238` | yes | `build_moorti_primitive`; callers only in its own tests (`:164-260`) |
| `ka_avadhi.py:229-246`; `gochara_intensity/_dbutil.py:122` | yes | writer-local SAVEPOINT; `savepoint_scope` |
| `now.ts:497-556`, `:519-520`; `query_moorti_nirnaya.ts:81`, `:94`, `:106` | yes | as cited; `as_of` compared as DATE |
| `source_query_availability.ts:2891-2894`; `data-plane-ownership-preflight.ts:63` | yes | as cited |
| 401 `:3`, `:35` | yes | `platform/supabase/migrations/401_bg_transit_moorti.sql`, Ch.26 |
| 855 `:5`, `:41`, `:72`; `test_migration_855_…:34-37` | yes | 72; per-graha sum 20+17+16+9+4+2+2+2 = 72 |
| `KALA_NATIVE_RULING_SHEET_v1_0.md:147-151` | yes | R6, "migration 1071" at `:151` |
| blueprint `:189`, `:286`, `:326`, `:398`, `:484`, `:889`, `:908` | yes | as cited |
| Strategy `:62-74`, `:96`, `:276` | yes | L3-Q01–Q13; coverage keys; L3-A06 "Actual v3 input" |
| Foundation `:39`, `:72` | yes | F12 operators; F06 companions |
| binding B1 `:24-33`, B2 `:36-46`, B5 `:65-70` | yes | resolver row `:33`; `t_exact` `:27` |
| Graha Sañcāra brief "§4 item 3" | yes | v1.0 file (changelog to 1.2), §4 item 3 `:207-217`: positions at an instant; no ingress offer (grep `ingress`: none) |
| `ka_tithi_pravesha/writer.py:3,:13,:27`; `logic.py:31-72` | yes | Moon-return root-find; tolerance at `:69-72` |
| `pipeline/transit_search.py:506-560`, `:242-272`, `:951` (not cited by the brief) | — | `find_ingress_events` bisection; `_get_planet_pos` (Lahiri, `FLG_SIDEREAL`, TRUE node) |
| `services/ka_temporal/date_resolver.py:1-60`, `:92-674` | — | no date↔instant conversion |
| `632_…sql:26-39`, `:143-145`; `1070_…sql:63-73` | — | evidence-role grant incl. `bg_transit_moorti`; builder grants without it |
| `tests/l3/test_ka_moorti_nirnaya_writer.py:90-115`; `test_ka_moorti_nirnaya_property.py:87-175` | yes / — | as cited; omitted from the brief's inventory |

## 4. What I could not verify

- The live grant state on `bg_transit_moorti` for `data_plane_builder`, the live row count (71/72), the live `target_floor` (72 is what 670/855 set and the seed preserves; not queried), and the live 670 result — no database access.
- Whether the deploy runner applies `platform/migrations/` or `platform/supabase/migrations/` (both exist; 525 is in the latter, 670/855/632/1070 in the former).
- PR #2717 / migration 1073 — no network; neither exists in this tree.
- Whether `mechanism_register.yaml` (vs `grammar_v3_registry.yaml` or the per-mechanism YAML) is the register any runtime path reads — only `w43/w44` scripts and `context.py`'s comment reference `admission_state`; no runtime loader was found, not proven absent.
- The psycopg exception class on a missing grant — not executed.
- No test was run; the proof-row analysis (N-03, N-07) is from reading the code paths the rows would exercise.
- The ≈49 % figure and its caveats (N-08) are a priori (uniform ingress phase × mean lunar motion), not measured; the brief's §4.11 ablation remains the measurement.
- Kṣetra's `stage3` branch — only this worktree was read.

## 5. Conformance to the binding

No field is spelled outside B1–B7: `claim_grain='date_grain'`, `time_basis='noon_ut_knot'`, `inclusivity='closed_closed'`, `completeness_state` (six values) with the Foundation `:72` companions, `epistemic_class`, `source_qualification` ∈ {`verse_cited`, `algorithmic_approximation`}, `corpus_verifiable`, `comparable_with='self'` (enum member), `tier_basis='relative_uncalibrated'`, `operator_role='applicability'`, `window_id` (sha256 over natural key + version), `generation`, `window_ref`, `independence_group` with `basis:'declared_lineage'`, `coverage` with all seven B5 keys. Asset-local names (`grading_basis`, `grading_lag`, `reason`, `moorti_computed`, `requested_horizon`, truncation flags) are declared as such. The two B1 defects are semantic, not lexical: the `t_start/t_end` mapping contradicts the declared `time_basis` and cites a resolver capability that does not exist (N-05); `t_exact` is not mentioned at all — B1 requires it as `timestamptz or NULL`, and NULL is the honest value here until the DEMAND lands.

## 6. Disposition of the v1.0 findings (verified at source, not from §12)

| v1.0 id | severity | disposition | evidence |
|---|---|---|---|
| F-01 | BLOCKER | **RESOLVED** (as stated) | §4.2 names (a) with a 670-successor in `may_touch` and (b) as the no-migration path; 670(f) `:1079-1089` and the gate `:1041,:1064-1072` are cited correctly. The *new* defect is that the recommended (b) masks rather than types the outage — N-01, not a re-open of F-01. |
| F-02 | MAJOR | **RESOLVED** | one-sided forward lag, ≈49 %, §3 example (Jupiter 02:00 UT → `start_date=D`, knot 10 h after) correct, §8 re-ranked, "decorative" clause removed. Caveats outstanding: N-08. |
| F-03 | MAJOR | **RESOLVED** | §2.3 "prefetched, not consumed"; `engine.py:100-101,:632` correct. The contradiction's *statement* is inaccurate — N-06. |
| F-04 | MAJOR | **RESOLVED** | §2.2 dead branch `:292-295`; §7 Context row expects the whole-build refusal. |
| F-05 | MAJOR | **PARTIAL** | the inverted instant row is fixed and gated `[X]`; but the replacement `[U]` row is not runnable against today's shape and its second detector clause cannot fire (N-07), and the date-grain Boundary row now carries a false expected result (N-03). |
| F-06 | MINOR | **RESOLVED** | §2.3 Kṣetra "ruled future consumer, not executed"; `stage1_symbolization.py:222-238`, no caller outside tests. v1.0 §11.4 removed. |
| F-07 | MINOR | **RESOLVED** | §0/§3 carry the savepoint→rollback→`last_error` trace, the `enforce` dependency, the `ka_avadhi` precedent; §2.2/§10.1 raise the two other bare fetches. |
| F-08 | MINOR | **RESOLVED** | §7 splits Irrelevant control (same chart-zone date) from Relevant influence (zone). |
| F-09 | MINOR | **RESOLVED** | §7 Value tied to §4.11, `[X]`. |
| F-10 | MINOR | **RESOLVED** | every re-pinned line checked (§3 above); one ±1 (N-15). |
| F-11 | MINOR | **RESOLVED** | both migration paths correct; grant number "1071 per R6 / 1073 per blueprint — unresolved". Residual: the "no GRANT exists" sentence — N-11. |
| F-12 | MINOR | **PARTIAL** | `completed_horizon`, `resolution`, `corpus_verifiable`, `comparable_with`, `tier_basis`, the F06 companions added; the B1 position is stated but wrong (N-05); the companions have no home under the recommended (b) (N-01); `t_exact` absent. |
| F-13 | MINOR | **RESOLVED** | §10.3 gives the CHECK/conjunct rationale and retracts "consumers filter on them". |
| F-14 | NOTE | **PARTIAL** | owner and bracket named; tolerance promised, not stated; the ownership claim ignores `find_ingress_events` (N-04). |
| F-15 | NOTE | **RESOLVED** | §1/§2.1 carry 71 (blueprint) and 72 (855/670), both [A]. |
| F-16 | NOTE | **RESOLVED** | §4.1 Q05; Q-K06 labelled a blueprint cross-reference. |
| F-17 | NOTE | **RESOLVED** | §2.3 lists the availability probe and the ownership preflight. |
| F-18 | NOTE | **RESOLVED** | §2.4/§4.3 name the over-reading column and `grading_basis` as its corrective. |
| F-19 | NOTE | **RESOLVED** | Ch.26 vs Ch.27 in `accepted_upstream_contract` and §10.4. |
| F-20 | NOTE | **DEFERRED (by the brief's own note)** | short SHA and unnamed owner remain; the diff command's path is wrong (N-12). Must be filled at PROPOSED. |

Net: 15 RESOLVED, 3 PARTIAL (F-05, F-12, F-14), 1 DEFERRED (F-20), 0 UNRESOLVED; 1 new BLOCKER (N-01), 5 new MAJOR (N-02–N-06), 3 MINOR, 6 NOTE.
