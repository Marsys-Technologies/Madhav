---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_AVADHI
version: "1.0"
status: REVIEW_ISSUED
date: 2026-09-24
brief_under_review: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_AVADHI_ELEVATION_BRIEF_v1_0.md
reviewer: "Fable 5.1 review agent, fresh context, read-only"
worktree: "/Users/Dev/madhav-l3/layer-briefs @ 3387c9ac3 (branch l3/kala-layer-briefs)"
source_revision_check: "brief cites 9feac52d7; `git diff 9feac52d7 HEAD` on ka_avadhi.py, tree_walk.py, migrations 395/670/859/1023, kala_temporal.ts, query_dasha_dossier.ts is empty — the code the brief read is the code reviewed. Migration 1023 (#2556, 2026-09-10) is an ancestor of 9feac52d7 (`git merge-base --is-ancestor` true): it was on the brief's base and the brief did not see it."
method: "every file:line re-read with sed/grep/cat -n on this worktree; no DB, no network, no git write; no test run"
---

# Independent review — `KA_AVADHI_ELEVATION_BRIEF_v1_0`

## 1. Verdict

**`REWORK`.** The brief's diagnosis of the seventh-system vocabulary split, the mislabelled
pratijñā list, the absent `strength_factor` and the vimshottari-only timeline all verify at
source; but its primary mechanism (a build-time orphan anti-join) has no code path that can fire,
its §3 failure statement ("(e) stays red after rebuild") is false for the same reason, its
integrity-contract citation is a superseded migration (1023 replaced 670 for this asset on the
brief's own base and fixes a conjunct the brief's Positive row silently depends on), one of its
four native decisions would drop a *real* registry edge on a wrong premise, its domain map is not
merely "uncited" but 58% outside the ontology's CHECK vocabulary (Rahu can never match), its B3
offer is a random surrogate id, and its "two silent caps of ten" cannot bind under the current
vocabularies. The preservation list and the refusal-path reading are sound.

## 2. Findings

| id | sev | brief claim (section) | found at source (exact lines read) | proposed correction |
|---|---|---|---|---|
| F1 | **BLOCKER** | §4.5: *"the orphan detector: build-time anti-join, dangling ids written under `completeness_state='unavailable'` with `reason='pratijna_orphaned'`"*; §4.13 ablation: *"Regenerate L2 … after, the orphan count appears in coverage"*; §7 "Relevant influence" row: *"regenerate `bodha_pratijna` ids in the fixture → `exclusions.orphaned.dropped` = the count"*, detector fails when *"silent dangling ids (today)"*. | `activated_ids` are compiled from `_FETCH_PRATIJNA_SQL` (`ka_avadhi.py:101-109`, `:231-241`) — a `SELECT … FROM bodha_pratijna WHERE chart_id = %s` in the same transaction as the INSERT (`:324-327`). Every id the writer emits exists by construction at write time; a build-time anti-join has no path to return a dangling row. After an L2 regeneration, either ka_avadhi is rebuilt (delete-then-insert `:325` — the old ids are *removed*, count = 0) or it is not (the writer does not run, nothing is written). The ablation cannot produce the count it promises; the proof row cannot go red in the direction claimed. Same defect class as `REVIEW_KA_YOJAKA_v1_0` F1. | Re-specify orphan detection as **read-time**: the registry conjunct (e) (already exists, `1023_…:175-184`) and a serve-time anti-join in `query_dasha_dossier` (an interface packet with an L3-owned test), or a `pratijna_refs[].generation` compared to the L2 head at read time. Rewrite §4.5, §4.13 and the Relevant-influence row so the fixture is "regenerate L2, do **not** rebuild ka_avadhi, then read" and the detector is the resolver. If a *persisted* orphan state is wanted, say explicitly that the writer preserves prior rows — which is accretion (§N.3) and must be ruled, not assumed. |
| F2 | MAJOR | §0/§1/§3/§7: the integrity contract is *"migration 670 … conjunct (c) `:86-97`, (d)"*; Positive row: *"670 (re-pinned) returns TRUE"*; `must_not_touch` names *"670_*, 859_*"* only; §5 *"one registry successor migration (670(b)/859 re-pin)"*. | `platform/migrations/1023_nirmana_l3_ka_avadhi_integrity_conjunct_d_graha_code_fix.sql` (merged #2556, 2026-09-10; ancestor of 9feac52d7) does `UPDATE asset_registry SET integrity_check_sql = …` for `ka_avadhi` (`:92-187`), replacing the whole 670 text. Conjunct (b) still carries `'chara'` (`1023:122-123`) — the vacuity claim survives — but (d) is rewritten (`1023:153-174`): under 670's text (`670:101-109`), `upper('Jupiter')` vs `'JUP'` fails for 7 of 9 grahas the moment refs are non-empty (`1023:13-34`). The brief's Positive row would be FALSE under the contract it cites and TRUE only under the one it does not. | Cite 1023 as the live contract; add 1023 to `must_not_touch`; make the successor migration a successor **of 1023** (re-pin (b) only, every other conjunct byte-identical — 1023's own discipline `:36-43`); note that (d)'s CASE is a wrapper-local copy of `norm_graha`'s alias table (§N.7 item 3) the brief should at least name. |
| F3 | MAJOR | §3 Observed behavior: *"Conjunct (c) turns green (refs resolve under M4) but (e) stays red: 3,087 `activated_pratijna_ids` point at no `bodha_pratijna` row"*. | See F1: a rebuild recompiles the ids from the live `bodha_pratijna` rows of the same chart (`:104-107`); conjunct (e) (`1023:177-184`) checks exactly that join. Immediately after a rebuild (e) is green by construction. The 3,087 (670 header `:26`) are the *post-build* drift of an L2 regeneration (G20) against rows built earlier; (e) is red today, not after the rebuild. | Restate: after rebuild, (a)–(e) can all be TRUE under 1023; (e) goes red again only when L2 regenerates without a Kāla rebuild. That is the SC-9 case, and it is a read/check-time detection, not a build-time one. |
| F4 | MAJOR | §2.4/§3 Non-claim: *"no claim that the domain map is classically wrong — only that it is uncited"*; §4.4: keep `_GRAHA_DOMAINS` with `source_qualification='unsourced'`; §9 walkthrough: Jupiter AD → *"`pratijna_refs` of four domain-matched pratijñā"*. | `brahma_event_ontology.domain` is CHECK-constrained to 13 values (`388_brahma_ghatana_ontology.sql:14-16`: career, wealth, relationship, progeny, health, education, family, residence, travel, spirituality, character, transition, general; no later migration alters it — grep of both trees). `_GRAHA_DOMAINS` (`ka_avadhi.py:40-50`) uses 36 strings; **21 are outside the CHECK** (dharma, authority, mind, home, property, disputes, commerce, communication, children, creativity, luxury, karma, longevity, foreign, technology, unusual, moksha, loss, liberation — plus repeats). Per graha: Sun 2/4, Moon 2/4, Mars 2/4, Mercury 2/4, Jupiter 2/4 (wealth, education — 'children' ≠ 'progeny'), Venus 2/4, Saturn 2/4, **Rahu 0/4**, Ketu 1/4. `pratijna_by_domain.get(d, [])` (`:285`) returns `[]` for every dead string. Every Rahu MD/AD row has `activated_pratijna_ids = []` structurally, and (e) passes vacuously for it. This is the M4 defect (vocabulary mismatch, `:117-119`) one array over. | Name it as a defect, not a citation gap. Reconcile the map to the ontology vocabulary (aliases: children→progeny, home→residence, moksha→spirituality, foreign→travel, property→residence; the rest have no ontology domain and must be dropped or raised as an ontology DEMAND). Add a `[U]` proof row that fails today: every `_GRAHA_DOMAINS` value ∈ the 13-value CHECK set. Emit `reason='domain_not_in_ontology'` for a lord whose live domain set is empty (Rahu), distinct from `lord_not_a_graha`. Rewrite the §9 walkthrough count from the real ontology (Jupiter: wealth 3 + education 2 = 5 classes max). |
| F5 | MAJOR | §1 Seed row delta: *"`bg_ghatana` is **declared and not read** (grep `ghatana` → docstring only); `brahma_event_ontology` is **read and not declared**"*; §5 *"`bg_ghatana` declared-unread → drop by registry packet"*; §10 decision 4: *"drop `bg_ghatana`, declare `brahma_event_ontology`"*. | `asset_registry_seed.ts:2247+` region for `bg_ghatana`: `english_name: 'Event Ontology'`, **`target_table: 'brahma_event_ontology'`**; the table is created by `388_brahma_ghatana_ontology.sql:11`; the writer joins it at `ka_avadhi.py:105`. The edge is declared **and** read — `bg_ghatana` *is* `brahma_event_ontology`. Blueprint `:322` already lists it as `bg_ghatana (appl)`. Also: grep `ghatana` in `ka_avadhi.py` returns nothing — the "docstring" the brief saw is the seed's comment (`asset_registry_seed.ts:2349-2350`). | Strike the first two items of decision 4 and the §5 registry-edge line; the only edge question left is the `ka_dasha_kala` constant (F10). A decision put to the native on a false premise is the kind the reviewer rule exists to catch. |
| F6 | MAJOR | §7 Binding: *"B3: `window_ref = {asset_id:'ka_avadhi', generation: FORMULA_VERSION + build, id: avadhi_id}` offered"*. | `avadhi_id UUID PRIMARY KEY DEFAULT gen_random_uuid()` (`395_kala_avadhi.sql:10`); the writer deletes and re-inserts every rebuild (`ka_avadhi.py:325-327`) so the id changes on every build. Binding B3: *"content-addressed sha256 over the natural key + method version, never a bigint surrogate"*; `generation` *"on every row; part of the PK"*. A random uuid is a surrogate in every sense B3 forbids, and no `generation` column exists on the row. | Offer `window_ref.id` as sha256 over `(chart_id, system_id, level_n, period_start, FORMULA_VERSION)` (the DDL natural key `395:22`), emitted in `dossier` or as an additive column (§10.3), or downgrade B3 to DEMAND/`not_offered` honestly. |
| F7 | MAJOR | §4.12 *"Simpler baseline: the current writer after a rebuild (M4 only)"* vs §7 Value row: *"refs resolve to sign/house/nakṣatra facts; the baseline returns `[]`"*. | Under the brief's own stated baseline (M4-only rebuild) refs already resolve (`:132-143`, `:259`; `test_m4_avadhi_lord_condition_refs.py:15` "8 refs for each of the 9 grahas"). The Value row measures the *unrebuilt* live state, i.e. it credits the M4 rebuild — plumbing — to the elevation. The distinction the elevation itself would earn (coverage, honest orphan/dead-domain reasons, one vocabulary, disclosed system filter) is not what the Value row tests. | Make the Value row compare M4-only rebuild vs elevated rebuild on a frozen Q06 question, and let the measured difference be the labelled `pratijna_refs` + `coverage.exclusions` + the seventh system present — or state honestly that the value distinction is disclosure, not answer content. |
| F8 | MAJOR | §0 *"two silent caps of ten"*; §4.6 *"The caps stay (Q8 ruling) but are disclosed"*; §2.4 *"the cap can drop `sign` for `combustion_state` alphabetically"*. | (i) Q8 is **not ruled**: blueprint §9.2 (`:658`, `:672`) lists it under "still the native's"; the ruling sheet `:109` sends hard caps *to* the R-list Q8. Calling it a ruling is a native ruling in disguise. (ii) Q8's proposed text is *"complete coverage where the set is finite; else an explicit reported bound"* — both sets here are finite and small: the fact query asks for 8 keys (`:138-140`) so `LIMIT 10` (`:142`) cannot bind unless `chart_facts` holds ≥2 build generations for one subject (unverified, DB); `bodha_pratijna` is `UNIQUE(chart_id, ayanamsha_id, event_class_id)` (`391:17`) and the ontology seeds 26 classes (388 + 456), so at lahiri a chart has ≤ 26 pratijñā and the largest live domain union per graha is 8 (career 5 + health 3 for Sun/Mars/Saturn) — `[:10]` (`:290`) **cannot bind today**. The caps are dead code, not live silent truncation; a `coverage.exclusions.cap_10.dropped` would read 0 forever (a detector with no red path, §N.8). | Say the caps are currently unreachable; under Q8's own text the honest fix is to remove them (finite sets → complete coverage) and keep the Boundary `[U]` row only as a regression guard. Do not cite Q8 as ruled; put the cap question *to* Q8. |
| F9 | MAJOR | §7 Duplication row covers only *"two L1 rows for one period at two ayanāṃśas"*; §2.2 *"total `ORDER BY`"* is claimed for fact refs only. | The rebuild the brief mandates admits `chara_karaka`, for which `ka_kshetra/stage3_clocks.py:1071-1076` documents *"up to 5 MD rows with the same start_iso"* in L1 [A: code comment, not measured here]. `_FETCH_MD_SQL`/`_FETCH_AD_SQL` order by `(system_id, start_date)` only (`:81`, `:94`) — not total — and `executemany` + `ON CONFLICT … DO UPDATE` (`:155-162`, `:327`) collapses same-`(system, level, period_start)` rows with last-row-wins. The surviving `lord_graha` is order-dependent, and neither (a) (some L1 row still matches) nor (b) (every L1 start has an avadhi row) can see the collapse. | Add a `[I]` Duplication row for same-start L1 rows; a collapse detector (L1 candidates − rows written, per system) reported as a B5 `exclusions` entry; a total `ORDER BY` on both spine fetches (§N.7 item 2). State whether the natural key should carry `start_iso` (Strategy `:282` asks the brief to assess the key). |
| F10 | MINOR | §1/§4.2/§10-4: declare the `ka_dasha_kala` import *"as a `vocabulary` edge (F12 role)"*. | F12's eight roles are enumerated (`FOUNDATION_CONTRACT…:39`): computation, applicability, counterevidence, uncertainty, interpretation, exclusion, relevance_navigation, evaluation. `vocabulary` is not one. Blueprint `:322` calls the constant "(nav, undeclared)". | Either `relevance_navigation` or, more honestly, "not an F12 input — a code-level constant import; no `depends_on` edge, recorded in the brief". |
| F11 | MINOR | Header `asset_or_interface_ids`: *"SC-10 (one daśā-system vocabulary: chara vs chara_karaka)"*; §3 *"SC-9/SC-10"*. | Blueprint `:289`: SC-10 is **ayanāṃśa hygiene** (`'lahiri'` defaults, `SUPPORTED_AYANAMSHAS` vs `chart_facts` ids). No synergy contract covers daśā-system ids. | Cite DP01 (no rival definitions) alone, and raise "one daśā-system vocabulary across L1/L3/registry" as a synergy-map amendment if wanted. |
| F12 | MINOR | Line citations into `ka_avadhi.py` (file unchanged since 9feac52d7). | `_DASHA_SYSTEMS` is `:37` (brief `:34`); `FORMULA_VERSION` `:33` (brief `:36`); `LIMIT 10` `:142` (brief `:143`); refusal `:207-224` (brief `:200-215`); citations `:305-307` (brief `:306-308`); fetch calls `:194-201` (brief `:195-199`); `_build_row` `:280-309` (brief `:276-309`); `norm_graha` call `:259` (brief `:257`, the comment). Blueprint `:284`/`:904` cite `:142/290` correctly. Seed row is `asset_registry_seed.ts:2334-2352` (brief `:2335-2353`). | Re-pin. |
| F13 | MINOR | `may_touch`: *"tests/l3/test_migration_859_avadhi_expected_volume.py"*. | The file is `platform/python-sidecar/tests/test_migration_859_avadhi_expected_volume.py` (tests root, not `tests/l3/`); `_DECLARED_SYSTEMS` there still lists `'chara'` (`:36`) and `test_migration_859_documents_the_chara_gap_without_resolving_it` (`:72-78`) asserts the *migration text* — a successor migration leaves 859's file untouched, so that test does not move; only the live-DB integration test (`:105+`) is affected. | Correct the path; say which assertions actually move. |
| F14 | MINOR | `may_touch` lists `platform-mcp/src/tools/retrieval/kala_temporal.ts:277-314` while §4.10 calls it *"Pūrṇa-owned TS, L3-owned sentinel"*; `must_not_touch` lacks the guide's boilerplate. | A Pūrṇa-owned file cannot be in `may_touch`. Guide §2 requires `must_not_touch` to include *"applied migrations 1033–1070", ".github/workflows/deploy.yml", "kala_gochara_windows WHERE generation='v1'"* — absent. | Move `kala_temporal.ts` to `must_not_touch`; carry the disclosure as an L3-U04/U11 packet; add the boilerplate. |
| F15 | MINOR | §7 Irrelevant control: *"reorder `_GRAHA_DOMAINS` dict → identical rows"*. | Reordering dict **keys** cannot change `_GRAHA_DOMAINS.get(lord)` (`:282`) — the test has no red path. Reordering a graha's domain **list** changes `activated_ids` order (`:284-286`, then `dict.fromkeys` preserves insertion) and `quality.domains` (`:304`) — rows differ. Either reading defeats the row. | Choose a control that can go red (e.g. shuffle `pratijna_by_domain` insertion order → list order must be canonicalised, or assert set-equality explicitly). |
| F16 | MINOR | §4.10 interface packet for `kala_temporal.ts`: disclose `system_id: 'vimshottari'` only. | `kala_temporal.ts:317-321` also requests `level_n: 3` for the snapshot `pd`; the writer never writes level 3 (`:73-95` fetch levels 1 and 2 only; 395 allows 1–3). `pd` is undefined on every call — a perpetual honest-empty the packet should disclose alongside the system filter. | Add to the packet. |
| F17 | MINOR | §4.10/§7 vocabulary: `systems_included`, `systems_available`; B1 offer omits `sandhi_flag`. | Blueprint §12.1 Clock-interval row (`:742`) names the coverage fields `clocks_consulted` / `clocks_unavailable` and the qualification set *"applicability per system, `sandhi_flag`, `inclusivity`, `time_basis`, `claim_grain`"*; L1 carries `sandhi_flag` (`206_ga3_supporting_tables.sql:16`). | Use §12.1's names; add `sandhi_flag` to the B1 offer (referenced from L1, not recomputed). |
| F18 | MINOR | §11.2 / §4.5: *"`bodha_pratijna` generation column if one exists … else the L2 build id"*. | `391_bodha_pratijna.sql:5` has `build_id UUID` (nullable), no generation; but `1036_data_plane_l2_producer_generations.sql:14-37` creates `data_plane_l2_producer_generations (chart_id, asset_id, generation_id)` on this base. The SC-9 reference has a substrate; "else the build id" is the fall-through Context §3 warns against. | Bind `pratijna_refs[].generation` to the 1036 head for `bo_pratijna`, and state what changes if native decision 1 goes the other way. |
| F19 | MINOR | §3 Non-claim / §11.1: *"Whether `chart_dashas` holds `chara_karaka` MD **and** AD rows … inferred from 859's text"*. | Verifiable at source without a DB: `ga_writers/ga_dashas_writer.py:2026, 2055, 2084, 2112` insert `"chara_karaka"` at four nesting levels; `_verify_chara` (`:882-896`) requires sign-name lords at level 1; `chart_dashas.lord_graha` is `NOT NULL` (`206:9`). The refusal path (`:207-224`) is therefore satisfiable in principle and the INSERT will not NOT-NULL-fail on sign lords. Whether the canonical chart's live rows exist stays [DB]. Also: the refusal and the `ALL_DASHA_SYSTEMS` import both landed in `fa9857f00` (2026-09-16), nine days *after* 859 (`54174fca3`, 2026-09-07) — 859's "derived from the writer" was true when written. | Upgrade to [V] with the emitter citation; date the vocabulary change so the reader sees 859 was not wrong at its time. |
| F20 | NOTE | §4.1/§6: *"Q-K01"*. | Q-K ids are Lane E's portfolio; Context §2 admits L3-Q01–Q13 only (same as `REVIEW_KA_YOJAKA` F12). | Cite as cross-check only, or drop. |
| F21 | NOTE | §4.5 `reason='pratijna_orphaned'`; §4.11 `reason='lord_not_a_graha'`. | Foundation `:72`: completeness records `reason`, `owner`, `evidence_ref`, `next_eligible_action`. | Add the other three. |
| F22 | NOTE | §4.7 recommends striking `strength_factor`. | Repo-wide grep (`platform/src`, `platform-mcp/src`, `python-sidecar`): the only occurrence is the writer docstring `ka_avadhi.py:15`; zero readers of `strength_factor`, `modulation_note`, `activated_pratijna_ids` or `lord_condition_fact_refs` by field name outside the writer — the dossier is served as opaque jsonb. Supports "strike"; also means every §7 served-sentinel test must assert on jsonb passthrough. | Cite the grep as [V]; state the passthrough consequence. |
| F23 | NOTE | §2.3 consumer list. | Verified complete for readers (`query_dasha_dossier.ts:93,101`; `kala_temporal.ts:277-321`; `source_query_availability.ts:3054-3064`); `tool_name_bridge.ts:200` is a name map, not a reader; `kala_views/**` has none. Stale row-count comments (`query_dasha_dossier.ts:5` "~1,571-1,585"; `kala_temporal.ts:116` "1,571") vs 1,169 — not the brief's error. | Optionally fold the comment corrections into the interface packet. |
| F24 | NOTE | §1 does not mention migration 1034. | `1034_nirmana_purna_anvesana_wave1_output_digest_specs.sql:53-55` sets `ka_avadhi`'s output-digest spec (a registry packet on the same row). | The successor migration must not clobber it; mention. |
| F25 | NOTE | Shape. | 318 lines (≤ ~400 ✓). Lenses (§6) precede the proof matrix (§7); guide §12 puts lenses as an appendix after it. `from datetime import date` (`ka_avadhi.py:23`) is unused — harmless. | Reorder; optional. |

## 3. Citations verified

All paths from the worktree root. "resolves" = the lines say what the brief says.

| citation | result |
|---|---|
| `ka_avadhi.py:13-14` (docstring "overlaps within the dossier window") | resolves |
| `ka_avadhi.py:15` (`strength_factor` promised) | resolves |
| `ka_avadhi.py:29` (import `ALL_DASHA_SYSTEMS`) | resolves |
| `ka_avadhi.py:34` (`_DASHA_SYSTEMS`) | **off**: `:37` |
| `ka_avadhi.py:36` (`FORMULA_VERSION`) | **off**: `:33` |
| `ka_avadhi.py:40-50` (`_GRAHA_DOMAINS`, Title-case keys) | resolves |
| `ka_avadhi.py:72-96` (canonical ayanāṃśa + fetch SQL) | resolves (`:71-95`) |
| `ka_avadhi.py:97-100` (R6 comment) | resolves |
| `ka_avadhi.py:101-108` (`_FETCH_PRATIJNA_SQL`, no gate) | resolves (`:101-109`) |
| `ka_avadhi.py:105` (`brahma_event_ontology` join) | resolves |
| `ka_avadhi.py:133-143` (`_FETCH_FACT_REFS_SQL`, 8 keys, total ORDER BY) | resolves (`:132-143`) |
| `ka_avadhi.py:143` (`LIMIT 10`) | **off**: `:142` |
| `ka_avadhi.py:145-166` (`_INSERT_SQL`) | resolves (`:145-163`) |
| `ka_avadhi.py:195-199` (fetch calls) | resolves (`:194-201`) |
| `ka_avadhi.py:200-215` (refusal) | **off**: `:207-224` |
| `ka_avadhi.py:226-247` (pratijñā savepoint) | resolves |
| `ka_avadhi.py:250-273` (per-graha savepoint) | resolves (`:249-276`) |
| `ka_avadhi.py:257` (`norm_graha`) | comment at `:257-258`, call `:259` |
| `ka_avadhi.py:276-309` (`_build_row`) | resolves (`:280-309`) |
| `ka_avadhi.py:280-290` (domain loop, no time test, `[:10]`) | resolves |
| `ka_avadhi.py:291-294` (`sublord_modulation = {graha, note}`) | resolves |
| `ka_avadhi.py:306-308` (literal citation) | **off**: `:305-307` |
| `ka_avadhi.py:325-327` (delete-then-insert) | resolves (`:324-327`) |
| `tree_walk.py:40-43` (`ALL_DASHA_SYSTEMS` incl. `chara_karaka`) | resolves |
| `670_…:25-26` (header: ka_avadhi red; 3,087) | resolves |
| `670_…:44-122` (contract), `:71-85` (b), `:75-76` (`'chara'`), `:86-97` (c), `:110-119` (e) | resolve — **but superseded for this asset by 1023 (F2)** |
| `859_…:15-24, :21-22, :60-65, :60-77` | resolve |
| `395_…:3, :9-23, :27, :34-37, :37` | resolve |
| `asset_registry_seed.ts:2335-2353` | resolves at `platform/scripts/seed/asset_registry_seed.ts:2334-2352` |
| `query_dasha_dossier.ts:93-113` (LIMIT 50, `total_matching`, `more_available`) | resolves (`:96`, `:103-110`) |
| `kala_temporal.ts:277-314, :278, :309, :314` (vimshottari only) | resolves; **also `:319` (level 3, F16)** |
| `source_query_availability.ts:3054-3064` | resolves |
| Strategy `:47, :91, :336, :346, :374` | resolve |
| Strategy §6.1 `L3-A12` (`:282`) | exists; **not cited by the brief** (asks for key/ayanāṃśa assessment, F9) |
| Blueprint `:284` (SC-5), `:322` (row 6), `:394`, `:904` | resolve |
| Blueprint `:289` (SC-10 = ayanāṃśa hygiene) | contradicts brief's SC-10 use (F11) |
| Blueprint `:672` (Q8 text), `:658` (§9.2 still the native's) | contradicts "Q8 ruling" (F8) |
| `388_brahma_ghatana_ontology.sql:14-16` (domain CHECK) | read; basis of F4 |
| `391_bodha_pratijna.sql:1-17` (columns; UNIQUE) | read; basis of F8/F18 |
| `1023_…:92-187` | read; basis of F2 |
| `1036_data_plane_l2_producer_generations.sql:14-37` | read; basis of F18 |
| `206_ga3_supporting_tables.sql` chart_dashas DDL (`lord_graha NOT NULL`, `start_iso/end_iso`, `sandhi_flag`) | read; basis of F17/F19 |
| `ga_dashas_writer.py:882-896, :2026-2112` | read; basis of F19 |
| `stage3_clocks.py:1071-1076` | read; basis of F9 |
| `FOUNDATION_CONTRACT…:31, :39, :63-66, :72` | read; F04/F06/F12 spellings the brief uses (`INTERPRETIVE_INFERENCE`, `relevance_navigation`, `unavailable`) are correct |
| `git diff 9feac52d7 HEAD` on all files above | empty |
| `git merge-base --is-ancestor 9a5835b02 9feac52d7` | true (1023 on the brief's base) |
| `git log -S"incomplete dasha-system coverage"` / `-S"ALL_DASHA_SYSTEMS"` on the writer | both `fa9857f00` 2026-09-16 |

## 4. What could not be verified, and why

- Whether `chart_dashas` holds `chara_karaka` level-1 **and** level-2 rows for the canonical chart, and whether same-`start_iso` duplicates exist there — no DB (emitter code says yes to the levels; the duplicate claim is a code comment in `ka_kshetra`).
- Whether migration 1023 has been applied to production (its file is merged on `main`; the migration ledger is DB) — the brief's "registry state `error`" and the live text of `integrity_check_sql` are therefore [A] on both sides.
- The 3,087 orphan count, the 100.00% empty-refs figure, the 1,169 row count and the 2,930 all-chart count — migration headers, not re-measured.
- Whether `chart_facts` carries more than one build generation per subject (which alone could make `LIMIT 10` bind) — no DB.
- No test was run; the `[U]` rows were assessed for red paths by reading, not execution.

## 5. Conformance to the binding (B1–B7)

- **B3 — non-conformant**: `window_ref.id = avadhi_id` is a `gen_random_uuid()` surrogate regenerated on every rebuild; B3 requires a content-addressed sha256 id and a `generation` on the row (F6).
- **B1 — partial**: `t_start/t_end`, `inclusivity`, `time_basis`, `claim_grain` are offered with the binding's names; `sandhi_flag` (blueprint §12.1 qualification for Clock interval) is omitted; the served type of the instants ("JSONB or columns") should be stated as `timestamptz` if columns, ISO-with-offset text if JSONB (F17).
- **B2 — conformant** in spelling (`completeness_state='unavailable'`, `epistemic_class='INTERPRETIVE_INFERENCE'`, `operator_role='relevance_navigation'`, `source_qualification='unsourced'`); `comparable_with`/`tier_basis` correctly n/a (no score). The `reason` records lack `owner`, `evidence_ref`, `next_eligible_action` (F21).
- **B5 — conformant** in shape; but `exclusions.cap_10.dropped` has no red path under current vocabularies (F8), and the collapse of same-start L1 rows is not in `exclusions` (F9).
- **B4, B6** — n/a, correctly stated.
- **Outside the vocabulary**: `vocabulary` as an F12 role (F10); `systems_included`/`systems_available` where blueprint §12.1 names `clocks_consulted`/`clocks_unavailable` (F17). Asset-local fields (`pratijna_refs`, `match_basis`, `sublord_modulation`, `quality.domains`, `citations`) are declared as such — acceptable.
