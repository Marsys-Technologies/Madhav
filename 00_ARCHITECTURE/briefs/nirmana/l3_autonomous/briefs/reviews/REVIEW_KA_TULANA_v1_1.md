---
artifact: KALA_BRIEF_INDEPENDENT_REVIEW
canonical_id: REVIEW_KA_TULANA
version: "1.1"
status: COMPLETE
date: 2026-09-24
brief_reviewed: 00_ARCHITECTURE/briefs/nirmana/l3_autonomous/briefs/KA_TULANA_ELEVATION_BRIEF_v1_0.md (frontmatter version "1.1")
supersedes: REVIEW_KA_TULANA_v1_0.md (22 findings, REWORK)
reviewed_on: "worktree /Users/Dev/madhav-l3/layer-briefs, branch l3/kala-layer-briefs, HEAD 27b0146f3; brief's source_revision 9feac52d7 and the HEAD it names (bf70a3477) are both ancestors; `git diff 9feac52d7 27b0146f3` over every cited code path (ranker/writer/shim, service_probes.py, routers/, migrations 849/246/360, nirmana_probe_contracts.json, call_service_wrappers.ts, priority.ts, register_p1_aliases.ts, asset_registry_seed.ts, engine.py, main.py, python-sidecar/tests) is empty"
reviewer: "Fable 5.1 review agent, fresh context, read-only; no DB, no network, no git write"
method: "REVIEWER_PROMPT_TEMPLATE_v1_0.md followed as written; every v1.0 finding re-checked at source against v1.1's text (§12 not trusted); new defects hunted in §4.5 partition key vs B2, the legacy_composite/849 claim, the new-route proposal and IP-4 enumeration, the §7 Delivery sentinel, and the §7 tier labels"
---

# Re-review — `KA_TULANA_ELEVATION_BRIEF` v1.1

## 1. Verdict

**ACCEPT_WITH_CORRECTIONS.** v1.1 now says what the code says on every v1.0 code-fact finding (exclusion at `:61-62`, the 849 sha-pin, the seven claim sites, the `:135` default, the two fixture paths, B5 shape, spelling map, citation fixes), and the recommendation, decisions and non-claims are sound — but five bounded defects remain that must be corrected before an `approval_record` is written: the new route cannot be reached inside the declared scope (`main.py` is not in `may_touch`), the §8 sequencing retires `confidence_*` one step *before* the probe packet that is the only thing letting `legacy_composite` keep producing 0.795 (breaking the deployed probe and every orchestrator build in between), the Delivery sentinel is still a value the TS wrapper can compute from the same rows, two `may_touch` paths do not resolve, and the §4.5 partition key is built from three producer columns that exist on no branch and are not declared DEMANDED.

## 2. Findings

| id | severity | brief's claim (section) | found at source | proposed correction |
|---|---|---|---|---|
| N-01 | **MAJOR** | `may_touch`: *"platform/python-sidecar/routers/tulana.py (NEW at stage 3 …)"*; §4.10 *"a **new** FastAPI route (`routers/tulana.py`) exposes `rank/compare/attention_map`"*. | A router module is inert until registered: every sidecar route is mounted in `platform/python-sidecar/main.py` via `app.include_router(<router>, prefix=…, dependencies=[Depends(verify_api_key)])` (`main.py:37-108`; the nearest precedent for a service route is `muhurta_score` at `:107-108`). `main.py` is in neither `may_touch` nor `must_not_touch`. The TS side that would call it (`call_service_wrappers.ts` fetch pattern, `:126-152`, `:491-…`) *is* in scope. | Add `platform/python-sidecar/main.py (one `include_router` line, stage 3)` to `may_touch`, and name the prefix/auth dependency the route will mount under, so the F-02 gap is actually closed rather than moved one file over. |
| N-02 | **MAJOR** | §8 order: *"(4) `independence_group` in / `confidence_*` out (Saṅgam-coordinated) → (5) the 849-successor probe packet"*; §5 `RETIRE_AFTER_MIGRATION`: *"`confidence_*` inputs"*; §5 *"Until it lands, the probe asserts the legacy values and `legacy_composite` must satisfy them."* | 0.795 = 0.40·0.8 + 0.25·(15/30) + **0.20·1.0** + 0.15·1.0 — the third term is `_CONFIDENCE_NUMERIC[window_a.confidence_label]` (`ranker.py:49`, `:222`, `:224-229`); `legacy_composite` cannot produce the 849 fixture values without a confidence input. The deployed probe (`routers/nirmana_probe.py:111` → `service_probes.py:797-811,844-848`) and the writer self-test that runs on **every orchestrator build** (`services/ka_tulana/writer.py:37-52` constructs `WindowInput(confidence_label="high"/"moderate", rarity_years=…)`, raises at `:119` on failure) both consume `confidence_label`. Executing step (4) before step (5) breaks both for the interval between them. | Reorder §8 so the 849-successor packet (5) lands before or atomically with (4), and state in §5 that `legacy_composite` keeps reading `confidence_label` (so the `confidence_*` retirement is gated on the successor migration, not on Saṅgam alone). Add `services/ka_tulana/writer.py:25-62` (self-test fixture) to the §5 "must flip" list — it is a live build-time caller of the retired field (see N-08). |
| N-03 | **MAJOR** | §7 Delivery: *"envelope shows X first with `decisive_factor` = the lexicographic key — **a value the TS wrapper cannot produce without the kernel**"*; invariant *"kernel-only sentinel (B7)"*; evidence *"route test + MCP census"*. | Under §4.6, `decisive_factor` = the first differing key in `(activity, declared_current_count, t_start)` — a total function of three columns the wrapper reads from the same `kala_convergence` rows it would pass to the route; one `CASE WHEN a.activity<>b.activity THEN 'activity' …` reproduces it, and the ordering itself is one `ORDER BY`. The wrapper today already ranks in SQL (`call_service_wrappers.ts:672-730`). Nothing in the row is kernel-only. The contrast *"last under `legacy_composite`"* also does not discriminate: the served surface has never computed the composite (`service_manifest.json:664-668`), so the SQL baseline and the composite are different orders — the ablation (§4.13) is what distinguishes, not the fixture. "MCP census" is a static generated inventory (`platform/src/generated/projections/*`), not a runtime observation. | Be honest that for a pure comparator no *output value* is kernel-only. Make the Delivery detector structural + ablation: (a) the capability test asserts the sidecar route was invoked (fetch spy / mocked `PYTHON_SIDECAR_URL` receiving the `WindowInput` payload — the same pattern the muhurta/transit wrappers are testable under), (b) the envelope carries a kernel-emitted `kernel_receipt = {kernel_version, request_digest}` where `request_digest` is sha256 over the canonicalised inputs + `kernel_version`, computed only in `ranker.py` (a TS copy would be a §N.7-item-3 shadowing constant the lint class catches), and (c) the F28 ablation (route call removed → served order changes). Drop "MCP census" as evidence. Re-tier the route half as `[P]` — a route test on a synthetic fixture is runnable today (see N-06). |
| N-04 | **MAJOR** | `may_touch` / §5: *"platform/scripts/nirmana_probe_contracts.json"*; `may_touch` / §7 Positive: *"platform/python-sidecar/tests/test_ka_tulana.py"*, *"`pytest tests/test_ka_tulana.py -k strongest_orders`"*. | Neither path exists. The contracts mirror is `platform/python-sidecar/scripts/nirmana_probe_contracts.json` (`ka_tulana` key present, JSON identical to migration 849's `$hp$` block; `test_nirmana_probe_route.py:159` digests it). The test file is `platform/python-sidecar/tests/l3/test_ka_tulana.py` (`:212` holds `_normalise_rarity(None) == 0.5`; `:114-133` the confidence tests). Contract §1 makes `may_touch` "exact files"; a non-resolving path leaves the real file outside the declared scope. | Correct both paths in `may_touch`, §5 and §7. |
| N-05 | **MAJOR** | §4.5: *"the **partition key** is `(mode, ayanamsha_id, node_convention, kernel_version)` **from the producer row**"*; §7 binding line DEMANDS only B1/B2/B3/B4 fields. | `kala_convergence` has, across `0001_brahma_baseline.sql` and every ALTER (`244:3-11`, `340:2`, `361:20`, `409:16-17`, `1071:4-5`, `1072:4-8`), only `mode` of the four. `ayanamsha_id` is *"used for the daśā query, never stored"* (`SANGAM_ELEVATION_BRIEF_v1_0.md:147,:208` — proposed column + NK), `kernel_version` is proposed by plan R-6 (`SANGAM_ALGORITHM_ELEVATION_PLAN_v1_0.md:193`: legacy rows `kernel_version = legacy_i16`), `node_convention` is a column nowhere (binding B6 names the *convention*, not a field). None of the three is in the §7 DEMANDS list or the §4.7 spelling map. Two further gaps in the B2 mapping: (i) the brief assigns `same_convention_same_inputs` / `same_convention_newer_inputs` "within one partition" without saying what splits them for two *distinct* windows (the enum was written for re-derivations of one object; sibling briefs collapse it to `self` within / `different_convention` across — `KA_DASHA_KALA…:179`, `KA_MUHURTA_SEVA…:188`, blueprint `:834`); (ii) `comparable_with` is both **DEMANDED** as a producer input (§4.7 "`comparable_with` inputs", §7) and **OFFERED** "as computed" relative to `reference_window_ref` (§4.5) — one binding name, two reference frames, against B2's "one name" rule. | Declare `ayanamsha_id`, `kernel_version` (and `node_convention`, or derive it from `kernel_version`) as DEMANDED producer columns with their spelling, and mark the partition rows synthetic until they land. Define the split: same `generation` → `same_convention_same_inputs`, differing `generation` within a partition → `same_convention_newer_inputs` (or state that ranking collapses B2 to `{self, different_convention}` and say so). Resolve the overload: either Tulana consumes the producer's `comparable_with` and emits its pairwise verdict under an asset-local name (e.g. `pair_comparability`), or it does not DEMAND `comparable_with` at all and derives the partition from the four key columns. |
| N-06 | MINOR | §7 header: *"tier: `[P]` pure-synthetic today, `[D]` data-bound, gated on Saṅgam"*; §9: *"every `[D]` row is synthetic today"*; Delivery row tier `D`. | The instantiation guide's tier vocabulary is F24's three **verdict** tiers, kept separate — `COMPUTATIONAL_CORRECTNESS`, `EXPLANATORY_DISCRIMINATIVE_VALUE`, `EMPIRICAL_OUTCOME_PERFORMANCE` (`KALA_ASSET_BRIEF_INSTANTIATION_GUIDE_v1_0.md:133`); no §7 row carries one. The brief's `[P]/[D]` axis is data-boundedness, and its own legend is contradicted by §9. The Delivery route-test on a synthetic fixture is executable today (there is precedent: `tests/test_muhurat_route.py`, `test_nirmana_probe_route.py`) yet is tagged `D` and thereby parked behind a gate it does not need; Revision genuinely is `D`; Value is `EXPLANATORY_DISCRIMINATIVE_VALUE` and should say so. Guide `:129` also requires a **command** per row — only Positive has one; Negative … Boundary give fixtures and "same" as evidence path. | Add an F24 verdict-tier column (or suffix) to every row; keep `[P]/[D]` as a separate "input boundary" column with a legend that matches §9; move the route half of Delivery to `[P]`; give each row its `pytest … -k <name>` command. |
| N-07 | MINOR | §5 *"Tests that must flip (F-20): … `test_migration_849_*.py` … (probe pins)"*. | `tests/test_migration_849_tulana_health_probe.py:47-49` reads the **849 file itself** (never edited, per `must_not_touch`) and asserts `contract.keys() >= sp._TULANA_REQUIRED_FIELDS` (`:89`) and, under `@integration`, that running the actual 849 migration round-trips to GREEN through `run_health_probe` (`:123-129`). After the composite retires this test does not "flip" — the integration half fails permanently and the static half fails as soon as `_TULANA_REQUIRED_FIELDS` grows. | Say: retire or mark-historical `test_migration_849_*` in the successor packet and author `test_migration_<successor>_*` on the same two-layer convention; "flip" applies to `test_ka_tulana.py`, `test_w2_…`, `test_service_probes.py`, `test_nirmana_probe_route.py` only. |
| N-08 | MINOR | §5 "must flip" list omits the writer self-test; §2.2 lists it as a caller only. | `services/ka_tulana/writer.py:25-62` builds two `WindowInput`s with `confidence_label` and `rarity_years` and asserts rank order at `:55-56`; it runs on every build (`:119` raises → `mark_asset_error`). Migration 933's `integrity_check_sql` for `ka_tulana` (`933_…:135-151`) is shape-only (`selftest_detail` non-empty object) — it does **not** pin the detail's keys, so the self-test may change freely without a migration. | List `writer.py:25-62` under §5 as a fixture that must be rewritten under items 3/4, and note 933 is shape-only (no re-pin needed). |
| N-09 | MINOR | §2.3: *"plus 5 generated projection files, `capability_knowledge.snapshot.json`, `capability_estate_census.json`, `mcp_surface_profiles.generated.ts`"*; `may_touch`: *"interface packet only (IP-4, enumerated in §4.9)"*. | Files carrying the wrap-claim text (`grep 'ka_tulana service|ka_tulana priority-ranking service'`): **7** under `platform/src/generated/projections/` (`chat_tool_defs`, `family_tool_defs`, `docs_resource_catalog`, `tool_search_index`, `mcp_surface_profiles.generated.json`, `mcp_tool_registrations`, `machine_census`), plus `capability_knowledge.snapshot.json`, `harvest/e1_declared.json` (omitted), and `platform-mcp/src/generated/mcp_surface_profiles.generated.ts`. `capability_estate_census.json` does **not** carry the claim text — it carries the probe contract digest (`generate_capability_estate_census.ts:728,870`), so it regenerates with the 849-successor packet, not with IP-4. The IP-4 enumeration is §4 item **10**; §4.9 is coverage. | "7 projection files + snapshot + `e1_declared.json` + the `.ts` profile" for IP-4; move `capability_estate_census.json` to the probe-packet regeneration list; fix the §4.9→§4.10 cross-reference. |
| N-10 | NOTE | §4.8: *"no `date.today()` (`:281`)"*. | `:281` is the docstring line. The three default sites are `:287` (`rank_windows`), `:319` (`compare`), `:402` (`attention_map`). | Cite `:287,:319,:402`. |
| N-11 | NOTE | §5: *"`test_w2_first_frontier_service_contracts.py:160,184-200` (`"unknown mode"`)"*. | `:160` is `"unknown confidence_label"`; `"unknown mode"` is `:161` (§2.2 has it right); `:184-200` is `test_ka_tulana_fixture_scores_remain_content_bound` (composite pins). | `:160-161` (label + mode), `:184-200` (composite). |
| N-12 | NOTE | §4.4: *"behind the §6.2 sentinel"*. | This brief's §6 is the lens table (no 6.2); the layer execution brief's §6 (`MADHAV_DATA_PLANE_L3_KALA_EXECUTION_BRIEF_v1_0.md:282-307`) has no subsections; the sentinel language lives in its §7 (guide `:137`, context `:106`). | Write "behind the §7 Delivery sentinel (Execution Brief §7)". |
| N-13 | NOTE | §1 last row: *"no `EVENTS.jsonl` row [R]"*. | One row names `ka_tulana` (`EVENTS.jsonl`, 2026-09-20 C2 `LIVE_SERVING_SNAPSHOT_VIA_MCP`: *"Empty on ka_kalasutra, ka_tulana, …"*); none is a freeze. | "no freeze row in `EVENTS.jsonl`". |
| N-14 | NOTE | §4.2 / §10.4: `strongest` secondary keys *"activity → independence count → `t_start`"* presented without a competing convention. | The producer's own binding design pin (RRV-03, `SANGAM_ALGORITHM_ELEVATION_PLAN_v1_0.md:193,:442`) fixes the within-class ordering key as `activity DESC, contact instant ASC`, with independence carried as data, never pooled into the key. Tulana's `strongest` inserts `declared_current_count` between them. | §10.4 should name RRV-03 as the producer's declared key and state why `strongest` differs (a comparator, not the producer's projection order) so the native decides with both in view. |
| N-15 | NOTE | Header `source_revision`: *"byte-identical to HEAD bf70a3477 per the reviewer's diff"*. | HEAD is now `27b0146f3`; the diff over every cited code path is still empty (verified this pass), but the header pins a HEAD that will keep moving. | Phrase as "checked identical at bf70a3477 and again at 27b0146f3 (2026-09-24)". |

Not findings, confirmed at source this pass: `ranker.py:38-43,:49,:61-62,:80,:114,:129-143,:135,:136,:192-194,:198-215,:207-208,:222-230,:245,:268-288,:307-388,:328-334,:365-366,:384,:390-428` all as §2.2 states; `writer.py:25-62,:55,:119`; shim `writers/ka_tulana.py:1-19`; `nirmana_probe.py:24,:63,:106-107,:111`; `service_probes.py:655-846` (`:797,:804-811,:844,:847-848`); migration `849:78-104` values 0.795 / 0.4234 / `proximity_factor` (and 0.795 re-derived by hand from the weights); `360_kala_convergence_mode_c.sql:12` (under `platform/supabase/migrations/`); `engine.py:2115,2134,2201`; `246_l3_ka_kalasutra.sql:3-33` (no `rarity_years`/`confidence_label`); `call_service_wrappers.ts:10,:518-535,:525,:672-730,:689,:703-706,:707-714`; `priority.ts:8-10,:329-331`; `register_p1_aliases.ts:1017,:1022-1024`; `authority_basis_census_seed.ts:147-151`; `producer_editorial_review.ts:384-387`; `service_manifest.json:664-668`; `L3_kala/index.ts:27`; `asset_registry_seed.ts:2427-2441`; stage3 `ka_sangam.py:990-999` byte-identical to HEAD and `exposure.py:65-82` still `comparability_class`; Saṅgam brief `:184` `independence_groups`; merge-base `39af7d29f`; no migration diff on stage3; `fa9857f00` (#2607) / `47131772b` (Avadhi/Yojaka only); `793972c754…` is a commit; Register `:157`; Strategy `:63,:66,:97,:274,:444`; Product v3.0 `:270` under §7.1; Blueprint v5.0 `:222,:320,:343-352,:671,:758,:902`; Amendments `:32`; Elevation plan `:193`; W0 register `:38,:648-654`; Lane D `:692-710` (under `readiness/_work/`); T1 Spine `:389-393`; Lane E `:87,:96-97,:689-725` and Q-K05/Q-K14 (`:178,:285,:344,:353`); `F2.md:126,:140`; Foundation F04 `:66` / F12 `:39`. No `estimated_seconds` cited as a cost basis; no question outside L3-Q01–Q13; no confidence scalar; no orchestrator change; `kala_views` only by packet; the simpler baseline is the actual current behaviour; the date-swap ablation under `strongest` genuinely distinguishes.

## 3. Citations verified

| citation (as in brief v1.1) | resolved? | what is there |
|---|---|---|
| `ranker.py:268,307,390` | yes | `rank_windows` / `compare` / `attention_map` |
| `ranker.py:61-62`, `:288` | yes | `mode not in {"A","B"} → ValueError` inside `_validate_window` (def `:57`); called via `_validate_window_set` (def `:114`) at `:288` |
| `ranker.py:38-43`, `:49`, `:80` | yes | `I11_WEIGHTS`; `_CONFIDENCE_NUMERIC`; `unknown confidence_label` raise |
| `ranker.py:129-143`, `:135`, `:136` | yes | `WindowInput`; `confidence_label='speculative'`; `rarity_years=None` |
| `ranker.py:192-194`, `:198-215`, `:207-208`, `:222-229`, `:245` | yes | `None→0.5`; `_proximity_factor`; past→0.05; composite; `~unknownyr` rationale |
| `ranker.py:268-281` (`date.today()`) | docstring only | defaults at `:287,:319,:402` (N-10) |
| `ranker.py:307-388`, `:328-334`, `:365-366`, `:384`, `:390-428` | yes | `compare`; unweighted `factor_deltas`/argmax; dead labels; verdict label; `attention_map` |
| `services/ka_tulana/writer.py:25-62,:55,:119` | yes | self-test; `rank_windows` call; `RuntimeError` |
| `writers/ka_tulana.py:1-19` | yes | shim |
| `routers/nirmana_probe.py:24,111`, `:63-69,106-107` | yes | probe-type map; `run_health_probe`; `_contract_digest` `:63`; digest compare `:106-107` |
| `service_probes.py:655-846`, `:797,:844`, `:804-811,:847-848` | yes | ka_tulana probe block; `rank_windows`/`compare` calls; asserted expectations |
| `849_…tulana_health_probe.sql:78-104` | yes | `$hp$` block with 0.795 / 0.4234 / `proximity_factor` |
| `platform/scripts/nirmana_probe_contracts.json` | **no** | file is `platform/python-sidecar/scripts/nirmana_probe_contracts.json` (N-04) |
| `360_kala_convergence_mode_c.sql:12` | yes (`platform/supabase/migrations/`) | `DROP CONSTRAINT … mode_check` / re-add incl. `'C'` |
| `engine.py:2115,2134,2201` | yes | `'C'`, `'C'`, `'D'` |
| `246_l3_ka_kalasutra.sql:3-33` | yes (`platform/supabase/migrations/`) | `kala_activation` DDL, no rarity/confidence |
| `call_service_wrappers.ts:10,524-535` (`:525`), `:518-535`, `:672-730`, `:707-714`, `:703-706`, `:689` | yes | header comment; descriptor + description "(ka_tulana service)"; SQL; JOIN/WHERE; `orb_strength_available`; 99.6% comment |
| `priority.ts:8-10,329-331` | yes | header; tool description |
| `register_p1_aliases.ts:1017,1022-1024` | yes | `// ka_tulana → kala_priority_ranking_get`; alias description |
| `authority_basis_census_seed.ts:147-151` | yes (`platform/scripts/census/shad_darshana_gates/`) | "Wraps the ka_tulana priority-ranking capability" |
| `producer_editorial_review.ts:384-387` | yes | `asset_ids: ['ka_tulana']` on `scu.catalog.call_priority_ranking` |
| `service_manifest.json:664-668` | yes | "Direct Postgres query … NOT a sidecar HTTP call" |
| `L3_kala/index.ts:27` | yes | `call_priority_ranking — ka_tulana` |
| "5 generated projection files" | undercount | 7 projection files + snapshot + `e1_declared.json` + `.ts` profile (N-09) |
| `asset_registry_seed.ts:2427-2441`, `:2438` | yes | registration; `depends_on` |
| `tests/test_ka_tulana.py:212`, `:114-133` | **path wrong** | `tests/l3/test_ka_tulana.py` (N-04); lines resolve |
| `test_w2_first_frontier_service_contracts.py:161` (§2.2) / `:160` (§5) / `:184-200` | `:161` yes; `:160` is the label case (N-11); `:184-200` yes | — |
| `test_service_probes.py`, `test_migration_849_*.py`, `test_nirmana_probe_route.py` | yes | pins at `test_service_probes.py:95-98,:400-490`; `test_migration_849…:47-49,:89,:123-129` (cannot flip — N-07); `test_nirmana_probe_route.py:91,:159` |
| `933_…selftest_shape_contracts.sql` (not cited; N-08) | — | `:135-151` shape-only pin for `ka_tulana` |
| `main.py` (not cited; N-01) | — | `include_router` pattern `:37-108` |
| `sangam/stage3` `ka_sangam.py:990-999`, `exposure.py:65-82`, `SANGAM_ELEVATION_BRIEF_v1_0.md:184` | yes | as stated |
| `9feac52d7`, `bf70a3477`, `fa9857f00`, `47131772b`, `793972c754…`, `90435d96a`, `39af7d29f` | yes | ancestors / #2607 / Avadhi+Yojaka / commit / stage3 tip / merge-base |
| `REGISTER:157` | yes (`MADHAV_DATA_PLANE_ASSET_CONTRIBUTION_REGISTER_v2_0.md`) | verbatim |
| Strategy `:63,:66,:97,:274,:444` | yes | L3-Q02, Q05, §3 comparison, A04, U04 |
| Product §7.1 `:270` | yes (`00_ARCHITECTURE/MADHAV_PRODUCT_DEFINITION_v3_0.md`; heading `:266`) | "'Strongest' is not an unexplained scalar" |
| Blueprint `:320,:671,:758,:902` (+`:222,:343-352`) | yes (frontmatter `version: "5.0"`) | as stated |
| Amendments `:32`; Elevation plan `:193`; W0 register `:38,:648-654` | yes | as stated |
| Lane D §14 `:692-710`; T1 Spine `:389-393`; Lane E `:87,:96-97,:689-725` | yes (Lane D/E under `readiness/_work/`, T1 under `audit/_work/`) | as stated |
| `F2.md:126,140` | yes | t2 tally |
| `EVENTS.jsonl` "no row" | one non-freeze row (N-13) | — |
| Foundation F04 `:66`, F12 `:39` | yes | `INTERPRETIVE_INFERENCE`; `relevance_navigation`/`computation` |
| `KALA_SYNERGY_BINDING_v1_0.md` B2 `:43`, B4 `:62`, B5 `:69`, B7 `:81-86` | yes | enum; `independence_group`; seven-key `coverage`; sentinel discipline |
| Contract §6 `:105-115` (`:112` Delivery) | yes (`MADHAV_DATA_PLANE_ASSET_INTERFACE_EXECUTION_BRIEF_CONTRACT_v1_0.md`) | eleven rows; "decisive non-default sentinel" |
| Guide `:129,:133,:137`; Template §2.5 `:81-86`; Context `:100-108` | yes | command per row; F24 tiers; sentinel ownership; walkthrough; kala_views by packet |
| "§6.2 sentinel" | **no** | (N-12) |

## 4. What I could not verify

- **Any live database figure** (`kala_convergence` 0 rows for `482012f1`; `orb_strength` NULL 669,964/672,551; Mode A/B ≤ 0.3805 / C ≥ 0.70) — [A] from Lane E and a code comment; no DB access.
- **The t2 `asset_frozen` ledger row** — only the `F2.md:126,140` tally exists in the repo.
- **Whether `origin/sangam/stage3` has moved** past local `90435d96a`; not fetched.
- **Which files the frozen writer digest hashes**: `platform/src/generated/nirmana-writer-digests.json` and `nirmana-analysis-layer-pins.json` both carry a `ka_tulana` sha256 that `dispatch_frozen_rebuild.py:58` and `generate_capability_estate_census.ts:868-869` consume; I did not trace the generator far enough to say whether `services/ka_tulana/ranker.py` is in its hash set. If it is, editing the kernel invalidates the t2-era digest and the census must regenerate — a blast-radius item the brief does not name; if it is not, no action. Flagged for the author to settle at source, not asserted.
- **Non-Git callers** of the kernel (notebooks, cron, external MCP clients); search scope was the worktree.
- **Test outcomes** — read, not run (read-only mandate).

## 5. Conformance to the binding (B1–B7)

Conformant names used: `t_start`, `t_end`, `t_peak`, `inclusivity` value `closed_open`, `reference_instant` in place of `date.today()` (B1); `epistemic_class='INTERPRETIVE_INFERENCE'`, `completeness_state='unavailable'`, `operator_role ∈ {relevance_navigation, computation}`, `tier_basis='relative_uncalibrated'`, R-6 `activity`/`valence` (B2); `window_ref`, `generation` (B3); `independence_group`, `declared_current_count` (B4); `coverage` in the seven-key shape with `exclusions[]`, `unsearched_regions[]`, `completion_detector` (B5). The asset-local list in §7 correctly fences `criterion`, `verdict`, `tie_on`, `decisive_factor`, `factors_available`, `legacy_composite`, `strength`, `candidates_supplied`, `candidates_ranked`, `reference_window_ref`.

Non-conformant or undeclared:

1. **`comparable_with` overloaded** — DEMANDED as a producer input (§4.7, §7) and OFFERED "as computed" relative to a reference window (§4.5): one binding name, two reference frames. (N-05)
2. **`same_convention_same_inputs` / `same_convention_newer_inputs` undefined** for a pair of distinct windows. (N-05)
3. **Partition-key fields outside B1–B7 and not declared** — `ayanamsha_id`, `node_convention`, `kernel_version` are neither DEMANDED in the §7 binding line nor listed as asset-local nor in the F-07 spelling map, and none exists on `kala_convergence` on any branch. (N-05)
4. **`reference_instant` not in the asset-local list** — B1 has no field of that name (its tz rule governs the *source*, not a field); it is an asset-local input and should be listed beside `reference_window_ref`.
5. **B7 sentinel discipline still unmet** — the Delivery row's `decisive_factor` is derivable by the wrapper from the same rows; "a decisive non-default sentinel" that the serving layer could not have produced itself is not yet named. (N-03)

## 6. Disposition of the 22 v1.0 findings (verified at source, not from §12)

| v1.0 id | status | where v1.1 addresses it | residual |
|---|---|---|---|
| F-01 rank_windows refuses C/D, not "flattens" | **RESOLVED** | §0, §2.2, §3 Observed behavior, §4.5, §4.11, §7 Negative (detector distinguishes `ValueError` from a cross-partition rank) | — |
| F-02 no sidecar route; INTEGRATE unexecutable in scope | **PARTIAL** | `may_touch` adds `routers/tulana.py (NEW)`; §4.10 split; §10.2 | route cannot be mounted without `main.py`, which is not in `may_touch` (N-01) |
| F-03 849 sha-pins the composite; "no migration" false | **PARTIAL** | §1 849 row, §5 probe packet, `must_not_touch` "applied migrations", `legacy_composite` interim rule, "one migration — not 'no migration'" | §8 retires `confidence_*` before the probe packet, which breaks `legacy_composite`'s 0.795 in the interim (N-02); contracts JSON path wrong (N-04); 849's own test cannot "flip" (N-07) |
| F-04 "one line" → enumerated blast radius; `may_touch` range | **PARTIAL** | §2.3 table, §4.10, `may_touch` `:10,518-535,672-730` + six sibling files | generated-file count/omission and the `capability_estate_census.json` misattribution (N-09) |
| F-05 Delivery sentinel not kernel-only | **PARTIAL** | §7 Delivery re-written around `decisive_factor` on a flipping fixture; F28 ablation kept | the value is still computable by the wrapper; evidence path names a static census (N-03) |
| F-06 `coverage` not B5 shape | **RESOLVED** | §4.9 seven keys; counters declared asset-local | — |
| F-07 DEMANDED columns persisted nowhere; spelling map | **RESOLVED** | frontmatter `accepted_upstream_contract`, §4.7 mapping, §7 "persisted on no branch", `[D]` rows marked synthetic | (N-05 adds three more undeclared DEMANDs) |
| F-08 wrong commit for validation | **RESOLVED** | §1 W0 row: `fa9857f00` (#2607), "not `47131772b`" | — |
| F-09 `:222` unreachable; `:135` default; rationale discloses | **RESOLVED** | §0, §2.2 | — |
| F-10 two live fixture paths | **RESOLVED** | §0, §2.3 table incl. `routers/nirmana_probe.py` | — |
| F-11 `WindowInput` required fields | **RESOLVED** | §2.2 (`window_id, mode, peak_date`), §2.3 live-path statement, 246 DDL | — |
| F-12 `decisive_factor` unweighted argmax | **RESOLVED** | §2.4, §4.6 lexicographic first-differing key, §5 PRESERVE concept / RETIRE computation | — |
| F-13 eleven rows; command + evidence columns | **PARTIAL** | Evaluation row `not_applicable` with reason; columns added | only Positive carries a command; tier column is not F24's (N-06) |
| F-14 consumer walkthrough; ordinary-period fixture | **RESOLVED** | §4.11 ordinary period; §9 experience-5 walkthrough; §7 Boundary "ordinary-period pair" | — |
| F-15 `comparable_with` relational, partition key undefined | **PARTIAL** | §4.5 key + relative-to-reference rule; `incomparable` iff `different_convention` | key columns do not exist and are undeclared; same/newer split undefined; DEMAND/OFFER overload (N-05) |
| F-16 `priority.ts:525` ambiguity | **RESOLVED** | §2.3 `call_service_wrappers.ts:10,524-535 (description :525)`, `priority.ts:8-10,329-331` | — |
| F-17 t2 freeze without source | **RESOLVED** | §1 last row, §2.5, §11.5 cite `F2.md:126,140` [A] | wording "no `EVENTS.jsonl` row" slightly overstated (N-13) |
| F-18 line ranges | **RESOLVED** | `:38-43`, `:307-388`, `:390-428`, `:672-730`, `:707-714` | `:281` for `date.today()` (N-10) |
| F-19 `:365-366` dead code; `:384` | **RESOLVED** | §2.2; §5 RETIRE `:365-366 (dead)` | — |
| F-20 tests that must flip | **PARTIAL** | §5 list | wrong path for `test_ka_tulana.py`; `:160` vs `:161`; 849 test cannot flip; writer self-test omitted (N-04, N-07, N-08, N-11) |
| F-21 `depends_on` edges out of scope | **RESOLVED** | §5 fences, §10.3, `must_not_touch` names `asset_registry_seed.ts:2438` | — |
| F-22 "blob" → "commit" | **RESOLVED** | header `parent_layer_contract` | — |

Tally: 15 RESOLVED · 7 PARTIAL · 0 UNRESOLVED. Every PARTIAL is bounded and none reverses the recommendation; N-02 and N-03 are the two that must be re-verified at source in v1.2 before an `approval_record` is written.

*End of REVIEW_KA_TULANA_v1_1.*
