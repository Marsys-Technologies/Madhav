---
artifact: KALA_W0_DELTA_KSHETRA
version: "1.0"
status: CURRENT
date: 2026-09-22
addressed_to: the ka_kshetra brief session
effect: delta to your existing prompt; supersedes anything in it that conflicts; base branch is main
---

# W0 delta — ka_kshetra

You were told no brief and little prior work existed for this asset. The brief part is true. The
rest is not: Kshetra has the **most** accepted W0 work of any Kāla asset. Base branch: **`main`** —
it carries P0 (`3f109869d`), the DHARA midpoint fix (`87cc8c9baf`) and
`DHARA_SWEEP_SEMANTIC_VERSION = '1.2'` (`services/ka_kshetra/dhara_sweep.py:52`) by content.

## Read now

- `briefs/nirmana/MADHAV_DATA_PLANE_L3_W0_FOUNDATION_SAFETY_v1_0.md` §4.1, §5, §6 (item 5), §8
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_DHARA_NUMERICAL_CONTRACT_v1_0.md` — all of it (101 lines)
- `briefs/nirmana/MADHAV_DATA_PLANE_DP019_KSHETRA_GATE_ACCEPTANCE_v1_0.md` (59 lines)
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_KALA_STRATEGY_v1_0.md` §6.2 — the data families and the
  stage diagram, and the paragraph beneath it on rectification inputs
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_W0_FIELD_CONTRACT_REGISTER_v1_0.md` — Kshetra's 15
  relations (13 Q1, 2 QX); fences 4 and 6
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_W0_BENCHMARK_BASELINE_v1_0.md` — the Kshetra sections
- `briefs/nirmana/MADHAV_DATA_PLANE_L3_CURRENT_STATE_AND_DISPOSITION_v1_0.md` §3, §4.1, §4.2

## What W0 already established — inherit, do not re-derive

1. **The fifteen-table stage plan is frozen** (FOUNDATION_SAFETY §6 item 5; Strategy §6.2):
   `S0 + S2 → S3 → S1 → S4 → S5 → S6 → S6.5 → S8 → complete snapshot`, with direct S0 also pinned
   into S4. Use it. Do not invent a decomposition.
2. **Planning is read-only and ACCEPTED** (`3f109869d`; 27 focused / 133 expanded; independent
   ACCEPT). A populated owned slice raises `KshetraReplacementHeld` *before any DML*. **Populated-
   chart replacement is held until W7 immutable candidate/publication.** Your brief cannot propose a
   rebuild trial on the canonical chart before that infrastructure exists, and must say so.
3. **The DHARA numerical contract is CURRENT**: semantic version 1.2; the left-limit rule
   supersedes the old F-12 right-limit rule; the change is a *semantic correction* (+4.656% on the
   reference integral), proven against an independent Decimal-60 / 4,096-Simpson reference; resume
   version 9 rejects pre-correction checkpoints. Anything touching the field integral inherits it.
4. **The null-engine midpoint fix is ACCEPTED** (DP-SD-019: `87cc8c9baf`; oracle 1515 vs the
   predecessor's 300; generation `l3:87cc8c9baf89:002a118b218e` admitted; writer digest
   `c735c02b…`). Your correctness baseline is post-fix code.
5. **Fences:** you own only `kala_insights.lel_derived=false` (`mi_bhara` owns true);
   `kala_field_weight_versions` / `weights` are **inputs owned outside Kshetra**, not outputs;
   `build_substep_progress` is orchestrator-owned (register fences 4, 6).
6. **Measured "before" exists**: null kernel 0.31 s, preparation/recovery 0.47 s, publication
   6.99 s (medians, five matched runs, small fixture, Moshier fallback). Explicitly *not*
   full-chart. Cite it; do not re-measure the same workload.
7. **The 10.97M `kala_field` rows are classified** "not accepted useful coverage or a rebuild
   warrant" (CURRENT_STATE §3). W0 already declines to treat them as capital.
8. **Rectification inputs are already ruled on** — Strategy §6.2: *"Rectification and L5 weight
   inputs require separately admitted, purpose-compatible immutable artifacts. An earlier
   timestamp does not make an event-derived rectification posterior admissible under the
   event-free prospective contract."* This decides your `phala_rectification` question: the live
   read at `stage3_clocks.py:1012` is not merely un-granted, it is contrary to the Strategy. The
   brief replaces it with an **admitted immutable artifact** input. That read is also absent from
   the W0 field register — it is a register amendment the Phase 0/1 session is filing.

## What is genuinely new since W0 — carry forward

The 8.57M-row generation is **incomplete and unpublished**: every row references a
`field_snapshot_id` with zero manifest rows; stage 5 died in alphabetical class order (25 classes
have segments, 15 null stats, 14 windows, 0 salience/insights/timeline). Only 6 classes carry a
real prior; **85.7% of windows are `baseline_is_synthetic`**, and `kala_field` has no such column
despite `hazard.py:150-155` claiming the tag rides every downstream row. The other chart holds a
complete, calibrated 6-class run — two configurations, not two runs. **No retrieval capability
exists over any `kala_field*` table.** Last run crashed on a lost connection under the old
identity's 600 s idle killer (hypothesis, not reproduced); measured cost ~7.5 h.

## What changes in your brief

Adopt the frozen stage plan (1); inherit DHARA 1.2 and the midpoint fix as the baseline (3, 4);
replace the rectification read with an admitted artifact per §6.2 (8); no populated rebuild before
W7 (2); cite the W0 baseline as the "before" (6); then answer the question that was always yours —
what this asset is *for* (which L3-Q, which distinction, which ablation) — before any of P1/P2/P6.
