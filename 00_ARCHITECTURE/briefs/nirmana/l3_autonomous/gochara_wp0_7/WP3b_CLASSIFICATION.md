---
artifact: WP3B_CLASSIFICATION
version: "1.0"
status: WP3b_DELIVERED
date: 2026-09-23
executes: KIMI_AUTONOMOUS_EXECUTION_PROMPT_GOCHARA_v1_0.md §4 WP3b + GOCHARA_FAMILY_ELEVATION_PLAN_v2_1.md §4.5 (equivalence contract) + §8.1 (H-1..H-6 — the fixes that come AFTER this baseline)
branch: l3/gochara-autonomous-wp0-7
baseline_module: platform/python-sidecar/services/gochara_kernel/legacy_semantics.py
baseline_tests: platform/python-sidecar/tests/l3/gochara/test_wp3b_legacy_semantics.py
---

# WP3b — Artifact classification: deliberate behavior vs. accidental bug

Every behavior reproduced in `legacy_semantics.py` is classified here as
**DELIBERATE** (documented, intended — with the docstring/comment proving
intent) or **ACCIDENTAL BUG** (a defect the plan's findings register names,
to be fixed by WP5 H-1..H-6 or WP3c R-1..R-6 — this baseline reproduces the
bug faithfully and labels it; it does NOT fix anything).

Line references are against this checkout (branch `l3/gochara-autonomous-wp0-7`,
base `46b9529cc`). Two files are cited at their **pre-fix** state via
`git show HEAD:` because WP3c is modifying them concurrently with this WP
(see "Concurrent-workstream drift" below): the resonance writer citations
for F-12/F-19 are `HEAD` (pre-WP3c) lines.

Reproduction status legend: **REPRODUCED** (behavior is in
`legacy_semantics.py` and has a golden test in `test_wp3b_legacy_semantics.py`),
**DOCUMENTED** (upstream of the pure algebra — the baseline records the
defect and its exact location but the fix lives in another WP).

## 1. The factor algebra (λ_v3 = PROMISE × PERMISSION × activity × tara × w30 × quality_gates)

| # | Behavior | Class | Evidence (file:line + quote) | Reproduction |
|---|----------|-------|------------------------------|--------------|
| F-1 | λ product order and membership: `promise * permission * activity * tara_modifier * w30_modifier * quality_gates`, clamped to [0,1] | DELIBERATE | `engine.py:632` "raw_lambda = promise * permission * activity * tara_modifier * w30_modifier * quality_gates"; `:635` "Clamp for floating-point edge cases"; hoisted canonical strings `engine.py:135-145` with the comment "Adding or removing a term here … automatically invalidates every stored gochara-window fingerprint" | REPRODUCED (`assemble_lambda_v3`, `LAMBDA_V3_FORMULA`, `TERM_BREAKDOWN_FORMULA`) |
| F-2 | PROMISE = noisy-OR over clamped target weights; empty target set → honest 0.0 | DELIBERATE | `promise.py:16-33` ("PROMISE uses a 'noisy-OR' combination … An empty target set … is an honest PROMISE=0.0, not a fabricated default"), `promise.py:61-74` | REPRODUCED (`compute_promise`) |
| F-3 | PERMISSION = weighted fraction of 12 active timing systems; SYSTEM_WEIGHTS sum 1.0; vimshottari highest (0.16) but not a gate | DELIBERATE | `permission.py:95-114` ("Disclosed structural-prior weights … Sum = 1.0"); `engine.py:1241-1243` | REPRODUCED (`compute_permission`, `SYSTEM_WEIGHTS`, `PERMISSION_SYSTEM_IDS`) |
| F-4 | activity = noisy-OR over per-sentence `p_i = orb_decay × clamp(target_weight, 0..1, default 0.5)`; only `_ACTIVITY_PRIMITIVES` sentences count; cancelled `gochara_vedha_pair` excluded | DELIBERATE | `engine.py:821-917` (whole `_compute_activity_v3` docstring: "This saturates at 1.0 …, is monotonically non-decreasing in sentence count"); primitive set + IR-6 rationale `engine.py:156-192`; cancelled-vedha skip `engine.py:852-853` | REPRODUCED (`compute_activity_v3`, `ACTIVITY_PRIMITIVES`) |
| F-5 | orb-decay selection: `orb_strength` preferred → clamp [0,1]; else `1 − min(|orb_degrees|/5.0, 1.0)`; else **0.5 fallback** | DELIBERATE | `engine.py:154` (`_ACTIVITY_MAX_ORB_DEG = 5.0`); `engine.py:859-876` with comment "use 0.5 as the honest 'present but unorbed' contribution level" and `engine.py:158-160` "a conservative 0.5 (not flat 1.0 as in v1's X(t))" | REPRODUCED (`orb_decay_from_detail`) |
| F-6 | W1.2 signed channels (supportive/afflicting noisy-ORs) and valence resolution with 0.1 tension threshold; class-level valence as prior when both channels zero | DELIBERATE | `engine.py:920-1015`, `engine.py:1018-1060`; W1.2 block comment `engine.py:579-596` | REPRODUCED (`compute_signed_channels_v3`, `resolve_valence_v3`) |
| F-7 | W2.3 tara bala: 9-Tara modifier table (1.00/1.15/0.75/1.10/0.80/1.05/0.70/1.15/1.20), position `((transit−natal) % 27) % 9 + 1`, enabled by default | DELIBERATE | `w23_tara_bala.py:22-47` (table + formula), `w23_tara_bala.py:85-97`; toggle `engine.py:118` (`_W23_TARA_BALA_ENABLED: bool = True`); wiring `engine.py:598-612` | REPRODUCED (`tara_modifier`, `compute_tara`, `TARA_MODIFIERS`) |
| F-7a | tara transit-Moon longitude computed under a bare `except Exception: pass` → modifier silently 1.0 on Swiss failure | DELIBERATE (documented skip) | `engine.py:602-606`: "except Exception: pass  # honest skip: modifier defaults to 1.0 via mechanism" | REPRODUCED (`tara_modifier(None …)` skip path) |
| F-8 | **w30 nodal drishti live on the served path**: Rahu/Ketu 5/7/9 aspects, per-target contribution (5th/9th → 1.05, 7th → 0.95), geometric mean over target contributions | DELIBERATE as *mechanism design* — **ACCIDENTAL as *served doctrine*** (see A-9) | `w30_nodal_drishti.py:89-97` (ASPECT_OFFSETS/MODIFIERS), `:280-287` (geometric mean); wiring `engine.py:624-629`; toggle `engine.py:107` (`_W30_NODAL_DRISHTI_ENABLED: bool = True`) | REPRODUCED (`w30_modifier`, `w30_aspected_signs`) |
| F-9 | W1.3 quality_gates: multiplicative product of per-vedha suppression factors over **DATE-string overlap**; latta path with effective malefic count 3; PG353 grade→factor schedule (0.85/0.75/0.65/0.55/0.45/0.35); empty scale table → 0.70; malefic_count=0 → 0.85; count>table → worst grade | DELIBERATE | `engine.py:272-415` (function docstring "AC1", "Graceful degrades"); schedule rationale `engine.py:205-250`; latta severity `engine.py:320-327` | REPRODUCED (`compute_quality_gates`) |
| F-10 | W1.5 term_breakdown + structural-prior CI band (±20%, clamped) | DELIBERATE | `engine.py:770-788` | REPRODUCED (`assemble_lambda_v3`) |

## 2. Classified artifacts (the defects — reproduced and labelled, not fixed)

| # | Artifact | Class | Evidence (file:line + quote) | Fix owner | Reproduction |
|---|----------|-------|------------------------------|-----------|--------------|
| A-1 (**F-08**) | **Evaluation exception → 0.0 → certified 'active'.** `_eval_single` swallows ANY exception and returns 0.0; the threshold predicate is `>=`; a chart whose century λ distribution is all-zero (or empty) gets `lambda_thresh = 0.0`; a JD whose evaluation *failed* is then certified **active** (0.0 ≥ 0.0). The 0.0 also feeds the bisection and the dense peak scan undetected. | ACCIDENTAL BUG | `interval_solver.py:125-136`: "Returns 0.0 on any exception (honest degrade)" — `except Exception as exc: … return 0.0`; `threshold.py:386-393`: "return lambda_v3 >= config.lambda_thresh"; `threshold.py:321-322` (empty dist → 0.0). Plan §3 F-08: "λ_v3 is a step function on the served path; lambda_thresh=0.0 with >= certifies 0.0 as active; _eval_single exception → 0.0 → 'active'". | WP5 **H-2** | REPRODUCED + labelled: `eval_single_legacy`, golden test `test_f08_exception_zero_certified_active` (failing eval → 0.0 → `is_above_threshold` True), `test_eval_exception_inside_hierarchy_is_zero` |
| A-2 (**F-08**) | **Step-function activity.** The orb decay is evaluated AT THE EVENT; every instant in `[event−5d, event+5d]` receives the full `p_i`; instant `event+5d+ε` receives 0. λ_v3 is a boxcar, not a graduated curve — the "orb-decayed" name describes the event-time weight only. | ACCIDENTAL BUG | `engine.py:499-501` (gather window ±5d), `engine.py:863-876` (decay from event-time detail only — no `|t − event_jd|` term anywhere in `_compute_activity_v3`); plan §3 F-08 "λ_v3 is a step function on the served path". Span-aware statement reproduced: `contribution_box` = `[t_exact−5d, t_exact+5d] ∩ [t_in,t_out] ∩ horizon`. | WP5 (H-2 family; the M-1 method call decides the replacement decay) | REPRODUCED + labelled: `legacy_activity_at`, golden test `test_f08_step_function_activity` (activity identical at t_exact and t_exact+4.9d, cliff at +5d) |
| A-3 (**F-09/F-27**) | **Equal-eighths fixture on the served path.** v3's zero-DB gather calls `kakshya_cell_crossing(…, conn=None)`, so the primitive ALWAYS takes the `equal_eighths_fixture_approximation` branch (8 × 3.75° per sign, lord order from Saturn) with `uncited_extension=True` — 147/202 sentences, 59% of cost, and the L1 grid is the *same* grid so activity saturates at [0.99964844, 1.0] regardless. The primitive's kakṣyā sentences carry no orb fields → each earns the 0.5 fallback; ~147 of them saturate noisy-OR. | ACCIDENTAL BUG (fixture-on-served-path); the 0.5 fallback itself is DELIBERATE (F-5) | `engine.py:1135-1137` ("call with conn=None so they degrade honestly"); `primitives.py:686-699` (`db_boundaries = … if conn is not None else []` → fixture branch with `source = "equal_eighths_fixture_approximation"`); `primitives.py:704-719` (detail carries boundary/kakshya/source only); plan F-09 "[X] E3/E4", F-27 "same equal-eighths grid … H-1a changes the citation, not the numbers". | WP5 **H-1a** (provenance) + **H-1b/N-13** (unqualified crossing contributes no activity — ruled, not pre-approved) | REPRODUCED + labelled: golden test `test_f09_fixture_on_served_path_and_saturation` (fixture grid, 0.5 fallback, 147-sentence saturation band) |
| A-4 (**F-10/H-3**) | **Era windows declared over ranges the sweep never measured.** In `find_threshold_crossings`, when the coarse series starts above threshold, `enter_jd` is set to `jd_series[0]` (the sweep/decade edge) with no check that the crossing is there; symmetric for `exit_jd` at the series end. Era windows can therefore cover ranges where λ is below threshold (or was never evaluated), and requested-vs-completed horizon is not reported. | ACCIDENTAL BUG | `interval_solver.py:304-306` ("Series starts above threshold — enter_jd is at the series start"), `:324-326` ("Series ends above threshold — exit_jd is at the series end"); era sweep spans are the whole-decade ranges (writer `ka_gochara_v3_century_materialize.py:2103-2109` calling `build_resolution_hierarchy(start_jd=decade.start_jd, end_jd=decade.end_jd)`); plan §3 F-10 "Era window = whole decade range", §8.1 H-3. | WP5 **H-3** | REPRODUCED + labelled: `find_threshold_crossings` (edge-clamp code paths preserved), golden test `test_f10_era_window_declared_over_zero_score_range` |
| A-5 (**F-10/H-5**) | **MAX_PEAKS_PER_ERA_WINDOW = 3 truncation before persistence.** Ranked (λ DESC, jd ASC) greedy retention capped at 3 per era window (pooled separation 90 d globally, MR-44); peaks beyond the cap are dropped before any row is written — truncation as absence. | ACCIDENTAL BUG | `resolution_hierarchy.py:104-105` (`MIN_PEAK_SEPARATION_DAYS: float = 90.0`, `MAX_PEAKS_PER_ERA_WINDOW: int = 3`); `:446-465` `retain_candidates` ("capped at max_peaks"); `:474-544` pooled variant; plan §3 F-10 "[X] E5", §8.1 H-5 "remove the 3-peaks-per-decade cap; trim at serve time". | WP5 **H-5** | REPRODUCED + labelled: `retain_candidates`, `retain_candidates_pooled`, golden test `test_f10_peak_cap_truncation` (6 candidates → 3 retained) |
| A-6 (**F-11**) | **Overlay absent → quality_gates = 1.0 (unknown-as-clear).** When no `kala_vedha_gochara` row overlaps the evaluation window (including the entire century outside the overlay's ±60/+400 d coverage), the multiplicative product stays 1.0 — "fully gated". The honest 'unavailable' state does not exist on this path; the WP2 oracle's `quality_gates=None → 'unavailable'` contract is the fix's target, not the legacy behavior. | ACCIDENTAL BUG | `engine.py:287` ("1.0 when no vedha overlaps — AC1"), `:400` ("quality_gates = product … 1.0 when no vedha fired"), `:409-413` note; plan §3 F-11 "[L] Overlay coverage 1.26% of century silently reads as quality_gates=1.0". | WP5 (H-family; honest-null overlay states per F06/§N.7 item 6 — the WP2 case-10 contract) | REPRODUCED + labelled: golden test `test_f11_overlay_absent_quality_gates_one` |
| A-7 (**F-12**) | **First source root wins; `afflicted` qualifier stripped (pre-fix).** The pre-fix resonance writer de-duped on `(target_type, target_ref)` with `setdefault` (first root kept, later roots discarded silently) and ontology qualifier tokens ("10L afflicted") dropped from the emitted target. *Concurrent-workstream drift: WP3c is landing R-5 right now (keeps first root but records the discard + preserves `target_qualifier`), so this citation is to the pre-fix `HEAD` state.* | ACCIDENTAL BUG (per plan, measured live 2026-09-22/23 on the served map) | Pre-fix `writer.py` (git `HEAD`): `:361` `deduped.setdefault(key, row)`; `:122-123` "lords field contains 'afflicted' qualifier text — qualifier tokens are dropped"; target construction did not carry a qualifier column. Plan §3 F-12 "[L] Resonance keeps only the first source root (setdefault) and strips the afflicted qualifier", fixed by "G-R"/WP3c R-5. | WP3c **R-5** (in flight on this branch — verified in the working tree) | DOCUMENTED (upstream of the algebra: the baseline scores whatever targets it is handed; golden test `test_wp2_honesty_fixture_negative_targets_have_no_algebra_filter` demonstrates the algebra has no value/qualifier filter) |
| A-8 (**F-19**) | **Negative-result sensitive-degree targets (154/176).** Pre-fix `_FETCH_SENSITIVE_FACTS_SQL` selects by subject+key with **no value filter**, and `_build_sensitive_degree_rows` emits a target for EVERY check row — including `not_gandanta`, `mrityu_bhaga=not_fired`, `not_pushkara`, `kartari=none`. A negative check masquerades as a target the engine "evaluated". *Drift: WP3c R-1 (value filter, negative rows produce zero targets) is in flight on this branch.* | ACCIDENTAL BUG | Pre-fix `writer.py` (git `HEAD`): `:380-385` (`_FETCH_SENSITIVE_FACTS_SQL` — `SELECT fact_id, fact_subject, fact_key … AND fact_key = ANY(%s)` with no `fact_value_text` predicate); `:293-301` (`_build_sensitive_degree_rows` returns a row per fetched fact, no value test). Plan §3 F-19 "[L] 154 of 176 sensitive_degree targets cite a NEGATIVE check result". | WP3c **R-1** (in flight) | DOCUMENTED + golden test demonstrating the algebra-side absence of any predicate filter |
| A-9 (**F-28/F-29 + N-14**) | **w30 nodal drishti: enabled candidate whose citation is refuted, contradicting the corpus and the codebase's other module.** `SPECIAL_DRISHTI_DEG` maps Rahu/Ketu to Jupiter's `[120,180,240]` with a "BPHS Ch.26" citation the corpus refutes (Ch.26 names only Saturn/Jupiter/Mars; no node aspect rule surfaces across the admitted 15 texts — grade UNVERIFIED, citation REFUTED); w30's own docstring admits "NOT found in the original BPHS"; the engine multiplies it into every served λ. Cost of removal, measured: 85 Rahu + 87 Ketu served `drishti_contact` records and one factor leaving the product. | ACCIDENTAL BUG (served doctrine gap — a candidate running enabled with a false citation; the toggle mechanism itself is deliberate) | `primitives.py:193-194` (`"Rahu": [120.0, 180.0, 240.0]  # 5th, 7th, 9th (BPHS Ch.26 — same as Jupiter)`); `w30_nodal_drishti.py:11-12` ("is NOT found in the original Brihat Parashara Hora Shastra (BPHS) text itself"); `engine.py:103-107` (toggle True + "When True the engine will call _w30.compute() … incorporate w30_modifier"); corpus checks plan §3 F-29. | **N-14** (native ruled: extend "no node drishti" here; same-generation removal + labelled non-scoring annotation) | REPRODUCED (semantics preserved for the baseline; removal is N-14's, later) |

## 3. Boundary / precision artifacts observed and reproduced

| # | Behavior | Class | Evidence | Reproduction |
|---|----------|-------|----------|--------------|
| B-1 | Threshold certification is `>=` (not `>`) | DELIBERATE as written ("A JD is considered 'active' iff λ_v3 ≥ λ_thresh", `threshold.py:110`) — but it is the predicate that makes A-1 certifiable | `threshold.py:386-393` | REPRODUCED (`is_above_threshold`) |
| B-2 | Bisection tolerance 0.1 d; coarse sweep 7 d; dense peak scan 50 samples with **first-index argmax** (`np.argmax` semantics) | DELIBERATE | `interval_solver.py:37-40`, `:372-393` | REPRODUCED (`_bisect_crossing`, `_find_peak` with strict `>`) |
| B-3 | Peak admission at the era window's **own P90** (never lambda_thresh); flat series admits nothing; era window shorter than 3 samples scans nothing | DELIBERATE | `resolution_hierarchy.py:96-101` (R8.3), `:413-439`, `:639-678` | REPRODUCED (`admit_candidates`) |
| B-4 | Plateau flat-top admitted **exactly once, at its first point** | DELIBERATE-legacy (in tension with WP2 case 12's pinned tie-break — see P-1) | `resolution_hierarchy.py:370-405` ("admits the flat top of a plateau exactly once, at its FIRST point, rather than once per equal sample") | REPRODUCED (`find_local_maxima`, golden test `test_plateau_first_point_retention_legacy`) |
| B-5 | Day refinement: 1-day argmax over ±7 d around each retained candidate; era peak = 50-sample dense scan over the whole era span (up to multi-year → ~74-day-effective grid), stamped `gochara_lambda_v3_coarse_argmax` on era rows vs `gochara_lambda_v3_argmax` on month/day rows | DELIBERATE | `resolution_hierarchy.py:107-109`, `:551-598`; `peak_basis_vocab.py:21-44,69-82` | REPRODUCED (`refine_peak_to_day`, hierarchy month/day construction) |
| B-6 | Era windows are per **whole-decade sweep ranges** (10 × 10·365.25 d from the chart's birth epoch), one `build_resolution_hierarchy` call per (event_class, decade) | DELIBERATE (grid design) | `ka_gochara_v3_century_materialize.py:652-683` (`build_decade_slices`), `:2103-2109` | REPRODUCED (`build_decade_slices`, golden test `test_decade_slices_whole_decade_era_ranges`) |
| B-7 | Vedha overlap uses **DATE-string lexicographic comparison** (not instants) | DELIBERATE-approximation (disclosed: "sub-day precision irrelevant here", `engine.py:259-267`) | `engine.py:307-315` | REPRODUCED (`compute_quality_gates` takes ISO date strings) |
| B-8 | quality-gates evaluation window is the **activity window** [t−5d, t+5d] converted to dates (`_jd_to_date_iso` of start/end JD) | DELIBERATE | `engine.py:618-622` | REPRODUCED (window args are explicit) |
| B-9 | Chain milestones: evaluation failure → `lambda_v3 = 0.0`, `is_above_threshold` applied to it | ACCIDENTAL BUG (same 0.0 family as A-1; scored_chain_milestones' "Honest gap: evaluation failed — lambda is unknown, treat as 0.0", `interval_solver.py:453-456`) | `interval_solver.py:448-466` | REPRODUCED semantics documented (A-1 covers the certification path) |

## 4. Upstream input defects the baseline documents but does not reproduce (fixes live in other WPs)

| # | Defect | Class | Evidence | Fix owner |
|---|--------|-------|----------|-----------|
| C-1 (**F-19**) | Negative sensitive-degree targets reach the engine | ACCIDENTAL | see A-8 | WP3c R-1 (in flight) |
| C-2 (**F-20**) | Arudha `longitude_sidereal` is a sign-cusp placeholder (every live value = 30·(sign−1)); resolving it "to degree" would fabricate exact contacts | ACCIDENTAL (data hazard) | Plan §3 F-20 "[L] (270.0000, 0.0000, 60.0000 …)"; WP1 §2.2 item 6 | WP3c R-2 / M-5 (interval targets) |
| C-3 (**F-21**) | 2 of 13 yoga ids (`ardhachandra`, `chatra`; 54 rows) have no live `ga_yoga_firings` row — dangling L1 reference served as a target | ACCIDENTAL | Plan §3 F-21 "[L]"; pre-fix `writer.py` `_FETCH_YOGA_FIRINGS_SQL` (HEAD:393-407) fires=true filter but no build-time re-validation | WP3c R-3 + §5.5 input generation vector |
| C-4 (**F-12**) | First-root retention + qualifier stripping | ACCIDENTAL | see A-7 | WP3c R-5 (in flight) |
| C-5 (**H-4**) | **Eclipse-window-edge placeholder timestamps.** `eclipse_degree` sentences are anchored at node–luminary **conjunction-window events** (`find_eclipse_proximity_events` wraps `find_conjunction_events`, orb 8°), not at true eclipse instants from the L0 substrate — the "eclipse" event time is the proximity-window geometry, and its weight/orb (8°) is separate. | ACCIDENTAL (timing placeholder — weight is a method call, out of scope) | `primitives.py:1038-1081` (`eclipse_degree(…, orb_deg: float = 8.0)` over `find_eclipse_proximity_events`); `transit_search.py:678-710` ("Find windows where node_planet is within orb_deg of luminary. Flags potential eclipse windows."); plan §8.1 H-4 "replace eclipse-window-edge placeholder timestamps with real eclipse instants (timing only)" | WP5 **H-4** |
| C-6 | Sade-sati PERMISSION phase check builds a pseudo-period with `end_iso = "9999-12-31"` — a phase, once started, never closes on the v3 permission path | ACCIDENTAL (unbounded interval; the "best-effort degrade" docstring describes a different fallback) | `engine.py:1311-1331` (`pseudo_period = {"start_iso": start_iso, "end_iso": "9999-12-31T00:00:00+00:00"}`) | WP5-adjacent (not in H-1..H-6 — flag for the native; outside this run's pre-approved list, so documented only) |

## 5. Deliberate behaviors with tension against the new contracts (classified DELIBERATE — WP4 diffs, WP5 does not "fix" these without a ruling)

| # | Behavior | Class | Evidence | Note |
|---|----------|-------|----------|------|
| P-1 | Plateau tie-break: legacy admits the flat top once at its first point; WP2 case 12 pins ALL tied maxima admitted with the same rank | DELIBERATE-legacy (documented intent) vs. new pinned contract | `resolution_hierarchy.py:374-376` vs `WP2_FIXTURES.md` §8 | WP4's duplication test compares against the WP2 oracle's independent expectation — a real delta, classified to its input |
| P-2 | w30/tara toggles default True; w23's module docstring still says "CANDIDATE mechanism — NOT wired into engine.py. Togglable dormant module" while it IS wired and live (`engine.py:607-612`) | DELIBERATE (toggle design) with a **stale docstring honesty gap** | `w23_tara_bala.py:4` ("NOT wired into engine.py") vs `engine.py:598-612` | Plan N-14 records the same class of gap for w30 ("candidate while running enabled … its own honesty gap") |

## 6. Concurrent-workstream drift (recorded per WP0 discipline)

- `services/ka_gochara_resonance/writer.py` and `tests/l3/test_ka_gochara_resonance.py` are **modified in the working tree by WP3c while this WP ran** (R-1..R-6 landing). All pre-fix citations here use `git show HEAD:…` and are labelled as such. The served generation `'3.0'` rows that WP4's comparison targets were produced by the **pre-fix** writer — the classification above describes what those rows rest on.
- `services/gochara_kernel/__init__.py` is being built by WP3a concurrently; `legacy_semantics.py` is self-contained and the test loads it by file path so this WP is decoupled from the package `__init__`'s in-flight state.

## 7. Exit-gate statement

- Factor-by-factor equivalence on the WP2 matrix: the pinned oracle (`tests/l3/gochara/oracle.py`) and `legacy_semantics.py` agree exactly on E1–E4 (activity, λ_raw, λ_signed, box-empty → 0, `quality_gates=None` semantics) — 26/26 tests pass (`pytest tests/l3/gochara/test_wp3b_*.py -q`).
- Every reproduced artifact is classified above with file:line evidence and a golden test: A-1/A-2 (F-08), A-3 (F-09), A-4/A-5 (F-10), A-6 (F-11), B-4 (plateau), B-6 (decade ranges).
- Real-behavior equivalence (deliverable D): `services.gochara_v3.engine` imported successfully in the project venv; `_compute_activity_v3` run against the reproduction on identical synthetic sentences → exact agreement on activity, term_breakdown and counts; `threshold.is_above_threshold` + `compute_threshold_config` semantics (including the 0.0 ≥ 0.0 edge) agree on both sides.
- No stop condition from the execution prompt §6 was hit. Nothing was staged or committed. No file outside the three WP3b deliverable paths was modified.
