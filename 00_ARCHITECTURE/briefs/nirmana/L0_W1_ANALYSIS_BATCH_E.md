# L0-W1 Analysis — Batch E (bg_parihara_rules, bg_kota_chakra_rings, bg_sarvatobhadra_grid, bg_vedha_malefic_scale, bg_phaladeepika_latta, bg_kp_sublord_division, bg_gochara_arcs, bg_gochara_citation_resolution)

## bg_parihara_rules

1. **Pillars.** D-GROUNDING (every row carries `source_citation`/`source_text_id`, or an honest `not_in_corpus`/`not_computed` disposition in the census table) and D-SERVICE (the writer seeds three tables — `bg_parihara_rules`, `bg_muhurta_activity_rules`, `bg_muhurta_factor_census` — and all three are read live). It is the right instrument for both: it never re-ingests corpus content, it strictly filters out the 53/79 `brahma_dosha_catalog` rows citing the placeholder `"classical_tradition"`, and it records corpus gaps (mṛtyu-yoga, dagdha-yoga, Śiva-vāsa, etc.) as `not_in_corpus` rather than fabricating them — a clean §N.8 pattern (a real detector, not a green rubber stamp).
2. **Dependencies.** Registry declares `depends_on: [bg_doshas, bg_texts]`. Writer (`platform/python-sidecar/pipeline/orchestrator/writers/bg_parihara_rules.py`) reads `brahma_dosha_catalog` (bg_doshas' table) and `classical_texts` (bg_texts' table) via `fetch_parihara_rows`, and imports `panchang_engine.shastra_tables.EVENT_TABLES` (in-memory constants, no DB dependency) for the activity-rules table. Declared and real dependencies match; no mismatch found.
3. **Leverage.** Real, live, end-to-end consumer: `platform/src/lib/retrieval/registry/layers/L0_brahmagyan/query_parihara_graph.ts` issues `SELECT ... FROM bg_parihara_rules`, `FROM bg_muhurta_activity_rules`, `FROM bg_muhurta_factor_census` directly, is registered in `L0_brahmagyan/index.ts` and mapped to the `query_parihara_graph` MCP tool in `tool_name_bridge.ts`. Not a "built-but-unplugged" case — this is one of the better-wired L0 assets in the batch.
4. **Grounding.** Yes — this is exactly the classical-rule/remedy catalog the rubric flags. `bg_parihara_rules` rows carry `source_citation`/`source_text_id`/`source_chapter` (sruti-tier where a real verse citation resolves); the census table's `disposition` field (`computed` / `not_computed` / `not_in_corpus`) is itself a grounding-honesty mechanism, distinguishing cited doctrine from uncited convention (`computed_uncited_convention` notes appear throughout, e.g. `visha_ghati`, `yamakantaka`, `krakaca`) from corpus gaps. No `grounding_tier` column exists on the table itself, but the underlying data already carries what a `grounding_tier` label would need to be derived from (cited vs uncited vs gap) — a straightforward W2 NOW candidate.
5. **Temporal identity.** N/A in the L3-arbitration sense — this is muhūrta (election) factor-quality data, not a transit/gochara temporal-identity question. It does interact with time (activity-class quality by tithi/nakshatra/vara), but there is no competing "which engine's clock is right" arbitration question here.
6. **Service.** Real consumer confirmed (point 3). `target_floor=447`; `count_sql` sums all three tables' row counts — reasonable, but note this makes the floor an aggregate over three tables rather than one, which could mask a regression in one table if another over-delivers. `integrity_check_sql` is `null` — no hash/shape integrity check exists for this asset, unlike its siblings in this batch (kota_chakra_rings, vedha_malefic_scale, phaladeepika_latta, kp_sublord_division, gochara_arcs, gochara_citation_resolution all have one).
7. **Cost.** Unmeasured — would need runtime instrumentation. No documented build-duration figure found for this writer (it is explicitly a LIGHT writer, bounded DB query + in-memory constant materialization, "nowhere near the heavy-writer threshold" per its own docstring, but no measured number is recorded anywhere in the repo).
8. **Findings → W2.**
   - NOW: no `integrity_check_sql` exists for this asset (unlike every sibling in this batch) — worth adding a byte-identity/shape check per the established pattern, bounded cost, direct precedent to copy from.
   - NOW: the census table's `disposition` field already encodes what a `grounding_tier` label needs; formalizing it into a `grounding_tier` column (or documenting the mapping) would close the D-GROUNDING gap cheaply, citing D-GROUNDING (P3).
   - LATER: the named corpus gaps (mṛtyu-yoga, dagdha-yoga, Śiva-vāsa OCR-translation) are real ingestion work items but are out of L0-W1/W2 scope — they require OCR-translation work on `classical_text_chunks`, not an L0 writer change.

## bg_kota_chakra_rings

1. **Pillars.** D-GROUNDING (ADJUDICATION-9 explicitly disclosed tier: "tier-(iii) secondary-source transcription; `corpus_status='not_in_corpus'` on every row") and D-SERVICE (moved out of an inline writer-code dict into a versioned, byte-identity-checked L0 table specifically to be a shared, citable reference). Right instrument: it correctly demotes an inline hardcoded dict to governed reference data rather than claiming a primary-source citation it doesn't have.
2. **Dependencies.** Registry declares `depends_on: []`. Writer (`bg_kota_chakra_rings.py`) delegates entirely to `brahmagyan.l0_kota_chakra_rings.seed_kota_chakra_rings`, a pure in-memory constant table with no DB reads beyond its own INSERT. Declared and real dependencies match — genuinely dependency-free reference geometry.
3. **Leverage.** Real consumer, but only at the writer level, not a served surface: `platform/python-sidecar/services/ka_kota_chakra/writer.py:89` — `FROM bg_kota_chakra_rings ... WHERE table_version = (SELECT MAX(table_version) FROM bg_kota_chakra_rings)`, feeding `ka_kota_chakra` (L3). This is confirmed by `00_ARCHITECTURE/control/ZERO_CONSUMER_EVIDENCE_v1_0.md` §`bg_kota_chakra_rings` as reading **INPUT-ONLY** — "consumed, but by writers rather than by a served surface. The absence of a serving consumer is what the asset is for, not a defect." No TS-side (`platform/src`, `platform-mcp`) reference to this table was found at all — the serving surface a caller would read is `kala_kota_chakra` (the L3 derived product), not this L0 substrate directly, which is architecturally correct (L0 substrate is an L3 input, not itself a served table).
4. **Grounding.** Yes, and already disclosed honestly: every row is tier-(iii) secondary-source with `corpus_status='not_in_corpus'` — a documented gap (primary-source ingestion is filed as a separate work item, not attempted here). This is a `yukti`/uncited-convention case, not `sruti` — matches the doctrine's "an honest yukti/pratyaksa label is success, not failure."
5. **Temporal identity.** N/A — this is a nakshatra-distance ring partition (spatial/relational, not time-indexed); no L3 clock-arbitration question attaches to it.
6. **Service.** Real consumer confirmed (point 3), floor 27 = measured 27 (met). `integrity_check_sql` is present and strong — a full byte-identity SHA256 hash over the ordered row set plus a `count(*) = 27` check.
7. **Cost.** Unmeasured — would need runtime instrumentation. No documented build-duration figure found (this is a 27-row constant-table seed; cost is presumptively negligible but no number is recorded).
8. **Findings → W2.**
   - NEVER/LATER: primary-source ingestion for the ring partition (upgrading tier-(iii) to tier-(i)) is a real, named, but out-of-scope work item — the writer itself already files it honestly rather than fabricating a citation.
   - NOW candidate (low cost): confirm/record the `ka_kota_chakra` → `kala_kota_chakra` serving path explicitly reads this L0 table's rows correctly (a lineage-proof exercise, cheap given the byte-identity check already exists) so D-SERVICE's "every active asset has a consumer or a recorded disposition" is demonstrable end-to-end, not just at the immediate-writer hop.

## bg_sarvatobhadra_grid

1. **Pillars.** D-GROUNDING primarily — this asset exists specifically to avoid a false grounding claim. Per ADJUDICATION-11, SBC grid geometry varies by Jyotish tradition and no single school has been source-verified, so the table is registered DELIBERATELY EMPTY rather than seating one unverified school's geometry as fact. This is the right instrument for the purpose: an empty, school-keyed table that honestly states "variants exist" is itself the correct output, not a defect to fix. Per the task's explicit instruction and the plan's own §5 framing, this asset's emptiness is NOT flagged as a MUST or a defect here.
2. **Dependencies.** Registry declares `depends_on: []`, `has_writer: false`. No writer file exists under `pipeline/orchestrator/writers/` for this asset — confirmed by grep (no match). Table creation is by migration only (`platform/supabase/migrations/529_bg_sarvatobhadra_grid.sql`), with a separate throughput-sentinel migration (`553_bg_sarvatobhadra_grid_throughput_sentinel.sql`) presumably marking it `lit` with 0 rows so the orchestrator does not treat 0 rows as an unbuilt/failed asset. No dependency mismatch possible since there is no writer to check against.
3. **Leverage — this is the interesting case.** A real consumer already exists and already queries the table: `platform/python-sidecar/services/ka_vedha_gochara/writer.py:115` — `FROM bg_sarvatobhadra_grid`. Per the registry description and `query_vedha_gochara.ts`'s own comment (`L3_kala/query_vedha_gochara.ts:23-27`), a populated `school_tag` row would activate `ka_vedha_gochara`'s DB-sourced-grid-first path "with zero code change" — the consumer-side plumbing is already built and waiting; only the data (and the school-selection ruling) is missing. This is a clean instance of "leverage already wired, deliberately withheld pending a ruling" — not a leverage gap to close via code, but a data/decision gap.
4. **Grounding — candidate schools/sources found for W2's presentation.** Searching the writer/schema/adjacent code for named candidate schools surfaces:
   - The `school_tag` column itself (confirmed in `query_vedha_gochara.ts`'s comment) implies the table is designed to hold multiple competing school variants side by side, keyed by tag — not a single ruling baked into the schema.
   - `bg_gochara_citation_resolution` (migration 565) independently names three untraced classical-source candidates for Sarvatobhadra Chakra construction, all recorded `status='unresolved'` (honest corpus gaps, not rulings): `Muhurta Chintamani` / `Jyotish Sara Sangraha` (28-nakshatra 9×9 grid — citing `l1_sarvatobhadra_positions`/`l1_sarvatobhadra_vedha` table comments and the archived `platform/migrations/_archive/140_sarvatobhadra_chakra.sql`), and `Prasna Marga` (rekha/kona/vithi vedha geometry — noted as not even present as a `text_id` in the current corpus). These three are the concrete "sources" W2 can present alongside the school-selection decision, though none is yet corpus-verified.
   - The currently-populated fallback path is `'algorithmic_approximation'` (per `query_vedha_gochara.ts`'s own doc comment), flagged `uncited_extension=true` — i.e., today's served SBC data is explicitly NOT a classical-school grid, it's an algorithmic stand-in, disclosed as such on every row via `grid_basis`/`grid_school_tag`/`uncited_extension` columns.
5. **Temporal identity.** N/A — Sarvatobhadra Chakra is a nakshatra/direction grid (spatial), not itself a time-indexed engine; it feeds `ka_vedha_gochara`'s vedha grading, which IS gochara-adjacent, but the grid geometry question is a school-adjudication question, not a clock-arbitration one.
6. **Service.** Consumer exists and executes today (falls back to the algorithmic-approximation path since the table is empty) — floor is honestly 0, matching measured 0. No `integrity_check_sql` (appropriately none needed for an intentionally-empty table). This matches `ZERO_CONSUMER_EVIDENCE_v1_0.md`'s own reading: **BY DESIGN EMPTY / CATEGORY MISMATCH** — "the consumer question as posed does not apply to this row in its current form."
7. **Cost.** N/A — no build cost; the table is empty by design and there is no writer.
8. **Findings → W2.**
   - NOT a MUST — explicitly not a defect per the task's framing and per ADJUDICATION-11's own record.
   - W2 DECIDE item (per plan §5, this is the plan's own named decision point, not this document's proposal): present the school-selection choice with the three candidate-source threads above (school_tag design intent, the two unresolved corpus citations, and the current algorithmic-approximation fallback's disclosed uncited-extension status) and populate only on a ruling.
   - NOW-adjacent observation (not a defect): the consumer-side "zero code change on population" design is already proven by `query_vedha_gochara.ts`'s own architecture — this lowers the cost of a future population decision, worth noting in W2 as a reason the decision is cheap to action once made.

## bg_vedha_malefic_scale

1. **Pillars.** D-GROUNDING — a small (5-row), REAL, cited classical table (Phaladeepika Adh. XXVI PG353's malefic-count → effect-grade scale), transcribed verbatim per ADJUDICATION-11 Part 4. Right instrument: a small, high-precision, citation-carrying lookup table is exactly the correct shape for this doctrine (it should not be folded into a larger, less-precisely-cited catalog).
2. **Dependencies.** Registry declares `depends_on: []`. Writer (`bg_phaladeepika_vedha.py`) delegates to `brahmagyan.l0_phaladeepika_vedha.seed_vedha_malefic_scale`, a pure in-memory constant table (`MALEFIC_SCALE_ROWS`), no DB reads. Match confirmed — genuinely dependency-free.
3. **Leverage.** Real, live consumers at the writer level, not merely declared: `platform/python-sidecar/services/gochara_v3/context.py:485` — `FROM bg_vedha_malefic_scale` (pre-fetched for "W1.3 suppression grading"), and `platform/python-sidecar/services/ka_vedha_gochara/writer.py:128` — same table. Named (not queried) in the TS serving layer's provenance comment (`query_vedha_gochara.ts:154`). `ZERO_CONSUMER_EVIDENCE_v1_0.md` reads this **INPUT-ONLY** — consumed by two independent writer paths (gochara_v3 engine's suppression grading AND ka_vedha_gochara), which is a stronger leverage picture than a single-consumer asset; the absence of a *serving* consumer is architecturally correct (this is L0 substrate feeding L3 computation, served indirectly via `kala_vedha_gochara`).
4. **Grounding.** Yes, cleanly — REAL and cited (Phaladeepika Adh. XXVI PG353), verbatim transcription, `source_citation`/`verse_ref` columns present per the `integrity_check_sql`'s own hashed-column list. This is a `sruti`-tier case (text-direct verse citation), the strongest grounding tier available.
5. **Temporal identity.** N/A directly — this is a static effect-grading scale (malefic-count → effect-grade), not itself a temporal engine; it is consumed BY the gochara/vedha temporal machinery (as a suppression-factor lookup) but does not itself answer a "when" question.
6. **Service.** Real consumers confirmed (point 3); floor 5 = measured 5 (met); `integrity_check_sql` present and strong (byte-identity SHA256 over the 5-row set). Noted per the task's briefing: this is the O-wave's rehearsal-B asset, currently being actively exercised to test delta-skip/rebuild machinery — its build/rebuild behavior is instrumented right now by concurrent work, not stale.
7. **Cost.** Unmeasured — would need runtime instrumentation. Given it is a 5-row constant-table writer, cost is presumptively trivial; no documented figure found. (Being the rehearsal-B asset means the O-wave work may produce a measured number for this asset specifically as a side effect — worth checking that output when available rather than re-measuring independently.)
8. **Findings → W2.**
   - No MUST-level defects found.
   - NEVER/LATER: none identified beyond what's already correctly scoped (this table is complete at its own 5-row target).
   - Coordination note (not a W1 finding per se): this asset is under active O-wave rehearsal; W2 routing decisions should confirm they don't conflict with or duplicate the rehearsal's own build/rebuild exercises.

## bg_phaladeepika_latta

1. **Pillars.** D-GROUNDING — REAL, cited Phaladeepika Adh. XXVI PG338-339 Sloka 42-44 Lattā (obstruction-point) rule, transcribed verbatim, 8 graha rows (Ketu deliberately absent — its counting rule was not found in the retrieved passage, an honest omission rather than an invented value). Right instrument, same reasoning as `bg_vedha_malefic_scale`.
2. **Dependencies.** Registry declares `depends_on: []`. Writer (`bg_phaladeepika_vedha.py`) delegates to `brahmagyan.l0_phaladeepika_vedha.seed_phaladeepika_latta`, in-memory constants (`LATTA_ROWS`), no DB reads. Match confirmed.
3. **Leverage.** Real consumer: `platform/python-sidecar/services/ka_vedha_gochara/writer.py:123` — `FROM bg_phaladeepika_latta`, feeding `ka_vedha_gochara`'s `vedha_kind='latta'` rows with `uncited_extension=false`. Named (not queried) in `query_vedha_gochara.ts:154`'s provenance list. `ZERO_CONSUMER_EVIDENCE_v1_0.md` reads **INPUT-ONLY**, same architecturally-correct pattern as its scale sibling.
4. **Grounding.** Yes — REAL and cited (Phaladeepika Adh. XXVI PG338-339 Sloka 42-44), `sruti`-tier, with the Ketu gap honestly disclosed rather than papered over (an explicit instance of the "honest null beats an invented judgment" principle, CLAUDE.md §N.7 item 6).
5. **Temporal identity.** N/A directly — a static per-graha counting/obstruction rule, consumed by the vedha/gochara machinery as a lookup, not itself a temporal-question engine.
6. **Service.** Real consumer confirmed; floor 8 = measured 8 (met); `integrity_check_sql` present and strong (byte-identity SHA256 over the 8-row set, keyed by `table_version, graha`).
7. **Cost.** Unmeasured — would need runtime instrumentation; no documented figure found; presumptively trivial (8-row constant writer).
8. **Findings → W2.**
   - No MUST-level defects found.
   - LATER (named, out of scope): Ketu's Lattā counting rule remains an honest, disclosed gap — a genuine corpus-research item (finding the missing passage), not an L0 writer defect.

## bg_kp_sublord_division

1. **Pillars.** D-GROUNDING (tier-(iii) KP-convention citation, honestly disclosed — "no KP text is ingested in this corpus," verified instead against a committed fixture) and D-SERVICE (the explicit §N.5 authority: `ga_nakshatra` REFERENCES this table rather than re-deriving KP boundaries). Right instrument — the module derives the 249 count from first principles (243 Vimśottarī sub-segments + 6 rāśi-boundary splits) rather than hardcoding it, which is exactly the kind of "derived not asserted" discipline the doctrines want.
2. **Dependencies.** Registry declares `depends_on: [bg_nakshatra]`. The writer's own docstring is easy to misread as contradicting this ("depends_on: [] — pure reference geometry, no upstream dependency"), but reading the actual code (`brahmagyan/l0_kp_sublord_division.py`, `verify_star_lords_against_reference`) shows a real, if *optional*, read: it queries `reference_nakshatra.vimshottari_lord` (one of the three tables `bg_nakshatra` writes) as a cross-check on every division's derived `star_lord`. Crucially this is a **fail-open soft dependency**: if `reference_nakshatra` is absent or under-populated, the check reports `unverified` (never a fabricated pass) rather than halting the build — but if a real mismatch is found, it correctly halts (`RuntimeError`, `divergent_flagged`). So the registry's hard `depends_on` entry is stricter than the code technically requires, but it is the *correct* entry: it guarantees `bg_nakshatra` builds first, which is what lets the cross-check actually run instead of silently degrading to `unverified`. Declared and real dependency are consistent; no mismatch, but the writer-level docstring's blanket "no upstream dependency" phrasing is locally misleading and worth a one-line correction.
3. **Leverage.** Real consumers at two levels: (a) `ga_nakshatra.py` (L1) calls `load_divisions()` from `l0_kp_sublord_division.py` — a genuine §N.5-compliant "reference the authority, never re-derive" read, with `load_divisions` raising loudly if the table is unbuilt/empty rather than silently falling back to re-derivation; (b) `platform/python-sidecar/services/ka_kshetra/stage3_clocks.py:624` — `FROM bg_kp_sublord_division` (per `ZERO_CONSUMER_EVIDENCE_v1_0.md`). No TS-side serving consumer reads this table directly; `ganita_kp_cusps_get` (the plausible TS-side candidate) reads `chart_facts` instead (the L1-derived per-chart projection), which is architecturally correct layering, not a leverage gap. `platform-mcp/src/resources/school_conventions.ts` mentions the table in prose (documentation for an LLM-facing resource describing KP method), not as a live query — worth distinguishing documentation-mentions from real reads.
4. **Grounding.** Mechanical/computational for the geometry itself (pure rational arithmetic over BPHS Vimśottarī year constants — no interpretive claim), but the KP-specific *convention* (starting the sub-cycle from the nakshatra's own lord, cutting at rāśi boundaries) is a tier-(iii) published-convention citation, honestly labeled as such (no KP text is ingested; verified against a committed fixture instead of corpus citation). So: computational at the arithmetic layer, `yukti`-tier at the convention layer — both disclosed accurately in the module's own `SOURCE_CITATION` string.
5. **Temporal identity.** N/A — this is a static sidereal-longitude geometry table (ayanāṃśa-invariant by ADJUDICATION-7's own binding ruling), not a temporal/transit engine; no L3 clock-arbitration question attaches.
6. **Service.** Real consumers confirmed (point 3); floor 249 = measured 249 (met); `integrity_check_sql` is unusually strong for this batch — validates count, version uniqueness, index contiguity 1..249, exact-arc-sum-to-360° tolerance check, and non-empty citation on every row.
7. **Cost.** Unmeasured — would need runtime instrumentation; no documented figure found (a 249-row Fraction-arithmetic computation is presumptively cheap).
8. **Findings → W2.**
   - No MUST-level defects found.
   - NOW candidate (very low cost, editorial only): correct the writer module's docstring line "depends_on: [] — pure reference geometry, no upstream dependency" to acknowledge the optional-but-real `reference_nakshatra` cross-check read, so a future reader doesn't have to reconcile it against the registry's `depends_on` entry the way this analysis had to.

## bg_gochara_arcs

1. **Pillars.** D-TIME (P6) primarily, and D-SERVICE. This is explicitly the W2G production-scalability keystone: chart-independent decomposition of every graha's ecliptic-longitude history into monotone arcs, computed once globally and joined by every chart forever, replacing the ~110-120ms-per-contact-primitive-call cost of the v1 day-stepping sweep with a range predicate + bisection. Right instrument, and a well-documented architectural win (measured: **34,553 arcs for the entire 1900-2150 epoch in ~48 seconds**, per `00_ARCHITECTURE/NIRMANA_ELEVATION_PLAN_v2_0.md`/`v4_0.md` and `control/ZERO_CONSUMER_EVIDENCE_v1_0.md`).
2. **Dependencies.** Registry declares `depends_on: [bg_ephemeris]`. Writer (`bg_gochara_arcs.py`) calls `services.w2g.db_source.build_arcs_for_body`, which reads the `ephemeris_daily` table — confirmed as the table `bg_ephemeris`'s writer populates (`ga_writers`... actually `pipeline/orchestrator/writers/bg_ephemeris.py`, "seeds ephemeris_daily via WriterBase"). Declared and real dependency match exactly.
3. **Leverage.** Real consumer at the writer level: `platform/python-sidecar/pipeline/orchestrator/writers/ka_gochara.py` — `SELECT DISTINCT body, arc_fingerprint FROM bg_gochara_arcs`, feeding `ka_gochara`'s L3 `generation='2.0'` rows into `kala_gochara_windows_v2`. `ZERO_CONSUMER_EVIDENCE_v1_0.md` reads this **INPUT-ONLY**: "0 serving consumers, 1 writer consumer (`ka_gochara`)... zero read-context matches under `platform/src` or `platform-mcp/src`." This is architecturally correct — the served gochara surfaces (`gochara_forecast_get`, `gochara_activation_get`, etc.) read the materialized per-chart `kala_gochara_windows` product, not the global arc substrate directly, exactly as the module's own docstring intends ("built ONCE by the writer... joins the chart-INDEPENDENT contact stream against ONE chart's natal map").
4. **Grounding.** Correctly mechanical/computational, as the rubric anticipated — the writer's own docstring is explicit: "Geometry only: no grammar, no orbs, no astrology... those stay frozen at v1." No interpretive claim is made here; no `grounding_tier` is warranted on this asset itself.
5. **Temporal identity — the L3-relevant case.** The question this asset answers: "over the entire stored ephemeris epoch, on which contiguous time intervals is graha B's ecliptic longitude strictly monotone and confined to one 360° band" — i.e., it turns "when does body B reach degree L" into a bracketable range query rather than a day-stepping scan. It feeds the L3 `ka_gochara` writer's `generation='2.0'` product in `kala_gochara_windows_v2`. The plan's §5 L3 note ("gochara v1 archive, retired writer, snapshot-only; v2/v3 confirmed authority") is directly relevant: `kala_gochara_authority` (read by `ka_kshetra/stage4_field.py` and `ka_kshetra/writer.py`) is the arbiter table that selects, per chart, which `generation` (v1/v2/v3) of `kala_gochara_windows` is authoritative — this asset's downstream (v2.0) is one of the candidate generations that authority table arbitrates between, not itself the arbiter.
6. **Service.** Real consumer confirmed; floor 34,553 = measured 34,553 (met); `integrity_check_sql` is the strongest in this batch — validates total count, version uniqueness, per-body distinct fingerprint, engine/ayanamsha consistency, AND an exact per-body row-count table (Saturn 503, Jupiter 494, Rahu 13544, Ketu 13553, Mars 376, Sun 252, Mercury 1894, Venus 580, Moon 3357) with contiguous-index and single-fingerprint checks. This is materially more rigorous than most siblings in this batch.
7. **Cost — a real documented figure exists**, satisfying the rubric's exception clause: **~48 seconds** for the full 1900-2150 epoch, all 9 bodies (`NIRMANA_ELEVATION_PLAN_v2_0.md`: "`bg_gochara_arcs`: the whole epoch in ~48 seconds, computed once, reused forever"; corroborated in `v4_0.md` and `ZERO_CONSUMER_EVIDENCE_v1_0.md`). Per-body substep timing and live serve-side query cost are unmeasured — would need runtime instrumentation.
8. **Findings → W2.**
   - No MUST-level defects found. This is one of the more solidly built and verified assets in the batch.
   - NOW-adjacent observation (not a gap to close, but worth W2 recording): the INPUT-ONLY reading is correct by design — D-SERVICE's "built-but-unplugged" defect class does not apply here, since the intended consumer (`ka_gochara`) is confirmed wired.
   - Cross-reference for L3 (out of L0 scope, named for continuity): the `kala_gochara_authority`/generation-arbitration mechanism this asset ultimately feeds is exactly the D-TIME Temporal Concordance Contract surface named in plan §5's L3 section — L0-W1/W2 should not attempt to resolve that arbitration question, only confirm the L0 substrate itself is sound (which it is).

## bg_gochara_citation_resolution

1. **Pillars.** D-GROUNDING (P3) — closes the "W2.9 gap": citation strings used inside gochara windows (from `gochara_grammar/citations.py` constants and `primitives.py` families) resolved to `classical_text_chunks` verse refs, with honest `CORPUS_GAP:`-prefixed stubs for unresolved ones. Right instrument for the purpose: a small, explicit resolution table is exactly the correct shape for bridging a citation-string vocabulary to a verse-ref corpus without re-deriving or guessing.
2. **Dependencies.** Registry declares `depends_on: [bg_texts]`, `has_writer: false`. There is no orchestrator writer for this asset — it is seeded entirely by migration `platform/supabase/migrations/565_bg_gochara_citation_resolution.sql`, which reads/cross-checks against `bg_texts`' `classical_text_chunks` table (verified-before-seeded pattern) before INSERTing. The dependency is real (the migration's every `resolved` row was checked against `l0_texts.py`'s `SEED_CHUNKS` before being written) even though it is expressed as a migration rather than a writer read — consistent with `data_disposition: RETAINED_AS_CAPITAL` (a one-time capital-seeded asset, not a rebuildable writer output).
3. **Leverage.** Real, live, currently-wired consumer: `platform-mcp/src/tools/retrieval/register_gochara_windows.ts` — `fetchVerseRefs()` issues `SELECT citation_string, chunk_id, text_id, verse_ref, status FROM bg_gochara_citation_resolution WHERE citation_string IN (...)`, enriching served gochara windows with `citation_verse_refs` on `gochara_forecast_get`/`gochara_activation_get`. Fails soft (empty Map) if the table is unreachable, so it never breaks serving — a correctly-designed additive enrichment, not a silent-NULL defect. This is one of only two assets in this batch (with `bg_parihara_rules`) confirmed to have a genuine TS-side serving read, not merely a writer-level one.
4. **Grounding.** This table IS the grounding mechanism for gochara citations — its own `status` column (`resolved`/`unresolved`) is a direct, machine-checkable grounding-tier signal (resolved = verified corpus chunk_id FK; unresolved = honest, explicitly-tagged `CORPUS_GAP:` stub, never fabricated). This is a clean, working instance of exactly the D-GROUNDING discipline the doctrine calls for.
5. **Temporal identity.** Distinct from `bg_gochara_arcs` in kind: this table does not itself answer a "when" question — it answers "which classical verse backs this already-computed gochara citation string." It supports the temporal *narration* of gochara windows (grounding, not arbitration). No L3 clock-arbiter consumes it; it is consumed purely by the serving-layer enrichment path above. So: N/A for temporal-identity arbitration, though it is gochara-domain-adjacent as the task anticipated.
6. **Service and integrity — a MUST-level finding.** Registry `target_floor=14`, and `integrity_check_sql` demands exactly **`count(*)=14`, 1 `resolved` row, 13 `unresolved` rows**, plus an exact SHA256 byte-identity hash over the full ordered row set. However, migration `565_bg_gochara_citation_resolution.sql` — **the only migration for this table present on the current branch (`campaign/nirmana-autonomous`, HEAD `86140afb7`)** — seeds **4 `resolved` rows** (`GRAHA_DRISHTI_BPHS_26`, `GRAHA_DRISHTI_RASI_BPHS_26`, `VAKRA_RETROGRADE_BPHS_27`, `PHALADEEPIKA_VEDHA_26`) and 10 `unresolved` rows (14 total — the count matches, but the resolved/unresolved split does not: 4/10 vs. the required 1/13). Git history shows the correction: migrations `630_nirmana_l0_wave1_correctness_contract.sql` and `631_nirmana_l0_gochara_citation_chunk_repair.sql` demote the two `GRAHA_DRISHTI_*` rows to `unresolved` (their `bphs_ch26_v001` chunk mapping was found to be absent from the frozen 10,651-chunk corpus — migration 631's own comment: "migration 630 exposes the fact that the two Graha Dṛṣṭi rows point to a chunk which is not in the frozen ... corpus... retain the citations as explicit corpus gaps"), and these migrations **are present on `origin/main`** (confirmed via `git ls-tree origin/main -- platform/supabase/migrations/` and `git branch -r --contains 6e01c2a77`) but are **not an ancestor of the current campaign branch's HEAD** (`git merge-base --is-ancestor 6e01c2a77 HEAD` → not an ancestor). A third demotion (bringing `VAKRA_RETROGRADE_BPHS_27` or `PHALADEEPIKA_VEDHA_26` from resolved to the final 1-resolved state) was not directly located in this pass but is implied by the same integrity target; not confirmed further within this read-only pass's scope.
7. **Cost.** Unmeasured — would need runtime instrumentation; not applicable in the writer sense since this is a one-time migration seed, not a rebuildable writer (`has_writer: false`).
8. **Findings → W2.**
   - ~~**MUST**: verify whether the current campaign branch (and/or the live production DB it targets) has migrations 630/631 applied.~~ **RESOLVED, downgraded (verified 2026-09-04 by the orchestrating session, not this batch agent):** confirmed live against production Cloud SQL via a read-only query — `SELECT status, count(*) FROM bg_gochara_citation_resolution GROUP BY status` returns exactly `resolved=1, unresolved=13`, matching `integrity_check_sql`'s requirement. Migrations 630/631 ARE applied in production; both files exist on `origin/main` (`platform/supabase/migrations/630_nirmana_l0_wave1_correctness_contract.sql`, `631_nirmana_l0_gochara_citation_chunk_repair.sql`). The apparent gap was an artifact of this batch agent correctly-but-locally observing that `campaign/nirmana-autonomous` (this session's long-running tracking branch, used for governance/tracker commits, not a deploy branch) is 165 commits behind `origin/main` — the branch that every O-wave code PR (#1696-#1699) is actually based on and merges into. Not a production defect. **Genuine but far lower-priority NOW, not MUST:** `campaign/nirmana-autonomous` itself is significantly stale relative to `main` and should be rebased/refreshed before it's used as a basis for any future work, to avoid this exact false-alarm pattern recurring for other assets analyzed against this checkout.
   - NOW: once (or if) the branch is reconciled with main's migration set, re-verify the full byte-identity hash in `integrity_check_sql` actually matches — this analysis could not execute SQL to confirm the hash live (read-only constraint), only that the row-count *shape* (resolved/unresolved split) is currently inconsistent as checked out.
   - LATER (named, out of scope for L0): the 10 (or more, post-correction) `unresolved` corpus gaps recorded here (BPHS Ch.29/66/71/22, Phaladeepika double-gochara, Muhurta Chintamani Tara Bala/Sarvatobhadra OCR gaps, Prasna Marga absent-from-corpus) are real corpus-ingestion work items, correctly disclosed as gaps rather than fabricated, and out of L0 writer scope.
