# SESSION PROMPT — L2 BODHA (v2.1)

Read `/Users/Dev/Vibe-Coding/Apps/Madhav/00_ARCHITECTURE/briefs/nirmana/sessions/SESSION_CHARTER_V21.md`
FIRST and obey it fully (E-gate C2, autonomy C3, worktree/git C4, resources C5 — migration range
660-669, capability-deltas C6, scheduler C8, resumability C9, gate SQL C10). You are the
**L2 session**: you own layer L2's 22 assets through all six waves, W1 ANALYZE → W6 FREEZE,
per `NIRMANA_UNIFIED_ELEVATION_PLAN_v2_0.md` §4 (template) and §5 (your mandate). The native is
asleep; questions go to the Conductor per C3, never to a human.

## Bootstrap
Worktree `~/nirmana-s/l2` per C4 · state file `L2_STATE.md` (stub landed by Conductor —
rebase onto it) · read your layer's §5 mandate + the five doctrines (plan §2) + the L0 method
inheritance: reuse the Conductor-owned batch runner, two-phase idempotent dispatch/evidence,
prove-once-then-batch — do NOT rebuild tooling.

## Your mandate (plan §5, L2)
THE PAYOFF LAYER. (a) Salience completion: argala (41,760 L1 facts) + AV support + vargottama into STORED salience; cancellation modifiers verified; downstream re-rank cascade. (b) Synthesis rollups via bo_samvada: populate system_convergence_count, cross_system_consensus_count, contradicts_signals_array from existing L1 facts; land the adjudication-rule table (D-SYNTHESIS: singular verdict voice, stored reasoned rulings). (c) Grounding matcher: signal config → sutravali antecedent_jsonb → verse refs; PLUG bg_concordance (721 rows, currently unconsumed); populate classical_sources_array + corroboration counts + grounding_tier on the ~15-20 interpretive signal classes ONLY (D-GROUNDING tiers sruti/yukti/pratyaksa — an honest yukti/pratyaksa label is success; fabricated citation is a floor violation). (d) Tail lane: bodha_anomalies low_salience_high_consequence promoted to first-class serving input; hard-floored tail_watch section in umbrella envelopes (D-SALIENCE). (e) Serving siblings in W3: consensus chip, resolve_grounding spine extension, lens drill. (f) Hygiene: UCN→UCD retirement completed per the L2 handoff; L2 __ssv_* dispositions; signal-embedding serve path verified.

## Wave specifics
- **W1**: all 22 assets against the plan §4 rubric (leverage-NULL check, grounding-tier
  labelability, service state, measured cost); fan out read-only subagents; reuse any
  current-pinned analysis bases. Deliverable: `L2_W1_ANALYSIS_*.md`.
- **W2**: one route per asset + MUST/NOW/NEVER triage with doctrine citations →
  `L2_W2_DECIDE_v1_0.md`. Publish your contract/capability-delta list (C6) immediately.
- **W3**: batched disjoint-write-set PRs through the queue. HELD until upstream capability
  lands (C6): none inbound (you are the PUBLISHER others wait on — announce each landed capability in L2_STATE §CAPABILITIES LANDED the moment its PR merges: consensus columns populated; grounding fields populated; tail_watch shipped; adjudication table live)
- **W4**: E-gate per asset (C10); slot etiquette (C5); `force=true` on `rebuild_only`
  (logged); **canary first — prove the cross-layer gate on: the first W2-routed asset whose L0/L1 ancestors freeze (derive with C10 each loop; expect early candidates among bo_* whose ancestors are L0-only) — one fully through
  build→verify→freeze before batching.**
- **W5**: mechanical scripts + fresh-context verification subagent; verifier SA appends
  capsules; implementer≠certifier absolute.
- **W6**: request Conductor's ordering ack, then submit your layer freeze; closure-safe sync;
  publish `L2_W6_CLOSE_REPORT_v1_0.md` per C11 (incl. pillar movement + cost actuals).

## Layer notes
- You are the campaign's critical path: your W3 is the heaviest in the programme — start it immediately (it needs no upstream), and sequence salience+rollup writers before serving siblings.
- 50,104-signal re-rank is a heavy write: schedule its build as a monster (solo slot).
- Re-read L2_BODHA_CAMPAIGN_HANDOFF traps (MSR computed-value authority inversion; UCN contamination) before touching writers.

Definition of done: C11. Then end cleanly.
