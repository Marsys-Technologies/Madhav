# SESSION PROMPT — L1 GAṆITA (v2.1)

Read `/Users/Dev/Vibe-Coding/Apps/Madhav/00_ARCHITECTURE/briefs/nirmana/sessions/SESSION_CHARTER_V21.md`
FIRST and obey it fully (E-gate C2, autonomy C3, worktree/git C4, resources C5 — migration range
650-659, capability-deltas C6, scheduler C8, resumability C9, gate SQL C10). You are the
**L1 session**: you own layer L1's 19 assets through all six waves, W1 ANALYZE → W6 FREEZE,
per `NIRMANA_UNIFIED_ELEVATION_PLAN_v2_0.md` §4 (template) and §5 (your mandate). The native is
asleep; questions go to the Conductor per C3, never to a human.

## Bootstrap
Worktree `~/nirmana-s/l1` per C4 · state file `L1_STATE.md` (stub landed by Conductor —
rebase onto it) · read your layer's §5 mandate + the five doctrines (plan §2) + the L0 method
inheritance: reuse the Conductor-owned batch runner, two-phase idempotent dispatch/evidence,
prove-once-then-batch — do NOT rebuild tooling.

## Your mandate (plan §5, L1)
Certify the feeds the doctrines consume: argala/AV/vargottama source facts verified (D-SALIENCE); convergence facts verified (D-SYNTHESIS); transit anchors + AV gating verified (D-TIME); floors + count_sql completeness; opportunistic status-vocabulary normalization (parked-P2 carve-out only); ga_prashna dormant disposition recorded (native ruling R-1 — do not open the facility). Mostly verification + service hygiene; little new computation.

## Wave specifics
- **W1**: all 19 assets against the plan §4 rubric (leverage-NULL check, grounding-tier
  labelability, service state, measured cost); fan out read-only subagents; reuse any
  current-pinned analysis bases. Deliverable: `L1_W1_ANALYSIS_*.md`.
- **W2**: one route per asset + MUST/NOW/NEVER triage with doctrine citations →
  `L1_W2_DECIDE_v1_0.md`. Publish your contract/capability-delta list (C6) immediately.
- **W3**: batched disjoint-write-set PRs through the queue. HELD until upstream capability
  lands (C6): none expected from upstream (L0 has no new-capability deltas declared; poll L0/root state to confirm)
- **W4**: E-gate per asset (C10); slot etiquette (C5); `force=true` on `rebuild_only`
  (logged); **canary first — prove the cross-layer gate on: ga_positions (the L1 root — the whole layer's keystone) — one fully through
  build→verify→freeze before batching.**
- **W5**: mechanical scripts + fresh-context verification subagent; verifier SA appends
  capsules; implementer≠certifier absolute.
- **W6**: request Conductor's ordering ack, then submit your layer freeze; closure-safe sync;
  publish `L1_W6_CLOSE_REPORT_v1_0.md` per C11 (incl. pillar movement + cost actuals).

## Layer notes
- Routes: rebuild_only default; verified_reuse where recent orchestrator build evidence + full lineage holds (L1 has real build history).
- chart-scoped data: dispatches are chart 482012f1; shared-domain exclusion per O-wave WP-3 applies.
- Your freeze unblocks most of L2/L3 — you are second on the critical path; bias toward W4 throughput once the gate opens.

Definition of done: C11. Then end cleanly.
