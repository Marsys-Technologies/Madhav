# SESSION PROMPT — L3 KĀLA (v2.1)

Read `/Users/Dev/Vibe-Coding/Apps/Madhav/00_ARCHITECTURE/briefs/nirmana/sessions/SESSION_CHARTER_V21.md`
FIRST and obey it fully (E-gate C2, autonomy C3, worktree/git C4, resources C5 — migration range
670-679, capability-deltas C6, scheduler C8, resumability C9, gate SQL C10). You are the
**L3 session**: you own layer L3's 23 assets through all six waves, W1 ANALYZE → W6 FREEZE,
per `NIRMANA_UNIFIED_ELEVATION_PLAN_v2_0.md` §4 (template) and §5 (your mandate). The native is
asleep; questions go to the Conductor per C3, never to a human.

## Bootstrap
Worktree `~/nirmana-s/l3` per C4 · state file `L3_STATE.md` (stub landed by Conductor —
rebase onto it) · read your layer's §5 mandate + the five doctrines (plan §2) + the L0 method
inheritance: reuse the Conductor-owned batch runner, two-phase idempotent dispatch/evidence,
prove-once-then-batch — do NOT rebuild tooling.

## Your mandate (plan §5, L3)
One clock, many instruments. (a) Temporal Concordance Contract: engine question-declarations in the registry; the arbiter surface extending kala_explain/kala_now emitting aligned|partially_aligned|disputed(adjudicated_by) with per-engine testimony as drill; authority profiles generalized from kala_gochara_authority + kala_paddhati_profile; concordance verdict wired as salience temporal multiplier (D-TIME). (b) Dispositions: gochara v1 archive (17,240 rows, retired writer, snapshot-only) formally archival — SNAPSHOT RULE ABSOLUTE; v2/v3 authority confirmed; kala_taranga decided (derived view vs independent witness) — decided either way, logged; all Kāla __ssv_* dispositioned. (c) WP-4 heavy pass: profile ka_kshetra (8.6M rows) + gochara century materializer; substep chunk-parallelism where the writer contract allows; width/LPT tuning from measured data (freeze-exception rides §3.5, scheduling constants only, logged). (d) Quality overlays (moorti, kota, vedha, sudarshana, tithi-pravesha) verified as consumed modulation, not shelf inventory.

## Wave specifics
- **W1**: all 23 assets against the plan §4 rubric (leverage-NULL check, grounding-tier
  labelability, service state, measured cost); fan out read-only subagents; reuse any
  current-pinned analysis bases. Deliverable: `L3_W1_ANALYSIS_*.md`.
- **W2**: one route per asset + MUST/NOW/NEVER triage with doctrine citations →
  `L3_W2_DECIDE_v1_0.md`. Publish your contract/capability-delta list (C6) immediately.
- **W3**: batched disjoint-write-set PRs through the queue. HELD until upstream capability
  lands (C6): the salience temporal-multiplier wiring waits for L2's consensus/salience capabilities (C6 — poll L2_STATE); everything else in your mandate is upstream-independent
- **W4**: E-gate per asset (C10); slot etiquette (C5); `force=true` on `rebuild_only`
  (logged); **canary first — prove the cross-layer gate on: ka_gochara_resonance and ka_graha_sancara (both fully eligible TODAY — run one as the gate canary immediately after W2 routes it) — one fully through
  build→verify→freeze before batching.**
- **W5**: mechanical scripts + fresh-context verification subagent; verifier SA appends
  capsules; implementer≠certifier absolute.
- **W6**: request Conductor's ordering ack, then submit your layer freeze; closure-safe sync;
  publish `L3_W6_CLOSE_REPORT_v1_0.md` per C11 (incl. pillar movement + cost actuals).

## Layer notes
- ka_kshetra and the century materializer are the campaign's monsters: solo slots, always (C5).
- The v1-gochara corpus is the hard floor's named irreplaceable: archival disposition must not touch rows; snapshot verified before ANY destructive step near it.
- Many of your assets have shallow ancestor chains — expect your W4 to open earlier than layer intuition suggests; run the C10 batch query every loop.

Definition of done: C11. Then end cleanly.
