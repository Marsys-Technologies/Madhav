# SESSION PROMPT — L5 MĪMĀṂSĀ (v2.1)

Read `/Users/Dev/Vibe-Coding/Apps/Madhav/00_ARCHITECTURE/briefs/nirmana/sessions/SESSION_CHARTER_V21.md`
FIRST and obey it fully (E-gate C2, autonomy C3, worktree/git C4, resources C5 — migration range
690-699, capability-deltas C6, scheduler C8, resumability C9, gate SQL C10). You are the
**L5 session**: you own layer L5's 15 assets through all six waves, W1 ANALYZE → W6 FREEZE,
per `NIRMANA_UNIFIED_ELEVATION_PLAN_v2_0.md` §4 (template) and §5 (your mandate). The native is
asleep; questions go to the Conductor per C3, never to a human.

## Bootstrap
Worktree `~/nirmana-s/l5` per C4 · state file `L5_STATE.md` (stub landed by Conductor —
rebase onto it) · read your layer's §5 mandate + the five doctrines (plan §2) + the L0 method
inheritance: reuse the Conductor-owned batch runner, two-phase idempotent dispatch/evidence,
prove-once-then-batch — do NOT rebuild tooling.

## Your mandate (plan §5, L5)
Seam-keeping for the parked loop (native ruling: P7 built subsequently): STRUCTURAL mode re-documented as deliberate; prediction provenance retention verified; journal/adjudication-log seams confirmed intact; insight-embedding serve path noted for the future programme; NO calibration values invented (§N.8 absolute). Routes: mostly verified_reuse/static against existing build evidence; lel_events is the user-authored source disposition (L5 exception per the frozen manifest contract).

## Wave specifics
- **W1**: all 15 assets against the plan §4 rubric (leverage-NULL check, grounding-tier
  labelability, service state, measured cost); fan out read-only subagents; reuse any
  current-pinned analysis bases. Deliverable: `L5_W1_ANALYSIS_*.md`.
- **W2**: one route per asset + MUST/NOW/NEVER triage with doctrine citations →
  `L5_W2_DECIDE_v1_0.md`. Publish your contract/capability-delta list (C6) immediately.
- **W3**: batched disjoint-write-set PRs through the queue. HELD until upstream capability
  lands (C6): nothing feature-inbound; your W4 tail (deep mi_* assets, global tiers up to 25) waits on L1-L4 ancestor freezes — the C10 query is your calendar
- **W4**: E-gate per asset (C10); slot etiquette (C5); `force=true` on `rebuild_only`
  (logged); **canary first — prove the cross-layer gate on: lel_events (source disposition — fitting first capsule), then mi_jivanaghatana and mi_vistara (all three fully eligible TODAY) — one fully through
  build→verify→freeze before batching.**
- **W5**: mechanical scripts + fresh-context verification subagent; verifier SA appends
  capsules; implementer≠certifier absolute.
- **W6**: request Conductor's ordering ack, then submit your layer freeze; closure-safe sync;
  publish `L5_W6_CLOSE_REPORT_v1_0.md` per C11 (incl. pillar movement + cost actuals).

## Layer notes
- Your three eligible canaries prove the cross-layer gate at the FAR end of the DAG — valuable early signal; run them as soon as W2 routes them.
- Anything that would ADD calibration/learning machinery is out of scope → NEVER/LATER with the deferred-register citation (plan §7.3).
- Your layer's freeze closes the build arc: your close report feeds the Conductor's Phase Z directly — draft it early.

Definition of done: C11. Then end cleanly.
