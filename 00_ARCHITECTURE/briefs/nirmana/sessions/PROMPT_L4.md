# SESSION PROMPT — L4 PHALA (v2.1)

Read `/Users/Dev/Vibe-Coding/Apps/Madhav/00_ARCHITECTURE/briefs/nirmana/sessions/SESSION_CHARTER_V21.md`
FIRST and obey it fully (E-gate C2, autonomy C3, worktree/git C4, resources C5 — migration range
680-689, capability-deltas C6, scheduler C8, resumability C9, gate SQL C10). You are the
**L4 session**: you own layer L4's 9 assets through all six waves, W1 ANALYZE → W6 FREEZE,
per `NIRMANA_UNIFIED_ELEVATION_PLAN_v2_0.md` §4 (template) and §5 (your mandate). The native is
asleep; questions go to the Conductor per C3, never to a human.

## Bootstrap
Worktree `~/nirmana-s/l4` per C4 · state file `L4_STATE.md` (stub landed by Conductor —
rebase onto it) · read your layer's §5 mandate + the five doctrines (plan §2) + the L0 method
inheritance: reuse the Conductor-owned batch runner, two-phase idempotent dispatch/evidence,
prove-once-then-batch — do NOT rebuild tooling.

## Your mandate (plan §5, L4)
Verdicts adopt the doctrines: one agreement line + strongest sruti quote per verdict (D-SYNTHESIS + D-GROUNDING); varshaphala/tithi-pravesha consumption into anchors PROVEN (Discovery D-7); tail_watch in outlooks (D-SALIENCE); honest probability surfaces preserved; prediction provenance hygiene (parked-P7 seam) verified untouched.

## Wave specifics
- **W1**: all 9 assets against the plan §4 rubric (leverage-NULL check, grounding-tier
  labelability, service state, measured cost); fan out read-only subagents; reuse any
  current-pinned analysis bases. Deliverable: `L4_W1_ANALYSIS_*.md`.
- **W2**: one route per asset + MUST/NOW/NEVER triage with doctrine citations →
  `L4_W2_DECIDE_v1_0.md`. Publish your contract/capability-delta list (C6) immediately.
- **W3**: batched disjoint-write-set PRs through the queue. HELD until upstream capability
  lands (C6): agreement-line + sruti-quote + tail_watch W3 items wait on L2's consensus/grounding/tail capabilities (C6); varshaphala consumption proof waits on L3's tithi-pravesha verification landing — HOLD those specific items, do everything else (W1/W2 fully; provenance-hygiene and probability-surface W3 items are unheld)
- **W4**: E-gate per asset (C10); slot etiquette (C5); `force=true` on `rebuild_only`
  (logged); **canary first — prove the cross-layer gate on: the first ph_* asset whose ancestors freeze (derive with C10; your layer's min global tier is 16 — expect mid-campaign) — one fully through
  build→verify→freeze before batching.**
- **W5**: mechanical scripts + fresh-context verification subagent; verifier SA appends
  capsules; implementer≠certifier absolute.
- **W6**: request Conductor's ordering ack, then submit your layer freeze; closure-safe sync;
  publish `L4_W6_CLOSE_REPORT_v1_0.md` per C11 (incl. pillar movement + cost actuals).

## Layer notes
- Smallest layer; your W1/W2 completes fast — then C8.5 productive-wait: pre-write W5 verification scripts for the whole layer, draft close-report scaffolding, deepen the anchors-consumption analysis so held W3 items land within hours of their capability unlocking.
- Never idle-loop noisily; quiet polls per C8.6.

Definition of done: C11. Then end cleanly.
