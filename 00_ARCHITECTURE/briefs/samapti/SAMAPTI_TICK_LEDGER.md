# SAMĀPTI_TICK_LEDGER

One entry per tick. The run's spine.

---

## TICK 0 — kickoff

- Spawned persistent agents: VER (opus/xhigh, agent id a9f067bcc32b8f97b), DVA (opus/high, agent id a14026bd1770a9b32). Both ACKed after resuming past a transient 529-overload wave that hit most of the initial dispatch.
- Dispatched all 8 Wave-A lanes: A1-PRESERVE, A2-CI-POINTERS, A3-SHADBALA-EVIDENCE, A4-LOOP-G1, A5-LOOP-G2G3, A6-GOCHARA-DIAG, A7-N8-AUDIT, A8-NAR-TRIAGE.
- Lanes A1, A2, A4, A6, A7 needed a resume after the same 529 wave interrupted them before real work began (no state lost — each resumed from its original instructions).
- A1-PRESERVE surfaced an early finding: the shared checkout is 68 commits behind origin/main, so some paths untracked locally may already be on origin/main — instructed to diff against origin/main specifically, not local HEAD, before dispositioning anything.

## TICK 1 — first lane completion

- A5-LOOP-G2G3 reported OUTCOME: COMPLETE. Branch samapti/loop-g2g3-cron-and-ci @ 1fc3eaad, draft PR #895 (not merged, auto-merge not enabled). Real CI run (30488849699) green with all 6 previously-skipped DB-integration test files executing and passing; can-fail proof via a deliberately-broken-secret dispatch (30489529898, went red) then reverted (1fc3eaad).
- Forwarded to VER for independent re-derivation (git diff pull, CI run URL inspection, revert-cleanliness check, and a residuals sanity-check on which lane owns the real-secret daily-job trigger). VER verdict pending.

## TICK 2 — A3 and A1 complete

- A3-SHADBALA-EVIDENCE reported OUTCOME: COMPLETE (read-only). All 7 grahas match the SUDDHA_VACA golden table live, zero divergence; FORENSIC 7/7 confirmed live; MCP smoke-test can-fail proof genuine (wrong-key mutation -> 401/FAIL, reverted -> PASS). Residuals: stale deploy.yml IAM comment, row-count baseline reconciliation ambiguity in CLAUDE.md/L1_GANITA_CLOSURE, shared checkout ~40 commits behind main. Forwarded to VER.
- A1-PRESERVE (blocking_for_run) reported OUTCOME: COMPLETE. Branches: samapti/preserve-work-at-risk @ a5cc942d (PR #896, open), preserve/unknown-provenance-20260730 @ 7df0813e (PR #898, draft), preserve/parishodhana-20260730 @ fa2e5c28 (PR #899, draft), preserve/parishodhana-dark-corpus-remeasure-20260730 @ f91ec00b (push-only). T0 BASELINE measured at origin/main: drift=216/exit3, schema=43/exit3. Two REFUTATIONS of the brief's own assumptions: kala_elevation/ is byte-identical to main (not ahead — no preserve branch needed, empty-diff); parishodhana's "2 unpushed commits" are patch-identical to commits already on origin/main (git cherry -v = "-" both). No force-push; parishodhana's own uncommitted diff survived byte-for-byte. Disclosed delta: A1's own merge moves schema 43->45 (two pre-existing missing-frontmatter defects in newly-tracked files, not fixed by this lane). Builder self-caught and discarded a false-green verification bug in its own evidence-gathering before finalizing.
- 3 QUESTIONS routed to DVA: (1) baseline disposition 216/43 vs 216/45 for E1-SAMGATI 8.8, (2) .fuse_hidden0000000500000001 deletion timing, (3) kala_elevation preserve-branch waiver (empty-diff case). Forwarded to VER for independent fact-verification in parallel (this is the blocking lane — nothing merges until VER confirms).

## TICK 3 — A6 complete

- A6-GOCHARA-DIAG reported OUTCOME: COMPLETE (read-only). Root cause pinned: writer_timeout_seconds (21600s) / per-substep cost (~253-280s median) / 303-substep plan ~= 27% -> one dispatch completes ~1/4 of the plan; canonical chart 482012f1 is fully lit only via 6 sequential resumed dispatches (attribution sums to exactly 303); operators 1c826d5a and cb73cd3d each dispatched exactly once, ever. 3 alternative hypotheses tested and refuted (resume-wipe livelock, idle-transaction timeout, 2027-horizon-truncation). 2 new findings: F3 (TS watchdog falsely-lit gap, contradicts C3's planned EP-1 NOT-APPLICABLE shortcut), F5 (hardcoded scoring-span/specimen years, scheduling-only bug). Branch samapti/gochara-parity @ d5907e64, PR #900. 4 QUESTIONS routed to DVA (C2 proceed?, BUILD-LOCK duration for C2, F5 fix timing, F3 vs C3's EP-1). Forwarded to VER for independent re-derivation of the mechanism.

## TICK 4 — recovery from a stall wave + A2 complete

- Three agents (A7-N8-AUDIT, A4-LOOP-G1, A2-CI-POINTERS) and VER itself hit a 600s stream-watchdog stall mid-task (distinct from the earlier 529-overload wave); all resumed from exactly where their transcripts left off, no state lost.
- VER's resumed transcript revealed its CONFIRMED verdict on A5-LOOP-G2G3 had already landed before the stall (recorded in SAMAPTI_VERIFICATION_LEDGER.md) — VER was moving to A1-PRESERVE next when it stalled; redirected it there plus the queued A3/A6 verifications.
- A2-CI-POINTERS (merge_priority 1) reported OUTCOME: COMPLETE. Branch samapti/ci-pointer-validation @ 4cae7c23, PR #901 (open, mergeable, no auto-merge). SC-pointer:x fixed at root (bad test placeholder); both QUARANTINED pointers resolved (stale test fixtures, prod already moved on); query_classical_texts filed as SC-23 (builder self-corrected an initial miswrite); SC-17/18/19 register rows corrected from stale OPEN to FIXED; tap5_seam_conservation's Law-7 fixed from a hardcoded no-op QUARANTINED string to actually executing the real battery (a self-found §N.8 violation inside the audit harness itself). 3 can-fail proofs. Change is vitest-neutral (77/946/18 both sides, reproduced via git stash A-B). Residuals: 77 pre-existing platform-mcp vitest failures (own lane needed, may overlap B-OIR-RECONCILE); tap-ci.yml network-dependent npx fetch (flagged only); SC-1..16/20..22 register rows still unverified; coordination flag vs B-NAR-TS's likely touch on registry_bridge.ts fixtures. Forwarded to VER, flagged as priority since it's supposed to merge first.

## TICK 5 — connection-drop/stall storm; A7's sub-audits land; urgent security finding

- This tick saw an elevated rate of transient connection drops and 600s stalls across nearly every live agent (VER, A1/A2/A4/A6/A7/A8 builders, and 3 of A7's 4 fanned-out sub-agents). No work was lost in any case -- every resume picked up from the agent's own transcript. Two of A7's sub-agents (2 sub-audits) repeatedly tried to SendMessage to invalid targets ("general-purpose", "the peer", "the coordinator") instead of returning their findings as final text; redirected each to report as plain final-message text, which worked.
- A7-N8-AUDIT's 3 of 4 sub-audits landed with strong results:
  - "known-member §N.8 writers" (bo_pramana_mapa.py, ga_nakshatra.py, bo_chart_gestalt.py): 11 confirmed findings (lel_zero_leak_pass proxy, pillars_meet_reachability_pass tautology, 2 literal-zero counters, ga_nakshatra.py:87's unconditional verification_pass_status='PASS' -- the run's strongest single finding, with a second-order effect: 3,955 chart_facts rows now score as UNGROUNDED in envelope.ts's grounding check due to a vocabulary case-mismatch --, bo_chart_gestalt's doctrine-violating verdict-in-writer, plus 5 siblings), several rejected/genuine findings recorded as negatives.
  - "CI gates, smoke tests, guards": PB-2 byte-equality gate reclassified from register's "no comparison" to PROXY (real comparison, but of a test-owned reimplementation never the shipped adapter); PB-3 item 9 no-auto-promotion confirmed NO-DETECTOR-EXISTS; MCP smoke cross-referenced to A3, bounded PROXY; several swallowed-exit-code findings (icr_weekly_scan.yml CANNOT-FAIL, hard_gates_check.sh claims exit 1 on RED but has zero exit statements, chat-v2-ci.yml no-op gate stages); schema_validator.py flagged as MED/PROXY (ci.yml misdescribes its own gate as hardened). **SECURITY: found a live plaintext production DB password in hard_gates_check.sh:46, invisible to secret_scan.sh because its scope excludes 00_ARCHITECTURE/ entirely -- routed to DVA as an out-of-band urgent ruling, not folded into the normal register.**
  - "TS serving layer verdicts": 7 confirmed findings incl. a first-class §N.6 GOVERNANCE FALSE-GREEN -- the density_contract compliance harness (generate_tool_census.ts scoreA4) grades machine-backfilled empty_reason/paginated fields as "enforced" without ever inspecting source; synthesis_gate/fully_accounted tautology (critical, affects every domain-assessment tool response); freshness.stale literal-false estate-wide; an inherited-claim cross-reference to the other sub-audit's ga_nakshatra.py finding via the verification_pass_status -> verified_fraction -> epistemic.grade chain.
  - 4th sub-audit (Python build-layer writers: runner.py/staleness.py/dag_edge_guard.py/kala_derivation_completeness_guard.py/service_probes.py) never produced usable output after ~6 resume attempts -- being reported as incomplete-coverage residual.
- Urgent DVA ruling requested: credential rotation timing, whether to touch the non-owned-provenance file to redact it, and whether to fix secret_scan.sh's 00_ARCHITECTURE/ exclusion gap.

## TICK 6 — Wave A builder work now fully reported; A1 CONFIRMED

- VER returned CONFIRMED on A1-PRESERVE (see SAMAPTI_VERIFICATION_LEDGER.md) -- the T0 baseline is now operative, pending DVA's disposition ruling (216/43 vs 216/45). VER flagged origin/main moved mid-run (cdb6fc3b -> 5f5033a5 via PR #897, SHAD-DARSHANA) -- routed to DVA to confirm this is just a routine pre-merge-lock re-check, not a new ruling. VER lost A3/A6/A2 FINAL_SUMMARYs to the connection-drop storm and requested resends; done.
- A7-N8-AUDIT's 4th sub-audit (Python build-layer writers) finally landed after ~7 resume cycles: 5 confirmed findings (2 TAUTOLOGY in ga_dashas_writer.py's per-dasha-system verifiers, most only checking chart 482012f1 or closed-loop against their own source constant; 11 sites of hardcoded "pass" status that silently score LOWEST tier server-side while a different consumer treats the same string as fully verified; a tautological points_only_assertion flag; a fabricated two-valued source-corroboration count that ignores the real source array). Relayed to A7 for final register assembly.
- A4-LOOP-G1 (P0 highest-leverage lane) reported OUTCOME: COMPLETE. Branch samapti/loop-g1-mount-confirm @ 6e60a1be, PR #902. Mounted the missing confirm entry point via /api/pariprashna/route.ts calling captureDetectedCandidates post-writeTurn; new migration 474 fixes a latent FK trap (470's NO ACTION would have blocked turn rewrites once ledger rows exist) with SET NULL. Full chain proven live against real Postgres end-to-end (not production). 4 can-fail mutations, shasum-verified clean reverts. Builder explicitly does NOT claim the live production proof (PB-3.1 A1-A3) -- hands VER/C4 a complete, exact post-deploy verification protocol instead. Forwarded to VER with instructions to split pre-merge verification (now) from live post-deploy verification (after merge, feeding into C4-LOOP-LIVE-PROOF).
- A8-NAR-TRIAGE reported OUTCOME: COMPLETE. Branch samapti/nar-triage @ 97c77ae8, PR #903. Confirmed SV-3/SV-4(code)/P1-e already closed by SHA, PLUS found a 4th previously-unnoticed already-closed item (mi_darshana.py:159, #839). 7 PLAUSIBLE P2 items adversarially re-examined: 4 confirmed (2 escalated above P2), 3 rejected with mechanisms. Reversed seed F18's standing CANNOT-REPRODUCE disposition with live proof it's now reproducible and shipping -- reported the correction rather than following the original instruction blindly. Clean 42-file/42-path partition for the 6 B-NAR-* lanes, zero overlaps. 3 QUESTIONS routed to DVA (ga_nakshatra.py two-lane file conflict, F18 correction standing, severity re-band for 2 escalated findings). Residual: a prior audit's pointer file + a seed-audit file are at risk of being lost (uncommitted) -- new small preservation task, not yet dispatched.
- **All 8 Wave-A lanes have now reported FINAL_SUMMARY at least once** (A1-A8 all COMPLETE from the builder's side). A7-N8-AUDIT is finalizing its register with all 4 sub-audits folded in. Once VER confirms A7 and A8, Wave B (6 narration lanes + 3 N8 lanes, all depends_on: [A7-N8-AUDIT] or [A8-NAR-TRIAGE]) becomes dispatchable.

## SECURITY ESCALATION — incident materially worse than Ruling 1 assessed

B-SECRET-ROTATE-PREP's investigation (read-only, zero mutations, PR #907) found:
- INC-1 (Ruling 1's subject, hard_gates_check.sh:46) is ALREADY DEAD -- superseded 2026-06-02. Its live-and-public window was ~4 days, not ~2 months.
- INC-2 (the credential that REPLACED it, committed in plaintext 3 days later, 2026-06-05) is LIVE RIGHT NOW -- amjis-web is currently serving with it. 16 tracked files, 23 occurrences, public 55 days.
- INC-3 (a THIRD credential, in no prior finding) is LIVE and authenticates as the Postgres SUPERUSER role (cloudsqlsuperuser, CREATEDB, CREATEROLE). 2 files, first committed 2026-04-29, public 92 days, held in NO Secret Manager secret -- likely survived 3 months of audits because its DSN is mislabelled with the wrong username.
- secret_scan.sh returns PASS/exit=0 with a live superuser credential on origin/main -- two independent gaps: scope (already known, B-SECRETSCAN-SCOPE) AND pattern-shape (the scanner's regex needs `KEY=value` shape; the actual code reads via `os.environ.get("KEY", "<literal>")` -- widening scope alone will NOT catch this).
- amjis-mcp holds NO database credential at all (refutes Ruling 1's assumption it was a consumer).
- deploy.yml:304 hardcodes the current secret VERSION NUMBER -- if not bumped as part of rotation, the next ordinary merge to main would silently revert amjis-web to an old credential, a delayed-outage trap.
- Two other production-proxying workflows found with unclear/absent credential sourcing (fresh_chart_smoke.yml reads prod AS THE POSTGRES ROLE via a passwordless DSN; shad-darshana-circularity-guard.yml, SAD-DARSANA-owned, not touched).
5 QUESTIONS routed to DVA: does Ruling 1 need amending (INC-3 should be the lead item, not INC-1)? widen B-SECRET-REDACT from 9 to 27 files? where does the pattern-shape scanner gap land? how to handle the two other prod-proxying workflows? should a compromise assessment be a lane given the early window may be past log retention?

## SECURITY CROSS-VALIDATION — B-SECRETSCAN-SCOPE independently confirms B-SECRET-REDACT's 27-file scope from a different angle

B-SECRETSCAN-SCOPE (repaired scanner, PR #911) independently found THREE distinct credential VALUES for the same amjis_app role on the same production DB, across 27 tracked files -- CRED-A (9 files, the originally-inventoried one), CRED-B (15 files, never inventoried by name before this scan), CRED-C (2 files). This lines up almost exactly with B-SECRET-REDACT's own independently-derived 27-file count (INC-1=9/INC-2=16/INC-3=2 -- off by one on the middle number, worth a quick reconciliation but not alarming given two completely different methodologies converged on the same total).
Coverage proof: repaired scanner detects 27/27 real credential-bearing files; the OLD scanner detected 0/27. Can-fail proof: planted 2 synthetic credentials outside the old allowlist -- OLD scanner falsely PASS, NEW scanner correctly RED, reverted to clean.
Also found: platform/scripts/governance/v13_production_gate.py was INSIDE the old scanned allowlist the whole time and STILL hid a live credential -- the pattern-shape gap (env-read-with-literal-default, actively excused by the old env-indirection tolerance rule) is a real, separate defect from the path-coverage gap, exactly as DVA's Ruling 20 anticipated.
Also found and correctly did NOT fix (out of lane): secret_naming.md:102's claim "the literal no longer exists in HEAD" is FALSIFIED -- the scan finds that value still live on origin/main in 2 files outside the old scanned directories.
4 questions routed to DVA: whether B-SECRET-REDACT's scope needs further extension (likely moot, already resolved by Ruling 19 + B-SECRET-REDACT's completed 27-file round -- needs reconciling the 15-vs-16 file-count discrepancy); whether the rotation runbook needs restating for 3 credential values not 1; disposition of an incidentally-found Firebase public API key; confirmation that landing the gate fully-blocking (stronger than the warn-only fallback Ruling 3 authorized) was the right call given manageable volume.

## SECURITY — B-SECRET-ROTATE-PREP final round: severity/persistence/reachability findings

Key new findings beyond the already-recorded severity upgrade:
- MECHANISM DETAIL: CREATEROLE grants membership in ANY non-superuser role without ADMIN OPTION -- so rotation ALONE does not evict an attacker who already established persistence (e.g. by creating a new role). Not tested (would be a mutation) but documented from observed PG15 semantics.
- Version-match CONFIRMED not just probable: amjis-db-password v2 (2026-05-28, still enabled) and amjis-pipeline-db-url v2 (2026-05-30, still enabled) both hash-match INC-1; v3 of both match INC-2.
- CROSS-CONFIRMS B-SECRET-REDACT's own fix: independently found the SAME live credential in test_ka_jivana_parva_circularity_guard.py:64 that B-SECRET-REDACT already redacted (with DVA's Ruling 29/34 degradation-shape fix) -- this branch's read predates that fix (separate branches), so this is confirmatory, not a new gap.
- fresh_chart_smoke.yml confirmed: declares role via a PASSWORDLESS DSN, has NEVER actually reached the DB (its only run failed at GCP auth, all DB steps skipped) -- a declared-not-live consumer, needs repointing to retrieval_census_ro before its WIF issue is fixed.
- RETENTION IS BETTER than assumed: Admin Activity logs reach back to the instance's creation day (2026-04-25), giving 100% coverage of all 3 exposure windows, not ~30 days as originally estimated.
- Exhaustive connection-principal sweep: exactly 6 principals have EVER connected (4 current + 2 dormant project SAs); negative queries excluding all 6 over the full window return ZERO rows -- no 7th identity has ever connected.
- Persistence sweep clean (30 roles, 13 grant edges, no anomaly) but EXPLICITLY caveated as point-in-time only -- retrieval_census_ro itself was created IN-SQL with no corresponding CREATE_USER admin-op, proving the admin trail doesn't cover in-SQL role creation at all.
- NEW SIGNIFICANT FINDING: ~10 service-account identities (not just credential-holders) can reach the instance via IAM roles (roles/cloudsql.client x8, roles/editor x3 which confers it implicitly) -- narrows the "empty authorizedNetworks is the only gate" framing: the real exploitation precondition is "compromise ANY ONE of ~10 service accounts," not "compromise the project owner."
- Biggest open gap: authorizedNetworks CHANGE HISTORY still not established -- the one open question that could still change the compromise conclusion, with a rolling-retention deadline (earliest exposure days start dropping 2027-05-30).
5 questions routed to DVA: does the reachability finding change the risk rating; sequencing/authority for touching the circularity-guard file (already resolved by B-SECRET-REDACT, needs cross-lane confirmation not new work); whether authorizedNetworks history needs its own lane; whether the CREATEDB/CREATEROLE-narrowing for the new role should be mandated not just recommended; whether to prune 2 unnecessary IAM grants now or as a follow-on.

## B-AUTHNET-HISTORY resolved -- the time-critical finding is NEGATIVE (good news)

authorizedNetworks on amjis-postgres NEVER held any value (broad or narrow) between instance creation (2026-04-25) and today. Two independent instruments (Cloud SQL control plane request-body inspection + windowed Admin Activity audit log) agree exactly: 17 non-backup operations total, 8 that could touch the field (1 CREATE + 7 UPDATE, all SqlInstancesPatchRequest = merge semantics so absence means not-touched not cleared), NONE contain an ipConfiguration key. Zero discrepancies between the two methods. amjis-postgres is the only Cloud SQL instance in the project, no CLONE operation.
Notable technique finding: DVA's ruled PRIMARY method (Cloud Asset Inventory) was both unavailable (API disabled, enabling would be a mutation) AND structurally insufficient (35-day retention vs the 96-day question) -- the builder used the ruled FALLBACK method instead and it worked cleanly. Worth DVA's awareness for future rulings.
CONCLUSION: Ruling 36's overturn risk did NOT materialize. Rotation is still NOT discharged -- severity is now driven entirely by credential privilege + the ~10-identity IAM reachability (exactly Rulings 39/40's target), not network exposure. E1-SAMGATI's binding from Ruling 38 is now UNBLOCKED -- the close report may state its compromise conclusion.

## VER's exhaustive 19-PR-body audit (DVA Ruling 41) -- CLEAN, incident contained

VER ran 5 independent checks across all 19 open SAMAPTI/preserve PRs: self-identification, declared-lane-id-vs-branch, foreign-lane-id legitimacy, body-cited-files-vs-actual-diff overlap (the strong test), and pairwise body-similarity/duplicate-block detection. RESULT: ZERO no-overlap cases, ZERO similarity-threshold hits, no second mismatch found. The scratchpad collision that hit PR #910 (B-VERIFSTATUS-VOCAB) was a single contained incident, already caught and fixed by that lane's own builder.
Two cosmetic (non-blocking) findings: PR #905 (B-SECRET-REDACT) and #911 (B-SECRETSCAN-SCOPE) don't spell out their literal lane id in the body text, though both are substantively about their own correct work -- worth a one-line gh pr edit each for close-report readability, not a correctness issue.
VER's own recommendation for E1-SAMGATI: re-run the body-vs-diff overlap check (the strongest of the 5) immediately before the merge queue opens, since bodies remain editable until merge and this audit has a shelf life.

## CROSS-LANE ALERT -- SHAD-DARSANA is actively building concurrently (Night 2), not quiescent

VER found origin/main advanced during its own checks: 5f5033a5 -> d7bc0a9a via PR #916 "docs(shad-darshana): Night 2 open -- ADJUDICATION-1 (cohort MD-lord), 5 lanes dispatched", merged 2026-07-30T04:15:49Z. SHAD-DARSANA merge cadence on main shows 5 merges in the run's window (#892/893/894/897/916), the latest of which explicitly announces 5 NEW lanes just dispatched -- this is an actively-building autonomous swarm running concurrently with SAMAPTI, not a quiescent campaign that merged once and stopped.
Zero samapti/* or preserve/* PRs have merged (A2's merge, dispatched earlier this tick, has not yet completed). All 19 open SAMAPTI lanes are now behind main and require rebase (12 one-behind at 5f5033a5, 6 two-behind at cdb6fc3b).
Consequence flagged by VER: every CONFIRMED verdict's CITED CI EVIDENCE was measured against a pre-this-move SHA -- verdicts on the CODE hold, but CI-run-specific evidence (e.g. A5-LOOP-G2G3's un-skip proof, A2-CI-POINTERS's harness-green measurement) should be re-checked post-rebase before final merge, not assumed to still apply verbatim.
Routed to DVA as a stop-and-decide per Ruling 11's MERGE-LOCK precondition (SHAD-DARSANA is not merely "open", it is actively adding lanes -- the quiescence window this run's design assumes may not resolve on a normal one-tick timescale while another swarm's own Night 2 is running).

## B-LOOP-G4G5 interim: G5d premise-refutation independently VERIFIED by VER (full lane verdict still pending G4/G5 can-fail reproduction)

VER traced the CALIBRATION_CONTEXT_ONLY_URIS mechanism to source (no_leakage_filter.ts + generate_projections.ts's explicit "excluded from all three profiles" comment) and confirmed: applying G5d as originally briefed WOULD have deleted query_spine_bundle from all 3 MCP surface profiles -- a real surface deletion, not a metadata annotation. Builder's refusal to apply it, parking with a costed reversal spec instead, confirmed correct -- "the fourth time in this run a brief premise has decayed, and the builder caught it rather than executing it."
VER self-disclosed correcting its own initial misread (first pass suggested the flag only governed planner-selectability, which would have made the builder wrong -- following it further through generate_projections.ts revealed the actual profile-exclusion effect).
ONE PRECISION CORRECTION flagged for the eventual close-report/reversal-spec wording: query_spine_bundle is in the PROFILE MANIFEST but NOT in the live served catalog and NOT registered as an MCP tool in platform-mcp/src at all -- this is the same manifest-vs-served gap B-MCP-CATALOG-GAP already established (152 manifest/124 served/15 overlap). The park is still correct; the reversal spec should say "profile manifest" not "live profile" to avoid misleading whoever eventually executes it.
Full B-LOOP-G4G5 verdict still pending G4/G5 can-fail reproduction by VER.

## CONFIRMED: live migration-474 collision -- A4 vs origin/main itself, not lane-vs-lane

VER independently re-confirmed B-MIGGUARD's headline finding via its own read-only check (git ls-tree/diff only, no branch checked out or modified) and SHARPENED it: B-WATCHDOG-LIT adds NO migration at all (VER verified this earlier -- 'incomplete' was already shipped by the pre-existing 474_asset_throughput_incomplete_state.sql). The real collision is A4-LOOP-G1 (branch samapti/loop-g1-mount-confirm) vs ORIGIN/MAIN ITSELF: A4 adds platform/supabase/migrations/474_samiksha_ledger_part_fk_on_delete_set_null.sql while platform/migrations/474_asset_throughput_incomplete_state.sql is ALREADY on main. This means A4 collides the moment it merges REGARDLESS OF MERGE ORDER relative to any other lane -- worse than a race condition, and invisible to any single-directory check (exactly the defect class this campaign exists to kill). This is the migration guard proving its exact intended value before either PR merged.
RESOLUTION (no new process needed): the EXISTING Ruling 46 rule already handles this correctly -- migration numbers are recomputed fresh via max() across both directories from actual origin/main content at the MOMENT MERGE-LOCK IS TAKEN, not from any lane's authoring-time claim or from other lanes' unmerged branches. Since MERGE-LOCK is strictly serial (one holder at a time), whichever of A4 / B-PB8-BYTEEQ (which VER's sub-agent is separately checking for its OWN migration 475 claim -- also now "contested" in the sense that both lanes currently think they're getting 475) merges FIRST will correctly receive 475 (474 taken, 475 free); whichever merges SECOND will correctly receive 476 (475 now also taken). The process self-resolves AS LONG AS each merge does a genuinely fresh recomputation immediately before taking the lock, not a cached/assumed number. Noted for whoever eventually handles A4's and B-PB8-BYTEEQ's merges: do not trust either lane's self-reported migration number, always recompute at that lane's own merge moment.
Neither A4 nor B-PB8-BYTEEQ nor B-MIGGUARD is merging immediately (A2 is the only lane currently at the merge queue head) -- this is tracked for correct handling when their turns come, not an active blocker right now.
