# SAMAPTI_DVARAPALA_LEDGER

One entry per DVA ruling. Every would-be human gate resolved here with a documented decision.

---

## RULING 1 · A7-N8-AUDIT (out-of-band security — live plaintext production DB password)

QUESTION: A live production DB password sits in plaintext in the repo, public on GitHub for ~2 months. Must it be rotated now, and does this swarm execute the rotation?
OPTIONS: (a) swarm rotates immediately; (b) rotate as the run's last action; (c) swarm prepares everything, names it as the P0 action for the native's hand; (d) log only.
EVIDENCE: hard_gates_check.sh:46 literal password for user=amjis_app/dbname=amjis. Tracked, PRESENT on origin/main, introduced in 39b91dd0 (2026-05-29, native's own commit — not another campaign's work). Same literal found in 9 tracked files total (hard_gates_check.sh, preflight.sh, apply_migration.sh, session_queue.yaml, operator_runs/2026-05-30/.psql_url, 4 migration test files). Repo amonty84/Madhav is PUBLIC. gcloud sql instances list: amjis-postgres RUNNABLE, ipv4Enabled=True, authorizedNetworks EMPTY, requireSsl=False. Brief's authorization_grant does not include credential/IAM administration.
DECISION: (c). Rotation REQUIRED and P0, not optional, not deferrable past run close — but this swarm does NOT execute it. Performs everything it IS authorized for: incident record, consumer inventory, ordered zero-downtime runbook (new 2nd Postgres role → migrate consumers via new Secret Manager version → verify both services healthy → disable leaked role), redaction (Ruling 2), scanner fix (Ruling 3), recurrence guard. Named as the P0 action at the TOP of SAMAPTI_CLOSE_REPORT_v1_0.md §15 opening, never buried. May close COMPLETE with this PARKED-HONEST — BLOCKED-ON-CREDENTIAL-ADMIN-AUTHORITY. Also record as findings: requireSsl=False on a public-IP instance; empty authorizedNetworks is the ONLY thing standing between this leak and direct exploitation.
RATIONALE: Exposure is real/public — rotation is the only remediation. Not actively internet-exploitable today (empty authorizedNetworks gates it via IAM/Auth Proxy) — a defense-in-depth failure, not a 2am emergency. Brief's authorization grant deliberately withholds infra-mutation authority; a swarm cannot self-grant what the grant withheld, and unsupervised prod-auth rotation risks cascading outage with C1-C4 all needing that DB.
REVERSIBILITY: Ruling reversible (promote to executing lane if native authorizes). Rotation reversible at each step via the two-role path. Doing nothing is the only irreversible option (public history cannot be recalled).

## RULING 2 · A7-N8-AUDIT (redaction)

QUESTION: Redact the credential from the 9 files, or leave strictly untouched under §7 non-owned-provenance?
OPTIONS: (a) untouched; (b) delete literal outright; (c) env-var indirection.
EVIDENCE: build_orchestrator/ is not on §7's not-owned list (that list names active campaigns / unknown-provenance working state) — this is committed-on-main, native-authored, retired scaffold. 4 of 9 files are plainly inside platform/** reach.
DECISION: (c), scoped and immediate. New lane B-SECRET-REDACT (Sonnet, medium). Replace literal with env-var indirection (${PGPASSWORD:?} / os.environ pattern already used elsewhere) in all 9 files — never a placeholder that silently breaks the script. Redact + inline incident-reference note in .psql_url. NOT authorized: git history rewrite, force-push, history purge.
RATIONALE: Redaction has near-zero remediation value alone (bytes are public forever) but real recurrence value (removes copy-paste seed). Blanking the literal (option b) would break working tooling — the exact harm §7 exists to prevent. Env indirection survives rotation unchanged.
REVERSIBILITY: Trivial, single-commit revert.

## RULING 3 · A7-N8-AUDIT (secret_scan.sh scope gap)

QUESTION: Does the scanner's scope gap land in B-N8-FIX or its own item?
OPTIONS: (a) fold into B-N8-FIX; (b) new dedicated lane; (c) findings-register entry only.
EVIDENCE: secret_scan.sh is allowlist-scoped to 8 SCAN_TARGETS despite its docstring claiming repo-wide scan with suppression. Gap is wider than reported: 00_ARCHITECTURE/, platform/migrations/, platform/src/, platform-mcp/src/, everything else unscanned. All 9 leaked-credential files sit outside the 8 targets. B-N8-FIX carries rebuild_required:true, sequenced behind C1-REBUILD.
DECISION: (b). New lane B-SECRETSCAN-SCOPE (Opus, high, depends_on: [], no rebuild), merge_priority 1 alongside A2-CI-POINTERS. Scope: invert allowlist-of-8 to repo-wide-minus-SUPPRESS_RE (what the docstring already promises); add fail/ fixtures reproducing the exact leaked shape so --self-test proves new coverage; reconcile docstring to real bounds; wire as blocking CI gate. VER can-fail proof mandatory: plant synthetic credentials under 00_ARCHITECTURE/ and platform/migrations/__tests__/, watch CI go red, revert.
RATIONALE: Purest §N.8 violation in the run — a detector whose docstring overclaims coverage, green because it stopped looking (brief §7.5 names exactly this). Folding into B-N8-FIX would delay the one control that stops the next credential landing unseen, by many hours, for no reason (no rebuild dependency, no file overlap).
REVERSIBILITY: Trivial. Risk is a wider scan surfacing more pre-existing findings — disclosed and itemized as inherited, never silenced by re-narrowing scope; land as warn-only with dated expiry if volume is unmanageable, never quiet re-exclusion.

## RULING 4 · A1-PRESERVE (baseline disposition — 216/43 vs 216/45)

QUESTION: Is the operative baseline 216/45 or 216/43? Which does E1-SAMGATI measure against?
OPTIONS: (a) accept 216/45; (b) hold 216/43, route fix to B-DOCS-GOVERNANCE; (c) fix inside A1 before merge.
EVIDENCE: Both files' frontmatter confirmed missing `artifact:` (schema requires it per artifact_schemas.yaml for both classes). Both are OWNED work already in PR #896. VER independently reproduced 216/43→216/45 with no compensating disappearances. Brief §10.2: tolerated-residual backlog "keeps growing, which is the actual problem."
DECISION: (b), interval named. T0 baseline = 216/43, immutable for the run, measured at origin/main HEAD pre-A1, reproduced independently by both builder and VER. E1-SAMGATI measures against 216/43 and must show 216/43. The two new violations recorded as KNOWN-TRANSIENT, owned by B-DOCS-GOVERNANCE (adds `artifact: PARIPRASHNA_DESIGN_ENGINEERING_PLAN` / `artifact: REPORT_WHOLE_CHART_SYNTHESIS_AND_MCP_DIAGNOSTIC`, also checks PLAN's version/status vocab while there). Failure mode ruled now: if B-DOCS-GOVERNANCE parks/fails, E1 reports 216/45 with the +2 explicitly itemized/attributed as PARKED-HONEST governance items — never quietly absorbed. Rejected (c): A1 is blocking_for_run, already complete, under VER verification; reopening contaminates its "recovered, unmodified" provenance guarantee.
RATIONALE: (a) is the exact failure the brief names — absorbing new violations into the tolerated baseline is how a whitelist erodes to meaninglessness. Separating measurement (immutable T0) from current reading (transient, owned, closure-conditioned) preserves the ability to change course.
REVERSIBILITY: Trivial, two-line metadata additions.

## RULING 5 · A1-PRESERVE (.fuse_hidden0000000500000001 deletion)

QUESTION: Delete the stray dotfile now, or wait?
OPTIONS: (a) delete now; (b) wait until PR #896 merges; (c) other.
EVIDENCE: Bytes preserved at ledgers/RECOVERED_BRIEF_PB-3_PRIOR_DRAFT_fuse_hidden0000000500000001.md (279 lines) in PR #896, currently OPEN (not merged).
DECISION: (b), with defined trigger. Leave in place until PR #896 is MERGED (verified via git cat-file -e origin/main:<path>, not PR state alone). Delete as immediate follow-up once merged; record commit SHA in close report. Recovered file's name/location approved as-is. Do NOT add .fuse_hidden* to .gitignore — ruled explicitly.
RATIONALE: Deletion is the irreversible half; only an unmerged branch currently protects these bytes. The orphan contained a real earlier draft with content not subsumed by the final brief — an ignore rule would have made it invisible and lost. Visibility is the detector; keep the noise.
REVERSIBILITY: Trivial after merge-check; irreversible before it — the whole reason for the wait.

## RULING 6 · A1-PRESERVE (kala_elevation/ preserve-branch waiver)

QUESTION: Does "ALREADY-PRESERVED-ON-MAIN" satisfy §7, or is a token preserve branch required?
OPTIONS: (a) confirm waiver; (b) require no-op preserve branch.
EVIDENCE: Independently re-derived (not accepting builder's md5s) — git ls-tree shows main has 9 files, working tree 8 (main ahead by KALA_W2_FIELD_DESIGN_v1_0.md). All 8 shared files confirmed identical via git hash-object vs git rev-parse (blob-SHA equality, stronger than md5). No kala_elevation/ path in PR #896.
DECISION: (a), confirmed. §7 obligation DISCHARGED — ALREADY-PRESERVED-ON-MAIN. No branch, no PR (empty-diff branch would be a null artifact implying rescue work that never happened). Two riders: (i) the 8 blob SHAs + the KALA_W2_FIELD_DESIGN_v1_0.md asymmetry recorded in the close report (currently exist only in A1's transient FINAL_SUMMARY); (ii) close report records that BRIEF §3.1's premise is FALSIFIED — it called kala_elevation/ "Highest risk — content AHEAD of main," but the working tree is strictly BEHIND main.
RATIONALE: §7 protects against lost/modified work; both hold absolutely — nothing exists locally that main lacks, nothing was touched. A ceremonial branch would substitute ritual for substance and mislead future readers.
REVERSIBILITY: Trivial — a preserve branch can be cut later from bytes already on main.

## RULING 7 · A6-GOCHARA-DIAG (does C2 proceed?)

QUESTION: Does the standing "PARK if stalls for unresolved reason" instruction bar C2?
OPTIONS: (a) accept resolved-mechanism reading, authorize with fallback; (b) require more first; (c) PARK regardless.
EVIDENCE: writer_timeout_seconds=21600 (migration 462, replacing a prior 1800s watchdog that killed 11 prior attempts). Fingerprint/resume mechanism verified sound in source. 303 = 3×101. Builder refuted 3 alternative hypotheses with live evidence.
DECISION: (a), authorized — WITH THE STOP CONDITION REWRITTEN. Trigger is "the same UNRESOLVED reason" — now resolved (a budget mechanism, not a hang/defect). C2 proceeds. REJECTED the builder's proposed AND-conjunction stop condition; replaced with a DISJUNCTION where progress is the primary detector: PARK-HONEST if EITHER (i) any dispatch completes fewer than 40 new substeps, OR (ii) any non-budget failure signature appears (the 2026-07-28 FATAL signature being one instance of (ii), not a precondition). Each dispatch must record its substep delta explicitly; an unrecorded dispatch counts as a trip. No build_substep_progress mutation ever.
RATIONALE: The builder's AND would let a zero-progress dispatch pass unremarked absent one specific log string — measuring the wrong claim, the exact §N.8 failure mode this campaign exists to eliminate. 40-substep floor sits comfortably below the ~82 predicted, won't false-trip. Declined (b) — identifying who issued the 2026-07-28 terminations is not a precondition; condition (ii) already catches recurrence.
REVERSIBILITY: Trivial and continuous — each dispatch independently resumable/snapshot-guarded.

## RULING 8 · A6-GOCHARA-DIAG (BUILD-LOCK duration for C2)

QUESTION: Authorize ~34h monolithic BUILD-LOCK hold, or bound it?
OPTIONS: (a) full ~34h both charts; (b) 1c826d5a only, defer cb73cd3d; (c) other sequencing.
EVIDENCE: Manual §5 does not require holding the lock across multiple sequential dispatches. wall_clock.target_hours=14 for the entire run. CLAUDE.md/L1_GANITA_CLOSURE names 1c826d5a as the operator E2E gate chart; cb73cd3d carries no named gate. Canonical 482012f1 already 303/303.
DECISION: (c) — per-dispatch lock, prioritized by named gate, hard stop rule. C2 acquires/releases BUILD-LOCK per ~6h dispatch, not one ~34h reservation — Conductor re-evaluates the lock queue between dispatches so C1/C3 needs interleave. Order: 1c826d5a to 303/303 first (named gate). cb73cd3d proceeds only in idle windows thereafter. If E1-SAMGATI is reached with cb73cd3d incomplete: terminal PARKED-HONEST-INCOMPLETE with exact substep count + mechanism + remaining dispatch count — a fully honest close, not a failure. C2's wall-clock never weakens any other lane's proof.
RATIONALE: A 34h hold is an over-reservation the resume/fingerprint machinery (just root-caused) doesn't need — each dispatch is an independent interruptible unit. Serializing C3/E1 behind operator-chart parity (second-order value vs. the native's own already-complete chart) is the wrong trade.
REVERSIBILITY: Trivial at every dispatch boundary — progress is durable, a deferred chart resumes later at no cost.

## RULING 9 · A6-GOCHARA-DIAG (fix F5 before or after C2?)

QUESTION: Fix hardcoded _SCORING_SPAN_END_YEAR=2027 / specimen years before C2, or after?
OPTIONS: (a) fix first, no replan needed per builder; (b) log as B-lane item, run C2 as-is.
EVIDENCE: Read source directly. Constants consumed ONLY inside _substep_sort_key ("pure scheduling, zero change to substep semantics/keys/idempotency" per its own docstring) — corroborates builder's claim. DECISIVE FACT: _compute_build_fingerprint does NOT include these constants, yet the file's own documented convention is to bump _RESUME_VERSION on any semantics change, and doing so "forces a full replan-all… deletes ALL of this chart's kala_gochara_windows rows AND build_substep_progress rows."
DECISION: (b), STRONGER than the builder argued. Do NOT touch F5 before C2. Logged as post-C2 B-lane item with two binding constraints: (i) an F5 fix MUST NOT bump _RESUME_VERSION — provably outside the fingerprint, provably dispatch-order-only; (ii) if a future maintainer concludes a bump IS required, that fix does not run until all three charts are at 303/303 (replan-all then costs re-derivation, not destruction of partial progress). Any lane proposing a _RESUME_VERSION bump while a chart holds partial progress STOPS and returns to DVA.
RATIONALE: Builder's rationale ("ordering is moot once complete") is true but misses the hazard: fixing first risks a well-intentioned bump wiping ALL progress on ALL THREE charts — including the canonical chart's completed 303/303 — turning a ~34h job into ~70h and destroying the native's own completed chart. Largest avoidable blast radius seen in this run. Zero benefit to fixing first, catastrophic downside risk.
REVERSIBILITY: Deferring trivially reversible. Fixing first risks an irreversible outcome recoverable only from a snapshot, if one exists.

## RULING 10 · A6-GOCHARA-DIAG (does F3 reopen C3's EP-1 premise?)

QUESTION: May C3 still take the EP-1 NOT-APPLICABLE shortcut given F3 (TS watchdog falsely-lit gap)?
OPTIONS: (a) direct C3 to do real reconciliation; (b) separate lane for watchdog, C3 keeps shortcut; (c) defer pending VER.
EVIDENCE: Confirmed mechanism from source directly (doesn't depend on Cloud Logging). watchdog/route.ts: candidate query selects ANY building-state row idle >15min, no exclusion for multi-substep assets; promotion gated solely on rows-present, zero build_substep_progress consultation anywhere in the path. Route's own comment cites the Python asset_runner.py pattern as its model — implements the rows-present half, omits the completeness half. For a 303-substep writer, rows exist after substep 1 — any >15min heartbeat gap promotes to lit at 78/303.
DECISION: (a) AND (b) together; explicitly NOT (c) — code is dispositive, no need to wait on log forensics. (a) C3 may NOT take EP-1 NOT-APPLICABLE as a present-tense disposition. Precise wording required: SATYA-DĪPA's finding stands as a historically accurate point-in-time census, but its DURABILITY IS VOID since an unguarded promotion path remains live. C3 performs the real reconciliation (build_substep_progress vs. full plan, read-only, every lit/service_ok asset). (b) New lane B-WATCHDOG-LIT (Opus, high, no rebuild), ruled IN SCOPE (§7 owns "orchestrator lit predicate, build-state truth"). MUST MERGE BEFORE C1-REBUILD RUNS. Fix mirrors the Python path: substep plan → require plan completeness before promotion; no plan → retain rows-present behavior; stale-heartbeat + incomplete plan → incomplete/error, never lit. VER can-fail proof required.
RATIONALE: Merge-before-C1 sequencing is DVA's own addition, not the builder's: C1-REBUILD is a long multi-asset BUILD-LOCK rebuild — exactly the workload that produces >15min heartbeat gaps, inviting the watchdog to falsely light half-built assets DURING the run, corrupting the very build-state truth E1-SAMGATI §8.7 must verify. Letting C3 keep the shortcut while the mechanism is live would be the campaign committing its own signature error.
REVERSIBILITY: Trivial — one revertible PR, strictly more conservative than current behavior (promotes fewer assets, never more).

## RULING 11 · MERGE-LOCK precondition (origin/main moved mid-run, PR #897)

QUESTION: Does ṢAḌ-DARŚANA's PR #897 moving main (cdb6fc3b→5f5033a5) change how/when the merge queue starts?
OPTIONS: (a) routine, INT's existing §5 check covers it; (b) additional constraint required.
EVIDENCE: Manual §5's quiescence check has two clauses (no open/auto-merging PR AND no live session editing the tree) — "no open PR alone is insufficient." Brief §13: other campaigns may move main/migrations underneath; always rebase immediately before merge, re-check migration max() across both directories at that moment.
DECISION: Confirmed as routine — WITH ONE CORRECTION. #897 merging is positive evidence a ṢAḌ-DARŚANA session was live within the last tick — the §5 SECOND clause, not the first. An empty gh pr list at a single instant does NOT discharge the check alone. Before the FIRST MERGE-LOCK acquisition: INT confirms both (i) no open/auto-merging campaign PR, AND (ii) origin/main tip UNCHANGED across a re-check after one tick (a stability window, not a point sample). Migration numbers recomputed as max() across both directories read from the NEW tip at the moment MERGE-LOCK is taken. Subsequent merges revert to standard §5 check.
RATIONALE: "No open PR" is a lagging indicator — a session that just merged is likely still working. A one-tick stability window converts a point sample into an actual observation, cheap insurance on the one operation touching another live campaign's shared surface.
REVERSIBILITY: Trivial — delays a merge by at most one tick.

---

## ACTIONS FOR THE CONDUCTOR (from DVA, beyond ledger append)

1. Add four lanes: B-SECRETSCAN-SCOPE (merge_priority 1, with A2), B-SECRET-REDACT, B-SECRET-ROTATE-PREP (runbook + consumer inventory + incident record; NO execution), B-WATCHDOG-LIT (must merge before C1-REBUILD).
2. The security item is P0 and out-of-band — belongs in the close report's opening §15 summary as a named, unclosed action requiring the native's credential-admin authority, never in an appendix.
3. E1-SAMGATI measures drift/schema against 216/43, not 216/45.
4. The credential itself must never be reproduced in any lane's output — reference by file:line and incident id only.

## RULING 12 · A7-N8-AUDIT (17 findings F-19..F-35 outside declared B-lane scope)

QUESTION: Where do F-19..F-35 (TS serving layer + CI/gates/guards) land?
OPTIONS: (a) new B-lane(s); (b) extend an existing lane (e.g. B-NAR-TS); (c) park some/all.
EVIDENCE: F-19 verified AT SOURCE by DVA directly: registry_bridge.ts:701 fully_accounted===slice_size after a pagination loop; the code's OWN comment at :686-687 admits "it is, for a fully-paged flagship slice" — measures "did pagination finish" not "is the slice accounted for." Re-exported at :774, consumed by dossier.ts:515. F-21 verified structurally: density_contract defaults backfilled by descriptor_defaults.ts, census reads the descriptor POST-backfill — grading its own defaults; CLAUDE.md §N.6 part 4 explicitly says the field exists "so a harness can assert... WITHOUT re-deriving it from source" — if the harness grades backfilled defaults, that guarantee is hollow. B-NAR-TS's declared scope (kala_temporal.ts, capabilities.ts, envelope.ts, vidhi_registry_resource.ts, server.ts, register_p1_synthesis.ts, register_p1_ganita.ts; Sonnet/medium; rebuild_required:false) does NOT include registry_bridge.ts or descriptor_defaults.ts.
DECISION: (a) — three new lanes by subsystem; (b) rejected; (c) rejected for F-19/F-21. New lanes: B-N8-F19-COVERAGE (Opus/high, own worktree, no rebuild, merge_priority 2, owns F-19 alone); B-N8-CI-GATES (Opus/high, owns F-21 as FIRST shipped item + the CI/gates/guards subset); B-N8-TS-SERVE (Opus/high, remaining TS serving-layer findings). Within the two multi-finding lanes: every one of the 17 gets a disposition (VERIFIED-FIXED/PARKED-HONEST/REJECTED/NOT-APPLICABLE) with evidence, not every one needs code. F-19's fix must make fully_accounted measure the real claim — early-termination reported distinctly (honest null/incomplete flag), never collapsed into a misleading boolean. VER can-fail mandatory on both: force early pagination termination and confirm fully_accounted no longer reads true; strip a hand-authored density_contract field and confirm the census stops calling it enforced.
RATIONALE: (b) is tempting and wrong on three counts — different verification standard (Opus/earned-signal vs Sonnet/narration "fix at origin"), would silently triple a lane's scope after its model/effort were chosen, applies a weaker bar to a harder class. Parking F-19 unacceptable: live false-green on the flagship domain-assessment path. F-21 worse in kind: a governance harness certifying compliance by inspecting values it inserted itself, corrupting the one machine-readable check CLAUDE.md §N.6 rests on. Three-way split preserves zero-file-overlap.
REVERSIBILITY: Trivial — three independent PRs, none requires a rebuild.

## RULING 13 · A7-N8-AUDIT (F-11/F-25/F-38 atomicity — SHARPENED, larger than the register stated)

QUESTION: May F-11/F-25/F-38 ship partially, or must they close together?
OPTIONS: (a) no partial fix without the other two; (b) independent fixes with documented sequencing.
EVIDENCE: DVA read all three sides directly. ga_nakshatra.py:87 writes "PASS" with zero verification logic. envelope.ts:1608 extractGroundingFromFactRows checks s==='two_pass_verified'||s==='pass' — case-sensitive TWO-VALUE allowlist. THE PICTURE-CHANGING FACT: writers across the sidecar emit TWELVE distinct literals (documented_approximation 24 sites — the MOST common, two_pass_verified 20, pass 11, single_pass 9, single 8, computed_extension 5, floored 4, scope_cap_sentinel 2, classical_match 2, skipped_malformed_source 1, external_computation_required 1, PASS 1). The only CHECK constraints in either migrations directory allow just FOUR values, and 'pass' (which the serve layer DOES count as verified) is NOT among them. register_p1_ganita.ts defines verified_fraction as "share of rows with verification_pass_status=two_pass_verified" — a DIFFERENT definition of verified than envelope.ts uses, served from the same instrument. File-overlap hazard: envelope.ts is already inside B-NAR-TS's declared scope, F-38 sits at the same file ~:1608.
DECISION: (a), STRONGER. New lane B-VERIFSTATUS-VOCAB (Opus/high, rebuild_required:true, feeds C1) owns ALL THREE SITES as one atomic PR, removed from B-NAR-GA's and B-N8-FIX's scope. Binding order: (1) ga_nakshatra.py:87 must NEVER be lowercased — give it a real detector or write NULL, casing normalization on that line is PROHIBITED outright; (2) audit the 11 bo_* lowercase 'pass' sites for earnedness first, null every unearned one (these currently DO count toward grounding_score — unearned ones are a live false-green TODAY); (3) settle the vocabulary once as a single source of truth reconciling the 4-value CHECK against the 12 emitted literals, reconcile envelope.ts's definition of verified against register_p1_ganita.ts's (two definitions of "verified" in one instrument is itself in scope for this lane); (4) the serve-layer predicate change ships LAST and never alone — widening the allowlist before steps 1-2 land would promote 3,955 currently-correctly-excluded rows to falsely grounded; (5) B-NAR-TS merges FIRST (serve-side, no rebuild, fast) — B-VERIFSTATUS-VOCAB rebases onto it before touching envelope.ts. VER must verify the COMBINED effect on the 3,955-row population and reject any intermediate state where that count moves the wrong direction.
RATIONALE: Today's case mismatch is an ACCIDENTAL SAFETY NET — 'PASS' fails the serve-layer test, so 3,955 never-actually-verified rows are correctly excluded by a BUG rather than by design. A well-meaning "normalize the casing" commit would remove this and hand the LLM 3,955 fake groundings. Partition must follow the DEFECT not the subsystem — here correctness demands one owner.
REVERSIBILITY: PR revertible, but the REBUILD it triggers is not free (nulling ga_nakshatra rows changes stored data) — C1's snapshot is the rollback path, must be tested before this lands. Shipping partially is the option with an irreversible-in-practice failure mode.

## RULING 14 · B-N8-SWEEPFIX (may it touch asset_runner.py? — FROZEN contract question)

QUESTION: F-01/F-05 trace to asset_runner.py, undeclared by any lane, behind the FROZEN orchestrator contract with SATYA-DĪPA's exception spent. Authorize, or CONTRACT-CHANGE-REQUIRED+PARKED-HONEST?
OPTIONS: (a) narrow named exception; (b) CONTRACT-CHANGE-REQUIRED+PARKED-HONEST; (c) other.
EVIDENCE: CLAUDE.md §N.2's frozen contract is 5 WRITER-FACING guarantees (not a file-level freeze). asset_runner.py already carries substantial post-freeze internal work (_data_rows_present, _guard_state_write, D-1.6 no-op-completion, all SATYA-DĪPA-era, none altering a writer-facing guarantee). Incidental load-bearing finding: final_state='lit' is reachable ONLY after _drive_substeps returns normally — a raising substep routes to mark_asset_error → state='error'. The Python runner CANNOT mark an asset lit mid-plan — exactly why the operator charts sit honestly at error/78 and error/70 (this SHARPENS Ruling 10: the TS watchdog is not "a" path to a falsely-lit mid-plan asset, it is the ONLY one).
DECISION: (c) — a bright-line contract-invariance test, applied per finding. asset_runner.py edits are IN SCOPE, no exception needed, when ALL FIVE hold: (1) no change to WriterBase interface/signatures/WriterResult/SubStep; (2) no change to what ctx carries or transaction ownership; (3) orchestrator remains sole writer of asset_throughput; (4) ZERO writer files change in the same PR (mechanical test: if fixing the orchestrator requires touching any writer, it IS a contract change); (5) change is additive/narrowing only — detector, null-instead-of-green, log/event, or a guard that can only REDUCE promotions — never a redefinition or widening. Failing any clause → CONTRACT-CHANGE-REQUIRED+PARKED-HONEST, lane returns to DVA naming the specific clause broken. B-N8-SWEEPFIX must state per-finding which clauses it satisfies, no blanket assertion. Any asset_runner.py change merges in the SAME pre-C1 window as B-WATCHDOG-LIT, never concurrent with a running rebuild.
RATIONALE: The freeze protects the CONTRACT not the FILE — §N.2's own trigger is "if a writer seems to need a contract change," an interface condition not a filename. Clause 4 converts an abstract doctrinal question into a mechanical diff inspection. Clause 5 encodes the campaign's whole thesis: narrowing can only reduce false greens, widening is how the corpus got here.
REVERSIBILITY: Any conforming change is one revertible commit, narrowing-only worst case is a manual state nudge. Pre-C1 merge window keeps it reversible in practice.

## RULING 15 · B-DOCS-GOVERNANCE (F-33/F-34 — who amends CLAUDE.md + re-grades PB-3 item 9?)

QUESTION: Who owns correcting CLAUDE.md §N.8's mis-worded instance 3 and re-grading PB-3 §G item 9?
OPTIONS: (a) B-DOCS-GOVERNANCE; (b) audit lane self-edits; (c) DVA-owned doctrine change.
EVIDENCE: Builder correctly declined to self-edit CLAUDE.md. Brief §12.8 assigns standing-doc reconciliation to the governance track. PB-3 §G item 9 already routed to the §N.8 sweep as a no-detector claim.
DECISION: (a), with a boundary + precondition. B-DOCS-GOVERNANCE may correct a mis-worded ILLUSTRATIVE INSTANCE within §N.8 and re-grade a disposition label; may NOT alter the §N.8 principle itself or §N.1-§N.6 doctrine (a doctrine change is DVA's ruling, not a scribe's edit — if the correction requires touching the principle, the lane stops and returns to DVA). Precondition: amendment lands ONLY after VER confirms instance 3 is actually wrong. Mechanics: bump BOTH frontmatter and footer versions, changelog entry naming SAMĀPTI/A7-N8-AUDIT + finding id, register the fingerprint rotation so schema_validator's canonical-artifact check doesn't fire a false new violation against the 216/43 baseline. PB-3 §G item 9 moves to VERIFIED-BY-INSPECTION-ONLY, stays open until its detector exists.
RATIONALE: An audit lane self-editing its own governing document would be both source and arbiter of the correction — the same structural inversion §N.5 warns about, applied to governance. VER precondition matters because CLAUDE.md's authority is what makes an error in it expensive.
REVERSIBILITY: Trivial, documentation-only, single-commit revert.

## CORRECTION TO RULING 10 (unprompted, DVA-initiated)

Ruling 10's reasoning is CONFIRMED and SHARPER: asset_runner.py:562->596 shows the Python runner is structurally substep-safe (cannot reach lit unless the substep loop completed normally). The TypeScript watchdog is therefore not "a" path to a falsely-lit mid-plan asset — it is THE ONLY ONE, bypassing an invariant the Python side enforces by construction. B-WATCHDOG-LIT merging before C1-REBUILD is CONFIRMED REQUIRED, not merely prudent.

## ACTIONS FOR THE CONDUCTOR (batch 2)

1. Add four lanes: B-N8-F19-COVERAGE (merge_priority 2), B-N8-CI-GATES (F-21 first), B-N8-TS-SERVE, B-VERIFSTATUS-VOCAB (rebuild_required:true, feeds C1).
2. Remove ga_nakshatra.py:87 from B-NAR-GA's scope and the bo_* verification_pass_status sites from B-N8-FIX's scope — both reassigned to B-VERIFSTATUS-VOCAB (apply this when B-NAR-GA / B-N8-FIX are dispatched later).
3. Sequence B-NAR-TS ahead of B-VERIFSTATUS-VOCAB on envelope.ts (merge ordering, not dispatch-blocking).
4. Hold B-WATCHDOG-LIT and any asset_runner.py change (B-N8-SWEEPFIX) in the same pre-C1-REBUILD merge window.

## RULING 16 · A3-SHADBALA-EVIDENCE / E1-SAMGATI §8.7 (row-count baseline drift)

QUESTION: Re-baseline CLAUDE.md/L1_GANITA_CLOSURE's documented counts to live values, or keep as snapshot + have E1 explain deltas?
OPTIONS: (a) re-baseline to current live; (b) keep as snapshot, E1 explains deltas; (c) other.
EVIDENCE: DVA re-measured production directly, confirming VER exactly (chart_facts total 138,414; chart_dashas 484,387; chart_divisionals 23,542). DECISIVE FINDING: L1_GANITA_CLOSURE_v2_0.md (CURRENT) supersedes v1.0, describes it as "premature seal; floors stale, enrichment not folded" and ALREADY SAYS of the disputed figure: "chart_facts: 27,554 (per v1.0 §2.3; now higher post-enrichment — not yet re-measured)." CLAUDE.md cites the SUPERSEDED v1.0 in §C/§D while §E names v2.0 as the seal — a registry disagreeing with itself, the GA.1 failure mode B.8 forbids. CLAUDE.md's own arithmetic doesn't close either: 27,554+536,471+21,635=585,660 vs its stated total 585,710 — off by exactly 50 (ga_positions' floor), a hand-assembled double-count.
DECISION: (c) — split by defect class, explicitly NOT (a). (1) Documentation defect → B-DOCS-GOVERNANCE (per Ruling 15): re-point CLAUDE.md §C/§D at L1_GANITA_CLOSURE_v2_0.md and replace hardcoded counts with a POINTER, not fresher hardcoded numbers (hardcoding is the mechanism that produced this — re-hardcoding only resets the clock). (2) E1-SAMGATI §8.7 RESPECIFIED: "row-count invariants hold" means counts measured live at close, recorded in the close report, with every delta against the prior RECORDED measurement explained by mechanism — re-baselining is never itself an explanation. (3) chart_dashas delta NOT auto-blessed: chart_facts/chart_divisionals moved UP (consistent with post-enrichment story), but chart_dashas moved DOWN 52,084 (9.7%) against a v2.0-marked "prod-confirmed" figure — enrichment does not explain a decrease, and §N.3 rebuilds REPLACE not accrete, so a drop means fewer rows now generate (fix or regression) or a partial rebuild. Assigned as a NAMED INVESTIGATION ITEM (check whether ga_dashas' 4-level Sukshma tree is truncated for part of the span) — must be explained with mechanism OR recorded PARKED-HONEST with delta+hypothesis before E1 closes; may NOT be absorbed into a re-baseline. No natural lane claims it — attach to C3-BUILDSTATE-RECON (already inspects rows_written vs expected_rows per asset).
RATIONALE: (a) is the same trap as Ruling 4 — adopting current reality as the new baseline makes the number agree with the world by moving the number, silently blessing an unexplained 52,084-row decrease. (b) mis-locates the fault — two of three deltas aren't findings at all, they're an artifact of citing a self-declared-superseded source; treating them as anomalies wastes terminal-track attention. Pointer-not-hardcode is the durable fix, matching §D's existing discipline for canonical paths.
REVERSIBILITY: Trivial for docs (single-commit revert, live counts re-derivable anytime). Part 3 is read-only. Irreversible-in-practice option is (a) — overwriting stale figures destroys the evidence a 52,084-row drop ever occurred.

## RULING 17 · A2-CI-POINTERS / E1-SAMGATI §8.6 (tool-count three-way mismatch: 167 registered / 124 served / 152 asserted)

QUESTION: Who owns reconciling the gap, and does it change A2's re-dispatch scope?
OPTIONS: (a) fold into A2's reopened scope; (b) new dedicated lane; (c) E1-time investigation only.
EVIDENCE: DVA attempted the live probe itself, got "Unauthorized — Invalid or missing Bearer API key" (needs the mcp-canary-key credential path). Brief §0.2 recorded "152 tools serving incl. 8 new kala_*" as of 2026-07-29 18:40-19:44Z, right after INF-2's successful deploys. VER now measures 124. A2's refutation already traced get_signals as one instance of up to 43 registered-but-unserved names.
DECISION: (a) for characterization, (b) for repair — split on that seam; (c) rejected outright. A2's reopen EXTENDED to characterize the full gap, READ-ONLY: exact live count + probe timestamp + revision id; exact source-registered count on origin/main; per-tool classification of every registered-but-unserved name (deliberately gated / conditionally registered / genuinely dropped); and answer whether 152->124 is a REGRESSION or 152 was never a like-for-like measurement. Cheap, builder already inside the resolver, adds no merge weight — A2 KEEPS merge_priority 1, not delayed. Any REPAIR (restoring tools to the catalog) goes to a NEW lane B-MCP-CATALOG-GAP (Opus/high, gated on A2's characterization) so A2 can merge first. STANDING ESCALATION: if characterization shows tools served on 2026-07-29 that are NOT served now, that is a LIVE PRODUCTION REGRESSION, not a queued finding — comes to DVA immediately, takes priority over the repair lane's ordinary sequencing. E1-SAMGATI §8.6 RESPECIFIED: "reconciles against the registry" cannot mean counts are equal (some may be legitimately unserved) — means every registered-but-unserved tool is individually accounted for with a stated reason; a raw count match would not by itself pass it.
RATIONALE: (c) unacceptable on timing — if tools silently left the catalog inside this run's own window, discovering that at E1 means the instrument served a degraded surface the whole run while every lane assumed a healthy baseline, and E1 is the wrong place to first learn an incident (E1 confirms convergence, doesn't discover incidents). (a)/(b) split protects the critical path — A2 has merge_priority 1 for a structural reason (every later PR's signal depends on it); widening it with a 43-tool repair would trade the run's most load-bearing early merge for unurgent work. Respecifying 8.6 matters independently — count-equality would be satisfiable by registering FEWER tools, the same false-green species this campaign exists to eliminate.
REVERSIBILITY: Fully reversible — characterization mutates nothing; repair lane is ordinary revertible code; the scope split can collapse back into one lane if the gap proves trivial.

## NOTES FOR THE CONDUCTOR (batch 3)

1. Capability gap: live MCP tools/list probe needs a Bearer key (mcp-canary-key); the claude.ai MARSYS-JIS MCP connector in this session is unauthenticated and cannot be authorized non-interactively. Whichever lane owns the catalog probe must obtain the key path explicitly — a lane that can't reach the catalog must report UNPERFORMED, never a passing or empty result (the §4.5 dead-proxy trap in new clothes).
2. Add lane B-MCP-CATALOG-GAP (Opus/high, gated on A2's characterization). Extend A2's reopen brief with the characterization scope above (already sent to the builder separately). The ga_dashas -52,084 investigation attaches to C3-BUILDSTATE-RECON.

## RULING 18 · SECURITY (amendment to Ruling 1 — SEVERITY UPGRADE)

QUESTION: Does new evidence change Ruling 1's severity grading and runbook?
EVIDENCE: DVA queried pg_roles directly on live production: amjis_app is memberof {cloudsqlsuperuser} with rolcreatedb=true AND rolcreaterole=true (in Cloud SQL, cloudsqlsuperuser is the effective-admin role; CREATEROLE means the holder can create/alter roles -- escalate and persist). postgres role carries the identical profile. amjis-db-password version 3 enabled 2026-06-02; version 2 enabled 2026-05-28, STILL ENABLED TODAY; INC-1 literal committed 2026-05-29 -- one day after v2 was created (high-probability match, not yet proven). deploy.yml:304 pins DB_PASSWORD=amjis-db-password:3, one of 13 version-pinned secrets in that file.
DECISION: (b) amend, do not reverse. (1) SEVERITY UPGRADED: the leaked literal is an EFFECTIVE DATABASE-ADMINISTRATOR credential capable of self-escalation and persistence, not app-scoped as Ruling 1 said. Every downstream artifact must carry the corrected grading. (2) Runbook gains 3 mandatory steps: (i) confirm (not assume) whether the leaked literal matches an enabled Secret Manager version (v2 is the high-probability match); (ii) rotation must create version 4, bump deploy.yml:304 to :4, THEN disable versions 2 and 3 (disable-without-bump breaks next deploy; bump-without-disable leaves the leak live); (iii) audit all 13 version pins in that file for the same trap. (3) (c) explicitly REJECTED -- authorizedNetworks still empty (second control holds), authorization grant still excludes credential admin, cascade risk unchanged. Severity raises PRIORITY, does not manufacture authorization.
RATIONALE: The difference between "rotate a leaked app password" and "a publicly-published string may authenticate as an effective DB admin with role-creation ability" belongs at the top of the close report in those words. Confirming rather than assuming the version-2 match matters because strong circumstantial timing is not evidence -- this run does not close on inference.
REVERSIBILITY: Documentation amendment. Runbook prescribes pin-bump-then-disable specifically because that ordering is reversible at each stage; disable-first is not.

## RULING 19 · B-SECRET-REDACT (scope 9 -> 27 files)

DECISION: (a) widen to 27, same constraints (env indirection, never function-breaking, historical artifacts get inline note, no history rewrite) PLUS: the lane's deliverable is "zero literal credentials remain, PROVEN BY THE REPAIRED SCANNER running clean over the whole tree" -- not "27 files edited." The count is an input; the scan is the acceptance test. If the repaired scanner finds a 28th, the lane fixes it.
RATIONALE: The "nine files" in Ruling 2 was one git-grep's output for one literal, never a scope boundary -- treating a number from an already-ruled-defective detector as authoritative would be the exact error this campaign exists to eliminate.
REVERSIBILITY: Trivial, one revertible PR.

## RULING 20 · pattern-shape scanner gap

DECISION: (a) B-SECRETSCAN-SCOPE owns BOTH axes -- retitled "scope AND pattern repair." Ruling 3 addressed WHERE the scanner looks; this is WHAT it recognizes -- one defect (a detector that doesn't measure the claim it asserts), one acceptance test. The psycopg2.connect(password='literal') shape that actually defeated it becomes a fail/ fixture alongside path-coverage fixtures. Acceptance = single composite proof: repaired scanner catches all 27 real instances (Ruling 19) plus every synthetic fixture; VER's can-fail proof plants a credential in a path the OLD scanner covered but whose SHAPE it missed, demonstrating both axes independently.
RATIONALE: Scope and pattern are the two multiplicands of scanner coverage -- a green from either alone is meaningless.

## RULING 21 · The two production-proxying CI workflows

DECISION: (b) findings-register + costed spec for the ARCHITECTURAL question (should CI reach prod at all vs a disposable replica) -- outside this run's authorization, same reasoning as Ruling 1. BUT three things are in scope NOW, via B-SECRET-ROTATE-PREP: (i) both workflows become NAMED CONSUMERS in the rotation runbook; (ii) record which role each authenticates as -- if amjis_app or postgres, that's CI running as an effective DB admin, belongs in the incident record at Ruling 18's corrected severity; (iii) confirm neither carries a literal credential (folds into Ruling 19's scan). NOTE: fresh_chart_smoke.yml DOES exist on origin/main -- DVA's earlier "absent" read was branch lag (shared checkout 39 behind on parishodhana/dark-corpus-remeasure). No finding there beyond the consumer-inventory items above.
RATIONALE: Redesigning CI's data access under an autonomous run with no human present is the class of infra mutation the authorization grant withholds. But a rotation runbook that omits two workflows holding live prod connections will fail -- consumer enumeration is not optional and is cheap.

## RULING 22 · Compromise assessment

DECISION: (b) fold into B-SECRET-ROTATE-PREP, bounded, best-effort read-only: query pg_stat_activity + available connection logs for unattributable connections; enumerate roles/recent role changes (CREATEROLE makes role-creation the highest-signal persistence indicator); check authorizedNetworks change history. MUST report the honest epistemic limit: log retention ~30 days against a ~60-day exposure, and this project has no queryable long-horizon event history (same B-EVENTREG gap that already blocked SATYA-DĪPA's forensic method) -- a clean result means "no evidence of misuse in the window we CAN see," written in exactly those words. A clean result may NEVER be reported as "not compromised."
RATIONALE: A dedicated lane implies an investigation this project's telemetry cannot support and would produce a confident-looking verdict resting on an inadequate window -- exactly the false-green class §N.8 exists to eliminate.

## RULING 23 · B-WATCHDOG-LIT (plan.ts scope ratification)

EVIDENCE: DVA verified at source: plan.ts:324's build-candidate filter excludes 'incomplete' (not in the AssetState union), so an incomplete asset is unbuildable AND not in READY_STATES -- stranded exactly as the builder described. Live state confirms: 229 lit/3 error/1 dormant, ZERO incomplete rows today (provable no-op). 26 of 28 has_substeps assets never write a resumption ledger -- the watchdog fix withholds rescue from all 26, making this strand imminent.
DECISION: (a) ratified as in-scope, WITH A HARD CONSTRAINT: the edit may add 'incomplete' to AssetState and the build-candidate filter at :324 ONLY. It must NOT be added to READY_STATES (:148) -- that set governs downstream dependency gating; admitting incomplete there would let a half-built asset satisfy its dependents, recreating falsely-lit semantics at the DAG layer. VER must verify READY_STATES is unchanged in the diff.
RATIONALE: A narrowing change bundled with the recovery path narrowing requires -- one fix viewed from two files, already blessed by Ruling 14 clause 5.

## RULING 24 · B-COCKPIT-INCOMPLETE (cross-lane seam) — AND A RE-GRADE

EVIDENCE: DVA read deriveState.ts:20-54 directly: the 'partial' badge is reachable ONLY via `if (error) { if (has_substeps && substepsCommitted>0) return 'partial' }`; with error null, falls through to `if (actualRows>0) return 'lit'`. Combined with the lane's own residual 3 (the Python path NULLS last_error on 'incomplete'): a Python-produced incomplete asset with rows present renders as LIT in the cockpit TODAY, ALREADY, independent of anything B-COCKPIT-INCOMPLETE does.
DECISION: (b) — adopt the acceptance criterion AND two upgrades. (1) Acceptance criterion as a can-fail proof: VER constructs an incomplete asset with actualRows>0, confirms badge is never 'lit' under BOTH last_error-set and last_error-null. (2) RE-GRADE: the queue calls this lane "cosmetic, non-blocking" predicting an unstyled default branch -- WRONG, it renders as lit. This is a falsely-lit UI surface, the campaign's core defect class. Lane moves Sonnet/medium/cosmetic -> OPUS/HIGH/CORRECTNESS, and becomes NO LONGER OPTIONAL for E1-SAMGATI §8.7 (asserts no asset lit with incomplete plan -- currently false IN THE COCKPIT regardless of backend). (3) Fix the fragility not just the symptom: deriveState gains a first-class throughputState==='incomplete' branch positioned BEFORE the actualRows>0 fallthrough, deriving from substep completeness rather than a diagnostic string's presence -- last_error mitigation becomes belt-and-braces, not the sole mechanism.
RATIONALE: Encoding a semantic state in a diagnostic string and deriving UI correctness from its presence is fragile by construction -- broken by the first well-intentioned "clear the message" commit, which the Python path has in effect already made. Same shape as Ruling 13's directional-safety refusal to trust cross-lane sequencing; same answer: fix the mechanism so no future lane CAN undo it.
REVERSIBILITY: Trivial, UI-layer, no data mutation, additive/narrowing.

## ACTIONS FOR THE CONDUCTOR (batch 4)

1. Ruling 18's corrected severity (effective DB administrator, CREATEROLE, self-escalation capable) replaces prior wording everywhere it propagated -- close report, incident record.
2. Rotation runbook additions per Ruling 18/21/22 -- route back to B-SECRET-ROTATE-PREP (already completed once, needs reopening).
3. B-SECRET-REDACT (already completed once, PR #905, 9 files) needs reopening -- widen to 27 files, acceptance bound to repaired scanner.
4. B-SECRETSCAN-SCOPE (still in progress) needs its scope message updated -- retitle scope+pattern, add the psycopg2 literal fixture shape, composite acceptance proof.
5. B-COCKPIT-INCOMPLETE (not yet dispatched) needs a substantially upgraded dispatch: Opus/high, deriveState structural fix, can-fail criterion, no longer optional for E1 §8.7.
6. B-WATCHDOG-LIT's plan.ts edit ratified -- VER must confirm READY_STATES unchanged in the diff (add to VER's checklist if not already there).
7. METHOD NOTE for all lanes: DVA's own fresh_chart_smoke.yml false-negative came from probing a shared checkout 39 commits behind on parishodhana/dark-corpus-remeasure. Every "X does not exist" claim must be checked against origin/main, not the shared working tree.

## RULING 25 · mcp_server_info.tool_count (28-tool false-green, "INF-2 wearing new clothes")

QUESTION: New lane, fold into B-MCP-CATALOG-GAP, fold into B-N8-CI-GATES, or defer?
EVIDENCE: Located source: mcp_catalog_version.ts:39 -- MCP_CATALOG_TOOL_COUNT = MCP_SURFACE_PROFILES.full.total, imported from a GENERATED module derived from a DIFFERENT population than the served catalog hash. server.ts:388-411 comments acknowledge the generated manifest is built from a different catalog than what is served. No other lane touches mcp_catalog_version.ts.
DECISION: (b) B-MCP-CATALOG-GAP, ordered FIRST within it. (1) Ships before any tool restoration -- detector before subject, per §N.8 (restoring tools while the health signal stays decoupled means the restoration can't be measured by anything trustworthy). (2) Fix shape: RENAME AND RE-SOURCE, not one or the other -- tool_count becomes the REAL served count (every existing consumer already reads it that way); the registry-size metric is renamed to something self-describing (e.g. retrieval_registry_profile_total) and carries its generation date, not deleted. (3) Can-fail mandatory: VER gates a tool OUT of the served catalog, confirms tool_count MOVES -- a number that can't move when the surface moves is REFUTED. (4) E1-SAMGATI §8.6 gains a new checkable clause (extending Ruling 17): mcp_server_info.tool_count must EQUAL the live tools/list count at close; any divergence is itself a finding.
RATIONALE: INF-2 failure mode wearing new clothes -- a health signal decoupled from the health it purports to report, worse than staleness since 152 was NEVER a served count (only 15/124 overlap), so it could hold steady while the serving surface collapsed to near-zero.
REVERSIBILITY: Trivial, one revertible PR, no data mutation, no rebuild.

## MERGE ORDERING FOR SHARED TS SURFACES (consolidated, supersedes prior scattered notes)

Order | Lane | Shared files | Constraint
1 | A2-CI-POINTERS | registry_bridge.ts, register_p1_ganita.ts, phala_outlook.ts, phala_event_anchors.ts, register_d9_judgment.ts, register_d10_pact.ts, get_dashas.ts | merge_priority 1 unchanged -- everything touching its files rebases onto it
2 | B-N8-F19-COVERAGE | registry_bridge.ts:701/774 | newly surfaced overlap with A2 -- rebase onto A2 (Ruling 12 priority 2 already sequences correctly)
3 | B-NAR-TS | register_p1_ganita.ts:374, server.ts:687, envelope.ts:1416 | rebase onto A2
4 | B-MCP-CATALOG-GAP | mcp_catalog_version.ts (clean), possibly server.ts | if it touches server.ts, rebase onto B-NAR-TS
5 | B-VERIFSTATUS-VOCAB | envelope.ts (grounding fn) | rebase onto B-NAR-TS per Ruling 13

GENERAL RULE: A2 merges first; every lane sharing a file with it rebases onto it. Line-level disjointness reduces conflict probability but does NOT eliminate it -- same-file merges always need the rebase.

STANDING FLAG (3rd appearance this run, the §4.5 dead-proxy trap): B-MCP-CATALOG-GAP cannot verify itself without the Bearer key (mcp-canary-key route). A lane that can't reach the live catalog reports UNPERFORMED, never a pass, never an empty result.

## RULING 26 · B-N8-TS-SERVE (F-20 merge ordering vs SHAD-DARSANA — resolves a real §7 textual ambiguity)

QUESTION: Does F-20 touching kala_envelope.ts/argument_composer.ts (SHAD-DARSANA W0.3-authored, already merged via PR #897) need coordination beyond normal rebase discipline?
EVIDENCE: manual §7's not-owned prose reads "the kala_* W0/W1/W2 work" which, read literally, could sweep in any kala-prefixed file. But the queue's operative not_owned_preserve_only globs are precise (kala_elevation/**, **/SHAD_DARSHANA_*, PARISHODHANA_*, .mcp.json, **/CONDUCTOR_HALT_LOG.md, refs parishodhana/*/shad-darshana/*) -- kala_envelope.ts and argument_composer.ts match NONE of them. Self-refuting literal reading: B-NAR-KA is a declared OWNED lane scoped to Kala-layer files (ka_bhavishya_lekha.py etc) -- if §7 conferred a permanent kala-namespace claim the queue would be internally contradictory.
DECISION: (a) confirmed, ambiguity resolved on the record + 2 riders. §7's phrase means SHAD-DARSANA's WORKSTREAM ARTIFACTS AND IN-FLIGHT STATE, not a permanent namespace claim over every kala-prefixed filename. The queue's globs are operative and govern. Riders: (1) a clean textual rebase is NOT sufficient verification -- if a SHAD-DARSANA PR touching either file lands before this lane merges, the risk is SEMANTIC not textual (two arcs adjusting the same logic differently can rebase cleanly and still break each other's intent) -- on any such rebase the lane RE-RUNS its can-fail proof, does not assume the fix survived. (2) Report it: close report records that this run modified two SHAD-DARSANA-authored files, with finding id + rationale -- preserve half doesn't apply (it's committed code), report half costs nothing.
RATIONALE: §7 is a rule about work products and in-flight state, not filename prefixes -- committed code on main is shared substrate every lane builds on; a permanent per-arc claim over merged files would make the codebase unmaintainable and contradict the queue's own owned-lane assignments.
REVERSIBILITY: Trivial, ordinary revertible PR on merged code, no cross-arc state touched.

## RULING 27 · B-N8-TS-SERVE (query_plan_log.parsing_success semantics — confirmed, plus an unasked-for finding)

QUESTION: Is "parsed on FIRST attempt" the correct meaning vs the prior "a plan exists at all"?
EVIDENCE: parsing_success is a PERSISTED column (migrations, NOT NULL DEFAULT TRUE), not computed. consult/route.ts:711 wrote the hardcoded literal true -- the original false-green in its purest form.
DECISION: (a) confirmed as correct product semantic. Rename declined (would require a migration on a NOT NULL DEFAULT TRUE column with historical rows and would DISGUISE rather than fix the real problem). THE FINDING NOBODY ASKED ABOUT, more consequential than naming: every pre-fix row reads true because it was hardcoded -- after this fix, false becomes meaningful, so the COLUMN'S MEANING CHANGES MID-HISTORY WITH NO MARKER. Anyone querying across that boundary (a future calibration/L5 pass) silently mixes a tautology with a measurement. Required: (1) record the semantic cutover (date + merge SHA) in the field's definition comment and the close report; (2) document at the definition site + make the UI legible ("clean first parse" not bare "parsed"); (3) residual recorded not chased: NOT NULL DEFAULT TRUE is itself a false-green generator (any insert omitting the field silently yields true) -- DEFAULT NULL would be §N.8-correct but disproportionate to fix now given it's a migration on a live table.
RATIONALE: A persisted boolean meaning one thing before a commit and another after, with no marker, is a data-integrity trap for every future consumer -- this project's calibration ambition depends on historical rows meaning what they claim.
REVERSIBILITY: Trivial -- code fix is one revertible PR; no schema change mandated.

## DVA QUEUE CLEARED (all 27 rulings delivered as of this point)

Reminders for the Conductor, restated by DVA:
- Ruling 18's security severity upgrade (effective DB administrator, CREATEROLE) needs to propagate into the incident record and close report opening summary; rotation runbook additions are the load-bearing part.
- Ruling 24's B-COCKPIT-INCOMPLETE re-grade (Opus/high/correctness, no longer optional for E1 §8.7) must be used when that lane is finally dispatched.
- Ruling 16's chart_dashas -52,084 investigation still needs an owner if C3-BUILDSTATE-RECON doesn't absorb it.
- Ruling 25's merge-ordering table now governs 5 lanes on shared TS surfaces; A2 merges first, everything sharing its files rebases onto it.

## RULING 28 · A2-CI-POINTERS (live-catalog-parity check never runs live in CI — premise inverted)

QUESTION: Small lane to wire env vars into tap-ci.yml, or findings-register+defer?
EVIDENCE: DVA read all three workflows on origin/main. THE WIRING ALREADY EXISTS -- tap-ci.yml:80-81 already reads MCP_SERVER_URL/MCP_SMOKE_BEARER_TOKEN as secrets. No missing reference, no two-line fix available. The comment immediately above (:74-75) states the REAL blocker in a prior author's own words: "Wire ... as repo secrets once a STAGING platform-mcp..." -- explicitly noting this "cannot satisfy from a hosted runner alone." Secret NAMES differ from the sibling workflows (TAP_* vs MARSYS_*) -- not a copy-paste situation. Repo is PUBLIC (Ruling 1) -- provisioning a bearer token to a PR-triggered workflow is a credential-exposure question in its own right.
DECISION: Neither (a) nor (b) as posed -- premise wrong. Correct disposition: (b) REDIRECTED into Ruling 21's existing spec, not a new item. (1) Provisioning repo secrets is credential administration, same class as Ruling 1's rotation, excluded by the same reasoning -- no lane does it. (2) The unmade decision is staging-vs-production CI -- pointing at production is a materially different risk profile nobody has decided. (3) Same question as Ruling 21 -- joins that spec, decided ONCE for all 3 workflows (tap-ci, elev-serving-gates, shad-darshana-ci-skeletons), not three times with three different secret-name conventions. Spec must carry: tap-ci.yml's trigger set (if pull_request in a public repo, a live token is exfiltratable by a malicious PR -- staging-or-nothing); the TAP_*/MARSYS_* name divergence; the fact 2 sibling workflows already have live-mode capability (precedent to examine, not silently extend). RECORDED HONESTLY: the check IS a genuine detector reporting SKIPPED (not an §N.8 violation, honestly labelled) but is NOT coverage -- close report must state plainly that after this run, the only automated guard on E1-SAMGATI §8.6 is the static model, live half remains unprovisioned.
RATIONALE: The Conductor's lean toward a quick fix was reasonable on the summary alone -- the correction is entirely due to reading the actual file. Pattern worth naming, recurred several times this run: a "trivial mechanical fix" turns out on reading the file to be a decision someone deliberately deferred and documented. Overriding a written prior judgment with a 2-line patch would substitute swarm convenience for a decision already made. Consolidating into one spec preserves optionality -- 3 separate ad hoc provisioning decisions would entrench production-credentials-in-public-CI by accretion, precisely how the Ruling 1 credential came to exist.
REVERSIBILITY: Nothing mutated -- findings-register+spec only. The deferred decision is reversible; provisioning a production bearer token into a public repo's CI is the hard-to-walk-back branch (a token exposed to a fork PR must be treated as compromised).

CONFIRMED (not a new ruling): the "152 tools" figure is settled -- 124 is authoritative (VER's two-directional confirmation), consistent with Ruling 25. Close report/E1-SAMGATI §8.6 should cite 124 and explain WHY 152 appeared authoritative (a decoupled field named tool_count) as part of the finding, not a footnote. Brief §0.2's "152 tools serving" gets corrected in the same B-DOCS-GOVERNANCE pass as Ruling 16's stale figures, same discipline: cite the measurement and its date.

DVA queue cleared again -- 28 rulings delivered, nothing pending as of this point.

## RULING 29 · B-SECRET-REDACT (test_ka_jivana_parva_circularity_guard.py — SHAD-DARSANA-thematic file)

QUESTION: Does touching this file for security-only redaction need anything beyond the minimal fix + close-report notation?
EVIDENCE: File present on origin/main, matches none of the queue's not_owned_preserve_only globs. Credential sits in LIVE_DSN, consumed inside a block with an existing clean-degradation idiom (pytest.skip if psycopg not installed). SAFETY PROOF: shad-darshana-circularity-guard.yml runs this file with -m "not integration", and the live test that reads the DSN is marked @pytest.mark.integration -- DESELECTED in both CI surfaces (ci.yml's only pytest invocation also runs -m "not integration"). The substitution cannot break SHAD-DARSANA's gate because that gate never executes the line. That workflow's paths: trigger explicitly lists this file, so it WILL fire on this PR.
DECISION: (a) confirmed, builder handled correctly, WITH 2 RIDERS. Ownership settled by precedent (Ruling 26: §7 governs work products/in-flight state, not thematic territory; Ruling 2/19 already authorized exactly this class of touch in platform/migrations/__tests__/). RIDER 1: verify the degradation shape -- when the env var is ABSENT, must resolve to a clean pytest.skip, NOT an exception or a connection attempt against an empty DSN (the file already has this idiom 2 lines up -- redaction should mirror it). This matters because the integration path is CI-deselected -- only SHAD-DARSANA engineers running it LOCALLY would ever hit a bad shape, and they must get an honest skip. VER should confirm this specific shape. RIDER 2: their gate running on our PR is FREE authoritative verification -- if it goes red, the lane STOPS and reports to DVA, does NOT override/re-run-to-green/merge past it. A red gate belonging to another campaign is a hard stop under §7's spirit even when the file itself is fair game.
RATIONALE: The ownership question was the easy half, already answered by precedent. The half worth a probe was whether a security edit could silently break another live campaign's CI gate -- that would be real §7 harm regardless of filename ownership, surfacing days later as someone else's mysterious red build.
REVERSIBILITY: Trivial, one-line function-preserving substitution, revertible, with the other arc's own CI gate running on the PR as an independent check.

DVA queue cleared again -- 29 rulings delivered, nothing pending as of this point.

## RULING 30 · B-SECRETSCAN-SCOPE / B-SECRET-REDACT (scope reconciliation — SETTLED, stale-tree trap #3)

DECISION: Settled by DVA running the diff itself. B-SECRET-REDACT is CORRECT at 9/16/2=27. B-SECRETSCAN-SCOPE's CRED-B count of 15 is WRONG BY ONE -- DVA's own working tree (parishodhana/dark-corpus-remeasure, 39 commits behind) gave 9/15/2=26 SUM=UNION=26 (matching the scanner's undercounted claim!); against origin/main (authoritative): 9/16/2=27, SUM=UNION=27. The single differing file: test_ka_jivana_parva_circularity_guard.py (the exact Ruling 29 file, added to main within the 39-commit gap). Neither lane missed a file in REMEDIATION; one lane under-reported in CENSUS because its scan tree was stale.
ACTIONS: (1) Add to B-SECRETSCAN-SCOPE: the scanner must RECORD THE COMMIT SHA IT SCANNED in its output, and its CI invocation must run against merge-base/HEAD, not whatever a runner happens to have -- "a census that cannot say what it measured is not a census." (2) Every count this run has produced from a working-tree scan is SUSPECT and must be re-derived against origin/main before entering the close report, including DVA's own Ruling 19 framing. (3) THIS IS THE THIRD APPEARANCE of the stale-tree trap this run (after fresh_chart_smoke.yml and this) -- promoted from a repeated lesson to a STANDING RULE: any "X does not exist" or count-based claim must be checked against origin/main, never a possibly-stale working tree.
RATIONALE: Convergence on a total (27) with divergence in a component (15 vs 16) was the tell that one side's population differed, not that someone miscounted. The scanner's answer was wrong because its INPUT was stale -- a false-green appearing inside the campaign's own instrumentation.
REVERSIBILITY: Nothing mutated, measurement only.

## RULING 31 · B-SECRET-ROTATE-PREP (three credential values — mechanism confirmed, premise challenged)

DECISION: (a) for mechanism (one role-rotation invalidates all leaked literals for that role at once, runbook structure doesn't change) BUT REJECT the unverified assumption that all three values authenticate the SAME role until checked -- Ruling 18 already found one credential mislabelled with the wrong username while actually authenticating as superuser. REQUIRED: (1) confirm the role per credential value INDIVIDUALLY -- if any authenticates as postgres rather than amjis_app, that's a SECOND rotation target (both are cloudsqlsuperuser/CREATEROLE per Ruling 18); (2) verification step MANDATORY and EXPLICIT: after rotation, confirm ALL THREE values fail authentication individually by attempted connection -- not inferred from "we rotated the role," DEMONSTRATED; (3) all Ruling 18 additions stand (confirm-then-disable versions, bump deploy.yml:304, audit all 13 pins).
RATIONALE: A runbook executed once under pressure by someone who didn't write it -- every unverified inference is a place the rotation silently completes while a live admin credential stays in public history. Verification costs 3 connection attempts, converts "we believe this is safe" into "we watched all three stop working."
REVERSIBILITY: Runbook preparation only, nothing executes under this ruling.

## RULING 32 · Firebase Web API key (permanent-accept, narrowly scoped)

DECISION: (a) permanent-accept -- Firebase WEB keys are designed to be public (ship in client bundles, identify a project not a principal, security enforced by Firebase Security Rules/App Check not key secrecy; NEXT_PUBLIC_ prefix confirms it's already compiled into browser code). CONSTRAINT (the substance of the ruling): do NOT add a blanket AIza* tolerance -- that prefix also covers Google Cloud API keys (Maps, Places, etc.) which ARE billable/sensitive. Tolerance must key on the WEB-CONFIG CONTEXT (NEXT_PUBLIC_FIREBASE_* / the Firebase web config object shape), docstring must state non-Firebase-web AIza keys remain in scope, add a fail/ fixture with a non-Firebase AIza key to prove the tolerance doesn't over-reach. Note: FIREBASE_ADMIN_CREDENTIALS is correctly handled separately as a real secret (Secret Manager reference) -- this ruling touches only the public web key.
RATIONALE: A permanent tolerance is right when the rationale is permanent; a dated expiry would just schedule re-deriving the same conclusion. But narrow-scoping is what keeps this from becoming the next overclaiming docstring.
REVERSIBILITY: Trivial, one revertible tolerance line; narrowing later is easy, a blanket exemption hiding future findings is the risk to avoid.

## RULING 33 · B-SECRETSCAN-SCOPE (blocking vs warn-only — CONFIRMED, with a condition from Ruling 30)

DECISION: (a) confirmed -- Ruling 3's exact words were warn-only is the FALLBACK for unmanageable volume, never a target; 30 findings is manageable, blocking is correct. CONDITION forced by Ruling 30: the 30-finding baseline was very likely taken against the SAME stale tree that undercounted CRED-B -- re-take the register against origin/main WITH THE SCANNED SHA RECORDED before the gate is treated as complete. If the true count comes back higher, that does NOT automatically flip to warn-only -- returns to DVA, since a larger backlog is a reason to examine findings, not weaken a working detector.
RATIONALE: A warn-only secret scanner is a scanner that gets ignored within a week -- this run exists because a credential sat readable for 2 months behind checks that were green. An accurate register is what makes blocking survivable.
REVERSIBILITY: Trivial, one-line downgrade if unworkable -- landing strong and adjusting on evidence beats landing weak and hoping someone strengthens it later.

## RULING 34 · B-SECRET-REDACT (Ruling 29 correction — degradation-shape fix REQUIRED, DVA WITHDRAWS its own prior claim)

DECISION: (a) required before merge -- and the timing correction makes the case STRONGER, not weaker. DVA EXPLICITLY WITHDRAWS Ruling 29 Rider 2 ("their gate runs on our PR") as FACTUALLY WRONG -- VER checked the actual triggers, confirmed no pull_request trigger exists, gh run list on the branch is empty. DVA asserted a CI behavior it did not verify, in the same ruling that required verification of others. Consequence runs OPPOSITE to what was assumed: there is NO pre-merge cross-arc signal at all -- if this redaction breaks SHAD-DARSANA's guard, it's discovered ON MAIN, in another campaign's gate, with no chance to catch it first. The pre-merge bar goes UP, not down. Both of VER's findings confirmed MANDATORY: (1) the misleading skip message is a false diagnostic (§N.8 in miniature) -- must be an explicit if-not-LIVE_DSN guard mirroring the file's own idiom; (2) the empty-DSN hazard is SERIOUS, not merely theoretical -- libpq's empty-conninfo behavior reliably falls back to local defaults, so on a dev machine with local Postgres this could SILENTLY CONNECT TO THE WRONG DATABASE -- for a circularity guard, that's not a confusing skip, it's a path to a FALSE PASS in another arc's correctness gate, categorically worse than the original hardcoded credential it replaced.
STATUS: fix already applied and pushed by the builder (commit 8d69c239) before this ruling completed -- confirmed necessary in retrospect, not contingent.
RATIONALE: The question offered an off-ramp (gate can't run, maybe fix after) and the honest answer is the missing gate removes the only thing that would have caught us -- this is not a cosmetic follow-up.
REVERSIBILITY: The fix itself is a trivially-revertible 2-line guard. Merging without it was the branch with the non-obvious failure mode.

CLOSE-REPORT CORRECTIONS FROM THIS BATCH: Ruling 29's "their gate runs on our PR" is WITHDRAWN -- no pre-merge cross-arc signal existed. Authoritative credential census is 9/16/2=27 distinct files on origin/main (B-SECRET-REDACT's figure, not the scanner's 15).

DVA queue cleared again -- 34 rulings delivered as of this point.

## RULING 35 · cross-arc observation (SHAD-DARSANA circularity guard's live half — systemic finding)

QUESTION: Is report-only right for the unexercised live half of the circularity guard?
EVIDENCE: DVA re-read the FULL workflow (not the head-limited slice used in Ruling 29) and found TWO pytest invocations, not one: :85 runs "-m not integration", and :93 runs "-m integration" inside a job literally named circularity-guard-live. The DSN-consuming test IS SELECTED, not deselected as DVA previously inferred -- it skips at RUNTIME for want of DATABASE_URL, which the workflow provides nowhere. The author's own comment at :89 already observes the skip-not-failure shape.
DECISION: (a) confirmed, report-only -- with 3 riders. (1) DVA CORRECTS ITS OWN RECORD: Ruling 29's conclusion survives (the redaction still can't break the gate) but the MECHANISM was wrong (runtime-skip not collection-time-deselection) -- this also REINFORCES Ruling 34: because :93 genuinely selects the test, the empty-DSN hazard becomes live the moment anyone provisions a DATABASE_URL anywhere, making the explicit empty-check required not optional. (2) Name it in the campaign's own vocabulary: a job called circularity-guard-live that has NEVER executed its live check and reports green is an UNEARNED SIGNAL in another campaign's correctness gate -- same class as F-19, F-21, mcp_server_info.tool_count, the MCP smoke test. Strengthens the case for B-N8-LINT (a permanent cross-arc §N.8 guard). (3) Make the handoff specific: file, job name, both invocation lines, the missing env var, PLUS the one improvement this run made (the skip now carries an honest message instead of "proxy not reachable").
RATIONALE: This is the SECOND time this session VER was right where DVA reasoned from an incomplete read -- recorded rather than quietly corrected, because the verification architecture catching its own Dvārapāla twice is the system working as designed. SYSTEMIC FINDING: this is the THIRD unexercised live gate this run has surfaced (tap-ci.yml's live-catalog-parity half /Ruling 28, the two production-proxying workflows /Ruling 21, now circularity-guard-live) spanning TWO campaigns, sharing ONE root cause: no staging target + no CI credential strategy, so every live-mode gate degrades to a permanent skip. Each was written honestly (skips rather than lies) but collectively a meaningful share of this project's CI assurance is nominal. Belongs in Ruling 21's costed spec as evidence and in the close report as ONE named finding, not three scattered footnotes -- the fix is one capability, not three patches.
REVERSIBILITY: Nothing mutated, observation and reporting only.

DVA queue cleared again -- 35 rulings delivered, nothing pending.

STANDING NOTES FOR CLOSE REPORT (recurring themes, both now well-established):
- MCP connector authorization: claude.ai MARSYS-JIS connectors unauthenticated, cannot be OAuth'd non-interactively, blocked live-catalog verification twice (Rulings 17, 25). Any lane unable to reach the live catalog reports UNPERFORMED, never pass/empty.
- Stale-tree discipline: THIRD confirmed instance this run (Ruling 30's CRED-B undercount, DVA's own fresh_chart_smoke.yml scare, this partial read). Every census/count/"X does not exist" claim entering the close report must be re-derived against origin/main with the scanned SHA recorded.

## RULING 36 · Risk rating vs ~10-identity reachability (CORRECTED MECHANISM, RATING HOLDS)

DECISION: (a) rating holds, but for a BETTER reason than originally given. Ruling 1's "empty authorizedNetworks is the only gate" is SUPERSEDED by "IAM is the gate, held by ~10 service-account identities; compromise of any one would suffice." What actually justifies not escalating: exhaustive connection history (100% window coverage, exactly 6 principals ever connected, zero unknowns) OUTWEIGHS the wider theoretical surface -- we're no longer reasoning about gate width, we're OBSERVING nobody came through it. 3 riders: (1) restate the mechanism wherever Ruling 1's wording propagated; (2) the clean result is CONDITIONAL on Ruling 38 (authorizedNetworks history) -- if a broad CIDR was ever present, the whole analysis collapses; (3) Ruling 22's caveat sharpened not removed -- the in-SQL role-creation blind spot is PROVEN not hypothesized (retrieval_census_ro itself has no CREATE_USER admin-op).
RATIONALE: Letting a correct conclusion stand on a flawed premise because the answer didn't change is exactly what this campaign exists to eliminate.
REVERSIBILITY: Rating is a documented judgment, revisable the moment Ruling 38 lands.

## RULING 37 · test_ka_jivana_parva_circularity_guard.py (triple-confirmed, no further action)

DECISION: Confirmed closed by Rulings 29/34/30. Two investigations on SEPARATE branches, different methods, independently located the SAME credential in the SAME file -- corroboration, not duplication. Third independent confirmation of the 27-file census.
REVERSIBILITY: N/A, confirmation only.

## RULING 38 · authorizedNetworks change history (NEW TIME-CRITICAL LANE)

DECISION: (a) own lane, B-AUTHNET-HISTORY -- THE SINGLE MOST TIME-CRITICAL ITEM IN THE RUN. Justified by DECAY not size: every other open item holds at constant cost; this one has a rolling-retention deadline, converting a knowable fact into a permanently unknowable one. Per Ruling 36, this is the ONE finding that could overturn the entire compromise conclusion. TECHNIQUE RULED, not left open: use Cloud Asset Inventory (gcloud asset get-history against sqladmin.googleapis.com/Instance) -- purpose-built for config-change history, does not scan logs (the 9-min timeout means retrying the same log-scan approach will fail identically). Fallback: WINDOWED Admin Activity queries (day/week-sliced, filtered to cloudsql.instances.update, paginated), never another unbounded sweep.
BINDING ON E1-SAMGATI: the close report may NOT state a final compromise conclusion until this resolves OR is declared UNPERFORMED with the retention deadline recorded. An unperformed check is an honest close; a conclusion resting on an unperformed check is not.
RATIONALE: Everything else this run surfaced can be fixed later at the same cost. This cannot -- evidence is expiring while we deliberate.
REVERSIBILITY: Read-only. The irreversible branch is NOT doing it.

## RULING 39 · Rotation role privilege narrowing (MANDATED, supersedes Ruling 1's "equivalent grants")

EVIDENCE: DVA checked what actually consumes CREATEDB/CREATEROLE across BOTH migration directories on origin/main: CREATE ROLE=0 files, CREATE USER=0 files, CREATE DATABASE=0 files. Only CREATE EXTENSION (8 files, requires cloudsqlsuperuser -- a DIFFERENT privilege, migration-time not runtime) and ALTER ROLE (1 file).
DECISION: (a) MANDATED, not recommendation. Ruling 1's "equivalent grants" framing is SUPERSEDED -- correct shape is a PRIVILEGE SPLIT not a privilege copy. New application runtime role carries NEITHER CREATEDB NOR CREATEROLE (zero identified consumers in the corpus). Privileged migration-time operations get a SEPARATE, migration-only credential. Runbook must verify-before-cutover (confirm the narrowed role satisfies real workload in a non-production exercise) so availability is preserved by TESTING not over-granting.
RATIONALE: This is the single highest-leverage action available -- the only one that changes the CLASS of the next incident rather than closing this instance. Ruling 18 established severity is driven entirely by CREATEROLE; rotating into an equally-privileged role means the next leak (and there will be one, on a public repo with a scanner only now repaired) is identically catastrophic. Leaving it a recommendation lets the highest-value item be the one most easily dropped under time pressure.
REVERSIBILITY: Fully reversible (grant restorable in one statement if pre-cutover reveals a consumer). Unrecoverable direction is provisioning a second admin credential and discovering the cost at the NEXT leak.

## RULING 40 · Pruning 2 IAM grants (mandated for the runbook, swarm does not execute)

DECISION: Neither (a) nor (b) as posed -- add both to the rotation runbook as MANDATED steps for the native to execute, with unused-proof attached. Swarm does NOT execute (same class as rotation, same answer as Rulings 1/18/21/28/31 -- consistency IS the ruling). Riders: (1) both mandated not optional -- directly reduce the ~10-identity surface Ruling 36 identified; (2) "unused" must be EVIDENCED not asserted -- cite the six-principal connection sweep explicitly for amjis-mcp-runtime, cite retirement+no-recent-auth for the other. CONSOLIDATION: every credential/IAM action this run surfaced (rotation, version disables, deploy.yml pin bumps, privilege narrowing, fresh_chart_smoke.yml repoint, these 2 prunes) belongs in ONE runbook, executed in ONE session, by ONE authorized operator -- five separate asks is how one gets missed.
RATIONALE: "Lower-risk" is not "in scope" -- the authorization grant draws its line at infrastructure mutation, not risk level. Bundling gives the native one coherent artifact instead of a trickle of individually-plausible requests inviting fatigue-driven case-by-case judgment.
REVERSIBILITY: An IAM binding is trivially restorable WHEN EXECUTED BY SOMEONE WATCHING PRODUCTION -- executed unsupervised by a swarm mid-run, a wrong prune takes down amjis-mcp with no one to notice. That asymmetry is what the ruling turns on.

DVA queue cleared again -- 40 rulings delivered. TOP PRIORITY FOR CONDUCTOR: B-AUTHNET-HISTORY (Ruling 38) is the run's most time-critical lane.

## RULING 41 · Scratchpad race — detection sweep (VER assigned, exhaustive not sampled)

EVIDENCE: gh pr list -> 19 PRs from this run, ALL STILL OPEN -- nothing merged, no corrupted body has landed permanently, all 19 remain editable.
DECISION: (c) exhaustive, narrowly scoped to the class actually exposed, VER owns it. KEY DISTINCTION: a scratchpad swap corrupting a lane's EVIDENCE is largely SELF-DETECTING under this run's architecture -- VER reads the real diff/runs the real tests, so corrupted evidence would surface as REFUTED, not a silent pass. The genuinely exposed class is PROSE ARTIFACTS VER doesn't independently re-derive -- principally PR DESCRIPTIONS, exactly where the incident occurred. ACTIONS: (1) check ALL 19 PR bodies, not a sample -- 19 is small enough to be exhaustive and sampling is the wrong instrument for an enumerable population; (2) assigned to VER (natural extension of its whole-run cross-lane duty); (3) widen ONLY on a hit -- one incident is a race, two is systemic corruption and a different ruling; (4) nothing merged yet so correction cost is just gh pr edit, rising sharply once the merge queue opens.
RATIONALE: "Light spot-check" and "exhaustive check of a narrow class" cost about the same here, and the second yields a statement the close report can actually make ("every PR body was verified") instead of a qualified "we sampled and found nothing" -- exactly the kind of assurance this run has spent 40 rulings refusing to accept from others.
REVERSIBILITY: Read-only detection; corrections are gh pr edit on unmerged PRs, trivial now, harder after merges begin.

## RULING 42 · Scratchpad race — prevention (root cause: isolation was overridden, not absent)

EVIDENCE: each agent session's scratchpad is ALREADY session-unique (per-session UUID path), documented as isolated. The queue already assigns every lane its own worktree. B-AUTHNET-HISTORY used its own worktree correctly.
DECISION: (a) yes, bake into dispatch prompts -- WITH A DIAGNOSIS THAT CHANGES THE FIX. The harness already isolates scratchpads per session -- a collision means two lanes were POINTED AT A COMMON PATH, most likely one named in a dispatch prompt or a repo-relative temp write, overriding isolation that already existed. PRIMARY FIX IS SUBTRACTIVE: stop naming a shared path in dispatch prompts. Bake into every remaining/future dispatch: (1) each lane uses its own agent-session scratchpad or a gitignored .lane-scratch/ inside its own worktree, NEVER a path shared with another lane, never a repo-root temp file; (2) if .lane-scratch/ is used it MUST be gitignored (an un-ignored scratch dir trades a race for a dirty diff); (3) any artifact published outside the worktree (PR body, ledger entry, close-report section) is RE-READ FROM ITS SOURCE immediately before publishing -- the builder only caught this swap because the mismatch was visually obvious, which is luck not process; a read-back before publish is the cheap deterministic check that doesn't depend on noticing.
RATIONALE: This is the SAME THEME that has now bitten the run three times, worth naming as one lesson: infrastructure the run ASSUMED was isolated or current was NEITHER (shared scratchpad assumed per-lane; a working tree 39 commits behind assumed current /Ruling 30; a stale tree assumed authoritative for a credential census). In each case the assumption was invisible until something contradicted it. The read-back constraint (point 3) is worth keeping even if scratchpads were perfectly isolated -- it converts silent corruption into detectable corruption regardless of cause.
REVERSIBILITY: Prompt-level change only, trivially adjustable, no code/data/production surface touched.

DVA queue cleared again -- 42 rulings delivered. PRIORITY ORDER: B-AUTHNET-HISTORY (done, resolved negative) -> VER's 19-PR-body check -> security runbook consolidation (Rulings 39-40).

## RULING 13 CORRECTION · ga_nakshatra.py ownership phrasing + a NEW overlap surfaced

DVA self-corrected its own Ruling 13 action-item phrasing: ga_nakshatra.py:87 was NEVER in B-NAR-GA's scope (B-NAR-GA's own queue entry explicitly routed it away to B-N8-FIX) -- the ruling BODY was correct ("removed from B-NAR-GA's AND B-N8-FIX's scope"), only the action-item's attribution was imprecise. Net effect unchanged: B-N8-FIX will not touch ga_nakshatra.py at all, confirmed.
NEW FILE OVERLAP surfaced (not in any prior ordering table): B-NAR-GA still owns ga_nakshatra.py:289; B-VERIFSTATUS-VOCAB owns ga_nakshatra.py:87. SAME FILE, two lanes, BOTH rebuild_required:true. Applying the same pattern as the envelope.ts case: B-NAR-GA merges FIRST (narration-only, simpler), B-VERIFSTATUS-VOCAB rebases onto it before touching the file. Added to the merge-ordering table (apply when B-NAR-GA is dispatched, still blocked on A8).

## RULING 43 · Build concurrency cap overage (11 dispatched vs declared cap of 8)

QUESTION: 11 build lanes dispatched in parallel against the declared cap of 8, right after the scratchpad-collision incident (Ruling 42) whose fix hadn't yet been applied to those dispatches. Ratify, recall, or something else?
EVIDENCE: manual §2 caps build lanes at 8; only VER/DVA exempt (INT not named as exempt). 11 build lanes + INT + B-AUTHNET-HISTORY = >=13 concurrent agents against a cap of 8.
DECISION: (c) — let the 11 run, close the hazard BY MESSAGE not recall, restore cap to 8 for subsequent waves. (1) Do NOT recall -- killing in-flight work to satisfy a planning number wastes real progress; the cap is a resource/coordination aid, not a correctness invariant. (2) Apply Ruling 42's protections to the in-flight 11 NOW via SendMessage (the read-back-before-publish constraint specifically) since they were dispatched without scratch-isolation instructions and 11 concurrent lanes is materially more race surface than the 2 that collided. (3) Restore cap to 8 for the NEXT wave, lift only once dispatch prompts confirmed to carry scratch-isolation instructions -- if sustained throughput above 8 proves needed, that's a deliberate manual amendment, not drift. (4) VER's 19-PR check (Ruling 41) moves up in priority since the exposed population is growing under the same unfixed condition.
RATIONALE: Discovering a concurrency-induced corruption race and then, in the same tick, going 37% over the declared limit with the mitigation not yet applied is the precise setup that produced the incident, scaled up. The cap wasn't arbitrary -- exceeding it silently is the same species of drift this campaign keeps finding elsewhere (a declared constraint quietly stops being true while everything still looks green).
REVERSIBILITY: Fully reversible -- SendMessage to running agents, no work discarded; cap is a dispatch-time parameter adjustable at any tick.

STATUS UPDATE FROM CONDUCTOR: all 11 dispatch prompts for this wave ALREADY included the scratch-isolation instruction (gitignored .lane-scratch/ inside each lane's own worktree, never a shared path; re-read any artifact from source before publishing) -- this was baked in proactively before dispatch, so Ruling 43's requested mitigation is already satisfied for 10 of the 11 (the 11th, B-MIG474-COMMENT, completed successfully before this ruling landed and didn't carry the instruction, but it was a trivial 1-line fix with no scratch-file use and completed without incident). No additional SendMessage sweep needed. Cap restored to 8 for all future dispatch waves per this ruling.

MERGE-LOCK PRECONDITION NOTE (Ruling 11) applied to A2's live merge: INT has been instructed to check both clauses (no open/auto-merging campaign PR, AND origin/main tip unchanged across a one-tick re-check) before taking the lock -- not a point-sample.

MIGRATION ALLOCATION NOTE: with B-MIGGUARD, B-MIG474-COMMENT, and B-EVENTREG (needs a number) all in flight against a continuously-draining merge queue, the max()-across-both-directories rule read fresh from origin/main AT MERGE TIME (not dispatch time) is now load-bearing, not a formality -- will apply when each reaches the head of the merge queue.

B-DOCS-GOVERNANCE NOTE: carries accumulated scope from Rulings 4/15/16/27 including the CLAUDE.md amendment with its VER-precondition. Sonnet scribe lane, not re-graded. If VER's confirmation on §N.8 instance 3 doesn't arrive before that lane reaches the amendment step, it must HOLD that item and ship the rest rather than proceed on assumption -- will convey this constraint if not already clear from the dispatch prompt.

## RULING 44 · B-MIGGUARD / B-MIG474-COMMENT (17 header mismatches, not 1 -- fourth instance of the pattern)

EVIDENCE: DVA swept all 326 non-archived migrations across both directories: 17 filename-vs-header-number mismatches, not 1. Migration 474 (the one B-MIG474-COMMENT already fixed) is the brief-named instance; the other 16 are ALL in platform/supabase/migrations/ (225/header171, 182+183/both header174, a contiguous run 335-341 off by 1-3, 433/header431).
DECISION: (c) primarily + (a) for the completed lane. (1) B-MIGGUARD's BLOCKING gate is duplicate-filename-numbers ONLY, as declared in its queue scope. If it ALSO implements header/filename consistency checking, THAT PART MUST BE WARN-ONLY until the 17-file backlog clears -- a blocking header check would hard-stop the merge queue at the exact tick it opened, on 17 pre-existing defects this campaign didn't create (Ruling 33's blocking-vs-warn principle applied in reverse: blocking is right for a clean/small backlog, warn-with-named-owner is right for a real inherited backlog that would otherwise fire immediately). (2) B-MIG474-COMMENT merges AS-IS -- do not reopen a completed, VER-forwarded lane to absorb 16 more files; it fixes the one the brief named, that's a clean close. (3) The other 16 go to B-MIGGUARD (which already owns the standing migration-protocol doc) as evidence for that doc -- fixing them is AUTHORIZED but NOT BLOCKING (comments only, no functional effect, applied SQL correct in every case).
RATIONALE: Preventing a self-inflicted red on main in the run's FIRST merge window -- a guard firing on inherited debt right as the queue opens would look exactly like a regression this campaign introduced. BROADER PATTERN (4th instance this run): a scope derived from ONE named instance turns out to be a CLASS -- 9 credential files became 27 (Rulings 19/30), 1 tool-count discrepancy became a decoupled-metric class (Ruling 25), now 1 header mismatch is 17. Every one found by SWEEPING, none by reading a list -- the strongest argument this run can make for detectors over inventories.
REVERSIBILITY: Trivial -- warn-only upgrades to blocking in one line once backlog clears; 16 header edits are comment-only and revertible; nothing touches applied SQL or migration sequence.

## RULING 45 · Ruling 13 clause 5 re-confirmed on corrected premise (B-NAR-TS overlap)

EVIDENCE: DVA read B-NAR-TS's declared scope directly from session_queue_SAMAPTI.yaml (not accepting either party's characterization): verbatim scope includes "envelope.ts:1416" AND "register_p1_ganita.ts:374 (PLAUSIBLE)". VER's correction confirmed on both counts -- builder conflated B-NAR-TS with B-N8-TS-SERVE, real overlap spans TWO files not zero.
DECISION: (a) ordering STANDS unchanged. B-VERIFSTATUS-VOCAB rebases onto B-NAR-TS before touching envelope.ts, now extended to register_p1_ganita.ts too. Direct application of Ruling 25's standing rule, not carving an exception on its first test: same-file merges always require rebase; line-level disjointness reduces conflict probability but does NOT eliminate it; no lane may treat "different lines" as "no conflict." VER's low-risk assessment is likely correct on the merits but is explicitly a PREDICTION from undispatched-lane scope -- "a rule that holds only when someone predicts the risk is low is not a rule."
TWO REFINEMENTS: (1) the register_p1_ganita.ts overlap is CONDITIONAL (marked PLAUSIBLE, A8-NAR-TRIAGE may adversarially REJECT it, removing the file from B-NAR-TS entirely) while the envelope.ts overlap is UNCONDITIONAL -- binds regardless. (2) MORE IMMEDIATE ADJACENCY, LIVE RIGHT NOW: register_p1_ganita.ts is ALSO in A2-CI-POINTERS's widened set (lines 640/1084), and A2 is merging THIS TICK. B-VERIFSTATUS-VOCAB's own sites sit at ~35/717/1104 -- 1104 is roughly 20 LINES from A2's 1084. Per Ruling 25, B-VERIFSTATUS-VOCAB rebases onto A2 regardless -- but the point worth adding: this is not only about avoiding conflict markers. B-VERIFSTATUS-VOCAB was developed against PRE-A2 main, so A2's merge SHIFTS LINE NUMBERS and the lane's own line references (used throughout VER's evidence citations) go stale. The rebase is required for the CORRECTNESS OF ITS OWN EVIDENCE, not merely to avoid a textual clash -- VER should re-confirm its line citations POST-REBASE rather than carrying pre-A2 numbers into the ledger.
RATIONALE: VER declining to let a builder's false scope claim carry an ordering release is exactly the verification architecture working as designed -- 2nd time this run a lane asserted a scope fact the queue text contradicts (1st was DVA's own Ruling 13 mis-attribution). Both point the same way: scope claims must be read from the queue, not recalled. The answer being "boring" (ordering just stands) is the point -- a standing rule that bends to a favorable prediction stops constraining anything.
REVERSIBILITY: Trivial -- one rebase, droppable once B-NAR-TS dispatches and its true diff proves disjoint. Merging without it risks a silent semantic collision in the run's highest-stakes lane.

NOTE: B-VERIFSTATUS-VOCAB's CONFIRMED status is UNTOUCHED by this ruling -- the factual error was in a merge-sequencing claim, not the directional safety property (independently re-derived and extended to 300/300 by VER).

ACTION FOR CONDUCTOR: when B-VERIFSTATUS-VOCAB reaches the merge queue (after A2), rebase it onto the then-current origin/main (post-A2, and post-B-NAR-TS if that lane has merged by then), and ask VER to re-confirm its line-citation-dependent evidence (especially the ~1104 site near A2's ~1084) against the rebased code before final merge sign-off.

## RULING 46 · MERGE-LOCK against a concurrently-active swarm (SUPERSEDES Ruling 11's stability window)

EVIDENCE: manual §5's two clauses guard TWO DIFFERENT hazards, per brief §4's own stated rationale ("multiple autonomous campaigns sharing one working tree is the top structural risk" -- referring to a 2026-07-29 incident where a session's shell drifted into the SHARED LOCAL CHECKOUT). Clause (i) = no campaign PR open/auto-merging AT LOCK ACQUISITION. Clause (ii) = no live session editing the SHARED CHECKOUT specifically (not "another campaign's GitHub activity" broadly). main advanced 5f5033a5->d7bc0a9a via merged PR #916; 5 SHAD-DARSANA merges in this run's window; A2 touches TS harness/tool files, adds NO migration.
DECISION: (a), Ruling 11's one-tick stability window FORMALLY SUPERSEDED for this condition. Ruling 11's window was calibrated for "a session merged recently and may still be working" (transient, expected to resolve) -- against a CONTINUOUSLY ACTIVE SWARM it tests for a condition that will NEVER obtain, making it a deadlock not a safety control. CORRECT READING (resolves without weakening): clause (i) is checkable even against an active swarm (there ARE gaps between their merges) -- a MERGED PR is discharged, not blocking; clause (ii) is about OUR local tree, unaffected by SHAD-DARSANA working in its own worktrees.
INT'S REPLACEMENT PRECONDITION FOR A2 (and future merges): (1) no campaign PR open/auto-merging at lock acquisition, AND none touching A2's SPECIFIC FILES (file-scoped not activity-scoped -- the Conductor's original rider, adopted); (2) rebase onto current origin/main tip, CI green must be on the REBASED tree not the pre-rebase branch; (3) re-check the tip immediately before merge -- if main advanced between CI-green and merge, re-rebase and re-verify; (4) BOUNDED RETRY: if main advances 3 CONSECUTIVE times before landing, STOP and return to DVA -- evidence the two swarms' cadences are genuinely incompatible, needs a different answer, not an infinite loop; (5) migration allocation is the live hazard for FUTURE lanes (A2 is clear, adds no migration) -- with B-MIGGUARD not yet landed and 2 swarms merging concurrently, every subsequent lane allocating a number (B-EVENTREG especially) must compute max() across both directories at lock AND re-verify after rebase immediately before merge.
RATIONALE: Two autonomous swarms on one repo is ordinary concurrent development git handles fine, PROVIDED each rebases onto current main and proves CI green on the REBASED tree -- what it does NOT tolerate is merging a branch validated against a tree that no longer exists. Waiting for silence would stall this run indefinitely against a campaign that just announced 5 fresh lanes. FREE ASSET: their PR titles announce their own state (#916 literally says "Night 2 open, 5 lanes dispatched") -- no direct channel to them, but their cadence is readable from public PR metadata, INT should use it for timing.
REVERSIBILITY: Fully reversible -- every step is rebase-and-recheck, nothing merges unverified, bounded retry means genuine incompatibility escalates rather than silently degrading. Waiting is the option with unbounded cost.

INT CLEARED TO PROCEED ON A2 under conditions 1-5.

STANDING CONSEQUENCE FOR CLOSE REPORT: all 19 SAMAPTI PRs are now behind main and will need rebasing. Rebase-per-merge is now the norm for the remainder of the run, not the exception -- per Ruling 45, any lane whose evidence cites line numbers must re-confirm those citations post-rebase, not carry pre-rebase numbers into the ledger.

## RULING 47 · B-COCKPIT-INCOMPLETE (DataAssetsView TERMINAL set)

EVIDENCE: DVA read the actual code (DataAssetsView.tsx:126-127) -- TERMINAL has exactly one use, gating whether an SSE overlay is deleted or kept. Confirmed 3 facts making the effect benign: only to_state==='lit' events can produce a 'lit' overlay reading and 'lit' IS terminal (deletes the overlay) -- so this CANNOT produce a false 'lit'; any subsequent transition self-corrects; ALL overlays clear on run.state_change regardless, so polled stats resume as sole truth at run end. Also: per Ruling 24's fix, deriveState now has a first-class incomplete branch, so overlay and polled value AGREE -- the divergence is mechanism (timing), not content (wrong badge).
DECISION: (c) defer as proposed, but REFRAMED not left loose. Do NOT reopen the lane (cost of a re-verification cycle on an already-VER-passed lane is disproportionate to a display-timing quirk). BUT: do NOT record as "cosmetic" -- the underlying error is SEMANTIC: incomplete IS a terminal build state (the run ended, the asset didn't finish) -- excluding it from a set named TERMINAL tells the next developer the opposite. Codebase now has 6 places that understand incomplete and 1 that doesn't -- same fragility class as Ruling 24 (partial knowledge of a state scattered across a subsystem). Tracked item, named owner (next cockpit-touching lane), CORRECTED RATIONALE recorded ("incomplete is semantically terminal, missing from 1 of 7 state-aware sites" not "SSE overlay timing"). If VER hadn't yet returned its verdict, fold the token in now for free.
RATIONALE: "Cosmetic" is how a semantic gap survives three campaigns -- this run has repeatedly found the thing filed as cosmetic was a correctness defect wearing a cosmetic label (Ruling 24 itself). Recording the real reason costs nothing.
REVERSIBILITY: Trivial, one token, no data/behavior change, deferral collapsible into any cockpit-touching lane anytime.

## RULING 48 · B-OIR-RECONCILE (yoga catalog corruption -- PARK-HONEST, NOT attached to C1)

EVIDENCE: DVA queried the real table (brahma_yoga_catalog, not "bg_yogas" which doesn't exist): 186 rows confirmed, both named OCR examples (dariclra, datidra) confirmed present as canonical_id values -- finding is real. BUT: no_sanskrit_name=0, no_formation_rule=0, no_citation=0 across ALL 186 rows -- every row is FULLY POPULATED. The garbage rows aren't empty stubs, they carry real formation rules/citations attached to a CORRUPTED IDENTIFIER. This is an identifier-derivation defect, not a content-absence defect.
DECISION: (c) PARK-HONEST with a costed spec, explicitly NOT absorbed into C1. Three reasons: (1) WRONG LAYER/SEMANTICS -- brahma_* is L0 reference data using ON CONFLICT upsert per §N.3, C1 is a per-chart L1->L5 delete-then-insert rebuild -- folding a global L0 catalog repair into a per-chart rebuild puts two different idempotency contracts under one BUILD-LOCK hold. (2) needs a FIX before a rebuild helps -- C1 is a rebuild operator, cannot author extractor validation. (3) DECISIVE: REFERENTIAL INTEGRITY nobody analyzed -- canonical_id is global reference data consumed by yoga detection (ga_yoga_firings and anything else keyed on those ids) -- re-deriving canonical_ids would ORPHAN EXISTING FIRINGS ROWS, a migration-shape question requiring its own design.
SPEC GUIDANCE: two DISTINCT defects needing different fixes -- the 47/144 coverage gap needs real extraction with a corrected extractor; the 89/186 identifier corruption may be repairable FAR more cheaply by re-deriving canonical_id from the ALREADY-POPULATED name_sa field, with an FK-migration plan for dependents -- NO re-extraction needed. This distinction may turn a feared re-extraction into a bounded data-repair migration.
RATIONALE: A rebuild with the same extractor reproduces the garbage -- the referential-integrity dimension makes this decisive, not just a scheduling call. Against a run already carrying a security escalation + an expiring-evidence lane + 19 unmerged PRs, adding an unanalyzed schema-adjacent migration is the scope creep that risks the close itself. Truth over completion means an honest park here is the BETTER outcome -- a rushed re-derivation could orphan every yoga firing in the database.
CLOSE-REPORT NOTE: record prominently as a newly-discovered instrument-quality defect, not an appendix line -- CLAUDE.md §J's acharya-grade standard is not met by a catalog keyed on "dariclra". Genuinely important AND genuinely outside this brief's scope, both halves belong in the report.
REVERSIBILITY: Parking fully reversible (defect stable, evidence recorded, spec makes next wave cheap to start). Attaching to C1 is the branch with the irreversible failure mode.

## RULING 49 · B-OIR-RECONCILE (D3 supersede REJECTED -- 5th instance of "one instance is actually a class")

EVIDENCE: DVA measured directly: 68 worktrees, 337 local branches, 217 remote branches, 175 remote branches NOT merged into origin/main. C7's declared scope says "git worktree list = 35 entries... audit the remaining 24" -- describes a population HALF the actual size. E1 §8.2 tests "nothing UNPUSHED" (git log origin/<b>..<b> empty) -- a DIFFERENT property than UNMERGED (a branch can be fully pushed, §8.2 passes cleanly, and still sit unmerged forever). §8.3 covers dangling PRs, not stale branches.
DECISION: (b) explicit PARK, supersede REJECTED. Two independent gaps: (1) C7 scoped to 35 worktrees, reality is 68 -- can still run but must be RE-SCOPED against a measured count, not the stale queue figure, or it will "complete" having audited a minority of what exists. (2) NOTHING covers 175 unmerged remote branches -- the unpushed-vs-unmerged distinction is easy to miss (both sound like "branch hygiene") but getting it wrong would let E1 report clean while 175 stale branches sit untouched forever. D3 recorded PARKED-HONEST with measured counts (68/337/217/175), not SUPERSEDED-BY-LANE -- a supersede asserts another lane covers it, that assertion is FALSE and would close a real item on a wrong premise.
RATIONALE: This run has now hit the "scope measured against a stale figure" pattern enough times (9->27 credential files, 1->17 migration headers, 35->68 worktrees) that it's worth stating once as a finding in its own right -- a lane that measures its own scope against a stale brief figure will under-deliver and report success.
REVERSIBILITY: Trivial -- park is a recorded disposition upgradeable anytime; C7's scope correction is a queue edit before dispatch (C7 not yet dispatched, gated on all_build_lanes_terminal -- correction can be applied then).

## RULING 50 · Governance-ceiling enforcement timing (found: NO ceiling is actually CI-enforced)

EVIDENCE: DVA checked how the ceiling is ACTUALLY evaluated: ci.yml runs drift_detector.py/schema_validator.py with NO ARGUMENTS -- whole-repo absolute scans, gated as exit-code 0-or-3. Grepped both tools for baseline/threshold/ceiling/delta logic: NONE EXISTS. SAMAPTI_T0_BASELINE.md isn't even on origin/main yet (rides in A1's unmerged PR).
DECISION: (c) -- premise wrong in an instructive way, cheaper fix than (a). TWO CORRECTIONS: (1) the check is ALREADY absolute not delta-against-branch-base -- combined with Ruling 46's rebased-tree CI requirement, each PR's scan already sees every earlier merge, so the combinatorial scenario is largely already covered. (2) MORE IMPORTANT: the CI gate enforces NO COUNT CEILING AT ALL -- it gates on exit code, and exit 3 means "findings exist, tolerated" -- 216 findings and 316 findings BOTH exit 3 and BOTH PASS. Ruling 4's ceiling was NEVER CI-enforced, it's a reporting standard measured once at E1.
FIX: no new gate, no new merge step. INT records BOTH counts in SAMAPTI_MERGE_LEDGER.md at EACH merge (the tools already run -- this is transcription, not computation). Converts "E1 discovers 45 instead of 43 with no attribution across 19 merges" into "we know exactly which merge moved it." Declined to add a BLOCKING count gate mid-run (same reasoning as Ruling 44 -- could red-line a draining queue on inherited debt) -- record now, gate later if the trail shows it's needed.
RATIONALE: The Conductor's instinct toward merge-time re-checking was right about the RISK, wrong about the MECHANISM -- re-checking without a comparison enforces nothing, and the actual protection mostly already exists via the rebase requirement. The real finding: a number declared a hard invariant in Ruling 4 has NO enforcement anywhere except a single terminal measurement. Recording per-merge is the minimum that makes it a real invariant rather than an aspiration.
REVERSIBILITY: Trivial -- a ledger column, upgradeable to a gate at any point on evidence.
ACTION FOR CONDUCTOR: instruct INT to record drift_detector/schema_validator counts in SAMAPTI_MERGE_LEDGER.md at every merge going forward, starting with A2.

## RULING 51 · B-LOOP-G4G5 G5d — RATIFIED, but RELABELED (REJECTED-ON-EVIDENCE, not PARKED)

DECISION: (a) ratified with a relabel that IS the substantive part of the ruling. The refutation is mechanism-level, specific, with a named harmful consequence (query_spine_bundle confirmed live in the full profile + compact overflow; calibration_context_only confirmed load-bearing via no_leakage_filter.ts + a parity test; feature_flags.ts confirms it's a deliberately-reviewed live addition). Applying G5d as briefed would DELETE A LEGITIMATELY-SERVED TOOL from every profile -- not a deferral candidate, a WRONG INSTRUCTION. THIS IS NOT A PARK, IT IS REJECTED-ON-EVIDENCE per brief §14.1's four-way taxonomy: PARKED-HONEST means "real work, deferred"; REJECTED means "investigated, not a defect." Recording as parked would leave a phantom open item a future wave might dutifully "close" by breaking the serving profile -- the campaign committing the exact error it exists to prevent. Conditions: preserve the builder's reversal spec verbatim; record the refutation WITH its evidence, not just the verdict; state plainly in the close report that G5 closed 3 of 4 sub-items with G5d REJECTED-ON-EVIDENCE -- G5 must not be reported as wholly closed.
ADDITIONAL FINDING ROUTED: the 6-tool artifact drift (152 committed vs 158 regenerated vs 124 live-served -- a THIRD number in the same decoupled-metric family as Ruling 25) routes to B-MCP-CATALOG-GAP, which already owns tool-count reconciliation and strengthens its core requirement (re-source from the ACTUAL served catalog, since every generated figure in this codebase has drifted). Builder correctly declined to drag ~4,000 unrelated lines across 11 files into this lane.
RATIONALE: The builder did precisely what this campaign asks -- verified before fixing, found the premise false, measured the collateral cost of "doing it properly," reverted cleanly, escalated rather than deciding alone. The label change matters because disposition labels are how findings survive into the next campaign -- "parked" would quietly re-arm a defect-shaped instruction that is actually a bug in the brief.
REVERSIBILITY: Trivial and pre-specified, builder left an exact reversal spec.

## RULING 52 · C1-REBUILD vs. corrupted yoga catalog (Ruling 48 addendum -- gap VER identified, now closed)

EVIDENCE: DVA ran the check rather than routing it. C1's writer set DOES consume brahma_yoga_catalog (ga_yoga.py, ga_yoga_writer.py, ga_structural_writer.py, AND bo_laksana.py the L2 root node; plus l0_yogas.py/l0_rules.py at L0) -- VER's concern was well-posed. Decisive query against live production: ga_yoga_firings = 213 rows, all fired=true, 33 distinct yoga_canonical_id values. Firings referencing dariclra/datidra/another = 0. Firings on ANY suspect id = 0. Firings with NO matching catalog row = 0 -- referential integrity currently holds.
DECISION: (a) already performed -- C1 CLEARED TO PROCEED, with one cheap post-run assertion. Corruption is real but INERT for C1's purposes -- none of the suspect ids has ever fired, every existing firing resolves to a genuine row, C1 rebuilds the SAME charts against the SAME catalog (a deterministic re-run should reproduce the same 33 ids). No gate, no exclusion, no pre-C1 lane needed. ADDITION: after C1, VER asserts (as part of its existing FORENSIC 7/7 + row-count-invariant verification) that ga_yoga_firings contains NO row matching the suspect set, and firings_with_no_catalog_row is still 0 -- converts "we reasoned it stays inert" into "we watched it stay inert."
TWO FINDINGS SHARPENING RULING 48's PARKED SPEC: (1) the FK concern is confirmed live and currently clean -- 213 rows/33 ids is a BOUNDED, TRACTABLE migration scope for whoever eventually fixes the catalog, not open-ended. (2) yoga detection coverage is thin INDEPENDENT of the corruption -- only 33/186 catalog entries have EVER fired (~18%), separately from the 47/144 YOGAS_CORE coverage gap -- the corrupted rows are dormant partly because MOST of the catalog is dormant, a more interesting instrument-quality finding than the OCR garbage alone. Also: dariclra/datidra being 2 corrupted spellings of the SAME real yoga (Daridra/poverty yoga) means the corruption produces DUPLICATE ENTITIES not just ugly names -- a de-duplication step belongs in the eventual spec alongside re-derivation.
RATIONALE: Ruling 48 ruled on who owns the FIX but said nothing about whether the UNFIXED state was safe to build against -- an incomplete ruling, and VER's catch was right. Worth 5 minutes to establish rather than assume, since the alternative was running a BUILD-LOCK rebuild hoping dormant garbage stayed dormant.
REVERSIBILITY: Read-only throughout, C1's own snapshot remains the rollback path, post-run assertion changes nothing.

## RULING 53 · B-PB8-BYTEEQ live-verification ownership (SPLIT: merge half vs verification half)

DECISION: Split the list -- INT does merge+migrate+deploy-watch+health (its normal job), WITH ONE CORRECTION: migration number is NOT 475-by-assumption, recomputed as max() across BOTH directories at MERGE-LOCK per Ruling 46 condition 5 (SHAD-DARSANA merging concurrently on its own cadence = exactly the "467 claimed 3 times" stale-base defect risk). C4-LOOP-LIVE-PROOF absorbs the rest (flag toggle, real reading, verify script, record output) -- NOT inside the MERGE-LOCK window, since MERGE-LOCK is the run's scarcest serial resource with 19 PRs queued and a live reading+verify has no need of the lock. C4 already performs "a real deployed reading" as its core scope -- near-total overlap.
RATIONALE: merge-and-migrate genuinely is INT's job; flag-toggle-and-verify genuinely is not and must not sit inside the lock -- keeping the scarce resource free is the whole point of the two-lock model.
REVERSIBILITY: Trivial, routing decision, reassignable anytime.

## RULING 54 · PARIPRASHNA_STREAM_CAPTURE default posture (STANDING, low sample rate)

DECISION: Standing low-sample-rate capture with a force-capture path, NOT a one-off toggle. DECISIVE EVIDENCE: this run has already ruled on 3 SEPARATE verification capabilities that never actually run because they require manual enablement (tap-ci's live-catalog-parity half /Ruling 28, the 2 production-proxying workflows /Ruling 21, SHAD-DARSANA's circularity-guard-live /Ruling 35) -- each honestly written, each degrades to permanent skip. A one-off toggle is the same pattern with a fresh coat of paint -- it will be off, and the next person who needs it will find the path rotted. Countervailing concern (always-on capture of reading content) is real but mild (native's own chart, own infra, 14-day retention). CONDITIONS: document the sample rate explicitly; ENFORCE (not just default) the 14-day retention; make capture state OBSERVABLE (not just code-readable); provide a force-capture-next-reading path (a low sample rate means the specific reading you want probably isn't sampled).
RATIONALE: choice is between a capability always slightly on vs one reliably off when needed -- given this project's demonstrated pattern, the second isn't really a capability.
REVERSIBILITY: Trivial, one-line flag default, bounded retention = no accumulating liability.

## RULING 55 · PARK_PB-3_L-5 Option A vs B (DECIDED: Option B, separate table) -- memo has MORE unrated items, flagged

EVIDENCE: DVA read the memo on origin/main directly -- §4 already marks Option B RECOMMENDED ("never touches mi_pramana, honors §14.1 provenance"). mimamsa_calibration is the mi_pramana per-match ANALYTICAL scoring schema from migration 348.
DECISION: Option B -- separate mimamsa_conversational_calibration table. Memo's own recommendation confirmed correct, holds on 3 INDEPENDENT doctrinal grounds beyond what it cited: (1) §N.5/B.1 layer separation -- deterministic analytical scoring vs user-confirmed conversational outcomes are genuinely different provenance, co-locating requires a discriminator that will eventually be forgotten; (2) THE DOCUMENTED CONTAMINATION TRAP -- MSR_UCN_CONTAMINATION_AUDIT (CLAUDE.md §C item 16) is one of two traps L2+ is explicitly forbidden to repeat (interpretation contamination of the deterministic base) -- writing conversational outcomes into mi_pramana's table IS that trap relocated to L5; (3) §N.6 Serving Density -- two calibration sources with different confidence semantics flattened into one set is precisely what the density principle forbids. Option A's only advantage (avoiding a new table) is not an advantage -- L5 is SEALED IN STRUCTURAL MODE BY DESIGN (CLAUDE.md §E), expecting calibration data to accrue over time -- build for 2 distinct accruing sources from the start.
CONDITIONS: additive only (no ALTER on mimamsa_calibration, no contact with mi_pramana); migration number allocated at MERGE-LOCK per Ruling 46 + the memo's own §6 guard.
FLAG: the memo's §5 lists SEVERAL items needing a ruling, of which A-vs-B is only item 1 -- "a memo half-ruled is a park with extra steps." REMAINDER ROUTED TO DVA (Conductor action: fetch and relay the memo's remaining §5 items).
REVERSIBILITY: additive table, trivially droppable if unused. Option A would be the hard-to-unwind branch -- disentangling 2 provenances from 1 table after data accrues is far costlier than the reverse.

## RULING 56 · INF-3 Write-block root cause (CLOSED as EXPLAINED, with a standing-practice correction)

DECISION: (a) EXPLAINED, confirmed FIRST-HAND -- DVA's own operating instructions carry the identical policy verbatim ("Do NOT Write report/summary/findings/analysis .md files. Return findings directly as your final assistant message"). Builder isolated the trigger correctly (filename pattern). Deliberate harness policy, not a defect. Closes with NO code fix.
CORRECTION TO STANDING PRACTICE (important): the builder's workaround (rename away from report-pattern filenames) satisfies the LETTER of the policy while DEFEATING ITS INTENT -- NOT to be enshrined as standing practice ("evade the filter" as doctrine would be this campaign doing the exact thing it exists to prevent). CORRECT STANDING PRACTICE: (1) a lane producing a GENUINE GOVERNANCE DELIVERABLE (REPORT_PB.md, the close report, ledgers, versioned register artifacts) hands the content back to the CONDUCTOR, who writes it -- already brief §1/§9's rule, just being restated as the actual practice; (2) renaming is acceptable ONLY where the artifact genuinely is NOT a report and the filename merely trips the pattern incidentally; (3) NEVER a heredoc or alternate-tool bypass (brief §1 forbids explicitly, untouched by this ruling).
RATIONALE: This run has spent 56 rulings insisting signals mean what they claim -- adopting "rename to dodge the check" as doctrine would be the campaign quietly doing what it exists to prevent. Hand-back costs one message and is already the documented rule.
REVERSIBILITY: Documentation only, prompt-level convention adjustable anytime.

## RULING 57 · B-EVENTREG sibling test-stub compatibility (RATIFIED, standing precedent) + coverage-gap flag for VER

DECISION: (a) ratified, STANDING PRECEDENT for the rest of this run and future ones: "fixing what your own change broke is in scope by default; only a BEHAVIORAL change to a sibling file needs a ruling." A change that breaks call sites isn't complete until fixed -- leaving them red means landing a PR that breaks main. Splitting into a separate commit would be WORSE (creates an interval where main is red by construction).
RATIONALE: trivial on the merits, worth a standing precedent because this run has spent real time on "yes, obviously" questions -- each round-trip costs more than the change.

WHILE CHECKING, DVA FOUND A POTENTIAL GAP (not asserted as a defect -- DVA's own regex count was unreliable and explicitly disclosed as noise, not evidence): emit_event has 25 production call sites in the sidecar; the new cursor parameter is OPTIONAL with a None default -- the same property that made the sibling stubs fixable by widening ALSO means any call site not updated keeps working, emits nothing to the durable store, and looks IDENTICAL to one that persisted. No detector, no error, no gap in the logs. This is the §N.8 shape, landing inside the exact lane whose purpose is fixing "no queryable history at all."
REQUIRED OF VER BEFORE CONFIRMED (not a re-open, a verification requirement): (1) confirm the TWO MANDATED events (asset.noop_completion, asset.noop_completion_rejected, named explicitly in brief §9.6) pass a cursor at their ACTUAL emission sites -- if these two persist, the lane's core mandate is met regardless of the other 23 sites; (2) the coverage gap must be DISCLOSED not implied-complete -- state how many of 25 sites persist vs don't, "a durable register exists" and "every event reaches it" are different claims; (3) the queue's own acceptance still governs: VER emits a real event and queries it back out END-TO-END, not by inspection. If only the 2 mandated events persist, that is a PERFECTLY GOOD OUTCOME (it's what the brief asked for) -- it just has to be said plainly, not left to read as full coverage.

## RULING 58 · B-MIGGUARD R3 -- doctrine vs live pipeline (RESOLVED: doctrine narrowed, real tracker defect found)

EVIDENCE: deploy.yml:205-210 confirmed to bulk-migrate via npx tsx scripts/migrate.ts with a hard exit 1 + visible ::error:: if PROD_DATABASE_URL unset (fails loudly, doesn't skip silently). migrate.ts's own header: "Idempotent migration runner... Tracks applied migrations in _migrations_applied (id, filename, applied_at, sha256)... per-migration transactions... ROLLBACK and exit non-zero on error" -- a well-built runner. DECISIVE CLUE: the doctrine's own provenance tag is [[feedback-deploy-migrations-silent-noop]] -- the remembered failure was NOT "bulk migration is dangerous," it was "migrations silently doing nothing while the deploy reported success."
DECISION: (c) doctrine meant something narrower; clarifying it surfaces a live defect that IS the original failure's actual mechanism. (b) wrong -- the runner is transactional/tracked/loud-failing, has run without incident, removing auto-migration would ship code ahead of schema (strictly worse). (a) too permissive -- deleting the rule discards a real hazard still present.
TRUE DOCTRINE CONTENT (replaces current wording in both CLAUDE.md and ONGOING_HYGIENE_POLICIES): never RELY on the deploy-time bulk runner to apply your migration -- author it surgically, VERIFY it actually applied, and NEVER EDIT A MIGRATION FILE AFTER IT HAS BEEN APPLIED. The pipeline running migrate.ts is fine and intended; blindly TRUSTING it is not.
THE DEFECT GIVING THE RULE TEETH: getApplied() executes SELECT filename FROM _migrations_applied -- returns a set of FILENAMES. The tracker records sha256 but the applied-check DVA could see keys on FILENAME ALONE. If that holds through runMigrations: (1) a migration EDITED after being applied is SILENTLY SKIPPED (filename already present, never re-runs, deploy reports success) -- THIS IS THE SILENT NO-OP THE FEEDBACK TAG NAMES, sha256 exists for exactly this comparison and appears unused -- a recorded value with no detector behind it, textbook §N.8; (2) a RENUMBERED migration RE-RUNS (applied as 467_..., renamed to 474_..., tracker sees an unfamiliar filename, executes again -- non-idempotent SQL would fail or double-apply). CONNECTS DIRECTLY TO RULING 44: 17 header/filename mismatches = 17 pieces of evidence of renumbering, and migration 474 itself went 466->467->474 -- renumbering practice and a filename-keyed tracker are in DIRECT CONFLICT, and this project does both. That is the systemic finding underneath R3.
ASSIGNMENTS: (1) B-DOCS-GOVERNANCE rewrites §N.4's line in CLAUDE.md + ONGOING_HYGIENE_POLICIES to the narrow form above, under Ruling 15's constraints (may correct wording, may not alter doctrine scope; CLAUDE.md needs both version bumps + changelog + fingerprint registration). (2) B-MIGGUARD takes the tracker defect: VER FIRST CONFIRMS whether sha256 is actually compared anywhere in runMigrations (DVA only read getApplied, explicitly declines to assert beyond what it read) -- if not compared, fix is to compare content hashes and FAIL LOUDLY on mismatch, never skip; add to the standing protocol doc B-MIGGUARD already owns, alongside the renumbering conflict.
NO NATIVE ESCALATION NEEDED: manual §4 places "any risk call (destructive rebuild, MIGRATION SHAPE, deploy-on-red)" squarely with DVA -- this is a doctrine clarification + code defect, ordinary in-scope work, unlike the credential/IAM actions DVA has consistently held outside the authorization grant.
RATIONALE: A rule written as a blanket prohibition against a pipeline that demonstrably works had become unfollowable and was therefore being ignored -- which is how a real hazard hides behind an over-broad rule. Narrowing makes it both true and followable.
REVERSIBILITY: Doctrine edits single-commit revertible. Tracker fix is NARROWING (can only cause a loud failure where a silent skip occurred, never the reverse) -- the safe direction per Ruling 14 clause 5.

## RULING 59 · B-N8-CI-GATES reopen-cycle-2 questions (F-29 ceiling coupling: NO ACTION NEEDED; hard_gates_check.sh exit 2: ACCEPTABLE, residual routed)

EVIDENCE (Q1, DVA verified directly rather than trusting either lane's report): checked out `origin/samapti/governance-docs` @ `b3e0d510` (B-DOCS-GOVERNANCE's post-Ruling-58 tip) into an isolated worktree and ran both governance tools live: `schema_validator.py` -> `43 violations; exit=3`; `drift_detector.py` -> `216 findings; exit=3`. Both exactly match the Ruling 4 T0 baseline that F-29's fix (per B-N8-CI-GATES' own SAMAPTI_B_N8_CI_GATES_DISPOSITIONS_v1_0.md) now pins as a CI ceiling. B-DOCS-GOVERNANCE's own builder report said the same (43/216 unchanged) -- independently reproduced, not merely trusted.
DECISION Q1: (a) no action needed, no temporary 45-with-expiry authorized. The "two `artifact:` additions would make it 45" concern does not materialize against the actual current tip of either branch -- B-DOCS-GOVERNANCE's new files (PARIPRASHNA plan, PB campaign close, memo index, etc.) did not add schema violations. CONFIRMED as the ratchet Ruling 4/F-29 intends: the ceiling holds at the ORIGINAL ID SET's ambient state, not a lane-by-lane entitlement to add violations. If a FUTURE lane's merge does push the count above 43/216, that lane fixes its own contribution before merge (or gets a dated exception ruled individually, evidence-first as here) -- no standing allowance granted.
EVIDENCE (Q2): re-read F-26's own disposition (already on `origin/samapti/n8-ci-gates`): exit 2 = "12 GREEN, 0 RED, 1 NOT_ASSERTED, 2 STALE" -- G3 globs for a retired Python-era `test_chart_facts*.py` (coverage now lives in TypeScript), G12 globs for `build_chart.py`, superseded by the orchestrator per L1_GANITA_CLOSURE. Both were previously reported fake-GREEN; STALE is the honest label per the same file's own doctrine ("an unperformed check is never a pass either"). Confirmed separately: no GitHub workflow invokes `hard_gates_check.sh` at all (F-26's own "reported, not fixed" note) -- exit 2 is invisible to CI either way.
DECISION Q2: (a) exit 2 is ACCEPTABLE to ship as-is. Repointing G3/G12 to their current TypeScript-era targets is a BUILD-LAYER test-location judgment (L1/L2 test-inventory knowledge), not a CI-wiring defect -- squarely outside B-N8-CI-GATES' brief, and the file's own author correctly declined to guess at it rather than silently repointing globs without owning-layer context. ROUTED AS RESIDUAL: repoint G3 -> current chart_facts TypeScript test location, G12 -> orchestrator-native equivalent of the retired build_chart.py, to whichever lane/session next touches L1 test hygiene or hard_gates_check.sh itself. Not blocking, not urgent (script is operator-run-by-hand only).
RATIONALE: Both questions were answerable by direct measurement rather than judgment call -- Q1 dissolves on re-run (the feared coupling isn't live), Q2 resolves by the disposition doc's own already-stated reasoning being sound and simply needing ratification plus explicit residual routing so it doesn't silently vanish. Consistent with Ruling 50/57's standing pattern: verify before ruling, and an honest gap gets a name and an owner rather than a guess.
REVERSIBILITY: Q1 is a no-op (nothing changed). Q2's residual is a future glob-pattern edit, trivially reversible, owner TBD.
ACTION FOR CONDUCTOR: mark B-N8-CI-GATES reopen-cycle-2 questions CLOSED; lane's PR #913 is unblocked on these two points -- clear for merge-queue consideration alongside its already-reported CONFIRMED items (Stage 5 live-green, F-27 regression tests fail-then-pass). Log G3/G12 repoint as a residual in the close report's residual-disposition record (ONGOING_HYGIENE_POLICIES §applicable), not a new lane.

## RULING 60 · A2-CI-POINTERS merge questions (auto-merge authorized; clause (ii) confirmed NOT superseded; RC-14 drift routed as residual)

EVIDENCE: A2 merged clean -- ea6497ff, CI green on the rebased tree, 216/43 held exactly, TAP CI (SC-17/18/19, failing on main for 4 consecutive commits) confirmed fixed live. INT landed on retry 2 of 3 only because both blocking SHAD-DARSANA PRs happened to be simultaneously stuck -- explicitly flagged as luck, not design. Separately, INT observed an untracked `dispatch_shaddarshana_c2_gochara_resume_1c826d5a.py` appear in the SHARED checkout (not a SHAD-DARSANA worktree) during the merge window, then reported doing all its own work in a throwaway worktree regardless, so the shared checkout was never itself written to by INT.

DECISION Q1 (cadence): (a) AUTHORIZED -- enable GitHub native auto-merge (squash) + auto-update-branch on every SAMAPTI merge-lock PR from here forward. This is mechanical, reversible per-PR, requires no coordination channel with SHAD-DARSANA's conductor (Ruling 46 already established there isn't one), and mirrors what SHAD-DARSANA's own PRs already do (#916 et al. use auto-merge squash) -- so it's matching the other swarm's own idiom, not inventing a new one. Ruling 46 condition 4's bounded-retry-then-escalate stays as the backstop for genuine incompatibility; auto-merge should reduce how often that backstop gets hit, not replace it as the safety net.

DECISION Q2 (clause (ii)): NOT superseded -- confirmed on the record. Ruling 46 read clause (ii) as "about OUR local tree, unaffected by SHAD-DARSANA working in ITS OWN worktrees" -- this incident is a DIFFERENT shape: a SHAD-DARSANA-originated script briefly touched the SHARED checkout itself, which is exactly the 2026-07-29 hazard clause (ii) exists to guard against. It caused no harm here ONLY because INT had already, on its own initiative, isolated all its work into a throwaway worktree -- not because the hazard was moot. STANDING PRACTICE, effective immediately: every future INT merge action runs in an isolated throwaway worktree as a STRUCTURAL requirement, not best-effort discipline -- makes clause (ii) compliance true by construction regardless of what any other campaign does to the shared checkout, rather than something to re-verify was gotten away with after the fact.

DECISION Q3 (RC-14 static-model drift from SHAD-DARSANA's kala_views additions): routed, not ruled -- same tool-count-reconciliation territory Ruling 51 already assigned to B-MCP-CATALOG-GAP ("re-source from the ACTUAL served catalog, since every generated figure in this codebase has drifted"). No new lane; flagged there as an additional pressure on why re-sourcing from the live catalog beats maintaining a static model. Not blocking any current merge.

DECISION Q4: acknowledged, no ruling needed -- both items (307 auth-edge redirect, decoupled `tools:88` field) are informational, already understood, unchanged by this merge.

RATIONALE: Q1/Q2 both resolve in the direction of "make the safe behavior structural" rather than "trust it was safe this time" -- auto-merge removes the human-timing luck from Q1's near-miss, and mandatory worktree isolation removes the "did another campaign touch our tree" question from Q2 by making it not matter. Consistent with Ruling 46's own approach (rebase-and-recheck over waiting-for-silence).
REVERSIBILITY: Both trivial. Auto-merge is a per-PR GitHub setting, toggleable anytime. Worktree-isolation is a process convention for INT, not a code change.
ACTION FOR CONDUCTOR: instruct INT to (1) enable auto-merge+auto-update-branch on the next SAMAPTI merge-lock PR onward, (2) always stage merge work in a throwaway worktree, never the shared checkout, going forward -- restate as standing practice in the close report alongside Ruling 56's report-handback correction.

## RULING 61 · Ruling 60 Q1 correction -- auto-update-branch declined, INT's own rebase discipline already sufficient

EVIDENCE: INT correctly found `allow_update_branch: false` repo-wide and correctly DECLINED to flip it unilaterally, naming it a cross-campaign setting affecting SHAD-DARSANA (not-owned, §7) rather than acting -- exactly the right instinct. Its evidence: PR #918 (SHAD-DARSANA's OWN PR) sat auto-merge-enabled-but-stuck for 75 minutes under `strict: true` with nothing refreshing it. On closer read, that evidence proves something narrower than INT concluded: #918 got stuck because SHAD-DARSANA's own process has no active rebase step -- it relies on GitHub auto-merge alone. INT's process is NOT that process. Ruling 46 conditions 2-3 already require INT to actively rebase onto current tip, re-verify CI on the REBASED tree, and re-check tip stability immediately before merge -- which is exactly what got A2 landed (3 rebases, zero starvation, retry 2 of 3). #918's starvation is evidence that PASSIVE auto-merge-without-rebase fails on this repo; it is not evidence that INT's ACTIVE rebase-and-recheck loop fails, because that loop was never relying on the passive mechanism in the first place.
DECISION: Option 1 only (auto-merge as pure convenience, already directed in Ruling 60 -- unchanged). Options 2 (`allow_update_branch` repo-wide) and 3 (GitHub merge queue) DECLINED, both for the same reason INT itself flagged: repo-wide branch-protection changes have blast radius on SHAD-DARSANA, an active not-owned campaign, with no channel to coordinate the change or absorb its effect on their cadence -- exactly the shape Ruling 28 named as a "mechanical toggle that's actually a decision." Neither is needed: Ruling 46's active rebase-recheck-retry discipline is the actual load-bearing mechanism, already proven on A2, and does not depend on any repo setting. Auto-merge remains authorized as a convenience that may save INT a poll cycle when timing is favorable, but the bounded-retry backstop (Ruling 46 condition 4) stays the real safety net for genuine incompatibility, exactly as already ruled -- nothing about #918's evidence changes that.
RATIONALE: The instinct to flag a repo-wide setting rather than flip it was correct and is itself worth ratifying as standing practice -- but the conclusion drawn from the evidence conflated "this repo's passive automation is insufficient" with "our active process needs the same automation," which are different claims. INT already built and proved the thing #918 was missing.
REVERSIBILITY: No setting changed, nothing to revert. Purely a clarification of which mechanism to trust.
ACTION FOR CONDUCTOR: no new INT action -- continue Ruling 46's rebase-recheck-retry discipline as primary, `--squash --auto` as directed convenience, mandatory worktree isolation per Ruling 60 Q2. Close report should note #918 as external evidence supporting why SAMAPTI's more effortful active-rebase protocol was the right design, not as an unresolved gap in it.

## RULING 62 · VER's halt-worthy A6/C2 build finding -- NOT unauthorized, NOT out-of-order; A6 CONFIRMED with timestamped snapshot caveat

EVIDENCE: VER reported a live `building` state on `(1c826d5a, ka_gochara_sweep)`, substeps moved 78->132 (+54) DURING this run under the SAME build_fingerprint (a resume, not a rebuild), and asked DVA to confirm whether this is C2 dispatched out of dependency order (A6 unverified, C1-REBUILD not yet run) or an unauthorized build. DVA verified directly, not by inference: (1) `platform/scripts/dispatch_shaddarshana_c2_gochara_resume_1c826d5a.py` exists on disk, untracked (matches INT's Ruling-60 observation of an untracked file appearing in the shared checkout). (2) `SHAD_DARSHANA_STATE.md:111-135` independently confirms this exact script as a "(non-lane) action: `1c826d5a` gochara-sweep horizon rebuild, dispatch 1 of ~3, IN PROGRESS" and explicitly states "diagnosis on `samapti/gochara-parity` @ `d5907e64` ... **its diagnosis was reused here, not duplicated**" -- i.e. this dispatch runs directly off A6-GOCHARA-DIAG's own branch. (3) `SAMAPTI_TICK_LEDGER.md:27` confirms A6-GOCHARA-DIAG is that exact branch/PR (#900) and that it routed "4 QUESTIONS ... (C2 proceed?, BUILD-LOCK duration for C2, F5 fix timing, F3 vs C3's EP-1)". (4) Those 4 questions are NOT unanswered -- they are RULINGS 7, 8, 9, 10 (lines 61-95 of this ledger), already recorded: Ruling 7 authorized C2 to proceed with a disjunctive stop condition (PARK-HONEST iff <40 new substeps in a dispatch, OR a non-budget failure signature); Ruling 8 explicitly authorized PER-DISPATCH BUILD-LOCK (not one ~34h hold), with `1c826d5a` prioritized FIRST as the named operator-E2E-gate chart -- exactly the dispatch now running.
DECISION: This is the DVA-authorized C2 action proceeding exactly as Ruling 8 specified (dispatch 1 of ~3, 1c826d5a first). It is NOT out-of-order (A6 already produced its diagnosis and that diagnosis is precisely what C2 consumes -- A6 does not need a VER verdict to be CONSUMED, only to be CONFIRMED as a register entry; Ruling 7 already did DVA's own authorization review of the diagnosis before greenlighting C2) and NOT unauthorized (Rulings 7-8 are the authorization). No BUILD-LOCK violation: Ruling 8 anticipated exactly this multi-dispatch shape. **This dispatch's own numbers prove Ruling 7's stop condition is satisfied, not tripped**: +54 substeps this dispatch is comfortably above the 40-substep PARK floor -- meaning this is evidence the mechanism is HEALTHY, is CORROBORATING A6's diagnosis live, not contradicting it.
A6 VERDICT DIRECTED: CONFIRMED. The mechanism (writer_timeout_seconds=21600, median substep cost, resume-via-fingerprint) is independently re-derived and stable per VER's own measurements. The per-chart snapshot numbers (78/303 at time of A6's writing) are not the load-bearing claim -- the MECHANISM is, and a live resume adding 54 substeps under the same fingerprint is direct, currently-unfolding proof the mechanism VER is trying to hold still actually works exactly as diagnosed. Register A6 as CONFIRMED with the per-chart table captioned "point-in-time snapshot, measured <A6's write timestamp>; per Ruling 8, expect further movement across ~3 dispatches" -- this is a moving target BY DESIGN (Ruling 8), not an evidentiary defect, and dating the snapshot is the correct fix, not blocking the verdict on the target going still.
STANDING GUIDANCE FOR THE REMAINDER OF C2: no SAMAPTI lane may dispatch a build against `(1c826d5a, ka_gochara_sweep)` or `(cb73cd3d, ka_gochara_sweep)` concurrently with this in-flight dispatch (write-write collision risk on `build_substep_progress`) -- checked: none of the currently-dispatched agents (Crown Proof, deploy-audit, halt-log, Track 0/5, Track 4, small-leftovers) touch this asset/chart pair. C1-REBUILD, when it eventually runs, must check `asset_throughput` state for both charts before acquiring BUILD-LOCK, per Ruling 8's own sequencing (cb73cd3d proceeds only in idle windows).
RATIONALE: VER did exactly right to halt and ask rather than assume -- a moving load-bearing number under a live write IS worth stopping for, and the finding surfaced that A6's own routed questions had in fact already been answered (Rulings 7-10) but that resolution wasn't visible from inside a verification pass focused on re-deriving evidence, not re-reading the ruling log. The fix is procedural: VER should cross-check the ledger for existing rulings on a lane's OWN routed questions before treating their absence as still-open.
REVERSIBILITY: No action taken beyond the verdict; the dispatch continues exactly as already authorized. Trivial.
ACTION FOR CONDUCTOR: relay to VER -- CONFIRM A6 now with the snapshot caveat, resume the rest of its priority-1 queue (A4, A7, A8) unblocked, this was never a blocker for those.

## RULING 63 · PARK_PB-3_L-5 memo, §5 items 2-4 (the remainder Ruling 55 routed)

EVIDENCE: memo at `00_ARCHITECTURE/briefs/pariprashna_build/PARK_PB-3_L-5_MIMAMSA_CALIBRATION_WRITE.md` (on origin/main, commit `5f65292a`, PR #874). §5 items 2-4, relayed verbatim by the small-leftovers agent: (2) exact target name/column set for Option B; (3) confirm the write stays COLLECT-ONLY (W-3) -- no priors bump, no serving annotation reads this row, enforced by a grep/runtime guard per "Lane L-6"; (4) confirm `mimamsa_predictions`, `brahma_prospective_ledger`, `phala_anchors` stay untouched. Memo's own §4 spec for item 2: table `mimamsa_conversational_calibration` (name already fixed by Ruling 55 itself) with columns `chart_id`, `source_citation` (described as "= brahma_mimamsa_prediction_ledger.id, the ledger IS the citation"), `prediction_ledger_row_id` (FK to the same table's id), `domain`, `confidence_point`, `outcome`, `outcome_value`, `brier`, `brier_excluded`, `scored_at`, `UNIQUE(prediction_ledger_row_id)`.
DECISION item 2: column set CONFIRMED AS PROPOSED, with one implementer's note (not a blocking change): `source_citation` and `prediction_ledger_row_id` appear to store an IDENTICAL value (both reference the same ledger row's id) -- if that's literally true at write time, keep only `prediction_ledger_row_id` as both the FK and the citation, dropping `source_citation` as redundant. If `source_citation` is meant to diverge from `prediction_ledger_row_id` in some case not stated in the memo (e.g. a future multi-source citation), keep both but the memo should say so. Whoever implements this checks which is true before writing the migration; not worth a re-ruling either way, additive-only per Ruling 55 makes this reversible regardless.
DECISION item 3: CONFIRMED, COLLECT-ONLY stands, and per this campaign's own standing distrust of documentation-only promises (§N.8 pattern, repeated across ~10 findings this run): the guard must be BOTH (a) a CI-enforced grep/static-analysis check that fails if any code path outside the write-path itself reads from `mimamsa_conversational_calibration` (not just a comment saying so), AND (b) a one-line doc comment on the table stating the constraint and citing this ruling. A comment alone is not a guard; a grep alone with no explanation is what confused the source_citation/DEFAULT_DURABLE_EVENT_TYPES-class naming issues elsewhere in this campaign. Whichever lane implements L-6 owns writing this check; VER can-fail proof required (plant a read from outside the write path, confirm it trips).
DECISION item 4: CONFIRMED. Zero changes to `mimamsa_predictions`, `brahma_prospective_ledger`, or `phala_anchors` authorized under this park. Any future lane proposing to touch any of the three returns to DVA for a fresh ruling -- this park's scope boundary does not extend by inference.
RATIONALE: All three items are low-risk, additive-only, already bounded by Ruling 55's conditions -- the only judgment call worth recording is item 3's "guard means enforced, not documented," consistent with how this run has treated every other soft-promise-as-guard finding.
REVERSIBILITY: Trivial -- this rules on a not-yet-implemented spec, nothing exists yet to revert.
ACTION FOR CONDUCTOR: memo's §5 is now fully ruled (items 1 via Ruling 55, items 2-4 via this ruling). Whichever lane eventually implements PB-3 L-5 (not yet dispatched this campaign) proceeds directly from these two rulings without further park.

## RULING 64 · INT's two spot-check findings -- SIGABRT crash logged as observation (not SAMAPTI's), MCP credential-in-logs is a NEW incident, mitigation authorized (not rotation)

EVIDENCE, FINDING A (production SIGABRT): INT found a real 22-request 5xx window on `amjis-mcp` revision `00521-ldr` @ `8f896784` -- signal 6 (SIGABRT), auto-restarted in ~1s, now fully recovered (8/8 probes 200). Confirmed NOT attributable to A2: A2's own revision `00519-c5l`'s entire service life (05:28-07:16Z) carries zero 5xx; the crash is on a LATER ṢAḌ-DARŚANA revision. INT's hypothesis (V8 heap exhaustion at 512Mi, unproven, disclosed as such) is plausible but not asserted as fact.
DECISION A: logged as observation only, does NOT hard-stop the SAMAPTI merge queue. Not our deploy, not our lane, already self-recovered, current health confirmed green. Record in the close report as a transient production incident observed during this pass, with INT's disclosed-unproven hypothesis attached -- native's call whether to investigate the not-owned campaign's memory ceiling further. No SAMAPTI action.

EVIDENCE, FINDING B (credential leaking into Cloud Logging): the MCP `?api_key=` URL-token fallback auth path puts the live bearer credential in the query string; Cloud Run logs `httpRequest.requestUrl` verbatim; `scripts/operator/mcp_end_to_end_smoke.sh` deliberately exercises this path on EVERY deploy ("PASS: URL-token fallback auth path works"), meaning a live `mcp_prod_…` key is written to production logs on every single deploy this campaign has watched happen, plus real client traffic using the same path. Same class as INC-1/2/3 (Rulings 1/18/36-40) -- live credential in a durable, broadly-readable store, active RECURRING mechanism (worse than INC-1/2/3's static-file exposure, since this one re-fires on a schedule). INT correctly did not rotate anything (Ruling 1's authority boundary) and disclosed its own process slip (the value appeared once in raw tool output before it started suppressing the field) rather than hiding it.
DECISION B: NEW incident, name it INC-4 for the close report's incident inventory, folded into the existing INC-1/2/3 runbook rather than a standalone document. Authorized NOW, mirroring Ruling 2's model (mitigate the recurrence mechanism, do not rotate -- rotation stays native's per Ruling 1): (1) a Cloud Logging exclusion/redaction filter on the `amjis-mcp` log sink so `requestUrl` query parameters matching `api_key=` are redacted before they're durably stored -- stops the log-store side of the leak immediately, does not require touching the credential itself; (2) `mcp_end_to_end_smoke.sh`'s URL-token probe either switches to a dedicated non-privileged throwaway test credential that authenticates NOTHING else, or the probe is changed to assert the auth PATH exists without actually round-tripping a real production key through a URL. NOT authorized: disabling the URL-token auth path itself (a design/product decision outside this campaign's scope), rotating the already-logged canary key (native/credential-authority action, name as a P0/P1 item in the close report alongside the existing INC-1/2/3 rotation runbook, do not execute).
NEW LANE: B-MCP-LOG-REDACT (Sonnet, medium, no rebuild, depends_on: []) -- scope is exactly decision B's two items, read-mostly with one small config change (log sink exclusion filter) and one small script change (smoke test credential). VER can-fail proof required: confirm a synthetic `?api_key=` request's URL is redacted in the resulting log entry after the fix, present before.
RATIONALE: A credential that re-leaks on every deploy is a live, active, recurring exposure -- worse in kind than the static files Rulings 1/2/3 addressed, and it was found only because INT was doing a routine ledger spot-check, not because anything was watching for it. Consistent with this run's whole posture: mitigate the recurrence mechanism immediately within the swarm's authority, name the rotation as the native's P0 item, never silently sit on a live finding.
REVERSIBILITY: Log exclusion filter and smoke-script credential swap are both trivial, single-commit revertible. Not rotating anything means nothing irreversible happens under this ruling.
ACTION FOR CONDUCTOR: dispatch B-MCP-LOG-REDACT immediately (does not depend on merge-queue state, can run in parallel with everything else); add INC-4 to the close report's incident table alongside INC-1/2/3, explicitly flagging that the canary key used in the URL-token path (and any real client key that has ever used it) should be treated as compromised-by-logging and added to the native's rotation list, same P0 framing as Ruling 1.

## RULING 65 · A4 and A7 CONFIRMED (VER); A7's register corrected in place; merge-queue priority set to A1 next

EVIDENCE: VER independently re-derived and CONFIRMED both A4-LOOP-G1 (mount is real, FK trap is real, can-fail mutation B reddens the detector, mutation A exposed a real but non-fatal weakness in the detector's own claim) and A7-N8-AUDIT (40 findings, 12 adversarially spot-refuted, 11 survived intact, 1 partially -- F-27's "two reasons" overclaim). VER required 4 citation corrections in A7's register before the 8 downstream lanes (6 B-NAR-*, B-N8-FIX/SWEEPFIX/LINT) consume it, plus two disambiguation asks (bare `envelope.ts` collides with a second unrelated file of the same name; bare `ga_dashas_writer.py` should carry its directory). DVA applied all of these directly rather than routing back to a builder for a citation-only fix: F-04's `:112`->`:111`; F-11's `:1620-1624`->`:1622-1625` (both occurrences, previously self-contradicting F-25/F-38's correct citation of the identical check); F-02's spliced ci.yml quote corrected to accurately represent two distinct remarks about two different test files, with the underlying finding independently re-verified to survive on its own (confirmed no `DATABASE_URL` is ever set in `ci.yml` before the sidecar test job runs); F-27 narrowed from "two independent reasons" to the one actually demonstrated. Pushed to `samapti/n8-audit` @ `495cba13`, register bumped to v1.1 with a changelog entry per B.8. No finding's substance or severity changed.
DECISION: A4 CONFIRMED, A7 CONFIRMED (register now v1.1, corrected). A4's flagged weakness (the `loop_entry_mounted.test.ts` static-source-text detector passes on a neutered-but-present call, only the real behavioral test `capture_turn_commit.db.test.ts` would catch it) is recorded as a residual for the close report, not reopened -- the lane is not defenceless (behavioral coverage exists), the detector's own claim just overreaches what it measures, same shape this whole campaign has been naming elsewhere. Not blocking.
MERGE-QUEUE PRIORITY, set now: **A1-PRESERVE is next**, ahead of A4/A7 even though both just confirmed. Two independent reasons converge: (1) A1 was already `blocking_for_run` from session start (nothing else was supposed to merge before it resolved the T0-baseline question); (2) VER's own new finding -- A7's register frontmatter declares `consumes: SAMAPTI_IMPLEMENTATION_BRIEF_v2_0.md §7` and `session_queue_SAMAPTI.yaml`, and NEITHER is on `origin/main` yet, only on A1's unmerged branch -- meaning anyone reviewing A7's PR #904, or any of the 8 dependent lanes checking it out, cannot currently read the brief that defines its own scope. Merging A1 first resolves both. A4 and A7 queue behind A1, in that order (A4 is P0-highest-leverage per its own tick-ledger framing; A7 unblocks 8 further lanes but ships no code itself, lower merge urgency than actually landing something P0).
STANDING NOTE FOR WHOEVER SEQUENCES B-NAR-*/B-N8-* NEXT: per VER, treat A7's register line numbers as base-relative to `cdb6fc3b`, NOT current main. Two PRs, both still OPEN/unmerged as of this ruling (checked directly), will shift cited line numbers once they land: PR #929 (B-EVENTREG) shifts `asset_runner.py` by +6 lines (F-01's `:695-699`/F-05's `:707-719` become `:701-705`/`:713-725`), and PR #919 rewrites 252 lines of `ga_dashas_writer.py`, invalidating F-36/F-37's line numbers outright. Neither PR fixes those findings -- re-locate by symbol name, not by trusting the register's line numbers, once either has merged.
RATIONALE: Applying VER's citation corrections directly (rather than round-tripping through a builder agent for what is, definitionally, exactly the same class of fix DVA already applied to CLAUDE.md's own doctrine wording under Ruling 58) is proportionate -- these are citation-only edits with no substance change, re-verified independently before applying (the F-02 DATABASE_URL claim was checked, not assumed), and getting the 8 blocked lanes moving sooner has real value given how much of this run has been spent waiting on exactly this dependency chain. Re-sequencing the merge queue on a freshly-discovered cross-lane dependency (A7 needs A1's content to even be reviewable) is the same instinct Ruling 46/61 have applied throughout: fix the real blocking dependency, don't just process confirmations in the order they arrived.
REVERSIBILITY: Register corrections are a single revertible commit. Merge-queue re-sequencing is a priority-list edit, not a commitment -- reversible anytime before MERGE-LOCK is actually taken for the next lane.
ACTION FOR CONDUCTOR: relay to INT -- next MERGE-LOCK acquisition is for A1-PRESERVE (PR #896), not A4 or A7. A4 and A7 are both CONFIRMED and next-in-line after A1, in that order.

## RULING 66 · Two lanes park-candidated on missing builder summary; B-N8-CI-GATES status correction on the record

EVIDENCE: VER reports it has never received a FINAL_SUMMARY for **B-PB6-GEOMETRY** or **B-COCKPIT-INCOMPLETE** -- not lost, never sent. DVA independently searched `SAMAPTI_TICK_LEDGER.md` and `SAMAPTI_VERIFICATION_LEDGER.md` for both and found nothing beyond VER's own pre-ruling on B-PB6-GEOMETRY's deployed-route question and DVA's own Ruling 47 on B-COCKPIT-INCOMPLETE's TERMINAL-set finding -- neither is a builder claim-list. Separately: DVA had told the operator earlier this pass that B-N8-CI-GATES was "CLOSED," based on the builder's own self-reported live-green CI run (30514027837) plus Ruling 59's scope answers -- but VER's own queue tracker shows it recorded as **still REFUTED**, meaning VER never independently verified the Stage-5-fixed claim. That was a DVA error: a builder's self-report plus a scope ruling is not the same as VER confirmation, and this campaign does not get to skip that step for its own convenience.
DECISION: (1) B-PB6-GEOMETRY and B-COCKPIT-INCOMPLETE formally recorded as **PARK-CANDIDATE, blocked-on-missing-builder-summary** -- not silently dropped, not fabricated a summary to unblock. Whoever next has capacity checks whether either builder agent/session is still resumable; if genuinely gone, these become PARKED-HONEST in the close report with "builder summary never produced, VER cannot verify a claim it never received" as the named blocker -- an honest gap, not a fabricated pass. (2) B-N8-CI-GATES status CORRECTED: NOT closed until VER independently verifies the Stage 5 Firebase-env fix live. Retracting the earlier "CLOSED" characterization. VER is now checking this directly.
RATIONALE: The PERFECT CLOSURE directive is explicit that nothing gets called done without live proof -- catching my own premature "CLOSED" claim before it propagated into the close report is exactly the discipline the whole campaign has been enforcing on every lane; DVA is not exempt from the standard DVA has been holding everyone else to.
REVERSIBILITY: Trivial -- status corrections, no code/data change.
ACTION FOR CONDUCTOR: do not report B-N8-CI-GATES as closed anywhere until VER's verdict lands. Track B-PB6-GEOMETRY/B-COCKPIT-INCOMPLETE as open park-candidates in the close report prep, not as silently-resolved.

## RULING 66 ADDENDUM · B-N8-CI-GATES now genuinely CONFIRMED by VER, with an honest-but-vacuous residual

EVIDENCE: VER independently checked at current head `a852d729` (not the builder's cited, now-superseded `3ecbbf7c`): Stage 5's env block carries all six `NEXT_PUBLIC_FIREBASE_*` vars, `gh pr checks 913` shows Stage 5 PASS, and the swallow is replaced by a genuine `exit 1` can-fail path if no `@visual` spec is found. The refutation that held this REFUTED is discharged. NEW RESIDUAL VER surfaced: the one `@visual` spec (`axe.spec.ts:79`) is gated on `MARSYS_SUPER_ADMIN_SESSION`, which Stage 5 never sets and which is not a repo secret at all -- so the spec always skips, Stage 5 goes green having asserted nothing. VER declined to hold the lane on this (never claimed visual regression was verified, only that the swallow was fixed; the skip predates this lane; a real failure path now exists where none did) but flagged it as the same shape this campaign exists to eliminate.
DECISION: B-N8-CI-GATES CONFIRMED. Record the vacuous-Stage-5 finding as a residual for the close report, not a blocker and not a new lane tonight -- closing it needs either a CI-satisfiable session secret or a `@visual` spec that doesn't depend on one, real scoping work for a future session. Retracts the earlier premature "CLOSED" and replaces it with the real thing: independently checked, at current head, confirmed.
RATIONALE: This is the PERFECT CLOSURE directive working exactly as intended -- a self-report was wrong to trust blindly, VER checked, found both the fix and a residual the self-report didn't mention, and the lane closes on real evidence instead of an assertion.
REVERSIBILITY: N/A, verdict only.
ACTION FOR CONDUCTOR: B-N8-CI-GATES is genuinely closed now, cleared for merge-queue consideration behind A1/A4/A7. Log the vacuous-Stage-5 residual in the close report.

## RULING 67 · B-SECRET-ROTATE-PREP CONFIRMED (most consequential lane VER has verified); stays OPEN-UNREMEDIATED in the close report; one process incident disclosed and handled

EVIDENCE: VER independently re-derived the severity-defining claim straight from production `pg_roles` (both incident principals hold `rolcreaterole=t`/`rolsuper=f`, exactly the shape the lane claims), confirmed the 30-role sweep denominator, confirmed the 27-file/9-dead-plus-18-live arithmetic, reproduced `secret_scan.sh` returning PASS/exit=0 while silently degrading to a weaker bash-regex engine when `gitleaks` is absent, and confirmed via sha256-derived (never-printed) full-value AND prefix grep that the 1391-line document itself leaks none of the three credentials it documents.
**Process incident, self-disclosed by VER**: while demonstrating the scanner's pattern-shape gap, VER's own DSN-shaped redaction filter missed an `os.environ.get("KEY", "<literal>")`-shaped line and a live credential value landed in VER's own tool-call transcript. VER disclosed this immediately and unprompted, same standard this campaign has held every builder to (Ruling 56's precedent: disclosure over silent fix). DVA has NOT reproduced or repeated the value here or anywhere in this ledger.
DECISION on the incident: no new incident number, no new rotation urgency -- the value is already public on `origin/main` (part of INC-1/2/3's existing footprint), so this is an additional OCCURRENCE of already-known exposure, not a new exposure. It independently and concretely proves the lane's own claim that widening `secret_scan.sh`'s scope alone won't catch the pattern-shape class -- ironic and useful confirmation, not a new problem. No cleanup action required of the task-output transcript: once the eventual native-authorized rotation invalidates the old value, its presence anywhere (including this transcript) becomes moot; until then, DVA/Conductor will not further quote, repeat, or propagate it.
DECISION on the lane: CONFIRMED. Per VER and the lane's own framing, this MUST be recorded in the close report as an **OPEN, UNREMEDIATED security exposure** -- documentation/preparation only, nothing rotated, the two live credentials remain public and live on main until B-SECRET-REDACT merges and the native executes an actual rotation. Not a closed item under any framing.
TWO FRAMING REQUIREMENTS FOR E1-SAMGATI'S EVENTUAL COMPROMISE-ASSESSMENT LANGUAGE: (1) state the IAM-reachability finding (~10 identities can reach the instance via `roles/cloudsql.client`/`roles/editor`) ALONGSIDE the network finding (B-AUTHNET-HISTORY's authorizedNetworks-never-held-a-value) -- never the network finding alone, since that closes only one of two gates; (2) the compromise conclusion is worded "no evidence of misuse in the window we can see," never "not compromised" -- carry this exact phrasing forward, do not let it drift to a stronger claim in the close report.
ROUTING: `shad-darshana-circularity-guard.yml`'s hardcoded production credential is a §7 not-owned surface (ṢAḌ-DARŚANA's), correctly read-only-reported by the lane, not touched. Named in the close report as a cross-campaign finding with no direct channel to hand it off through (same standing gap as every other not-owned-campaign finding this run).
RATIONALE: This is the highest-stakes verification this campaign has run, and VER treated it accordingly -- re-deriving the severity claim from the actual role table rather than trusting the document's own analysis, and disclosing its own mistake in the same breath as confirming everything else. That is exactly the discipline the PERFECT CLOSURE directive asks for, including from the verifier itself.
REVERSIBILITY: N/A, verdict + framing requirements only. No code/data/credential change under this ruling.
ACTION FOR CONDUCTOR: close report's incident table must show B-SECRET-ROTATE-PREP's rotation as the P0 item it has been since Ruling 1, now with the CREATEROLE persistence caveat attached -- rotation is necessary but insufficient alone, per this lane's own central finding.

## RULING 68 · B-HALT-LOG-ROOTCAUSE REFUTED (cut from shared checkout, not origin/main); systemic audit CLEAN elsewhere; fix routed

EVIDENCE: VER found the builder's branch (`samapti/halt-log-root-cause`) was cut from the SHARED CHECKOUT while it locally sat on `parishodhana/dark-corpus-remeasure @ f91ec00b`, not from `origin/main` as manual §9 requires. Consequence: a 3-dot diff of 42 files / +65,130/-56, of which only 5 files (+134 total) are the lane's own genuine fix; the other 37 are PARIŚODHANA's unpushed work, including two §7 NOT-OWNED paths by name and four ~12,000-line dark-corpus remeasure JSONs -- exactly the content A1-PRESERVE was chartered to preserve-not-merge, and which DVA had already separately verified onto `preserve/parishodhana-dark-corpus-remeasure-20260730` earlier this run specifically so it would never take this path. Root cause is mechanical (branch cut from the wrong base), not malicious, and the underlying 5-file fix itself is sound per VER's read (unchanged from the earlier report: 4 duplicate `_write_halt_log()` implementations gain an env-var override, new autouse `conftest.py` fixture, correctly covers the 2 previously-untested implementations too).
DECISION on the lane: REFUTED, not discarded. Fix required before re-verification: re-cut the branch from CURRENT `origin/main`, cherry-pick/replay ONLY the 5 files (`conftest.py`, `ga_panchanga_writer.py`, `ga_positions_writer.py`, `ga_sade_sati_writer.py`, `ga_structural_writer.py`), force-push the corrected branch (feature branch only, `--force-with-lease`, never main), and confirm via `git diff <new-base>...HEAD --stat` shows exactly those 5 files and nothing else before asking VER to re-verify. VER's own before/after proof and commit-audit claim were correctly NOT re-validated by VER against an unmergeable branch shape -- no point verifying behavior on a branch that cannot land as-is.
DECISION on systemic risk: VER asked DVA to check every other open lane for the same defect (`git merge-base origin/main <branch>`, anything resolving to a pre-campaign commit is suspect). DVA ran this across all 30 open `samapti/*`/`preserve/*` branches directly. RESULT: CLEAN. Every other lane merge-bases at a legitimate T0-or-later origin/main point (`cdb6fc3b`, `5f5033a5`, `d7bc0a9a`, `2aaf35c2`, or later). The only two branches resolving to the anomalous old `a3b5ffb9` are the defective lane itself and `preserve/parishodhana-dark-corpus-remeasure-20260730` -- which is EXPECTED, being the sanctioned preserve branch built specifically to hold that content. This is isolated to one lane, not a campaign-wide pattern.
RATIONALE: VER's instinct to ask "is this isolated or systemic" before either lane-fixing or declaring victory is exactly right, and the answer -- checked, not assumed -- matters: a systemic finding would have meant re-auditing 30 branches' diffs before ANY further merge, a real queue-halting event; an isolated one means one lane gets fixed and the queue proceeds. Distinguishing the two is worth the five minutes it took.
REVERSIBILITY: Re-cutting a not-yet-merged feature branch is trivial and fully reversible. No main-branch action taken or needed.
ACTION FOR CONDUCTOR: dispatch the re-cut fix now. Merge queue is UNAFFECTED and proceeds as already sequenced (A1, A4, A7, B-N8-CI-GATES) -- this finding does not block any of those, all four independently confirmed clean by this same audit.

## RULING 69 · New lane B-MCP-PROMPT-REDACT (live credential found incidentally, redacted directly); self-correction on worktree discipline

EVIDENCE: B-MCP-LOG-REDACT (INC-4 lane) flagged, but correctly did not touch, a live `mcp_prod_...` API key committed in plaintext in `00_ARCHITECTURE/CONDUCTOR/MCP_DEPLOY_AND_VERIFY_PROMPT_v1_0.md:22` since 2026-05-22 (~69 days public). DVA confirmed the single occurrence and its first-commit date directly. **Self-correction, recorded rather than quietly fixed**: DVA's first redaction attempt edited this file directly in the shared checkout (the exact discipline violation Ruling 60/61 mandate against and that VER has been holding every builder to) -- caught before committing, diff saved, shared checkout reverted clean, then redone properly: fresh worktree cut directly from `origin/main` (not the shared checkout -- the identical lesson Ruling 68 just produced for B-HALT-LOG-ROOTCAUSE, applied immediately rather than repeated), redaction re-applied via pattern-based `sed` so the credential literal was never typed into any tool call or message, committed, pushed to `samapti/mcp-prompt-redact`, PR #937 opened, worktree removed.
DECISION: redaction authorized and applied (Ruling 2's model: instructional indirection pointing at Secret Manager, not a placeholder that silently breaks the runbook; no rotation, same as every other INC this run). Recorded as a new minor lane, **B-MCP-PROMPT-REDACT**, PR #937, not folded into B-MCP-LOG-REDACT's scope (that lane's authorization was two specific items, this is a third, unrelated file). Not gated behind VER verification given the trivial, mechanical, single-line nature of the change and the value of closing a live public exposure quickly -- but logged here for the close report's incident inventory same as every other credential finding, and VER may spot-check at its discretion.
RATIONALE: DVA is not exempt from the discipline it enforces on every builder -- catching my own worktree-isolation lapse before it became a commit, and reverting cleanly rather than papering over it, is the same standard Ruling 66 already applied to my own premature "CLOSED" claim. Worth naming explicitly rather than silently fixing and moving on, consistent with this entire campaign's practice of disclosure over quiet correction.
REVERSIBILITY: Trivial, single-line, single-commit, not-yet-merged PR.
ACTION FOR CONDUCTOR: log B-MCP-PROMPT-REDACT (PR #937) in the close report's incident table as an additional occurrence of the ongoing credential-hygiene sweep, not a new numbered INC (same underlying credential family, found opportunistically not systematically).

## RULING 70 · Live migration-484 duplicate on main (ṢAḌ-DARŚANA, not-owned); allocation + B-MIGGUARD baseline handling

EVIDENCE: VER found `main` (now at `d5c4b359`, having moved 3 times -- `d7bc0a9a`→`8f896784`→`d5c4b359` -- during one check) carries TWO distinct migration files both numbered 484 (`484_bg_muhurta_lattice.sql` and `484_bg_synthetic_cohort_md.sql`, both ṢAḌ-DARŚANA, both landed today, different tables, not a rename). Corrected high-water mark under the existing max()-across-both-directories rule (Ruling 46 condition 5): `platform/migrations` highest=474, `platform/supabase/migrations` highest=485, next free = **486**, not the 475 the original brief assumed. A4's existing 474 cross-directory collision (already tracked since the earlier "CONFIRMED: live migration-474 collision" tick-ledger entry) is unaffected in kind but its target number moves. B-PB8-BYTEEQ's claimed 475 is free today but sits below the 485 high-water mark. B-EVENTREG's placeholder-numbering approach is unaffected and remains correctly deferred.
DECISION on allocation: A4 receives **486**, B-PB8-BYTEEQ receives **487**, allocated TOGETHER at whichever's merge-lock comes first (so they cannot race each other), and -- per Ruling 46's own standing rule, restated because it bears repeating given how fast main is moving -- RECOMPUTED FRESH at the actual moment either lane's MERGE-LOCK is acquired, not reserved now. This ruling sets the *expected* numbers for planning; the live number is whatever max()+1 resolves to when the lock is actually taken.
DECISION on B-MIGGUARD's guard dilemma: the guard's baseline (35 pre-existing duplicate groups, frozen by exact file-list, precisely so the baseline cannot be silently widened to smuggle a NEW collision through) now faces a 36th, freshly-shipped, NOT-OWNED duplicate. Neither of the builder's two options is right as posed: hard-failing B-MIGGUARD's own PR on a bug that belongs to ṢAḌ-DARŚANA blocks SAMĀPTI's own lane on someone else's defect; silently widening the baseline is exactly the erosion the frozen-baseline design exists to prevent. CORRECT THIRD OPTION, matching this campaign's own established pattern (`schema_validator`/`drift_detector`'s `known_residuals` whitelist mechanism -- exit-3-iff-itemized, never blanket): add the 484 duplicate to the baseline as ONE new, explicitly itemized, DATED, ATTRIBUTED entry (`484_bg_muhurta_lattice.sql` / `484_bg_synthetic_cohort_md.sql`, owner: ṢAḌ-DARŚANA, landed 2026-07-30, not fixed by SAMĀPTI, disclosed not hidden) -- not a silent widen, a named and dated one, same disclosure standard as every other not-owned finding this run (B-N8-CI-GATES's SC-17/18/19 attribution, the circularity-guard credential, etc.).
RATIONALE: This is the guard proving its own value a second time within the same campaign -- the collision class it exists to catch recurred DURING the campaign built to eliminate it, on a different campaign's own migrations. Treating it via the same itemized-disclosure mechanism this codebase already uses elsewhere (rather than inventing a new exception path) keeps B-MIGGUARD's own doctrine intact while not making SAMĀPTI responsible for fixing bugs it doesn't own.
MERGE-QUEUE IMPACT: none for A1 (next in sequence, touches no migrations, unaffected). A4's merge (queued after A1) now targets 486 instead of 475/476 -- update the tick-ledger note accordingly. INT should expect MORE rebase cycles than average while ṢAḌ-DARŚANA's cadence stays this fast -- this is Ruling 46's existing protocol working as designed against a busier-than-usual window, not a new precondition failure requiring a queue halt.
REVERSIBILITY: Allocation guidance is a planning number, recomputed live regardless. B-MIGGUARD's baseline addition is a single itemized-list entry, trivially revertible.
ACTION FOR CONDUCTOR: relay to B-MIGGUARD's fix agent (already dispatched) -- add the ONE new dated/attributed baseline entry for 484, do not silently widen scope beyond it. Relay to VER/INT: A4→486, B-PB8-BYTEEQ→487 as planning numbers, final allocation always fresh-computed at lock time.

## RULING 71 · A1-PRESERVE merged; three items INT surfaced -- panchanga bug fix authorized, orphan file deletion authorized, workflow_dispatch gap logged

EVIDENCE: A1-PRESERVE merged clean (`0d919718`), 19-file preservation proof verified at 3 checkpoints (identical blob SHAs pre-rebase/post-rebase/post-merge), full health verification, drift/schema at 216/45 exactly as Ruling 4 anticipated (the +2 are B-DOCS-GOVERNANCE's two itemized files, verified not a third). INT surfaced three items:
(1) **Pre-existing production defect, not caused by this merge** (`git diff` empty across the sidecar tree, bad call site dates to `8c9cd8d5`, 2026-05-20, ~71 days old): `routers/panchang.py:95` calls `compute_panchang(..., tz_offset_minutes=330)` against a signature that takes `tz_offset`, causing a `TypeError` that escapes the intended `except (ValidationError, OutOfRangeError, PanchangEngineError)` handler and surfaces as an unhandled 500. Measured: ~56% failure rate on `POST /api/compute/panchanga` over a 6h window (4 success / 5 failure). Panchanga underpins the FORENSIC anchors this whole project's grounding depends on.
(2) A `workflow_dispatch` manual deploy (actor `amonty84`, the native) ran entirely inside Ganga CI's window and completed ~2 minutes before CI's own conclusion, because `workflow_dispatch` bypasses both path-gating and the `conclusion == 'success'` gate. Benign this time (native's own action, deploy succeeded, health confirmed), but the same *mechanism* as the deploy-on-red hazard §4 routes to DVA.
(3) Ruling 5's orphan-file deletion trigger (`.fuse_hidden0000000500000001`) is now provably safe -- `git hash-object`-identical to the recovered draft now on `origin/main` -- exactly Ruling 5's stated reversibility condition, previously unmet.

DECISION (1): AUTHORIZED, new small lane **B-PANCHANGA-KWARG-FIX**, Sonnet/low, no rebuild dependency. Two-part scope: (a) the one-word kwarg fix itself (`tz_offset_minutes` → `tz_offset` at the call site, or widen the signature to accept both if a caller elsewhere legitimately uses the other name -- builder checks before assuming which side is wrong); (b) name, don't fix, the coverage gap that let a core endpoint fail 56% of the time for 71 days undetected -- record as a residual pointing at A7-class instrumentation work (a smoke/health-check gap, not this lane's to solve). VER can-fail proof required: confirm the exact `birth_date/lat/lon/tz_offset_minutes=330` request that currently 500s returns 200 after the fix, and that the fix doesn't break any other call site.
DECISION (2): logged as informational, NOT authorized to be hardened tonight -- naming the mechanism in the close report is sufficient for now (native's own deploy tooling, native's own action, no harm resulted). If a future session wants `workflow_dispatch` to also gate on CI status, that's an infra-hardening decision for the native directly, not a SAMĀPTI in-scope fix.
DECISION (3): AUTHORIZED. Ruling 5's condition is met -- INT or whoever next holds a lock may delete `00_ARCHITECTURE/briefs/pariprashna_build/.fuse_hidden0000000500000001` on the next lock acquisition, citing this ruling + Ruling 5 + the hash-identity proof.
A7 PRE-MERGE GUIDANCE (INT's own analysis, ratified not re-derived): when B-DOCS-GOVERNANCE's PR #928 is processed, its add/add conflict on 3 files against A1's now-merged content resolves by taking #928's version -- confirmed a strict superset (A1's exact bytes plus the ruled `artifact:` keys and the 152→124 correction). Resolving the other way would silently revert A1's preservation. Whoever handles that merge uses this as the resolution rule, no re-analysis needed.
RATIONALE: The panchanga bug is exactly the shape this whole campaign has been finding elsewhere -- a real defect hiding behind an exception handler that was supposed to catch it but didn't, undetected because nothing was watching the failure rate. Fixing the one-word bug is cheap and high-value; naming the detection gap rather than also building the detector tonight matches this campaign's established pattern of not letting a found defect balloon into unscoped work.
REVERSIBILITY: Panchanga fix trivial single-line, VER-gated. Orphan-file deletion trivial, hash-proven-safe. workflow_dispatch gap is a no-op decision, nothing to revert.
ACTION FOR CONDUCTOR: dispatch B-PANCHANGA-KWARG-FIX now. Instruct INT: delete the orphan file on next lock acquisition; proceed with A4 next per existing sequencing; apply the #928-supersedes-A1 resolution rule when B-DOCS-GOVERNANCE's turn comes.

## RULING 72 · Two legitimate merge blockers -- B-N8-CI-GATES waits on B-DOCS-GOVERNANCE; B-HALT-LOG-ROOTCAUSE's allowlist drift authorized for direct mechanical fix

EVIDENCE: INT correctly did NOT merge either lane despite one being merely UNSTABLE (non-required check) rather than BLOCKED, on the reasoning "a red I don't understand isn't a red I'll ship" -- exactly right.
(1) B-N8-CI-GATES (#913) introduces its own new CI ceiling (schema<=43 per Ruling 4 T0) and correctly trips on main's current 45 -- which is 45 not because of a regression but because A1-PRESERVE (already merged) committed Ruling 4's own two KNOWN-TRANSIENT files exactly as authorized, pending B-DOCS-GOVERNANCE (#928) adding their missing `artifact:` keys to bring the count back to 43. #913's gate is doing its job correctly; its authored constant is simply dated relative to a `main` that moved (predictably) after A1 landed.
(2) B-HALT-LOG-ROOTCAUSE (#935) trips a non-required "Fact-Category Pinning Gate (§5 C.7)" allowlist on 3 entries in `ga_sade_sati_writer.py` -- INT confirmed all 3 are the SAME pre-existing, already-justified violations, shifted by exactly +16 lines, matching the lane's own +16-line insertion above them. Zero new violations; the allowlist is keyed on line number and went stale as a direct, mechanical consequence of this lane's own edit.
DECISION (1): DO NOT insert #928 ahead of #913 yet. #928 (B-DOCS-GOVERNANCE) is one of VER's four sub-agents still in flight -- unconfirmed lanes do not jump the merge queue regardless of how much they'd unblock, same standard applied to everything else this run. Once VER confirms #928, it merges immediately ahead of #913 (schema 45->43), then #913 merges clean as authored. #913 stays parked, not force-merged and not weakened -- the tempting fix (bump its ceiling to 45) is explicitly REJECTED, exactly the erosion Ruling 4 exists to prevent and the failure mode this gate was built to catch on its own first live run.
DECISION (2): AUTHORIZED -- INT applies the mechanical 3-line allowlist coordinate bump (1349->1365, 1383->1399, 1535->1551 in whatever file holds the §5 C.7 allowlist) directly to the halt-log-root-cause branch before merging, justifications and code content unchanged, coordinates only. This is in-scope by Ruling 57's standing precedent ("fixing what your own change broke is in scope by default; only a BEHAVIORAL change to a sibling file needs a ruling") -- a pure line-number renumber caused mechanically by this lane's own edit is not a behavioral change to anyone else's content. SEPARATELY, name (do not build tonight) the durable fix as a residual: the allowlist should be re-keyed on content/pattern rather than line number, since it will keep breaking on any future edit above an entry -- #935 is merely the first lane unlucky enough to trip it. Route to a future hygiene lane, same disclosure-not-fix pattern as every other out-of-scope finding this run.
DECISION (3): CONTINUE to lanes 3-8 (#921, #939, #929, #936, #937, #907) now, in parallel with the above two resolving -- they are independent, no reason to idle the queue waiting on #928's verdict or a 3-line edit.
RATIONALE: Both blockers are gates correctly catching real (if harmless) state, not gates malfunctioning -- the discipline is to fix the actual cause (sequencing for #913, stale coordinates for #935) rather than weaken either gate to make the red go away, which is the single most repeated lesson of this entire campaign applied one more time to DVA's own merge-queue management.
REVERSIBILITY: Sequencing decision reversible anytime (queue order, not code). Allowlist coordinate bump trivial, single-commit, mechanical, verifiable by inspection.
ACTION FOR CONDUCTOR: relay to INT -- proceed with lanes 3-8 now; apply the 3-line allowlist fix to #935 directly and merge once green; hold #913 until #928 confirms via VER, then sequence #928 immediately ahead of it.

## RULING 73 · B-MIGGUARD's sha256 guard found 16 real historical mismatches on its first live run -- deploy pipeline HALTED for both campaigns; disclosed-residual allowlist authorized, blind reconciliation explicitly PARKED not attempted

EVIDENCE: #921 merged clean (CI green, 27 pass) at `81af26b7`, then the DEPLOY failed at the migration step -- `assertAppliedHashMatches()` (Ruling 58/67's own fix) correctly fail-closed on `0000_seed_legacy_applied.sql`, whose on-disk content no longer matches its recorded sha256. INT independently recomputed both hashes and confirmed the mismatch is real, then enumerated (read-only) all 349 `_migrations_applied` rows against `origin/main`: 326 match, **16 mismatch**, **7 applied-but-absent-from-disk** (a second, related but distinct finding the guard doesn't currently check). Production is confirmed NOT degraded -- fail-closed held, `Post-deploy smoke`/`Promote` both skipped, no partial migration, 9/9 health probes green on the previous (still-serving) revision. Consequence: production sits one commit behind main until resolved, and -- critically -- this blocks BOTH campaigns' deploy pipelines, since ṢAḌ-DARŚANA's next merge would hit the identical guard.
DECISION: Option 3, same disclosed-residual mechanism this exact lane already used for the 484 migration-number duplicate (Ruling 70), NOT option 1 (revert -- would neuter the guard for correctly catching real drift on its first live run, the precise anti-pattern this whole campaign exists to prevent) and NOT option 2 attempted now (blind reconciliation of 16 undocumented historical edits under deploy-pipeline pressure is exactly the rushed, high-blast-radius move this campaign has consistently declined elsewhere -- Ruling 48's yoga-catalog park is the direct precedent). AUTHORIZED NOW: freeze the 16 known mismatches as itemized, dated, attributed disclosed-residual entries (same `disclosed_additions`-class mechanism, extended to the migrate.ts guard) -- `fixed_by_samapti: false`, and where a common cause is determinable (the builder should check whether commit `751ce3b8`, "seed all 86 pre-tracking legacy migration files," explains some or all of the 16 -- a single shared root cause would make this ONE dated entry-class, not 16 independent mysteries) attribute to that commit rather than guessing per-file. Where no common cause is determinable, mark `cause: UNDER-INVESTIGATION` rather than fabricating one. The guard must still fail loudly on the stored hash if a DISCLOSED file's on-disk content changes AGAIN after this ruling (disclosure freezes today's known-mismatch state, not a standing permission to keep editing) -- can-fail proof required: (a) all 16 disclosed, deploy succeeds; (b) a 17th, undisclosed mismatch still fails the deploy; (c) editing one of the 16 AGAIN past its disclosed hash still fails (the disclosure pins one specific historical hash pair, not "this file is exempt forever").
PARKED, NOT ATTEMPTED TONIGHT: Option 2 (real reconciliation -- for each of the 16, determine whether the historical edit was benign, then either restore original content or bless the edit with documented rationale) and the 7-applied-but-missing finding. Both are genuine future lanes with real investigative work, not mechanical fixes -- named here with the evidence INT already gathered (the specific 16 filenames, the 349-row comparison) so the next session doesn't have to re-derive it, per this campaign's standing practice of costed parks over rushed action.
RATIONALE: The guard did exactly its job -- found a real, previously-invisible integrity gap in production's own migration history, on its very first live deploy. Reverting it would be the campaign undoing its own best-verified lane's core purpose under time pressure; blindly reconciling 16 unknown historical edits right now would be the exact rushed-high-blast-radius move this campaign has declined every other time it came up (Ruling 48, Ruling 65's caution on citation-only edits, Ruling 68's insistence on re-cutting rather than patching around contamination). The itemized-disclosure path is the one direction that unblocks BOTH campaigns' pipelines today without weakening the control or gambling on unreviewed historical SQL.
REVERSIBILITY: Disclosure entries trivially revertible/extendable. No production data touched -- migration content on disk and in `_migrations_applied` both remain exactly as found. The failed deploy attempted zero writes (fail-closed).
ACTION FOR CONDUCTOR: dispatch a builder for the disclosed-residual allowlist fix NOW -- this is deploy-pipeline-blocking for two campaigns, treat as elevated priority over the remaining lanes 4-8. INT resumes the merge queue once this lane's fix is confirmed and a real deploy succeeds end-to-end (not just CI green -- this incident is exactly why "CI green" and "deploy succeeded" are different claims). Option 2 and the 7-missing-files finding go in the close report as named, costed, PARKED-HONEST residuals.

## RULING 73 ADDENDUM · Disclosure list was incomplete (regex undercounted); 9 more mismatches identified, fix round 2 authorized

EVIDENCE: #941 merged (`3e05ef17`) but deploy failed AGAIN -- INT's own enumeration that fed #941's scope used a regex requiring exactly 64 hex characters, silently dropping 17 `_migrations_applied` rows that aren't SHA-256 at all (7 MD5-length values, plus free-text sentinels like `"manual"`, `"applied-2026-06-16"`). Corrected full enumeration (no length filter): 32 total mismatches, 7 not on disk (never reached by the runner, out of scope), 25 genuinely blocking, 16 already pinned by #941, **9 still unpinned**: `165_chart_panchanga.sql`, `216_chart_facts_partial_indexes.sql`, `232_drop_ganita_positions.sql`, `233_bg_throughput_dormant_to_lit.sql`, `234_activate_romanize_service_engines.sql`, `235_all_dormant_throughput_to_lit.sql`, `323_ga_structural_graph_theoretic_floor_update.sql`, `ws2_l0_reference.sql`, `ws2_l0_texts.sql`. Production remains healthy and unaffected (fail-closed held again, nothing promoted) -- now two merges behind main. INT self-reported the root cause plainly (a silent-drop parsing bug, same shape as an earlier `--stat` truncation error it also self-caught) rather than let a plausible-looking wrong number stand.
DECISION: authorize round 2 -- pin the remaining 9 using the SAME disclosed-residual mechanism (already VER-CONFIRMED as correctly two-sided/non-exemption-creating), via the verification query INT itself proposed (`SELECT filename, length(sha256), left(sha256,12) FROM _migrations_applied WHERE left(sha256,10) NOT IN (<every on-disk migration's sha256 prefix>)`) run fresh by the fix builder, not trusted from INT's list alone -- independently re-derive before pinning, same discipline as everywhere else this run. Builder also flags, for the future Option-2 reconciliation lane: 17 of the 32 total mismatches were never sha256 to begin with (MD5s, manual-apply sentinels) -- "restore to match the recorded hash" is definitionally impossible for those, they need a different eventual disposition (most likely: re-record with a real sha256 once someone confirms current content is correct, an operator action, not restoration) than the true sha256-drift files. Note this distinction in the disclosure entries' `cause` field so it isn't lost.
RATIONALE: The same standard applied to every builder tonight applies to INT's own enumeration -- a plausible number is exactly the one that escapes scrutiny, and the fix is cross-checking a count by a second method before building on it, not blaming the miss. INT volunteering "my error" and the shared-shape observation (two truncation/silent-drop bugs in one night) is exactly the disclosure practice this whole campaign has asked of every lane.
REVERSIBILITY: Same as round 1 -- disclosure entries trivially extendable/revertible, no production data touched, fail-closed held twice running.
ACTION FOR CONDUCTOR: dispatch immediately, same elevated priority as round 1. INT stays halted on lanes 4-8 until THIS round's real deploy succeeds -- two consecutive failed-deploy attempts on the same class means the resume bar (actual success, not CI green) stays non-negotiable.

## RULING 73 CLOSE · Deploy pipeline UNBLOCKED (deploy #3 succeeded); workflow_dispatch bypass now 3x, decision held unchanged; incident closed

EVIDENCE: Round 2 (PR #943) merged and deploy #3 succeeded end-to-end, verified by INT through the migration step itself (25 disclosed-skip lines, 0 applied, 0 throws) and the database (366 applied rows, migration 486's FK effect still live, unregressed). All three services (`amjis-web`, `amjis-mcp`, `amjis-sidecar`) now match `origin/main` exactly at `29d98dc6`. INT independently re-ran the completeness cross-check whose absence caused round 1's miss, by two methods, before merging -- three independent derivations (INT's, VER's, the fix builder's) now agree on the same 25. A third `workflow_dispatch` bypass occurred at 14:21 (succeeded this time, ~4 min ahead of CI completion) -- the reflex is now well-established across 3 occurrences (1 failed safely against fail-closed during the incident, 2 succeeded including this one).
DECISION: incident CLOSED. Production healthy, pipeline clear, backlog fully drained. workflow_dispatch decision from Ruling 71 HELD UNCHANGED -- still not hardening it tonight (native's own tooling, all 3 occurrences either benign or safely caught by fail-closed, no catastrophic outcome, and removing/gating manual-dispatch capability could interfere with legitimate emergency-deploy needs DVA has no visibility into) -- but the close report must now show the escalated frequency (3x, not 1x) plainly enough that the native can make an informed call, not bury it as a single footnote.
SURVIVING RESIDUALS FOR THE CLOSE REPORT (none resolved tonight, all correctly named rather than left to age out): (1) 4 UNDER-INVESTIGATION hash-disclosure entries (well-formed sha256, no matching git blob anywhere in history); (2) R5/Hazard 2 -- migrate.ts's tracker remains filename-keyed, a renumbered migration would still re-apply; (3) 7 applied-but-missing-from-disk rows, a class the guard doesn't check at all; (4) Option 2 (real reconciliation of all 25 disclosed mismatches, judging which historical edits were benign) remains fully parked, costed, not attempted; (5) the two-silent-drop-enumeration-bugs-in-one-night lesson (an unverified count is the one that looks plausible enough to escape scrutiny); (6) workflow_dispatch bypassing the CI gate, now observed 3x.
RATIONALE: This whole multi-round incident is the campaign's own thesis proven under real pressure -- a guard caught something real, the first fix was incomplete (a plausible number, not a verified one), the process response (independent re-derivation, adversarial pin-attack, set-equality proof, real-data replay) is exactly what closed it correctly rather than papering over it a second time. Worth stating plainly in the close report's own account of itself.
REVERSIBILITY: N/A, incident closed, no outstanding action.
ACTION FOR CONDUCTOR: resume normal lane processing per INT's own stated order. No further action needed on this incident beyond carrying its residuals and narrative into the close report.

## RULING 74 · A8-NAR-TRIAGE REFUTED on Claim 4 only (the 42-path partition) — reopen cycle 1 of 3

QUESTION: VER's independent re-derivation of A8-NAR-TRIAGE (PR #903) confirmed 4 of 5 claims (the already-closed-items list, the 7-item PLAUSIBLE re-examination, the F18 reversal) but REFUTED Claim 4 — the 42-path zero-overlap partition that gates dispatch of all six B-NAR-* lanes. Two load-bearing defects: (a) §4.3 assigns `ga_nakshatra.py:87` to B-N8-FIX, directly contradicting Rulings 13/13-CORRECTION which already routed that exact site to the dedicated lane B-VERIFSTATUS-VOCAB (PR #910, already merged/CONFIRMED) — as written, `ga_nakshatra.py:289` (B-NAR-GA's legitimate site) would be orphaned, since B-NAR-GA's own scope text also excludes `:87` and nothing else claims `:289`. (b) §4.9's claim "the only intersection anywhere was ga_nakshatra.py" is false — VER found 8 real file collisions against 3 already-built lanes (B-VERIFSTATUS-VOCAB #910: same-function collisions on `ga_writers/gates.py` and `bo_cdlm_summary.py`, plus file-level overlap on `bo_upaya.py`/`envelope.ts`/`register_p1_ganita.ts`; B-N8-TS-SERVE #909 and B-N8-F19-COVERAGE #908: both insert ~90-150 lines above A8's own `registry_bridge.ts:3471`/`:3549-3557` citations, meaning those citations are already stale). Only 2 of the 8 are covered by any existing DVA ordering table.
EVIDENCE: full VER report, independently re-derived per-claim with command-level citations (SHAs checked against origin/main, live DB queries against the canonical + operator charts, git log -S searches, mechanical parse of the INSERT column lists). See conversation record; not re-transcribed here in full given length — the two defects above are the operative ones for re-dispatch.
DECISION: (b) — REOPEN A8-NAR-TRIAGE, reopen cycle 1 of 3, with VER's refutation as the builder's first input (per manual §3). This is a documentation-correction reopen (the underlying triage substance is sound; only §4.3 and §4.9 of the partition doc need rewriting), not a from-scratch redo. Concretely required before re-confirm: (1) rewrite §4.3 so `ga_nakshatra.py:87` is explicitly excluded from every B-NAR-*/B-N8-* lane (already merged via #910) and `:289` stays with B-NAR-GA; (2) replace §4.9's false zero-overlap sentence with the real 8-collision set, and add an explicit ordering/ownership decision for the 2 same-function collisions not covered by any existing table (`ga_writers/gates.py`'s `run_g7_only_facts_gate_db` and `bo_cdlm_summary.py`'s `_build_clusters` — both already touched by merged/confirmed #910; the corresponding B-NAR-GA/B-NAR-MI findings at those same functions must rebase onto #910's already-landed content, not treat it as still-pending); (3) re-derive the `registry_bridge.ts` F11 line citations (`:3471`/`:3549-3557`) against current `origin/main` (post #908/#909, if merged — check live) rather than carrying stale numbers; (4) add the 2 previously-omitted MI test files to the partition count (42 → 44); no other changes needed — claims 1/2/3 stand CONFIRMED and must not be re-litigated.
RATIONALE: The partition's *arithmetic* (zero duplicate paths across the 42 it did enumerate) is sound and independently verified — the defect is that it was authored against a stale read of the ordering rulings and a stale read of what other lanes had already built, both correctable without redoing the underlying triage. Re-running the whole lane would waste confirmed work; a targeted reopen matches the size of the actual defect.
REVERSIBILITY: Trivial — a documentation correction to an unmerged PR, no data/schema/deploy implication.

## RULING 75 · Two items VER found routed-but-unruled in A8's original dispatch — decided now rather than left open

QUESTION: A8-NAR-TRIAGE's original 3 DVA questions included (i) the ga_nakshatra.py two-lane conflict [now resolved by Ruling 74 above, it was already answered by Rulings 13/13-CORR, just not connected], (ii) whether the F18 CANNOT-REPRODUCE reversal's disposition wording stands, (iii) severity re-banding for the two items VER independently confirmed as escalated (`mi_bhavisya.py:161`, F25; `bo_cdlm_summary.py:348`). VER found zero ledger entries answering (ii) or (iii) — genuinely unruled, not merely hard to find.
EVIDENCE: VER independently reproduced both live. F25 (`mi_bhavisya.py:161`): exact reproduction of a domain-mismatch scoring bug — strength pinned at 1.0 for non-domain-matched buckets while domain-matched buckets run 2.16-2.81, live across the transition (50/250/0-matched) and general (3/15/0-matched) queries. `bo_cdlm_summary.py:348`: mechanical parse of the actual INSERT statement — 59 columns/59 values, `domain_relationship_class` at slot 48 is a literal `NULL` in the INSERT despite being computed in the writer's own row dict and never referenced by any UPDATE; live query confirms 225/225 cells NULL, all 15 clusters read `unclassified`/`mixed`. For F18: the reversal itself is independently reproduced and CONFIRMED (5/5 code citations land, live proof genuine on both built charts, every refutation path VER tried is closed) — the only issue is one word: the doc says the corrected narration is "already served," but no serve surface (`query_domain_result.ts` checked directly) currently selects `narration_jsonb`.
DECISION Q(ii): ACCEPT the F18 reversal as CONFIRMED, with one wording correction: replace "already served" with "already shipping in production data, not yet selected by any serve surface" wherever the disposition is recorded (the partition doc and any downstream citation of it). Not a re-open-worthy defect — a precision fix folded into Ruling 74's reopen since that document needs touching anyway.
DECISION Q(iii): ESCALATE both. `mi_bhavisya.py:161` (F25) escalates P2 → P1 — a live, reproducible scoring distortion on every non-domain-matched row is a correctness defect, not a plausible-severity judgment call. `bo_cdlm_summary.py:348` escalates P2 → **P0** — this is not a scoring nuance, it is a computed value discarded before its own INSERT statement; every row of this field has been silently NULL since the writer shipped. Both carry into whichever B-NAR-* lane owns their file (B-NAR-MI for both, on current evidence) as P1/P0 respectively, fixed with the same rigor as any other §N.8-class finding (real fix or explicit NULL never a silent default — moot here since NULL is already the observed state, so the fix is to make the field actually compute, not decide whether to null it).
RATIONALE: Both are independently reproduced with concrete, quantified evidence (not asserted) — routing them to further debate would just delay a decision the evidence already supports. bo_cdlm_summary.py:348 in particular is exactly the shape of finding this campaign exists to surface: a value computed, then dropped, with nothing anywhere signaling the loss.
REVERSIBILITY: Severity labels are metadata, trivially revisable if a fix investigation finds the impact narrower than measured here; no code changed by this ruling itself.

## RULING 76 · B-N8-FIX's bo_chart_gestalt fix — serving-layer consequence (small TS fix authorized, bundled not blocking)

QUESTION: B-N8-FIX correctly stopped `bo_chart_gestalt.py:210` from storing a verdict (its docstring forbids it), replacing `verdict_class`/`confidence` with whole-domain evidence counts + a `signal_id` pointer + a `verdict_note`. But `surface_gateway.ts:79-80` still reads `o['confidence'] ?? 0` / `o['verdict_class'] ?? 'unknown'` and always sets `signal_id` from the row — so `instrument.ts:244`'s `if (verdict && verdict.signal_id)` guard still passes (the pointer is still present), producing live prose "primary domain X is unknown (confidence 0)" instead of falling through to the ALREADY-BUILT honest-empty branch at instrument.ts:255-258 ("no pre-computed gestalt verdict was available... disclosed, not fabricated").
EVIDENCE: read both files directly. Confirmed `instrument.ts`'s guard is `verdict && verdict.signal_id` (line 244) and the honest-empty branch already exists and is exactly the right message for this case — it was written for the "no row returned" case but its wording is equally correct for "row returned, no verdict computed."
DECISION: authorize a small, targeted `surface_gateway.ts` fix — do NOT ship the writer fix without it, but do NOT block on a larger redesign either. `verdict_map[domain].signal_id` must be set to `''` (falsy) when the row genuinely has no `verdict_class` (i.e. the new evidence-only shape), so `instrument.ts`'s existing guard naturally routes to its existing honest-empty branch. This is a ~3-line change (a presence check before the signal_id assignment), not the larger "serving lane computes the verdict from the new evidence block" option — that remains a real future improvement (surfacing the evidence counts to the reader) but is NOT required to avoid shipping a regression, and is explicitly NOT authorized now (scope creep against an already-large run).
RATIONALE: The choice is between "confidently prints a meaningless 'unknown (confidence 0)'" and "uses the honest-empty sentence the codebase already wrote for exactly this situation" — the second costs 3 lines and reuses existing, reviewed copy. Shipping the writer fix without this would be a live UX regression on every domain query.
REVERSIBILITY: Trivial, 3-line TS conditional, no schema/data change.
ACTION FOR CONDUCTOR: dispatch a small builder to implement this in `platform/src/lib/retrieval/synthesis/surface_gateway.ts`, bundle with or immediately follow B-N8-FIX's merge (same PR is fine, or a tiny immediate follow-up — INT's call at merge time).

## RULING 77 · Two self-corrections surfaced by A8-NAR-TRIAGE's reopen — both accepted, ledger corrected in place

QUESTION: the reopened A8-NAR-TRIAGE builder, while fixing §4.3/§4.9 per Ruling 74, found two factual errors in this ledger's own prior entries (Rulings 74/75) rather than silently reconciling around them. Both checked directly rather than taken on the builder's word.
EVIDENCE (1): Ruling 74/75 and the correction sent to B-N8-FIX all stated PR #910 (B-VERIFSTATUS-VOCAB) was "already merged." Checked directly: `gh pr view 910` → `state: OPEN`, `mergedAt: null`. **It is VER-CONFIRMED (correct, per SAMAPTI_VERIFICATION_LEDGER.md) but NOT merged** — I conflated "VER confirmed the code" with "the PR landed." `origin/main` still carries the unfixed `"verification_pass_status": "PASS"` literal at `ga_nakshatra.py:87`.
EVIDENCE (2): Ruling 75 assigned the `bo_cdlm_summary.py:348` P0 escalation to B-NAR-MI. The builder traced it directly: that file's actual root cause (`_CDLM_INSERT`'s slot 48) lives in `bo_sangati.py`, and both are B-NAR-BO's files per the (now-corrected) partition — B-NAR-MI owns neither.
DECISION: both corrections ACCEPTED as stated, no further investigation needed (both independently re-verified above). CONSEQUENCES, recorded rather than re-litigated: (a) every "rebase onto #910's already-landed content" instruction issued so far (to A8's reopen, and implicitly assumed for future B-NAR-GA/B-NAR-MI dispatch) is PREMATURE, not wrong in principle — the correct merge order remains Ruling 13-CORRECTION's original: **B-NAR-GA merges FIRST (simpler, narration-only), B-VERIFSTATUS-VOCAB (#910) rebases onto it before touching `ga_nakshatra.py`** — this ordering never actually changed, my own restatement of it as "already done" was the error, not the ordering itself. (b) `bo_cdlm_summary.py:348`'s P0 finding is reassigned to **B-NAR-BO**, not B-NAR-MI; B-NAR-MI retains only the P1 (`mi_bhavisya.py:161`, F25).
RATIONALE: Worth recording plainly rather than quietly fixing — this run's own thesis is that unverified claims accumulate error even in the verification apparatus itself, and a Conductor/DVA ledger is not exempt from that just because it's the one keeping score. Caught by the same discipline (re-check before asserting) the whole campaign is built on.
REVERSIBILITY: N/A — factual correction, no code/schema/deploy implication. A8's own corrected partition doc (branch `samapti/nar-triage-fix`) is the authoritative version going forward; Rulings 74/75's prose is superseded on these two points only, not retracted wholesale (their underlying decisions — reopen A8's Claim 4, F18 wording, the two severity escalations — all stand).

## RULING 78 · SELF-INFLICTED PRODUCTION DEPLOY FAILURE — merge #9 (B-MIG474-COMMENT, PR #915) — HARD-STOPPED, root-caused, reverted, recorded per manual §6/§8.9

QUESTION/INCIDENT: merge #9 (B-MIG474-COMMENT) deployed and the migration step FAILED: `MigrationHashMismatchError` on `platform/migrations/474_asset_throughput_incomplete_state.sql`. Per manual §5, merge queue HARD-STOPPED immediately, no further merge attempted, production health checked first.
EVIDENCE: production was NOT degraded — `amjis-web` continued serving revision `amjis-web-01305-8b6` @ `commit-sha=638e5499` (pre-merge-#9 state) at 100% traffic throughout, `/api/health` → 200, confirmed via `gcloud run services describe` and a direct probe, not inferred from "the job failed so it must be fine." Fail-closed held exactly as designed.
ROOT CAUSE, self-inflicted: PR #915 (B-MIG474-COMMENT) edited `platform/migrations/474_asset_throughput_incomplete_state.sql`'s header comment (`"Migration 467"` → `"Migration 474"`) as a documentation-only fix, authorized under **Ruling 44** (issued before the hash-integrity guard existed, when "comment-only, no functional effect" was a reasonable exception). **B-MIGGUARD's `assertAppliedHashMatches()` (merged the SAME night, later) does not distinguish comment-only edits from functional ones** — it fails on ANY byte change to an already-applied file, which is precisely **Ruling 58**'s hardened doctrine ("NEVER EDIT A MIGRATION FILE AFTER IT HAS BEEN APPLIED... author it surgically"). Ruling 44 and Ruling 58 were both correct when issued, in sequence, and collided the moment a lane acting on Ruling 44's older authorization reached production after Ruling 58's guard had landed. **This was an Integrator sequencing failure, not a builder defect** — B-MIG474-COMMENT's own VER-CONFIRMED verdict was accurate for the code as scoped; I (INT) should have re-checked its premise against Ruling 58 before merging, since Ruling 58 postdates and narrows Ruling 44 exactly on this point, and did not.
DECISION: **Ruling 58 supersedes Ruling 44 on comment-only edits to already-applied migrations, full stop — no exception class survives the hash guard's existence.** (1) Revert PR #915's header-comment change byte-for-byte (verified: reverted file's sha256 = the stored `_migrations_applied` value exactly, `9297d799…`). (2) The header-number mismatch on this file (and the other 16 Ruling 44 identified) reverts to WARN-ONLY/documented-not-fixed status, matching Ruling 44's ORIGINAL fallback for the 16 it declined to touch — B-MIG474-COMMENT's attempt to be the one exception is retracted, not just this one instance. (3) Standing correction to `MIGRATION_AND_MERGE_PROTOCOL_v1_0.md`: remove the "(2) B-MIG474-COMMENT merges AS-IS" carve-out language from Ruling 44's record or annotate it superseded, so a future reader doesn't repeat this collision. (4) No further comment/header-only "fix" PRs against already-applied migrations are authorized for the remainder of this run.
RATIONALE: The guard did exactly what it was built to do — caught a real, if harmless, edit to applied SQL, including one this campaign's own earlier, since-superseded ruling had blessed. Fixing it by reverting (not by disclosing/allowlisting) keeps faith with Ruling 58's actual content: the answer to "an applied migration got edited" is never "note it and move on," even when the edit is provably inert. Disclosing this as a 26th allowlist entry would have quietly re-legitimized exactly the practice the guard exists to stop.
REVERSIBILITY: Trivial — one-byte revert, verified hash-exact; the corrective PR is itself as low-risk as the original.
ACTION FOR CONDUCTOR: merge the revert PR immediately, ahead of all other queued lanes (it is the recovery from a hard-stop, per manual §5's "route to DVA" — this ruling is that routing), watch the deploy through to a REAL end-to-end success (not CI green) before resuming the normal merge queue.

## RULING 79 · PARK_PB-3_L-5 memo §5 items 2-4 (the remainder Ruling 55 routed) — DECIDED, implementation remains PARKED-HONEST

QUESTION: Ruling 55 decided §5 item 1 (Option A vs B → B, separate `mimamsa_conversational_calibration` table). Items 2-4 were flagged "remainder routed to DVA" and never actually ruled.
DECISION:
**Item 2 (exact target name + column set):** ADOPT the memo's own §4 Option B schema verbatim — table name `mimamsa_conversational_calibration`, columns `id uuid PK`, `chart_id uuid NOT NULL`, `source_citation uuid NOT NULL`, `prediction_ledger_row_id uuid NOT NULL REFERENCES brahma_mimamsa_prediction_ledger(id)`, `domain text`, `confidence_point numeric`, `outcome text`, `outcome_value numeric`, `brier numeric`, `brier_excluded boolean NOT NULL`, `scored_at timestamptz NOT NULL default now()`, `UNIQUE (prediction_ledger_row_id)`. No deviation — the memo's own reasoning (matches `CalibrationWriteIntent` 1:1, no synthetic-key gymnastics) is sound and independently re-checked against the intent shape described in §3.
**Item 3 (COLLECT-ONLY confirmation):** CONFIRMED, standing requirement, not weakened. Whoever implements this write path must add a grep/runtime guard (per Lane L-6's existing pattern — same class of guard as `assertNoCalibrationLeak` built this same run for B-LOOP-G4G5/G5) proving no priors-bump or serving-annotation code path ever reads `mimamsa_conversational_calibration`. The guard is a REQUIRED PART of the implementation, not a follow-on — a persistence sink without its leak guard is not a complete close of this park.
**Item 4 (untouched tables):** CONFIRMED trivially — Option B is purely additive (one new table, zero ALTERs elsewhere), so `mimamsa_predictions`, `brahma_prospective_ledger`, and `phala_anchors` are structurally guaranteed untouched by this design; no separate verification needed once Option B is what ships.
SCOPE NOTE: this ruling SETTLES the design questions the memo asked for. It does NOT authorize building the migration + writer within THIS run — no lane in `session_queue_SAMAPTI.yaml` was ever dispatched for it, and authoring an unplanned migration this late in an already-eventful run is exactly the kind of scope creep Ruling 48 declined elsewhere tonight. **Disposition: PARKED-HONEST, now unblocked** — the park is no longer "awaiting a MEMO ruling," it is "ruled, implementation is a costed, ready-to-start follow-on for a future session," which is a real state change worth recording even though no code lands tonight.
RATIONALE: A park that sat on an unanswered MEMO for 3 days is worse than one that's honestly parked with the design settled — resolving items 2-4 now costs nothing (the memo did the real thinking already) and removes a stale "awaiting ruling" status that would otherwise persist into the close report as an open loose end it didn't need to be.
REVERSIBILITY: Pure ruling, no code/schema touched. Fully reversible before implementation starts.

## RULING 80 · B-PB8-BYTEEQ meets its own scope; Ruling 54 (standing capture posture) does not apply retroactively — gap PARKED-HONEST, not silently absorbed

QUESTION: VER's check of B-PB8-BYTEEQ (PR #927) found the lane genuinely delivers its briefed scope (fixture-corpus bridge real and honestly avoiding the rejected c2ProtocolAdapter path; stream-capture mechanism real and wired) — but does NOT meet Ruling 54's four standing-capture-posture requirements (low default sample rate, enforced retention, observable deployed state, force-capture path), because Ruling 54 was issued AFTER this lane's commits, reasoning over this very PR's own "still open" disclosure.
EVIDENCE: `PARIPRASHNA_STREAM_CAPTURE` defaults OFF and, when enabled, defaults to sample rate 1.0 (every turn) rather than low-rate; retention is a hard `expires_at` column but purge is manual-only (zero cron/workflow references); the CLI capture-state report reads the OPERATOR'S LOCAL env var, not the deployed service's — it can actively misreport `amjis-web`'s real state; no force-capture-next-reading path exists.
DECISION: (1) B-PB8-BYTEEQ's disposition is `done` for its OWN briefed scope — not faulted for a requirement that postdates it. (2) Ruling 54 conformance is a SEPARATE, NAMED, PARKED-HONEST item — NOT folded into this lane's `done`, NOT silently assumed covered by C4-LOOP-LIVE-PROOF. (3) Because the capture flag defaults OFF and is not flipped on anywhere in this run's scope, the gap is currently INERT — no production risk today, so no rush-fix is authorized. (4) STANDING CONDITION: `PARIPRASHNA_STREAM_CAPTURE` must NOT be turned on in production until a future lane closes all four Ruling 54 gaps (low default sample rate + a real cron/scheduled purge + a deployed-state-accurate observability check + a force-capture path). Recording this as a named pre-condition, not a vague "someday."
RATIONALE: The alternative — reporting B-PB8-BYTEEQ as fully done because its own scope is met — would silently launder a capability this run's own Ruling 54 named as belonging to a now-4-times-repeated pattern (a capability honestly built, then reliably off). Naming the gap and gating the flag is cheaper than either fixing it under time pressure tonight or letting it recur a fifth time.
REVERSIBILITY: Trivial — a named follow-on spec, flag stays off, nothing to revert.

## RULING 81 · B-DOCS-GOVERNANCE — 3 sections stale against LATER rulings (not fabrication); MERGE PRIORITY ESCALATED — main is currently over the schema-violation ceiling

QUESTION/FINDING: VER found 4 of 7 threads CONFIRMED (the CLAUDE.md §N.4 rewrite, the ONGOING_HYGIENE cross-reference-only check, the coordination-check accounting, and the governance-count re-derivation) and 3 REFUTED — but every REFUTED item is the document being stale against a DVA ruling issued AFTER the lane's commit (Ruling 56 closing INF-3, Ruling 55 deciding PARK_PB-3_L-5 Option B, later PRs #920/#925 dispatching), not an invented or false claim. Chronology confirms this: the lane committed around Ruling 42-44, well before Rulings 55/56/65.
DECISIVE ADDITIONAL FINDING, more urgent than the staleness items: VER independently ran `schema_validator.py`/`drift_detector.py` against CURRENT `origin/main` (not just this branch) and found **45 violations, not the Ruling-4 ceiling of 43** — the two Ruling-4 KNOWN-TRANSIENT files (`PARIPRASHNA_DESIGN_ENGINEERING_PLAN_v0_1.md`, `REPORT_WHOLE_CHART_SYNTHESIS_AND_MCP_DIAGNOSTIC_v1_0.md`) landed via A1-PRESERVE WITHOUT their `artifact:` frontmatter field, while PR #928's own copies DO carry it. **PR #928 is the only fix in flight for a ceiling breach that is live on main right now**, and the PR is `mergeStateStatus: CONFLICTING` on exactly those two files.
DECISION: (1) Reopen cycle 1 of 3, narrow mechanical fix list (VER's own 5-item list: memo index row 6's false "never committed" claim about BRIEF_PB-3.md — which A1-PRESERVE actually recovered and merged; two stale "not yet dispatched" lines for B-LOOP-G4G5/B-PB6-GEOMETRY; §7's INF-3 disposition rewritten to Ruling 56's actual text + the standing-practice correction; §4's PARK_PB-3_L-5 routing updated to cite Ruling 55's decision; §3's stale VER-pending caveat on A7 updated to cite Ruling 65; two small ONGOING_HYGIENE_POLICIES wording nits). None of it touches the CONFIRMED §N.4 doctrine text — that stands as-is. (2) MERGE PRIORITY ESCALATED: this lane jumps ahead of the normal merge_order once reopened and re-confirmed, specifically because main is currently in breach of its own governance ceiling and this is the only queued fix. (3) INT's merge-conflict resolution on the two frontmatter files MUST keep PR #928's `artifact:`-bearing versions — resolving the other way silently re-breaks the ceiling this lane exists to fix.
RATIONALE: Distinguishing "stale against a later ruling" from "fabricated" matters — this run has been careful about that distinction all night (see Ruling 77's own self-correction), and a Sonnet scribe lane correctly transcribing rulings that existed AT COMMIT TIME should not be penalized for rulings that came later. But the ceiling breach is a live, current governance-integrity gap, not a historical curiosity, and it changes this lane's merge priority materially.
REVERSIBILITY: Doc edits, single-commit revertible. The frontmatter conflict-resolution direction is the one irreversible-in-practice choice — get it right the first time.

## RULING 82 · B-SECRETSCAN-SCOPE REFUTED — detector genuinely repaired, two merge-blocking defects, reopen cycle 1 of 3

QUESTION: VER's check of B-SECRETSCAN-SCOPE (PR #911) confirmed the actual detection capability (scope inversion, pattern-shape fix, commit-SHA provenance, the composite can-fail proof, credential-value-never-in-diff) — but found two real defects that block a safe merge, neither cosmetic.
EVIDENCE: (1) The PR renames the CI job `Secret Scan (unit 0b.2)` → `Secret Scan (unit 0b.2 · repo-wide)`. `main`'s branch protection requires the status-check CONTEXT `"Secret Scan (unit 0b.2)"` by exact name (`strict: true`, `enforce_admins: true`) — the renamed job never reports under that context, so post-merge every future PR would face a permanently-pending required check, an obvious setup for someone to "fix" by deleting the required-context binding (exactly the weakening this lane exists to prevent). (2) VER's own fresh census against CURRENT `origin/main` (not the PR's stale base) found a 28th credential-bearing file has landed since this branch cut (`platform/python-sidecar/tests/l5/test_mi_bhara_circularity_guard_w2.py`) — the detector itself catches it correctly (proof the repair works), but the PR's own register is one row short, and branch protection's `strict: true` means the branch must rebase before merge, at which point this gate would go red against its own unregistered finding.
DECISION: reopen cycle 1 of 3, two-item fix: (1) restore the CI job's exact original name (`Secret Scan (unit 0b.2)`) so it keeps satisfying the existing required-status-check binding — do not touch branch protection settings to work around this, the job keeping its name is the lower-risk fix; (2) add the 28th file as a registered, dated, owned entry in the credential register (owner `DVA-ESCALATION-SEC-002`, matching the existing 27-entry convention) rather than leaving it to surface as an unexpected red after rebase. Both are small, mechanical, do not touch the actual detection logic (which stands CONFIRMED).
RATIONALE: A security-detection gate that breaks its own binding to branch protection on the exact merge that's supposed to strengthen it is the same "the check silently stops meaning what it claims" pattern this whole run has spent 82 rulings finding elsewhere — catching it before merge rather than after is the point of VER existing.
REVERSIBILITY: Trivial — one CI job name reverted, one register row added. No functional change to the repaired scanner.

## RULING 83 · NATIVE STRATEGIC REDIRECTION — SAMĀPTI stops concurrent Kāla work, closes PARTIAL, hands Kāla-touching items to ṢAḌ-DARŚANA

QUESTION: with ṢAḌ-DARŚANA actively rewriting the Kāla layer into a six-views architecture (W2 lanes A-E landed tonight, PR #947/#944-946/#949), does SAMĀPTI continue auditing/fixing/rebuilding Kāla assets it will not own the final shape of?
DECISION (native-issued, binding, supersedes Rulings 7/8 on C2 specifically): NO. Effective immediately: (1) C2-GOCHARA-RUN is PARKED-BY-DESIGN, not incomplete. Rationale for the record: canonical chart 482012f1 is already 303/303 (the operator-E2E gate chart, per L1_GANITA_CLOSURE); operator-chart parity (1c826d5a, cb73cd3d) is second-order; A6-GOCHARA-DIAG already proved the incompleteness is a resource-budget/timeout artifact (~27% of a 303-substep plan per 6h dispatch window), not a defect; and ka_gochara_sweep is being structurally restructured by ṢAḌ-DARŚANA's own W2 work regardless. Auditing/completing an asset about to be rewritten is waste, and the very concurrency that would require it (two swarms racing migration numbers, sharing worktree infrastructure, trading merge-lock overhead) is itself a cost this ruling eliminates. (2) The running Cloud Run execution (`brahma-build-pipeline-job-bk4cg`, run `f852d28b`) was CANCELLED, not left to complete — verified clean afterward: `build_substep_progress` for `1c826d5a` reads 209/303 (up from 175 before this run's dispatch, real progress preserved, no partial/corrupt row — every row present has `completed_at` set, `done == total`); `cb73cd3d` untouched at 70/303; canonical `482012f1` untouched at 303/303. No `build_substep_progress` mutation was performed directly — the cancellation stops future writer dispatches, it does not touch existing rows. (3) BUILD-LOCK is released; no further BUILD-LOCK acquisition for any Kāla asset this run. ṢAḌ-DARŚANA, which is running its own gochara dispatches concurrently, is not blocked by anything SAMĀPTI was holding — this campaign has vacated the asset entirely.
GOVERNING PRINCIPLE FOR EVERYTHING DOWNSTREAM: hold the AUDIT, ship the GUARDS. A guard (CI lint, integrity check, tracker fix) landed BEFORE the Kāla rewrite completes means the new six-views layer is born under it and structurally cannot reintroduce the defect class the guard catches. A guard landed after means re-auditing new code for old sins — strictly worse. Every remaining SAMĀPTI item from here forward is dispositioned by: does it touch Kāla STRUCTURE/ASSETS? → HAND OVER (a written spec, not code, to `SAMAPTI_KALA_HANDOVER_v1_0.md`, delivered to both campaigns' brief directories). Does it not, and is it protective/verified/merge-ready? → LAND IT. Does it not, but is unstarted/mid-flight? → PARK-HONEST with a resume spec, condition = "six-views lands / Kāla layer settled." No new lane dispatches to "finish" things that were never started — the run is closing PARTIAL, deliberately, not grinding to false completion.
RATIONALE: This is a sequencing call, not a failure — stated explicitly for the close report and for whoever reads this ruling later. The alternative (continuing to audit/fix/rebuild Kāla assets ṢAḌ-DARŚANA is actively restructuring) produces work that gets thrown away, plus the exact concurrency costs (migration-number races, shared-checkout near-misses, merge-lock contention) this run has already paid for twice tonight.
REVERSIBILITY: The C2 cancellation is reversible in the trivial sense that a future resume dispatch picks up exactly where 209/303 left off (per the resume script's own design) — nothing is lost, only deferred. The strategic redirect itself is a native decision, not reversible by this session.

## RULING 84 · B-N8-FIX (#952) REAL CI failure — PARKED-HONEST per native instruction, no further reopen

QUESTION: after a background-process interruption (this session's own background agents/watchers were silently orphaned for an extended period — confirmed via task-notification "no completion record found"), direct re-verification of PR #952's real CI run found a genuine test failure: `tests/l2/test_bo_a7_writers.py::TestBoChartGestalt::test_bo_chart_gestalt_emits_one_row_per_ayanamsha` — `StopIteration`. Root cause identified by inspection: the test is a white-box mock built on a hardcoded sequence of `_fetch_dict` cursor calls inside `_write_aya`, documented in the test's own comments as "Order of _fetch_dict calls inside _write_aya: 1. top_signals ... 10. INSERT cursor." B-N8-FIX's legitimate changes to `bo_chart_gestalt.py` (removing verdict storage, adding whole-domain evidence counts per its VER-CONFIRMED scope) changed that call sequence; the pre-existing mock test was never updated to match, so its cursor iterator runs out mid-call.
DECISION: per explicit native instruction (this session, current turn): a REFUTED/red verdict on one of the three in-flight checks does NOT trigger another reopen cycle — it is recorded PARKED-HONEST with the failure named, full stop. B-N8-FIX (PR #952) is PARKED-HONEST. Its underlying detector work (4 bo_pramana_mapa.py fields, the bo_chart_gestalt.py verdict-storage fix, the Ruling-76 serving-layer bundle) remains VER-CONFIRMED and CORRECT — the defect is confined to one pre-existing test's stale mock sequence, not the production logic. Named blocker for whoever resumes this: update `test_bo_chart_gestalt_emits_one_row_per_ayanamsha`'s cursor sequence to match `_write_aya`'s current (post-B-N8-FIX) call order — a mechanical, narrow fix, NOT a re-litigation of the confirmed detector logic.
RATIONALE: The native's instruction here is itself the correct discipline for a run this deep into elapsed real time with confirmed process interruptions — grinding another reopen cycle risks compounding whatever caused the interruption, and the underlying work's correctness is not in question, only one stale test's brittleness.
REVERSIBILITY: Trivial once resumed — the fix is confined to test-fixture data, no production code or schema change needed.

## RULING 85 · B-SECRETSCAN-SCOPE (#911) reopen agent interrupted mid-flight in a confused state — PARKED-HONEST, not salvaged

QUESTION: same background-process interruption as Ruling 84. The reopen agent dispatched for #911's 2-item fix (Ruling 82) left its worktree with 2 local commits PLUS uncommitted changes to `.github/workflows/ci.yml` and `secret_scan_inherited.txt` — but direct inspection found those 2 commits are NOT built on the original PR head (`866600a4`): `git merge-base --is-ancestor 866600a4 HEAD` returns false, and the diff against the original head shows ~194 files / 44,526 insertions, consistent with the agent having rebased onto a much later `origin/main` and substantially reworked the fix rather than cleanly continuing it, then being interrupted before finishing (uncommitted changes still present).
DECISION: do NOT attempt to salvage or verify this state under time pressure — per the same native instruction as Ruling 84 (no further reopen cycles on the three in-flight checks), PARK B-SECRETSCAN-SCOPE HONEST. The underlying detector fix (scope inversion, pattern-shape repair) remains VER-CONFIRMED from the FIRST VER pass — only the two Ruling-82 mechanical fixes (CI job name revert, 28th-file register entry) are unresolved, and now compounded by an uncertain intermediate state that needs a clean human/DVA look before anyone continues it, not a blind push of whatever is sitting in the worktree.
NAMED BLOCKER for resume: (1) confirm whether the confused worktree's 2 commits contain anything worth salvaging, or discard and restart cleanly from `866600a4` + Ruling 82's original 2-item fix description; (2) do not push/merge anything from worktree `agent-a2bf22711d06bbf98` without first verifying it actually contains `866600a4`'s original diff as an ancestor.
RATIONALE: A reopen fix built on the wrong base is worse than no fix — it risks silently dropping the original PR's real content while looking superficially complete. Honest park, named precisely, costs nothing; a blind push risks exactly the "green that means less than it claims" pattern this whole campaign exists to catch.
REVERSIBILITY: Fully reversible — nothing was pushed or merged from the confused state; the original PR #911 branch (`866600a4`, VER-CONFIRMED detector logic) is untouched and remains the resume point.

## RULING 86 · Remaining standalone items — PARKED-HONEST per Ruling 83's disposition test (batch, no new dispatch)

Per Ruling 83's own instruction ("do NOT dispatch new lanes to finish things") and the closing-PARTIAL discipline, the following are recorded PARKED-HONEST with a named resume condition rather than built or investigated further this run:

**PB-3.1 prediction loop remainder (G4/G5 follow-on polish, C4-LOOP-LIVE-PROOF, C5-PB7-BADGE, C6-PB4-PURNATA).** Its own separable track (`platform/src` TS, disjoint from Kāla). B-LOOP-G4G5's core work (#925) is VER-CONFIRMED and queued in the merge backlog independent of this park. The live end-to-end proof (a real deployed reading → ledger row → review → resolve, per C4) and PB-4 itself (R-0/PB-4, gated on C4) are READY-FOR-EXECUTION, blocked only on merge-queue capacity, not on any unresolved design question. Resume condition: next SAMĀPTI-class session picks up C4 first (it unblocks C5 and the PB-4 gate).

**C3-BUILDSTATE-RECON / EP-1 (falsely-lit re-audit).** Per Ruling 10, EP-1's "NOT-APPLICABLE" disposition is VOID as a present-tense claim — a real re-reconciliation (build_substep_progress vs. full plan, read-only) is still owed. PARKED, not attempted this run: it depends on C1-REBUILD (never run — no narration-fix lanes actually merged this run to make a consolidated rebuild worthwhile) and is explicitly Kāla-adjacent in places (B-WATCHDOG-LIT's own fix, once merged, changes what "falsely lit" even means for future assets). Resume condition: after C1-REBUILD next runs against whatever writer fixes have landed by then.

**`chart_dashas` −52,084 delta (Ruling 16).** No owner claimed it this run. PARKED with the numbers stated plainly: the v2.0-marked "prod-confirmed" figure recorded chart_dashas higher by 52,084 rows (9.7%) than the count measured during this campaign, while `chart_facts`/`chart_divisionals` moved UP over the same interval (consistent with a post-enrichment growth story) — a drop is NOT explained by that story. Named hypothesis, not confirmed: `ga_dashas`' 4-level Sukshma tree may be truncated for part of the covered span. Per Ruling 16's own binding: this must be explained by mechanism or stay PARKED — never silently absorbed into a re-baseline. Resume condition: whoever next touches `ga_dashas_writer.py` or runs a fresh row-count census should check this specifically before assuming the current count is simply "the new normal."

**MCP tool-count three-way mismatch.** Three numbers exist for "how many tools does the MCP server have": **124** (live `tools/list`, independently re-derived by VER from two directions this run — confirmed authoritative per Ruling 28), **152** (`mcp_server_info.tool_count`, sourced from a decoupled generated-registry-size metric, not a live measurement — Ruling 25), and **158** (a separate "regenerated, no source change" figure noted in B-LOOP-G4G5's own VER pass). PARKED with all three numbers stated rather than picking one without the reconciliation lane (`B-MCP-CATALOG-GAP`, already VER-CONFIRMED per its own verification-ledger entry but not yet merged this run) actually landing. **E1-SAMGATI §8.6 cannot claim PASS on tool-catalog reconciliation until B-MCP-CATALOG-GAP merges** — recorded as an explicit E1 gap, not silently assumed clear.

**The deferred 19-PR-body check (Ruling 41).** VER's own exhaustive audit found all 19 open SAMĀPTI/preserve PR bodies clean at the time it ran (zero self-identification mismatches, zero cited-files-vs-diff mismatches) — but VER's own recommendation was to re-run the check immediately before the merge queue closes, since PR bodies remain editable until merge and the audit has a shelf life. That re-run did not happen this session (time/scope). PARKED: treat PR body descriptions as unverified-since-audit-time for any PR that has not itself been independently re-verified by a fresh VER pass this session (i.e. everything actually merged this run WAS independently checked at merge time by direct diff inspection, which subsumes the body-trust question for those specific merges — this park applies to PRs that did NOT merge this run).

**B-NAR-BO/GA/MI/PH remainder + B-NAR-TS's non-Kāla files.** None of these lanes were ever dispatched (per the earlier finding that A8-NAR-TRIAGE's partition was authored but the six downstream lanes never started). Per Ruling 83's disposition test: not Kāla-touching (Bodha/Gaṇita/Mīmāṃsā/Phala files, plus B-NAR-TS's non-`kala_temporal.ts` findings — `capabilities.ts`, `envelope.ts`, `vidhi_registry_resource.ts`, `server.ts`, `register_p1_synthesis.ts`, `register_p1_ganita.ts`), unstarted → PARKED-HONEST, not dispatched fresh this run. The corrected 45-path partition (branch `samapti/nar-triage-fix`, PR #956) remains the authoritative resume spec — whoever picks this up next should dispatch from that partition directly rather than re-deriving scope.

**B-N8-FIX/SWEEPFIX residuals** (found, explicitly out-of-declared-scope, not fixed): `bo_pramana_mapa.py:265`'s `msr_no_threshold_drop_flag` (F-13, the identical tautology to the fixed F-08, one line away); `bo_chart_gestalt.py`'s F-15/F-16/F-17 (`fragility_class` literal, `linking_mechanism` literal, "genuinely balanced" contested-areas claim at 136 vs 632); the `strongest_domain`/`weakest_domain` insertion-order bug (reads alphabetically-first/last, not actually strongest/weakest). All PARKED, named, with exact file:line — cheap follow-ups for whoever next touches these files, explicitly not bundled into an already-large run.

REVERSIBILITY: All of the above are pure documentation/disposition — no code, schema, or deploy action taken or implied. Every resume condition is named so a future session can pick up without re-deriving scope from scratch.
