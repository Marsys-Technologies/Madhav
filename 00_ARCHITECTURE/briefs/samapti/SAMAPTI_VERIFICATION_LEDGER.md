# SAMĀPTI_VERIFICATION_LEDGER

One entry per verdict: lane, verdict, what VER re-derived, evidence.

---

## A5-LOOP-G2G3 — CONFIRMED

VER independently re-derived all 5 claims and found 2 of them stronger than stated:
- Diff read directly (git diff origin/main...HEAD --stat): only the two intended files changed, worktree clean.
- Claim 1 (secret name fix): confirmed pre-fix line 63 on origin/main read secrets.DATABASE_URL (a secret that does not exist per `gh secret list`); deploy.yml:203 already used the PROD_DATABASE_URL convention. VER additionally found and cited a REAL scheduled run on main (30426360677) that reported SUCCESS while silently logging an empty DATABASE_URL and exiting 0 — the silent-green defect exhibited, not merely asserted.
- Claim 5 (can-fail proof): VER opened run 30489529898 itself (probe commit fec4692a, conclusion FAILURE, real ::error:: annotation + exit 1), confirmed the revert is byte-identical at the tree level (git diff c3dd49f0 HEAD → empty), so no probe residue reaches main.
- Claim 4 (6 DB-integration test files now run): VER derived the arithmetic independently from two real CI runs — baseline at the exact merge-base (30486740360: 0 passed / 27 skip marks across the 6 files) vs. after (30488849699 AND the final HEAD's own run 30489716171: 27 passed / 0 skipped) — confirmed on the FINAL SHA 1fc3eaad, which the builder itself had not separately confirmed.
- Hygiene: actionlint clean on both branch and main (same pre-existing SC2086 note, untouched); PR #895 draft/mergeable/no auto-merge; the only red CI check (SC-17/18/19 boot-time pointer validation) is a pre-existing main failure owned by lane A2, not a regression from this lane.

VER's two flags (neither refuting the verdict):
(a) The builder's own RESIDUALS note mislabeled which lane owns the real-secret daily-job trigger — it's C4-LOOP-LIVE-PROOF's acceptance criterion, not A6-GOCHARA-DIAG's. Recorded; A6 has nothing to do with the daily job.
(b) Unreported residual: the 6 test files' header comments still assert "skipped in CI (no DB)", which this lane just made false — doc staleness, sweep on next touch. Also: CI provisioning hardcodes supabase/migrations/470_… by name — B-MIGGUARD should be aware a renumber of 470 would break CI.

STATUS: done — eligible for merge queue (A2-CI-POINTERS and B-MIGGUARD merge ahead of it per merge_order; A1-PRESERVE's baseline must land first per blocking_for_run).

## A1-PRESERVE — CONFIRMED (blocking lane; T0 baseline established)

VER independently reproduced 216/43 (T0, pinned to cdb6fc3b) and 216/45 (post-A1, pinned to a5cc942d) from the detectors itself, in a throwaway detached worktree, and confirmed by set difference that the +2 delta is EXACTLY the two disclosed frontmatter defects (PARIPRASHNA_DESIGN_ENGINEERING_PLAN_v0_1.md, REPORT_WHOLE_CHART_SYNTHESIS_AND_MCP_DIAGNOSTIC_v1_0.md) with zero compensating disappearances. Byte-identity confirmed on all 22 preserved paths (17 on PR #896, 3 on #898, 1 on #899, plus the .fuse_hidden recovery) via git hash-object / git rev-parse comparison against the live shared checkout. Both of the builder's refutations re-measured independently and confirmed correct: kala_elevation/ is byte-identical to origin/main on all 8 files (brief's "highest risk, ahead of main" claim had decayed to nothing); PARISHODHANA_REPORT_v1_0.md is genuinely v1.0-stale vs main's v1.1 (preserved on a draft branch, correctly not merged — merging would revert another campaign's close report to placeholder prose). No force-push (parishodhana/dark-corpus-remeasure still at 87311a65 on the remote); the "2 unpushed commits" are confirmed ancestors of the preserve ref AND patch-equivalent upstream via git cherry. Zero merges; #896 open/non-draft/no-auto-merge, #898/#899 open/draft. #896 contains zero not-owned paths — ownership separation clean.

STATUS: done — T0 BASELINE is now the operative measurement for the run (pending DVA's ruling on which of 216/43 vs 216/45 E1-SAMGATI measures against — see DVARAPALA_LEDGER, ruling pending).

VER's 4 non-refuting flags for the Conductor:
(a) Baseline disposition (216/43 vs 216/45) needs an explicit DVA ruling or E1-SAMGATI 8.8 reports against an ambiguous number — already routed to DVA.
(b) The kala_elevation refutation exists ONLY in the builder's FINAL_SUMMARY text, not in any committed artifact — must land in SAMAPTI_CLOSE_REPORT_v1_0.md or it evaporates.
(c) **origin/main moved mid-verification**: cdb6fc3b -> 5f5033a5 via PR #897 (ṢAḌ-DARŚANA, a NOT-OWNED active campaign). Two consequences: (i) the MERGE-LOCK precondition ("no live session editing the tree" / campaign quiescence check, manual §5) must be re-checked immediately before INT ever takes the lock; (ii) PR #896 will need a rebase before it can merge cleanly.
(d) BRIEF §3.3's "bring the checkout to origin/main" is correctly NOT done (deference to §7 non-owned-work preservation) but is still a technically-open brief item — belongs to C7-WORKTREE-RETIRE / E1-SAMGATI 8.1, not silently absorbed into A1's close. Shared checkout's git status --porcelain is non-empty (33 entries, incl. the Conductor's own live ledgers) — expected while the run is live; this is E1-SAMGATI 8.1's criterion to satisfy at the end, not A1's.

## A2-CI-POINTERS — REFUTED (reopen cycle 1 of 3)

VER independently re-derived 7 of 8 claims as CONFIRMED (fail-then-pass on pristine main vs branch; ratchet genuinely at zero by root-fix, both baseline/occurrence JSON files verified empty, not absorbed; the resolver lib/mcp_registered_tools.ts untouched by this lane's diff so the registered set can't have been inflated; vitest neutrality re-derived down to individual failing test NAMES, not just counts — 0 newly-failing, 0 newly-fixed; 2 of 3 can-fail proofs reproduced directly by VER itself, including the comment-vs-code discrimination test).

REFUTING on claim 3, specifically the query_signals -> get_signals repointing under SC-18. VER called get_signals on the LIVE deployed MCP server (amjis-mcp-00517-b5q, 100% traffic) and got "-32602: Tool get_signals not found" — while 9 production emission sites (registry_bridge.ts, register_p1_ganita.ts) still name it. get_signals IS registered in source (registry_bridge.ts:2157, landed a month before the current serving revision — not a stale-deploy explanation) but is NOT served live. The servable equivalent is bodha_signals_get ("[Phase-1 alias] ... same as get_signals"). SC-18 is marked FIXED for a HIGH-severity defect whose own stated harm ("the recovery path fires exactly when data was withheld and points at tool-not-found") still reproduces in production today — the pointer moved from a name that never existed to one that exists only in source, not one that's actually servable.

DEEPER SYSTEMIC ISSUE flagged: tap5's Law-7 claims (per its own title) to verify a pointer "names a tool that is actually registered ON THE LIVE MCP SERVER" but only does a static source scan — 167 statically-registered names vs 124 actually served by the live catalog, a 43-tool gap. This is the identical defect shape (a check that doesn't measure what it claims) one level up from what this lane was opened to fix.

SC-17 (bodha_bundle_get), SC-19 (asset-id-in-field), SC-23 (ref_rules_search) — all independently confirmed live and can stand.

REQUIRED FOR CONFIRMED (builder's work): (1) repoint the 9 production get_signals emission sites (+ 4 test fixtures) at bodha_signals_get, OR expose get_signals in the served catalog if that's the actual intent — then re-verify against the live server; (2) give Law-7 either a live-catalog cross-check or narrow its title/docstring to what a static scan actually proves; (3) re-disposition SC-18 accordingly. One alternative VER could not fully exclude: get_signals might be gated by serving principal rather than absent — cheap to test, should be checked first.

VER also flagged a live tool-count discrepancy relevant to A3-SHADBALA-EVIDENCE: A3's builder claimed 152 tools serving; VER independently measured 124 via a direct tools/list call. Worth resolving when VER verifies A3 next.

STATUS: reopened. Re-dispatched to the A2 builder with this refutation as its first input. Reopen cycle 1 of 3 (queue max before PARKED-HONEST).

## A3-SHADBALA-EVIDENCE — CONFIRMED

VER independently re-derived every claim from the live surface (not the builder's transcript): called graha_portrait itself for all 7 grahas on the canonical chart, diffed against the ACTUAL golden table text (SUDDHA_VACA_BRIEF §5 C.8, read via git show) rather than the builder's restatement — zero divergence on required/actual/surplus/grade for all 7. Confirmed the historical "weak" string is real, correctly cited as historical, and worked out its mechanism (the old code served Sun's ratio 1.694 as if it were rupas). Re-derived FORENSIC 7/7 live. Started the Cloud SQL proxy itself (found dead) and confirmed zero duplicate-key rows for the canonical chart's l1_tajik_varsha_year_lords. Ran the smoke test can-fail proof itself across all 4 states (correct key/PASS, wrong key/FAIL 401s, unset key/loud FAIL not skip, revert/PASS) and confirmed the cited CI run's smoke+promotion both succeeded (INF-2 demonstrably closed).

STATUS: done.

VER surfaced 2 findings requiring DVA attention (threaten E1-SAMGATI's terminal criteria):

(1) ROW-COUNT BASELINE DRIFT, sharper than the builder's residual: CLAUDE.md/L1_GANITA_CLOSURE cite chart_facts=27,554; chart_dashas=536,471; chart_divisionals=21,635. Live, canonical chart's chart_facts per-ayanamsha range 27,617-27,709 (total 138,414 across 5 ayanamshas + invariant) -- the documented figure matches NO ayanamsha exactly and is not the total either. chart_dashas documented 536,471 vs live 484,387. chart_divisionals documented 21,635 vs live 23,542. Likely a stale 2026-06 L1-closure snapshot superseded by later rebuilds, but E1-SAMGATI 8.7 requires "row-count invariants hold or every delta is explained" -- cannot pass against CLAUDE.md's current numbers as written.

(2) TOOL COUNT THREE-WAY MISMATCH: live tools/list returns exactly 124 tools (no pagination, nextCursor null, incl. 15 kala_* tools not the 8 the brief cites); the pointer harness's own resolver counts 167 registered names in source; the brief asserts 152. This is the SAME root cause as the A2-CI-POINTERS refutation (get_signals registered-but-not-served). E1-SAMGATI 8.6 ("live MCP tool catalog reconciles against the registry on origin/main") will fail as currently specified until someone establishes which number is authoritative and why up to 43 registered tools are not served.

Also confirmed stale (doc-fix only, not a defect): deploy.yml's inline comment predicting smoke will FAIL LOUD for want of secretAccessor -- the binding exists and the cited CI run succeeded.

## B-WATCHDOG-LIT — CONFIRMED (must merge before C1-REBUILD)

VER reproduced the F3 defect itself, the hard way: reverted the classifier to its pre-fix predicate, ran it against a REAL throwaway Postgres with migration 474's actual CHECK constraint and the REAL route handler, and watched it wrongly promote a 78/303 asset to 'lit' and flip build_run_assets to 'complete' -- then restored the fix and watched both correctly withhold. Property test confirmed genuinely exhaustive over its declared 48-combination input space and correctly one-directional (new-promotes subset-of old-promotes = strictly more conservative) -- noted as a non-regression proof, not fail-then-pass evidence in itself; the 3 dedicated withhold tests carry that standard. plan.ts corollary confirmed necessary (without it, withheld assets become unreachable by the Build button) and a verified no-op against current production (zero incomplete rows exist today). Read-only production census confirms no asset is currently falsely-lit mid-plan; ka_gochara_sweep's 3 charts read lit@303/error@78/error@70, corroborating A6-GOCHARA-DIAG independently. Regressions clean (tsc + 588 unit tests).

STATUS: done -- eligible for merge queue, VER recommends it take MERGE-LOCK ahead of C1. PR #906 will need a rebase since origin/main may have moved again.

NEW FINDING from VER (relevant to C1 and C3-BUILDSTATE-RECON): all 9 OTHER has_substeps=true assets (ga_condition, ga_dashas, ga_medical, ga_prashna, ga_transit_anchors, ga_vargas, ga_vastu, ga_vichara, ga_yoga) are currently lit across all 3 charts with substeps_committed=0 -- build_substep_progress holds zero evidence for them. Two consequences: (1) during C1, if any goes building and its heartbeat goes stale, this fix will correctly mark it incomplete rather than lit -- C1 should EXPECT to produce incomplete assets needing an explicit follow-up build pass; this is the fix working, not a failure. (2) C3-BUILDSTATE-RECON must not read "0 committed substeps" as "incomplete" for these 27 rows, nor assume the table is authoritative for them -- completeness is simply unverifiable from build_substep_progress for this whole class.

Minor notes: could not map builder's "96/96" test claim to a reproducible suite (ran broader platform tests/unit instead, 588 passed) -- not a discrepancy, just don't quote "96/96" in the close report without a named scope. Migration 474 exists only in platform/migrations/, not platform/supabase/migrations/ -- consistent with the two-directory split B-MIGGUARD documents, worth one line there.

## A2-CI-POINTERS — CONFIRMED (reopen cycle 1 resolved)

VER's original refutation (get_signals live "Tool not found" despite static-scan FIXED claim) is fully resolved -- and the fix went further than the refutation demanded. VER independently modelled the root cause (RC-14 runtime gate retiring 43 legacy names without removing their source call sites) and confirmed 167 scanned - 43 gated = 124 modelled, set-for-set IDENTICAL to the live catalog with zero drift either direction. VER's own live caveat (principal-gating) is dead: confirmed a first-party Bearer key resolves to 'full' profile which short-circuits applyProfileGate before wrapping server.tool, so 124 is the unfiltered truth. Ran the exhaustive test personally: all 97 non-comment pointer literals in the tree now name tools the deployed server actually serves, zero exceptions. Exercised the new live-catalog-parity check for real (not just SKIPPED) -- got a genuine PASS with credentials, then punched one name out of the gate set and watched it correctly FAIL (125 modelled vs 124 live). Caught and redid its own invalid first attempt at the fail-loud guard test (a renamed constant that still contained the substring being searched) -- second attempt was genuinely clean and confirmed the resolver throws rather than silently degrading to an ungated over-count. Regressions: identical failing-test-name sets, zero drift, on code touching production source.

STATUS: done -- eligible for merge queue at merge_priority 1, needs a rebase (base cdb6fc3b, origin/main has advanced).

Two flags from VER (neither blocking): (1) the LIVE half of the new parity check never runs in CI today (required env vars are exported by two other workflows, not the one that runs this harness) -- recommend a small follow-up B-lane to wire them in, not a merge blocker. (2) the brief's "152 tools serving" figure remains unexplained/unlocatable but is now MOOT for correctness -- 124 is verified authoritative from two independent directions; the brief's §0.2 and E1-SAMGATI 8.6 should reconcile against 124, not 152.

Process note: VER flagged it was verifying A2 in this pass, not B-N8-TS-SERVE, in case of any tick-ledger mislabeling from the connection-drop chaos -- noted, tick ledger will reflect the correct attribution going forward.

## B-N8-TS-SERVE — CONFIRMED

VER re-derived all 4 findings, reproducing 2 from the pre-fix side as requested. Scope guard verified directly (initially a false alarm from a substring match on "envelope.ts" catching kala_envelope.ts too -- self-corrected via exact-path disambiguation; only kala_envelope.ts is touched, B-VERIFSTATUS-VOCAB's exclusive surfaces genuinely untouched). F-22 confirmed as the standout: query_ucd.ts catches its own errors and RETURNS {is_error:true} rather than throwing, so the try/catch the old orientation_ok:true depended on could never engage for the ordinary DB-failure path -- reproduced the false-green directly (forced pre-fix semantics, 7 tests failed with "expected true to be false", reverted clean). F-24 confirmed via the self-documenting test that asserts the OLD predicate held true (40>=2) while both of a turn's actual messages were lost -- reproduced by reverting to the unscoped predicate, 2 tests failed, reverted clean. Regressions: platform-mcp A/B diffed by test NAME not count, zero drift; touched platform areas (persistence/pipeline/api-chat/chat-v2) 556 passed/0 failed; tsc clean both projects.

STATUS: done.

Two honest limitations VER stated rather than glossed: (1) could not complete the FULL platform vitest suite (exceeded a 10-minute command timeout) -- verified the 4 touched areas exhaustively instead; the builder's full-suite 7304->7315/0-failed claim is "plausible and consistent with everything measured, but not independently re-derived" -- would need a CI run to fully confirm. (2) the scope-guard false alarm (self-corrected, described above) -- noted for transparency even though it resolved cleanly.

## B-SECRET-REDACT — CONFIRMED (including the Ruling 29 degradation-shape fix @ 8d69c239)

VER independently re-derived the ENTIRE 27-file reconciliation from scratch (not trusting either the incident doc or the builder's count): extracted every credential-shaped literal from the pre-fix tree, deduped by SHA-256 prefix, counted files per secret -- decomposes EXACTLY as 9+16+2=27, matching DVA's Ruling 30 conclusion independently via a completely different method. All three credentials confirmed at ZERO occurrences post-fix. Reproduced the self-caught regression in both directions: planted the first-draft raising os.environ["VAR"] form, watched pytest collection genuinely ABORT with KeyError (would have broken CI for every subsequent lane, not just failed a test) -- confirms this was a real, material catch, not overcaution. Verified the Ruling 29 degradation-shape fix empirically both ways: unset var -> new honest message; set-but-unreachable -> original message unchanged -- confirms the fix disambiguates only the intended case and, by returning before connect(), structurally closes the empty-conninfo hazard (not just mitigates it). Integrity: py_compile 18/18 clean, node --check clean, no history rewrite (merge-base confirmed).

STATUS: done.

VER carries forward 2 flags about the GATE (not this lane's work, already partially addressed by DVA Ruling 34): shad-darshana-circularity-guard.yml genuinely has no pull_request trigger (confirmed, matches DVA's withdrawal of Ruling 29's premise) -- no pre-merge signal existed and none was missed. NEW flag: the post-merge run on main invokes -m integration but that workflow provides NO DATABASE_URL of its own -- so even after merging, the circularity guard's integration half will just skip (with the new honest message) rather than actually execute. This means the guard's live check remains unexercised in CI regardless of this fix -- a pre-existing gap in SHAD-DARSANA's own workflow config, not something this lane caused or worsened. Worth DVA's awareness for the close report (report, don't fix -- SHAD-DARSANA-owned).

## B-AUTHNET-HISTORY — CONFIRMED (with a scope correction that strengthens, not weakens, the finding)

VER independently re-derived the 17/8 operation counts from the operations API directly, confirmed the record's completeness by checking the oldest operation (CREATE, 6 seconds after instance birth -- no retention gap), retrieved all 8 request bodies itself via a DIFFERENT instrument (Admin Activity audit logs, since operations describe doesn't carry request bodies) confirming zero ipConfiguration/authorizedNetworks keys in any, and ran its OWN independently-windowed audit sweep (4 targeted windows, different slicing than the builder's) which agreed exactly. Scrutinized the logical crux (patch-semantics = omission proves non-modification) and found the conclusion is actually MORE robust than framed: even under full-replace semantics, a value could only exist if some operation SET it, and none did -- so the load-bearing premise is really the COMPLETENESS of the operation record, which VER verified independently.

SCOPE CORRECTION (strengthens the finding, doesn't weaken it): VER's own sweep surfaced a transient sibling instance the original lane didn't mention -- a ~11-minute PITR test clone (amjis-pitr-test-v12) created via cloudsql.instances.clone on 2026-05-28, deleted shortly after. VER chased this down: all 6 audit entries for the clone instance contain no ipConfiguration/authorizedNetworks, and the clone request body specifies none either (inherited the source's empty networking). Recommends E1-SAMGATI state the finding as "no Cloud SQL instance in this project, INCLUDING the transient 2026-05-28 PITR clone, ever held an authorizedNetworks value" rather than resting on "only one instance exists."

STATUS: done. DVA Ruling 38's most urgent question resolves negatively; compromise assessment not overturned.

Caveat VER wants on record: this establishes the NETWORKING CONFIGURATION never permitted broad access -- it does not by itself establish no access occurred through other paths (e.g. IAM/service-account compromise, the ~10-identity finding from Ruling 36). E1-SAMGATI should not let these two claims blur together.

IMPORTANT PROCESS NOTE from VER: flagged that its own past verdicts cite PR numbers/content but rest on branch SHAs and commit contents, NOT PR descriptions -- so prior technical verdicts stand even if a PR body was swapped. Recommends treating PR descriptions as untrusted until re-verified (the Ruling 41 sweep). VER has NOT yet done the assigned exhaustive 19-PR-body check (deferred it behind B-VERIFSTATUS-VOCAB/B-SECRETSCAN-SCOPE/B-MCP-CATALOG-GAP/B-N8-CI-GATES in its stated queue) -- re-prioritized per DVA's explicit Ruling 41 ordering (should come right after B-AUTHNET-HISTORY, before other verifications).

## B-MIG474-COMMENT — CONFIRMED

VER confirmed the diff is exactly one line, and that line is a SQL comment (first 2 chars "--"). Went further than eyeballing: stripped comments/blank lines from both sides and compared executable SQL by hash -- byte-identical (sha256 4444c6ba25178ad3 both sides), proving zero logic change rather than asserting it. Confirmed no migration-numbering collision (grep across both directories, exactly one file numbered 474, nothing in supabase/migrations/). Confirmed the residual the STRONGER way: line 61's stale "mig 467" reference is not just a file comment but a string literal inside a COMMENT ON CONSTRAINT statement -- VER queried production directly (pg_constraint via obj_description) and confirmed the wrong number IS live in the production database catalog today, exactly as the builder claimed.

STATUS: done.

VER's suggestion for DVA (not a ruling, just a recommendation): the residual is low-stakes (descriptive metadata only, no behavior, the constraint itself is correct) -- fold the catalog-comment fix into whichever future lane next legitimately needs a migration, rather than spending a migration number on it alone.

## B-VERIFSTATUS-VOCAB — CONFIRMED (highest-stakes lane; claim 6 REFUTED, does not undermine the fix)

VER's PR-body cross-check (requested given the scratchpad incident hit this exact PR): CLEAN, every element of the FINAL_SUMMARY present in the live PR #910 body, claims and body agree, no residue from the collision.

VER independently re-derived the directional safety property against PRODUCTION, not accepted: confirmed all builder numbers exactly (415,166 total; 352,485 grounded before AND after; delta=0; 0 rows with lowercase 'pass' -- confirming WHY delta is 0; 5,428 uppercase PASS rows matching the residual). Then went FURTHER than the builder on the actual load-bearing question: independently re-derived the classical nakshatra/pada division in raw SQL from live sidereal longitudes and compared against stored engine attribution across ALL THREE CHARTS (not just the native's, which is all the builder checked) -- 300/300 claims agree, zero divergences. Confirmed the detector is a genuine second pass (doesn't call PyJHora, honestly bounded to exactly the 2 fact pairs it can adjudicate). Confirmed the settled vocabulary by importing it directly: 13 members, exactly 1 verified status, UNVERIFIED_DEFAULT resolves to 'single' which is NOT in the verified set (directionally safe). Ran the can-fail proof itself: reintroduced bare 'pass' into bo_upaya.py, watched the estate-wide lint genuinely go red (1 failed/24 passed), reverted, 25/25 clean. Confirmed commit ordering: predicate genuinely never lands before steps 1-2, despite steps landing in a different NUMERIC order than planned.

STATUS: done.

CLAIM 6 REFUTED (does not undermine the fix, but must not be treated as settled): the builder claimed "B-NAR-TS's only envelope-matching file is kala_envelope.ts, zero overlap, no rebase needed" -- VER determined this CONFLATES B-NAR-TS with a DIFFERENT lane, B-N8-TS-SERVE (which VER verified earlier and which DOES only touch kala_envelope.ts). The QUEUE's actual B-NAR-TS scope names envelope.ts:1416 AND register_p1_ganita.ts:374 -- BOTH files this lane (B-VERIFSTATUS-VOCAB) also modifies. VER notes practical risk is probably low (this lane's hunks are at different line numbers, ~40/157/1728 and ~35/717/1104, not adjacent to 1416/374) but B-NAR-TS hasn't been dispatched yet so true overlap can't be measured, only predicted from scope -- VER explicitly declines to rule on this itself and routes the corrected facts to DVA rather than accepting the builder's premise.

Residuals confirmed and endorsed by VER: the 5,428+4,119 stale rows persist until C1-REBUILD runs -- THIS LANE'S BENEFIT IS NOT REALIZED UNTIL THE REBUILD; classical_match correctly held out of VERIFIED_STATUSES pending its own earnedness audit (a widening needing its own directional check, correctly not done here).

## B-MCP-CATALOG-GAP — CONFIRMED

VER personally re-verified the load-bearing crux claim (not just relayed from its sub-agent): read censusServedTools()'s implementation AND the installed SDK's real tools/list handler source (@modelcontextprotocol/sdk 1.29.0) side-by-side -- confirmed they use the SAME source object and SAME filter predicate, so tool_count equals tools/list BY CONSTRUCTION, not by a parallel reimplementation that could drift. Independently re-measured the live catalog itself (124, matching prior A2/A3 measurements). Resolved the "167 vs 142" reconciliation question the sub-agent couldn't close: invoked the project's own authoritative resolver directly, got 167 scanned/43 gated/124 registered, internally coherent -- but confirmed this requires the specific resolver tool (handles 3 distinct registration shapes), a naive regex scan will never reproduce it (VER's own hand-rolled scan failed the same way the sub-agent's did). Concluded this is NOT a real discrepancy and is IRRELEVANT to this lane specifically, since the fix abandons static counting altogether. Personally re-verified 3 of the sub-agent's findings: the banner's factually-wrong example (assess_career/assess_wealth ARE in the live 124, confirmed); the dangling doc pointer (confirmed 0 hits on this branch or main, exists only on A2's branch); the hand-edited generated file (confirmed mitigated by generate_projections.ts being in the same commit, but no CI drift check enforces the pairing going forward).

STATUS: done.

3 non-blocking pre-merge suggestions from VER (cheap, comment-only/non-behavioral, not required to close): (1) fix the banner's factually-wrong example; (2) drop or make-conditional the dangling doc-pointer citation; (3) route "add a CI drift check that regenerates and diffs *.generated.ts files" to B-N8-CI-GATES or B-N8-LINT as a future finding (the detector-less-gate class those lanes own) rather than leave the generated/generator pairing on the honour system. None of these block the CONFIRMED verdict.

Sign-off on finding #4 (catalog_version showing a 152-derived fingerprint next to a 124 tool_count): VER is content, NOT an §N.8 violation (catalog_version is documented as a cache key, not a size claim; fields are separately named/explained) -- suggests the close report state both numbers side by side once so a future reader doesn't mistake the juxtaposition for a bug.

## B-DASHA-SENTINEL — CONFIRMED

VER independently queried production itself and confirmed the defect is genuine and still live (zero scope_cap rows across all 3 charts, pre-fix state). Confirmed root cause via call-graph (not description): grepped build_ga_dashas() across the pre-fix tree, confirmed it's referenced ONLY by the CLI path (build_runner.py) and its own definition/entry point -- NEVER by the orchestrator adapter, which calls build_system/_run_concurrency_post_pass_db directly. Confirmed §N.2 frozen-contract compliance by reading the actual connection-ownership code (nullcontext + commit=owns_conn pattern, exactly matching the "never commits/closes ctx.db_conn" rule). Performed the fail-then-pass itself: restored only the 2 source files to pre-fix, kept the new tests, watched 4/4 fail, restored, 4/4 pass. Noted the test set is well-chosen -- 2 of 4 specifically assert the connection-ownership contract, not just row presence.

STATUS: done.

VER STRENGTHENED the cross-reference to Ruling 16's chart_dashas -52,084 investigation: this lane PROVES the CLI and orchestrator paths DID diverge on chart_dashas writes, empirically, in production (though the magnitudes don't match -- 2 rows/chart here vs 52,084 there, so this is NOT the cause). Removes "the two paths are equivalent" as a safe assumption. C3-BUILDSTATE-RECON should treat path-divergence as a LIVE HYPOTHESIS, not a ruled-out one, when it investigates.

Also noted: the sentinel rows remain invisible to serving (get_dashas.ts doesn't recognize system_id='scope_cap') -- correctly out of this lane's scope, but means the fix's user-visible value is zero until a future lane wires it in; and like other rebuild_required lanes, the benefit doesn't land until C1-REBUILD runs.

VER also pre-ruled on B-PB6-GEOMETRY's deployed-route question (queued next): will NOT waive the live-geometry standard but will NOT block the lane on an impossibility either -- will confirm the structural/DOM-level claims now, record the live-geometry check as explicitly outstanding and assigned to itself post-deploy, sequencing naturally since C4-LOOP-LIVE-PROOF (which depends on B-PB6-GEOMETRY) is the lane that performs live proofs. B-PB6-GEOMETRY can merge but must not be counted as discharging PB-6's live-geometry acceptance until VER reads the deployed route itself.

## B-N8-CI-GATES — REFUTED (reopen cycle 1 of 3)

VER personally verified via gh pr checks 913: Stage 5 (Visual Regression) FAILS. Root cause confirmed from workflow source on both sides: the lane correctly removed Stage 5's `|| echo "no visual specs yet"` swallow (F-30, correct fix in principle) but Stage 5's env block only carries MARSYS_FIXTURE_MODE, while its sibling Stage 3 (which PASSES on the same Playwright config) carries 6 additional NEXT_PUBLIC_FIREBASE_* secrets that playwright.config.ts's webServer.env explicitly forwards with a comment explaining Next.js needs them to start without an auth/invalid-api-key error. Confirmed the actual failure: FirebaseError: auth/invalid-api-key -> webServer 120s timeout. The "no visual specs yet" message the old swallow printed was ITSELF FALSE (axe.spec.ts:79 carries a real @visual test) -- the swallow was hiding an environment gap, not an absence of specs.

VERDICT RATIONALE: merging as-is reddens chat-v2-ci.yml for EVERY SUBSEQUENT PR in the run -- exactly the masked-signal condition A2-CI-POINTERS's whole lane exists to eliminate. VER will not confirm a CI-integrity lane that degrades CI integrity.

REST OF THE LANE EXPLICITLY NOT RE-LITIGATED, strong work: F-26 re-confirmed as "the finding of the run so far" (hard_gates_check.sh had ZERO exit statements, printed RED and returned 0 -- a gate that announced its own failure and passed anyway); F-27's regex fix re-confirmed load-bearing (old \bFORENSIC\b couldn't match FORENSIC_v8.0 due to word-boundary, ALL 1211 citations in MSR_v5_0.md use that style, reverting the regex alone reddens 563 of 569 signals -- the old gate passed vacuously over 0 parsed blocks); F-32 promotion coupling re-confirmed real; 216/43 baseline re-confirmed a THIRD independent time (delta zero by class AND path).

STATUS: reopened. Reopen cycle 1 of 3.

REQUIRED TO FLIP TO CONFIRMED: (1) add the 6 NEXT_PUBLIC_FIREBASE_* secrets to Stage 5's env block mirroring Stage 3, show Stage 5 green on the PR -- OR if it genuinely can't pass yet for a substantive reason, re-declare it explicitly as skip-with-stated-reason-and-expiry, NEVER restore the || echo swallow. (2) strongly recommended: F-27 has no regression test (existing icr_pr_gate.test.ts is blind to it, reverting the regex leaves 13/13 passing) -- a 563-signal regression is currently invisible until the Sunday cron; a single test asserting a FORENSIC_v8.0-style citation is grounded would close this gap.

3 findings routed for future attention, not required for this reopen: F-27's fix drops the trailing \b so "FORENSIC_GAP:" now also counts as grounding (checked, no live false-green today, but latent); generate_tool_census.ts (F-21) and hard_gates_check.sh (F-26) are not wired into ANY workflow, so both corrected surfaces are currently unreachable by CI; ci.yml's icr-pr-gate job remains continue-on-error:true with --dry-run --synthetic, so the PR-time ICR surface is still a no-op by design.

## B-OIR-RECONCILE — CONFIRMED

VER independently re-derived the headline B2/B3/B4 guard discharges: B2 confirmed at BOTH role and cluster level (pg_roles.rolconfig AND pg_settings default, both 600s) -- stronger evidence than the builder's own cited pg_stat_activity snapshot, which VER found to actually show 3 transient idle-in-txn connections (not 0) at the moment VER checked -- does NOT refute B2 since the load-bearing evidence is the CONFIGURED timeout not a point-in-time count, but VER flagged this as an evidence-quality issue: the register should cite rolconfig/pg_settings, not a fragile snapshot count that won't reproduce. B3/B4 confirmed directly from source (finally/rollback blocks at specific line numbers, the actual thread-pool constructor call with its documented width constant).

Confirmed the two-dot/three-dot diff distinction is real and consequential -- the two-dot diff would show a phantom SHAD-DARSANA file (not touched, an artifact of origin/main advancing after the worktree was cut) that a careless reviewer could mistake for a §7 violation; three-dot (correct) confirms exactly one file touched, zero §7-path hits.

Confirmed R-6 (OCR-noise) directly against production: queried all 5 named garbage canonical_ids, all 5 are live rows with real content (dariclra/datidra both confirmed as OCR corruptions of the SAME yoga, "Daridra" -- two different corrupted spellings occupying separate canonical rows for one real yoga; "another" is literally the English word lifted from prose, not a yoga at all). Corroborating metrics: 124/186 rows have category='other' (a dumping-ground signal), 13 have literal embedded newlines from raw OCR line breaks surviving into primary keys.

STATUS: done.

VER honestly flagged 2 claims it could NOT independently verify (not refuting, just not confirmed by VER personally): the specific "89 of 186 outside YOGAS_CORE" count (confirmed the 186 total and the 5 named examples, didn't enumerate full YOGAS_CORE membership); C4's "15 rows/11 assets" figure (reproduced the sha256('')[:16] constant itself, but upstream_hash isn't a column on asset_throughput -- couldn't locate which table holds the count, flagged for whoever picks up C4 to re-establish).

VER ESCALATES R-6 beyond "residual": "OCR noise admitted as canonical identifiers in the yoga catalog the instrument treats as classical authority... against CLAUDE.md §J's acharya-grade standard, serving these as classical material is precisely the failure mode that standard exists to prevent, and against §B.10 they are fabricated entities with real-looking citations." Suggests DVA consider whether brahma_yoga_catalog needs a validation GATE before C1 (distinct from the REBUILD FIX Ruling 48 already parked) -- i.e. should C1's other writers be prevented from reading/propagating from a catalog known to be corrupted, even while the catalog's own repair stays parked.

## B-LOOP-G4G5 — CONFIRMED (full verdict; G5d premise-refutation independently confirmed)

VER reproduced both can-fail proofs independently. G4: reverted actions.ts to pre-fix, 4/12 assertions correctly went red (including the exact named findings: "only file carrying an outcome map is outcome_calibration.ts" and "recordConversationalOutcome has a non-test caller"), restored 12/12 clean. QUALITY SIGNAL noted: all 6 no-false-positive assertions stayed GREEN under the revert -- the detector discriminates on the SHAPE of an outcome->number map, not keyword proximity (doesn't flag an outcome-literal list, an outcome->string label map, or a Zod enum of the vocabulary). G5: reverted calibration_leak_guard.ts to pre-fix (narrower mutation scope than the builder's own stash, explicitly reconciled as a benign discrepancy not a contradiction), 5 assertions correctly went red, restored 36/36 clean. Confirmed the emitter test is BEHAVIORAL not structural -- drives contaminated events through the REAL PariprashnaEmitter and asserts BOTH CalibrationLeakError thrown AND controller.enqueue never called (the leak is stopped before the wire, not just detected).

G5d premise-refutation: VER traced the mechanism to source (no_leakage_filter.ts + generate_projections.ts's explicit "excluded from all three profiles" comment) and confirmed applying G5d as briefed would have deleted query_spine_bundle from all 3 MCP surface profiles -- the builder's park is correct, the 4th decayed brief-premise this run. VER disclosed its own first reading was wrong (initially read the flag as governing only planner-selectability).

STATUS: done.

Precision correction for the eventual reversal spec: query_spine_bundle is in the PROFILE MANIFEST (4 occurrences) but NOT in the live 124-tool served catalog, NOT registered via server.tool/regAlias/globalAlias, NOT in the RC-14 gate set -- the same manifest-vs-served gap B-MCP-CATALOG-GAP established. Spec should say "profile manifest" not "live profile."

Flagged for E1-SAMGATI: the tool-count reconciliation now has THREE numbers in play -- 124 served / 152 committed manifest / 158 regenerated (no-source-change) -- E1 should not treat any single one as "the" tool count without specifying which population it means.

## A8-NAR-TRIAGE — REFUTED (scoped to Claim 4 only; reopen cycle 1 of 3)

VER independently re-derived all 5 claims. CONFIRMED: Claim 1 (4 already-closed items, all SHAs verified live on origin/main, content re-derived at each symbol, not read from commit messages); Claim 2 (7 PLAUSIBLE re-examinations -- 2 rejections sound on independent reproduction, 1 rejection over-claims its supporting argument but reaches the right empirical conclusion, both escalations independently reproduced live with quantified evidence); Claim 3 (F18 CANNOT-REPRODUCE reversal -- genuine, reproduced on both built charts, every refutation path closed; one wording correction required, see Ruling 75).

REFUTED: Claim 4, the 42-path zero-overlap partition gating all 6 B-NAR-* lane dispatches. The intra-partition arithmetic is sound (42 distinct paths, zero internal duplicates, independently parsed) but two load-bearing statements are false: (a) §4.3 assigns ga_nakshatra.py:87 to B-N8-FIX, contradicting Rulings 13/13-CORRECTION (that site belongs exclusively to B-VERIFSTATUS-VOCAB, PR #910, already merged/CONFIRMED) and would orphan :289; (b) §4.9 claims zero cross-lane file intersection anywhere -- VER found 8 real collisions against 3 already-built lanes (#908/#909/#910), only 2 of which have any existing ordering ruling.

Claim 5 (3 DVA questions routed): 1 of 3 was already answered (Rulings 13/13-CORR, just not connected by the builder); the other 2 were genuinely never ruled -- now decided at Ruling 75 (F18 wording accepted with correction; both escalations ratified, one to P1, one to P0).

DISPOSITION: Ruling 74 -- reopen A8-NAR-TRIAGE with this refutation as first input, targeted correction (rewrite §4.3 + §4.9, re-derive registry_bridge.ts citations, add 2 omitted test files) not a full redo. Do NOT dispatch any B-NAR-* lane against the current §4 as written.

## A8-NAR-TRIAGE reopen cycle 1 -- COMPLETE, awaiting VER re-pass

Branch samapti/nar-triage-fix @ b0a9f70b, PR #956. Corrected §4.3 (ga_nakshatra.py:87 now excluded from every lane, owned by #910 per Rulings 13/13-CORRECTION; :289 restored to B-NAR-GA), §4.9 (real 9-collision/8-path set re-derived live against #908/#909/#910's actual diffs), F18 wording, both severity escalations. Path count corrected 42->45 (not 44 as Ruling 74 itself miscalculated -- the builder caught this). Builder also caught two errors in this ledger's own Rulings 74/75 (see DVARAPALA Ruling 77): #910 is VER-CONFIRMED but NOT merged (contradicts what Ruling 74/75 assumed), and the bo_cdlm_summary.py P0 belongs to B-NAR-BO not B-NAR-MI. Not yet VER-repassed.

## B-N8-FIX (+ Ruling-76 TS bundle) -- CONFIRMED (6/6 claims)

Branch samapti/n8-detectors @ 2a1927b8 + samapti/n8-detectors-tsfix @ 5bb64a15, PR #952. VER independently re-derived all 6: ga_nakshatra.py correctly untouched (confirmed via diff, cross-checked against Rulings 13/13-CORRECTION); 4 bo_pramana_mapa.py detectors are real (not tautologies) -- VER ran its own additional mutations beyond the builder's own set (deleting bodha_rm_resonances, deleting bodha_cgm_nodes) and both independently produced correct red; bo_chart_gestalt.py verdict-storage removal confirmed against the actual diff; fail-then-pass reproduced live against a real Postgres 15.17 instance (16/16 fail pre-fix, 16/16 pass post-fix); zero orchestrator-contract violations (grep-verified); F-13 residual correctly left out of declared scope. VER ran a PRODUCTION-SAFETY check beyond the builder's own evidence: all 8 detector SQL constants executed read-only against the live canonical chart, zero halts, zero false-reds -- the builder's own test schema diverged from production's column types (UUID vs TEXT) and this was independently caught and cleared. One routed item (not a refutation): B-N8-FIX's new `_VERIFICATION_TIER_RANKS` plants a second vocabulary definition that #910 (B-VERIFSTATUS-VOCAB, chartered to be the single source of truth) will need to reconcile -- routed, not blocking.

## B-N8-LINT -- CONFIRMED (5/5 claims)

Branch samapti/n8-lint @ 9b6518e7, PR #954. VER reproduced all three can-fail proofs independently (synthetic injection, max_occurrences freeze, self-test mutation), verified the scope-honesty claim empirically in both directions (4 known-uncaught findings genuinely not flagged; F-11 genuinely rediscovered blind), confirmed CI wiring mirrors the sibling fact_key lint structurally, confirmed the 169/81 allowlist is real data with 51/81 entries honestly labelled "NOT INDIVIDUALLY REVIEWED" rather than fabricated. Two cosmetic numeric nits (N1/N2 header comments off by a measured amount, no enforcement impact).

## B-N8-SWEEPFIX -- REFUTED (6 of 7 claims confirmed; real CI is red)

Branch samapti/n8-sweepfix @ 4c318352, PR #953. CONFIRMED: F-01 runner.py fix (bounded in both directions -- widening OR narrowing the fix both correctly break it); F-05 staleness.py fix (proven against a real Postgres 17 instance, pre-fix reproduced the false from_state claim); the "zero automated invocation" premise for dag_edge_guard.py/kala_derivation_completeness_guard.py; F-04/F-06 service_probes.py (VER independently re-derived every ephemeris figure via swisseph itself -- the register's own diagnosis was wrong, the builder's JD+ayanamsha fix is right, confirmed to the decimal); zero orchestrator-contract violations; the asset_runner.py scope decision.

REFUTED: the CI-wiring half of the dag_edge_guard.py/kala_derivation_completeness_guard.py fix. Both now do `from .db import connect` at MODULE level, but the CI job (`governance-gates`) installs only pyyaml+pytest before the self-test step -- psycopg isn't available yet. VER reproduced this in a clean venv, THEN confirmed it live: real CI run 30582248535, Governance Gates job = failure, ModuleNotFoundError: No module named 'psycopg'. Both new "hard gates" abort on import and never execute a single check -- for a lane about detectors-that-never-run, shipping two CI steps that cannot run is exactly the failure mode in miniature. VER also caught the builder's cited test count (4530) was one commit stale (4540 is current, delta = the new service_probes tests added in the final commit).

FIX IDENTIFIED (verified working by VER): defer `from .db import connect` into the function body of `analyze()`/equivalent in both files, rather than module level. One line per file. Reopen cycle 1 of 3.

## B-PB8-BYTEEQ -- CONFIRMED (lane scope); Ruling-54 capture posture gap named separately, not absorbed

Branch samapti/pb8-byte-equality @ 84555903, PR #927. VER confirmed: the fixture-corpus bridge is real (492-line bridge module, 13 authored per-type mapping decisions, exact drop-tally assertion, explicitly avoids the previously-rejected c2ProtocolAdapter.ts path); the real-reading stream-capture mechanism is genuinely wired (route hook, migration, flag, verify script) not a stub, and its unapplied-on-production state is honestly disclosed, not hidden; the gate itself can go red -- VER ran its own two mutations (corrupting the production writer path -> 12/23 red with full diff; restoring a wire-order interleaving defect -> exactly 1/23 red, precisely reproducing the builder's own honest disclosure that the 12-fixture corpus lacks discriminating power for the ordering contract). Per Ruling 53, live production verification is correctly out of this lane's scope (C4's job).

Separately found and NOT folded into the CONFIRMED verdict: the lane does not meet DVA Ruling 54's standing low-sample-rate capture posture (issued after this lane's commits) -- default sample rate is 1.0 not low-rate, retention is column-bounded but not cron-swept, capture-state observability reads the wrong (local, not deployed) environment, no force-capture path exists. Disposition at Ruling 80: named PARKED-HONEST follow-on, capture flag stays OFF until closed.

## B-SECRETSCAN-SCOPE -- REFUTED (detection logic CONFIRMED; 2 merge-blocking defects)

Branch samapti/secretscan-scope @ 866600a4, PR #911. VER independently re-derived the repair itself as genuinely sound: scope inversion (git ls-files-based, 8742 files vs the old 8-item allowlist, self-test hard-fails under 100 files), the pattern-shape gap fix (env_read_literal_default, exempted from the old os.environ blanket tolerance, catches the real v13_production_gate.py defeating instance), commit-SHA provenance with dirty-tree detection, and a composite can-fail proof VER ran itself (4 synthetic plants, old scanner falsely PASS with 3/4 planted, new scanner correctly catches all 4, reverts clean). 27-file coverage exactly matches Ruling 30's authoritative census with exact set equality.

REFUTED on two real, non-cosmetic defects: (1) the PR renames the CI job, breaking main's branch-protection required-status-check binding (the renamed job never reports under the required context, setting up a permanently-pending check and an obvious pressure to weaken branch protection to compensate); (2) a fresh VER census against CURRENT origin/main (not the stale PR base) found a 28th credential-bearing file has landed since this branch cut -- the scanner correctly catches it (proof the repair works), but the register is one row short, and strict branch protection means this would surface as an unexpected red after the mandatory pre-merge rebase. Ruling 82: reopen cycle 1, both fixes narrow and mechanical, detection logic untouched.

## B-DOCS-GOVERNANCE -- PARTIAL (4/7 threads CONFIRMED, 3 REFUTED as staleness-against-later-rulings, not fabrication)

Branch samapti/governance-docs @ b3e0d510, PR #928. CONFIRMED: the CLAUDE.md §N.4 doctrine rewrite (near-verbatim to Ruling 58's mandated text, §N.8 byte-identical confirming Ruling 15's boundary respected, versions/changelog correctly bumped); the ONGOING_HYGIENE_POLICIES cross-reference-only claim (re-grepped, zero independent restatement); the coordination-check accounting (12 itemized rows, spot-checked citations resolve); the governance-count re-derivation (43/216 reproduced exactly at this branch's tip).

REFUTED, all three as chronological staleness (the lane committed before the rulings that supersede these sections, not fabrication): the PB memo index's row 6 falsely claims BRIEF_PB-3.md was "never committed" when A1-PRESERVE recovered and merged it hours earlier; PB_CAMPAIGN_CLOSE's §1/§4 call B-LOOP-G4G5/B-PB6-GEOMETRY "not yet dispatched" when both now have open PRs (#925/#920), and §4 routes a question to DVA that Ruling 55 already answered; §7's INF-3 disposition is stated as "no ruling issued" when Ruling 56 closed it, and misses Ruling 56's own standing-practice correction (of which this very document, having renamed REPORT_PB.md to dodge the write-block filter, is the instance).

DECISIVE ADDITIONAL FINDING: VER's fresh governance-count run against CURRENT origin/main (not just this branch) found 45 violations, not the Ruling-4 ceiling of 43 -- this PR's own frontmatter fixes are the only queued remedy, and the PR is in merge CONFLICT on exactly those two files. Ruling 81: reopen cycle 1 (5-item mechanical fix list), merge priority ESCALATED given the live ceiling breach, conflict resolution must keep PR #928's frontmatter.

## B-N8-SWEEPFIX reopen cycle 1 -- CONFIRMED (7/7, all claims now stand)

Branch samapti/n8-sweepfix @ 0bd38151, PR #953. VER re-check reproduced the exact fix: import moved from module level into analyze() in both dag_edge_guard.py and kala_derivation_completeness_guard.py. Reproduced the ORIGINAL failure in a fresh CI-matching venv (pyyaml+pytest only) on the pre-fix tree (ModuleNotFoundError, exact match to the real CI log), then confirmed the fix tree passes clean in the same venv with substantive self-test output (not vacuous). Confirmed psycopg/`.db` genuinely absent from sys.modules immediately after import (not merely deferred-looking). Confirmed the live-scan path (fresh_chart_smoke.yml's actual usage) still resolves correctly -- analyze() reaches a real connection attempt, not an ImportError. Zero regressions: guard tests 8/8+1skip identical pre/post fix; broader orchestrator selection 328/328+2skip identical; full governance-gates pytest command 4540 passed/12 skipped/0 failed. Flagged (not blocking): runner.py and global_runner.py carry the same module-level-import pattern but aren't invoked by any pre-requirements.txt CI step, so not a live red -- worth a future lane. Lane is merge-ready.
