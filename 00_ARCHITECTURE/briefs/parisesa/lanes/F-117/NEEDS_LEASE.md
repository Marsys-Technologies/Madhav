PAR-F117-NEEDS-LEASE

Confirmed (S3 Stage-D, final): F-117's real mechanism is `platform/python-sidecar/bodha_writers/formulas.py::resonance_score_v1()` (the formula, not the bug) plus `platform/python-sidecar/pipeline/orchestrator/writers/bo_upaya.py::_build_resonances_and_prescriptions()` (the actual input-supply defect, ~lines 1153-1183). This is genuinely **L2 Bodha**, outside S3's `L4_phala/**` / `L5_mimamsa/**` / `ph_nimitta/**` / `muhurta.py` lease.

Requesting: finalize the tentative S3-specs -> [Bodha-owning stream]-builds routing already noted in LEASES.json. S3 will write SPEC.md for F-117 under Stage S; the builder needs conductor's confirmed re-lease of `bo_upaya.py` / `bodha_writers/formulas.py` (or equivalent) before Stage B.

Full diagnosis: `lanes/F-117/DIAGNOSIS.md`.
