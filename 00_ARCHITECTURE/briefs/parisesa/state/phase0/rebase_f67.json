{
  "finding_id": "F-67",
  "outcome": "NEEDS_FRESH_BUILD",
  "pr_url": null,
  "branch": null,
  "test_results": "Not run -- no candidate code exists to test. `git merge-base --is-ancestor origin/par/s1-f67-register-pratijna origin/main` returns true: the branch tip (7459f8837, dated 2026-08-16, message \"ekv(b-01): F-72 -- dignity oracle module + moolatrikona degree gate in 3 consumers (#1296)\") is already fully contained in origin/main's history. `git diff origin/main...origin/par/s1-f67-register-pratijna --stat` (three-dot, against merge-base) is empty -- zero unique commits, zero line diff. The branch is not a stale-but-real F-67 implementation; it is a bare pointer sitting at an old point on main's own mainline, whose tip commit is someone else's F-72 work, not F-67 work. It was never advanced with any F-67-specific commit. Confirmed no PR exists either: `gh pr list --search \"F-67\"` returns nothing. `git grep -n \"bodha_pratijna_get\" origin/main -- platform-mcp/src/tools/register_p1_aliases.ts` and a repo-wide `server.tool('bodha_pratijna_get'` grep both return zero hits on current main, confirming the underlying finding (tool descriptor'd + bridge-aliased but never registered) is still live and unfixed.",
  "review_notes": "Sibling branch origin/par/s1-f67-register-pratijna has no usable content -- ruled out, not a rebase candidate. However the ingested SHAD review corpus at /Users/Dev/shad_overnight/par-night/results/ingested/F-67.{reviewer,ratifier,reviser}.json contains a fully worked, independently ratified SPEC.md (verdict COMPLETE, ratifier-2 spot-checked source lines and held, reviewer traced Q1-Q7 and held) for a narrow, low-risk fix: add one new `server.tool('bodha_pratijna_get', ...)` registration block to platform-mcp/src/tools/register_p1_aliases.ts, modeled on the existing bodha_signals_get block (lines 530-604 as of the review pass), routing through the already-correct tool_name_bridge.ts:592 alias to the already-wired marsys://tool/L2/query_pratijna capability. Root cause: the capability is fully descriptor'd (mcp_surface_profiles.generated.ts:4688) and bridge-aliased but the server.tool() registration call was simply never written (confirmed by exhaustive grep, 26 registration files checked, zero hits) -- a pure omission, pure-addition fix, no existing call sites touched, reviewer/ratifier both assessed cross-lane regression risk as nil. This is real, reviewable, buildable content -- but it lives only as a spec/diagnosis inside the SHAD corpus, not as an actual commit on any branch or PR. Per this train's scope (review + rebase existing sibling-branch candidates only, WIP-3-of-3, no fresh multi-stage build authorized this session), building this fresh from spec is out of scope for a REBASE job and was not attempted. Did not create a worktree since there was nothing to rebase (three-dot diff is empty) and no working tree was needed to reach this conclusion.",
  "next_action": "Hand to a BUILD (not rebase) train: the F-67 SPEC.md content is ready-to-implement and already ratified COMPLETE by an independent reviewer+ratifier in the ingested corpus (/Users/Dev/shad_overnight/par-night/results/ingested/F-67.reviser.json carries the exact code block). A build train should implement that spec verbatim against current origin/main (line numbers in the spec are stale -- register_p1_aliases.ts has moved since the review pass and must be re-located before insertion), add/confirm a test exercising bodha_pratijna_get end-to-end, and open a fresh PR. Update ledger.json F-67 disposition from STRANDED to reflect that no rebase candidate exists and a fresh build from the existing ratified spec is the correct next step."
}
