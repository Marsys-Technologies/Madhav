{
  "finding_id": "F-129",
  "outcome": "PR_OPENED_FROZEN",
  "pr_url": "https://github.com/Marsys-Technologies/Madhav/pull/1389",
  "branch": "parisesa/repair-F129-build",
  "test_results": {
    "spec_verification": "Read F-129/{DIAGNOSIS,SPEC,REVIEW}.md in full (coord-wt worktree). Spec was ratified COMPLETE by ratifier-1 (draft COMPLETE by pool-1) with no open questions -- REVIEW.md Q1-Q5/Q7 all pass, sibling census complete (0 additional defect instances), citations verified against main-ro. Re-verified every cited line independently against current origin/main (dc78e82df, fetched fresh) before touching code: register_p1_synthesis.ts:816 (surface_reading AS statement) and :868 (top_discoveries: discResult.rows) matched exactly; bo_anveshana.py:377-433/499/575/638 matched exactly; query_discoveries.ts:110-114 sibling-correct-pattern matched exactly. Zero drift found -- spec was safe to implement literally.",
    "fix": "platform-mcp/src/tools/register_p1_synthesis.ts discResult query (lines 814-822): replaced `surface_reading AS statement` with honest-named `hypothesis_text, depth_reading, why_an_acharya_misses_it, surface_reading, discovery_class, discovery_subsystem` -- exactly per spec diff. Serving-layer TS change only, no migration, no rebuild.",
    "regression_test": "Added platform-mcp/src/__tests__/f129_top_discoveries_honest_fields.test.ts (vitest, not jest -- the spec's proposed `npx jest --testPathPattern` command does not apply to this package, which uses vitest exclusively; adapted to match repo test convention/location, same assertions). Verified genuinely red pre-fix: stashed the source fix, ran the suite, all 4 assertions failed as expected (surface_reading AS statement present, hypothesis_text/depth_reading/why_an_acharya_misses_it absent from the query block). Restored fix, reran: 4/4 PASS.",
    "typecheck": "npm run typecheck (tsc --noEmit) -> 0 errors",
    "related_tests": "mc015_bodha_discoveries_family_wiring.test.ts, r6_0b_deadtools_smoke.test.ts, composition_doctrine.test.ts, mc010_zero_support_verb_mask.test.ts, f10_prashna_undertaking_domain_filter.test.ts (all touch register_p1_synthesis.ts / synth_chart_brief_get) -> 20/20 PASS",
    "full_suite": "Ran full platform-mcp vitest suite twice: once on unmodified origin/main (93 pre-existing FAIL lines captured), once with the F-129 fix + new test applied (93 identical FAIL lines). diff of the two FAIL-line sets is empty -- byte-identical failure set both before and after. All 93 are environment-dependent (DB/sidecar network calls unavailable in this sandbox: kala_timeline, phala_muhurta, shad_darshana, gochara_windows, registry_bridge, etc.), unrelated to register_p1_synthesis.ts. Fix introduces zero new failures and fixes nothing outside its own scope."
  },
  "review_notes": "Independent review pass on own diff, looking specifically for a stronger invariant the literal spec might conflict with (per this campaign's F-125 precedent): (1) Consumer check -- top_discoveries has exactly one reference in the whole codebase (the assignment itself at line 868); no downstream TS/TSX consumer or contract depends on the removed `statement` key, so the fix is non-breaking. (2) Density/budget doctrine (CLAUDE.md SS N.6) -- dualOutput() in this same file already runs every response through applyAutoBudgetToEnvelope/autoDetectTrimmableSections, which generically auto-trims any array field with >10 rows; top_discoveries (20 rows at depth=complete) was already covered by this generic mechanism before the fix and remains covered after -- the added, longer text fields don't need a new hardFloor declaration or any budget-doctrine change, the existing generic trim already protects against oversized responses. (3) Blast-radius re-check -- F-135 (ranked_themes.weaknesses, same file, currently open as PR #1385) is built by a disjoint function (buildRankedThemes) over a different source table (mimamsa_insight_units, not bodha_discoveries) at a different line range; confirmed no overlap, no merge-order dependency. (4) Sibling pattern fidelity -- fix selects the exact same honest column set query_discoveries.ts already uses for the identical table, so no new synthesis/inference logic was introduced (holds SS N.5 -- L1/L2 authority is referenced, never re-derived). No literal-spec-vs-invariant conflict found this time; the spec was implementable as written. The one real spec inaccuracy caught was mechanical, not architectural: the exit-test command in SPEC.md (`npx jest ...`) targets the wrong test runner -- this package's package.json declares `\"test\": \"vitest run\"` and has no jest devDependency at all. Adapted the test to vitest with the same assertions, placed at the repo's actual test-file convention (platform-mcp/src/__tests__/f129_*.test.ts, matching sibling f10_/mc010_/mc015_ etc.) rather than the spec's proposed platform-mcp/src/tools/__tests__/F129.exit.spec.ts path (which doesn't match any existing test location in this package).",
  "next_action": "None from this session -- PR #1389 frozen open on main (not merged), ready for the campaign's normal merge/ratification path in a later phase. No further F-129 work needed unless review surfaces new findings."
}
