{
  "finding_id": "F-69",
  "outcome": "PR_OPENED_FROZEN",
  "pr_url": "https://github.com/Marsys-Technologies/Madhav/pull/1386",
  "branch": "parisesa/repair-F69-rebase",
  "test_results": {
    "targeted_suite": "npx vitest run src/lib/retrieval/registry/layers/L5_mimamsa/__tests__/query_insights_p3b_suppression.test.ts — 3/3 PASS (structural->suppressed, prior_only->suppressed, empirical->pass-through)",
    "layer_suite": "npx vitest run src/lib/retrieval/registry/layers/L5_mimamsa/ — 120/120 PASS, 12/12 test files, no regressions",
    "typecheck": "npx tsc --noEmit -p . — clean, 0 errors (verifier_v.json from the original ingest had noted 1 pre-existing unrelated error in test-setup.ts:17; that error is no longer present on current main, so this rebase is strictly cleaner)",
    "lint": "npx eslint on the 3 touched files — 0 errors, 1 pre-existing unrelated warning (unused _ctx param, not introduced by this change)"
  },
  "review_notes": "Original commit c872d8372 on origin/par/night-F-69 (base: platform/src/lib/retrieval/registry/layers/L5_mimamsa/{query_insights.ts, query_insight_embeddings.ts} + new test file) had already cleared the campaign's own review/ratify/verify pipeline (reviewer verdict COMPLETE with line-by-line source verification; ratifier verdict COMPLETE; verifier_v PASS, tsc PASS, 3/3 exit tests). The ratifier had flagged one real risk: F-35 ('earn the empirical evidence_grade — scored_count gate + pass-through fix', commit 355be01df, merged to main via batch-4 PR #1316) touches the exact same return block in query_insights.ts (adds `evidence_grade_counts`), creating a rebase-conflict risk requiring conductor sequencing. Confirmed F-35 had in fact landed on main since F-69 was authored. Did a fresh worktree rebase of origin/par/night-F-69 onto current origin/main (dc78e82df, tip as of 2026-08-20): `git rebase origin/main` completed with ZERO conflicts — git's own 3-way merge correctly interleaved F-69's `suppressIfNotCalibrated` wrapper around `insight_units` with F-35's `evidence_grade_counts` block. Independently re-verified post-rebase semantic correctness (not just clean merge): (1) `evidence_grade_counts` is computed from the raw pre-suppression `insightResult.rows`, which is correct since the counts should report the true evidence_grade distribution, not a suppressed view; (2) F-35 introduced a new 'assignment_only' evidence_grade tier (opp>=5 but scored<5) that did not exist when F-69 was authored — confirmed F-69's `EMPIRICALLY_CALIBRATED = 'empirical'` / `!== 'empirical'` predicate correctly treats 'assignment_only' as non-calibrated (suppressed), which is the correct behavior, not an oversight; (3) re-confirmed query_insight_embeddings.ts's untouched mode=lookup branch never selects rank_consequence at all, so F-69's scoping of the embeddings fix to mode=nearest only remains complete and correct on current main. No regressions found in the surrounding 120-test L5_mimamsa suite. The fix holds up; this is a legitimate rebase-and-refresh, not a reauthoring.",
  "next_action": "PR #1386 is open against main and left FROZEN (not merged) per the campaign's overnight scope boundary. No further action needed from this lane; a future daytime/merge session can fast-forward merge once the campaign's merge gate opens. No data action or deploy was taken."
}
