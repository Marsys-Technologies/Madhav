{
  "finding_id": "F-125",
  "outcome": "PR_OPENED_FROZEN",
  "pr_url": "https://github.com/Marsys-Technologies/Madhav/pull/1387",
  "branch": "parisesa/repair-F125-orientation-export",
  "test_results": "New platform-mcp/src/__tests__/b11_gate_f125.test.ts: 8/8 passing on the fix. All 6 exercised regression assertions independently re-verified to FAIL when the 3 production-file changes are git-stashed (not vacuous) -- traced failure output: 'expected false to be true' / 'expected {...} to have property orientation_ok' on kala_upaya_get and all 4 bodha_* regAlias tools pre-fix. npx tsc --noEmit: clean. Full `vitest run` (whole platform-mcp suite) run once pre-fix and once post-fix, failing-test lists diffed byte-identical (93 lines both, all pre-existing/unrelated -- kala_timeline.test.ts, phala_outlook.test.ts, phala_event_anchors.test.ts, etc.) -- this change introduces zero new failures. Two pre-existing tests (d1_6_s1_cr42_regressions.test.ts, parishodhana_b1_remedy_fields.test.ts) updated in the same PR to account for the second, legitimate fetch call bodha_remedies_get now makes (orientation pre-fetch) -- both pass.",
  "review_notes": "Diagnosis re-verified against current origin/main (dc78e82df) before building: fetchOrientationContext at registry_bridge.ts:2019 (line shifted from spec's cited 2061 due to unrelated intervening edits, same function) still had no `export`; grep for 'orientation' in kala_views/upaya.ts still returned zero hits; register_p1_aliases.ts still had no requiresOrientation option and no fetchOrientationContext import; b11_gate_f125.test.ts did not exist anywhere in the repo. Diagnosis held, spec (all 3 stages COMPLETE) still applied cleanly to current source with no structural drift -- built exactly what section 2a/2b/2c specified, with one deliberate, documented deviation from the literal spec text: the orientation merge in kala_upaya_get's handler is skipped on a mortality-exclusion refusal (isMortalityRefusal check added before the fetch), to preserve gate G16's separately-tested 'refusal makes no network call of any kind, before ANY await' invariant (upaya.test.ts's G16 firing-proof) -- the literal spec text would have added a query_ucd network call to every response unconditionally, which would have silently broken that stronger, ADJUDICATION-13-absolute invariant. Independent review of own diff: both call sites (upaya.ts, register_p1_aliases.ts) correctly import fetchOrientationContext from registry_bridge.js with no circular-import risk (registry_bridge.ts does not import from either file); the 4 flagged regAlias sites (bodha_domain_reading_get, bodha_remedies_get, bodha_remedies_search, bodha_quality_get) match spec section 4 exactly; the remaining ~16 regAlias registrations are untouched. One unrelated pre-existing test failure noted but explicitly NOT touched (out of scope): registry_bridge_r5w3_judgment_and_portrait.test.ts's graha_portrait drill_pointers-length assertion (expected 3, got 4) fails identically on a clean origin/main checkout with none of this PR's changes applied -- confirmed via git stash before attributing it. No merge, no deploy performed -- PR left open/frozen per campaign scope boundary.",
  "next_action": "owner reviews and merges PR #1387 at morning checkpoint"
}
