{
  "finding_id": "F-68",
  "outcome": "PR_OPENED_FROZEN",
  "pr_url": "https://github.com/Marsys-Technologies/Madhav/pull/1378",
  "branch": "parisesa/repair-F68-tier-honesty",
  "test_results": "npx vitest run on p3b_predictive_anchors_suppression.test.ts + posterior_provenance.test.ts: 11/11 passing on fresh origin/main (340b358cf). Full L4_phala test directory: 88/88 passing. npx tsc --noEmit: 0 errors (clean -- no pre-existing baseline errors touch this file on current main, an improvement over the original commit's baseline which had 4 unrelated pre-existing dependency errors). npx eslint on the 3 changed files: 0 errors, 2 pre-existing/cosmetic warnings only (an unused _ctx param dating to 2026-06-28, unrelated to this change; an intentional destructure-to-omit unused-var warning in the new test, matching the original commit's own style).",
  "review_notes": "Defect confirmed live on current origin/main: query_predictive_anchors.ts's row-shaping .map() served posterior/confidence_low/confidence_high/lift_vector_jsonb from phala_anchors verbatim with no branch on the row's own confidence_basis column -- every production anchor is permanently tagged 'structural_not_yet_empirical' (no calibration mechanism exists yet, JL-009/BA-P5B), so callers received full-precision numeric predictions indistinguishable from a calibrated forecast. Matches closure-matrix.json's CL-08 tier-honesty-leak classification exactly. Candidate fix ca1032135 (origin/par/night-F-68) was investigated first: it went through the full overnight SHAD pipeline (builder PASS, reviewer verdict COMPLETE, ratifier-3 verdict COMPLETE with spot-checks holding, verifier_v 11/11 GREEN + tsc PASS, plus a directed PAR-R-14 revision cycle that closed the one real defect found -- D1, the note-selector not distinguishing 'suppressed' from 'never computed' for legacy null-posterior rows, fixed via the (isCalibrated || row['posterior'] == null) secondary guard). 'Quarantined' turned out to mean staleness, not rejection: git merge-base --is-ancestor ca1032135 origin/main returns false -- the commit sits only on origin/par/night-F-68, 56 commits behind origin/main, and was never opened as a PR (gh pr list found nothing matching F-68). No procedural or technical rejection reason was found anywhere in the ingested review artifacts. Created a fresh worktree off origin/main and cherry-picked ca1032135: applied with zero conflicts. The file has grown substantially since the branch point (new empty_reason/known_gap/backing_data_reachable SS N.6 disclosure fields, additional SELECT columns) but the specific suppression block this fix touches was untouched by that drift, so the patch landed on exactly the intended lines; both post-divergence main features verified preserved unchanged. Independently re-traced the D1 fix logic (the (isCalibrated || row['posterior'] == null) note-selector) against all four test cases and confirmed it correctly distinguishes suppression from never-computed.",
  "next_action": "owner reviews and merges PR #1378 at morning checkpoint; PR stays FROZEN (open, not merged) per campaign overnight scope boundary"
}
