{
  "finding_id": "F-73",
  "outcome": "PR_OPENED_FROZEN",
  "pr_url": "https://github.com/Marsys-Technologies/Madhav/pull/1390",
  "branch": "parisesa/repair-F73-build",
  "test_results": {
    "typecheck": "npx tsc --noEmit (platform-mcp) -- clean, zero errors",
    "adapted_regression_tests": "platform-mcp/src/__tests__/kala_now_get_c4_narrative.test.ts (15 tests) + kala_explain_get_c4_a5.test.ts (8 tests) -- 23/23 passed after fix. Both files previously mocked the phantom marsys://tool/L4/gochara_forecast_get URI at the fetch layer, which meant they exercised a code path production never actually reached (false-confidence gap that let this defect ship undetected). Adapted both to module-mock computeGocharaForecast directly (vi.mock('../tools/retrieval/register_gochara_windows.js', ...) with vi.hoisted mock fn, configured per-test via the existing opts.gocharaWindows/reachable parameters), matching the real call path post-fix.",
    "vacuousness_check": "confirmed genuinely RED before the fix: git-stashed only the two implementation files (now.ts, explain.ts), kept the adapted tests, reran vitest -- 5 real failures (active_windows empty when mocked windows were present; field_gochara_alignment stuck on 'insufficient_data' instead of 'aligned'/'divergent'; A5 agreement stuck on 'insufficient_data' instead of 'concurs'/'dissents' across 3 scenarios). Restored the stash and reran -- all 23 green again.",
    "full_suite_before_vs_after": "platform-mcp full `npx vitest run`: 25 failed test files / 80 failed tests / 1759 passed, IDENTICAL count in both the unmodified origin/main baseline and the fixed+adapted-tests tree. All 80 pre-existing failures (kala_timeline.test.ts, phala_muhurta.test.ts, registry_bridge_r5w3_judgment_and_portrait.test.ts, register_gochara_windows_mr01.test.ts, shad_darshana_w1_verify_reopen.test.ts timeout, others) are unrelated to gochara-forecast wiring and confirmed pre-existing on origin/main -- zero regressions introduced by this change.",
    "diagnosis_reverification": "re-confirmed against current origin/main (fetched fresh) before implementing: grep across all 6 platform/src/lib/retrieval/registry/layers/*/index.ts files for 'gochara' -- zero capability registrations (only unrelated ka_vedha_gochara/ka_gochara asset references in L3_kala's doc comment/imports); grep for the literal URI string across platform-mcp/src/** -- exactly 2 production call sites (now.ts:1385, explain.ts:471, line numbers shifted slightly from the corpus's diagnosis due to intervening commits but same functions/mechanism) plus the 2 test-mock interceptions this PR replaces; computeGocharaForecast's exported signature and 'windows' return-key confirmed unchanged at register_gochara_windows.ts (line numbers shifted 1605->1712 due to intervening commits, structure identical)."
  },
  "review_notes": "Read all three F-73 lane artifacts in full (DIAGNOSIS.md stage D COMPLETE, SPEC.md revision 1 stage S COMPLETE, REVIEW.md verdict COMPLETE -- these live on branch par/coordination, not campaign-state; found via `git log --all --diff-filter=A --name-only | grep F-73` since the ledger's 'results/ingested/' pointer was stale/inapplicable to this worktree). This was a genuinely ready-to-build spec, not just diagnosis: SPEC.md §0 already self-corrected DIAGNOSIS.md's original (wrong) fix-location guess (register the URI in layers/L4_phala/index.ts) to the mechanically correct one (bypass the registry entirely -- the URI was never meant to exist; computeGocharaForecast is a plain exported in-process function in the same package), included exact file:line citations, working code prescriptions for both now.ts and explain.ts, exit tests that were shown to fail today, and an explicit sibling-site census (explain.ts) that a prior REVIEW.md round had flagged as an INCOMPLETE-RETURN deficiency and this revision closed. REVIEW.md (verdict COMPLETE, no blocking deficiencies) independently re-verified every cited file:line against source and flagged one actionable builder note: the two existing test files' fetch-mocks intercept the phantom URI and would silently stop injecting gochara data once the fix bypasses fetch entirely -- exactly the test-adaptation work this build performed. Implementation matches the spec's prescribed approach (call computeGocharaForecast directly from both call sites) with one deliberate improvement beyond the spec's literal pseudocode: both call sites wrap computeGocharaForecast in try/catch, because -- unlike callRegistryCapability, which the doc-comments above both functions document as 'never throws' (it catches its own fetch/transport errors internally) -- computeGocharaForecast has no such guarantee (it can throw on DB failure) and the functions' own doc-comments promise callers a 'never throws' contract that would otherwise be silently broken by this fix. No overlap with other in-flight PARISESA lanes touching the same files: F-38 (DECISION_PARKED, same now.ts file but the entitlement-gate function, no line overlap per its own spec's note) and F-123 (PR #1379, open) -- diffed PR #1379 directly and confirmed its now.ts hunk (near line 1761, explainPointerTo import) and explain.ts hunk (near line 541, new explainRequiresDomainOrBhava export) are in different functions/regions from this fix's now.ts:1359-1420 (fetchGocharaForecastWindows) and explain.ts:455-483 (fetchA5GocharaWindows). Independent self-review pass completed on the full diff before opening the PR (checked import correctness, computeGocharaForecast signature/argument-order match, preserved never-throws contract, removed an unnecessary `void bhava` marker added then reverted for minimal-diff consistency with the pre-existing convention of leaving that parameter unreferenced).",
  "next_action": "PR #1390 is open and frozen per campaign scope (no merge, no deploy). Awaiting owner/reviewer merge decision. No further action needed from this lane unless review requests changes."
}
