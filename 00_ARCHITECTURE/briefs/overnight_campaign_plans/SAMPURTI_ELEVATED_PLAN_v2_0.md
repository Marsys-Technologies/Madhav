# SAMPŪRTI ELEVATED PLAN v2.0 — the completion-and-union arc

status: DRAFT for native confirmation (supersedes SAMPURTI_REBASE_PLAN_v1_0 §4
phases R2–R6 on confirmation; every ruling and rail of that plan carries).
author: native's desk (Fable 5), 2026-08-13, from three grounding passes:
live data (retrodiction glimpse + corpus profiles), a full field-architecture
audit, and a full classical-doctrine + elevated-asset inventory audit. Every
claim below cites a verified repo fact; no classical assertion exceeds what
the repo's own corpus holds.

## §0 — WHAT WE ARE ACTUALLY TRYING TO ACHIEVE (recollected)

The mission (CLAUDE.md §A): acharya-grade reading; patterns beyond human
working memory; TIME-INDEXED, PROBABILISTIC, CALIBRATED predictions testable
against lived reality. The arc began with one question — "why does it show my
marriage as denied" — and found the temporal system serving century-wide
smears. SAMPŪRTI's objective is therefore not "windows exist"; it is:
  (a) a field with real temporal structure across all 27 event classes;
  (b) MEASURED skill against the native's 64 lived events, honestly scored;
  (c) one temporal authority, no contradicting legacy surfaces;
  (d) served explanations an acharya would recognize as his own reasoning;
  (e) a running learning loop so calibration accrues from outcomes.
The elevated gochara estate (UTKARṢA + PARIṢKĀRA) is not a neighbor to this
objective — it is the missing half of it. The repo's own doctrine name:
dvi-pramāṇa — daśā promise × gochara delivery (honestly flagged in
composition.py as an engineering decomposition, NOT a verse-attested
technique; the plan preserves that flag everywhere it serves).

## §1 — THE EXECUTION CHALLENGE, UNDERSTOOD (not just endured)

~30 P-G1 runs failed. The audits now explain the whole arc: (i) the true
killer of runs 5–11 was the missing PG-31 seam (4,600+ v1 rows per class →
INSERT amplification), fixed in R0; (ii) stage5's four measured walls were
each fixed (L1g/L1h/L1k/L1n: 16min→~8s per block); (iii) stage4 is NOW the
dominant cost and has never been CPU-optimized — three behavior-preserving
defects identified with file:line (edge-free ln_lambda fast path; memoized
breakpoints() — currently a ~550K-element set rebuilt 270 times; substep-
scoped terms_at memo — 40–80K redundant evaluations per decade). Together a
verified 2–5× band. (iv) The monolithic-run requirement was the design
error: the writer is checkpoint-resumable by construction; N4 (per-class
closure + incremental completion) is the correct posture. Conclusion: the
wall was never correctness — window output is demonstrably right — it was
an ops model (timed single runs) fighting a resumable design.

## §2 — GROUND TRUTH (verified this session)

Field: 6,708 windows · 6 classes · avg 1.5d · max 27d · frontier 123/534 ·
clocks 8 · null machinery (q_e, 256 replicates) live in rows. Retrodiction
glimpse (planning-read, R13-clean): 0/8 real events inside windows —
statistically consistent with the ~4.6% century coverage; nearest peaks
2–29d. SKILL IS UNPROVEN. This is the honest baseline Measurement #4 exists
to score, and the reason the dvi-pramāṇa union is load-bearing, not
decorative. Gochara gen-3.0: 943/941 rows per chart · 27 classes · chains
live (5 classes) · hierarchy era⊃month⊃day live · suppression firing 322/432
· term_breakdown populated · all calibration honest-zero because ALL TEN
mechanisms are DEFINED+CITED+CODED, NOT ENGINE-WIRED (MECHANISM_ENGINE_WIRED
all-False — the machine-checked truth).

## §3 — THE THREE ELEVATION WORKSTREAMS (what makes this surpass the target)

### W-A · DVIPRAMĀṆA — gochara becomes a real input to the field
Sequenced per the integration audit (⑤→①→③→②), ALL under one blind spec
committed before any effect is computed (R13/blind-before-effect):
A1 [S] PIN FIRST (audit ⑤): gochara_generation + calibration_state (+ fit
   provenance) into FieldPins.config_pin, resolved ONCE in plan_substeps —
   makes CG-1 hold by construction AND fixes a latent live bug (per-substep
   authority re-resolution). CG-1 RULE ADOPTED: the field consumes ONLY
   calibration_state='structural_prior' gochara rows; any future admission
   of an empirically-calibrated corpus is an explicit config_pin change
   (the field can never promote its own confidence — contracts.py doctrine).
A2 [M] RESONANCE LANE (audit ①): consume the PAID-FOR, NEVER-READ DAG edge
   ka_gochara_resonance — classically-cited per-class target weights sharpen
   route gain g_ℓ in hazard.relevance. No new covariate, no migration, no
   version bump. CG-1 clean (resonance is built from bg_transit_rules +
   chart facts, never LEL).
A3 [M] GOCHARA CLOCK VOICE (audit ③): the design's own open door (§11 item
   4: a new Law-2 voice joins "with no change to §5.1"). Ships INERT
   (unseeded w_s ⇒ factor exactly 1.0); activated later by a single weight
   row. Changes magnitudes + provenance, not boundaries — companion to A4.
A4 [L] λ_v3 AS STAGE-1 PRIMITIVE x13 (audit ②): the doctrinally exact
   answer and the ONLY seam that moves window SELECTION, because only
   stage-1 envelopes ride the circular-shift null (the audit's hardest
   constraint — a non-shiftable gochara term would inflate real and null
   equally and change nothing). Explicit x12_v0→x13_v0 bump + weights seed
   + mi_bhara refit, exactly the §11.2 procedure the design pre-authorizes.
   λ_v3∈[0,1] matches the covariate contract by construction.
A5 SERVING AGREEMENT (audit ④ redirected): the already-persisted
   agree/diverge xref classification becomes an EXPLAIN facet — never a
   hazard term (preserves the §5.3 "never copy signed_intensity" rail).

### W-B · YANTRA — switch the elevated engine's classical machinery ON
(gochara-side; territory reverted to SAMPŪRTI at PARIṢKĀRA's close; runs
PARALLEL to field completion — the pre-integration field ignores gochara
values, so a YANTRA corpus rebuild before A1's pin costs nothing.)
B1 WIRE w23_tara_bala into engine.py's production λ (the one mechanism with
   live measured evidence: 92% genuine term contribution, 2,612 instances).
   This single wire turns on the ENTIRE calibration apparatus: genuine w43
   ablation → non-zero w44 pooled fit (sealed split <2020) → w45's §N.8
   gate can finally EARN empirically_calibrated. Then wire the next
   mechanisms in classical-evidence order (w21 AV gating — best-cited;
   w24 sade sati; w22 moorti; w26 eclipses; w27 stack), each with its own
   ablation before the next (never batch-wire — evidence per mechanism).
B2 w30_nodal_drishti: give Rahu/Ketu their 5/7/9 in the transit layer —
   the native's own lineage ruling (R24), already cited+live in L1, absent
   from gochara grammar (nodes fall to 7th-only; every nodal transit trine
   in the 100-year corpus currently invisible). Registry-pattern mechanism
   citing L1's constants verbatim, hedge preserved ("later tradition").
B3 CONSUME LATTĀ: 8 verse-verbatim Phaladeepika rows (best textual
   provenance in the estate, zero consumers) join the vedha→quality_gates
   suppression path; Ketu's absence disclosed as the migration discloses it.
B4 DATA-DEFECT REPAIR: the resonance writer's own docstring discloses live
   malformed lord tokens ("10L afflicted" qualifier dropped; "maraka lords
   (2L/7L)" emitted as a literal target_ref) — fix the tokenizer, rebuild
   resonance, quality notes re-verified.
B5 Corpus rebuild (one leased window) folding B1–B4 → the corpus the field
   integrates in W-A is the yantra-enriched one.

### W-C · VYĀKHYĀ — serve the explanation the system already computes
C1 term_breakdown + verse_refs served: every forecast window explains
   itself — primitive, resonance target, orb, bindu gate, vedha state,
   with resolved verse citations (and the honest 'unresolved' status where
   the corpus lacks the text — BPHS Ch.29 stays honestly unresolved).
C2 Hierarchy NESTED in serving (era=context, month/day=timing claims, per
   PK-R-1) via parent_window_id.
C3 coverage_quality.tier as a first-class facet (thin-tier silence ≠
   rich-tier silence — the anti-S4-05 discipline).
C4 UNIFIED NOW/AHEAD: the audit found the exact slots (now.ts sub-elevation
   block; ahead.ts autofile; explain.ts field-diff) — one narrative citing
   field window + contributing gochara windows + clocks, dual-reference
   with Moon-primary per SIX_VIEWS.
C5 ★ LEARNING-LOOP RE-KEY (the audit's "gap worth naming"): G14b auto-files
   against activation predicates while mi_bhara measures field window_ids —
   OUTCOME DATA IS ACCRUING AGAINST A KEY THAT CANNOT CALIBRATE THE FIELD.
   Re-key the auto-filer to field window_id + authority_basis when the
   field is lit (item-44 alignment). This is what makes objective (e) real.

## §4 — MEASUREMENT DESIGN (one isolated delta per measurement; R13/R14)

M#4  BASELINE: daśā-alone field, 27 classes, both charts, CRPS via the
     reusable harness (field enters as a TemporalCurveModel contender —
     audit B.6) + 1000-shuffle noise floor + sealed split. Published at its
     earned tier; a null result is publishable truth, not failure.
M#5  THE DVI-PRAMĀṆA TEST: post-W-A field, measured BESIDE #4. The delta —
     does daśā×gochara concurrence time real events better than daśā alone —
     is the classical doctrine made falsifiable, decomposed per-seam by the
     w43-pattern ablation table published alongside. Served with the
     uncited_extension flag the repo itself insists on.
M#6  RUBRIC DELTA: after Wave-3 cycles (F-STRENGTH/F3/F-CONDITION+R24+PA-3),
     beside #5. G14c engine-pinned skill-CI gate seals after #6.
All three: blind specs committed first; degenerate-interval tripwire;
version-pinned; never overwrite (R14); no tuning to the 8-event glimpse or
any LEL content (R13 absolute).

## §5 — PHASES (P0–P8; supersedes R2–R6 mapping)

P0 CLOSE+SPEED: R1-CLOSE per-class (N4, verifier packet, GREEN-PER-CLASS
   6/27) ∥ perf triad lane (the three stage-4 CPU fixes — land BEFORE
   burning compute on 21 more classes) ∥ session-open hygiene (orphan reap,
   locks=0, phantom rows).
P1 COMPLETION: remaining 21 classes via checkpoint-resume inside S5
   full-DAG, native then Abhinandan (RB rails: asset_set, exclusions,
   clear_before=false, protection counts). S6 acceptance (planes, CDLM 13,
   L3 tools, facades, G13 live). ∥ W-B code lanes run parallel throughout;
   B5 corpus rebuild lands under lease before P3.
P2 MEASUREMENT #4 (baseline, pre-integration by construction).
P3 W-A DVIPRAMĀṆA: blind spec → A1..A3 (non-breaking) → scoped re-field →
   A4 (x13 bump + refit) → re-field → MEASUREMENT #5 + ablation table.
P4 W-C VYĀKHYĀ (C1–C5) — parallel lanes after P3's serving surfaces settle;
   C5 re-key gates before P8's loop claims.
P5 WAVE-2': non-gochara legacy retirements (PA-7 parity + PRATINIDHI ruling
   each; gochara_* RETAINED permanently per N1/R-COORD-4); authority_basis
   census armed-to-fail; L2b/c/d/e live checks.
P6 WAVE-3: three rubric cycles + R24 portal audit + PA-3 upaya wire + G9;
   MEASUREMENT #6; G14c gate.
P7 WAVE-4: loop live (with C5 in place, outcomes now calibrate the field);
   G2 Tranche-3; R23 T2/T3 tiers (27/27 speaking, thin classes labeled via
   coverage tiers); G15 LAST (N2).
P8 CLOSE: independent re-close verdict (PARIṢKĀRA MR-29 pattern);
   CURRENT_STATE/SESSION_LOG; acharya-review handoff (R27, native
   commissions); RUN-TERMINAL.

## §6 — HONESTY CLAUSES (carried into every served surface)

dvi-pramāṇa = uncited engineering decomposition, flagged; BPHS Ch.29
citation stays 'unresolved'; chain shapes = DR-13 measurement discipline,
not classical doctrine; nodal 5/7/9 = "later tradition" hedge verbatim;
tara-bala weights = named-source schedule; calibrated tiers only ever
EARNED via the §N.8-gated path; the field never promotes its own
confidence (CG-1).

## §7 — EXECUTION ARCHITECTURE (unchanged from confirmed v1 §5 + additions)

Same supervisor/conductor/swarm, model+effort policy, leases, territory,
iron rules. Additions: A4's refit lane and every blind-spec + measurement
writeup = opus/high; perf-triad + mechanism-wiring lanes = sonnet; YANTRA
corpus rebuild = one leased window with full evidence conditions (the
PARIṢKĀRA L-6 template). PARKED-FOR-NATIVE unchanged + one addition: any
admission of an empirically-calibrated gochara corpus into the field
(config_pin change) requires a recorded native ruling.

## §8 — WHY THIS SURPASSES THE ORIGINAL TARGET

Original target: close G1–G16 so the platform serves calibrated temporal
truth. This plan additionally delivers: (1) the two-campaign gochara estate
actually PLUGGED IN — resonance, λ_v3, chains, hierarchy, suppression —
not parked beside the field; (2) the classical mechanism set switched on
with per-mechanism earned evidence, unlocking the first honest
empirically_calibrated stamps; (3) the dvi-pramāṇa doctrine converted from
lore to a scored, falsifiable, published experiment on 64 lived events —
the "research tool for astrology" half of the mission, operational; (4) an
explanation layer citing verse-resolved classical sources per window;
(5) a learning loop whose outcome data can actually calibrate the field
(C5) — the flywheel the mission statement promises.
