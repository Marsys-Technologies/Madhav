# SAMPŪRTI-α DAY PLAN v1.0 — "KṢETRA-SIDDHI" (field completion, done right)
# Interactive daytime execution (Antigravity Claude Code plugin, NOT script)

status: FOR NATIVE REVIEW — iterate before kickoff generation.
grounding: every claim verified live 2026-08-13 14:4x–15:0x IST (DB, git,
Cloud Run, and the DEPLOYED MCP — the customer surface — as proof source).

════════════════════════════════════════════════════════════════════════
§1 — GROUND TRUTH (verified now, not remembered)
════════════════════════════════════════════════════════════════════════

1.1 MARRIAGE (correction absorbed): RESOLVED since engine v4.1.0 — live row:
    status=conditional, occurrence 0.450 MODERATE, condition 5.83 MODERATE,
    F1 dispositor-conjunction amendment visible in the factor ledger, both
    denial configs evaluated-and-not-fired, G10 five-ayanamsha varga
    consensus with recorded dissent. The PROMISE question is closed. The
    field consumes this 0.450 as its P̃ term. α's job is the WHEN.

1.2 THE ELEVATED GOCHARA ESTATE (what two campaigns + β delivered, and what
    it serves TODAY — proven by a live gochara_forecast_get call):
    · Assets: ka_gochara_v3_century_materialize (production corpus writer)
      · ka_gochara (renamed staging writer) · ka_gochara_resonance (27-class
      map) · ka_gochara_sweep RETIRED, v1 corpus intact+protected ·
      kala_gochara_authority seam ('3.0' both charts) · gochara_v3 engine
      (λ_v3 = PROMISE × PERMISSION × activity × quality_gates, bounded [0,1])
    · β's wiring, LIVE in corpus: w23 Tāra bala · w30 nodal 5/7/9 (the
      native's lineage, finally in the transit layer) · Lattā verse-verbatim
      suppression · tokenizer fixes — all folded via the leased B5 rebuild,
      G-B MCP-proven, SESSION-β-COMPLETE.
    · Serving depth (γ, deployed + flags on): term_breakdown per window
      (live call showed the full causal chain: Mars/Venus/Jupiter dṛṣṭi
      contacts on house 7, kakṣyā crossings, orb decays, λ_v3=0.35 =
      1.00×0.35×1.00) · coverage tier (marriage: "rich, 22 targets") ·
      nested hierarchy scaffold · verse-ref plumbing · honest context-only
      disclosure (PK-R-1 served).
    · Concept in one line: the dvi-pramāṇa DELIVERY leg — per-class transit-
      activation windows with a per-window audit trail, honestly tiered.

1.3 SESSION STATE: β COMPLETE (terminal, clean). γ ~90% — C1–C5 merged,
    deployed, flags on; only gate G-P4 remains, blocked on the field being
    lit; its ledger is stale (says PR-OPEN for merged lanes). α: R0 done,
    P-G1 GREEN-PER-CLASS declared, R2/R3 done, M4 taken 04:02, gate packet
    merged+deployed, A2' done — and A3 (the field rebuild) crawling on the
    mis-sized cloud job: exec szwkw RUNNING, frontier 98/534 (~11 min per
    substep; ~25–30 effective substeps remain for the 6 prior-backed
    classes — the other 21 skip honestly per ADJUDICATION-2). The amended
    hygiene rule is holding: szwkw has SURVIVED multiple session cycles.
    SMR-1 (PRATINIDHI): M4 baseline STANDS (correct — pre-integration field
    values are gochara-inert; M5's verifier will spot-check equivalence).
    The KṢETRA-VEGA perf triad: STILL not landed (zero ka_kshetra commits).

1.4 ★ TWO NEW LIVE SERVING DEFECTS (found by today's grounding call):
    D-NEW-1 [SEV-1 risk] COVERAGE MISPOINT: the live response shows
      event_classes_covered = [] and ALL 27 classes "targeted_not_swept";
      sweep_completeness reads build_substep_progress asset_id='ka_gochara'
      (MR-02's authority-aware switch) — but the v3 corpus's substeps were
      committed under asset_id='ka_gochara_v3_century_materialize'. The
      W6.4 rename left MR-02 pointing at an id with ZERO substeps →
      any DOMAIN-FILTERED call on the three gochara tools now takes the
      S4-05 refusal path ("not covered") despite rich data. The anti-
      kill-switch fix (PG-27/MR-02) is itself mispointed post-rename.
    D-NEW-2 [SEV-2] RESOLUTION UNSTAMPED ON REBUILT ROWS: the served
      marriage row (computed 08-12 23:14, POST-migration-567) has
      resolution=NULL → hierarchy nesting serves 0 roots, row demoted to
      flat "context, not timing" (is_timing_window=false). The B5-era
      century-materializer path does not stamp resolution; the earlier
      42-day/42-month tagged rows predate the rebuild. Consequence: for
      ranges like the one probed, marriage serves ONLY a decade context
      envelope — no month/day timing claim — undoing PK-R-1's intent at
      the data layer while γ's serving layer sits ready above it.

════════════════════════════════════════════════════════════════════════
§2 — WHAT THIS SESSION WILL WORK ON, AND WHAT IT ACHIEVES
════════════════════════════════════════════════════════════════════════

The single interactive α session (you present, checkpoints not gates)
finishes the field CORRECTLY — best implementation first, then build —
and repairs the two new defects, ending with the field lit, M5-ready.

PHASE V — VEGA: BEST IMPLEMENTATION FIRST (~1–2h, parallel lanes)
  V1 Perf triad (TDD, one builder + Opus verifier): edge-free
     hazard.ln_lambda_only fast path · memoized breakpoints() (a ~550K
     set rebuilt 270×) · substep-scoped terms_at memo (40–80K duplicate
     evals/decade). GATE: byte-identical windows on a fixture decade +
     MEASURED ≥2× on real substeps, numbers pasted. Rider: per-substep
     duration_seconds telemetry (never archaeology again).
     _RESUME_VERSION 3→4 in the same PR (FM-17).
  V2 Cloud job right-sizing [NATIVE CHECKPOINT #1]: 
     gcloud run jobs update brahma-build-pipeline-job --cpu=8
     --memory=16Gi — kills the 4Gi ceiling under the ~2GB index.
  V3 Merge+deploy V1 (pinned packet, deploy-green, tracker verified).
  Meanwhile szwkw continues untouched — sunk compute we keep for free.

PHASE F — FINISH THE FIELD (~1h after V)
  F1 Stop-flag szwkw once V1+V2 are live [NATIVE CHECKPOINT #2] →
     redispatch on the fixed image + resized job; checkpoint-resume from
     the current frontier (nothing lost, replan expected from version
     bump — acceptable: substeps are now cheap).
  F2 Native completes (6 prior-backed classes; 21 honest skips ledgered)
     → Abhinandan (his checkpoints correctly invalidated by the bump).
  F3 G-P1 PRODUCT GATE, by MCP: kala_now_get sub-elevation lit ·
     kala_explain_get field-diff · judgment_query field-backed timing ·
     window tables + rung criteria pasted → FIELD-INTEGRATED
     prerequisite state achieved.

PHASE R — REPAIR THE TWO NEW DEFECTS (parallel with F; small lanes)
  R1 D-NEW-1: coverage source follows the authority-aware ASSET THAT
     ACTUALLY BUILT the serving corpus (v3 → ka_gochara_v3_century_
     materialize's substeps; keep v1→sweep). TDD: seeded test that
     coverage goes non-empty for the native; live MCP proof: a
     domain-filtered gochara call returns windows, not a refusal.
  R2 D-NEW-2: century-materializer stamps resolution (era for decade
     envelopes; preserves month/day rows where the hierarchy producer
     emits them) OR the hierarchy producer re-runs post-B5 — builder
     root-causes which path lost the tags, fixes at the writer (never a
     backfill-only patch), corpus refresh rides the next leased window.
     Live MCP proof: marriage serves ≥1 month-or-day timing window with
     nested era parent, is_timing_window=true.
  (Territory note: these are gochara-side serving/writer fixes — β/γ
  territory now dormant; α session absorbs with their rails.)

PHASE M — MEASURE + INTEGRATE (the payoff)
  M1 SMR-1 honored: M4 stands; M5 verifier spot-checks value-equivalence
     of N windows across the A3 rebuild as its first act.
  M2 P3 DVIPRAMĀṆA per the confirmed plan: A2 resonance lane → G-P3a
     (boundaries unchanged, magnitudes changed) → A3 clock voice (inert,
     then activation PR) → A4 x13 [NATIVE CHECKPOINT #3: the version-
     bump packet] → re-field → MEASUREMENT #5 beside #4 + per-seam
     ablation table → BRILLIANCE GATE #1 (Opus PRATINIDHI full reading,
     rubric-graded, via product tools only).
  M3 FIELD-INTEGRATED posted → γ relaunched once to close G-P4
     (auto-file proof against a live field window) → γ terminal.

CHECKPOINT MODEL (interactive): continuous execution; pauses ONLY at the
three marked points (infra change, build stop-flag, version-bump packet).
Everything else — PRs, merges to main, deploys, dispatches — proceeds
under the standing delegations with evidence pasted as it goes.

════════════════════════════════════════════════════════════════════════
§3 — WHAT REMAINS AFTER THIS SESSION (the spine, later sessions)
════════════════════════════════════════════════════════════════════════
P5 retirements (gochara_* RETAINED per N1) · P6 rubric cycles + M6 +
Brilliance #2 · P7 loop live / Tranche-3 / T2-T3 tiers / G15 last ·
P8 close with the full MCP customer-journey exhibit + independent
re-close verdict. Ledger reconciliations (α's stale entries, γ's PR-OPEN
rows) ride the next session-close of each.

════════════════════════════════════════════════════════════════════════
§4 — WHY THIS ORDER IS CORRECT
════════════════════════════════════════════════════════════════════════
Optimize-before-build (your ruling) — the triad is measured-gated, not
promised. Infra sized before dispatch — no more subsidizing engineering
debt with wall-clock. The two serving defects are fixed WHILE the build
runs, so the moment the field lights, the whole stack above it —
explanation layer, coverage honesty, hierarchy timing claims — is
actually true end-to-end. And every gate proves itself on the deployed
MCP, the surface your customer touches.

════════════════════════════════════════════════════════════════════════
§5 — KṢETRA-VEGA v2: THE REBUILD-TIME RE-ARCHITECTURE (native concern,
     2026-08-13; supersedes Phase V's scope with a three-tier program)
════════════════════════════════════════════════════════════════════════
Verdict of record: the DESIGN (continuous hazard + circular-shift null) is
sound and is the product's calibration core — untouchable. The
IMPLEMENTATION is pointwise pure-Python, serial, full-recompute, and was
run on starved infra. All three tiers fit INSIDE the frozen orchestrator
contract (writer-internal changes only). All timing figures below are
ENGINEERING ESTIMATES to be measured at each tier's gate — never promises.

TIER 1 — REMOVE THE WASTE (hours of work; est. build ~2–4h/chart cloud)
  The confirmed Phase V: perf triad (edge-free ln_lambda_only · memoized
  breakpoints · substep-scoped terms_at memo) + duration_seconds telemetry
  + cloud 8vCPU/16Gi. GATE: byte-identical fixture windows + measured ≥2×
  + full-build extrapolation pasted.

TIER 2 — VECTORIZE + PARALLELIZE (≈1 builder-day; est. ~15–40 min/chart)
  T2a NUMPY-VECTORIZED HAZARD: envelopes are piecewise-linear — segment
      construction and ln_lambda evaluate over breakpoint GRIDS as array
      ops (the gochara v3 evaluate_lambda_vector precedent). Equivalence
      GATE is tolerance-specified (≤1e-9 on ln λ; IDENTICAL window
      boundaries and peaks on golden fixtures) — spec committed blind
      BEFORE the port, since float reordering forbids naive byte-compare.
      Provenance edges still computed exactly, but ONLY at the ~per-window
      moments they are persisted (the design already reads them only
      there).
  T2b WRITER-INTERNAL CLASS-PARALLELISM: run_substep computes via a
      multiprocessing pool (compute-only workers; results marshaled back;
      ALL DB writes stay on the orchestrator's single connection — the
      §N.2 contract is untouched). Restructure stage-4/5 plan to
      decade-fat substeps that fan classes across cores. 6–8× on an
      8-vCPU box, checkpoint granularity preserved.
  GATE: golden equivalence + measured wall-clock + one full-chart cloud
  build timed end-to-end via telemetry.

TIER 3 — REBUILD ONLY WHAT CHANGED + ELEVATE THE STATISTICS (≈1 day)
  T3a STAGE-SCOPED INPUT PINS: split the resume fingerprint into
      per-stage pins (stage0–3 pin = code version + chart-data digests;
      stage4–5 pin = that + weights + gochara corpus digest). A
      corpus/config change then SKIPS stages 0–3 entirely (§N.3
      delete-then-insert applied per-stage, not per-asset). Typical
      rebuild (the common case for P3's re-fields and every future
      iteration): single-digit MINUTES. Cold build unchanged.
  T3b QUALITY ELEVATION, PAID FOR BY SPEED: null replicates 256 → 1024
      (q_e resolution 4× finer; confidence tiers sharpen) — a blind-spec
      parameter change (R14: measurements re-versioned), affordable only
      after T2. The re-architecture does not merely preserve quality; it
      buys better statistics with the saved compute.
  GATE: pin-matrix test (each input class change invalidates exactly its
  stages, proven by seeded changes) + a 1024-replicate build timed +
  MCP end-to-end.

SEQUENCING WITH THE ARC: Tier 1 now → finish the field (unblocks M4/γ) →
Tiers 2+3 land during M4/P3-prep, so P3's TWO required re-fields and
every future rebuild run at minutes-scale. The field becomes a component
the product can rebuild routinely — which is what "one temporal
authority" requires operationally, not just doctrinally.

════════════════════════════════════════════════════════════════════════
§6 — KṢETRA-DHĀRĀ: THE DEEP-PASS RE-ARCHITECTURE (supersedes §5's Tier 2/3
     with a stronger design; native's accuracy+rebuild mandate, 2026-08-13)
════════════════════════════════════════════════════════════════════════

6.1 THE MATHEMATICAL FINDING: ln λ_e(t) is piecewise-smooth with an a
    priori KNOWN knot set — clock terms piecewise-CONSTANT at daśā
    boundaries (kala_field_boundaries, 262,730), modifiers piecewise-
    LINEAR at envelope knots, suppression = rare log-affine corrections.
    The current engine ignores this and adaptively BISECTS (depth 6,
    midpoint sampling) as if λ were a black box. Between knots every
    published quantity has a CLOSED FORM: threshold crossings (window
    edges) exact; peaks exact; ∫λ (expected_count) = exponential-affine
    integrals, exact; suppression-active intervals via certified-bracket
    Newton. ENGINE: event-driven sweep — one global sort of knots per
    class, O(1) delta-updates per event, exact emission. Vectorized:
    per-term (knot, delta) arrays; a null replicate = re-indexed envelope
    events merged with fixed clock events (numpy merge+cumsum, ~tens of
    ms per replicate per class).

6.2 ACCURACY ELEVATIONS (the same defect caused slowness AND approximation):
    e1 Exact window EDGES (currently quantized to sampled segments).
    e2 Null coarsening REMOVED: L1g's ~819-point coarse grid — an
       approximation inside q_e itself — replaced by full-fidelity
       sweeps; replicates 256 → 1024 (blind-spec'd; R14-versioned).
    e3 Exact peaks + exact expected_count.
    e4 Per-term contribution CURVES persisted (term matrix) → EXPLAIN can
       serve exact term attribution at ANY date — a new astrological
       capability (the "why is this rising now" curve), not just speed.
    §5.4 provenance reconciliation preserved (terms evaluated exactly at
    peak); CG-1 pins discipline unchanged.

6.3 REBUILD TAXONOMY → (STAGE × CLASS) PIN MATRIX (replaces the monolithic
    fingerprint; per-class invalidation so class additions never replan
    existing classes):
      cold build/chart ............ ~20–30 min (est., gate-measured)
      corpus/config change ........ ≤5 min (stages 0–3 skipped by pins)
      WEIGHTS REFIT (living loop) . seconds–2 min (stage-4b recombination
                                    over the persisted term matrix only)
      new prior-backed class ...... ~1 min/class (G2 growth path)
    IO: COPY-based bulk loads; served artifacts (windows/null/provenance)
    stay in DB; dense segments additionally persisted as a compact
    term-matrix artifact; mi_bhara consumes via adapter (design decision
    D3 — options documented, native taste applies).

6.4 ROLLOUT (design → verify → implement, per native):
    d1 DHĀRĀ DESIGN DOC (the spec: event algebra, closed forms, pin
       matrix, null vectorization, equivalence tolerances + EXPECTED
       differences) — committed BLIND before any comparison runs.
    d2 ADVERSARIAL DESIGN REVIEW: Opus VERIFIER + PRATINIDHI attack the
       spec (edge cases: suppression-active crossings, knot collisions,
       century boundaries, empty classes, shift wraparound).
    d3 IMPLEMENT AS DUAL ENGINE behind a flag (engine='sampled'|
       'analytic'), old path untouched → parity battery on golden
       fixtures + property tests (integral additivity; clock-term shift
       invariance; null-rank uniformity on synthetic H0 data; mutation
       tests on the sweep combiner) → flag flip is its own PR.
    d4 MEASUREMENT DISCIPLINE: engine swap = measurement-version event.
       SMR-2 (PRATINIDHI): M4 re-baselines on DHĀRĀ (minutes, published
       BESIDE); P3 and M5 run on DHĀRĀ only; no cross-engine deltas.
    d5 Current crawling cloud build: PAUSE now (its output is superseded
       by the version bump regardless); telemetry + 8vCPU/16Gi resize
       retained from §5 Tier 1; the triad is SUBSUMED by DHĀRĀ (fallback
       hedge: triad-only path stays available if d3's parity gate fails).
    Estimated effort: 2–3 focused builder-days + verification. Native has
    pre-accepted the investment (2026-08-13).

6.5 NATIVE DECISION POINTS CARRIED TO THE IMPLEMENTATION-PLAN PASS:
    n1 DHĀRĀ-first sequencing confirmed? (pause current build; M4' on new
       engine; single engine for the whole measurement family)
    n2 D3 persistence choice (recommend: DB for served artifacts + compact
       term-matrix artifact for refits/explain).
    n3 1024-replicate null adoption (blind-spec parameter).
