---
version: 1.0
status: CURRENT (native-directed 2026-08-15)
purpose: Full-system end-to-end audit of the elevated MARSYS-JIS instrument
  against the evolved transformation-elevation lineage, closing with a graded
  gap ledger and remediation lanes. Goal — the native's words — a perfectly
  smoothly running accurate engine, fully astrologically aligned,
  architecturally sound, delivering superlative experience and completeness
  of astrological insight, usable flawlessly.
---

# PARIPŪRṆA — THE COMPLETENESS AUDIT

## 1. THE LINEAGE UNDER AUDIT (objective inventory — auditors READ these)

1. MASTER_PLAN_IDENTITY_AND_PROMISE.md (2026-08-08) — identity contract
   (R17 adoption-not-addition), bounded rubric scoring (R18), L1 sealed
   (R19), PRATIJÑĀ v4 promise engine, proof ladder, success dashboard §7.
2. GAP_REMEDIATION_MASTER_PLAN.md (2026-08-10) — R23–R28; GAP CARDS
   G1–G16 (each with fix·propagation·PROOF clauses); Waves 0–4; PA-0–8.
3. The campaign estate that executed/evolved it: ṢAḌ-DARŚANA, GOCHARA-
   UTKARṢA + PARIṢKĀRA (elevated gochara, MR-fixes), SAMPŪRTI Δ1/Δ2/Δ3
   (ledgers on sampurti/* branches + CAMPAIGN_COORDINATION SM-R-1..11),
   PŪRṆA-KṢETRA v1.1 + N5 (27-class tiers, DHĀRĀ engine, F-wave),
   closing artifacts: M4′, DVIPRAMANA_27, BRILLIANCE_GATE_1, audit/
   grounding/investigation docs in /Users/Dev/shad_overnight/.

VERDICT VOCABULARY (per audited item, R16 detector-cited):
  MET · EXCEEDED (delivered beyond the card) · MET-EVOLVED (delivered in
  a deliberately changed form — cite the ruling that changed it) ·
  PARTIAL (graded/conditional pass on record) · GAP (promise not kept,
  no ruling excuses it) · BY-DESIGN-OPEN (honest residual with a named
  owner/loop, e.g. P-E priors). Every claim needs live evidence — no
  verdict from a ledger assertion alone (FM-09 applies to audits too).

## 2. AUDIT DIMENSIONS (the whole system, six lenses)

A. ASTROLOGICAL CORRECTNESS (the soul — heaviest weight):
   A1 FORENSIC anchors 7/7 re-verified live (chart_facts, both charts).
   A2 Classical spot-derivation battery: N=25 randomized fact checks
      (dignity, aspects incl. R24 nodal 5/7/9 census across EVERY
      aspect-consuming surface, shadbala pick-the-right-column, dasha
      boundaries vs independent recomputation, varga assignments).
   A3 Yoga estate: firings-vs-catalog discipline (§N.6), dead-slot G6
      band, sample re-derivation of 10 fired yogas from L1 facts.
   A4 Promise layer: R18 bounded-rubric conformance census (weights
      sum to 1, factors in [0,1], citations resolve); marriage verdict
      chain intact (occurrence vs condition, R12); G8 provisional
      classes' KaryatvaMaps.
   A5 The field: window sanity vs the 64-event LEL (retrodiction
      HARNESS only — R13 absolute, no fitting); tier honesty (calibrated
      vs shape_only absolutes suppressed end-to-end — P3-b census
      holds); ★ the OPEN marriage-zero-gochara-windows question:
      diagnose expected-physics vs data-gap (Δ3's handoff item).
   A6 Cross-system coherence: field vs gochara vs promise agreement
      surfaces (DVIPRAMĀṆA follow-through); dasha-lord capability vs
      firing windows; no contradiction served without a disclosure.
B. DATA COMPLETENESS & INTEGRITY:
   B1 27-class census on every producing table + both charts; tier
      table conformance; skipped-classes honesty records.
   B2 Invariants: field contiguity (gaps==0), coverage [0,H], null
      counts (25×10), windows presence, snapshot hash recomputation.
   B3 Identity: R17 adoption census (no surviving independent maps),
      Fact Identity Index resolvability, constituent_facts resolve.
   B4 G-cards with data proofs: G4 (W3 planes populated), G9
      (registry↔ontology reconciled), G10 (varga_confirmation).
C. SERVING / MCP — THE CUSTOMER EXPERIENCE (§7.1c: MCP IS THE PROOF):
   C1 FULL TOOL SWEEP: every registered marsys-jis tool called live
      with realistic args (both charts where chart-scoped): valid
      response, honest envelope (empty_reason, coverage, tier facets,
      resolution_disclosure, drill pointers), no stack traces, no
      silent empties. Budget/density contract behavior sampled.
   C2 Journey tests (the customer's actual paths): "when will I
      marry?" → promise+field+gochara coherent story; "rough patch
      for health?" → S4-05 refusal discipline; elect/muhurta flow;
      remedies flow; prashna flow; session memory recall.
   C3 G13 assess suite over the canonical 13 domains; G12/E6 facade
      elevations present per view (ELECT/NOW/AHEAD/STORY/PRIORITY).
   C4 Latency + stability: p50/p95 per tool family; zero 5xx.
   C5 Portal smoke: key pages render against live data (cockpit,
      review tab, badges = SQL truth).
D. ARCHITECTURAL SOUNDNESS & GOVERNANCE:
   D1 Mechanical guards ALIVE-tested (not prose): FM-23/25/26 CI
      guards fail when they should (mutation probe on a scratch
      branch); watchdog/strike/canary scripts intact; --run-id exit-2.
   D2 Orchestrator contract intact (frozen §N.2); migration ledger
      _migrations_applied == files; branch protection; no drifted
      registries (drift_detector + schema_validator green).
   D3 §N.7/§N.8 honesty sweep: sample 10 served verification/status
      flags → each has a real detector behind it.
   D4 The G14 loop: resolver → auto-file → skill CI actually cycling
      (one real prediction row moves through lifecycle stages).
E. SUPERLATIVE EXPERIENCE & DEPTH (the beyond-acharya bar):
   E1 G15 re-run: the 21-question dark-corpus quality measurement —
      bright% ON RECORD vs its baseline (the plan's own closing gate).
   E2 Brilliance rubric items (i)/(iii) (PARTIAL at BG#1): name
      precisely what moves each to full PASS; feasibility-graded.
   E3 Depth spot-reads: 3 PRATINIDHI (opus max) whole-chart reads on
      served output only — would an acharya find it complete, deep,
      non-generic? (§J bar; NOT the R27 commissioning itself — that
      remains reserved to the native as the closing ceremony.)
F. RESIDUAL CONSOLIDATION (one register to rule them all):
   Gather EVERY named residual across all ledgers/reports (M5
   ablation · pin-matrix W8 · term-matrix W9/EXPLAIN · marriage-zero
   diagnosis · 2 not_applicable classes · 19 shape_only→calibrated
   priors loop · E8 register · MARSYS_MCP_URL secret · ANTHROPIC_API_KEY
   unprovisioned · stage6/6.5 batching lane · G2 Tranche-3 · anything
   found in ledgers) → grade each GAP vs BY-DESIGN-OPEN, owner, size.

## 3. EXECUTION MODEL (proven machinery, audit-adapted)

One AUDIT CONDUCTOR (sonnet) under the existing rails + supervisor
pattern (watchdog harmless, $150 cap, FM-27/28 in force). READ-ONLY
against production: no builds, no writes to product tables, no deploys.
Remediation is NOT in this arc's scope except: trivially-safe fixes
(docs/tests/CI) may ship as PRs; everything else lands in the GAP
LEDGER for the native to sequence. PARĪKṢAKA (opus, fresh) verdicts on
each dimension; PRATINIDHI (opus max) for E3 reads + any ruling needs.
Dimensions A/B/C parallelize (≤4 sonnet probes); D/E follow; F closes.
Every probe's raw MCP/SQL evidence is committed under
00_ARCHITECTURE/briefs/paripurna/ (repo record).

DELIVERABLE: PARIPURNA_AUDIT_REPORT_v1_0.md —
  §1 lineage scoreboard (every G-card + R-ruling + master-plan §7
     dashboard item: verdict + evidence);
  §2 six-dimension findings;
  §3 THE GAP LEDGER (ranked: user-visible correctness > honesty >
     depth > polish), each gap with a proposed remediation lane;
  §4 the one-page "is it flawless?" answer with the honest bar:
     what IS flawless today, what is graded-and-disclosed, what needs
     the next wave.
TERMINAL: RUN-TERMINAL: SESSION-PARIPURNA-COMPLETE after PARĪKṢAKA
re-verifies the report's own claims by sampling.

## 4. TIME/COST ENVELOPE (FM-28 nets, not stopwatches)
Expected ~6-10h of conductor time, $40-80. Nets: $150 cap; every probe
read-only so the only failure mode is a wrong report, guarded by the
PARĪKṢAKA sampling re-verification.

═══════════════════════════════════════════════════════════════════════
## AMENDMENTS v1.1 (2026-08-15, second Fable-5 pass — native-directed)
Validated live: the desk ran kala_now_get from the user seat post-ARC.
ONE probe yielded six findings (below). These amendments are therefore
not theory — they are the method that already worked, made mandatory.

### AM-1 THE TRIANGULATION METHOD (now the CORE method, not implicit)
Every dimension-A/B/C verdict triangulates three views of the same fact:
  USER SEAT (marsys-jis-direct MCP response) ↔ CODE (the serving +
  writer source) ↔ DATA (the DB row). A divergence between ANY two is a
  first-class finding even when each view alone looks healthy. The
  desk's calibration probe is the exemplar (committed to
  briefs/paripurna/exemplars/).

### AM-2 POST-ARC RECONCILIATION SWEEP (new, URGENT class — 2 live hits)
The arc completed hours ago; serving surfaces still carry pre-arc
assumptions. Live evidence from the desk probe:
  · kala_now_get.state_delta = honest_empty with reason "requires
    field-build to complete first" — THE FIELD IS COMPLETE; the reason
    is stale and the E6 state_delta elevation is now computable.
  · gochara_narrative.field_gochara_alignment = "insufficient_data" /
    narrative_tier "thin" — despite the sealed 27-class field.
MANDATE: sweep EVERY honest_empty / not_computed / not_in_corpus /
"pending X" reason across all tools — each is either still-true
(verify), stale-unblocked (GAP: now computable, reason must update or
feature must light up), or wrong. This class converts completed build
work into user-visible value; missing it means the arc's output exists
but users never feel it.

### AM-3 CALIBRATION-SURFACE DISCREPANCY (live hit — dimension A/D)
kala_now_get.calibration_maturity reports n_events=0,
event_class_coverage=0 for the NATIVE chart that holds 64 LEL events.
Audit: what source does the maturity block read; is the G14 loop's
output actually wired into serving attestations; fix-or-explain.
Also: freshness.ephemeris_version + sweep_build_date served as null —
attestation fields half-populated.

### AM-4 CROSS-TOOL CONSISTENCY MATRIX (new C6)
~12 fact-pairs served by ≥2 tools must agree exactly (moon nakshatra
via ganita_nakshatra_get vs chart_snapshot vs graha_portrait; running
MD/AD via ganita_dashas_get vs kala_now_get; transit positions via
ref_planet_transit_get vs gochara_dual_reference; dignity via
ganita_strength_get vs dasha_lord_transit_condition.dignity; field
snapshot id via kala_now_get vs kala_ahead_get). Divergence = the
free-text-join sin the master plan §0 diagnosed, at the serving layer.

### AM-5 ADVERSARIAL/EDGE BATTERY (new C7)
Malformed args, nonexistent chart UUID, boundary dates (birth instant,
horizon end 2084, pre-birth), unknown domain names, not_applicable-tier
classes requested directly, pagination past end, empty-data charts.
Bar: graceful, honest, no stack traces, no invented data — "flawless
for users" is defined at the edges, not the happy path.

### AM-6 LLM-CONSUMABILITY TEST (new C8 — the real end-user is an LLM)
A FRESH sonnet agent, given ONLY the MCP tools and five natural user
questions ("when will I marry?", "is this a good period for a job
change?", "health outlook?", "what remedy helps now?", "explain my
current dasha"), must produce readings graded (by PRATINIDHI opus-max)
for: correct tool selection, synthesis coherence, citation fidelity,
depth vs generic-ness, honest-uncertainty handling. Response-size/
token-budget conformance measured per tool (density contract). This is
the closest proxy to the production customer experience that exists.

### AM-7 EXPERIENCE-GRADE LENS on every C-dimension probe (E4 register)
Beyond correct/honest, grade READABILITY-FOR-HUMANS: internal jargon
leaking into user-facing fields (live hit: window signature
"CLASSIFY_RESIDUAL/DIGNITY/YOGA/SUBSYSTEM" as the NOW headline), and
salience quality (live hit: a 17-year 2010-2027 window headlined as
"strongest" NOW context, where an acharya would foreground the current
2024-12→2027-08 Saturn antardaśā — technically true, experientially
weak). These land in the E4 ELEVATION OPPORTUNITIES register (value ×
effort graded), feeding the next elevation plan — the native asked for
"where there is opportunity to elevate," not only what is broken.

### AM-8 SECOND-CHART GENERALIZATION (B5 hardened)
Full C1/C2/C8 slice on Abhinandan (1c826d5a) — the zero-LEL chart. The
mission is an instrument for ANY consenting native; excellence must not
depend on 64 events of history. Every tier/coverage disclosure must be
correct for a chart with no outcome data.

### AM-9 PHASE-0 DESK CALIBRATION (execution-model change)
BEFORE conductor launch, the desk (dual seat: MCP + code + this arc's
full context) runs a ~10-probe calibration battery and commits the
annotated transcripts as briefs/paripurna/exemplars/ — the standard
every probe agent's evidence is graded against. Probe 1 (kala_now_get,
6 findings) already committed by this amendment. The conductor ADOPTS
desk findings AM-2/AM-3/AM-7 as pre-seeded audit items — they are not
re-discoveries.
