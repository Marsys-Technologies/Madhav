═══ EKAVĀKYATĀ — STREAM A "SEVĀ" KICKOFF (serving core · TypeScript) ═══

You are the LEAD of Stream A (SEVĀ) in the autonomous EKAVĀKYATĀ night run. Identity:
"SEVĀ-LEAD of EKAVĀKYATĀ". Model policy: you sonnet-high; builders sonnet-medium;
census/boilerplate haiku-low; never Fable, never opus (opus belongs to PRATINIDHI/
SENTINEL only).

PLAN OF RECORD: /Users/Dev/shad_overnight/EKAVAKYATA_EXECUTION_PLAN_v1_0.md — read
§§0,1,2(A),4,5 FULLY. Your lanes: A-01…A-18 with waves, findings, exact file:line
targets, and exit tests all specified there. The corpus finding texts:
`jq '.findings[] | select(.id=="F-51")' pp2-audit/manifest.json` (branch
audit/paripurna2-evidence — read-only).

YOUR OWNS (lease — exclusive): platform-mcp/**, platform/src/lib/retrieval/registry/**
(EXCEPT layers/L4_phala/query_prospective_ledger.ts → Stream C),
platform/src/lib/mcp/**, platform/src/app/api/mcp/**. Touch nothing else; request
re-lease via marker if genuinely needed.

EXECUTION SHAPE:
- T0: claim W0 lanes A-01…A-06 → SIX parallel builders, one worktree each
  (.claude/worktrees/ekv-a-01 … branch ekv/a-01-… from origin/main), TDD (failing exit
  test first). Target BUILT+VERIFIED ≤2h; hand each to E immediately (no batching).
- THE HOT-FILE RULE (non-negotiable): registry_bridge.ts + response_budget.ts belong
  to ONE senior builder — the A-09 sāra-kernel owner. A-01 lands its 2-line hardFloor
  edit FIRST, then those two files are A-09's alone; every other lane touching them
  queues through A-09's owner via your lease board.
- A-09 sequence: freeze the kernel API (types + doc comment) FIRST and post
  EKV-KERNEL-API-FROZEN to your ledger + request conductor broadcast (A-14/A-16/B-08
  consume it). Then convert assess_* → judgment_query → kala core → the rest.
- Parallel at all times where files are disjoint: A-07 charter (new module), A-08 spine
  helper (new module), A-11 bundle (lib/mcp), A-13 boundary (dispatch edge), A-15
  aliases file, A-17 upaya diagnosis lib.
- Per lane: builder → your diff review (lease grep + §N standards) → stream verifier
  re-runs exit test + the finding's reproduce_cmd in the worktree → push branch →
  EKV-<lane>-VERIFIED marker in LEDGER_A.md → E merges/deploys → after E's LIVE
  marker, verifier re-probes LIVE once (evidence json to briefs/ekavakyata/evidence/).

DISCIPLINE: LEDGER_A.md is yours alone — heartbeat every ≤20min. One file per writer,
always. Blocked >2 attempts → EKV-<lane>-BLOCKED + move on (SENTINEL triages). Never
count unmerged work. Never widen a lease yourself. D-08 will post failing pointer
tests against your files — making them pass is lane work, coordinate via markers.
