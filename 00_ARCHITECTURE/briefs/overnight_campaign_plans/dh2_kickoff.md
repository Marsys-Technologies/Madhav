═══ STREAM Δ2 ═══
You are the CONDUCTOR of SAMPŪRTI-Δ2 (PRAMĀṆA — verification harness +
infra). Identity string: "CONDUCTOR of SAMPŪRTI-Δ2". Home worktree:
/Users/Dev/Vibe-Coding/Apps/Madhav/.claude/worktrees/sm-d2 (branch
sampurti/pramana, created by the runner off origin/main). Ledger:
00_ARCHITECTURE/briefs/sampurti/SAMPURTI_STATE_D2.md (create at first
run: lane table + LOG). DB port 5434 (read-heavy; your own proxy).
PLAN: ALPHA_DAY_PLAN_v1_0.md §6 (you are d3's verification half) +
impl plan §7 failure register.

YOUR LANES (parallel from minute one — you do NOT wait for Δ1):
V1 GOLDEN FIXTURES: generate from the CURRENT sampled engine NOW (it is
   still default): frozen input snapshots + outputs (segments, windows,
   peaks, expected_counts, null stats) for ≥3 classes × ≥3 decades ×
   both charts, incl. suppression-active and empty-class cases. Commit
   as versioned fixtures. Post FIXTURES-READY.
V2 PROPERTY-TEST HARNESS: integral additivity · clock-term shift
   invariance · null-rank uniformity on synthetic H0 data · knot-
   collision and wraparound edges · mutation tests on the sweep
   combiner (kill-the-detector standard).
V3 PARITY BATTERY RUNNER: old-vs-new comparison per the BLIND tolerances
   in Δ1's frozen spec (never negotiate tolerances after seeing diffs);
   classified diff report (within-tolerance / expected-difference-
   register / VIOLATION). YOU issue PARITY-GREEN or the findings list —
   sole authority (opus verdict), Δ1 never self-certifies.
V4 INFRA: gcloud run jobs update brahma-build-pipeline-job --cpu=8
   --memory=16Gi [this single command is pre-authorized by the native] ·
   duration_seconds + per-substep telemetry PR · one timed real-substep
   cloud probe after resize, numbers ledgered.
V5 MEASUREMENT GUARD: pre-commit the M4'/M5 comparability checklist
   (same engine, same class set, matched-subset delta rule) as a CI-
   checkable doc; verify SMR-2 is recorded before M4' publishes.
Then SESSION-DONE-Δ2; Δ1 adopts residuals.
