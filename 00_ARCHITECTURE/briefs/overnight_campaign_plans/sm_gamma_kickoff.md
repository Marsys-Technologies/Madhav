═══ SESSION γ ═══
You are the CONDUCTOR of SAMPŪRTI-γ (VYĀKHYĀ — the explanation layer).
Identity string: "CONDUCTOR of SAMPŪRTI-γ". Home worktree:
/Users/Dev/Vibe-Coding/Apps/Madhav/.claude/worktrees/sampurti-vyakhya
(branch sampurti/vyakhya — YOUR single-writer ledger branch; runner
creates it off origin/main). Ledger: 00_ARCHITECTURE/briefs/sampurti/
SAMPURTI_STATE_GAMMA.md (create at first run: lane table C1–C5 + LOG).
NO DB port — you do NO database builds, hold NO locks; your evidence
instrument is the deployed MCP + committed HTTP probes exclusively.

SCOPE (impl plan §4, W-C): C1 term_breakdown + citation_verse_refs
served as the per-window WHY (honest 'unresolved' status preserved —
BPHS Ch.29 stays unresolved) · C2 era⊃month⊃day NESTED serving via
parent_window_id (era = context, month/day = timing claims, PK-R-1) ·
C3 coverage_quality.tier as a first-class facet (thin ≠ rich silence) ·
C4 unified NOW/AHEAD narrative (field + gochara, dual-reference with
Moon-primary attribution, honest tiers; the audit's slots: now.ts
sub-elevation block, ahead.ts autofile, explain.ts field-diff + A5
agreement facet) — BUILD behind flags now, ACTIVATE on α's
FIELD-INTEGRATED marker · C5 learning-loop re-key: AHEAD auto-file →
field window_id + authority_basis (item-44), built behind flag,
activated post-marker.
GATES: G-γ1 (pre-marker, MCP-proven): gochara_forecast_get response
carries term_breakdown summary + verse_refs + nested hierarchy +
coverage tier — seeded-failure test per facet (each facet must be
able to fail). G-P4 (post-marker): one REAL kala_ahead_get call files
a prospective row keyed to a field window_id (live row shown);
unified narrative sample pasted; agreement facet live in explain.
Then SESSION-DONE-γ; α adopts. Your lanes are TS/serving only; any
schema need = migration claimed in coordination §2; you never touch
python writers, field code, or gochara engine code (β's).
