# PG1 LANE Q-1 — Reading Quality vs the Acharya Bar

- **Lane:** Q-1 (reading quality — the only lane judging whether the readings the instrument produces are any good)
- **Branch:** pg1/wave
- **Mode:** READ-ONLY (mcp__postgres__query, SELECT-only)
- **Findings emitted:** 12 (1 coverage-critical + 10 per-reading verdicts + 1 cross-cutting)
- **Standard judged against:** CLAUDE.md §J — an independent senior Jyotish acharya reviewing the corpus should reach one of "this is my own level", "this is above my own level", or "this reveals things I wouldn't have seen on first pass." Nothing less.
- **Pre-committed rule honoured:** findings reported AS-IS; no softening of the unfavorable verdict, no lighter scrutiny for either sign. Symmetric obligation (PC-7 AND PC-8): where a finding overclaimed, it is corrected DOWN honestly; where a real defect survives correction, it is left standing.
- **VERIFICATION HISTORY:** attempt 1 REJECTED by the Phase-1 Opus verifier — PG1-Q1-0002/0004/0006 asserted "same chart, contradictory verdicts" that were in fact rows from two DIFFERENT charts (Abhisek 482012f1 vs Abhinandan 1c826d5a) compared without a chart_id filter. Attempt 2 (this revision) re-ran every disputed query scoped explicitly to chart_id='482012f1-710e-4a25-994a-93821f5871aa' and corrected those three findings. The correction is real and material — see the revised headline below.

---

## THE HEADLINE QUALITY VERDICT (full, unsoftened — this is THE wave's reading-quality verdict)

**The instrument does not, today, produce a single acharya-grade reading — because it does not, today, produce a served reading at all, and every persisted interpretive artifact that stands in for one describes the instrument's own machinery instead of reading the chart.**

I was charged to sample 10 real past assistant readings and judge each against the §J bar. **They do not exist.** The entire conversation store is empty: `conversation_messages = 0`, `conversations = 0`, `conversation_branches = 0`, `conversation_message_embeddings = 0`, `project_conversations = 0`. The LLM synthesis log that would have captured any generated reading is empty: `llm_call_log = 0`. So are `tool_execution_log`, `context_assembly_item_log`, and `query_plan_log`. No user-facing reading has ever left this instrument and been persisted. That fact alone is the most important quality finding in this lane, and it is recorded as PG1-Q1-0001 (critical): **the claim that this instrument reads at or above an acharya's level is, as of this audit, entirely unvalidated by any live output.** The Pariprashna conversational surface is target-architecture (v0.5), not a running, reading-producing system.

Faithful to the spirit of the charge — judge the readings the instrument actually produces — I judged the ten best available PROXIES: the deterministic interpretive artifacts the build pipeline DOES persist, spanning the domains asked for (career, health, marriage, wealth, timing, remedies): the L5 `mimamsa_insight_units` verdict objects, `mimamsa_predictions`, the L2 `bodha_discoveries` (whose very columns — `surface_reading`, `depth_reading`, `why_an_acharya_misses_it` — are built to embody the §J "reveals things I wouldn't have seen" clause), and `bodha_rm_remedy_prescriptions`.

**Verdict on all ten, rendered plainly, one at a time (findings 1, 3, 5 corrected in attempt 2 — see the boxed correction after the list):**

1. **Career verdict** — falls short (CORRECTED, milder than attempt 1). Scoped to Abhisek's chart 482012f1, `verdict_career_advancement` returns EXACTLY ONE row: "Career Advancement: conditional (grade 4.0/10). Conditional — context-dependent." The "promised (8.2/10)" row I originally paired with it is a DIFFERENT chart (Abhinandan 1c826d5a). There is **no intra-chart contradiction** — my attempt-1 "it has enumerated, not judged" charge is **withdrawn**. What survives: the single verdict is generic slot-filled template prose naming no graha/bhava/dasha/yoga (grade/label/band are internally consistent here — no incoherence). A real but milder §J shortfall, largely subsumed by the systemic finding (12).
2. **Health verdict** — falls short (unchanged; not disputed). "Chronic Illness Onset: denied (grade 1.2/10). Mixed or insufficient evidence." — the same canned tail attached to surgery, acute illness and chronic onset alike. "denied" and "insufficient evidence" are contradictory; no disease karaka, house, or maraka window is named. (Re-confirmed intra-chart on re-query by the verifier.)
3. **Marriage verdict** — falls short, and inverts §13.9 (CORRECTED, substance intact). Scoped to Abhisek 482012f1, `verdict_marriage` returns EXACTLY ONE row: "Marriage: denied (grade 1.6/10). Mixed or insufficient evidence." My attempt-1 "paired duplicate at grade 1.8" was a DIFFERENT chart (Abhinandan) — that sub-claim is **withdrawn**. The core survives fully, entirely intra-chart: (a) internal incoherence — definite "denied" co-occurs with self-cancelling "Mixed or insufficient evidence" in one string; (b) no 7th house/Venus/Darakaraka/Navamsa/dasha; (c) a bare frightening negative on the most anxiously-asked question — the §13.9 emotional-register failure, inverted.
4. **Grade/label/prose incoherence** — falls short (unchanged; not disputed). "Education Milestone: denied (grade 5.0/10). Conditional — context-dependent." The verdict word (maximally negative "denied"), the number (neutral 5.0/10), and the prose ("Conditional") contradict one another inside a single string. Re-confirmed intra-chart on re-query.
5. **Wealth verdict** — falls short (CORRECTED, narrower). Scoped to Abhisek 482012f1: "Major Financial Gain: conditional (2.3/10)" AND "Major Financial Loss: conditional (2.3/10)" — one row each. My attempt-1 "same lens resolves to opposite signs — conditional (2.3) AND denied (1.5)" was a cross-chart artifact (the "denied 1.5" rows are Abhinandan's); that framing is **withdrawn**. What survives, and is **independently confirmed by the verifier**: gain and loss — opposite phenomena — carry the IDENTICAL grade (2.3=2.3) and IDENTICAL band [0.23,0.43), a tell the grade is a shared subsystem constant, not a lens-specific dhana/vyaya derivation.
6. **Top-ranked discovery** — falls short of "reveals things I wouldn't have seen" (unchanged; not disputed). The native's highest-ranked discovery is: depth_reading "Stands -5.9σ from ga_sade_sati baseline (mean=0.591)"; why_an_acharya_misses_it "Statistically extreme within ga_sade_sati subsystem (5.9σ)…". Circular — an acharya "misses it" because the instrument's own z-score is large. No graha, bhava, dasha, or meaning. Stored 5+ times verbatim.
7. **Latent insight** — falls short (unchanged; not disputed). surface "Signal graha_kp_lords:prana_lord with low visibility (salience 0.272)", depth "Structurally consequential: consequence_score=1.000, non_obviousness=0.903". It names an internal DB key and three of its own scores, then asserts consequence without a single jyotish claim. Data dumped, not synthesized.
8. **Distributional hypothesis prose** — falls short (unchanged; not disputed). "Pattern lord_aspects_lord_per_varga:aspects_VEN is a ga_structural outlier; predicts distinctly unusual outcomes in character." Leaks raw column names ("constituent_facts_jsonb_atomic", "aggregate_D60") as findings and predicts "distinctly unusual outcomes" without ever saying what the outcome is. One row even mismatches its own varga (D108 vs d10 — a data-integrity bug).
9. **Career-timing prediction** — falls short (unchanged; not disputed). The outcome_claim is an untranslated machine slug, "elevated career career_discovery_event", repeated verbatim across 40+ rows with one identical window and a null base_rate. Not a sentence, not calibrated, not discriminatingly time-indexed.
10. **Remedy register** — falls short AND is a live §13.8 violation (unchanged; not disputed). Every remedy is a second-person command: "Recite the Mercury beej mantra… 108 times daily on Wednesday, facing east", "Donate black sesame seeds… Feed crows", "Chant the Shani Ashtottara…". This is the "you should do X" register §13.8 names as the exploitative line, not the attributive "the tradition prescribes X." The citation is a synthetic slug ("G27 remedy mercury_matrix_mantra for Mercury"), yet it is stamped classical_strength_rating 0.90. The mantra CONTENT is correct — the only favorable note in the sample — but correct content does not rescue a forbidden register.

> **ATTEMPT-2 CORRECTION (honest, per PC-7/PC-8 symmetry).** Three of the twelve findings (career/marriage/wealth — PG1-Q1-0002/0004/0006) originally asserted that the instrument emits SELF-CONTRADICTORY verdicts for one chart. That was **wrong**: those rows came from two different charts (Abhisek 482012f1 vs Abhinandan 1c826d5a) compared without a chart_id filter. Scoped correctly, **each chart carries exactly one verdict row per lens** — delete-then-insert idempotency (§N.3) is functioning; the instrument does NOT contradict itself within a chart. The "has not judged, it has enumerated" drama is retracted. The corrected picture is genuinely **less damning on the coherence axis**: the readings are not self-contradictory. But it is **equally damning on the axis that actually matters for §J** — the surviving intra-chart defects are real: (a) the single verdicts are generic slot-filled templates with no chart-specific grounding (career, and every other lens); (b) "denied" co-occurring with "Mixed or insufficient evidence" is internal incoherence within a single string (marriage, health, confirmed); (c) gain and loss carry an identical grade + band (wealth, verifier-confirmed); (d) a bare "denied" on marriage inverts §13.9. None of the OTHER nine findings (1, 3–4, 6–10, plus the cross-cutting 12) depended on the cross-chart conflation, and all were re-confirmed or left untouched.

**Ten of ten still fall short of §J — but the honest reason is NOT "the instrument contradicts itself." It is that every single-chart verdict is generic templated prose that describes the machinery instead of reading the chart, and the discovery layer built specifically to "reveal what an acharya wouldn't see" produces z-scores and column names instead. Zero reach "my own level," zero reach "above," zero "reveal things an acharya wouldn't have seen."**

**The single systemic diagnosis (PG1-Q1-0012, critical) — UNAFFECTED by the attempt-2 correction:** across every persisted interpretive layer, the instrument describes its own machinery — grades, z-scores, salience, consequence scores, embedding distance ("Semantic meaning-vector far from chart centroid" is the ENTIRE reasoning chain of a top discovery), internal signal keys — in place of reading the chart. Not one sampled row states an astrological mechanism in the native's own terms: which graha, in which bhava, under which dasha, producing which lived outcome, and why. **The pipeline computes structure impeccably and stops exactly one layer short of the reading.** The scaffolding is real and even admirable (confidence bands, falsifiers, the "why an acharya misses it" concept, honest uncertainty language) — but the synthesis that turns scaffolding into a reading has not been written, or if written has never run and persisted anything. This diagnosis never rested on any cross-chart comparison — it rests on the register of each single chart's own rows, which chart-scoped re-query (attempt 2) confirms verbatim — so the verifier's rejection of the three contradiction-framed findings leaves the systemic verdict fully intact.

**The one fair mitigation, stated so the verdict cannot be accused of ignoring it:** L5 is sealed in explicit STRUCTURAL mode (CLAUDE.md §E) — the missing empirical CALIBRATION numbers are by design, not neglect. That is true and it is honestly disclosed. But it does not touch this verdict, because the failure here is not missing calibration — it is missing astrological PROSE. A verdict can be uncalibrated and still read like an acharya wrote it; none of these do. The gap is in the synthesis layer, not the calibration loop.

**Bottom line for the wave report (attempt-2, honest net position):** Every other lane audited whether the architecture is sound. This lane audited whether the readings are good, and the answer is that there are no readings to be good yet — and the deterministic artifacts standing in for them fail the §J bar comprehensively and systemically. The attempt-1 rejection was correct and has been honoured: three findings (career/marriage/wealth) overclaimed a "self-contradictory verdict" pattern that dissolved once queries were scoped to Abhisek's chart 482012f1 alone — the instrument stores exactly one verdict per lens per chart and does NOT contradict itself; those three findings are corrected down and, for wealth, refounded on the verifier-confirmed identical-gain/loss-grade defect. The correction makes the picture **less lurid** (no self-contradiction) but **not materially less damning for §J**: the surviving, chart-scoped, independently re-confirmed defects — generic slot-filled verdict templates with zero graha/bhava/dasha grounding, "denied" co-occurring with "insufficient evidence," gain and loss sharing one grade, and the discovery/hypothesis/prediction/remedy layers all describing internal machinery instead of the chart — are enough, on their own, to place every one of the ten sampled proxies below the acharya bar. The §J claim must be treated as ASPIRATIONAL and UNPROVEN until the serve-time synthesis path runs end-to-end, persists real readings, and those readings are put in front of the acharya bar. Nothing short of that can substantiate the mission's central promise.

---

## Method / coverage note

- Conversation-table discovery ran exactly as the charge specified (`pg_tables ILIKE '%conversation%'/'%message%'`); all conversation and message-body tables returned count 0. Confirmed the same for `llm_call_log`, `tool_execution_log`, `context_assembly_item_log`, `query_plan_log`.
- Proxy corpus judged: `mimamsa_insight_units` (46 domain-tagged verdict rows — spanning BOTH live charts, Abhisek 482012f1 and Abhinandan 1c826d5a), `mimamsa_predictions` (384), `bodha_discoveries` (1,275 for the native chart `482012f1-…`, 3 real classes: distributional_anomaly / embedding_outlier / latent_insight), `bodha_rm_remedy_prescriptions` (270).
- Every finding carries a verbatim DB quote (no unverifiable escape hatch used).
- Coverage shortfall on the literal "10 conversational readings" is itself recorded as the critical finding PG1-Q1-0001, with the empty-table evidence.
- **Attempt-2 method fix (verifier-driven):** attempt 1 queried `mimamsa_insight_units` for the career/marriage/wealth lenses WITHOUT a `chart_id` filter, so rows from Abhisek (482012f1) and Abhinandan (1c826d5a) were compared as if they were one chart's self-contradiction. Attempt 2 re-ran every disputed query with `WHERE chart_id='482012f1-710e-4a25-994a-93821f5871aa'` and confirmed `n_rows=1` per (insight_id × question_lens) — i.e. exactly one verdict per lens for Abhisek; no intra-chart duplicates; idempotency (§N.3 delete-then-insert per chart×natural-key) functioning. The contradiction framing in PG1-Q1-0002/0004/0006 was an artifact of the missing filter and has been corrected. The surviving intra-chart defects (generic templates; "denied"+"insufficient evidence" incoherence; identical gain/loss grade) were re-verified scoped to 482012f1.
