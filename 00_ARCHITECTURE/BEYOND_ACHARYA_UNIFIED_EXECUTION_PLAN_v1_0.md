---
artifact: BEYOND_ACHARYA_UNIFIED_EXECUTION_PLAN
canonical_id: BEYOND_ACHARYA_UNIFIED_EXECUTION_PLAN
version: 1.0
status: CURRENT — §V verification completed 2026-07-03 (two Claude-agent repo sweeps); five corrections
  U1–U5 folded into §2
created: 2026-07-03
author: Cowork (strategic workstream, Claude Fable 5) — for native Abhisek Mohanty
ratified_strategy: >
  Native decision 2026-07-03: "one shot, rifle zeroed." ONE continuous execution run — no experience-pause
  between retrieval modernization and the Beyond-Acharya data waves. The retrieval phases (P0–P2) are the
  opening phases OF the program: serving truth + wiring are BA's own verification prerequisites, and P2's
  query-time ranking is the CALIBRATION BENCH where the class priors are tuned cheaply (seconds/iteration)
  before the one-shot L2 regeneration freezes them (hours/iteration). The Mahā-Brief builds ONCE, in P4,
  on regenerated data.
authorities: >
  SEQUENCING + GATES: this document. ASSET DELTA + embedding mechanics + traps: BA_MASTER v2.1 (§3/§4/§7).
  RETRIEVAL substance (envelope, tool census, wiring groups, query-time ranking, Mahā-Brief, eval):
  RETRIEVAL_MODERNIZATION_MASTER_PLAN v1.0 (§3–§7). L5 mechanics: MIMAMSA_V2 v1.0. Judgment seeds:
  BEYOND_ACHARYA_W1_JUDGMENT_SEED_PACKAGE v1.0 (Cowork-authored under the 2026-07-03 full delegation —
  native veto only, no sitting required; provisional values tuned live in P2).
supersedes_in_part: BA_MASTER v2.1 §6 wave table and RM plan §8 wave table — replaced by §2 below.
  W1-as-sitting is DISSOLVED: judgment inputs are Cowork-authored + P2-tuned + native-vetoable at any time.
changelog:
  - v1.0 (2026-07-03): first version; pending Claude-agent repo verification (§V).
---

# BEYOND-ACHARYA — UNIFIED EXECUTION PLAN v1.0 (one continuous run)

## §1 — THE SHAPE OF THE RUN

```
P-1 SYNC FREEZE (clean slate: branches/stashes/worktrees/untracked resolved; localhost ≡ GitHub ≡ prod)
  │  brief: CLAUDECODE_BRIEF_BA_PM1_SYNC_FREEZE_v1_0.md (repo root, READY) — native improvement 2026-07-03
  ▼
P0 serving truth ─→ P1 wiring+naming ─→ P2 QUERY-TIME RANKING (the zeroing bench)
                                              │  gate: G10-QT passes; priors tuned + frozen
                                              ▼
                    P3 L0 seeds + L1 EXT + L2 SINGLE REGENERATION (the one shot)
                                              │  gate: G10-stored + DEFECT-001 + degeneracy
                                              ▼
                    P4 verdict + serving + MAHĀ-BRIEF + eval ─→ P5 R-pipeline ─→ P6 learning engine
                                              (P7 classical+portal parallel to P5/P6; P8 research later)
```

Strictly serial: P0→P1→P2→P3→P4. P5 after P3 (parallelizable with P4 tail). P6 after P5. P7 alongside
P5/P6. P8 gated on corpus growth. Execution mechanics: Cowork authors each phase's CLAUDECODE brief(s);
Claude Code in Antigravity executes autonomously per AUTONOMOUS_MODE; every AC tagged
`[verify-against: prod|db|ci]`; wave-complete prod gate re-checks headline numbers (the V1.3 scar).

## §2 — PHASES, BRIEFS, GATES

| Phase | Content (substance authority) | Brief(s) | Exit gate |
|---|---|---|---|
| **P0 — Serving truth + caps** | Absorb/finish in-flight audit swarm W3/W4 (bounding F-026/F-021, L4 schema F-005/F-014, ephemeris/kala sidecar F-012/F-030, panchanga_daily); assess_* caps (6.2 MB kill); cache contract; error envelope; CURRENT_STATE governance sync (RM §8-RM0, BA C7/C8) | `BA_P0_SERVING_TRUTH_AND_CAPS` (extends authored BA_W0 brief) | All ~53 tools structured-respond ≤ budget on prod; kala/L4/panchanga serving; cache hit on repeat; CURRENT_STATE references the program. |
| **P1 — Tool estate** | Naming Phase-1 aliases + remedy dedup + wiring Groups 1–3 (≈17 new primaries: ganita_strength_get, ganita_structural_get [facet], ganita_condition/sade_sati/tajaka/nakshatra/yogas/transit_anchors_get, phala_rectification_get, ref_* reference tools, mimamsa_insight_get, bodha_discoveries_get, kala_life_arc_get) + envelope v1 on new tools (RM §3, §5) | `BA_P1_WIRING_AND_NAMING` | Uncovered assets 31 → ≤6 (deferred-documented); every wired tool passes C/A/O smoke on 482012f1 + 1c826d5a. |
| **P2 — THE ZEROING BENCH** | Query-time 4-dim composite ranking over existing data (RM §4): topic-relevance (affinity tables) × intrinsic strength (real shadbala) × structural role (yoga_fires + CGM centrality) × temporal activation (direct from ga_dashas, kala bypass); provisional seeds EXTRACTED from the W1 seed package; envelope retrofit on existing 53; synth_* apex repair (structured verdict, bounded); **then the tuning loop: iterate class-priors/affinities against the G10 question set until ranking behaves acharya-grade** — seconds per iteration, Cowork drives, native vetoes | `BA_P2_QUERYTIME_RANKING` then `BA_P2T_PRIOR_TUNING` (Cowork-led loop, config-only) | **G10-QT:** career top-10 on 482012f1 = 10th-lord/kāraka/yoga structures, zero sub-varga atoms — at query time; tuned prior set FROZEN as `prior_version=1.0` + written into the seed package; ranking_basis on every payload. |
| **P3 — THE ONE SHOT** | L0 seeds (`bg_class_priors` w/ P2-tuned values + `bg_ghatana` + `bg_formula_constants` incl. per-graha combustion) + domain-normalization + `charts.chart_type` migrations + L1 EXTs (bhava arudhas/UL, Chara dasha, per-varga shadbala, graha yuddha/avasthas) + **L2 single regeneration** (salience v2 stored: cap-lift, formula unification, S-A…S-D, A-B/A-C, aggregation, signature_tier recut, cross-ayanamsha fill, DEFECT-001 re-resolve; bo_bimba/karanajala typed edges + contradiction attribution; bo_pratijna; bo_sangati triangulation) (BA §3-L0/L1/L2) | `BA_P3A_L0_SEEDS_AND_L1_EXT`, `BA_P3B_L2_REGENERATION` | G10-stored (same test, stored salience); constituent_facts resolve ≥ target; signature_tier fires chart_defining; salience_pctl populated; degeneracy + trap-1 + FORENSIC 7/7 + contamination gates; two-chart rule. RM4 bridge AC: **zero consumer-visible interface change** (query-time intrinsic inputs swap to stored columns silently). |
| **P4 — Verdict + MAHĀ-BRIEF** | Verdict object (structured; mi_darshana as home) + triangulation/ensemble/activation-aware serving + query-time terms (S-E/A-A/A-D) + attention budget 70/20/10 + complement pass (`tail_divergence`) + **Mahā-Brief** (`synth_chart_brief_get`, 38-topic, depth-parameterized, built on regenerated data) + eval harness live with Cowork-authored golden answers (BA §5, RM §5–§7) | `BA_P4_VERDICT_MAHABRIEF_EVAL` | External 1M LLM over MCP produces cited, reconciled, acharya-grade career reading (blind rubric ≥13/15 on the golden set); Mahā-Brief(complete) ≤1M tokens with all 38 topics GREEN; tail_divergence non-trivial on ≥1 domain. |
| **P5 — R-pipeline (prophecy)** | ka_yojaka 7-system activation + ka_avadhi (period dossiers, Q1) + ka_taranga (waveform) + ph_nimitta v2 (anchor posterior = base_rate × lifts, structured falsifiers) + ph_muhurta activity-aware + prashna chart-type path + W4A conflation fixes (ka_sangam confidence, ka_vighnakara combustion) (BA §3-L3/L4) | `BA_P5A_KALA_ACTIVATION`, `BA_P5B_PHALA_V2` | Q1/Q3/Q4 GREEN on prod; anchors span full probability range w/ LEL-decidable falsifiers + lift_vectors; one combustion truth; confidence ≠ convergence. |
| **P6 — Learning engine** | MIMAMSA_V2 R1+R2: blind retrodiction over LEL + control windows + ablation + null-anchored scoring + shrinkage + analytic attribution + first calibration snapshot (two-key) + 3-site weight unification + constants sensitivity harness + tail-only retrodiction (MIMAMSA_V2 §3–§6; BA §3-L5) | `BA_P6_MIMAMSA_ENGINE` | First honest skill table (45 train/12 held-out); ≥1 family beats null OR the null finding published; snapshot moves served weights reversibly; constants ranked by sensitivity. |
| **P7 — Classical + portal (parallel)** | W6 classical completions (Nadi rules, AV-transit gates, remaining avasthas) + W7 portal loops (ask-cards, attestation, prashna follow-ups, co-sign UI) | `BA_P7A_CLASSICAL`, `BA_P7B_PORTAL_LOOPS` | New fact_categories arrive RANKED; closed windows → adjudicated ≥80% in 7 days. |
| **P8 — Research organs (post-run)** | Synastry sy_* (chart_type), chart embeddings + case retrieval, motif mining, rule induction w/ L5→L0 graduation | `BA_P8_*` (split at brief time) | Gated on multi-chart corpus. |

## §2.1 — THREE BINDING COMMITMENTS (native review 2026-07-03)

1. **PERFORMANCE IS A GATE.** The composite ranking is deterministic → computed once per
   (chart × domain × prior_version) and CACHED; the only time-varying input (dasha activation) changes at
   KNOWN dasha-boundary dates, so cache invalidation is scheduled, not guessed. P3 bakes intrinsic
   dimensions into stored columns. Latency budgets added: P0 gate gains "p95 per-tool response ≤ 3s on
   prod (bounded payloads, warm cache)"; P2 gate gains "ranking adds ≤ 500ms p95 over the P0 baseline on
   cache-miss, ≤ 50ms on cache-hit." Current slowness is payload/cold-start/cache debt (P-1/P0 scope) —
   response time must IMPROVE before ranking lands.
2. **SEED SCOPE:** the judgment seed package (class priors, varga weights, affinity tables, event/activity
   ontologies, constants) is GLOBAL — chart-agnostic śāstra, L0 reference, one table for all charts.
   Chart-SPECIFIC are: the evidence the weights multiply (per-chart strength/dignity/activation),
   signature_tier outcomes, and the learned surfaces (S3 dasha-fitness, S4 manifestation grammar, L5
   calibration overlays). Judgment global; evidence and learned expression per-chart.
3. **BACKWARDS DESIGN — the eval is the definition of done.** The golden-question harness becomes a
   STANDING REGRESSION GATE from P2 onward: every phase close re-runs the golden set (blind rubric) and
   must improve — never degrade — the scored quality of the eventual LLM's responses. The golden set
   expands from 10 questions to full query-class coverage (Q1–Q9) by P4. Phase gates are response-quality
   statements; engineering criteria are means.

## §3 — WHAT CHANGED vs THE TWO PREDECESSOR TABLES (the merge deltas)

1. **W1-as-sitting DISSOLVED** → full delegation (native 2026-07-03): seeds Cowork-authored; P2 tunes them
   empirically; native veto standing but nothing blocks on a calendar event. Doctrine/quarantine/attestation
   sign-offs: recorded as ratified-by-default with veto window through P3 brief authoring.
2. **RM3 dropped as standalone** → Mahā-Brief builds once in P4 on regenerated data (no double build).
3. **P2 gains the explicit TUNING LOOP** (`BA_P2T`) — the strategic reason the merge is safe: priors are
   wrong until tuned; tuning at query time costs seconds; P3 freezes tuned values.
4. **RM4 becomes P3/P5/P6 bridge ACs** (interface stability) instead of a wave.
5. Continuous run = P0→P6 with no pause; the "experience the product" value arrives DURING P2 (tuning IS
   using it) and at P4 (Mahā-Brief), not as a separate phase.

## §V — VERIFICATION LEDGER (completed 2026-07-03; two Claude-agent repo sweeps)

| # | Claim | Verdict |
|---|---|---|
| V1 | Next-free migration = 385 | **CONFIRMED** (platform=364 max, supabase=384 max) |
| V2 | Brief states | **CORRECTED** → U1/U2: audit-fix swarm brief v1.2 reports W1–W4 ALL CLOSED; CURRENT_STATE v6.13 concurs (open items are Wave-5 class: F-020/F-024/F-009/F-022/DEFECT-001). `BA_W0` brief NOT at repo root (lives only in the other conversation's outputs). **W1 seed package NOT committed** — referenced but absent from repo. Root CLAUDECODE_BRIEF.md status COMPLETE (no scope conflict). |
| V3 | New asset ids collision-free | **CONFIRMED** (all 9 clear) |
| V4 | charts.chart_type absent | **CONFIRMED** (only `role`; `chart_type` exists only on school_analysis_runs) |
| V5 | Tool census ≈53 + Group-1 handlers | **CORRECTED** → U3: census = **46** registered MCP tools. All 7 Group-1 handler files EXIST and are registered in the retrieval registry — the wiring gap is (at least partly) MCP-exposure, not missing handlers. P1 brief must open with a per-tool wiring-state audit (registry-registered vs MCP-exposed vs neither). |
| V6 | Query-time ranking feasibility | **FEASIBLE with one fallback** → U4: ga_strength categories confirmed (graha_shadbala_total + 6 components, ishta/kashta, 4×vimsopaka, bhava_bala, full ashtakavarga family); ga_structural yoga_fires/dosha_fires + functional classes confirmed; chart_dashas gives current MD/AD by simple SELECT (system_id, level_n, lord_graha, start/end). **bo_bimba pagerank_score is NULL-stubbed** (bo_karanajala backfills) — P2 structural-role dimension must COALESCE to yoga-membership + signature class when pagerank IS NULL. |
| V7 | assess_*/bounding code state | **CONFIRMED + REFRAMED**: register_d8_assess_domain.ts assembles 3 handler calls with NO size cap (P0 caps still needed on the assess_* path); get_domain_reading DOES honor max_lenses/max_signals in code → the 17.3 MB/param-ignored probes of 2026-07-02 were a STALE PROD REVISION, not code — P0 is deploy-truth first. |
| V8 | Mahā-Brief input tables | **CORRECTED** → U5: UCD home = `bodha_chart_gestalt` (+`vw_chart_digest` view), not a samvada table; `bodha_discoveries` exists (mig 325); **`mimamsa_insight_units` does NOT exist in any migration** — mi_darshana's target table was never migrated. Group-3 wiring must first ship the table migration (or repoint mi_darshana), else `mimamsa_insight_get` has nothing to read. |
| V9 | CURRENT_STATE position | **CORRECTED** (same as V2): v6.13; W3/W4 complete; P0 = deploy-truth verification + assess_* caps + cache + Wave-5-item honesty flags, NOT re-fixing W3/W4. |

### §V.1 — Corrections folded (U1–U5)

- **U1 (P0 reframed):** P0 = (a) DEPLOY-TRUTH: verify Cloud Run revision serves the closed W1–W4 fixes
  (the 2026-07-02 probes hit a stale revision); (b) assess_* size caps (the one confirmed un-capped path);
  (c) cache contract; (d) CURRENT_STATE sync. Smaller than drafted — good news.
- **U2 (seed package is Cowork's immediate deliverable):** `BEYOND_ACHARYA_W1_JUDGMENT_SEED_PACKAGE_v1_0.md`
  is NOT in the repo. Cowork authors + commits it BEFORE P2 (it feeds P2's provisional seeds). Under the
  2026-07-03 delegation this blocks on no one.
- **U3 (P1 reduced + re-scoped):** census 46 not 53; Group-1 handlers exist in the registry — P1 opens
  with a wiring-state audit, then exposes/wires the gap set; effort likely smaller than planned.
- **U4 (P2 design note):** structural-role dimension uses COALESCE(pagerank, f(yoga_membership,
  signature_class)) until bo_karanajala backfill is verified populated.
- **U5 (P1 Group-3 prerequisite):** ship `mimamsa_insight_units` table migration (number per V1: 385+)
  or repoint mi_darshana before wiring `mimamsa_insight_get`; Mahā-Brief front matter reads
  `bodha_chart_gestalt`/`vw_chart_digest`.

*End of BEYOND_ACHARYA_UNIFIED_EXECUTION_PLAN v1.0 (VERIFIED). Trigger sequence: (1) Cowork authors +
commits the W1 judgment seed package (U2) and `BA_P0_SERVING_TRUTH_AND_CAPS`; (2) open the implementation
conversation with the governing trio (this doc + BA_MASTER v2.1 + RETRIEVAL_MODERNIZATION v1.0) and execute
P0; (3) P1 → P2 → tuning loop → the one shot.*
