-- 982_nirmana_l2_bo_cgm_paths_output_digest_spec.sql
--
-- NIRMANA v2.5 -- L2 (Bodha). Transaction ownership belongs to
-- platform/scripts/migrate.ts.
--
-- Continuation of RESOLUTION_L1 v5 priority 3 (chain pre-clear, widened
-- fleet-wide audit). Re-confirmed this cycle that `bo_karanajala`'s spec
-- is live on origin/main (migration 976, PR #2495 MERGED 2026-09-09T02:35Z)
-- -- the transitive-contamination class it unblocked (bo_cgm_motifs,
-- bo_cgm_paths, bo_samskara [landed 978/979], bo_sangati,
-- bo_yantra_mechanism, bo_chart_gestalt, bo_anveshana) remains screenable.
-- This migration picks `bo_cgm_paths` (CGM Dispositor Chain Path Analysis,
-- pipeline/orchestrator/writers/bo_cgm_paths.py, 387 lines, LIGHT writer,
-- no services/ package) -- the smallest of the six remaining. Read in full.
--
-- Co-writer investigation (FOUR checks): tree-wide grep for
-- `bodha_cgm_paths` INSERT/DELETE/UPDATE across pipeline/orchestrator/
-- writers/*.py, services/*.py, and bodha_writers/_idempotency.py:
--   (1)/(2) The sole live INSERT site is `bo_cgm_paths.py`'s own
--       `_PATH_INSERT` (ON CONFLICT (chart_id, ayanamsha_id, build_id,
--       snapshot_type, path_type, from_node_id, to_node_id) DO NOTHING),
--       executed from `BoCgmPathsWriter.run()`. `bo_yantra_mechanism.py`
--       contains only a prose comment referencing bo_cgm_paths ("already
--       served by bo_cgm_paths, not a circuit") -- no read or write of the
--       table, confirmed by grep. `bodha_writers/_idempotency.py` defines
--       `replace_prior_cgm_paths()` (a DELETE-only helper) but it is NEVER
--       imported or called anywhere in the tree -- dead code, not a live
--       co-writer.
--   (3)/(4) `BoCgmPathsWriter` (`@register('bo_cgm_paths')`) is the
--       confirmed sole live BUILD-TIME writer of `bodha_cgm_paths`.
--
-- Non-determinism check: `_write_aya()` fetches `bodha_cgm_edges` (all
-- dispositor-class edges for the chart/ayanamsha) with NO explicit ORDER
-- BY, and `_build_dispositor_chains()` reduces same-`from_node_id` edge
-- candidates via `dispos_edges.sort(key=computed_strength,
-- reverse=True)[0]` -- a max-pick reduction, the exact defect class that
-- sank several `ka_*` writers this campaign UNLESS live data cannot
-- produce a tie. Live-verified directly: `GROUP BY chart_id,
-- ayanamsha_id, from_node_id HAVING COUNT(*) > 1` over all dispositor-
-- class edges in `bodha_cgm_edges` (all 3 charts, 40-45 edges each)
-- returns ZERO groups -- every graha node has exactly one dispositor edge
-- (matches classical astrology: a sign has exactly one ruling planet), so
-- the max-pick sort is always over a single-element list and can never
-- resolve a tie non-deterministically. `node_by_id` is keyed by
-- `node_id`, `bodha_cgm_nodes`'s own PRIMARY KEY -- no reduction risk.
--
-- Natural key: `(chart_id, ayanamsha_id, snapshot_type, path_type,
-- from_node_id, to_node_id)` -- the table's own live UNIQUE constraint
-- minus `build_id` (excluded per this campaign's standard build_id
-- exclusion class; the writer's own idempotent DELETE FROM bodha_cgm_paths
-- WHERE chart_id = %s removes ALL prior build_ids for the chart before
-- inserting, so at most one build_id's data exists per chart at any time
-- -- same precedent as every prior spec that drops build_id from
-- key_columns). Live-verified 0 duplicate-key groups and 0 NULLs across
-- all 135 live rows (45 rows x 3 charts: 482012f1 canonical, 1c826d5a,
-- cb73cd3d).
--
-- Surrogate/non-deterministic-across-rebuilds columns excluded from the
-- digest value columns (same exclusion class as every prior spec in this
-- campaign): bodha_cgm_paths.path_id (surrogate PK, uuid4-generated by
-- the writer), .build_id (build-run identifier), and .computed_at (bound
-- to a Python `datetime.now(timezone.utc)` value at write time -- the
-- standard wall-clock write-time-artifact exclusion, notwithstanding it
-- being parameterized rather than a SQL NOW() literal).
--
-- The canonical chart 482012f1 already HAS 45 live rows in
-- bodha_cgm_paths today (bo_cgm_paths has run to completion for it) --
-- rehearsal ran directly against it, no substitute-chart workaround
-- needed.
--
-- spec_sha256 computed and independently re-verified via the REAL server
-- functions, never hand-reimplemented:
--   cd platform/python-sidecar && python3 -c "
--   from pipeline.orchestrator.provenance import canonical_digest
--   from pipeline.orchestrator.output_digest import _validate_spec
--   spec = {...}  # exact object below
--   print(canonical_digest(spec))                                # == the literal below
--   print(_validate_spec('bo_cgm_paths', spec, sha).asset_id)     # passes the server's own validator
--   "
--
-- Rehearsed end-to-end against live prod inside a ROLLED-BACK transaction
-- (psycopg3, autocommit=False, row_factory=dict_row), calling the REAL
-- `compute_output_digest(cur, asset_id='bo_cgm_paths')` over all 45
-- canonical-chart rows -- got back a clean 65-hex digest
-- (d5a9b403d3ed72e5c217db7f59f4f15c5b83a8a33e0610b3d939199c540dd279, no
-- exception, key-preflight passed) -> rolled back -> re-queried
-- `asset_output_digest_specs` from a FRESH connection afterward and
-- confirmed 0 rows for bo_cgm_paths, i.e. genuinely rolled back, nothing
-- persisted by the rehearsal.
--
-- Numbering note: highest APPLIED migration in `_migrations_applied` at
-- cycle start is 979 (bo_samskara, this lane's own prior cycle). This
-- lane's own #2496 (ka_sangam) is open with its migration files renumbered
-- on-branch to 980/981 (its original 976/977 numbering collided with
-- bo_karanajala's 976, which merged to main first -- that PR's own
-- renumbering is #2496's concern, not this migration's; its DB-applied
-- record still reads 976/977 since it ran before the rename). L2's
-- bo_karanajala (976) is already MERGED to origin/main. Checked every
-- other open PR branch fresh this cycle for any 982/983 file -- zero hits
-- (pariprashna/p4-g tops out at 587, gochara3/w61 at 546, both preserve/*
-- at 474). 982/983 confirmed free against `_migrations_applied`, every
-- open PR branch, and origin/main.
--
-- Post-apply verification (SS N.4 -- never trust a silent no-op): expect
-- INSERT 0 1, then
--   SELECT asset_id FROM asset_output_digest_specs
--    WHERE asset_id = 'bo_cgm_paths' AND retired_at IS NULL  -- expect 1 row

INSERT INTO asset_output_digest_specs (asset_id, spec_sha256, spec)
VALUES (
  'bo_cgm_paths',
  '646ee0c6f331f1664a0443247e347266aab00758d5822d71acc196e6e1dfb6dd',
  '{"version":"nirmana-output-digest-spec-v1","components":[{"name":"bodha_cgm_paths","relation":"bodha_cgm_paths","key_columns":["chart_id","ayanamsha_id","snapshot_type","path_type","from_node_id","to_node_id"],"value_columns":["chart_id","ayanamsha_id","snapshot_type","path_type","from_node_id","to_node_id","path_node_ids_array","path_edge_ids_array","path_length","path_strength","is_final_dispositor","convergence_count","centrality_formula_version","path_label_human","verification_pass_status","citation_ref","citation_human"],"where_equals":{"chart_id":"482012f1-710e-4a25-994a-93821f5871aa"}}]}'::jsonb
)
ON CONFLICT (asset_id, spec_sha256) DO NOTHING;
