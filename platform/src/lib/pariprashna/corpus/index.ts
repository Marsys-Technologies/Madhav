/**
 * Paripraśna quality corpus — barrel (lane P2-N, roadmap id G3-F).
 *
 * The versioned fixture set (`fixtures.ts`), the 13-dimension scoring
 * harness (`dimensions/`), and the runner that executes the corpus against
 * a target and emits a versioned report (`runner.ts`).
 */

export * from './types'
export * from './fixtures'
export * from './dimensions'
export * from './report_schema'
export * from './runner'
