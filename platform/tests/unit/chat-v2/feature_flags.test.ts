/**
 * Unit tests — feature flag defaults.
 *
 * CHAT_V2_ENABLED removed at §M.16 (2026-05-18); tests for it retired.
 * CONSUME_UI_V2_ENABLED removed at CLEANUP-1 (2026-05-26); tests for it retired.
 */

import { describe, it } from 'vitest'

describe('feature_flags — retired flags', () => {
  it.todo('CHAT_V2_ENABLED retired at §M.16 — no tests remain')
})
