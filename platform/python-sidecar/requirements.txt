fastapi==0.115.0
uvicorn[standard]==0.30.6
# PyJHora — sole chart computation engine (native decision; pyswisseph retained for ephemeris ops).
# The PyJHora wheel declares no dependencies, so the calculation-path transitive deps are pinned
# explicitly below. PyQt6 / pyqtgraph / img2pdf are GUI-only and deliberately omitted — the adapter
# imports only jhora.panchanga.* + jhora.horoscope.chart.* + jhora.horoscope.dhasa.* (headless).
PyJHora==4.8.6
pyswisseph==2.10.3.2
numpy>=2.1.1
pytz>=2024.1
python-dateutil>=2.9.0
geopy>=2.4.1
geocoder>=1.38.1
reverse_geocode>=1.6.6
timezonefinder>=6.5.2
scipy>=1.13.0
requests>=2.32.0
pydantic==2.9.0
python-dotenv==1.0.1

# RAG stack — added Madhav 17 B.0 per PHASE_B_PLAN §G B.0 Task 6
# Voyage replaced by Vertex AI (text-multilingual-embedding-002) at B.3 migration.
google-cloud-aiplatform>=1.60.0
anthropic>=0.25.0
openai>=1.30.0
tiktoken>=0.7.0
networkx>=3.2
pyyaml>=6.0
sqlalchemy>=2.0
psycopg[binary]>=3.2
psycopg2-binary>=2.9
pgvector>=0.3.0
ragas>=0.2.0
pytest>=8.0
jsonschema>=4.22.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0

