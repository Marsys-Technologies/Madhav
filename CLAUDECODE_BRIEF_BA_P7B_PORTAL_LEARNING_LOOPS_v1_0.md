---
canonical_id: CLAUDECODE_BRIEF_BA_P7B_PORTAL_LEARNING_LOOPS
version: 1.0
status: READY-FOR-EXECUTION — gated on BA-P6 COMPLETE (needs adjudication engine); conductor fills ⟦SLOT⟧s
created: 2026-07-03
author: Cowork (Beyond-Acharya unified program; substance frozen — conductor fills slots only)
program: BEYOND_ACHARYA_UNIFIED_EXECUTION_PLAN_v1_0.md — phase P7B (portal learning loops)
slots: ⟦P6_CLOSE_SHA⟧
common_rules: FROZEN contract · existing design system ONLY (canonical tokens per the design-system
  reference; no new visual language) · latency budgets apply to portal · Loop-D resonance QUARANTINED.
may_touch: ["portal UI (new cards/forms/scheduler surfaces)", "mi_seva/mi_abhilekha wiring (ask-card triggers + resync)", "prashna follow-up scheduler", "snapshot co-sign surface"]
must_not_touch: ["mi_* scoring logic (P6 sealed it)", "design tokens/globals.css", "any predictive-weight path from resonance feedback"]
---

# BRIEF BA-P7B — PORTAL LEARNING LOOPS

**Code anchors:** journal plumbing EXISTS — `mi_seva` (journal write on surface) + `mi_abhilekha`
(resync on answer); this brief adds TRIGGERS + UI. Design reference: brand tokens + `.bt-*` ladder in
`globals.css`; dashboard = reference implementation.

## Step 1 — Ask-cards (Loop C)
Daily scan: anchors/predictions whose window closed UNRESOLVED → one-tap adjudication card in the portal
(happened / partially / didn't / can't say) → mi_abhilekha resync → mi_pramana re-adjudication.

## Step 2 — Attestation + structured LEL intake
Monthly "attest this period as curated" card (enables honest REFUTED per MIMAMSA_V2 §4). LEL entry form:
structured capture (event_class from brahma_event_ontology picklist + magnitude + date + notes) writing
to the LEL markdown via a governed append (the markdown stays source-of-truth) + provenance re-sync.

## Step 3 — Prashna follow-ups (Loop B — the fastest honest loop)
Every Q4 undertaking/election verdict auto-schedules a follow-up ask at its fructification date;
outcomes flow to mi_pramana as prospective evidence.

## Step 4 — Co-sign surface (the native's retro-review seat)
Calibration-snapshot review card: proposed weight changes (before/after, evidence cells, Judgment Ledger
refs) with approve/revoke — the native's retrospective veto over Pratinidhi-co-signed snapshots, exactly
as the charter's dissolution table promises.

## Step 5 — Resonance capture (Loop D — QUARANTINED)
Reading feedback (resonates / doesn't) captured + stored; flows ONLY to manifestation-grammar
presentation emphasis (S4); a static analysis / code-review check proves NO path from resonance tables
to priors/lifts/tradition-weights.

## Anti-goals
No new visual language. No resonance→predictive-weight path (verify structurally, not by intention).
No portal write to L1–L4 tables. The LEL markdown is never bypassed.

## Exit gates
- [ ] closed windows convert to adjudicated outcomes ≥80% within 7 days (measured over the run's own open
      windows where available, else staged test windows) `[verify-against: prod]`
- [ ] attestation + LEL intake round-trip: form → markdown append → provenance resync → visible to
      mi_pramana `[verify-against: prod]`
- [ ] resonance quarantine proven by dependency check `[verify-against: repo]`
- [ ] portal chat + build page visually consistent vs PG baseline; latency within budget
      `[verify-against: prod]`
